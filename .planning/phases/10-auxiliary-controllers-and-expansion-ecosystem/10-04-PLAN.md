---
phase: 10-auxiliary-controllers-and-expansion-ecosystem
plan: 04
type: execute
wave: 3
depends_on:
  - 10-03
files_modified:
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - BUILD.bazel
  - justfile
  - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md
autonomous: true
requirements:
  - IFCE-06
requirements_addressed:
  - IFCE-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-06-14T15-08-30
generated_at: 2026-06-14T15:33:19Z
must_haves:
  truths:
    - "Developer can query and run Phase 10 verifier targets through Bazel."
    - "Developer can run `just phase10-verify` as the quick local IFCE-06 aggregate gate."
    - "Phase 10 validation document records the actual Wave 0 through Wave 3 automated evidence and keeps non-local hardware, simulator, RS485, MMU live-transport, toolchanger, long-running update, and final cutover proof outside local claims."
    - "Bazel/just wiring exposes manifests, Rust sources, and validation docs as queryable inputs without embedding auxiliary firmware bytes, signing keys, credentials, or crash-dump payloads."
  artifacts:
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 10 verifier shell_binary targets"
      contains: "phase10_verify"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Workflow dispatch for Phase 10 verifier targets"
      contains: "phase10_verify)"
    - path: "BUILD.bazel"
      provides: "Root aliases and Phase 10 documentation filegroup"
      contains: "phase10_auxiliary_controller_docs"
    - path: "justfile"
      provides: "Local Phase 10 verification recipe"
      contains: "phase10-verify:"
    - path: ".planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md"
      provides: "Completed Nyquist validation evidence register"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "justfile"
      to: "//tools/bazel:phase10_verify"
      via: "`just phase10-verify` invokes Bazel target"
      pattern: "bazel run //tools/bazel:phase10_verify"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase10_verify.py"
      via: "shell workflow case dispatch"
      pattern: "phase10_verify\\)|python3 tools/bazel/phase10_verify.py --all"
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/manifests/phase10_auxiliary_controllers.json"
      via: "data dependency for hermetic queryability"
      pattern: "phase10_auxiliary_controllers.json"
    - from: ".planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md"
      to: "tools/bazel/phase10_verify.py"
      via: "final automated evidence commands"
      pattern: "python3 tools/bazel/phase10_verify.py --security-only"
---

<objective>
Expose Phase 10 verification through Bazel and `just`, then complete the Nyquist validation register.

Purpose: Make IFCE-06 auxiliary-controller coverage executable from the project-standard developer entrypoints without overstating local proof.
Output: Bazel targets, workflow dispatch, a `just phase10-verify` recipe, and completed `10-VALIDATION.md` evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-CONTEXT.md
@.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-RESEARCH.md
@.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md
@.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-01-PLAN.md
@.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-02-PLAN.md
@.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-03-PLAN.md
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@tools/bazel/phase10_verify.py
@tools/bazel/phase10_verify_test.py
@tools/bazel/manifests/phase10_auxiliary_controllers.json
@tools/bazel/manifests/phase10_mmu_transport.json
@tools/bazel/manifests/phase10_modbus_rs485.json
@tools/bazel/manifests/phase10_toolchanger_dock_offsets.json
@tools/bazel/manifests/phase10_auxiliary_build_update.json
@tools/bazel/manifests/phase10_concern_dispositions.json
@rust/crates/domain/src/auxiliary.rs
@rust/crates/domain/src/lib.rs

<interfaces>
Expected Phase 10 verifier entrypoints:
```text
python3 tools/bazel/phase10_verify_test.py
python3 tools/bazel/phase10_verify.py --all
bazel run //tools/bazel:phase10_verify_tests
bazel run //tools/bazel:phase10_verify
just phase10-verify
```

Required root labels:
```text
//:phase10_verify
//:phase10_verify_tests
//:phase10_auxiliary_controller_docs
//tools/bazel:phase10_verify
//tools/bazel:phase10_verify_tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 10 verifier into Bazel and just</name>
  <files>tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, BUILD.bazel, justfile</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
    - tools/bazel/phase10_verify.py
    - tools/bazel/phase10_verify_test.py
    - tools/bazel/manifests/phase10_auxiliary_controllers.json
    - tools/bazel/manifests/phase10_mmu_transport.json
    - tools/bazel/manifests/phase10_modbus_rs485.json
    - tools/bazel/manifests/phase10_toolchanger_dock_offsets.json
    - tools/bazel/manifests/phase10_auxiliary_build_update.json
    - tools/bazel/manifests/phase10_concern_dispositions.json
    - rust/crates/domain/src/auxiliary.rs
    - rust/crates/domain/src/lib.rs
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-CONTEXT.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-RESEARCH.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md
  </read_first>
  <action>
    Update `tools/bazel/BUILD.bazel` following the existing Phase 8 and Phase 9 target style. Add:
    - `shell_binary(name = "phase10_verify", src = "rust_workflow.sh", data = [...])` where `data` includes `phase10_verify.py`, all six `tools/bazel/manifests/phase10_*.json` files, `//:phase10_auxiliary_controller_docs`, and `//:rust_workspace_sources`.
    - `shell_binary(name = "phase10_verify_tests", src = "rust_workflow.sh", data = ["phase10_verify.py", "phase10_verify_test.py"])`.

    Update `tools/bazel/rust_workflow.sh` with two dispatch cases:
    - `phase10_verify)` runs `python3 tools/bazel/phase10_verify.py --all`
    - `phase10_verify_tests)` runs `python3 tools/bazel/phase10_verify_test.py`

    Update root `BUILD.bazel` with a queryable filegroup:
    ```starlark
    filegroup(
        name = "phase10_auxiliary_controller_docs",
        srcs = [
            ".planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-CONTEXT.md",
            ".planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-RESEARCH.md",
            ".planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md",
        ],
    )
    ```

    Also add root aliases `phase10_verify` and `phase10_verify_tests` pointing to `//tools/bazel:phase10_verify` and `//tools/bazel:phase10_verify_tests`.

    Update `justfile` with recipe `phase10-verify:`. The recipe must run `bazel run //tools/bazel:phase10_verify_tests` before `bazel run //tools/bazel:phase10_verify`. Do not add hardware, simulator, network, flashing, or credential-dependent commands to this recipe.
  </action>
  <verify>
    <automated>bazel query "//tools/bazel:phase10_verify + //tools/bazel:phase10_verify_tests + //:phase10_verify + //:phase10_verify_tests + //:phase10_auxiliary_controller_docs"</automated>
    <automated>bazel run //tools/bazel:phase10_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase10_verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'name = "phase10_verify"' tools/bazel/BUILD.bazel`
    - `rg 'name = "phase10_verify_tests"' tools/bazel/BUILD.bazel`
    - `rg 'phase10_auxiliary_controllers.json|phase10_mmu_transport.json|phase10_modbus_rs485.json|phase10_toolchanger_dock_offsets.json|phase10_auxiliary_build_update.json|phase10_concern_dispositions.json' tools/bazel/BUILD.bazel`
    - `rg 'phase10_verify\\)|python3 tools/bazel/phase10_verify.py --all|phase10_verify_tests\\)|python3 tools/bazel/phase10_verify_test.py' tools/bazel/rust_workflow.sh`
    - `rg 'name = "phase10_auxiliary_controller_docs"|phase10_verify|phase10_verify_tests' BUILD.bazel`
    - `rg '^phase10-verify:|bazel run //tools/bazel:phase10_verify_tests|bazel run //tools/bazel:phase10_verify' justfile`
    - Bazel query command in `<verify>` exits 0.
    - Both Bazel run commands in `<verify>` exit 0.
  </acceptance_criteria>
  <done>Phase 10 verifier targets are queryable and runnable through Bazel and the `just phase10-verify` recipe.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Phase 10 Nyquist validation evidence</name>
  <files>.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-CONTEXT.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-RESEARCH.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-01-PLAN.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-02-PLAN.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-03-PLAN.md
    - tools/bazel/phase10_verify.py
    - tools/bazel/phase10_verify_test.py
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
    - tools/bazel/manifests/phase10_auxiliary_controllers.json
    - tools/bazel/manifests/phase10_mmu_transport.json
    - tools/bazel/manifests/phase10_modbus_rs485.json
    - tools/bazel/manifests/phase10_toolchanger_dock_offsets.json
    - tools/bazel/manifests/phase10_auxiliary_build_update.json
    - tools/bazel/manifests/phase10_concern_dispositions.json
    - rust/crates/domain/src/auxiliary.rs
    - rust/crates/domain/src/lib.rs
  </read_first>
  <action>
    After Task 1 verification passes, update `.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md` to record the implemented validation strategy.

    In frontmatter or the existing metadata block, set:
    - `status: complete`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - Keep `phase_lifecycle_id: 10-2026-06-14T15-08-30`.

    Replace draft Wave 0 task placeholders with concrete task IDs, plan numbers, and waves:
    - `10-01-01` Plan 10-01 Wave 1 manifest/source evidence
    - `10-01-02` Plan 10-01 Wave 1 Modbus/toolchanger evidence
    - `10-01-03` Plan 10-01 Wave 1 build/update/concern evidence
    - `10-02-01` Plan 10-02 Wave 1 Rust tests
    - `10-02-02` Plan 10-02 Wave 1 Rust implementation
    - `10-03-01` Plan 10-03 Wave 2 verifier tests
    - `10-03-02` Plan 10-03 Wave 2 verifier implementation
    - `10-04-01` Plan 10-04 Wave 3 Bazel/just wiring
    - `10-04-02` Plan 10-04 Wave 3 validation sign-off

    Add a `Final Automated Evidence` section listing these commands exactly:
    - `python3 tools/bazel/phase10_verify.py --manifests-only`
    - `python3 tools/bazel/phase10_verify.py --rust-only`
    - `python3 tools/bazel/phase10_verify.py --package-update-only`
    - `python3 tools/bazel/phase10_verify.py --evidence-only`
    - `python3 tools/bazel/phase10_verify.py --security-only`
    - `python3 tools/bazel/phase10_verify_test.py`
    - `bazel query "//tools/bazel:phase10_verify + //tools/bazel:phase10_verify_tests + //:phase10_verify + //:phase10_verify_tests + //:phase10_auxiliary_controller_docs"`
    - `bazel run //tools/bazel:phase10_verify_tests`
    - `bazel run //tools/bazel:phase10_verify`
    - `just phase10-verify`
    - `cargo fmt --all -- --check`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`

    Add or preserve the `Manual-Only Verifications` table and explicitly state that RS485/Modbus timing on physical buses, physical toolchanger dock and offset behavior, long-running startup flashing and update recovery, live MMU behavior, simulator auxiliary flows, and final replacement cutover proof remain non-local and are not claimed by local Phase 10 verification.

    Add threat references `T-10-01-01` through `T-10-04-06` or a concise grouped reference showing that manifest tampering, payload leakage, local proof overclaims, package/update path confusion, Modbus/RS485 fault evidence, and credential/payload disclosure checks were represented.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase10_verify.py --manifests-only</automated>
    <automated>python3 tools/bazel/phase10_verify.py --rust-only</automated>
    <automated>python3 tools/bazel/phase10_verify.py --package-update-only</automated>
    <automated>python3 tools/bazel/phase10_verify.py --evidence-only</automated>
    <automated>python3 tools/bazel/phase10_verify.py --security-only</automated>
    <automated>python3 tools/bazel/phase10_verify_test.py</automated>
    <automated>bazel query "//tools/bazel:phase10_verify + //tools/bazel:phase10_verify_tests + //:phase10_verify + //:phase10_verify_tests + //:phase10_auxiliary_controller_docs"</automated>
    <automated>bazel run //tools/bazel:phase10_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase10_verify</automated>
    <automated>just phase10-verify</automated>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'status: complete' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - `rg 'nyquist_compliant: true' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - `rg 'wave_0_complete: true' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - `rg '10-2026-06-14T15-08-30' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - `rg '10-01-01|10-01-02|10-01-03|10-02-01|10-02-02|10-03-01|10-03-02|10-04-01|10-04-02' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - `rg 'Final Automated Evidence|Manual-Only Verifications|RS485/Modbus timing|physical toolchanger|live MMU behavior|final replacement cutover proof' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - `rg 'python3 tools/bazel/phase10_verify.py --security-only|bazel run //tools/bazel:phase10_verify|just phase10-verify|cargo clippy --all-targets --all-features -- -D warnings' .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VALIDATION.md`
    - All automated commands listed in `<verify>` exit 0.
  </acceptance_criteria>
  <done>`10-VALIDATION.md` is complete, lifecycle-aligned, Nyquist-compliant, and honest about local versus non-local evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel target graph to verifier | Build labels determine what gets checked and must include all Phase 10 manifests, docs, Rust sources, tests, and verifier scripts. |
| `just` recipe to developer shell | The recipe is the human-facing command and must not silently skip tests or add hardware, simulator, flashing, or secret-dependent side effects. |
| Validation document to release evidence | The validation register can be mistaken for final hardware/cutover proof unless non-local exclusions are explicit. |
| Package/update path docs to verifier | Auxiliary firmware path handling must expose only paths and labels, not firmware bytes or signing credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | `tools/bazel/BUILD.bazel` Phase 10 targets | mitigate | Add queryable `phase10_verify`, `phase10_verify_tests`, root aliases, and docs filegroup that include all manifests and validation docs. |
| T-10-04-02 | Repudiation | `just phase10-verify` | mitigate | Recipe runs verifier tests before aggregate verifier and is listed in `10-VALIDATION.md` final automated evidence. |
| T-10-04-03 | Information Disclosure | Package/update validation evidence | mitigate | Validation records path and label checks only; no firmware bytes, signing-key values, credential values, or raw crash-dump payloads. |
| T-10-04-04 | Spoofing | Validation proof claims | mitigate | `10-VALIDATION.md` explicitly marks hardware, simulator, RS485, MMU live-transport, toolchanger, long-running update, and cutover proof as non-local. |
| T-10-04-05 | Denial of Service | Developer verification command | accept | `just phase10-verify` runs bounded local Bazel targets and cargo checks; it does not start hardware, simulator, network, or flashing flows. |
| T-10-04-06 | Elevation of Privilege | Workflow shell dispatch | mitigate | `rust_workflow.sh` cases invoke fixed Python commands and do not pass user-provided arguments into shell-expanded command strings. |
</threat_model>

<verification>
Run:
- `bazel query "//tools/bazel:phase10_verify + //tools/bazel:phase10_verify_tests + //:phase10_verify + //:phase10_verify_tests + //:phase10_auxiliary_controller_docs"`
- `bazel run //tools/bazel:phase10_verify_tests`
- `bazel run //tools/bazel:phase10_verify`
- `just phase10-verify`
- `python3 tools/bazel/phase10_verify.py --security-only`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
</verification>

<success_criteria>
Phase 10 verification is exposed through Bazel and `just`, validation is marked complete with concrete automated evidence, and non-local proof remains explicitly out of scope for local Phase 10 completion.
</success_criteria>

<output>
After completion, create `.planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-04-SUMMARY.md`
</output>
