---
phase: 36-normalize-evidence-and-blocker-rows
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase32_blocker_register_triage_contract.json
  - tools/bazel/phase32_blocker_normalization.py
  - tools/bazel/phase32_blocker_normalization_test.py
  - tools/bazel/phase32_blocker_register_triage.py
  - tools/bazel/phase32_blocker_register_triage_test.py
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
autonomous: true
requirements:
  - INTAKE-04
  - TRIAGE-01
  - TRIAGE-02
must_haves:
  truths:
    - "A valid all-passed Phase 26 `rows` table referenced by an accepted-final Phase 31 receipt produces no release/signing blocker and remains proof-eligible through the Phase 32 boundary."
    - "A recognized malformed Phase 26 table fails atomically as one critical proof-ineligible malformed blocker; no valid-looking subset becomes eligible."
    - "Unknown Phase 26/27/28 envelopes, row kinds, and statuses remain visible critical `unknown_unclassified` blockers instead of being skipped."
    - "Every Phase 27/28 decision-domain blocker exposes immutable source identity and separate decision-resolution identity, and its row ID does not change when mutable evidence/status/owner metadata changes."
    - "Retained-code, residual-risk, exception, readiness, and demotion identities cannot collide or resolve by path, prefix, stream, or gate similarity."
    - "Focused regressions generate actual Phase 26 output through Phase 31 and actual Phase 27/28 output into Phase 32, stopping before Phase 34 reconciliation."
  artifacts:
    - path: "tools/bazel/manifests/phase32_blocker_register_triage_contract.json"
      provides: "Required source-identity, decision-identity, producer-adapter, and fail-closed shape policy"
      contains: "source_subject_id"
    - path: "tools/bazel/phase32_blocker_register_triage.py"
      provides: "Contract-keyed table/row adapters, immutable canonical IDs, and Phase 32 integration"
      contains: "decision_subject_id"
    - path: "tools/bazel/phase32_blocker_normalization.py"
      provides: "Pure producer-shape parsing and canonical identity core extracted from the oversized Phase 32 shell"
      contains: "source_subject_id"
    - path: "tools/bazel/phase32_blocker_normalization_test.py"
      provides: "Fast one-concern adapter and identity unit regressions"
      contains: "decision_axis"
    - path: "tools/bazel/phase32_blocker_register_triage_test.py"
      provides: "Producer-shaped positive and one-concern negative regressions"
      contains: "all_passed"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Bazel runfiles for the real Phase 26/27/28/31 producer-shaped Phase 32 tests"
      contains: "phase32_blocker_register_triage_test"
  key_links:
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase26/upstream-result-row-table.json"
      via: "accepted-final Phase 31 consumed_upstream_row_refs and the explicit Phase 26 table adapter"
      pattern: "consumed_upstream_row_refs|rows"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "tools/bazel/manifests/phase32_blocker_register_triage_contract.json"
      via: "required canonical identity fields and adapter/enum validation"
      pattern: "source_domain|decision_axis"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "tools/bazel/phase32_blocker_normalization.py"
      via: "thin imperative shell calling pure producer adapters and identity construction"
      pattern: "phase32_blocker_normalization"
    - from: "tools/bazel/phase32_blocker_register_triage_test.py"
      to: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      via: "real producer output used by the Phase 26 -> Phase 31 -> Phase 32 regression"
      pattern: "phase26"
    - from: "tools/bazel/phase32_blocker_register_triage_test.py"
      to: "tools/bazel/phase27_retained_code_acceptance_decisions.py"
      via: "real retained-code, residual-risk, exception, and readiness producer output"
      pattern: "phase27"
    - from: "tools/bazel/phase32_blocker_register_triage_test.py"
      to: "tools/bazel/phase28_final_readiness_packet.py"
      via: "real readiness, residual-risk, and demotion producer output"
      pattern: "phase28"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 36-2026-07-26T00-27-52
generated_at: 2026-07-26T00:50:00Z
---

<objective>
Repair the real producer-to-consumer shape boundary at Phase 32 and give every Phase 27/28 decision-domain blocker a stable, exact identity that Phase 37 can resolve later.

Purpose: close milestone audit gap B2 and supply the canonical row foundation for B1 without moving Phase 31 provenance authority, adding approval semantics, or implementing readiness reconciliation.

Output: an extended Phase 32 contract, explicit producer-shaped adapters, immutable canonical blocker identities, real producer boundary regressions, and Bazel runfiles needed to execute those regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-CONTEXT.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-RESEARCH.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-VALIDATION.md
@tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
@tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
@tools/bazel/manifests/phase28_final_readiness_packet_contract.json
@tools/bazel/manifests/phase31_final_evidence_intake_contract.json
@tools/bazel/manifests/phase32_blocker_register_triage_contract.json
@tools/bazel/phase26_release_signing_upstream_evidence.py
@tools/bazel/phase27_retained_code_acceptance_decisions.py
@tools/bazel/phase28_final_readiness_packet.py
@tools/bazel/phase31_final_evidence_intake.py
@tools/bazel/phase32_blocker_normalization.py
@tools/bazel/phase32_blocker_normalization_test.py
@tools/bazel/phase32_blocker_register_triage.py
@tools/bazel/phase32_blocker_register_triage_test.py
</context>

<decision-coverage>
| Locked decisions | Implemented by |
| --- | --- |
| D-01 through D-04 | Tasks 1-2 contract and implement the explicit atomic Phase 26 table adapter while preserving Phase 31 authority and rejecting producer/generic-flattener changes. |
| D-05 through D-10 | Tasks 1-2 contract and implement the five-field immutable source tuple, source-only `row_id`, separate decision identity pair, exact matching semantics, and collision rejection. |
| D-11 through D-14 | Tasks 1-2 implement contract-keyed required-core dispatch with explicit `malformed` versus `unknown_unclassified` fail-closed outcomes. |
| D-15 through D-17 | Task 3 exercises actual Phase 26-28 producer outputs, stops verification at Phase 32, and excludes Phase 34/35 authority behavior. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract canonical identities and add RED adapter regressions</name>
  <read_first>
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards-overrides.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-CONTEXT.md`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-RESEARCH.md`
    - `tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json`
    - `tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json`
    - `tools/bazel/manifests/phase28_final_readiness_packet_contract.json`
    - `tools/bazel/manifests/phase31_final_evidence_intake_contract.json`
    - `tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `tools/bazel/phase32_blocker_register_triage.py`
    - `tools/bazel/phase32_blocker_register_triage_test.py`
  </read_first>
  <files>
    tools/bazel/manifests/phase32_blocker_register_triage_contract.json
    tools/bazel/phase32_blocker_normalization_test.py
  </files>
  <behavior>
    - D-05 through D-10: the Phase 32 contract requires `source_domain`, `producer_phase`, `producer_artifact_kind`, `source_row_kind`, `source_subject_id`, `decision_axis`, and `decision_subject_id` on every canonical blocker row.
    - An all-passed canonical Phase 26 `{"rows": [...]}` table referenced by an accepted-final Phase 31 receipt emits no release blocker.
    - Missing, non-list, empty, duplicate, incomplete, unknown-criterion, and non-object Phase 26 tables fail atomically without partially accepting rows.
    - Mutable owner, status, evidence-ref, timestamp, or action changes do not change canonical `row_id`.
    - Retained-code, residual-risk, exception, readiness, and demotion axes remain distinct for the same or similar subjects.
  </behavior>
  <action>
Extend `tools/bazel/manifests/phase32_blocker_register_triage_contract.json` without changing Phase 26-31 producer contracts.

1. Implement D-05 through D-10 in the contract by adding the exact required canonical fields `source_domain`, `producer_phase`, `producer_artifact_kind`, `source_row_kind`, `source_subject_id`, `decision_axis`, and `decision_subject_id`.
2. Declare closed enums for the producer domains/artifact kinds/row kinds actually emitted by Phases 26-28 and decision axes `retained_code`, `residual_risk`, `exception`, `readiness`, and `demotion`.
3. Implement D-01 through D-04 by declaring a Phase 26 table adapter selected only for the `release-signing` stream and expected upstream-result table artifact. Contract `rows` as a non-empty list with complete unique canonical criterion coverage and required decision-bearing fields.
4. Implement D-11 through D-14 by preserving `malformed` for recognized invalid shapes and `unknown_unclassified` for unsupported envelopes, row kinds, or statuses. Both remain critical and proof-ineligible.
5. Create `phase32_blocker_normalization_test.py` for the new pure core instead of growing the already 717-line Phase 32 integration suite. Add focused Arrange/Act/Assert tests for the positive table and identity behaviors plus one negative concern per test: missing/non-list/empty `rows`, non-object row, missing/mistyped field, duplicate/unknown criterion, unsupported envelope/status, and duplicate source tuple.
6. Make the two identity layers explicit in tests: `row_id` derives exclusively from the five-field source tuple; decision identity is the pair `decision_axis` plus `decision_subject_id`; identical subjects on different axes remain distinct decision identities; an incompatible remapping of one source tuple is rejected without changing `row_id`.
7. Add a stability test that builds two otherwise identical blockers with changed owner/status/evidence/timestamp/action fields and asserts the same `row_id`.

Keep the new tests RED because the current Phase 32 loader treats a table as one row and hashes the whole mutable signal. Do not weaken existing unknown/security/proof-eligibility tests.
  </action>
  <acceptance_criteria>
    - `python3 -m json.tool tools/bazel/manifests/phase32_blocker_register_triage_contract.json` exits 0.
    - The contract contains all seven exact canonical identity field names and all five exact decision-axis values.
    - Each table-shape or identity failure has one focused test with explicit Arrange, Act, Assert comments.
    - RED execution fails on missing adapter/identity behavior, not syntax, import, or fixture-construction errors.
    - No Phase 26, 27, 28, or 31 source/contract file is modified.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -m json.tool tools/bazel/manifests/phase32_blocker_register_triage_contract.json &gt;/dev/null; python3 -m py_compile tools/bazel/phase32_blocker_normalization_test.py; red_log=$(mktemp); trap "rm -f $red_log" EXIT; set +e; python3 tools/bazel/phase32_blocker_normalization_test.py -q &gt;"$red_log" 2&gt;&amp;1; red_status=$?; set -e; test "$red_status" -ne 0; grep -F "FAILED" "$red_log"; ! grep -E "SyntaxError|ImportError|NameError" "$red_log"'</automated>
  </verify>
  <done>The Phase 32 contract freezes the canonical identity and table-adapter behavior, and focused RED tests fail only because implementation is not yet present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement contract-keyed adapters and immutable blocker identities</name>
  <read_first>
    - `tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `tools/bazel/phase32_blocker_normalization.py`
    - `tools/bazel/phase32_blocker_normalization_test.py`
    - `tools/bazel/phase32_blocker_register_triage.py`
    - `tools/bazel/phase32_blocker_register_triage_test.py`
    - `tools/bazel/phase26_release_signing_upstream_evidence.py`
    - `tools/bazel/phase27_retained_code_acceptance_decisions.py`
    - `tools/bazel/phase28_final_readiness_packet.py`
    - `tools/bazel/phase31_final_evidence_intake.py`
    - `.planning/v1.3-MILESTONE-AUDIT.md`
  </read_first>
  <files>
    tools/bazel/manifests/phase32_blocker_register_triage_contract.json
    tools/bazel/phase32_blocker_normalization.py
    tools/bazel/phase32_blocker_normalization_test.py
    tools/bazel/phase32_blocker_register_triage.py
    tools/bazel/phase32_blocker_register_triage_test.py
  </files>
  <behavior>
    - All Task 1 RED cases pass without weakening existing Phase 32 classification, derived-view, security, or proof-eligibility assertions.
    - Phase 31 remains the only finality/provenance authority for Phase 26 release rows.
    - Known malformed inputs become explicit blockers rather than escaping as unhandled exceptions or disappearing.
    - Canonical IDs depend only on immutable producer identity and never on paths or mutable classification metadata.
    - Phase 32 emits identities but performs no Phase 33 approval or Phase 34 readiness resolution.
  </behavior>
  <action>
Refactor the Phase 32 input boundary around small pure helpers and guard-style returns.

1. Create `phase32_blocker_normalization.py` as the pure functional core for D-01 through D-14. Move no filesystem writes, CLI parsing, report generation, or security scanning into it.
2. Implement D-05 through D-10 by replacing the mutable-signal `stable_row_id(prefix, source_stream, payload)` behavior with a canonical source identity record containing only `source_domain`, `producer_phase`, `producer_artifact_kind`, `source_row_kind`, and `source_subject_id`. Serialize that exact record deterministically and derive `row_id` from it.
3. Keep decision identity separate: `decision_axis` and `decision_subject_id` never enter `row_id`. Identical subjects on different axes are distinct decision identities, while incompatible remapping of one immutable source tuple raises a validation error.
4. Extend `build_blocker_row` to require the immutable source identity plus the separate decision identity pair, then emit all seven fields. Keep classification metadata, owner, evidence refs, source refs, severity, and next action outside the ID input.
5. Implement D-01 through D-04 with an explicit Phase 26 table adapter called from `load_phase31_rows` only after an `accepted-final` release/signing receipt provides the table ref. Validate the complete table first; on valid input return normalized rows, on recognized invalid input return one malformed table signal, and on unsupported input return one unknown signal.
6. Preserve lineage through structured source refs to the receipt, table, and criterion. A clean passed row returns no blocker; a non-passed valid row uses the canonical `criterion_id` as `source_subject_id`.
7. Normalize actual Phase 27 residual-risk, exception, and handoff rows and Phase 28 readiness, residual-risk, and demotion rows into distinct producer artifact/row kinds and decision axes. Preserve producer-native `packet_id`, `row_id`, or `criterion_id`; use fixed `final-reference-demotion-allowed` for demotion.
8. Extend `validate_register_rows` to reject missing canonical fields, duplicate immutable source tuples, duplicate `row_id`, empty decision subjects, and incompatible source/decision axis combinations.
9. Ensure derived views still reference canonical register IDs and include enough identity context for Phase 37, while reports retain the explicit no-approval/no-readiness/no-demotion-authority disclaimer.
10. Keep security scans, path containment, unsafe-ref behavior, Phase 31 receipt validation, and existing status taxonomy intact. Do not catch and ignore validation errors.

Do a simplification pass before finishing: keep shape parsing, identity derivation, classification, and filesystem effects separate; split helpers only where responsibility is coherent; do not introduce a generic framework or external dependency.
  </action>
  <acceptance_criteria>
    - `phase32_blocker_register_triage.py` emits all seven canonical identity fields on every blocker and `validate_register_rows` rejects absent/colliding identities.
    - A real-shaped valid all-passed Phase 26 table returns no release blocker; every specified malformed or unsupported mutation returns a critical proof-ineligible blocker.
    - Mutable metadata changes leave source-only `row_id` unchanged; distinct source tuples have distinct row IDs; identical subjects on different axes have distinct decision identities; incompatible axis remapping of one source tuple is rejected without changing `row_id`.
    - Phase 27/28 retained-code, residual-risk, exception, readiness, and demotion rows expose the intended distinct axes and producer-native subjects.
    - All Phase 32 tests, contract-only mode, and security-only mode pass.
    - No new dependency, approval inference, readiness reconciliation, or producer-contract modification is introduced.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase32_blocker_normalization.py tools/bazel/phase32_blocker_normalization_test.py tools/bazel/phase32_blocker_register_triage.py &amp;&amp; python3 tools/bazel/phase32_blocker_normalization_test.py -q &amp;&amp; python3 tools/bazel/phase32_blocker_register_triage.py --contract-only &amp;&amp; python3 tools/bazel/phase32_blocker_register_triage.py --security-only</automated>
  </verify>
  <done>Phase 32 correctly adapts known producer shapes, fails closed on bad ones, and emits stable exact source and decision identities without expanding authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exercise real Phase 26-28 producers through the Phase 32 boundary</name>
  <read_first>
    - `tools/bazel/phase26_release_signing_upstream_evidence.py`
    - `tools/bazel/phase26_release_signing_upstream_evidence_test.py`
    - `tools/bazel/phase27_retained_code_acceptance_decisions.py`
    - `tools/bazel/phase27_retained_code_acceptance_decisions_test.py`
    - `tools/bazel/phase28_final_readiness_packet.py`
    - `tools/bazel/phase28_final_readiness_packet_test.py`
    - `tools/bazel/phase31_final_evidence_intake.py`
    - `tools/bazel/phase31_final_evidence_intake_test.py`
    - `tools/bazel/phase32_blocker_register_triage.py`
    - `tools/bazel/phase32_blocker_register_triage_test.py`
    - `tools/bazel/BUILD.bazel`
    - `tools/bazel/rust_workflow.sh`
    - `justfile`
  </read_first>
  <files>
    tools/bazel/phase32_blocker_register_triage_test.py
    tools/bazel/BUILD.bazel
    tools/bazel/rust_workflow.sh
  </files>
  <behavior>
    - The positive release regression calls the real Phase 26 producer, submits its table through the real Phase 31 intake boundary, then runs Phase 32.
    - Phase 27 and Phase 28 positive regressions call their real producer functions and consume their generated artifacts without retyping production schemas.
    - Bazel and `just phase32-verify` execute those producer-shaped regressions in hermetic runfiles.
    - The tests stop at Phase 32 outputs and do not assert Phase 34 readiness resolution.
  </behavior>
  <action>
Replace handwritten integration substitutes with isolated producer-generated fixtures while retaining small handcrafted unit fixtures for one-concern negative mutations.

1. Implement D-15 through D-17 with a temp-root fixture that copies only safe checked-in contracts/source refs required by the Phase 26, 27, 28, 31, and 32 producer functions.
2. Generate an all-passed sanitized Phase 26 table using the real Phase 26 module, submit/register it through the real Phase 31 module as accepted-final, then invoke the Phase 32 loader/generator. Assert no release/signing `unknown_unclassified` or malformed blocker exists and lineage/eligibility are preserved.
3. Generate Phase 27 outputs with retained-code, residual-risk, exception, and readiness rows using the real producer module. Feed its output directory to Phase 32 and assert producer-native subjects, distinct decision axes, stable IDs, and no approval inference.
4. Generate Phase 28 readiness, residual-risk, and blocked demotion outputs using the real producer module. Feed its output directory to Phase 32 and assert readiness/residual-risk/demotion identity separation and the fixed demotion subject.
5. Keep negative mutation tests one-concern and local: mutate the producer-generated object after generation instead of duplicating full schemas.
6. Add a focused `Phase32ProducerShapeTest` class in the existing integration test file and keep shared producer fixture construction compact so the already oversized file does not accumulate pure adapter/identity tests.
7. Update `phase32_verify_tests` runfiles in `tools/bazel/BUILD.bazel` with the new normalization module/test plus only the exact Phase 26/27/28/31 modules, contracts, and safe manifests required in the sandbox.
8. Update the existing `phase32_verify_tests)` case in `tools/bazel/rust_workflow.sh` to run `phase32_blocker_normalization_test.py` and `phase32_blocker_register_triage_test.py`, in that order. Preserve `just phase32-verify` tests-before-verifier ordering and run `bash -n`.
9. Run the full validation matrix only at the wave/final gate, review the diff for producer or Phase 37/38 scope creep, and record any residual risk in the plan summary.
  </action>
  <acceptance_criteria>
    - Tests invoke actual Phase 26, 27, 28, and 31 producer modules and assert Phase 32 output, rather than restating producer JSON schemas.
    - The Phase 26-through-31-through-32 all-passed path is regression-protected and emits no false release blocker.
    - Every Phase 27/28 decision domain has a producer-shaped stable-identity assertion.
    - Bazel runfiles contain exactly the producer modules/contracts needed by the Phase 32 tests, and `bazel run //tools/bazel:phase32_verify_tests` runs both Phase 32 test modules successfully.
    - `just phase32-verify` passes with tests before verifier/security/wiring checks.
    - `git diff --check` passes and diff review shows no Phase 34 reconciliation, Phase 35 cutover, or producer-contract change.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase32_blocker_register_triage_test.py Phase32ProducerShapeTest -q &amp;&amp; bash -n tools/bazel/rust_workflow.sh</automated>
  </verify>
  <done>Actual Phase 26-28 producer shapes are proven compatible with Phase 32, the Phase 26 table no longer becomes a false unknown blocker, and the canonical identity foundation is ready for Phase 37.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 31 receipt → Phase 26 table | Only an accepted-final receipt may make a producer table eligible for adaptation. |
| Producer JSON → normalized signal | Malformed or unknown data must become visible critical blockers without partial eligibility. |
| Source identity → canonical row ID | Mutable or attacker-controlled classification data must not churn or collide IDs. |
| Canonical row → decision identity | Similar criteria across retained-code, residual-risk, exception, readiness, and demotion must never cross-resolve. |
| Adapter output → generated report/handoff | Secret-bearing fields, unsafe refs, and authority overclaims must not propagate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-36-01 | Spoofing / Tampering | Phase 26 table envelope | mitigate | Dispatch only from accepted-final release receipt context; validate complete canonical criterion coverage and required fields before accepting any row. |
| T-36-02 | Tampering / Elevation of Privilege | Partial table validity | mitigate | Treat table validation atomically and emit one critical malformed blocker on any structural failure. |
| T-36-03 | Spoofing / Tampering | Canonical row identity | mitigate | Hash only the immutable typed source tuple; reject duplicate tuples and row IDs; test mutable metadata stability. |
| T-36-04 | Elevation of Privilege | Decision-domain matching | mitigate | Keep explicit `decision_axis` and `decision_subject_id`; prohibit path, stream, prefix, or gate fallback joins. |
| T-36-05 | Information Disclosure | Producer metadata and refs | mitigate | Use shape-specific required-core adapters, existing safe-ref validation, and Phase 32 secret/overclaim scans; never recursively flatten arbitrary JSON. |
| T-36-06 | Elevation of Privilege | Readiness/demotion projection | mitigate | Preserve no-approval disclaimers and add identity only; defer all resolution/authorization behavior to Phase 37/38. |
| T-36-07 | Repudiation | Producer-shaped regression evidence | mitigate | Generate actual Phase 26/27/28 outputs in isolated tests and preserve structured receipt/table/criterion lineage in assertions. |
</threat-model>

<verification>
1. Run the focused Phase 32 test after every task:

```bash
python3 tools/bazel/phase32_blocker_register_triage_test.py -q
```

2. After producer-shaped integration, run all directly affected producer/intake suites:

```bash
python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py -q
python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py -q
python3 tools/bazel/phase28_final_readiness_packet_test.py -q
python3 tools/bazel/phase31_final_evidence_intake_test.py -q
```

3. Run contract, security, Bazel, and repo-native gates:

```bash
python3 tools/bazel/phase32_blocker_register_triage.py --contract-only
python3 tools/bazel/phase32_blocker_register_triage.py --security-only
bazel run //tools/bazel:phase32_verify_tests
just phase32-verify
```

4. Run scoped pre-commit and diff review:

```bash
pre-commit run --files tools/bazel/manifests/phase32_blocker_register_triage_contract.json tools/bazel/phase32_blocker_normalization.py tools/bazel/phase32_blocker_normalization_test.py tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py tools/bazel/BUILD.bazel tools/bazel/rust_workflow.sh
git diff --check
git diff --stat -- tools/bazel/manifests/phase32_blocker_register_triage_contract.json tools/bazel/phase32_blocker_normalization.py tools/bazel/phase32_blocker_normalization_test.py tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py tools/bazel/BUILD.bazel tools/bazel/rust_workflow.sh
```

5. Review the final diff for the explicit simplification questions: can less code preserve the contract, are parsing/identity/classification/effects cleanly separated, and did any workaround or Phase 37/38 logic enter the phase?
</verification>

<success-criteria>
- An accepted all-passed Phase 26 table remains eligible through Phase 31/32.
- Known malformed and unknown producer shapes fail closed without false `unknown_unclassified` classification of valid tables.
- Phase 27/28 decision-domain rows have stable source and exact resolution identities.
- Actual Phase 26, 27, and 28 outputs are covered by focused Phase 32 boundary regressions.
- `INTAKE-04`, `TRIAGE-01`, and `TRIAGE-02` are satisfied at the Phase 32 handoff boundary.
- All affected Python, Bazel, `just`, pre-commit, and diff verification passes without new dependencies or broader authority.
</success-criteria>
