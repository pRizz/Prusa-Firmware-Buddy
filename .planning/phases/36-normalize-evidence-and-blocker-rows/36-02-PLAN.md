---
phase: 36-normalize-evidence-and-blocker-rows
plan: "02"
type: execute
wave: 2
depends_on: ["36-01"]
gap_closure: true
files_modified:
  - tools/bazel/phase32_blocker_register_triage.py
  - tools/bazel/phase32_blocker_register_triage_test.py
autonomous: true
requirements:
  - TRIAGE-01
  - TRIAGE-02
must_haves:
  truths:
    - "Every recognized Phase 27/28 expected artifact with a missing, mistyped, or non-object-member required collection becomes one atomic visible critical proof-ineligible `malformed` canonical blocker row instead of aborting Phase 32."
    - "Every Phase 27/28 non-object top level or explicit incompatible artifact discriminator becomes one visible critical proof-ineligible `unknown_unclassified` canonical blocker row instead of aborting Phase 32."
    - "Legitimate empty Phase 27 residual/exception and Phase 28 blocker/residual collections remain valid, publish the complete Phase 32 bundle, and emit neither a container-problem row nor an ordinary row for the empty producer collection."
    - "For every malformed or unsupported Phase 27/28 container case, Phase 32 still publishes the canonical blocker register, all existing derived JSON views, the downstream handoff manifest, and the redacted report."
    - "Valid Phase 27/28 rows, row/status/demotion classification, policy overrides, provenance/security gates, and all existing Phase 32 output semantics remain unchanged."
    - "Container-problem row IDs use an exact immutable producer-container source tuple; malformed-versus-unknown status, failure text, and other mutable values cannot change identity or create a fallback join."
    - "Phase 32 continues to grant no exception, retained-code, residual-risk, readiness, demotion, or cutover authority."
  artifacts:
    - path: "tools/bazel/phase32_blocker_register_triage.py"
      provides: "Producer-specific Phase 27/28 container adaptation that preserves fail-closed publication"
      contains: "phase27_rows"
    - path: "tools/bazel/phase32_blocker_register_triage_test.py"
      provides: "Eight negative and four positive one-concern real-producer regressions covering every Phase 27/28 collection call site, both fail-closed categories, and valid empty collections"
      contains: "missing_rows_is_malformed"
  key_links:
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase27/residual-risk-register.json"
      via: "producer-specific container loading before existing row classification"
      pattern: "phase27_rows|residual-risk-register"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase27/exception-decision-register.json"
      via: "producer-specific container loading before existing exception classification"
      pattern: "phase27_rows|exception-decision-register"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase28/blocker-summary.json"
      via: "producer-specific container loading before existing readiness classification"
      pattern: "phase28_rows|blocker-summary"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase28/exception-residual-risk-summary.json"
      via: "producer-specific container loading before existing residual-risk classification"
      pattern: "phase28_rows|exception-residual-risk-summary"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase32/downstream-handoff-manifest.json"
      via: "container problems flow through canonical row validation and the unchanged output writer"
      pattern: "validate_register_rows|generate_handoff"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 36-2026-07-26T00-27-52
generated_at: 2026-07-26T02:55:59.774Z
---

<objective>
Close the remaining Phase 36 verifier gap by keeping malformed and unsupported Phase 27/28 producer containers visible in the canonical Phase 32 bundle.

Purpose: TRIAGE-01 and TRIAGE-02 remain partial while `require_list` and top-level-object validation can terminate Phase 32 before a blocker row, derived views, or handoff exists.

Output: one producer-specific container adapter in the existing Phase 32 shell and twelve producer-shaped regressions proving every Phase 27/28 collection call site publishes `malformed` and `unknown_unclassified` outcomes while legitimate empty collections remain valid, without changing producer contracts or downstream authority.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-CONTEXT.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-RESEARCH.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-VALIDATION.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-VERIFICATION.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-01-PLAN.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-01-SUMMARY.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-REVIEW.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-REVIEW-FIX.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-SECURITY.md
@tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
@tools/bazel/manifests/phase28_final_readiness_packet_contract.json
@tools/bazel/manifests/phase32_blocker_register_triage_contract.json
@tools/bazel/phase27_retained_code_acceptance_decisions.py
@tools/bazel/phase27_retained_code_acceptance_decisions_test.py
@tools/bazel/phase28_final_readiness_packet.py
@tools/bazel/phase28_final_readiness_packet_test.py
@tools/bazel/phase32_blocker_normalization.py
@tools/bazel/phase32_blocker_normalization_test.py
@tools/bazel/phase32_blocker_register_triage.py
@tools/bazel/phase32_blocker_register_triage_test.py

<interfaces>
Current aborting seams:

```python
for item_index, item in enumerate(
        require_list(residual.get("rows"), "phase27 residual rows")):
    if not isinstance(item, dict):
        raise VerificationError("phase27 residual rows must be objects")

for item_index, item in enumerate(
        require_list(blocker_summary.get("blockers"), "phase28 blockers")):
    if not isinstance(item, dict):
        raise VerificationError("phase28 blockers must be objects")
```

The same pattern exists for Phase 27 `exception-decision-register.json` and Phase 28 `exception-residual-risk-summary.json`.

Required category split:

```text
recognized expected artifact + collection field missing
recognized expected artifact + collection field present but wrong type
recognized expected artifact + collection contains any non-object member
  -> row_problem_kind: malformed

non-object top-level value
top-level object with an explicit incompatible producer_artifact_kind discriminator
  -> row_problem_kind: unknown_unclassified

recognized expected artifact + required collection is an empty list
  -> valid container; emit no container-problem row
```

Both categories use the existing contract policy:

```text
severity: critical
proof_eligibility: ineligible
```

Valid list contents continue through the current artifact-specific row loops without reclassification.

Exact container-row mapping:

| Artifact / collection | source_domain | producer_phase | producer_artifact_kind | source_row_kind | fixed source_subject_id | decision_axis | decision_subject_id | source_stream | affected_gate | decision_impact |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `build/ci-evidence/phase27/residual-risk-register.json` / `rows` | `retained_code` | `phase27` | `phase27_residual_risk_register` | `residual_risk` | `phase27-residual-risk-register-container` | `residual_risk` | `phase27-residual-risk-register-container` | `retained-code` | `final-retained-code-acceptance` | malformed: `repair_required_before_cutover`; unknown: `cutover_verdict_blocked` |
| `build/ci-evidence/phase27/exception-decision-register.json` / `rows` | `retained_code` | `phase27` | `phase27_exception_decision_register` | `exception_request` | `phase27-exception-decision-register-container` | `exception` | `phase27-exception-decision-register-container` | `retained-code` | `final-retained-code-acceptance` | malformed: `repair_required_before_cutover`; unknown: `cutover_verdict_blocked` |
| `build/ci-evidence/phase28/blocker-summary.json` / `blockers` | `readiness` | `phase28` | `phase28_blocker_summary` | `readiness_blocker` | `phase28-blocker-summary-container` | `readiness` | `phase28-blocker-summary-container` | `readiness` | `final-readiness` | malformed: `repair_required_before_cutover`; unknown: `cutover_verdict_blocked` |
| `build/ci-evidence/phase28/exception-residual-risk-summary.json` / `rows` | `readiness` | `phase28` | `phase28_exception_residual_risk_summary` | `residual_risk` | `phase28-exception-residual-risk-summary-container` | `residual_risk` | `phase28-exception-residual-risk-summary-container` | `readiness` | `final-readiness` | malformed: `repair_required_before_cutover`; unknown: `cutover_verdict_blocked` |

Every mapping uses existing Phase 32 contract enum values. For either category, set `source_ref` to the exact artifact path plus `#container`, and set signal `evidence_refs` to the exact artifact path so the canonical row contains both the artifact ref and its container source ref. Do not add or widen identity enums.
</interfaces>
</context>

<gap-analysis>
Root-cause category: incomplete boundary adaptation. Plan 36-01 correctly handles valid producer rows and unknown row/status/demotion values, but Phase 27/28 collection shape validation still raises before `run_quick` reaches `validate_register_rows`, `build_derived_views`, `generate_handoff`, and the output writers. The repair must translate only producer-container shape problems into canonical rows; it must not turn path, provenance, contract, identity-collision, security, or output-validation failures into ordinary blockers.
</gap-analysis>

<decision-coverage>
| Decision | Coverage | Plan/task |
| --- | --- | --- |
| D-01 through D-04 | Full/preserved | Task 2 leaves the Phase 26-through-31 accepted-final adapter and producer contracts untouched; the full Phase 32 gate protects the accepted release path. |
| D-05 through D-10 | Full/preserved | Task 2 constructs container rows through `build_blocker_row` from exact immutable producer-container tuples and keeps decision axes/subjects explicit; no fallback match key is added. |
| D-11 | Full | Task 2 selects only the four exact Phase 27/28 artifact/collection contracts already loaded by `phase27_rows` and `phase28_rows`. |
| D-12 | Full | Task 2 validates required container/core shape, preserves empty lists as valid collection values, and continues to allow additive non-semantic top-level metadata. |
| D-13 | Full | Tasks 1-2 prove missing/mistyped/non-object-member collections become `malformed`, unsupported envelopes become `unknown_unclassified`, and legitimate empty collections emit neither, while fail-closed rows remain critical and proof-ineligible. |
| D-14 | Full | Tasks 1-2 prove no malformed or unsupported Phase 27/28 container is dropped and no valid producer table is reclassified. |
| D-15 | Full | Task 1 mutates outputs created by the actual Phase 27/28 producer functions in `generate_producer_fixture`; no handwritten substitute replaces the producer boundary. |
| D-16 | Full | Task 1 adds one focused regression for each Phase × category pair and keeps existing row-kind/status, demotion, identity, and collision tests. |
| D-17 | Full | Task 2 stops at the existing Phase 32 register/views/report/handoff bundle and adds no Phase 33 decision or Phase 34 readiness reconciliation. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED producer-container publication regressions</name>
  <read_first>
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards-overrides.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-CONTEXT.md`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-VERIFICATION.md`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-01-SUMMARY.md`
    - `tools/bazel/phase27_retained_code_acceptance_decisions.py`
    - `tools/bazel/phase28_final_readiness_packet.py`
    - `tools/bazel/phase32_blocker_register_triage.py`
    - `tools/bazel/phase32_blocker_register_triage_test.py`
  </read_first>
  <files>
    tools/bazel/phase32_blocker_register_triage_test.py
  </files>
  <behavior>
    - Phase 27 residual register: missing required `rows` is `malformed`; a non-object top-level envelope is `unknown_unclassified`.
    - Phase 27 exception register: mistyped required `rows` is `malformed`; an explicit incompatible `producer_artifact_kind` discriminator is `unknown_unclassified`.
    - Phase 28 blocker summary: missing required `blockers` is `malformed`; a non-object top-level envelope is `unknown_unclassified`.
    - Phase 28 residual summary: any non-object member makes the whole `rows` collection atomically `malformed`; an explicit incompatible `producer_artifact_kind` discriminator is `unknown_unclassified`.
    - Empty Phase 27 exception `rows`, Phase 28 blocker `blockers`, and Phase 28 residual `rows` are valid and emit no container-problem row.
    - Every case exits zero after publishing the existing Phase 32 register, decision-impact view, exception-request view, residual-risk view, downstream handoff, and report.
  </behavior>
  <action>
Extend `Phase32ProducerShapeTest` without changing its real-producer fixture construction.

1. Add exactly eight negative one-concern Arrange/Act/Assert tests, pairing one malformed and one unsupported-envelope mutation for every collection call site:
   - `test_phase27_residual_missing_rows_is_malformed`
   - `test_phase27_residual_non_object_envelope_is_unknown`
   - `test_phase27_exception_mistyped_rows_is_malformed`
   - `test_phase27_exception_incompatible_discriminator_is_unknown`
   - `test_phase28_blocker_missing_blockers_is_malformed`
   - `test_phase28_blocker_non_object_envelope_is_unknown`
   - `test_phase28_residual_non_object_member_is_atomic_malformed`
   - `test_phase28_residual_incompatible_discriminator_is_unknown`
2. Add exactly four positive one-concern Arrange/Act/Assert tests:
   - `test_phase27_residual_empty_rows_remain_valid`
   - `test_phase27_exception_empty_rows_remain_valid`
   - `test_phase28_blocker_empty_blockers_remain_valid`
   - `test_phase28_residual_empty_rows_remain_valid`
3. Generate every fixture through the existing real Phase 26, Phase 27, Phase 28, and Phase 31 producer functions. Mutate only the one Phase 27 or Phase 28 artifact named in `<behavior>` after producer generation.
4. For incompatible-discriminator cases, retain the producer's required collection but add `producer_artifact_kind` with another mapping-table artifact kind. The adapter must reject that explicit conflict as `unknown_unclassified`; absence of the optional discriminator remains compatible with current producer output.
5. In each test, invoke the existing Phase 32 `--quick` subprocess and require return code `0`.
6. Add one assertion helper keyed by the exact `<interfaces>` mapping. For negative cases, assert exactly one canonical container-problem row for the mutated artifact and require every mapped identity/routing field, expected `row_problem_kind`, `severity == "critical"`, `proof_eligibility == "ineligible"`, non-empty `owner_ref`, non-empty `required_next_action`, exact `affected_gate`, category-specific `decision_impact`, exact `source_ref` ending in `#container`, and `evidence_refs` containing both the artifact path and container source ref.
7. Compute the expected canonical row ID from the five mapped source fields with the existing normalization API. Both malformed and unsupported tests for each artifact must assert that same expected row ID, explicitly proving the problem category, reason, and status cannot churn identity.
8. For the Phase 28 residual non-object-member case, start with the real non-empty producer list, append one non-object, and assert exactly one container-problem row and zero ordinary `phase28_exception_residual_risk_summary` rows. This proves atomic rejection rather than partial publication.
9. For each positive empty-list case, set only the expected producer collection to `[]`, require return code `0`, assert the complete register/views/handoff/report bundle exists, assert no row with that artifact's fixed container `source_subject_id`, and assert no ordinary row for that now-empty producer collection. In particular, `test_phase27_residual_empty_rows_remain_valid` must find neither a `phase27_residual_risk_register` container-problem row nor any ordinary `phase27_residual_risk_register` row. Do not require a blocker merely because a valid collection has zero members.
10. Assert that `blocker-register.json`, `decision-impact-index.json`, `exception-request-register.json`, `residual-risk-request-register.json`, `downstream-handoff-manifest.json`, and `redacted-blocker-register-report.md` all exist. Require handoff `row_count` to equal the canonical register length; negative cases include the emitted container row ID, while positive empty cases exclude the artifact's fixed container row ID.
11. Keep the current valid-row, unknown row-kind/status, demotion-state, provenance, security, mutable-metadata identity, and no-authority assertions unchanged.

The eight negative tests must be RED against the current implementation through assertion failures: missing/mistyped/member failures currently abort before publication, non-object envelopes currently abort at top-level object validation, and incompatible discriminators currently pass through without an unknown container row. The four positive empty-list tests must already pass and remain green. Do not weaken the expected command result or accept an absent handoff.
  </action>
  <acceptance_criteria>
    - All twelve exact negative/positive test names exist under `Phase32ProducerShapeTest`.
    - Each test changes one producer-owned container concern and uses the real producer fixture; missing, mistyped, non-object member, non-object top level, incompatible discriminator, and legitimate empty-list cases are all represented.
    - Each negative test checks the exact mapping, affected gate, category-specific decision impact, evidence refs, critical severity, proof ineligibility, canonical row ID, and full register/view/handoff publication.
    - Each malformed/unsupported pair asserts the same expected canonical row ID for its artifact.
    - Each positive empty-list test proves the complete output bundle is published with no container-problem or ordinary row for that artifact's empty collection; this is explicit for `phase27_residual_risk_register`.
    - RED execution reports all eight negative tests as unittest failures and all four positive tests as passing, with zero unittest errors; syntax, import, missing-file, JSON-decoding, permission, fixture-generation, and other runtime errors are forbidden.
    - No Phase 26, 27, 28, 31, contract, manifest, Bazel, workflow, or `justfile` file is modified.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -m py_compile tools/bazel/phase32_blocker_register_triage_test.py; red_log=$(mktemp); trap "rm -f $red_log" EXIT; negative_tests=(test_phase27_residual_missing_rows_is_malformed test_phase27_residual_non_object_envelope_is_unknown test_phase27_exception_mistyped_rows_is_malformed test_phase27_exception_incompatible_discriminator_is_unknown test_phase28_blocker_missing_blockers_is_malformed test_phase28_blocker_non_object_envelope_is_unknown test_phase28_residual_non_object_member_is_atomic_malformed test_phase28_residual_incompatible_discriminator_is_unknown); positive_tests=(test_phase27_residual_empty_rows_remain_valid test_phase27_exception_empty_rows_remain_valid test_phase28_blocker_empty_blockers_remain_valid test_phase28_residual_empty_rows_remain_valid); args=(); for test_name in "${negative_tests[@]}" "${positive_tests[@]}"; do args+=("Phase32ProducerShapeTest.${test_name}"); done; set +e; python3 tools/bazel/phase32_blocker_register_triage_test.py "${args[@]}" -v &gt;"$red_log" 2&gt;&amp;1; red_status=$?; set -e; test "$red_status" -ne 0; for test_name in "${negative_tests[@]}"; do grep -F "FAIL: ${test_name} (" "$red_log"; done; for test_name in "${positive_tests[@]}"; do grep -E "^${test_name} .* \\.\\.\\. ok$" "$red_log"; done; ! grep -E "^ERROR:|errors=[1-9]|SyntaxError|ImportError|NameError|FileNotFoundError|JSONDecodeError|PermissionError" "$red_log"'</automated>
  </verify>
  <done>Eight producer-shaped RED regressions cover all four collection call sites and both categories, while four green regressions freeze valid empty-list publication for every collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Phase 27/28 container failures into canonical rows</name>
  <read_first>
    - `tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `tools/bazel/phase32_blocker_normalization.py`
    - `tools/bazel/phase32_blocker_normalization_test.py`
    - `tools/bazel/phase32_blocker_register_triage.py`
    - `tools/bazel/phase32_blocker_register_triage_test.py`
    - `tools/bazel/phase27_retained_code_acceptance_decisions.py`
    - `tools/bazel/phase28_final_readiness_packet.py`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-SECURITY.md`
    - `.planning/phases/36-normalize-evidence-and-blocker-rows/36-VERIFICATION.md`
  </read_first>
  <files>
    tools/bazel/phase32_blocker_register_triage.py
    tools/bazel/phase32_blocker_register_triage_test.py
  </files>
  <behavior>
    - All eight Task 1 RED tests become green and all four empty-list positive tests remain green.
    - The Phase 27 `residual-risk-register.json` and `exception-decision-register.json` collection boundaries use the same exact malformed/unsupported rules.
    - The Phase 28 `blocker-summary.json` and `exception-residual-risk-summary.json` collection boundaries use the same exact malformed/unsupported rules.
    - Valid list containers continue through the current item loops with unchanged row IDs, statuses, policy overrides, decision axes, and downstream projections.
    - Path, JSON-read, provenance, contract, identity-collision, security, and output-validation failures keep their existing error behavior and are not converted into ordinary blocker rows.
  </behavior>
  <action>
Implement the smallest producer-specific fix inside `phase32_blocker_register_triage.py`; do not change the Phase 26-31 producer contracts or the Phase 32 contract.

1. Add one shared Phase 27/28 container-loading helper used only by the four exact producer artifacts listed in `<behavior>`. It may parse a JSON value before enforcing the envelope, but it must continue to use the existing safe `read_text`, root-containment, symlink, and JSON error paths. Do not add recursive flattening or a generic permissive loader.
2. Dispatch by the exact artifact path and mapping-table `producer_artifact_kind`. If a top-level object contains `producer_artifact_kind`, accept it only when it equals that expected kind; an explicit incompatible value is an unsupported envelope. The discriminator remains optional so current producer contracts and outputs do not change.
3. Return valid item dictionaries unchanged when the top-level value is an object, any explicit discriminator is compatible, and the expected `rows` or `blockers` field is a list of objects. An empty list is valid for every one of the four producer collections and returns zero items with no container-problem row.
4. Return one atomic container-problem row with `adapter_problem_kind: malformed` when the recognized exact artifact omits its expected collection, supplies a non-list collection, or contains any non-object member. Validate the entire collection before returning items so no valid-looking subset is partially classified.
5. Return one container-problem row with `adapter_problem_kind: unknown_unclassified` only when the top-level JSON value is not an object or carries an explicit incompatible `producer_artifact_kind`. Do not infer rows from alternative keys. Missing collections on these recognized exact artifact paths are `malformed`, never unknown.
6. Build every container problem through the existing `build_blocker_row` path with the exact mapping table, explicit `affected_gate`, exact artifact `evidence_refs`, and `source_ref` ending `#container`. Do not apply the valid-row policy overrides: the existing `malformed` policy must yield `repair_required_before_cutover`, while `unknown_unclassified` must yield `cutover_verdict_blocked`; both remain critical and proof-ineligible.
7. Use the exact fixed five-field source identity and decision identity from the mapping table. Use the same tuple for malformed and unsupported outcomes so category, failure reason, status, source ref, and evidence values cannot churn `row_id`. All values already exist in the current Phase 32 contract enums; do not change identity enums.
8. Replace only the four `require_list`/non-object aborting loops in `phase27_rows` and `phase28_rows`. Keep all valid row-kind, readiness-status, demotion-authorization, policy-override, source-ref, and decision-identity logic byte-for-byte equivalent where practical.
9. Preserve fail-fast behavior for unsafe paths, invalid JSON text, contract drift, duplicate/colliding canonical identities, security scan failures, and output write/validation failures. Container visibility must not become an authority or security bypass.
10. Keep `run_quick`'s existing output transaction and artifact set: validate the combined rows, write `blocker-register.json`, write all three derived views, copy snapshots, write `downstream-handoff-manifest.json`, write the report, then run the security scan.
11. Perform a simplification pass before finishing: one mapping-backed helper and four thin call sites should replace duplicated exception handling; remove any dead branches; do not introduce a new file, dependency, adapter framework, producer-schema change, or Phase 33/34 behavior.
  </action>
  <acceptance_criteria>
    - All twelve Task 1 regressions pass in under 30 seconds: eight negative cases each emit exactly one correctly classified container row, and four legitimate empty-list cases emit none while preserving Phase 32 publication.
    - All four Phase 27/28 collection call sites use the shared helper; none calls `require_list` or directly raises for container member shape.
    - Missing, mistyped, and non-object-member collections are malformed; empty lists are valid; only non-object or explicitly incompatible top-level envelopes are unknown.
    - Every row matches the exact mapping table, affected gate, category-specific decision impact, source/container evidence refs, source tuple, decision tuple, and canonical row ID.
    - Malformed and unsupported variants of each artifact use the same exact source tuple and canonical row ID.
    - Existing valid producer output still yields the same valid-row identity/status/demotion behavior, derived views, handoff fields, report disclaimer, and security result.
    - Contract-only, wiring-only, and security-only modes pass without changing any contract, producer, Bazel, workflow, or `justfile` file.
    - No approval, exception acceptance, retained-code acceptance, residual-risk acceptance, readiness opening, demotion authorization, or cutover verdict is added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -m py_compile tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py; tests=(test_phase27_residual_missing_rows_is_malformed test_phase27_residual_non_object_envelope_is_unknown test_phase27_exception_mistyped_rows_is_malformed test_phase27_exception_incompatible_discriminator_is_unknown test_phase28_blocker_missing_blockers_is_malformed test_phase28_blocker_non_object_envelope_is_unknown test_phase28_residual_non_object_member_is_atomic_malformed test_phase28_residual_incompatible_discriminator_is_unknown test_phase27_residual_empty_rows_remain_valid test_phase27_exception_empty_rows_remain_valid test_phase28_blocker_empty_blockers_remain_valid test_phase28_residual_empty_rows_remain_valid); args=(); for test_name in "${tests[@]}"; do args+=("Phase32ProducerShapeTest.${test_name}"); done; python3 tools/bazel/phase32_blocker_register_triage_test.py "${args[@]}" -q; python3 tools/bazel/phase32_blocker_register_triage.py --contract-only; python3 tools/bazel/phase32_blocker_register_triage.py --security-only'</automated>
  </verify>
  <done>Malformed and unsupported Phase 27/28 containers remain visible as canonical fail-closed rows, and Phase 32 still publishes its complete non-authorizing bundle.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 27/28 producer JSON → container adapter | Untrusted or drifted top-level shape must become a visible fail-closed row without permissive recursive discovery. |
| Container adapter → canonical identity | Mutable shape category and reason text must not alter or collide with the exact producer-container identity. |
| Container problem → Phase 32 bundle | A malformed source must not prevent register/views/handoff publication or bypass row/security validation. |
| Validation error → blocker classification | Only expected producer-container shape problems may become rows; provenance, path, collision, security, and write failures must stay hard failures. |
| Phase 32 bundle → downstream phases | Visible blocker rows must remain proof-ineligible and must not confer decision, readiness, demotion, or cutover authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-36-08 | Tampering / Denial of Service | Phase 27/28 collection envelopes | mitigate | Atomically validate the expected collection and emit one critical `malformed` row instead of raising or partially consuming members. |
| T-36-09 | Spoofing / Elevation of Privilege | Unsupported producer envelope | mitigate | Reject non-object top levels and explicit incompatible artifact discriminators, refuse alternative-key discovery, and emit one critical `unknown_unclassified` row through the exact artifact adapter. |
| T-36-10 | Tampering | Container canonical identity | mitigate | Use one fixed five-field producer-container tuple per artifact and keep category, reason, status, paths, and evidence outside the row-ID input. |
| T-36-11 | Information Disclosure | Container failure projection | mitigate | Persist only safe artifact refs and bounded failure reasons; preserve the recursive secret/forbidden-field scan over all generated outputs. |
| T-36-12 | Elevation of Privilege | Error conversion boundary | mitigate | Convert only expected container-shape failures; retain hard failures for path, provenance, contract, collision, security, and output validation. |
</threat-model>

<verification>
1. Task 1 records the current failure with eight real-producer RED cases, one malformed and one unsupported-envelope concern per collection call site, while four positive empty-list regressions remain green.
2. Task 2 runs all twelve tests plus compilation and contract/security modes as the under-30-second feedback gate.
3. Run scoped pre-commit before the final affected verification matrix so hook edits cannot invalidate later evidence:

```bash
pre-commit run --files tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py
```

4. After pre-commit, run both complete Phase 32 test modules and the affected producer suites:

```bash
python3 tools/bazel/phase32_blocker_normalization_test.py -q
python3 tools/bazel/phase32_blocker_register_triage_test.py -q
python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py -q
python3 tools/bazel/phase28_final_readiness_packet_test.py -q
```

5. Run all Phase 32 contract/security/wiring modes:

```bash
python3 tools/bazel/phase32_blocker_register_triage.py --contract-only
python3 tools/bazel/phase32_blocker_register_triage.py --wiring-only
python3 tools/bazel/phase32_blocker_register_triage.py --security-only
```

6. Run the Bazel targets and full repository-native Phase 32 gate after hooks, then inspect successful output for the canonical register, all derived views, handoff row count/identities, report disclaimer, and passing security scan:

```bash
bazel run //tools/bazel:phase32_verify_tests
bazel run //tools/bazel:phase32_verify
just phase32-verify
```

7. Before committing in this Rust repository, run the mandatory Cargo gate in order:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```

8. Review the final diff after all hooks and formatters:

```bash
git diff --check
git diff --stat -- tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py
git diff -- tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py
```

Confirm the final diff contains only the two declared Phase 32 files and does not modify Phase 26-31 producers/contracts, manifests, Bazel wiring, workflow scripts, `justfile`, planning verification, or configuration files.
</verification>

<success-criteria>
- Both Phase 27 fail-closed container categories publish critical proof-ineligible canonical rows and the complete Phase 32 bundle.
- Both Phase 28 fail-closed container categories publish critical proof-ineligible canonical rows and the complete Phase 32 bundle.
- Valid producer rows and all existing status, demotion, identity, provenance, security, derived-view, and no-authority behavior remain unchanged.
- TRIAGE-01 and TRIAGE-02 re-verification passes without override or manual evidence.
</success-criteria>

<output>
After completion, create `.planning/phases/36-normalize-evidence-and-blocker-rows/36-02-SUMMARY.md`.
</output>
