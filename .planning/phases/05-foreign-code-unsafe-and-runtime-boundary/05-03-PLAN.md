---
phase: 05-foreign-code-unsafe-and-runtime-boundary
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - rust/crates/runtime-adapter/Cargo.toml
  - rust/crates/runtime-adapter/src/lib.rs
  - rust/crates/runtime-adapter/src/startup.rs
  - rust/crates/runtime-adapter/src/linker.rs
  - rust/crates/runtime-adapter/src/allocator.rs
  - rust/crates/runtime-adapter/src/panic_boundary.rs
autonomous: true
requirements:
  - RUST-04
  - CORE-01
requirements_addressed:
  - RUST-04
  - CORE-01
must_haves:
  truths:
    - "Developer can inspect startup, vector, linker-section, allocator, panic/assert, watchdog, and crash-dump contracts without changing retained startup assembly or linker scripts."
    - "STM32F4, STM32G0, and STM32H503 xBuddy Extension startup/linker surfaces remain distinct contract data."
    - "Runtime contracts classify hardware-only startup, watchdog, clock, and crash-dump evidence honestly instead of reporting host checks as hardware proof."
  artifacts:
    - path: "rust/crates/runtime-adapter/src/startup.rs"
      provides: "Startup surface and evidence contracts"
      contains: "StartupSurface"
    - path: "rust/crates/runtime-adapter/src/linker.rs"
      provides: "Linker script and section contracts"
      contains: "BootModeLinkerScript"
    - path: "rust/crates/runtime-adapter/src/allocator.rs"
      provides: "Allocator and heap boundary contracts"
      contains: "AllocatorBoundary"
    - path: "rust/crates/runtime-adapter/src/panic_boundary.rs"
      provides: "Panic, BSOD, assert, watchdog, and crash-dump boundary contracts"
      contains: "PanicBoundary"
  key_links:
    - from: "tools/bazel/manifests/unsafe_boundary_audit.json"
      to: "rust/crates/runtime-adapter/src/startup.rs"
      via: "source_path audit row"
      pattern: "startup-vector-contracts"
    - from: "tools/bazel/manifests/unsafe_boundary_audit.json"
      to: "rust/crates/runtime-adapter/src/linker.rs"
      via: "source_path audit row"
      pattern: "linker-section-contracts"
    - from: "tools/bazel/manifests/unsafe_boundary_audit.json"
      to: "rust/crates/runtime-adapter/src/panic_boundary.rs"
      via: "source_path audit row"
      pattern: "panic-bsod-assert-boundary"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-03T12-58-01
generated_at: 2026-06-03T12:57:57Z
---

<objective>
Build the runtime-side Rust boundary contracts for STM32 startup vectors, linker sections, allocator/heap assumptions, panic/assert/BSOD behavior, watchdog evidence, and crash-dump raw-memory boundaries.

Purpose: Later firmware subsystems can depend on typed runtime bootstrap contracts without mutating retained C/C++/ASM reference code.
Output: Runtime adapter modules with host tests for startup/linker selection, evidence classes, panic/assert/watchdog boundaries, and allocator assumptions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-VALIDATION.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-01-PLAN.md
@tools/bazel/manifests/foreign_code_inventory.json
@tools/bazel/manifests/unsafe_boundary_audit.json
@Cargo.toml
@rust/crates/domain/src/product.rs
@rust/crates/runtime-adapter/Cargo.toml
@rust/crates/runtime-adapter/src/lib.rs
@src/device/stm32f4/startup/CMakeLists.txt
@src/device/stm32g0/startup/CMakeLists.txt
@src/puppy/xbuddy_extension/CMakeLists.txt
@src/puppy/xbuddy_extension/stm32h503.s
@src/puppy/xbuddy_extension/stm32h503.ld
@src/puppy/xbuddy_extension/stm32h503_boot.ld
@src/puppy/xbuddy_extension/stm32h503_noboot.ld
@include/stm32f4_hal/FreeRTOSConfig.h
@include/stm32g0_hal/FreeRTOSConfig.h
@src/puppy/xbuddy_extension/config/FreeRTOSConfig.h
@src/common/crash_dump/dump.cpp
</context>

<interfaces>
From `rust/crates/domain/src/product.rs`:
```rust
pub enum BoardKind {
    Buddy,
    XBuddy,
    XlBuddy,
    Dwarf,
    ModularBed,
    XlDevKitXlBuddy,
    XBuddyExtension,
}

pub enum McuKind {
    Stm32F407Vg,
    Stm32F427Zi,
    Stm32F429Vi,
    Stm32G070RbT6,
    Stm32H503CbU7,
}

pub struct ProductProfile;
impl ProductProfile {
    pub fn board(&self) -> BoardKind;
    pub fn mcu(&self) -> McuKind;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add startup and linker contracts</name>
  <read_first>
    rust/crates/runtime-adapter/Cargo.toml
    rust/crates/runtime-adapter/src/lib.rs
    rust/crates/domain/src/product.rs
    tools/bazel/manifests/foreign_code_inventory.json
    tools/bazel/manifests/unsafe_boundary_audit.json
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
    src/device/stm32f4/startup/CMakeLists.txt
    src/device/stm32g0/startup/CMakeLists.txt
    src/puppy/xbuddy_extension/CMakeLists.txt
    src/puppy/xbuddy_extension/stm32h503.s
    src/puppy/xbuddy_extension/stm32h503.ld
    src/puppy/xbuddy_extension/stm32h503_boot.ld
    src/puppy/xbuddy_extension/stm32h503_noboot.ld
  </read_first>
  <files>rust/crates/runtime-adapter/src/lib.rs, rust/crates/runtime-adapter/src/startup.rs, rust/crates/runtime-adapter/src/linker.rs</files>
  <behavior>
    - Test 1: F4 boot and noboot profiles select F4 startup/linker evidence, not H503 evidence.
    - Test 2: G0 auxiliary profiles select G0 startup/linker evidence.
    - Test 3: xBuddy Extension profiles select H503 xBuddy Extension startup/linker evidence and both `stm32h503_boot.ld` and `stm32h503_noboot.ld` are named.
    - Test 4: Startup/linker contracts expose `ManualHardwareRequired`, `SimulatorFlow`, and `HardwareSmoke` evidence classes without marking hardware behavior locally passed.
  </behavior>
  <action>Add `pub mod startup` and `pub mod linker` in `lib.rs`, and re-export `StartupSurface`, `StartupVectorTable`, `BootModeLinkerScript`, `LinkerSection`, and `EvidenceClass`. Preserve `buddy-domain` and `buddy-application` unsafe-free posture. Do not modify retained startup assembly, linker scripts, CMake runtime source selection, or current C/C++ behavior per D-09, D-10, and D-15. Implement contracts that parse `ProductProfile` into explicit STM32F4, STM32G0, and STM32H503 xBuddy Extension surfaces. H503 must use `src/puppy/xbuddy_extension/stm32h503.s`, `src/puppy/xbuddy_extension/stm32h503.ld`, `src/puppy/xbuddy_extension/stm32h503_boot.ld`, and `src/puppy/xbuddy_extension/stm32h503_noboot.ld`; do not create a generic H5 source path per D-11. `EvidenceClass` must include `ManifestCheck`, `StaticSourceAudit`, `RustHostTest`, `BazelQuery`, `SimulatorFlow`, `HardwareSmoke`, and `ManualHardwareRequired` per D-08 and D-12.</action>
  <verify>
    <automated>cargo test --all-features -p buddy-runtime-adapter &amp;&amp; python3 tools/bazel/phase5_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --all-features -p buddy-runtime-adapter` exits 0.
    - `rg "StartupSurface|BootModeLinkerScript|stm32h503_boot.ld|stm32h503_noboot.ld|ManualHardwareRequired" rust/crates/runtime-adapter/src` finds all five strings.
    - `python3 tools/bazel/phase5_verify.py --quick` exits 0.
  </acceptance_criteria>
  <done>Runtime startup/linker contracts exist, pass host tests, and preserve retained startup/linker files as named audit surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add allocator, panic, watchdog, and crash-dump contracts</name>
  <read_first>
    rust/crates/runtime-adapter/Cargo.toml
    rust/crates/runtime-adapter/src/lib.rs
    tools/bazel/manifests/unsafe_boundary_audit.json
    include/stm32f4_hal/FreeRTOSConfig.h
    include/stm32g0_hal/FreeRTOSConfig.h
    src/puppy/xbuddy_extension/config/FreeRTOSConfig.h
    src/common/crash_dump/dump.cpp
    src/common/wdt.cpp
  </read_first>
  <files>rust/crates/runtime-adapter/Cargo.toml, rust/crates/runtime-adapter/src/lib.rs, rust/crates/runtime-adapter/src/allocator.rs, rust/crates/runtime-adapter/src/panic_boundary.rs</files>
  <behavior>
    - Test 1: Allocator boundary contracts reject zero heap size and distinguish static-only from heap-backed runtime assumptions.
    - Test 2: Panic/assert boundary contracts include `configASSERT`, `_bsod`, and `fatal_error` evidence strings.
    - Test 3: Watchdog and crash-dump contracts include `watchdog-boundary` and `crash-dump-memory-boundary` audit surface IDs.
    - Test 4: Hardware-only watchdog and crash-dump behavior is classified as `ManualHardwareRequired`, `SimulatorFlow`, or `HardwareSmoke`, not local proof.
  </behavior>
  <action>Add `pub mod allocator` and `pub mod panic_boundary` in `lib.rs`, and re-export `AllocatorBoundary`, `PanicBoundary`, `WatchdogBoundary`, and `CrashDumpBoundary`. Preserve pure crate `#![forbid(unsafe_code)]` settings. Do not mutate retained C/C++ assert, BSOD, fatal error, watchdog, or crash-dump behavior per D-08, D-12, and D-15. Allocator contracts must name static-memory versus heap-backed assumptions without introducing allocation behavior. Panic/assert contracts must name `configASSERT`, `_bsod`, `fatal_error`, watchdog refresh/init surfaces, and crash-dump raw-memory collection. Use typed evidence classes from Task 1 and keep hardware proof classified as non-local.</action>
  <verify>
    <automated>cargo test --all-features -p buddy-runtime-adapter &amp;&amp; python3 tools/bazel/phase5_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --all-features -p buddy-runtime-adapter` exits 0.
    - `rg "AllocatorBoundary|PanicBoundary|WatchdogBoundary|CrashDumpBoundary|panic-bsod-assert-boundary|watchdog-boundary|crash-dump-memory-boundary" rust/crates/runtime-adapter/src` finds all seven strings.
    - `python3 tools/bazel/phase5_verify.py --quick` exits 0.
  </acceptance_criteria>
  <done>Runtime allocator, panic/assert, watchdog, and crash-dump contracts exist, pass host tests, and keep hardware-only behavior evidence-classed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain profile -> runtime adapter | Validated product profiles select startup, linker, allocator, and panic/watchdog contracts. |
| runtime adapter -> retained startup/linker/panic code | Rust contracts describe retained C/C++/ASM runtime behavior without changing it. |
| host tests -> MCU startup/watchdog behavior | Host tests verify contract logic, not reset flow, interrupt vector behavior, clock behavior, or watchdog behavior on MCU hardware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering / Elevation of privilege | Linker symbols and startup vectors | mitigate | `startup.rs` and `linker.rs` must name STM32F4, STM32G0, and H503 xBuddy Extension source paths and evidence classes; no retained linker/startup file is edited. |
| T-05-03-02 | Denial of service / Repudiation | Board clock tree and startup timing evidence | transfer | Plan 02 owns `BoardClockTree`; this plan must keep startup evidence linked to `board-clock-tree-contracts` and avoid local hardware-proof claims. |
| T-05-03-03 | Denial of service / Repudiation | Panic/assert/watchdog boundaries | mitigate | `panic_boundary.rs` must name `configASSERT`, `_bsod`, `fatal_error`, watchdog, and crash-dump evidence without claiming local hardware pass. |
| T-05-03-04 | Information disclosure / Tampering | Crash dump memory collection | mitigate | Crash-dump memory ranges must remain an explicit `crash-dump-memory-boundary` audit surface with non-local evidence classes. |
| T-05-03-05 | Repudiation | Hardware evidence overclaiming | mitigate | Runtime contract tests must use `EvidenceClass` and retain `ManualHardwareRequired`, `SimulatorFlow`, and `HardwareSmoke` variants for non-local checks. |
</threat_model>

<verification>
- `cargo test --all-features -p buddy-runtime-adapter`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `python3 tools/bazel/phase5_verify.py --quick`
</verification>

<success_criteria>
Plan 03 succeeds when runtime adapter startup/linker/allocator/panic modules compile and pass host tests, F4/G0/H503 startup/linker distinctions are explicit, panic/watchdog/crash-dump boundaries are typed, and hardware-only runtime behavior remains evidence-classed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-03-SUMMARY.md`.
</output>
