---
phase: 05-foreign-code-unsafe-and-runtime-boundary
plan: 05
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - BUILD.bazel
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - tools/bazel/phase5_verify.py
  - justfile
autonomous: true
requirements:
  - RUST-03
  - RUST-04
  - CORE-01
  - CORE-02
requirements_addressed:
  - RUST-03
  - RUST-04
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - "Developer can run Phase 5 verification through `bazel run //tools/bazel:phase5_verify` and `just phase5-verify`."
    - "Developer can query retained-code inventory and unsafe-boundary audit labels through Bazel."
    - "The verifier checks adapter modules, board-clock contracts, synchronization contracts, unsafe locality, manifest coverage, Bazel labels, just recipes, and Rust fmt/lint/build/tests."
    - "Pure crates remain unsafe-free while audited adapter crates are the only allowed unsafe surfaces."
  artifacts:
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 5 Bazel labels and manifest filegroups"
      contains: "phase5_verify"
    - path: "BUILD.bazel"
      provides: "Root aliases for Phase 5 labels"
      contains: "phase5_verify"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Bazel-run dispatcher for Phase 5 verification"
      contains: "phase5_verify)"
    - path: "tools/bazel/phase5_verify.py"
      provides: "Full Phase 5 local verifier"
      contains: "check_adapter_surface"
    - path: "justfile"
      provides: "Developer facade for Phase 5 verification"
      contains: "phase5-verify:"
  key_links:
    - from: "justfile"
      to: "//tools/bazel:phase5_verify"
      via: "phase5-verify recipe"
      pattern: "bazel run //tools/bazel:phase5_verify"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase5_verify.py"
      via: "phase5_verify case"
      pattern: "python3 tools/bazel/phase5_verify.py --all"
    - from: "tools/bazel/phase5_verify.py"
      to: "rust/crates/board-adapter/src/clock.rs"
      via: "required adapter module check"
      pattern: "board-clock-tree-contracts"
    - from: "tools/bazel/phase5_verify.py"
      to: "rust/crates/runtime-adapter/src/synchronization.rs"
      via: "required adapter module check"
      pattern: "freertos-event-group-contracts"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-03T12-58-01
generated_at: 2026-06-03T12:57:57Z
---

<objective>
Expose Phase 5 inventory, audit, adapter contracts, and verification through Bazel and the `justfile`, then harden the verifier against adapter drift and hardware-evidence overclaiming.

Purpose: Developers get one authoritative local gate for RUST-03, RUST-04, CORE-01, and CORE-02 before later subsystem phases build on these boundaries.
Output: Phase 5 Bazel labels, root aliases, `just phase5-verify`, and a full verifier that runs static checks plus Rust checks.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-VALIDATION.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-01-PLAN.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-02-PLAN.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-03-PLAN.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-04-PLAN.md
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@tools/bazel/phase5_verify.py
@justfile
@Cargo.toml
@rust/crates/domain/src/lib.rs
@rust/crates/application/src/lib.rs
@rust/crates/board-adapter/src/lib.rs
@rust/crates/board-adapter/src/clock.rs
@rust/crates/runtime-adapter/src/lib.rs
@rust/crates/runtime-adapter/src/synchronization.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 5 Bazel labels and just recipe</name>
  <read_first>
    tools/bazel/BUILD.bazel
    BUILD.bazel
    tools/bazel/rust_workflow.sh
    justfile
    tools/bazel/shell_rules.bzl
    tools/bazel/manifests/foreign_code_inventory.json
    tools/bazel/manifests/unsafe_boundary_audit.json
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
  </read_first>
  <files>BUILD.bazel, tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, justfile</files>
  <action>Update `tools/bazel/rust_workflow.sh` with a `phase5_verify)` case that runs exactly `python3 tools/bazel/phase5_verify.py --all`. In root `BUILD.bazel`, add `filegroup(name = "phase5_runtime_boundary_docs", srcs = [".planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md", ".planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md"])` plus aliases for `phase5_verify`, `retained_foreign_code`, and `unsafe_boundary_audit`. In `tools/bazel/BUILD.bazel`, add `shell_binary(name = "phase5_verify", src = "rust_workflow.sh", data = ["phase5_verify.py", "manifests/foreign_code_inventory.json", "manifests/unsafe_boundary_audit.json", "//:phase5_runtime_boundary_docs", "//:rust_workspace_sources"])`. Add `filegroup(name = "unsafe_boundary_audit", srcs = ["manifests/unsafe_boundary_audit.json"])`. Update the existing runnable `retained_foreign_code` target so its runfiles include `manifests/foreign_code_inventory.json` and `//:phase5_runtime_boundary_docs` while preserving `src = "reference_contract.sh"`. In `justfile`, add `phase5-verify:` with exactly `bazel run //tools/bazel:phase5_verify`. Do not remove existing Phase 2 through Phase 4 labels or recipes.</action>
  <verify>
    <automated>bazel query "//tools/bazel:phase5_verify + //tools/bazel:retained_foreign_code + //tools/bazel:unsafe_boundary_audit" &amp;&amp; just --list</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query "//tools/bazel:phase5_verify + //tools/bazel:retained_foreign_code + //tools/bazel:unsafe_boundary_audit"` exits 0.
    - `just --list` output contains `phase5-verify`.
    - `rg "phase5_verify|unsafe_boundary_audit|retained_foreign_code" BUILD.bazel tools/bazel/BUILD.bazel` finds all three strings.
    - `rg "phase5-verify:|bazel run //tools/bazel:phase5_verify" justfile` finds both strings.
  </acceptance_criteria>
  <done>Phase 5 verifier, retained-code inventory, and unsafe audit labels are queryable through Bazel, and `just phase5-verify` is discoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden verifier for adapters, labels, and evidence claims</name>
  <read_first>
    tools/bazel/phase5_verify.py
    tools/bazel/BUILD.bazel
    BUILD.bazel
    tools/bazel/rust_workflow.sh
    justfile
    Cargo.toml
    rust/crates/domain/src/lib.rs
    rust/crates/application/src/lib.rs
    rust/crates/board-adapter/Cargo.toml
    rust/crates/board-adapter/src/lib.rs
    rust/crates/runtime-adapter/Cargo.toml
    rust/crates/runtime-adapter/src/lib.rs
    tools/bazel/manifests/foreign_code_inventory.json
    tools/bazel/manifests/unsafe_boundary_audit.json
  </read_first>
  <files>tools/bazel/phase5_verify.py</files>
  <behavior>
    - Test 1: `--quick` requires all board adapter modules from Plan 02 and all runtime adapter modules from Plans 03-04.
    - Test 2: `--quick` fails if `buddy-domain` or `buddy-application` lacks `#![forbid(unsafe_code)]`.
    - Test 3: `--quick` fails if an unsafe operation, `unsafe extern`, or adapter unsafe allowance appears outside allowed audited adapter files.
    - Test 4: `--quick` requires board-clock and synchronization audit surface IDs.
    - Test 5: `--all` runs quick checks, Cargo format, clippy, build, and tests.
  </behavior>
  <action>Extend `tools/bazel/phase5_verify.py` with functions named `check_adapter_surface`, `check_bazel_surface`, `check_just_surface`, and `check_no_hardware_overclaim`. `check_adapter_surface` must require these files: `rust/crates/board-adapter/src/mcu.rs`, `clock.rs`, `memory_region.rs`, `dma.rs`, `mmio.rs`, `interrupt.rs`, `ffi.rs`, and `rust/crates/runtime-adapter/src/startup.rs`, `linker.rs`, `allocator.rs`, `panic_boundary.rs`, `static_memory.rs`, `task.rs`, `queue.rs`, `timer.rs`, `synchronization.rs`. It must require strings `#![forbid(unsafe_code)]` in `rust/crates/domain/src/lib.rs` and `rust/crates/application/src/lib.rs`. It must require audit surface IDs `board-clock-tree-contracts`, `freertos-mutex-contracts`, `freertos-binary-semaphore-contracts`, `freertos-counting-semaphore-contracts`, `freertos-event-group-contracts`, and `freertos-wait-condition-contracts`. It must scan `rust/crates/**/*.rs` for unsafe operations, `unsafe extern`, `#[unsafe(...)]`, or adapter unsafe allowances and fail if any finding appears outside audited adapter files or lacks a matching `source_path` in `tools/bazel/manifests/unsafe_boundary_audit.json`. The scan must ignore pure-crate `#![forbid(unsafe_code)]` lines because those lines enforce the unsafe-free posture. `check_bazel_surface` must require `phase5_verify`, `retained_foreign_code`, and `unsafe_boundary_audit` in Bazel files. `check_just_surface` must require `phase5-verify:` and `bazel run //tools/bazel:phase5_verify` in `justfile`. `check_no_hardware_overclaim` must fail on strings `hardware-safe`, `hardware passed`, `hardware verified locally`, or `locally passed hardware` in Phase 5 manifests and Markdown artifacts. Preserve `--inventory-only` and `--audit-only`; `--quick` must run all static checks; `--all` must run quick plus Cargo format, clippy, build, and tests.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase5_verify.py &amp;&amp; python3 tools/bazel/phase5_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile tools/bazel/phase5_verify.py` exits 0.
    - `python3 tools/bazel/phase5_verify.py --quick` exits 0 and prints `Phase 5 runtime boundary verification passed`.
    - `rg "check_adapter_surface|check_bazel_surface|check_just_surface|check_no_hardware_overclaim" tools/bazel/phase5_verify.py` finds all four function names.
    - `python3 tools/bazel/phase5_verify.py --quick` exits 0 after all Phase 5 adapter and Bazel files are present.
  </acceptance_criteria>
  <done>The Phase 5 verifier enforces manifest coverage, adapter module presence, pure-crate unsafe-free posture, audited unsafe locality, board-clock/synchronization surfaces, Bazel labels, just recipe discoverability, and local versus hardware evidence boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel/just facade -> verifier | Developer-visible commands must run the same Phase 5 checks as direct Python invocation. |
| verifier -> Rust adapter crates | Static checks gate unsafe locality and required adapter contracts before later phases depend on them. |
| local verification -> non-local evidence | Aggregate verification must not represent simulator or hardware work as locally passed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering / Elevation of privilege | FFI/linker unsafe scan | mitigate | `check_adapter_surface` must fail on unaudited unsafe occurrences and require matching audit `source_path` entries. |
| T-05-05-02 | Tampering / Denial of service | MMIO/DMA adapter drift | mitigate | Verifier must require Plan 02 board modules and manifest rows for `mmio`, `dma`, `interrupt`, and `board-clock-tree-contracts` surfaces before `--quick` passes. |
| T-05-05-03 | Denial of service | Task dependency and synchronization drift | mitigate | Verifier must require Plan 04 runtime modules and audit rows for `task-dependency`, `static-memory`, `queue`, `timer`, `mutex`, `semaphore`, `event-group`, and `wait-condition`. |
| T-05-05-04 | Denial of service / Repudiation | Panic/assert/watchdog boundary drift | mitigate | Verifier must require `panic_boundary.rs` plus audit strings for `panic-bsod-assert-boundary` and `watchdog-boundary`. |
| T-05-05-05 | Information disclosure / Tampering | Retained vendor inventory hidden from Bazel | mitigate | Bazel labels must expose `retained_foreign_code` and manifest runfiles, and `phase5_verify` must validate the retained-code inventory. |
| T-05-05-06 | Repudiation | Hardware evidence overclaiming | mitigate | `check_no_hardware_overclaim` must reject local-pass language and keep `simulator-flow`, `hardware-smoke`, and `manual-hardware-required` as evidence classes only. |
</threat_model>

<verification>
- `bazel query "//tools/bazel:phase5_verify + //tools/bazel:retained_foreign_code + //tools/bazel:unsafe_boundary_audit"`
- `just --list`
- `python3 tools/bazel/phase5_verify.py --quick`
- `just phase5-verify`
- `just rust-format`
- `just rust-lint`
- `just rust-build`
- `just rust-test`
</verification>

<success_criteria>
Plan 05 succeeds when Phase 5 is visible through Bazel and `just`, the quick verifier passes direct invocation, the aggregate `just phase5-verify` phase gate passes once, Rust checks pass through the existing facade, and no local verification artifact overclaims simulator or hardware evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-05-SUMMARY.md`.
</output>
