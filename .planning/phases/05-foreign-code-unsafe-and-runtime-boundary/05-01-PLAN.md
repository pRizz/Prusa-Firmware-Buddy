---
phase: 05-foreign-code-unsafe-and-runtime-boundary
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/bazel/phase5_verify.py
  - tools/bazel/manifests/foreign_code_inventory.json
  - tools/bazel/manifests/unsafe_boundary_audit.json
  - .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md
  - .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
autonomous: true
requirements:
  - RUST-03
  - RUST-04
  - CORE-01
  - CORE-02
requirements_addressed:
  - RUST-03
  - RUST-04
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - "Developer can run a local Phase 5 verifier before adapter implementation depends on manifests."
    - "Developer can inspect every retained C, C++, ASM, generated, vendor, startup, HAL, RTOS, Marlin, WUI/network/filesystem, and auxiliary runtime island by explicit inventory row."
    - "Developer can inspect every unsafe, FFI, MMIO, DMA, interrupt, linker-symbol, static-memory, allocator, panic/assert, mutable-static, and crash-dump boundary by explicit audit row."
    - "Local verification distinguishes manifest/static checks from simulator, hardware-smoke, and manual-hardware-required evidence."
  artifacts:
    - path: "tools/bazel/phase5_verify.py"
      provides: "Phase 5 static verifier and schema gates"
      contains: "Phase 5 runtime boundary verification passed"
    - path: "tools/bazel/manifests/foreign_code_inventory.json"
      provides: "Machine-readable retained foreign-code inventory"
      contains: "stm32h503-xbuddy-extension-startup-linker"
    - path: "tools/bazel/manifests/unsafe_boundary_audit.json"
      provides: "Machine-readable unsafe and runtime-boundary audit"
      contains: "panic-bsod-assert-boundary"
    - path: ".planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md"
      provides: "Human-readable retained-code inventory"
      contains: "xBuddy Extension STM32H503"
    - path: ".planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md"
      provides: "Human-readable unsafe/runtime audit"
      contains: "manual-hardware-required"
  key_links:
    - from: "tools/bazel/phase5_verify.py"
      to: "tools/bazel/manifests/foreign_code_inventory.json"
      via: "JSON schema validation"
      pattern: "foreign_code_inventory.json"
    - from: "tools/bazel/phase5_verify.py"
      to: "tools/bazel/manifests/unsafe_boundary_audit.json"
      via: "JSON schema validation"
      pattern: "unsafe_boundary_audit.json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-03T12-58-01
generated_at: 2026-06-03T12:57:57Z
---

<objective>
Create the Phase 5 validation foundation: a standard-library verifier, a retained foreign-code inventory, and an unsafe/runtime-boundary audit that later adapter work must satisfy.

Purpose: Wave 0 removes missing-verifier risk before implementation tasks rely on manifest and audit infrastructure.
Output: `tools/bazel/phase5_verify.py`, two machine-readable manifests, and two human-readable phase artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-VALIDATION.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md
@.planning/phases/04-rust-architecture-and-invariant-model/04-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@tools/bazel/phase4_verify.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 5 verifier schema gates</name>
  <read_first>
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-VALIDATION.md
    tools/bazel/phase4_verify.py
    AGENTS.md
    AGENTS.bright-builds.md
    standards-overrides.md
  </read_first>
  <files>tools/bazel/phase5_verify.py</files>
  <behavior>
    - Test 1: `--help` lists `--quick`, `--all`, `--inventory-only`, and `--audit-only`.
    - Test 2: `--inventory-only` fails with a readable missing-file error before the inventory manifest exists.
    - Test 3: `--audit-only` fails with a readable missing-file error before the unsafe audit manifest exists.
  </behavior>
  <action>Create `tools/bazel/phase5_verify.py` as a Python standard-library verifier modeled on `tools/bazel/phase4_verify.py`. Define `REQUIRED_INVENTORY_FIELDS` exactly as `id`, `path`, `kind`, `language`, `source_version_evidence`, `ownership_boundary`, `retention_reason`, `safe_facade`, `replacement_posture`, `risk_class`, `evidence_required`, `bazel_label`, and `requirements` per D-01 through D-04. Define `REQUIRED_UNSAFE_FIELDS` exactly as `surface_id`, `crate`, `module`, `source_path`, `kind`, `raw_operation`, `invariant`, `safe_facade`, `test_or_static_check`, `evidence_class`, `review_status`, and `requirements` per D-05 through D-08. Implement `--quick` to run both manifest checks, `--inventory-only` for only `tools/bazel/manifests/foreign_code_inventory.json`, `--audit-only` for only `tools/bazel/manifests/unsafe_boundary_audit.json`, and `--all` as `--quick` plus Cargo format, clippy, build, and test commands. Default to `--quick` when no mode is supplied. The success line must be exactly `Phase 5 runtime boundary verification passed`. Use no third-party Python packages.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase5_verify.py &amp;&amp; python3 tools/bazel/phase5_verify.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile tools/bazel/phase5_verify.py` exits 0.
    - `python3 tools/bazel/phase5_verify.py --help` output contains `--quick`, `--all`, `--inventory-only`, and `--audit-only`.
    - `rg "REQUIRED_INVENTORY_FIELDS|REQUIRED_UNSAFE_FIELDS|Phase 5 runtime boundary verification passed" tools/bazel/phase5_verify.py` finds all three strings.
  </acceptance_criteria>
  <done>The verifier exists, compiles, exposes all required modes, and can fail fast before Wave 0 manifests are created.</done>
</task>

<task type="auto">
  <name>Task 2: Create retained foreign-code inventory</name>
  <read_first>
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
    .planning/codebase/STRUCTURE.md
    .planning/codebase/INTEGRATIONS.md
    ProjectOptions.cmake
    CMakeLists.txt
    src/CMakeLists.txt
    src/device/CMakeLists.txt
    src/device/stm32f4/CMakeLists.txt
    src/device/stm32g0/CMakeLists.txt
    src/puppy/xbuddy_extension/CMakeLists.txt
    lib/CMakeLists.txt
  </read_first>
  <files>tools/bazel/manifests/foreign_code_inventory.json, .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md</files>
  <action>Create `tools/bazel/manifests/foreign_code_inventory.json` with a top-level object containing `schema_version: 1`, `phase: "05-foreign-code-unsafe-and-runtime-boundary"`, and `components: []`. Add explicit component rows for at least these IDs: `stm32f4-startup-linker`, `stm32g0-startup-linker`, `stm32h503-xbuddy-extension-startup-linker`, `stm32-hal-cmsis`, `freertos-kernel`, `marlin-reference-core`, `wui-network-stack`, `lwip-network-stack`, `mbedtls-tls`, `fatfs-media`, `littlefs-internal`, `tinyusb-device`, `prusa-mmu`, `prusa-error-codes`, `libbgcode`, `esp-network-firmware`, `generated-runtime-assets`, and `puppy-auxiliary-runtime`. Every row must include every `REQUIRED_INVENTORY_FIELDS` key from Task 1. `requirements` must include the relevant IDs from `RUST-03`, `CORE-01`, and `CORE-02`. Evidence for STM32F4 must mention `src/device/stm32f4/startup/` and `src/device/stm32f4/linker/`; evidence for STM32G0 must mention `src/device/stm32g0/startup/` and `src/device/stm32g0/linker/`; evidence for STM32H503 must mention `src/puppy/xbuddy_extension/stm32h503.s`, `src/puppy/xbuddy_extension/stm32h503.ld`, `src/puppy/xbuddy_extension/stm32h503_boot.ld`, and `src/puppy/xbuddy_extension/stm32h503_noboot.ld` per D-09 through D-11. Create `.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md` with a Markdown table containing the same row IDs and columns named `Component`, `Path`, `Kind`, `Source/version evidence`, `Ownership boundary`, `Retention reason`, `Safe Rust facade`, `Replacement posture`, `Risk class`, and `Evidence required`. Do not classify printing-core, persistence, GUI, network behavior, or auxiliary behavior parity as implemented; inventory them only when retained runtime code is visible per D-01 and the deferred scope.</action>
  <verify>
    <automated>python3 tools/bazel/phase5_verify.py --inventory-only</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase5_verify.py --inventory-only` exits 0.
    - `rg "stm32h503-xbuddy-extension-startup-linker|stm32f4-startup-linker|stm32g0-startup-linker|manual-hardware-required" tools/bazel/manifests/foreign_code_inventory.json` finds all four strings.
    - `rg "xBuddy Extension STM32H503|lib/Marlin|lib/WUI|lib/Middlewares/Third_Party/FreeRTOS" .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md` finds all four strings.
  </acceptance_criteria>
  <done>The retained-code inventory is machine-readable, human-readable, covers the required retained families, and treats H503/xBuddy Extension as a distinct surface.</done>
</task>

<task type="auto">
  <name>Task 3: Create unsafe and runtime-boundary audit</name>
  <read_first>
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-CONTEXT.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-RESEARCH.md
    tools/bazel/phase5_verify.py
    include/tasks.hpp
    src/freertos/system_tasks.cpp
    src/common/Pin.cpp
    src/common/crash_dump/dump.cpp
    include/stm32f4_hal/FreeRTOSConfig.h
    include/stm32g0_hal/FreeRTOSConfig.h
    src/puppy/xbuddy_extension/config/FreeRTOSConfig.h
  </read_first>
  <files>tools/bazel/manifests/unsafe_boundary_audit.json, .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md</files>
  <action>Create `tools/bazel/manifests/unsafe_boundary_audit.json` with a top-level object containing `schema_version: 1`, `phase: "05-foreign-code-unsafe-and-runtime-boundary"`, and `surfaces: []`. Add audit rows for at least these `kind` values: `ffi`, `mmio`, `dma`, `interrupt`, `linker-symbol`, `startup-vector`, `static-memory`, `allocator`, `panic-boundary`, `mutable-static`, `crash-dump-memory`, `task-dependency`, `queue`, `timer`, and `watchdog`. Required `surface_id` values must include `ffi-symbol-contracts`, `linker-section-contracts`, `startup-vector-contracts`, `mmio-register-contracts`, `dma-buffer-contracts`, `interrupt-registration-contracts`, `static-task-memory-contracts`, `allocator-heap-contracts`, `panic-bsod-assert-boundary`, `mutable-static-boundary`, `crash-dump-memory-boundary`, `task-dependency-readiness`, `freertos-queue-contracts`, `freertos-timer-contracts`, and `watchdog-boundary`. Every row must include every `REQUIRED_UNSAFE_FIELDS` key from Task 1. Use `evidence_class` values from the verifier allow-list only: `manifest-check`, `static-source-audit`, `rust-host-test`, `bazel-query`, `simulator-flow`, `hardware-smoke`, or `manual-hardware-required`. The audit must explicitly state that host checks do not prove MCU clock, interrupt, DMA, watchdog, or scheduler timing behavior per D-08 and D-12. Create `.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md` with a Markdown table using the same `surface_id` values and a short `Local vs non-local evidence` section that contains the exact strings `local manifest/static/Rust checks`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`.</action>
  <verify>
    <automated>python3 tools/bazel/phase5_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase5_verify.py --quick` exits 0 and prints `Phase 5 runtime boundary verification passed`.
    - `rg "panic-bsod-assert-boundary|task-dependency-readiness|manual-hardware-required|hardware-smoke" tools/bazel/manifests/unsafe_boundary_audit.json` finds all four strings.
    - `rg "local manifest/static/Rust checks|simulator-flow|hardware-smoke|manual-hardware-required" .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md` finds all four strings.
  </acceptance_criteria>
  <done>The unsafe/runtime audit exists, verifier quick mode passes, and hardware-only claims are evidence-classed instead of locally passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest -> verifier | Untrusted JSON or Markdown can overclaim retained-code or hardware evidence unless schema and evidence classes are enforced. |
| Rust adapter plan -> retained C/ASM/vendor code | Adapter work will later consume retained startup, HAL, RTOS, and vendor surfaces named by this inventory. |
| local checks -> hardware evidence | Host checks can verify contracts and manifests, not physical MCU behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering / Elevation of privilege | FFI and linker-symbol manifest rows | mitigate | `phase5_verify.py --quick` must require `ffi`, `linker-symbol`, and `startup-vector` audit rows with `source_path`, `invariant`, and `safe_facade` fields before later plans add adapter code. |
| T-05-01-02 | Tampering / Denial of service | MMIO and DMA boundary rows | mitigate | Require `mmio` and `dma` audit rows, require evidence for CCMRAM versus DMA-visible memory, and reject missing `evidence_class`. |
| T-05-01-03 | Denial of service | FreeRTOS task dependency readiness | mitigate | Require `task-dependency`, `static-memory`, `queue`, and `timer` audit rows tied to `include/tasks.hpp` and `src/freertos/system_tasks.cpp`. |
| T-05-01-04 | Denial of service / Repudiation | Panic, assert, watchdog, and crash-dump boundaries | mitigate | Require audit rows for `panic-bsod-assert-boundary`, `watchdog-boundary`, and `crash-dump-memory-boundary` with non-local evidence classes where hardware behavior is involved. |
| T-05-01-05 | Information disclosure / Tampering | Retained vendor and generated code inventory | mitigate | Inventory rows must name retained `lib/`, generated, WUI/network/filesystem, HAL/CMSIS, FreeRTOS, Marlin, and auxiliary surfaces instead of allowing anonymous glob-only dependencies. |
| T-05-01-06 | Repudiation | Hardware evidence overclaiming | mitigate | Verifier and audit docs must distinguish `manifest-check`, `rust-host-test`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`; local checks must not mark hardware-only behavior as passed. |
</threat_model>

<verification>
- `python3 tools/bazel/phase5_verify.py --quick`
- `rg "RUST-03|RUST-04|CORE-01|CORE-02" tools/bazel/manifests/foreign_code_inventory.json tools/bazel/manifests/unsafe_boundary_audit.json`
- `rg "manual-hardware-required" .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md`
</verification>

<success_criteria>
Wave 0 is complete when the Phase 5 verifier exists, both manifests and both Markdown artifacts exist, `python3 tools/bazel/phase5_verify.py --quick` passes, every Phase 5 requirement ID appears in the manifests, and no manifest or audit text claims hardware-only behavior passed locally.
</success_criteria>

<output>
After completion, create `.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-01-SUMMARY.md`.
</output>
