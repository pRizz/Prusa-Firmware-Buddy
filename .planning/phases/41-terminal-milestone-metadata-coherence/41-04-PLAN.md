---
phase: 41-terminal-milestone-metadata-coherence
plan: 04
type: execute
wave: 4
depends_on:
  - 41-03
files_modified:
  - tools/bazel/phase41_terminal_consistency_contracts.py
  - tools/bazel/phase41_terminal_consistency_policy.py
  - tools/bazel/phase41_terminal_consistency_test_support.py
  - tools/bazel/phase41_terminal_consistency_projection_test.py
  - tools/bazel/phase41_terminal_consistency_markdown.py
  - tools/bazel/phase41_terminal_consistency_projection_parser.py
  - tools/bazel/phase41_terminal_consistency.py
  - tools/bazel/phase41_terminal_consistency_projection_boundary_test.py
  - tools/bazel/BUILD.bazel
autonomous: true
gap_closure: true
requirements:
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
  - READY-02
  - READY-03
  - CUTOVER-01
  - CUTOVER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 41-2026-08-01T16-27-53
generated_at: 2026-08-01T19:44:47.929Z
must_haves:
  truths:
    - "Changing any one exact REQUIREMENTS coverage value changes checker output and emits a deterministic path-qualified P41_REQUIREMENTS_COVERAGE_* violation derived from the sixteen canonical requirement identities."
    - "Changing the bottom ROADMAP Progress Phase 41 completed/total/status projection, v1.3 totals/status, or execution-order projection changes checker output and emits a deterministic path-qualified P41_ROADMAP_PROGRESS_* or P41_ROADMAP_EXECUTION_PROJECTION violation."
    - "Changing any required nested audit scores, integration-checker totals, or Nyquist projection changes checker output and emits a deterministic path-qualified P41_AUDIT_* violation instead of relying only on top-level status and body tables."
    - "Pre-audit continues to accept a truthful active Phase 41 projection and pre-archive continues to require terminal completion, independent passed verification, fresh audit ordering, and exact PLAN/SUMMARY pairing."
    - "Every Phase 41 test surface passes directly and through Bazel while the managed Bright Builds checker and pre-existing .planning/config.json modification remain byte-for-byte untouched."
  artifacts:
    - path: tools/bazel/phase41_terminal_consistency_contracts.py
      provides: Normalized exact coverage, progress/execution, and nested audit projection contracts
      contains: "RequirementsCoverageProjection"
    - path: tools/bazel/phase41_terminal_consistency_projection_parser.py
      provides: Strict live Markdown/frontmatter adapters for the three omitted projection families
      contains: "parse_requirements_coverage"
    - path: tools/bazel/phase41_terminal_consistency_policy.py
      provides: Evidence-derived mode-aware comparison rules and stable P41 codes
      contains: "evaluate_terminal_consistency"
    - path: tools/bazel/phase41_terminal_consistency_projection_test.py
      provides: Pure one-concern policy mutations for every new normalized field
      contains: "class Phase41ProjectionPolicyTest"
    - path: tools/bazel/phase41_terminal_consistency_projection_boundary_test.py
      provides: Isolated live-boundary disconfirmation tests matching the verifier report
      contains: "class Phase41ProjectionBoundaryTest"
  key_links:
    - from: tools/bazel/phase41_terminal_consistency.py
      to: tools/bazel/phase41_terminal_consistency_projection_parser.py
      via: load_snapshot parses the omitted live Markdown projections before pure evaluation
      pattern: "parse_(requirements_coverage|roadmap_progress|audit_frontmatter)"
    - from: tools/bazel/phase41_terminal_consistency_projection_parser.py
      to: tools/bazel/phase41_terminal_consistency_contracts.py
      via: raw strings and nested frontmatter become immutable normalized records
      pattern: "(RequirementsCoverageProjection|RoadmapProgressProjection|AuditFrontmatterProjection)"
    - from: tools/bazel/phase41_terminal_consistency_policy.py
      to: tools/bazel/phase41_terminal_consistency_contracts.py
      via: exact evidence-derived expectations compared with normalized projections
      pattern: "P41_(REQUIREMENTS_COVERAGE|ROADMAP_PROGRESS|AUDIT_)"
    - from: tools/bazel/BUILD.bazel
      to: tools/bazel/phase41_terminal_consistency_projection_boundary_test.py
      via: dedicated test target included in the aggregate Phase 41 suite
      pattern: "phase41_terminal_consistency_projection_boundary_tests"
---

<objective>
Close the single verifier-reported fail-open boundary by making every duplicated terminal projection an exact checked input.

Purpose: Ensure future drift in REQUIREMENTS coverage, bottom ROADMAP progress/execution metadata, or nested audit scores cannot leave the checker result unchanged while preserving every earlier review fix and the active-versus-terminal lifecycle contract.
Output: Extended normalized contracts and pure policy, strict projection parsers, focused direct/Bazel adversarial tests, and unchanged public checker/just entrypoints.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-CONTEXT.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-RESEARCH.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-VALIDATION.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-VERIFICATION.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-REVIEW.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-REVIEW-FIX.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md

<interfaces>
From tools/bazel/phase41_terminal_consistency.py:
```python
def parse_requirements(parser: BoundaryParser, requirements_text: str,
                       roadmap_text: str) -> tuple[RequirementRecord, ...]: ...
def parse_phases_and_inventories(
    parser: BoundaryParser,
    roadmap_text: str,
) -> tuple[tuple[PhaseLifecycle, ...], tuple[PlanInventory, ...]]: ...
def parse_milestone(parser: BoundaryParser, roadmap_text: str, state_text: str,
                    phases: tuple[PhaseLifecycle, ...],
                    inventories: tuple[PlanInventory, ...]) -> MilestoneProjection: ...
def parse_audit(parser: BoundaryParser) -> AuditRecord: ...
def load_snapshot(root: Path) -> TerminalSnapshot: ...
```

From tools/bazel/phase41_terminal_consistency_policy.py:
```python
MILESTONE_PHASES = tuple(range(31, 42))
CANONICAL_REQUIREMENTS: dict[str, tuple[int, str]]

@dataclass(frozen=True, order=True)
class Violation:
    path: str
    code: str
    observed: str
    expected: str

def evaluate_terminal_consistency(
    snapshot: TerminalSnapshot,
    mode: str | ConsistencyMode,
) -> tuple[Violation, ...]: ...
```

From tools/bazel/phase41_terminal_consistency_markdown.py:
```python
class BoundaryParser:
    def frontmatter(self, path: str, text: str | None) -> dict[str, str] | None: ...
    def required_section(self, path: str, text: str,
                         headings: str | tuple[str, ...],
                         identity: str | None = None) -> str: ...
    def required_table(self, path: str, text: str,
                       required_columns: tuple[str, ...], identity: str,
                       any_columns: tuple[str, ...] = ()) -> list[dict[str, str]]: ...
```

The public imports from `phase41_terminal_consistency_policy.py`, CLI arguments `--root` and `--mode {pre-audit,pre-archive}`, exit codes 0/1/2, Bazel aggregate label, and `just phase41-verify` recipe are compatibility surfaces and must remain stable.
</interfaces>
</context>

<gap-closure>
The independent verifier changed one projection at a time in isolated repository copies and observed byte-identical checker output for: `v1.3 requirements: 16 total` to `15 total`; the bottom Phase 41 Progress row from `3/3 | Complete` to `2/3 | Planned`; and nested audit `scores.requirements` from `16/16 coherent` to `15/16 coherent`. The same root omission covers the ROADMAP execution narrative plus nested integration and Nyquist audit projections. This plan closes only that grouped parser/snapshot/policy/test gap; final ROADMAP/STATE lifecycle mutation and audit freshness remain orchestrator-owned.
</gap-closure>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact projection contracts and mode-aware pure comparisons</name>
  <read-first>
    .planning/phases/41-terminal-milestone-metadata-coherence/41-CONTEXT.md
    .planning/phases/41-terminal-milestone-metadata-coherence/41-VERIFICATION.md
    .planning/phases/41-terminal-milestone-metadata-coherence/41-REVIEW-FIX.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/v1.3-MILESTONE-AUDIT.md
    tools/bazel/phase41_terminal_consistency_contracts.py
    tools/bazel/phase41_terminal_consistency_policy.py
    tools/bazel/phase41_terminal_consistency_test_support.py
    tools/bazel/phase41_terminal_consistency_test.py
  </read-first>
  <files>tools/bazel/phase41_terminal_consistency_contracts.py, tools/bazel/phase41_terminal_consistency_policy.py, tools/bazel/phase41_terminal_consistency_test_support.py, tools/bazel/phase41_terminal_consistency_projection_test.py</files>
  <behavior>
    - A coherent terminal snapshot passes both modes; a coherent active Phase 41 snapshot still passes pre-audit and fails pre-archive for the existing terminal prerequisites.
    - Each REQUIREMENTS rollup field is checked independently: total 16, mapped 16, behavior-evidenced 16/16, the exact seven Phase 41 ownership rows with active `pending phase completion` versus terminal-complete semantics, unmapped 0, and duplicate mappings 0.
    - The bottom ROADMAP Progress projection contains exact Phase 31–41 identities, derives 11/11 phase and 36/36 plan totals from on-disk inventories at terminal state, and checks Phase 41 completed/total/status independently; active pre-audit derives its truthful incomplete values from the same inventory/lifecycle evidence.
    - The ROADMAP execution projection preserves the exact dependency facts `38 -> 39`, `38 -> 40`, and `39 + 40 -> 41`; a missing, extra, reversed, or stale edge fails closed.
    - Audit nested `scores` require exact `16/16 coherent`, `11/11 evaluated`, `15/15 connected; 0 gaps`, and `7/7 complete; 0 gaps`; `integration_checker` requires passed status, exact 15/0/0 and 7/0/0 scores, and zero runtime-safety, metadata, and archival gaps; `nyquist` requires exact compliant phases 31–41, empty partial/missing sets, and `overall: compliant`.
    - Every single-field mutation changes the sorted violation tuple and emits its exact path plus one stable code family: `P41_REQUIREMENTS_COVERAGE_*`, `P41_ROADMAP_PROGRESS_*`, `P41_ROADMAP_EXECUTION_PROJECTION`, `P41_AUDIT_SCORE_*`, `P41_AUDIT_INTEGRATION_PROJECTION`, or `P41_AUDIT_NYQUIST_PROJECTION`.
  </behavior>
  <action>
Before edits, record `shasum -a 256 .planning/config.json` in `/tmp/phase41-04-config.sha256`; the file is already modified outside this plan and must not be staged, rewritten, normalized, or restored.

Move the existing normalized record dataclasses from the 621-line policy file into `phase41_terminal_consistency_contracts.py`, then import and re-export them from the policy module so every current import continues to work. Add immutable `RequirementsCoverageProjection`, `RoadmapProgressProjection`, and `AuditFrontmatterProjection` records and attach them to `TerminalSnapshot`/`AuditRecord` without introducing untyped dictionaries in the pure core. This split is required to keep every touched Python source below the managed 629-line limit while preserving the public module surface and all review fixes.

Write the new projection policy tests first and prove they fail. Extend coherent and active fixtures in `phase41_terminal_consistency_test_support.py`, then implement exact evidence-derived comparisons in the pure policy per D-01, D-03, D-06, D-08, D-09, and D-10. Do not make duplicated coverage/progress/audit fields authoritative: derive expectations from `CANONICAL_REQUIREMENTS`, exact PLAN/SUMMARY inventories, lifecycle records, validation evidence, and mode, then compare projections. Keep the audit consumer-only per D-04 and retain every existing verification freshness, duplicate Markdown, exact validation identity, diagnostic bounding, and active-versus-terminal rule from `41-REVIEW-FIX.md`.

Use the stable code families named in the behavior block, always path-qualified to `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, or `.planning/v1.3-MILESTONE-AUDIT.md`, and preserve final sorting by `(path, code, observed)`. Tests must use explicit Arrange, Act, Assert sections and one concern per method or named subtest.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase41_terminal_consistency_projection_test.py -q &amp;&amp; python3 tools/bazel/phase41_terminal_consistency_test.py -q</automated>
  </verify>
  <acceptance-criteria>
    - All new projection records are immutable and no raw Markdown string reaches the pure evaluator.
    - All existing policy imports and 0/1 repository-result semantics remain compatible.
    - Every exact rollup/progress/execution/nested-audit field has a focused failing mutation and deterministic expected P41 code/path.
    - Active pre-audit and terminal pre-archive expectations are derived from evidence rather than hardcoded to one lifecycle snapshot.
    - Requirement prose, cutover verdicts, reference-demotion authority, audit content, ROADMAP, REQUIREMENTS, STATE, VALIDATION files, and `.planning/config.json` are not edited.
  </acceptance-criteria>
  <done>The pure core represents and rejects drift in every verifier-reported projection without weakening any prior policy or lifecycle boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse exact live projections and prove isolated direct/Bazel mutations fail closed</name>
  <read-first>
    tools/bazel/phase41_terminal_consistency_contracts.py
    tools/bazel/phase41_terminal_consistency_policy.py
    tools/bazel/phase41_terminal_consistency_markdown.py
    tools/bazel/phase41_terminal_consistency.py
    tools/bazel/phase41_terminal_consistency_boundary_test.py
    tools/bazel/phase41_terminal_consistency_timestamp_test.py
    tools/bazel/BUILD.bazel
    BUILD.bazel
    justfile
    .planning/phases/41-terminal-milestone-metadata-coherence/41-VERIFICATION.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/v1.3-MILESTONE-AUDIT.md
  </read-first>
  <files>tools/bazel/phase41_terminal_consistency_markdown.py, tools/bazel/phase41_terminal_consistency_projection_parser.py, tools/bazel/phase41_terminal_consistency.py, tools/bazel/phase41_terminal_consistency_projection_boundary_test.py, tools/bazel/BUILD.bazel</files>
  <behavior>
    - The unique REQUIREMENTS Coverage block is required once, accepts only the exact six labeled values, and rejects missing, duplicate, malformed, renamed, or contradictory rows before policy evaluation.
    - The unique bottom ROADMAP `## Progress` section contains exactly one execution-order projection and one matching contiguous progress table; duplicate sections/tables/Phase 41 rows, wrong row widths, wrong phase identities, or contradictory milestone statuses fail closed.
    - Nested audit frontmatter is parsed as a bounded indentation-aware mapping that rejects duplicate case-normalized keys, wrong nesting, malformed scalar/list shapes, and absent required `scores`, `integration_checker`, or `nyquist` members without changing the existing top-level parser contract.
    - In an isolated coherent filesystem fixture exercised through `load_snapshot`, mutating only REQUIREMENTS total 16 to 15, only Phase 41 Progress 3/3 Complete to 2/3 Planned, only one execution edge, only `scores.requirements`, only an integration-checker score, or only a Nyquist set changes output and adds the exact new path-qualified P41 code while leaving the baseline violations unchanged.
    - All old malformed-boundary, archive, timestamp, and policy tests remain green, including every fix recorded in `41-REVIEW-FIX.md`.
  </behavior>
  <action>
Write the isolated live-boundary regressions first and demonstrate the three verifier probes remain invisible before implementation. Build their temporary fixture from bounded synthetic Markdown and exact Phase 31–41 plan/summary/validation identities; never mutate the working `.planning` tree. Compare complete sorted baseline and mutated violation tuples, asserting the exact newly added code/path rather than merely checking a nonzero exit.

Implement strict parsers in the new projection parser module so the 568-line CLI remains a thin shell below the 629-line limit. Extend `BoundaryParser` only with the bounded nested-frontmatter/unique labeled-block primitive needed by the new parser; retain its path-aware duplicate sections, duplicate normalized headers, contiguous table, row-width, fence, timestamp, and frontmatter protections from WR-01 through WR-03. Wire `load_snapshot` to parse all three projection records exactly once and pass them to the pure policy per D-07 through D-11.

For REQUIREMENTS, validate all six coverage facts against the exact sixteen IDs and mode-derived Phase 41 ownership state; reject count-only agreement with wrong semantics. For ROADMAP, require the bottom `Progress` section, exact Phase 31–41 rows, Phase 41 completed/total/status, v1.3 aggregate phases/plans/status, and the exact execution dependency graph. For the audit, parse and compare every field in nested `scores`, `integration_checker`, and `nyquist`, not only `scores.requirements`; preserve pre-audit audit-optional/consumer semantics and strict pre-archive verification/freshness ordering.

Add two dedicated Bazel test targets for the projection policy and live-boundary suites, include every new module in the binary/test data, and add both targets to the existing `phase41_terminal_consistency_tests` aggregate. Do not rename existing labels, alter the `just phase41-verify` recipe, modify `scripts/bright-builds-check.ts`, or weaken the root planning-input filegroup.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase41_terminal_consistency_projection_boundary_test.py -q &amp;&amp; python3 tools/bazel/phase41_terminal_consistency_boundary_test.py -q &amp;&amp; python3 tools/bazel/phase41_terminal_consistency_archive_test.py -q &amp;&amp; python3 tools/bazel/phase41_terminal_consistency_timestamp_test.py -q &amp;&amp; bazel test //tools/bazel:phase41_terminal_consistency_tests --test_output=errors</automated>
  </verify>
  <acceptance-criteria>
    - Each verifier-reported one-line mutation changes checker output and emits its exact deterministic path-qualified P41 code in both direct and Bazel-owned regression coverage.
    - Missing/duplicate/malformed coverage, Progress/execution, and nested audit fields fail closed at the boundary.
    - All existing Phase 41 direct tests and the aggregate Bazel suite pass with no review-fix regression.
    - Every touched/new Python source remains below 629 physical lines and passes syntax/format checks.
    - The public CLI, aggregate Bazel label, `just phase41-verify`, managed checker, planning/evidence documents, and pre-existing `.planning/config.json` bytes remain unchanged except for the planned checker source/test/build files.
  </acceptance-criteria>
  <done>The live checker observes every terminal projection named by verification, and isolated drift can no longer produce byte-identical output.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| Duplicated Markdown projections to normalized evidence | A truthful underlying inventory coexists with stale display metadata that is never observed | Parse each required projection exactly once, require unique identities, and compare it to independently derived evidence |
| Active pre-audit to terminal pre-archive | A gap fix accidentally requires terminal state too early or accepts active wording after completion | Derive expected coverage/progress/status from exact lifecycle evidence and selected mode; retain existing mode regressions |
| Nested audit frontmatter to audit record | Top-level-only parsing ignores contradictory score, integration, or Nyquist claims | Use a bounded indentation-aware parser with duplicate-key, required-field, scalar/list-shape, and exact-value checks |
| Adversarial diagnostics to logs | Malformed metadata injects secrets or unbounded content into output | Preserve 160-character bounding and report only paths, field names, counts, statuses, and semantic digests |

## ASVS L1 STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-41-09 | Tampering | REQUIREMENTS coverage rollup | mitigate | Exact six-field normalized projection checked against sixteen canonical IDs and lifecycle-derived ownership state. |
| T-41-10 | Spoofing | ROADMAP Progress and execution graph | mitigate | Require exact Phase 31–41 identities, Phase 41 counts/status, aggregate totals/status, and dependency edges. |
| T-41-11 | Tampering | Audit nested scores/integration/Nyquist | mitigate | Parse all required nested members and compare exact values/sets; missing or malformed fields fail closed. |
| T-41-12 | Elevation of Privilege | Pre-audit/pre-archive boundary | mitigate | Audit remains consumer-only; passed verification, freshness ordering, terminal lifecycle, and exact inventory remain mandatory only at pre-archive. |
| T-41-13 | Repudiation | Violation output | mitigate | Stable path-qualified codes and sorted observed/expected values make every isolated mutation reproducible. |
| T-41-14 | Information Disclosure | Parser diagnostics | mitigate | Never echo requirement prose, evidence payloads, credentials, keys, certificates, or raw audit content. |
</threat-model>

<verification>
1. Run all five direct suites: `phase41_terminal_consistency_test.py`, `phase41_terminal_consistency_archive_test.py`, `phase41_terminal_consistency_boundary_test.py`, `phase41_terminal_consistency_timestamp_test.py`, plus both new projection suites, all with `-q`.
2. Run `python3 -m py_compile` over every touched/new Phase 41 Python module and the repository-configured scoped YAPF check without broad formatting.
3. Run `bazel test //tools/bazel:phase41_terminal_consistency_tests --test_output=errors`; restore only Bazel-generated `MODULE.bazel.lock` drift if it occurs and was not part of the plan.
4. Run the live pre-audit checker read-only, record its expected workflow-transition/inventory findings, and require no coverage/progress/execution/nested-audit code on the unmutated repository. Do not relabel transition findings as this implementation gap.
5. Run `bun scripts/bright-builds-check.ts all` and require zero findings; verify `git diff --exit-code -- scripts/bright-builds-check.ts`.
6. Run the full Rust pre-commit sequence in this exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
7. Run `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 41 --require-plans --raw` and record the truthful gap-closure lifecycle result without fabricating terminal metadata.
8. Run `git diff --check`, inspect the exact changed-path diff, require `shasum -a 256 -c /tmp/phase41-04-config.sha256`, and confirm no planning, audit, validation, verification, runtime authority, cutover, or demotion artifact changed.
9. After task completion, create `.planning/phases/41-terminal-milestone-metadata-coherence/41-04-SUMMARY.md` with direct/Bazel mutation evidence, exact tests/counts, deviations, residual orchestrator transition prerequisites, and Self-Check status.
</verification>

<success-criteria>
- All three verifier disconfirmation probes and the broader execution/integration/Nyquist nested variants produce changed deterministic output and the exact path-qualified P41 violation.
- The checker validates all exact sixteen-requirement coverage facts, bottom ROADMAP Phase 41 and v1.3 progress/status/execution facts, and nested audit scores/integration/Nyquist facts.
- Pre-audit active-versus-terminal behavior, pre-archive strictness, every previous code-review fix, diagnostic secrecy, and exit codes 0/1/2 remain intact.
- Direct tests, aggregate Bazel tests, managed Bright Builds checks, the full Rust sequence, lifecycle validation, line limits, syntax/format checks, and diff review complete successfully.
- Only the nine planned checker source/test/build files plus the required execution summary are changed; `.planning/config.json` remains byte-for-byte at its pre-plan modified state and is never staged.
</success-criteria>

<output>
After completion, create `.planning/phases/41-terminal-milestone-metadata-coherence/41-04-SUMMARY.md`.
</output>
