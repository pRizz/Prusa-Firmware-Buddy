---
phase: 08-local-interface-and-workflow-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/crates/domain/src/gui.rs
  - rust/crates/domain/src/lib.rs
autonomous: true
requirements:
  - IFCE-01
requirements_addressed:
  - IFCE-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-06-13T16-58-45
generated_at: 2026-06-13T17:28:27Z
must_haves:
  truths:
    - "Rust code can represent supported GUI display classes, workflows, evidence classes, proof scopes, row IDs, localization surfaces, and print-control semantic actions as typed values."
    - "Invalid GUI row IDs, display classes, evidence/proof combinations, and print-control bindings are rejected before adapter code can consume them."
    - "Pure GUI domain code remains unsafe-free and unit-tested with Arrange/Act/Assert structure."
  artifacts:
    - path: "rust/crates/domain/src/gui.rs"
      provides: "Pure Phase 8 GUI/display/evidence domain contracts"
      contains: "DisplayClass"
    - path: "rust/crates/domain/src/lib.rs"
      provides: "Public exports for Phase 8 GUI domain contracts and invariant errors"
      contains: "pub mod gui;"
  key_links:
    - from: "rust/crates/domain/src/gui.rs"
      to: "tools/bazel/manifests/phase8_gui_workflows.json"
      via: "matching row IDs and rust_surface strings"
      pattern: "GuiParityRowId|GuiWorkflow|PrintControlAction"
    - from: "rust/crates/domain/src/gui.rs"
      to: "include/guiconfig/guiconfig.h"
      via: "DisplayClass parse values"
      pattern: "240x320|480x320|mock"
---

<objective>
Add pure Rust `buddy-domain` contracts for Phase 8 GUI/display/evidence facts.

Purpose: Make Phase 8 GUI compatibility rows parseable into typed values instead of verifier-only primitive strings, following Bright Builds functional-core guidance.
Output: `rust/crates/domain/src/gui.rs` plus `lib.rs` exports and invariant errors.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
@.planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
@.planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
@rust/crates/domain/src/lib.rs
@rust/crates/domain/src/resource.rs
@rust/crates/domain/src/storage.rs
@rust/crates/domain/src/safety.rs
@rust/crates/domain/src/print.rs
@include/guiconfig/guiconfig.h
@include/guiconfig/GuiDefaults.hpp

<interfaces>
From `rust/crates/domain/src/lib.rs`:
```rust
#![forbid(unsafe_code)]
pub mod resource;
pub mod storage;
pub use resource::{BazelLabel, GeneratedOutputOwnership, GeneratedSurface, ResourceRuntimePath, ResourceSurface};
pub enum InvariantError {
    InvalidEvidenceClass,
    InvalidBazelLabel,
    // Add Phase 8 GUI invariant variants here.
}
```

From `rust/crates/domain/src/safety.rs`:
```rust
pub enum EvidenceClass {
    ManifestCheck,
    SourceAudit,
    StaticSourceAudit,
    HostTest,
    RustHostTest,
    SimulatorFlow,
    HardwareSmoke,
    ManualHardwareRequired,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing GUI domain tests</name>
  <files>rust/crates/domain/src/gui.rs, rust/crates/domain/src/lib.rs</files>
  <read_first>
    - rust/crates/domain/src/gui.rs
    - rust/crates/domain/src/lib.rs
    - rust/crates/domain/src/resource.rs
    - rust/crates/domain/src/storage.rs
    - rust/crates/domain/src/safety.rs
    - rust/crates/domain/src/print.rs
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - include/guiconfig/guiconfig.h
    - include/guiconfig/GuiDefaults.hpp
  </read_first>
  <behavior>
    - Test 1: `DisplayClass::parse("240x320")` returns `DisplayClass::Mini240x320` with width `240`, height `320`, and `as_str() == "240x320"`.
    - Test 2: `DisplayClass::parse("480x320")` returns `DisplayClass::Large480x320` with width `480`, height `320`, and `as_str() == "480x320"`.
    - Test 3: `GuiEvidenceClass::parse("simulator-flow")` is non-local, while `GuiEvidenceClass::parse("manifest-check")` is local.
    - Test 4: `GuiParityRowId::parse("screen-stack-home-bootstrap")` succeeds, but empty strings, path-like values, and control characters fail.
    - Test 5: `PrintControlAction` parses exact manifest IDs `pause`, `resume`, `cancel`, `stop`, `reprint`, and `preview`.
    - Test 6: `GuiParityContract::new(...)` rejects local proof scope paired with `GuiEvidenceClass::HardwareSmoke`, `SimulatorFlow`, or `ManualHardwareRequired`.
    - Test 7: `GuiParityContract::new(...)` rejects `maybe_print_control_action: Some(...)` unless `workflow == GuiWorkflow::PrintControl`.
  </behavior>
  <action>Per D-07 and D-08, create `rust/crates/domain/src/gui.rs` with `#[cfg(test)]` unit tests for the behaviors above and add `pub mod gui;` in `lib.rs` so the tests compile against the intended module path. The tests should reference the planned public names `DisplayClass`, `GuiWorkflow`, `GuiSurface`, `GuiEvidenceClass`, `GuiProofScope`, `GuiParityRowId`, `LocalizationSurface`, `IntentionalDeltaStatus`, `PrintControlAction`, and `GuiParityContract`. Keep tests one concern each and use `// Arrange`, `// Act`, and `// Assert` comments when setup is more than one line. Run the focused test command and confirm it fails because the implementation is not complete yet.</action>
  <verify>
    <automated>cargo test -p buddy-domain --all-features gui</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'fn parses_mini_display_class|fn parses_large_display_class|fn rejects_non_local_evidence_as_local_proof|fn parses_print_control_action_manifest_ids' rust/crates/domain/src/gui.rs` finds the planned tests.
    - `rg 'pub mod gui;' rust/crates/domain/src/lib.rs` finds the module export.
    - `cargo test -p buddy-domain --all-features gui` fails before Task 2 because the implementation is incomplete.
  </acceptance_criteria>
  <done>Failing Rust tests define the Phase 8 GUI domain contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GUI domain contracts and exports</name>
  <files>rust/crates/domain/src/gui.rs, rust/crates/domain/src/lib.rs</files>
  <read_first>
    - rust/crates/domain/src/gui.rs
    - rust/crates/domain/src/lib.rs
    - rust/crates/domain/src/resource.rs
    - rust/crates/domain/src/storage.rs
    - rust/crates/domain/src/safety.rs
    - rust/crates/domain/src/print.rs
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
  </read_first>
  <action>Implement the pure Rust Phase 8 domain module per D-07, D-08, and D-09. Add `DisplayClass::{Mini240x320, Large480x320, MockTestOnly}` with `parse`, `as_str`, `width`, `height`, and `is_physical_display_proof()` where `MockTestOnly` is never physical proof. Add `GuiWorkflow::{ScreenStack, DialogFsm, MenuWorkflow, PrintPreview, PrintControl, SetupSelftestCalibration, ConnectRegistrationEntry, PrusaLinkCredentialDisplay, WarningRedscreenError, LocalizationLayout}`. Add `GuiSurface::{ScreenStack, Dialog, Menu, Wizard, PrintControl, Localization, ErrorWarning, ConnectEntry}`. Add `GuiEvidenceClass` with the same manifest strings as Phase 8 allowed evidence classes and `is_local_proof()`. Add `GuiProofScope::{Local, NonLocal}` and enforce that `SimulatorFlow`, `HardwareSmoke`, and `ManualHardwareRequired` cannot be paired with `Local`. Add `GuiParityRowId` with a fallible constructor rejecting empty, path-like, control-character, and over-96-byte IDs. Add `LocalizationSurface::{TextCapacity, FontResource, Truncation, PrintPreviewText, ProgressText, WarningErrorText}`. Add `IntentionalDeltaStatus::{None, Approved, Blocked}`. Add `PrintControlAction::{Pause, Resume, Cancel, Stop, Reprint, Preview}` with exact `parse`/`as_manifest_id` values `pause`, `resume`, `cancel`, `stop`, `reprint`, and `preview`. Add `GuiParityContract` with getters for workflow, display classes, evidence, proof scope, row ID, and optional print-control action. Extend `InvariantError` in `lib.rs` with exact variants `EmptyGuiParityRowId`, `InvalidGuiParityRowId`, `InvalidDisplayClass`, `InvalidGuiEvidenceClass`, `InvalidGuiProofScope`, `InvalidGuiWorkflow`, `InvalidIntentionalDeltaStatus`, `InvalidPrintControlAction`, and `InvalidGuiPrintControlBinding`; update `Display` messages. Export the new public types from `lib.rs`. Do not introduce any adapter calls, filesystem/network access, simulator calls, heap-backed GUI model, or `unsafe`.</action>
  <verify>
    <automated>cargo test -p buddy-domain --all-features gui</automated>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pub enum DisplayClass|Mini240x320|Large480x320|MockTestOnly' rust/crates/domain/src/gui.rs` finds display classes.
    - `rg 'pub enum PrintControlAction|Pause|Resume|Cancel|Stop|Reprint|Preview' rust/crates/domain/src/gui.rs` finds semantic action identities.
    - `rg 'pub struct GuiParityContract|InvalidGuiProofScope|InvalidGuiPrintControlBinding' rust/crates/domain/src/gui.rs rust/crates/domain/src/lib.rs` finds invariant enforcement.
    - `rg 'GuiParityRowId|GuiWorkflow|GuiSurface|GuiEvidenceClass|GuiProofScope|LocalizationSurface|IntentionalDeltaStatus|PrintControlAction|GuiParityContract' rust/crates/domain/src/lib.rs` finds exports.
    - `rg 'unsafe' rust/crates/domain/src/gui.rs` returns no uncommented unsafe syntax; if comments mention the word, the verifier in Plan 03 must strip comments before checking.
  </acceptance_criteria>
  <done>The `buddy-domain` crate exposes tested, unsafe-free Phase 8 GUI/display/evidence contracts that reject impossible combinations before adapter/runtime work can consume them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest strings -> Rust domain types | Raw row IDs, display classes, evidence classes, proof scopes, and semantic action IDs are parsed into typed contracts. |
| Pure domain -> future adapters | Domain contracts describe parity facts but do not perform retained GUI, simulator, hardware, network, or filesystem effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | `GuiParityRowId` and display/evidence parsers | mitigate | Use fallible constructors and exact accepted strings so invalid manifest identities fail early. |
| T-08-07 | Elevation of Privilege | `GuiProofScope` with non-local evidence classes | mitigate | Reject `Local` proof scope for simulator, hardware, and manual evidence classes. |
| T-08-08 | Tampering | `PrintControlAction` bindings | mitigate | Reject print-control action values unless the workflow is `GuiWorkflow::PrintControl`; keep action IDs exact. |
| T-08-09 | Information Disclosure | Pure GUI domain module | accept | Module stores no credentials, crash dump bytes, tokens, certificates, or runtime IO; verifier later scans for forbidden markers. |
| T-08-10 | Repudiation | Future adapter consumers | mitigate | Expose typed getters and manifest string conversions so later code can prove which compatibility fact it consumed. |
</threat_model>

<verification>
Run focused Rust tests first, then formatting and lint:

```bash
cargo test -p buddy-domain --all-features gui
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
```
</verification>

<success_criteria>
- `rust/crates/domain/src/gui.rs` exists and contains pure Rust Phase 8 GUI contracts.
- `rust/crates/domain/src/lib.rs` exports all new GUI domain types and invariant errors.
- Rust tests cover display-class parsing, row ID validation, evidence/proof compatibility, and print-control action IDs.
- The module contains no runtime GUI, simulator, hardware, network, filesystem, or unsafe implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-local-interface-and-workflow-parity/08-02-SUMMARY.md`.
</output>
