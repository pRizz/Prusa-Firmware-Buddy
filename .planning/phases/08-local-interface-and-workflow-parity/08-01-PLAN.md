---
phase: 08-local-interface-and-workflow-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase8_gui_workflows.json
  - tools/bazel/manifests/phase8_display_layouts.json
  - tools/bazel/manifests/phase8_concern_dispositions.json
autonomous: true
requirements:
  - IFCE-01
requirements_addressed:
  - IFCE-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-06-13T16-58-45
generated_at: 2026-06-13T17:28:27Z
must_haves:
  truths:
    - "Maintainer can inspect source-backed local GUI workflow contracts for screen stack, dialogs, menus, print controls, setup flows, Connect entry surfaces, warnings, and redscreens."
    - "Maintainer can inspect explicit 240x320 and 480x320 layout/localization contracts without treating one display class as a proxy for the other."
    - "Maintainer can see CL-008, crash dump warning, and GUI resource/font drift concerns dispositioned without secret, credential, certificate, or crash-dump byte material."
  artifacts:
    - path: "tools/bazel/manifests/phase8_gui_workflows.json"
      provides: "IFCE-01 source-backed GUI workflow manifest"
      contains: "print-control-stop-confirmation"
    - path: "tools/bazel/manifests/phase8_display_layouts.json"
      provides: "Display-class layout, localization, print preview, progress, and warning/error contracts"
      contains: "mini-240x320-gui-defaults"
    - path: "tools/bazel/manifests/phase8_concern_dispositions.json"
      provides: "Known Phase 8 GUI concern dispositions"
      contains: "concern-cl-008-home-screen-flash-start"
  key_links:
    - from: "tools/bazel/manifests/phase8_gui_workflows.json"
      to: "src/gui/ScreenHandler.hpp"
      via: "source_paths on screen stack rows"
      pattern: "ScreenHandler.hpp"
    - from: "tools/bazel/manifests/phase8_display_layouts.json"
      to: "include/guiconfig/GuiDefaults.hpp"
      via: "source_paths on display layout rows"
      pattern: "GuiDefaults.hpp"
    - from: "tools/bazel/manifests/phase8_concern_dispositions.json"
      to: ".planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md"
      via: "concern_id CL-008 and CL-011 traceability"
      pattern: "CL-008|CL-011"
---

<objective>
Create the source-backed Phase 8 manifest foundation for IFCE-01 local GUI workflow and display/layout parity contracts.

Purpose: Convert the existing C++ GUI oracle into explicit reviewable rows before Rust domain or verifier work can claim coverage.
Output: `phase8_gui_workflows.json`, `phase8_display_layouts.json`, and `phase8_concern_dispositions.json`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
@.planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
@.planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md
@.planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
@src/gui/guimain.cpp
@src/gui/ScreenHandler.hpp
@src/gui/ScreenFactory.hpp
@src/gui/screen_home.cpp
@src/gui/screen_printing.cpp
@src/gui/screen_printing.hpp
@src/gui/dialogs/DialogHandler.cpp
@src/gui/dialogs/DialogConnectReg.cpp
@include/guiconfig/guiconfig.h
@include/guiconfig/GuiDefaults.hpp

<interfaces>
From `src/gui/ScreenHandler.hpp`:
```cpp
inline constexpr size_t MAX_SCREENS = 16;
class Screens {
public:
    void Open(screen_node screen_creator);
    void PushBeforeCurrent(screen_node screen_creator);
    void Close();
    void CloseAll();
    void ClosePrinting();
    size_t Count();
    void gui_loop_until_dialog_closed(stdext::inplace_function<void()> callback = {});
};
```

From `src/gui/ScreenFactory.hpp`:
```cpp
class ScreenFactory {
    using Storage = std::array<uint8_t, 3344>; // MINI, 4096 for other supported display builds
public:
    using UniquePtr = static_unique_ptr<screen_t>;
    struct Creator { using Func = UniquePtr (*)(const Arg &arg); };
    template <class T, auto... args>
    static UniquePtr Screen(const Creator::Arg & = {});
};
```

From `include/guiconfig/guiconfig.h`:
```cpp
#define DISPLAY_TYPE_LARGE 0
#define DISPLAY_TYPE_MINI 1
#define DISPLAY_TYPE_MOCK 2
#define HAS_LARGE_DISPLAY() ...
#define HAS_MINI_DISPLAY() ...
#define HAS_MOCK_DISPLAY() ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write GUI workflow manifest</name>
  <files>tools/bazel/manifests/phase8_gui_workflows.json</files>
  <read_first>
    - tools/bazel/manifests/phase8_gui_workflows.json
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - src/gui/guimain.cpp
    - src/gui/ScreenHandler.hpp
    - src/gui/ScreenFactory.hpp
    - src/gui/screen_home.cpp
    - src/gui/screen_printing.cpp
    - src/gui/screen_printing.hpp
    - src/gui/resolution_240x320/screen_printing_layout.hpp
    - src/gui/resolution_480x320/screen_printing_layout.hpp
    - src/gui/screen_print_preview.cpp
    - src/gui/dialogs/resolution_240x320/radio_button_preview.cpp
    - src/gui/dialogs/resolution_480x320/radio_button_preview.cpp
    - src/gui/dialogs/DialogHandler.cpp
    - src/gui/dialogs/DialogConnectReg.cpp
    - src/gui/screen_prusa_link.cpp
  </read_first>
  <action>Create `tools/bazel/manifests/phase8_gui_workflows.json` per D-01, D-02, and D-03. Use top-level fields `schema_version: 1`, `phase: "08-local-interface-and-workflow-parity"`, `phase_lifecycle_id: "8-2026-06-13T16-58-45"`, and `workflow_contracts`. Every row must include `id`, `requirement: "IFCE-01"`, `source_paths`, `reference_behavior`, `rust_surface`, `display_classes`, `evidence_class`, `proof_scope`, `non_local_evidence`, and `intentional_delta`. Add rows with these exact IDs: `screen-stack-home-bootstrap`, `screen-stack-bounded-fixed-storage`, `dialog-fsm-display-config`, `menu-settings-and-home-entry`, `print-preview-entry`, `print-control-pause`, `print-control-resume`, `print-control-cancel-abort-request`, `print-control-stop-confirmation`, `print-control-reprint`, `setup-selftest-calibration-wizards`, `connect-registration-local-entry`, `prusalink-credential-local-display`, and `warning-redscreen-error-surfaces`. Per the UI-SPEC flag, every `print-control-*` row and `print-preview-entry` must carry `semantic_action_id` with one exact value from `pause`, `resume`, `cancel`, `stop`, `reprint`, or `preview`, plus `icon_resource_ids` naming source-backed 64x64 and 80x80 resources where the C++ source has them. Do not add network service, TLS, transfer, auxiliary runtime, physical LCD/touch, long-run simulator, or cutover proof claims; keep those in `non_local_evidence` when referenced.</action>
  <verify>
    <automated>python3 -m json.tool tools/bazel/manifests/phase8_gui_workflows.json >/dev/null</automated>
    <automated>python3 -c "import json; p='tools/bazel/manifests/phase8_gui_workflows.json'; d=json.load(open(p)); rows=d['workflow_contracts']; ids={r['id'] for r in rows}; required={'screen-stack-home-bootstrap','screen-stack-bounded-fixed-storage','dialog-fsm-display-config','menu-settings-and-home-entry','print-preview-entry','print-control-pause','print-control-resume','print-control-cancel-abort-request','print-control-stop-confirmation','print-control-reprint','setup-selftest-calibration-wizards','connect-registration-local-entry','prusalink-credential-local-display','warning-redscreen-error-surfaces'}; assert d['phase_lifecycle_id']=='8-2026-06-13T16-58-45'; assert required <= ids; actions={r.get('semantic_action_id') for r in rows if r['id'].startswith('print-control-') or r['id']=='print-preview-entry'}; assert {'pause','resume','cancel','stop','reprint','preview'} <= actions"</automated>
    <automated>rg '"semantic_action_id": "(pause|resume|cancel|stop|reprint|preview)"|"source_paths"|"display_classes"' tools/bazel/manifests/phase8_gui_workflows.json</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"id": "screen-stack-home-bootstrap"' tools/bazel/manifests/phase8_gui_workflows.json` finds exactly one row.
    - `rg '"id": "print-control-stop-confirmation"' tools/bazel/manifests/phase8_gui_workflows.json` finds exactly one row.
    - `rg '"semantic_action_id": "cancel"' tools/bazel/manifests/phase8_gui_workflows.json` finds the abort-request print-control row.
    - `rg 'pause_64x64|pause_80x80|stop_64x64|stop_80x80|reprint_64x64|reprint_80x80|print_80x80|mapping_80x80' tools/bazel/manifests/phase8_gui_workflows.json` finds source-backed icon resource IDs.
    - No row uses `proof_scope: "local"` for physical LCD/touch, timing-sensitive rendering, simulator flow, hardware smoke, network service parity, auxiliary runtime parity, or cutover evidence.
  </acceptance_criteria>
  <done>The GUI workflow manifest is valid JSON, source-backed, lifecycle-tagged, and includes IFCE-01 workflow rows with print-control semantic action identities per D-01 through D-03 and the UI-SPEC flag.</done>
</task>

<task type="auto">
  <name>Task 2: Write display layout and localization manifest</name>
  <files>tools/bazel/manifests/phase8_display_layouts.json</files>
  <read_first>
    - tools/bazel/manifests/phase8_display_layouts.json
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - include/guiconfig/guiconfig.h
    - include/guiconfig/GuiDefaults.hpp
    - src/gui/fonts.hpp
    - src/gui/resolution_240x320/screen_print_preview_base.cpp
    - src/gui/resolution_480x320/screen_print_preview_base.cpp
    - src/gui/resolution_240x320/screen_printing_layout.hpp
    - src/gui/resolution_480x320/screen_printing_layout.hpp
    - src/gui/dialogs/window_dlg_warning.cpp
    - src/gui/screen_bsod.cpp
    - src/lang/CMakeLists.txt
    - tools/bazel/manifests/phase7_resources.json
    - tools/bazel/manifests/phase7_generated_outputs.json
  </read_first>
  <action>Create `tools/bazel/manifests/phase8_display_layouts.json` per D-04, D-05, and D-06. Use top-level fields `schema_version`, `phase`, `phase_lifecycle_id`, and `layout_contracts`. Every row must include `id`, `requirement: "IFCE-01"`, `source_paths`, `reference_behavior`, `rust_surface`, `display_classes`, `layout_values`, `evidence_class`, `proof_scope`, `non_local_evidence`, and `intentional_delta`. Add exact row IDs `display-class-selectors`, `mini-240x320-gui-defaults`, `large-480x320-gui-defaults`, `menu-layout-display-differences`, `print-preview-layout-240x320`, `print-preview-layout-480x320`, `print-progress-layout-240x320`, `print-progress-layout-480x320`, `localized-text-font-contracts`, `warning-dialog-layout`, `redscreen-bsod-error-layout`, and `connect-registration-layout`. Include exact values from UI-SPEC and `GuiDefaults`: `240x320`, `480x320`, MINI header `24`, large header `32`, MINI footer `48`, large footer `23`, MINI print-control icons `64`, large print-control icons `80`, MINI print button padding `8`, large print button padding `90`, MINI print button spacing `16`, large print button spacing `30`, MINI `Font::normal` size `11x18`, large `Font::normal` size `11x19`, and large `Font::large` size `30x53`. Tie localization/font/resource rows to `src/lang`, `src/gui/res`, and Phase 7 resource/generated-output manifests; do not embed English-only replacement copy outside source-backed strings.</action>
  <verify>
    <automated>python3 -m json.tool tools/bazel/manifests/phase8_display_layouts.json >/dev/null</automated>
    <automated>python3 -c "import json; p='tools/bazel/manifests/phase8_display_layouts.json'; d=json.load(open(p)); rows=d['layout_contracts']; ids={r['id'] for r in rows}; required={'display-class-selectors','mini-240x320-gui-defaults','large-480x320-gui-defaults','menu-layout-display-differences','print-preview-layout-240x320','print-preview-layout-480x320','print-progress-layout-240x320','print-progress-layout-480x320','localized-text-font-contracts','warning-dialog-layout','redscreen-bsod-error-layout','connect-registration-layout'}; assert d['phase_lifecycle_id']=='8-2026-06-13T16-58-45'; assert required <= ids; text=json.dumps(rows); assert '240x320' in text and '480x320' in text and 'Font::normal' in text and '30x53' in text"</automated>
    <automated>rg '"240x320"|"480x320"|"HeaderHeight": 24|"HeaderHeight": 32|"FooterHeight": 48|"FooterHeight": 23|"ButtonIconSize": 64|"ButtonIconSize": 80' tools/bazel/manifests/phase8_display_layouts.json</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"id": "mini-240x320-gui-defaults"' tools/bazel/manifests/phase8_display_layouts.json` finds exactly one row.
    - `rg '"id": "large-480x320-gui-defaults"' tools/bazel/manifests/phase8_display_layouts.json` finds exactly one row.
    - `rg '"ButtonIconSize": 64|"ButtonIconSize": 80|"ButtonPadding": 8|"ButtonPadding": 90' tools/bazel/manifests/phase8_display_layouts.json` finds the print-control layout values.
    - `rg 'src/lang|src/gui/res|phase7_resources.json|phase7_generated_outputs.json' tools/bazel/manifests/phase8_display_layouts.json` finds localization/resource traceability.
  </acceptance_criteria>
  <done>The display/layout manifest makes 240x320 and 480x320 first-class IFCE-01 dimensions, records source-backed layout/font/resource facts, and classifies physical/timing proof as non-local per D-04 through D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Write GUI concern disposition manifest</name>
  <files>tools/bazel/manifests/phase8_concern_dispositions.json</files>
  <read_first>
    - tools/bazel/manifests/phase8_concern_dispositions.json
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - .planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md
    - .planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
    - .planning/codebase/CONCERNS.md
    - src/gui/screen_home.cpp
    - src/common/crash_dump/dump.cpp
    - src/common/crash_dump/crash_dump_distribute.cpp
    - tools/bazel/manifests/phase7_resources.json
    - tools/bazel/manifests/phase7_generated_outputs.json
  </read_first>
  <action>Create `tools/bazel/manifests/phase8_concern_dispositions.json` per D-10, D-11, and D-12. Use top-level fields `schema_version`, `phase`, `phase_lifecycle_id`, and `concerns`. Every row must include `id`, `concern_id`, `requirement: "IFCE-01"`, `source_paths`, `disposition`, `phase8_handling`, `evidence_class`, `proof_scope`, `intentional_delta`, and `regression_guard`. Add exact row IDs `concern-cl-008-home-screen-flash-start`, `concern-cl-011-crash-dump-warning-surface`, `concern-cl-003-generated-gui-resource-drift`, and `concern-cl-019-tracked-font-header-churn`. The CL-008 row must include exact strings `no-op flash action`, `event re-enable behavior`, and `src/gui/screen_home.cpp`; the CL-011 row must include exact strings `Crash detected. Save it to USB?`, `sensitive information`, and `no raw crash dump memory contents`. Keep `intentional_delta` set to `none` unless an approved delta is fully evidenced in this phase; do not write credential values, token values, certificate bytes, raw dump bytes, EEPROM bytes, or private key material.</action>
  <verify>
    <automated>python3 -m json.tool tools/bazel/manifests/phase8_concern_dispositions.json >/dev/null</automated>
    <automated>python3 -c "import json; p='tools/bazel/manifests/phase8_concern_dispositions.json'; d=json.load(open(p)); rows=d['concerns']; ids={r['id'] for r in rows}; concerns={r['concern_id'] for r in rows}; assert d['phase_lifecycle_id']=='8-2026-06-13T16-58-45'; assert {'concern-cl-008-home-screen-flash-start','concern-cl-011-crash-dump-warning-surface','concern-cl-003-generated-gui-resource-drift','concern-cl-019-tracked-font-header-churn'} <= ids; assert {'CL-008','CL-011','CL-003','CL-019'} <= concerns; text=json.dumps(rows); assert 'no-op flash action' in text and 'event re-enable behavior' in text and 'no raw crash dump memory contents' in text"</automated>
    <automated>python3 -c "from pathlib import Path; text=Path('tools/bazel/manifests/phase8_concern_dispositions.json').read_text(); forbidden=['password_value','token_value','certificate_bytes','raw_dump','ram_bytes','BEGIN PRIVATE KEY','eeprom_bytes']; assert not any(marker in text for marker in forbidden), [m for m in forbidden if m in text]"</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"concern_id": "CL-008"' tools/bazel/manifests/phase8_concern_dispositions.json` finds the home-screen flash-start row.
    - `rg 'no-op flash action|event re-enable behavior' tools/bazel/manifests/phase8_concern_dispositions.json` finds the CL-008 regression guard.
    - `rg 'Crash detected. Save it to USB\\?|sensitive information|no raw crash dump memory contents' tools/bazel/manifests/phase8_concern_dispositions.json` finds the crash dump warning boundary.
    - `rg 'phase7_resources.json|phase7_generated_outputs.json|src/gui/res/cc' tools/bazel/manifests/phase8_concern_dispositions.json` finds GUI resource/font drift traceability.
  </acceptance_criteria>
  <done>The concern manifest explicitly dispositions CL-008, the crash dump warning surface, generated GUI resource drift, and tracked font churn without reducing or silently fixing user decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C++ GUI oracle -> JSON manifests | Retained source paths and behavior statements become machine-readable compatibility claims. |
| Sensitive diagnostics -> planning artifacts | Crash dump and credential-adjacent GUI facts are named without committing secret or memory bytes. |
| Local evidence -> non-local proof | Static/source manifest proof must not be promoted to physical LCD, touch, simulator, hardware, or cutover proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `phase8_gui_workflows.json` source paths | mitigate | Require every row to name existing `source_paths` under the C++ GUI oracle per D-01 and D-03. |
| T-08-02 | Repudiation | Workflow and layout evidence wording | mitigate | Require `evidence_class`, `proof_scope`, and `non_local_evidence` fields; forbid local claims for LCD/touch/timing/simulator/hardware/cutover proof per D-06. |
| T-08-03 | Information Disclosure | Crash dump and credential-adjacent manifest rows | mitigate | Store names and warning text only; reject markers such as `raw_dump`, `ram_bytes`, `password_value`, `token_value`, `certificate_bytes`, and `BEGIN PRIVATE KEY`. |
| T-08-04 | Denial of Service | Display-class layout coverage | mitigate | Require explicit `240x320` and `480x320` display class rows and values from `GuiDefaults.hpp` per D-04. |
| T-08-05 | Repudiation | Known GUI defects | mitigate | Require CL-008 and CL-011 rows with regression guards and intentional-delta status per D-10 through D-12. |
</threat_model>

<verification>
Run all task-level JSON and grep checks. Before closing the plan, run:

```bash
python3 -m json.tool tools/bazel/manifests/phase8_gui_workflows.json >/dev/null
python3 -m json.tool tools/bazel/manifests/phase8_display_layouts.json >/dev/null
python3 -m json.tool tools/bazel/manifests/phase8_concern_dispositions.json >/dev/null
rg 'phase_lifecycle_id|8-2026-06-13T16-58-45|IFCE-01|240x320|480x320|CL-008|semantic_action_id' tools/bazel/manifests/phase8_*.json
```
</verification>

<success_criteria>
- All three Phase 8 manifest JSON files exist and parse.
- Every manifest row is requirement-mapped to `IFCE-01`.
- Print-control and print-preview workflow rows include source-backed semantic action IDs for `pause`, `resume`, `cancel`, `stop`, `reprint`, and `preview`.
- Display layout rows include both `240x320` and `480x320` contracts and exact source-backed values from `GuiDefaults.hpp` or UI-SPEC.
- Concern rows include CL-008 and CL-011 with regression guards and no sensitive material.
</success_criteria>

<output>
After completion, create `.planning/phases/08-local-interface-and-workflow-parity/08-01-SUMMARY.md`.
</output>
