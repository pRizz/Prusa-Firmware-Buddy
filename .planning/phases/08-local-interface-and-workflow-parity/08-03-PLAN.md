---
phase: 08-local-interface-and-workflow-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - tools/bazel/phase8_verify.py
  - tools/bazel/phase8_verify_test.py
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - BUILD.bazel
  - justfile
  - .planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md
autonomous: true
requirements:
  - IFCE-01
requirements_addressed:
  - IFCE-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-06-13T16-58-45
generated_at: 2026-06-13T17:28:27Z
must_haves:
  truths:
    - "Developer can run a deterministic Phase 8 verifier that checks manifests, Rust API surface, lifecycle metadata, source paths, evidence classes, concern dispositions, and overclaim wording."
    - "Developer can run Phase 8 verification through Bazel labels and `just phase8-verify`."
    - "Maintainer can inspect `08-VALIDATION.md` showing local automated evidence separated from simulator, physical display, touch, timing, hardware, network, auxiliary, and cutover evidence."
  artifacts:
    - path: "tools/bazel/phase8_verify.py"
      provides: "Static Phase 8 verifier"
      exports: ["--quick", "--all"]
    - path: "tools/bazel/phase8_verify_test.py"
      provides: "Verifier regression tests for required Phase 8 failure modes"
      contains: "Phase8VerifierTest"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase8-verify"
    - path: ".planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md"
      provides: "Nyquist validation evidence map and sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tools/bazel/phase8_verify.py"
      to: "tools/bazel/manifests/phase8_gui_workflows.json"
      via: "manifest schema and row ID checks"
      pattern: "phase8_gui_workflows.json"
    - from: "tools/bazel/phase8_verify.py"
      to: "rust/crates/domain/src/gui.rs"
      via: "Rust API string and unsafe-free checks"
      pattern: "GuiParityContract|PrintControlAction"
    - from: "justfile"
      to: "//tools/bazel:phase8_verify"
      via: "phase8-verify recipe"
      pattern: "bazel run //tools/bazel:phase8_verify"
---

<objective>
Add the Phase 8 verifier, Bazel/just wiring, and validation sign-off.

Purpose: Make IFCE-01 local GUI workflow/layout contracts runnable through the established repo verifier surface while blocking overclaims and preserving non-local evidence boundaries.
Output: `phase8_verify.py`, `phase8_verify_test.py`, Bazel labels, `just phase8-verify`, and completed `08-VALIDATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
@.planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
@.planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md
@.planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
@tools/bazel/phase7_verify.py
@tools/bazel/phase7_verify_test.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@rust/crates/domain/src/gui.rs
@rust/crates/domain/src/lib.rs
@tools/bazel/manifests/phase8_gui_workflows.json
@tools/bazel/manifests/phase8_display_layouts.json
@tools/bazel/manifests/phase8_concern_dispositions.json

<interfaces>
From `tools/bazel/rust_workflow.sh`:
```bash
case "$command_name" in
  phase7_verify)
    python3 tools/bazel/phase7_verify.py --all
    ;;
  phase7_verify_tests)
    python3 tools/bazel/phase7_verify_test.py
    ;;
esac
```

From `justfile`:
```make
phase7-verify:
    bazel run //tools/bazel:phase7_verify_tests
    bazel run //tools/bazel:phase7_verify
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing verifier regression tests</name>
  <files>tools/bazel/phase8_verify_test.py</files>
  <read_first>
    - tools/bazel/phase8_verify_test.py
    - tools/bazel/phase7_verify_test.py
    - tools/bazel/phase7_verify.py
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - tools/bazel/manifests/phase8_gui_workflows.json
    - tools/bazel/manifests/phase8_display_layouts.json
    - tools/bazel/manifests/phase8_concern_dispositions.json
    - rust/crates/domain/src/gui.rs
    - rust/crates/domain/src/lib.rs
  </read_first>
  <behavior>
    - Test 1: Missing required workflow rows fails quick verification.
    - Test 2: Missing print-control `semantic_action_id` for any of `pause`, `resume`, `cancel`, `stop`, `reprint`, or `preview` fails quick verification.
    - Test 3: Display layout rows omitting `240x320` or `480x320` fail quick verification.
    - Test 4: Missing CL-008, missing crash dump warning row, or missing `no-op flash action` / `event re-enable behavior` / `no raw crash dump memory contents` text fails quick verification.
    - Test 5: Secret markers such as `password_value`, `token_value`, `certificate_bytes`, `raw_dump`, `ram_bytes`, `BEGIN PRIVATE KEY`, or `eeprom_bytes` fail quick verification.
    - Test 6: Missing Rust API strings `DisplayClass`, `GuiParityRowId`, `GuiEvidenceClass`, `GuiParityContract`, and `PrintControlAction` fail quick verification.
    - Test 7: Overclaim strings such as `hardware display verified locally`, `physical LCD proof passed`, `Connect TLS parity passed`, `auxiliary runtime parity passed`, or `cutover evidence complete` fail quick verification.
    - Test 8: Missing Bazel labels, root aliases, `rust_workflow.sh` dispatch, or `just phase8-verify` fails quick verification.
  </behavior>
  <action>Create `tools/bazel/phase8_verify_test.py` using the Phase 7 temp-root unittest pattern. Name the test class `Phase8VerifierTest`. Copy the real verifier into a temp root when it exists, write minimal valid Phase 8 manifest/Rust/Bazel/just fixtures in helper methods, then mutate one concern per test. Include tests named `test_requires_print_control_semantic_action_ids`, `test_rejects_display_layout_without_both_display_classes`, `test_requires_cl008_and_crash_dump_concerns`, `test_rejects_secret_or_crash_dump_byte_markers`, `test_requires_gui_rust_api_surface`, `test_rejects_phase8_overclaims`, and `test_requires_bazel_and_just_wiring`. The RED run should fail until Task 2 creates `tools/bazel/phase8_verify.py`.</action>
  <verify>
    <automated>python3 tools/bazel/phase8_verify_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'class Phase8VerifierTest|test_requires_print_control_semantic_action_ids|test_rejects_display_layout_without_both_display_classes|test_requires_cl008_and_crash_dump_concerns|test_rejects_secret_or_crash_dump_byte_markers|test_requires_gui_rust_api_surface|test_rejects_phase8_overclaims|test_requires_bazel_and_just_wiring' tools/bazel/phase8_verify_test.py` finds all required tests.
    - `python3 tools/bazel/phase8_verify_test.py` fails before Task 2 because `tools/bazel/phase8_verify.py` is missing or incomplete.
  </acceptance_criteria>
  <done>Verifier regression tests define the Phase 8 aggregate contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 8 static verifier</name>
  <files>tools/bazel/phase8_verify.py, tools/bazel/phase8_verify_test.py</files>
  <read_first>
    - tools/bazel/phase8_verify.py
    - tools/bazel/phase8_verify_test.py
    - tools/bazel/phase7_verify.py
    - tools/bazel/phase7_verify_test.py
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - tools/bazel/manifests/phase8_gui_workflows.json
    - tools/bazel/manifests/phase8_display_layouts.json
    - tools/bazel/manifests/phase8_concern_dispositions.json
    - rust/crates/domain/src/gui.rs
    - rust/crates/domain/src/lib.rs
  </read_first>
  <action>Implement `tools/bazel/phase8_verify.py` per D-13, D-14, and D-15. Use Python standard library only. Define constants `PHASE = "08-local-interface-and-workflow-parity"`, `PHASE_LIFECYCLE_ID = "8-2026-06-13T16-58-45"`, manifest paths, `VALIDATION_CONTRACT`, `GUI_RUST`, and `RUST_DOMAIN_LIB`. Support `--quick` and `--all`; `--quick` must run manifest, source-path, lifecycle, evidence class, proof scope, Rust API, unsafe-free GUI module, validation contract, Bazel/just surface, secret marker, and overclaim checks. `--all` must run `--quick` plus `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` using the Phase 7 subprocess pattern. Required workflow row IDs are the Task 1 Plan 08-01 list. Required layout row IDs are the Task 2 Plan 08-01 list. Required concern row IDs are the Task 3 Plan 08-01 list. Allowed evidence classes are exactly `manifest-check`, `source-audit`, `static-source-audit`, `host-test`, `rust-host-test`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`. Reject `proof_scope: "local"` for simulator/hardware/manual evidence. Require print-control semantic action IDs `pause`, `resume`, `cancel`, `stop`, `reprint`, and `preview`. Require Rust API strings `DisplayClass`, `GuiWorkflow`, `GuiSurface`, `GuiEvidenceClass`, `GuiProofScope`, `GuiParityRowId`, `LocalizationSurface`, `IntentionalDeltaStatus`, `PrintControlAction`, and `GuiParityContract` in `gui.rs`, plus exports in `lib.rs`. Strip Rust comments and string literals before unsafe syntax checks, following the Phase 6/7 scanner pattern. Reject overclaim strings for physical LCD/touch proof, full simulator proof, network service parity, TLS/transfer parity, auxiliary runtime parity, and cutover evidence. Reject forbidden markers `password_value`, `token_value`, `certificate_bytes`, `raw_dump`, `ram_bytes`, `BEGIN PRIVATE KEY`, and `eeprom_bytes` in Phase 8 manifests and validation. Update the Task 1 tests if their temp fixtures need exact schema alignment, but do not weaken the failure cases.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase8_verify.py tools/bazel/phase8_verify_test.py</automated>
    <automated>python3 tools/bazel/phase8_verify_test.py</automated>
    <automated>python3 tools/bazel/phase8_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'PHASE_LIFECYCLE_ID = "8-2026-06-13T16-58-45"|check_gui_workflows_manifest|check_display_layout_manifest|check_concern_dispositions|check_rust_api_surface|check_no_phase8_overclaim|check_bazel_surface|check_just_surface|check_validation_contract' tools/bazel/phase8_verify.py` finds all required verifier checks.
    - `rg 'print-control-stop-confirmation|semantic_action_id|DisplayClass|GuiParityContract|PrintControlAction|phase8_concern_dispositions.json' tools/bazel/phase8_verify.py` finds exact required strings.
    - `python3 tools/bazel/phase8_verify_test.py` passes after implementation.
    - `python3 tools/bazel/phase8_verify.py --quick` passes in the real repo after Plans 08-01 and 08-02 are complete.
  </acceptance_criteria>
  <done>The direct Python verifier and regression suite enforce Phase 8 manifest, Rust, lifecycle, evidence, concern, and overclaim contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Bazel, just, and validation sign-off</name>
  <files>tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, BUILD.bazel, justfile, .planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
    - .planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md
    - tools/bazel/phase8_verify.py
    - tools/bazel/phase8_verify_test.py
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - justfile
  </read_first>
  <action>Expose Phase 8 verification per D-13, D-14, and D-15. In `tools/bazel/BUILD.bazel`, add `shell_binary(name = "phase8_verify", src = "rust_workflow.sh", data = [...])` with `phase8_verify.py`, all three Phase 8 manifests, `//:phase8_local_interface_docs`, and `//:rust_workspace_sources`; add `shell_binary(name = "phase8_verify_tests", src = "rust_workflow.sh", data = ["phase8_verify.py", "phase8_verify_test.py"])`. In root `BUILD.bazel`, add `filegroup(name = "phase8_local_interface_docs", srcs = [".planning/phases/08-local-interface-and-workflow-parity/08-CONTEXT.md", ".planning/phases/08-local-interface-and-workflow-parity/08-RESEARCH.md", ".planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md", ".planning/phases/08-local-interface-and-workflow-parity/08-UI-SPEC.md"])`, plus aliases `phase8_verify` and `phase8_verify_tests`. In `tools/bazel/rust_workflow.sh`, add case arms `phase8_verify)` running `python3 tools/bazel/phase8_verify.py --all` and `phase8_verify_tests)` running `python3 tools/bazel/phase8_verify_test.py`. In `justfile`, add `phase8-verify:` running `bazel run //tools/bazel:phase8_verify_tests` before `bazel run //tools/bazel:phase8_verify`. Update `08-VALIDATION.md` after evidence is collected: set `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`; change Plan TBD entries to Plan 01/02/03; mark local automated rows green only after commands pass; preserve manual-only rows for physical LCD/touch, long-run UI, simulator display, network service behavior, auxiliary runtime behavior, and cutover/display acceptance as non-local evidence.</action>
  <verify>
    <automated>bazel query "//tools/bazel:phase8_verify + //tools/bazel:phase8_verify_tests + //:phase8_verify + //:phase8_verify_tests + //:phase8_local_interface_docs"</automated>
    <automated>bazel run //tools/bazel:phase8_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase8_verify</automated>
    <automated>just phase8-verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'phase8_verify|phase8_verify_tests|phase8_local_interface_docs' tools/bazel/BUILD.bazel BUILD.bazel tools/bazel/rust_workflow.sh justfile` finds the new labels, dispatch cases, aliases, and recipe.
    - `rg 'nyquist_compliant: true|wave_0_complete: true|status: complete|08-W0-01|08-W0-05|manual-hardware-required|hardware-smoke|simulator-flow' .planning/phases/08-local-interface-and-workflow-parity/08-VALIDATION.md` finds final validation sign-off and non-local evidence classes.
    - The Bazel query command lists all five labels without package-boundary errors.
    - `just phase8-verify` runs verifier tests before the aggregate verifier.
  </acceptance_criteria>
  <done>Phase 8 verification is accessible through direct Python, Bazel, and `just`, and validation records passed local evidence without claiming non-local GUI proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON/Rust/planning files -> verifier | The verifier parses local files and converts their text into pass/fail evidence. |
| Bazel/just facade -> local commands | Developer-facing labels execute Python and Cargo commands through `rust_workflow.sh`. |
| Local validation -> phase evidence | `08-VALIDATION.md` records what actually passed and what remains non-local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | Phase 8 manifests | mitigate | Verify schema, required row IDs, lifecycle ID, requirement IDs, source paths, display classes, and semantic action IDs. |
| T-08-12 | Information Disclosure | Manifests and validation text | mitigate | Reject secret, credential, certificate, EEPROM byte, and raw crash dump markers. |
| T-08-13 | Repudiation | Phase 8 evidence wording | mitigate | Reject local proof overclaims for physical LCD/touch, simulator, hardware, network, auxiliary, transfer, TLS, and cutover scope. |
| T-08-14 | Denial of Service | Bazel/just wiring | mitigate | Require queryable labels and dispatch tests before marking validation green. |
| T-08-15 | Elevation of Privilege | Rust verifier `--all` execution | accept | `--all` runs repo-owned Cargo checks only; no external services, credentials, hardware, or network behavior are introduced. |
</threat_model>

<verification>
Final verification for this plan:

```bash
python3 -m py_compile tools/bazel/phase8_verify.py tools/bazel/phase8_verify_test.py
python3 tools/bazel/phase8_verify_test.py
python3 tools/bazel/phase8_verify.py --quick
bazel query "//tools/bazel:phase8_verify + //tools/bazel:phase8_verify_tests + //:phase8_verify + //:phase8_verify_tests + //:phase8_local_interface_docs"
just phase8-verify
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- `tools/bazel/phase8_verify.py` supports `--quick` and `--all`.
- `tools/bazel/phase8_verify_test.py` passes and covers required failure modes.
- Bazel exposes `//tools/bazel:phase8_verify`, `//tools/bazel:phase8_verify_tests`, `//:phase8_verify`, `//:phase8_verify_tests`, and `//:phase8_local_interface_docs`.
- `just phase8-verify` runs verifier tests then aggregate verification.
- `08-VALIDATION.md` is Nyquist-compliant and keeps simulator/hardware/manual evidence non-local unless actually run.
</success_criteria>

<output>
After completion, create `.planning/phases/08-local-interface-and-workflow-parity/08-03-SUMMARY.md`.
</output>
