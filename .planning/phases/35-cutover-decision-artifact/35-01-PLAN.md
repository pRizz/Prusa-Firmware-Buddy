---
phase: 35-cutover-decision-artifact
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
  - tools/bazel/phase35_cutover_decision_artifact_test.py
  - tools/bazel/phase35_cutover_decision_artifact.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
  - .planning/phases/35-cutover-decision-artifact/35-VALIDATION.md
autonomous: true
requirements:
  - CUTOVER-01
  - CUTOVER-02
  - CUTOVER-03
must_haves:
  truths:
    - "Maintainer can generate exactly one cutover verdict from the closed enum approved, blocked, or approved-with-exceptions, and every invalid or incomplete input fails to blocked."
    - "Maintainer can audit the verdict through one canonical exact-set link index covering evidence packets, blockers, exceptions, residual risks, retained-code decisions, readiness decisions/results, demotion decisions, and the demotion dry-run."
    - "An approved verdict routes only to production-cutover-planning, while blocked and approved-with-exceptions route to targeted-blocker-repair with source-backed follow-up scope."
    - "The artifact exposes cutover_verdict, demotion_decision_validation_state, demotion_decision_state, demotion_decision_source_refs, demotion_gate_state, and demotion_gate_reason_codes independently, so cutover approval never grants reference-demotion authority and malformed, stale, or lifecycle-mismatched inputs remain distinguishable."
    - "The default quick path emits blocked plus targeted-blocker-repair and never synthesizes evidence, maintainer approval, exceptions, or demotion authorization."
  artifacts:
    - path: "tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json"
      provides: "Closed verdict, route, audit-link, output, security, lifecycle, and demotion-separation contract"
      contains: "phase35_cutover_decision_artifact_contract"
    - path: "tools/bazel/phase35_cutover_decision_artifact_test.py"
      provides: "RED-first truth-table, anti-join, security, route, demotion, projection, and wiring regressions"
      min_lines: 300
    - path: "tools/bazel/phase35_cutover_decision_artifact.py"
      provides: "Standard-library Phase 35 verifier and deterministic artifact generator"
      min_lines: 300
    - path: "tools/bazel/BUILD.bazel"
      provides: "phase35_source_ref_manifests, phase35_verify, and phase35_verify_tests targets"
      contains: "phase35_verify"
    - path: "BUILD.bazel"
      provides: "Phase 35 planning-doc filegroup and root verification aliases"
      contains: "phase35_cutover_decision_artifact_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Phase 35 prerequisite generation and verifier/test dispatch"
      contains: "phase35_verify)"
    - path: "justfile"
      provides: "Developer-facing phase35-verify facade"
      contains: "phase35-verify:"
  key_links:
    - from: "tools/bazel/phase35_cutover_decision_artifact.py"
      to: "build/ci-evidence/phase34/final-readiness-run-manifest.json"
      via: "the only immediate Phase 35 source entrypoint, validated for artifact identity, lifecycle, output root, exact generated artifacts, and safe refs"
      pattern: "final-readiness-run-manifest\\.json"
    - from: "tools/bazel/phase35_cutover_decision_artifact.py"
      to: "build/ci-evidence/phase35/cutover-audit-link-index.json"
      via: "independently derived expected-link exact-set anti-join"
      pattern: "cutover-audit-link-index\\.json"
    - from: "build/ci-evidence/phase35/cutover-audit-link-index.json"
      to: "build/ci-evidence/phase35/cutover-decision.json"
      via: "shared canonical normalized link rows and counts, not self-declared decision links"
      pattern: "audit_link_index_ref"
    - from: "build/ci-evidence/phase35/cutover-decision.json"
      to: "build/ci-evidence/phase35/next-milestone-route.json"
      via: "closed verdict-to-route truth table"
      pattern: "production-cutover-planning|targeted-blocker-repair"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase35_cutover_decision_artifact.py"
      via: "phase35_verify regenerates blocked Phase 31-34 prerequisites, checks wiring, then runs Phase 35 quick generation"
      pattern: "phase35_cutover_decision_artifact\\.py --quick"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 35-2026-07-25T21-06-10
generated_at: 2026-07-25T21:33:25Z
---

<objective>
Publish the Phase 35 cutover decision as a fail-closed, auditable projection of the validated Phase 34 readiness bundle and Phase 33 decision lineage.

Purpose: Satisfy CUTOVER-01 through CUTOVER-03 with one explicit verdict, a canonical exact-set audit-link index, one deterministic next-milestone route, and an independent reference-demotion projection without creating a second approval authority or performing production actions.

Output: A Phase 35 JSON contract, RED-first focused regression suite, standard-library verifier/generator, generated `build/ci-evidence/phase35` bundle, Bazel/root/workflow/`just` wiring, and signed-off Phase 35 validation artifact.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cutover-decision-artifact/35-CONTEXT.md
@.planning/phases/35-cutover-decision-artifact/35-RESEARCH.md
@.planning/phases/35-cutover-decision-artifact/35-VALIDATION.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-01-SUMMARY.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-02-SUMMARY.md
@tools/bazel/manifests/phase31_final_evidence_intake_contract.json
@tools/bazel/manifests/phase32_blocker_register_triage_contract.json
@tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
@tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
@tools/bazel/phase33_maintainer_decision_inputs.py
@tools/bazel/phase34_final_readiness_demotion_dry_run.py
@tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
The executor must use these existing contracts directly and must not invent a new evidence or maintainer-decision schema.

From `tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`:

```text
contract id: phase34_final_readiness_demotion_dry_run_contract
phase lifecycle: 34-2026-07-25T18-18-48
output root: build/ci-evidence/phase34
immediate manifest: build/ci-evidence/phase34/final-readiness-run-manifest.json
manifest artifact_name: phase34-final-readiness-demotion-dry-run
raw_evidence_consumed: false

required Phase 34 artifacts:
  final-readiness-run-manifest.json
  readiness-coverage-ledger.json
  final-readiness-packet.json
  readiness-blocker-summary.json
  demotion-dry-run.json
  redacted-readiness-report.md
  contract-snapshots/phase34_final_readiness_demotion_dry_run_contract.json
  contract-snapshots/phase33_maintainer_decision_inputs_contract.json
  contract-snapshots/phase33-downstream-handoff-manifest.json
  contract-snapshots/phase32-blocker-register.json
  contract-snapshots/phase31-final-intake-manifest.json
  contract-snapshots/phase31-accepted-receipts.json
```

Phase 34 canonical ledger row fields:

```text
row_id
source_stream
source_ref
requirement_ids
affected_gates
proof_eligibility
evidence_status
row_problem_kind
blocker_kind
severity
evidence_refs
artifact_refs
classification_ref
retained_code_decision_refs
residual_risk_decision_refs
exception_decision_refs
readiness_decision_refs
coverage_state
readiness_effect
reason_codes
```

Phase 34 demotion dry-run fields and enums:

```text
readiness_state: blocked | unblocked
approval_validation_state: missing | invalid | valid
approval_decision_state: missing | approve | reject
gate_state: blocked | open
reason_codes: string[]
source_refs: string[]
```

From `tools/bazel/manifests/phase32_blocker_register_triage_contract.json`, repair-source rows require:

```text
row_id
source_stream
source_ref
requirement_ids
affected_gate
row_problem_kind
blocker_kind
severity
owner_ref
required_next_action
decision_impact
proof_eligibility
evidence_refs
```

From `tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`, normalized decision rows require:

```text
decision_id
decision_type
decision_value
source_row_refs
maintainer_identity_ref
maintainer_role
owner_signoff_ref
decision_timestamp
rationale
evidence_refs
artifact_refs
```

Phase 33 decision enums:

```text
decision_type: retained_code | residual_risk | exception | readiness | reference_demotion
retained_code value: accept | reject | exception_approve
residual_risk value: accept | reject
exception value: approve | reject
readiness value: approve | block
reference_demotion value: approve | reject
```

The Phase 33 handoff manifest exposes these exact register refs:

```text
normalized_decision_records
retained_code_decision_register
residual_risk_decision_register
exception_decision_register
readiness_decision_handoff
demotion_decision_handoff
decision_validation_report
```
</interfaces>
</context>

<decision-coverage>
Every Phase 35 locked decision is implemented in full; no deferred idea is included.

| Decision | Plan task | Coverage | Implementation point |
| --- | --- | --- | --- |
| D-01 | Task 1, Task 2 | Full | Closed typed pure truth-table reducer with `blocked` default |
| D-02 | Task 1, Task 2 | Full | Exhaustive fail-closed reason contract and regression families |
| D-03 | Task 1, Task 2 | Full | Exact nonempty validated active exception set selects `approved-with-exceptions` |
| D-04 | Task 1, Task 2 | Full | Unblocked readiness plus empty active exception set selects `approved` |
| D-05 | Task 1, Task 2 | Full | Phase 35 accepts no cutover confirmation input or caller-supplied verdict |
| D-06 | Task 1, Task 2 | Full | JSON verdict, route, and Markdown derive from one canonical audit index |
| D-07 | Task 1, Task 2 | Full | Expected link set derives from Phase 31-34 authoritative artifacts |
| D-08 | Task 1, Task 2 | Full | Exact audit-link fields, stable IDs, and local-only digest policy |
| D-09 | Task 1, Task 2 | Full | Exact-set anti-joins and cross-reference mismatch tests |
| D-10 | Task 1, Task 2 | Full | Sanitized refs/snapshots only; raw/secret-bearing material rejected |
| D-11 | Task 1, Task 2 | Full | Exclusive verdict-to-route tri-state truth table |
| D-12 | Task 1, Task 2 | Full | Repair scope uses exact upstream IDs, owners, actions, gates, requirements, and criterion refs |
| D-13 | Task 1, Task 2 | Full | Repair route sets `requires_fresh_cutover_decision: true` |
| D-14 | Task 1, Task 2 | Full | Approved route is planning-only and authorizes no production action |
| D-15 | Task 1, Task 2 | Full | Phase 33 demotion input projection is separate from Phase 34 dry-run state |
| D-16 | Task 1, Task 2 | Full | Three independent fields and exhaustive non-implication tests |
| D-17 | Task 1, Task 2 | Full | Missing/invalid/rejected/stale/mismatched demotion states survive projection |
| D-18 | Task 1, Task 2 | Full | POST-01 boundary is contract-defined and overclaim-scanned |
| D-19 | Task 1, Task 3 | Full | Contract, verifier, tests, Bazel labels, root aliases, workflow arms, and `just` facade |
| D-20 | Task 1, Task 2 | Full | Exact generated bundle under `build/ci-evidence/phase35` |
| D-21 | Task 1, Task 2, Task 3 | Full | Default path remains durable blocked/repair without synthesized authority |
| D-22 | Task 1, Task 2 | Full | All verdicts, boundaries, link exactness, routing, demotion, security, and projection consistency tested |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — freeze the Phase 35 contract and create RED-first regressions</name>
  <read-first>
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards/core/architecture.md`
    - `standards/core/code-shape.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/35-cutover-decision-artifact/35-CONTEXT.md`
    - `.planning/phases/35-cutover-decision-artifact/35-RESEARCH.md`
    - `.planning/phases/35-cutover-decision-artifact/35-VALIDATION.md`
    - `tools/bazel/manifests/phase31_final_evidence_intake_contract.json`
    - `tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`
    - `tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
  </read-first>
  <files>
    tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
    tools/bazel/phase35_cutover_decision_artifact_test.py
  </files>
  <behavior>
    - Contract identity is exactly `phase35_cutover_decision_artifact_contract`, phase is `35-cutover-decision-artifact`, lifecycle is `35-2026-07-25T21-06-10`, requirements are exactly CUTOVER-01/CUTOVER-02/CUTOVER-03, and output root is `build/ci-evidence/phase35`.
    - Verdict truth table returns `blocked` for every invalid/incomplete/readiness-blocking fact set, `approved-with-exceptions` only for unblocked readiness plus a nonempty exact valid active-approved-exception set, and `approved` only for unblocked readiness plus an empty active-exception set.
    - Route truth table maps only `approved` to `production-cutover-planning`; `blocked` and `approved-with-exceptions` map to `targeted-blocker-repair`.
    - Expected and emitted audit-link semantic IDs are exact-set equal across all nine categories, with focused failures for missing, extra, duplicate, dangling, lifecycle-mismatched, category-mismatched, and digest-mismatched links.
    - `cutover_verdict`, `demotion_decision_validation_state`, `demotion_decision_state`, `demotion_decision_source_refs`, `demotion_gate_state`, and `demotion_gate_reason_codes` remain independent. The validation enum preserves `missing`, `malformed`, `stale`, `lifecycle-mismatched`, other `invalid`, and `valid`; the decision enum preserves `missing`, `approve`, and `reject`; the gate remains `blocked` or `open` with the exact Phase 34 reason codes.
    - Absolute, traversal, wrong-root, overlapping, and symlink-escape paths; forbidden fields/text; unsafe refs; secrets; raw payloads; lifecycle drift; and production/demotion overclaims fail closed.
    - The default quick fixture yields `blocked`, `targeted-blocker-repair`, blocked/missing demotion projection, and no synthesized evidence or approval.
  </behavior>
  <action>
Create the contract and RED tests before the implementation exists, implementing D-01 through D-22 at the contract/test boundary.

1. Create `phase35_cutover_decision_artifact_contract.json` with:
   - source identity `phase34_final_readiness_demotion_dry_run_contract`, lifecycle `34-2026-07-25T18-18-48`, and immediate ref `build/ci-evidence/phase34/final-readiness-run-manifest.json`;
   - verdict enum `approved`, `blocked`, `approved-with-exceptions`;
   - route enum `production-cutover-planning`, `targeted-blocker-repair`;
   - audit-link kinds `evidence-packet`, `blocker`, `exception`, `residual-risk`, `retained-code-decision`, `readiness-decision`, `readiness-result`, `demotion-decision`, and `demotion-dry-run`;
   - mandatory audit-link fields `link_id`, `kind`, `target_id`, `target_ref`, `source_phase_lifecycle_id`, and `verdict_effect`, plus optional `digest` only for validated sanitized local targets;
   - blocked reason codes `source-artifact-missing`, `source-artifact-malformed`, `source-artifact-stale`, `source-artifact-duplicate`, `source-artifact-lifecycle-mismatched`, `redaction-failed`, `source-ref-failed`, `secret-tainted`, `unsafe-ref`, `unknown-input`, `underclassified`, `coverage-incomplete`, `readiness-blocked`, `exception-invalid`, `audit-link-missing`, `audit-link-extra`, `audit-link-duplicate`, `audit-link-dangling`, `audit-link-lifecycle-mismatched`, `audit-link-category-mismatched`, `audit-link-digest-mismatched`, and `route-scope-incomplete`;
   - generated artifacts `cutover-decision-run-manifest.json`, `cutover-audit-link-index.json`, `cutover-decision.json`, `next-milestone-route.json`, `redacted-cutover-decision-report.md`, `contract-snapshots/phase35_cutover_decision_artifact_contract.json`, `contract-snapshots/phase34_final_readiness_demotion_dry_run_contract.json`, and `contract-snapshots/phase34-final-readiness-run-manifest.json`;
   - the D-01/D-03/D-04 verdict truth table, D-11 route truth table, and the D-15/D-16 demotion projection fields `demotion_decision_validation_state`, `demotion_decision_state`, `demotion_decision_source_refs`, `demotion_gate_state`, and `demotion_gate_reason_codes`. Contract the validation states as `missing`, `malformed`, `stale`, `lifecycle-mismatched`, `invalid`, or `valid`; the explicit decision states as `missing`, `approve`, or `reject`; and the gate states as `blocked` or `open`. Keep Phase 33 validation/value/source refs separate from Phase 34 gate/reason codes, plus D-10 secret/raw-material prohibitions, D-14/D-18 no-production-action semantics, and verification commands.
2. Create focused standard-library `unittest` fixtures that import the future verifier and cover one concern per test with Arrange/Act/Assert sections. Derive fixture inputs from sanitized Phase 34/33 schemas, not raw evidence.
3. Cover all three verdicts; every D-02 invalid family; exact/broad/unmatched/rejected/expired/stale exception behavior; all nine audit categories; all D-09 anti-joins; deterministic link order/IDs/digests; D-12 repair-scope traceability, including unresolved and fabricated exit/review criterion refs producing `route-scope-incomplete` while the verdict remains `blocked`; planning-only approved route; D-15 through D-18 demotion non-implication and non-collapsing `missing`, `malformed`, `stale`, `lifecycle-mismatched`, other `invalid`, valid `approve`, and valid `reject` fixtures; unsafe path/symlink/ref/secret cases; JSON/Markdown shared-projection consistency; and required wiring strings.
4. Do not create `tools/bazel/phase35_cutover_decision_artifact.py` in this task. The test command must discover the suite and fail because the implementation module is absent, establishing the required RED state rather than a syntax or fixture failure.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -m json.tool tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json &gt;/dev/null; test ! -e tools/bazel/phase35_cutover_decision_artifact.py; python3 -m py_compile tools/bazel/phase35_cutover_decision_artifact_test.py; red_log=$(mktemp); trap "rm -f $red_log" EXIT; set +e; python3 -m unittest discover -s tools/bazel -p "phase35_cutover_decision_artifact_test.py" -q &gt;"$red_log" 2&gt;&amp;1; red_status=$?; set -e; test "$red_status" -ne 0; grep -F "ModuleNotFoundError: No module named '\''phase35_cutover_decision_artifact'\''" "$red_log"; grep -F "FAILED (errors=1)" "$red_log"; ! grep -E "SyntaxError|JSONDecodeError|AssertionError|FileNotFoundError" "$red_log"'</automated>
  </verify>
  <acceptance-criteria>
    - The contract parses as JSON and contains the exact lifecycle, requirements, verdict/route enums, nine audit kinds, fields, reason codes, truth tables, security constraints, non-collapsing demotion fields/enums, and eight generated artifact names above.
    - The test file is syntactically valid, `unittest` discovery starts, and the captured failure contains exactly the expected missing `phase35_cutover_decision_artifact` module boundary plus `FAILED (errors=1)`; syntax, JSON, fixture, discovery, assertion, and unrelated runtime failures do not satisfy RED verification.
    - Tests name every CUTOVER-01/02/03 truth table and T-35-01 through T-35-06 threat boundary; no deferred production action, OPA, signing, dashboard, or retained-code replacement behavior is specified.
  </acceptance-criteria>
  <done>Wave 0 contract and focused RED suite exist, fully trace D-01 through D-22, and fail only at the intentionally missing implementation boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the canonical audit index, verdict reducer, route projection, and generated bundle</name>
  <read-first>
    - `tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json`
    - `tools/bazel/phase35_cutover_decision_artifact_test.py`
    - `tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`
    - `tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
    - `tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `tools/bazel/phase33_maintainer_decision_inputs.py`
    - `build/ci-evidence/phase34/final-readiness-run-manifest.json`
    - `build/ci-evidence/phase34/readiness-coverage-ledger.json`
    - `build/ci-evidence/phase34/final-readiness-packet.json`
    - `build/ci-evidence/phase34/readiness-blocker-summary.json`
    - `build/ci-evidence/phase34/demotion-dry-run.json`
  </read-first>
  <files>
    tools/bazel/phase35_cutover_decision_artifact.py
    tools/bazel/phase35_cutover_decision_artifact_test.py
  </files>
  <behavior>
    - RED tests from Task 1 become green without weakening their assertions.
    - The pure core normalizes validated facts, derives expected audit links, reduces one verdict, derives one route, and projects demotion state without filesystem side effects.
    - The imperative shell alone validates paths/lifecycles/security, loads referenced artifacts, writes deterministic files, snapshots safe sources, and handles CLI modes.
  </behavior>
  <action>
Implement a standard-library-only verifier/generator that satisfies the frozen contract and D-01 through D-18, D-20 through D-22.

1. Expose CLI modes `--contract-only`, `--quick`, `--security-only`, and `--wiring-only`, with path options `--phase34-output-dir` defaulting to `build/ci-evidence/phase34` and `--output-dir` defaulting to `build/ci-evidence/phase35`. Do not accept a verdict, route, cutover confirmation, demotion approval, or evidence payload as a CLI input (D-05).
2. Start only from `build/ci-evidence/phase34/final-readiness-run-manifest.json`. Require artifact name `phase34-final-readiness-demotion-dry-run`, lifecycle `34-2026-07-25T18-18-48`, output root equality, `raw_evidence_consumed: false`, the exact Phase 34 artifact set, canonical filenames, and safe source/snapshot refs. Follow only those validated refs and the Phase 33 register refs reached through `contract-snapshots/phase33-downstream-handoff-manifest.json`; never filesystem-search for alternate inputs.
3. Reuse the exact Phase 34 containment model: repository-relative paths only, `build/ci-evidence/phase34` and contract-reached Phase 31-33 roots only, input/output non-overlap, resolved-path root containment, symlink escape rejection, allowed `external://phaseXX/` refs without dereference, recursive forbidden-field scans, forbidden-text scans, and pre-write rejection. Snapshot only the Phase 35 contract, Phase 34 contract, and Phase 34 run manifest listed in the contract (D-10).
4. Keep a pure functional core with exactly these transformations:
   - normalize validated Phase 34 readiness/ledger/blocker/demotion facts and the Phase 33 normalized decision/register lineage;
   - derive the expected canonical audit-link set independently from upstream rows/records;
   - reduce the validated facts through the frozen verdict truth table;
   - derive the route and independent demotion projection.
5. Derive stable link IDs from `kind` plus existing `submission_id`, `row_id`, or `decision_id` when present. Use a deterministic fallback from the sanitized semantic target, sort by `(kind, target_id, target_ref)`, include SHA-256 `digest` only for validated sanitized local targets or canonical sanitized snapshot rows, and omit it for opaque `external://phaseXX/` refs. Reject missing/extra/duplicate/dangling/lifecycle/category/digest mismatches by exact-set anti-join before trusting outputs (D-06 through D-09).
6. Emit `cutover-decision.json` with exact top-level fields `artifact_name`, `phase`, `phase_lifecycle_id`, `requirement_ids`, `cutover_verdict`, `reason_codes`, `readiness_state`, `readiness_result_ref`, `active_exception_ids`, `blocker_ids`, `audit_link_index_ref`, `audit_link_counts_by_kind`, `demotion_decision_validation_state`, `demotion_decision_state`, `demotion_decision_source_refs`, `demotion_gate_state`, `demotion_gate_reason_codes`, `route_ref`, and `raw_evidence_consumed`.
   - Derive `demotion_decision_validation_state` by validating the Phase 33 `demotion-decision-handoff.json`, its referenced normalized `reference_demotion` decision, and pinned lifecycle: `missing` for no supplied input, `malformed` for schema/shape failure, `stale` for an expired/out-of-date decision, `lifecycle-mismatched` for Phase 33 lifecycle drift, `invalid` for other cross-reference/value failures, and `valid` only after all checks pass. Do not collapse the first four failure classes into `invalid`.
   - Project `demotion_decision_state` separately as the explicit Phase 33 value `missing`, `approve`, or `reject`, and preserve `demotion_decision_source_refs` even when validation fails whenever the refs can be safely parsed. A rejected input is `validation_state: valid` plus `decision_state: reject`, not `invalid`.
   - Project `demotion_gate_state` and `demotion_gate_reason_codes` unchanged from Phase 34 `demotion-dry-run.json` fields `gate_state` and `reason_codes`; cross-check its `approval_validation_state`, `approval_decision_state`, and `source_refs` against the richer Phase 33 projection. Any inconsistency blocks with the appropriate source/lifecycle reason. None of these fields participates in an implicit cutover or demotion upgrade (D-15 through D-18).
7. Emit `next-milestone-route.json` with exact top-level fields `artifact_name`, `phase`, `phase_lifecycle_id`, `route`, `source_verdict`, `follow_up_scope`, `requires_fresh_cutover_decision`, `planning_only`, and `production_actions_authorized`.
   - For `approved`, emit `production-cutover-planning`, empty `follow_up_scope`, `requires_fresh_cutover_decision: false`, `planning_only: true`, and `production_actions_authorized: false`.
   - For `blocked` and `approved-with-exceptions`, emit `targeted-blocker-repair`, `requires_fresh_cutover_decision: true`, `planning_only: true`, and `production_actions_authorized: false`.
   - Each repair scope row must contain `scope_id`, `blocker_refs`, `exception_refs`, `residual_risk_refs`, `requirement_ids`, `affected_gates`, `owner_ref`, `required_action_ref`, and `exit_review_criterion_refs`. Derive blocker identity, owner, action, requirements, and gate only from Phase 32 `blocker-register.json` fields `row_id`, `owner_ref`, `required_next_action`, `requirement_ids`, and `affected_gate`; `required_action_ref` is the canonical field ref `build/ci-evidence/phase32/blocker-register.json#<row_id>/required_next_action`, never copied or invented free-form text.
   - Build `exit_review_criterion_refs` by exact source type: every ordinary `repair_item` or `unresolved_decision_blocker` includes Phase 32 field refs `#<row_id>/affected_gate` and `#<row_id>/required_next_action` plus the Phase 34 ledger row whose `classification_ref` equals that blocker ref, specifically `readiness-coverage-ledger.json#<ledger-row-id>/reason_codes` and `/readiness_effect`; an exception adds its Phase 33 `exception-decision-register.json#<decision_id>/expiry_or_review_trigger` and `/affected_gates` refs; a residual-risk decision adds its Phase 33 `residual-risk-decision-register.json#<decision_id>/follow_up_refs` and `/affected_gates` refs. Resolve exception/residual decision rows through exact `source_row_refs`/`linked_blocker_refs`, and require every referenced field/value to exist in the validated upstream artifact.
   - Reject any missing, fabricated, unresolved, wrong-row, wrong-axis, or free-form criterion/action ref. Retain `blocked`, add `route-scope-incomplete`, and emit only the source-backed portion of scope; focused tests must cover ordinary blockers as well as exception and residual-risk rows (D-12/D-13).
8. Write the eight contract-defined artifacts beneath `build/ci-evidence/phase35`. Generate the Markdown report from the same in-memory canonical link/verdict/route/demotion structures as the JSON; show verdict, reason codes, every blocking predicate, active exception, named repair scope, all nine link counts, and independent demotion states. Run generated-output validation after writes and reject projection drift.
9. Keep default/quick generation durable and non-authorizing: it must write a complete `blocked` decision and `targeted-blocker-repair` route for valid blocked inputs; malformed unsafe inputs may return nonzero only after emitting a minimal blocked manifest/decision/route sufficient to preserve the fail-closed state. Never synthesize a real receipt, decision, exception, readiness approval, or demotion approval (D-21).
10. Refactor only when it reduces duplication or keeps the verifier below the Bright Builds readability triggers. Do not add a dependency, policy runtime, evidence schema, maintainer-decision schema, signing layer, dashboard, production action, or reference mutation.
  </action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/phase35_cutover_decision_artifact_test.py &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact_test.py -q &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact.py --contract-only &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact.py --security-only &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact.py --quick --phase34-output-dir build/ci-evidence/phase34 --output-dir build/ci-evidence/phase35 &amp;&amp; python3 -c 'import json; from pathlib import Path; root=Path("build/ci-evidence/phase35"); decision=json.loads((root/"cutover-decision.json").read_text()); route=json.loads((root/"next-milestone-route.json").read_text()); assert decision["cutover_verdict"] == "blocked"; assert route["route"] == "targeted-blocker-repair"; assert route["requires_fresh_cutover_decision"] is True; assert route["production_actions_authorized"] is False; assert decision["demotion_decision_validation_state"] == "missing"; assert decision["demotion_decision_state"] == "missing"; assert decision["demotion_decision_source_refs"] == []; assert decision["demotion_gate_state"] == "blocked"; assert "approval-missing" in decision["demotion_gate_reason_codes"]'</automated>
  </verify>
  <acceptance-criteria>
    - All Task 1 tests pass, including every verdict/route row, all nine link categories, exact-set anti-joins, exception boundaries, non-collapsing demotion validation/value/source/gate/reason states, fabricated/unresolved repair-criterion failures, security failures, and JSON/Markdown consistency.
    - The quick bundle contains exactly the eight contract artifacts, `cutover_verdict: blocked`, `route: targeted-blocker-repair`, `requires_fresh_cutover_decision: true`, and `production_actions_authorized: false`.
    - `cutover-decision.json` contains all exact fields above; missing, malformed, stale, lifecycle-mismatched, other invalid, rejected, and approved Phase 33 demotion states remain distinguishable from the Phase 34 gate and its reason codes.
    - No raw evidence, private material, secret-bearing field, unsafe ref, new approval input, production mutation, or deferred feature is read, copied, emitted, or authorized.
  </acceptance-criteria>
  <done>The pure reducer and imperative generator produce a deterministic, secret-safe, fail-closed Phase 35 bundle that fully satisfies CUTOVER-01/02/03 at the code and artifact boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Bazel, root aliases, workflow regeneration, the just facade, and final validation</name>
  <read-first>
    - `tools/bazel/phase35_cutover_decision_artifact.py`
    - `tools/bazel/phase35_cutover_decision_artifact_test.py`
    - `tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json`
    - `tools/bazel/BUILD.bazel`
    - `BUILD.bazel`
    - `tools/bazel/rust_workflow.sh`
    - `justfile`
    - `.planning/phases/35-cutover-decision-artifact/35-VALIDATION.md`
    - `.planning/phases/34-final-readiness-and-demotion-dry-run/34-01-SUMMARY.md`
    - `.planning/phases/34-final-readiness-and-demotion-dry-run/34-02-SUMMARY.md`
  </read-first>
  <files>
    tools/bazel/BUILD.bazel
    BUILD.bazel
    tools/bazel/rust_workflow.sh
    justfile
    .planning/phases/35-cutover-decision-artifact/35-VALIDATION.md
  </files>
  <action>
Complete D-19/D-21 wiring with one shared-file integration task.

1. In `tools/bazel/BUILD.bazel`, add:
   - `phase35_source_ref_manifests` containing the Phase 31, 32, 33, 34, and 35 contract JSON files;
   - `phase35_verify` as a `shell_binary` over `rust_workflow.sh` with the Phase 26/27/28/31/32/33/34/35 verifier scripts, the Phase 35 contract, `:phase35_source_ref_manifests`, and `//:phase35_cutover_decision_artifact_docs` in `data`;
   - `phase35_verify_tests` as a `shell_binary` over `rust_workflow.sh` with the Phase 35 verifier, test, contract, and source-ref manifest filegroup in `data`.
2. In root `BUILD.bazel`, add `phase35_cutover_decision_artifact_docs` containing `35-CONTEXT.md`, `35-RESEARCH.md`, `35-VALIDATION.md`, and `35-01-PLAN.md`, plus root aliases `phase35_verify -> //tools/bazel:phase35_verify` and `phase35_verify_tests -> //tools/bazel:phase35_verify_tests`.
3. In `tools/bazel/rust_workflow.sh`, add exact case arms:
   - `phase35_verify_tests)` runs `python3 tools/bazel/phase35_cutover_decision_artifact_test.py`;
   - `phase35_verify)` runs, in order, the existing Phase 34 prerequisite commands for Phase 31 quick, Phase 26 quick, Phase 27 quick, Phase 28 quick, Phase 32 quick, Phase 33 quick, Phase 34 `--wiring-only`, and Phase 34 `--quick`; then runs Phase 35 `--wiring-only`; then runs `python3 tools/bazel/phase35_cutover_decision_artifact.py --quick --phase34-output-dir build/ci-evidence/phase34 --output-dir build/ci-evidence/phase35`.
   Preserve the exact existing Phase 34 command arguments from its case arm; do not replace them with hidden environment state or real evidence.
4. In `justfile`, add exactly:
   - `phase35-verify:`
   - `bazel run //tools/bazel:phase35_verify_tests`
   - `bazel run //tools/bazel:phase35_verify`
   with tests before generation.
5. Extend the verifier's wiring check only as needed to assert the exact target, alias, docs, case-arm, command-order, and `just` recipe strings above.
6. After all automated verification succeeds, update `35-VALIDATION.md` frontmatter to `status: verified`, `nyquist_compliant: true`, and `wave_0_complete: true`; mark all three task rows green with existing files; check every Validation Sign-Off item; and set Approval to `verified`. Do not mark validation green before the commands pass.
7. Run the focused, Bazel, developer-facade, Phase 28/31-35 regression, scoped pre-commit, diff, and full Rust commit gates below. Run `pre-commit` against exactly this plan's changed tracked Python, JSON, Markdown, shell, Bazel, and `justfile` paths before the Rust sequence; then inspect any hook-generated changes and rerun `git diff --check`, `git diff --stat`, and the scoped diff. Preserve the unrelated existing `.planning/config.json` worktree modification and exclude it from this plan's commit.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase35_cutover_decision_artifact.py --wiring-only &amp;&amp; bazel run //tools/bazel:phase35_verify_tests &amp;&amp; bazel run //tools/bazel:phase35_verify &amp;&amp; just phase35-verify &amp;&amp; python3 tools/bazel/phase28_final_readiness_packet_test.py -q &amp;&amp; python3 tools/bazel/phase31_final_evidence_intake_test.py -q &amp;&amp; python3 tools/bazel/phase32_blocker_register_triage_test.py -q &amp;&amp; python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q &amp;&amp; python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact_test.py -q &amp;&amp; pre-commit run --files tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json tools/bazel/phase35_cutover_decision_artifact_test.py tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/BUILD.bazel BUILD.bazel tools/bazel/rust_workflow.sh justfile .planning/phases/35-cutover-decision-artifact/35-VALIDATION.md &amp;&amp; git diff --check &amp;&amp; git diff --stat -- tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json tools/bazel/phase35_cutover_decision_artifact_test.py tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/BUILD.bazel BUILD.bazel tools/bazel/rust_workflow.sh justfile .planning/phases/35-cutover-decision-artifact/35-VALIDATION.md &amp;&amp; git diff -- tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json tools/bazel/phase35_cutover_decision_artifact_test.py tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/BUILD.bazel BUILD.bazel tools/bazel/rust_workflow.sh justfile .planning/phases/35-cutover-decision-artifact/35-VALIDATION.md &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    - Bazel targets `//tools/bazel:phase35_verify` and `//tools/bazel:phase35_verify_tests`, root aliases, docs filegroup, workflow arms, and `just phase35-verify` all exist and pass.
    - The workflow deterministically regenerates Phase 31-34 quick prerequisites, preserves their blocked/default meaning, and generates a Phase 35 blocked/repair bundle without real authorization.
    - Phase 28 and Phase 31-35 regression suites pass.
    - Scoped `pre-commit` passes before the Rust sequence; any hook-generated changes are reviewed, `git diff --check` is rerun, and the Rust gates then run in the mandatory order: fmt, clippy with `-D warnings`, build all targets/features, then test all features.
    - `35-VALIDATION.md` is verified/Nyquist-compliant/Wave-0-complete only after evidence is green, and final diff review shows no unrelated or generated build artifacts staged.
  </acceptance-criteria>
  <done>The complete Phase 35 verification facade is green, default output remains fail-closed, all relevant regressions and Rust commit gates pass, and validation is truthfully signed off.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 34 bundle → Phase 35 loader | Generated but untrusted JSON, Markdown refs, lifecycle metadata, and snapshot refs enter the Phase 35 validation boundary. |
| Referenced Phase 31-33 artifacts → canonical audit index | Repository-relative and `external://phaseXX/` refs are resolved into semantic audit links without reading raw evidence. |
| Filesystem paths → artifact reader/writer | Caller-controlled input/output paths and symlinks could escape allowed roots, overlap inputs, or redirect snapshots. |
| Canonical facts → verdict and route | Invalid facts could be used to overclaim approved cutover or production planning if the reducer or route mapping is not total. |
| Phase 33 demotion input / Phase 34 dry run → final report | Separate authorization and gate states could be collapsed into an inferred demotion approval. |
| Canonical JSON → redacted Markdown | Human-readable projection could omit blockers or claim a different verdict/route than machine-readable outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-35-01 | Tampering / Repudiation | Canonical audit-link derivation and index | high | mitigate | Derive expected links independently from validated Phase 31-34 semantic IDs; exact-set anti-join missing/extra/duplicate/dangling rows; validate category, lifecycle, and local digest before verdict publication; focused negative tests block on every mismatch. |
| T-35-02 | Tampering / Elevation of Privilege | Input/output path handling | high | mitigate | Require repository-relative contract-rooted paths, reject absolute/traversal/wrong-root/overlap paths, resolve containment, reject symlink escapes before reads/writes, and test nested source plus output escapes. |
| T-35-03 | Information Disclosure | Artifact loading, snapshots, JSON, and Markdown | high | mitigate | Consume sanitized summaries/refs only, never dereference opaque external refs or copy raw payloads, recursively reject secret-bearing fields/text, snapshot only the three contract-listed safe sources, and scan every generated artifact before trust. |
| T-35-04 | Spoofing / Tampering | Phase lifecycle and contract lineage | high | mitigate | Pin Phase 35 lifecycle `35-2026-07-25T21-06-10`, Phase 34 lifecycle `34-2026-07-25T18-18-48`, contract/artifact identities, canonical filenames, exact generated artifact set, and reached Phase 31-33 lifecycle values; any drift selects blocked. |
| T-35-05 | Elevation of Privilege / Repudiation | Verdict reducer and milestone route | high | mitigate | Use closed total enums and truth tables, default blocked, map only approved to production-cutover-planning, mark every route planning-only with `production_actions_authorized: false`, and reject caller-supplied verdict/route or overclaim markers. |
| T-35-06 | Elevation of Privilege | Demotion decision and dry-run projection | high | mitigate | Keep `cutover_verdict`, `demotion_decision_state`, and `demotion_gate_state` independent; project exact Phase 33/34 source refs and reason states; exhaustively test that green evidence, readiness, cutover approval, exceptions, and route never synthesize demotion authorization. |
</threat-model>

<verification>
The executor must preserve fast feedback after each task and run the full final gate in this order:

1. Task 1: compile the test file and prove the discovered suite is RED because the implementation file is absent.
2. Task 2: compile verifier/tests, run the complete Phase 35 unit suite, contract/security modes, quick generation, and explicit blocked/repair/demotion assertions.
3. Task 3: run wiring-only, both Bazel labels, `just phase35-verify`, and Phase 28 and Phase 31-35 regression suites.
4. Run scoped `pre-commit` for the plan's changed tracked files, inspect any hook-generated changes, then rerun `git diff --check`, `git diff --stat`, and the scoped diff.
5. Before commit, run exactly:
   1. `cargo fmt --all`
   2. `cargo clippy --all-targets --all-features -- -D warnings`
   3. `cargo build --all-targets --all-features`
   4. `cargo test --all-features`
6. Review the final scoped diff again after the Rust gates. Do not stage or revert the pre-existing `.planning/config.json` modification or any unrelated user/agent changes.
</verification>

<success-criteria>
- CUTOVER-01: One closed, deterministic verdict is emitted and every invalid/incomplete state fails to `blocked`.
- CUTOVER-02: The canonical link index covers all nine required audit categories with stable IDs, safe refs, lifecycle, verdict effect, and sanitized local digests; every anti-join or mismatch blocks.
- CUTOVER-03: `approved` exclusively routes to planning-only `production-cutover-planning`; `blocked` and `approved-with-exceptions` exclusively route to traceable `targeted-blocker-repair` and require a fresh decision.
- Default quick verification remains `blocked`/`targeted-blocker-repair` and does not synthesize evidence, approvals, exceptions, or demotion authorization.
- Reference demotion remains an independent POST-01 authorization/execution boundary.
- Focused tests, Bazel labels, `just`, Phase 28/31-35 regressions, diff checks, and all four Rust commit gates pass.
</success-criteria>

<output>
After completion, create `.planning/phases/35-cutover-decision-artifact/35-01-SUMMARY.md`.
</output>
