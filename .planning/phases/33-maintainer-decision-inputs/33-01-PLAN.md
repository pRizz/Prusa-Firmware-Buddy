---
phase: 33-maintainer-decision-inputs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
  - tools/bazel/phase33_maintainer_decision_inputs.py
  - tools/bazel/phase33_maintainer_decision_inputs_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
  - .planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md
autonomous: true
requirements:
  - DECIDE-01
  - DECIDE-02
  - DECIDE-03
must_haves:
  truths:
    - "Maintainer can record retained-code accept, reject, or exception-approved decisions only from explicit machine-readable input."
    - "Maintainer can record residual-risk accept or reject decisions only from explicit machine-readable input."
    - "Maintainer can approve or reject exception requests with exact Phase 32 blocker-row and affected-gate coverage."
    - "Maintainer can record readiness approve or block input as a Phase 34 handoff without generating a final readiness packet."
    - "Maintainer can record reference-demotion approve or reject input as a separate authorization handoff."
    - "Green evidence, Phase 32 blocker state, readiness input, retained-code input, residual-risk input, or exception input cannot imply another approval axis."
  artifacts:
    - path: "tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json"
      provides: "Phase 33 schema, decision enums, source contracts, artifact list, and verification commands"
      contains: "phase33_maintainer_decision_inputs_contract"
    - path: "tools/bazel/phase33_maintainer_decision_inputs.py"
      provides: "Phase 33 decision-input validator, normalizer, output writer, security scan, and wiring check"
      exports: ["main"]
    - path: "tools/bazel/phase33_maintainer_decision_inputs_test.py"
      provides: "Unit and integration tests for DECIDE-01, DECIDE-02, DECIDE-03, security, and wiring"
      contains: "unittest"
    - path: "build/ci-evidence/phase33/downstream-handoff-manifest.json"
      provides: "Machine-readable Phase 34 and Phase 35 handoff generated by quick verification"
      contains: "phase33-maintainer-decision-inputs"
  key_links:
    - from: "tools/bazel/phase33_maintainer_decision_inputs.py"
      to: "build/ci-evidence/phase32/downstream-handoff-manifest.json"
      via: "--phase32-handoff loader"
      pattern: "DEFAULT_PHASE32_HANDOFF"
    - from: "tools/bazel/phase33_maintainer_decision_inputs.py"
      to: "build/ci-evidence/phase32/blocker-register.json"
      via: "Phase 32 canonical_register_ref resolution"
      pattern: "canonical_register_ref"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase33_maintainer_decision_inputs.py"
      via: "phase33_verify case arm"
      pattern: "phase33_verify\\)"
    - from: "justfile"
      to: "//tools/bazel:phase33_verify"
      via: "phase33-verify recipe"
      pattern: "phase33-verify:"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-07-04T01-36-41
generated_at: 2026-07-04T01:57:26.775Z
---

<objective>
Create the Phase 33 maintainer decision-input contract, verifier, tests, and developer workflow wiring.

Purpose: Maintainers need explicit machine-readable retained-code, residual-risk, exception, readiness, and reference-demotion decision inputs that consume Phase 32 triage outputs without treating evidence status as authorization.
Output: A Phase 33 verifier that writes a secret-safe `build/ci-evidence/phase33` handoff bundle for Phase 34 and Phase 35.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-maintainer-decision-inputs/33-CONTEXT.md
@.planning/phases/33-maintainer-decision-inputs/33-RESEARCH.md
@.planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md
@.planning/phases/32-blocker-register-and-evidence-triage/32-01-SUMMARY.md
@.planning/phases/32-blocker-register-and-evidence-triage/32-VERIFICATION.md
@tools/bazel/manifests/phase32_blocker_register_triage_contract.json
@tools/bazel/phase32_blocker_register_triage.py
@tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
@tools/bazel/phase27_retained_code_acceptance_decisions.py
@tools/bazel/manifests/phase28_final_readiness_packet_contract.json
@tools/bazel/phase28_final_readiness_packet.py
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md

<interfaces>
Phase 32 canonical blocker rows expose these required fields and remain authoritative per D-01:
```text
row_id, source_stream, source_ref, requirement_ids, affected_gate, row_problem_kind,
blocker_kind, severity, owner_ref, required_next_action, decision_impact,
proof_eligibility, evidence_refs
```

Phase 32 decision-impact enum values that Phase 33 must consume without reclassifying:
```text
repair_required_before_cutover
exception_decision_required
residual_risk_decision_required
retained_code_decision_required
final_readiness_blocked
demotion_decision_required
cutover_verdict_blocked
```

Phase 32 hard blocker problem kinds that must fail closed for normal approval paths per D-08:
```text
redaction_failed
source_ref_failed
secret_tainted
lifecycle_mismatch
unsafe_ref
```

Phase 33 generated artifacts required by D-18:
```text
maintainer-decision-input-template.json
normalized-decision-records.json
retained-code-decision-register.json
residual-risk-decision-register.json
exception-decision-register.json
readiness-decision-handoff.json
demotion-decision-handoff.json
decision-validation-report.json
downstream-handoff-manifest.json
redacted-maintainer-decision-report.md
contract-snapshots/phase33_maintainer_decision_inputs_contract.json
contract-snapshots/phase32_blocker_register_triage_contract.json
contract-snapshots/phase27_retained_code_acceptance_decisions_contract.json
contract-snapshots/phase28_final_readiness_packet_contract.json
contract-snapshots/phase32-downstream-handoff-manifest.json
contract-snapshots/phase32-blocker-register.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 33 contract and RED regression tests</name>
  <files>tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json, tools/bazel/phase33_maintainer_decision_inputs_test.py</files>
  <read_first>
    - tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json (if present)
    - tools/bazel/phase33_maintainer_decision_inputs_test.py (if present)
    - .planning/phases/33-maintainer-decision-inputs/33-CONTEXT.md
    - .planning/phases/33-maintainer-decision-inputs/33-RESEARCH.md
    - .planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md
    - tools/bazel/manifests/phase32_blocker_register_triage_contract.json
    - tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
    - tools/bazel/manifests/phase28_final_readiness_packet_contract.json
    - tools/bazel/phase32_blocker_register_triage_test.py
  </read_first>
  <behavior>
    - Test 1: contract id, phase, lifecycle, output root, requirement IDs, source contracts, generated artifacts, and verification commands are exact.
    - Test 2: retained-code accept, reject, and exception-approved inputs require explicit decision metadata, residual-risk rationale, owner signoff, and Phase 32 source row refs.
    - Test 3: residual-risk accept and reject inputs require explicit owner signoff, affected gates, rationale, and follow-up refs.
    - Test 4: exception approval requires Phase 32 `exception_request` rows, exact source row ref matching, and affected-gate scope matching.
    - Test 5: readiness approval rejects unresolved critical blockers without approved exception coverage or accepted residual-risk coverage; readiness block remains valid.
    - Test 6: reference-demotion approve and reject decisions are separate from retained-code, residual-risk, exception, and readiness decisions.
    - Test 7: green evidence or a no-input quick run cannot create approval for retained code, residual risk, exceptions, readiness, or demotion.
    - Test 8: stale lifecycle refs, wrong source rows, path traversal refs, unknown decision types, missing required fields, and secret/overclaim markers fail closed.
  </behavior>
  <action>Create `tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json` with `schema_version: "1"`, `id: "phase33_maintainer_decision_inputs_contract"`, `phase: "33-maintainer-decision-inputs"`, `phase_lifecycle_id: "33-2026-07-04T01-36-41"`, `artifact_name: "phase33-maintainer-decision-inputs"`, `output_root: "build/ci-evidence/phase33"`, and `requirement_ids: ["DECIDE-01", "DECIDE-02", "DECIDE-03"]`. Add `source_contracts` entries for `tools/bazel/manifests/phase32_blocker_register_triage_contract.json`, `tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json`, and `tools/bazel/manifests/phase28_final_readiness_packet_contract.json` so the Phase 33 wrapper reuses Phase 27/28 vocabulary while binding decisions to Phase 32 rows per D-04. Add `source_inputs.phase32_handoff: "build/ci-evidence/phase32/downstream-handoff-manifest.json"`, `source_inputs.phase32_canonical_register: "build/ci-evidence/phase32/blocker-register.json"`, `source_inputs.phase32_lifecycle_id: "32-2026-07-03T14-13-51"`, and `source_inputs.raw_evidence_consumed: false` per D-01 and D-19. Add `decision_record_schema.required_fields` exactly as `["decision_id", "decision_type", "decision_value", "source_row_refs", "maintainer_identity_ref", "maintainer_role", "owner_signoff_ref", "decision_timestamp", "rationale", "evidence_refs", "artifact_refs"]` per D-02. Add `enums.decision_type` exactly `["retained_code", "residual_risk", "exception", "readiness", "reference_demotion"]`; add `enums.decision_value.retained_code` exactly `["accept", "reject", "exception_approve"]`; add `enums.decision_value.residual_risk` exactly `["accept", "reject"]`; add `enums.decision_value.exception` exactly `["approve", "reject"]`; add `enums.decision_value.readiness` exactly `["approve", "block"]`; add `enums.decision_value.reference_demotion` exactly `["approve", "reject"]` per D-03, D-06, D-07, D-09, D-12, and D-15. Add `hard_blocker_problem_kinds` exactly `["redaction_failed", "source_ref_failed", "secret_tainted", "lifecycle_mismatch", "unsafe_ref"]` per D-08. Add `exception_policy.exact_source_row_ref_match: true`, `exception_policy.affected_gate_must_match: true`, and `exception_policy.required_fields` exactly `["scope", "expiry_or_review_trigger", "affected_requirements", "affected_gates", "rationale", "owner_signoff_ref", "linked_blocker_refs"]` per D-09 and D-10. Add `generated_artifacts` exactly matching the list in the `<interfaces>` block per D-18. Add `prohibited_approval_semantics` strings stating that Phase 33 does not reclassify Phase 32 rows, collect new evidence, generate the final readiness packet, run a demotion dry run, publish a cutover verdict, infer retained-code acceptance, infer residual-risk acceptance, infer exception approval, infer readiness approval, infer reference-demotion approval, or emit `demotion_allowed`. Add `verification_commands` exactly `python3 -m py_compile tools/bazel/phase33_maintainer_decision_inputs.py tools/bazel/phase33_maintainer_decision_inputs_test.py`, `python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q`, `python3 tools/bazel/phase33_maintainer_decision_inputs.py --contract-only`, `python3 tools/bazel/phase33_maintainer_decision_inputs.py --security-only`, `python3 tools/bazel/phase33_maintainer_decision_inputs.py --wiring-only`, `python3 tools/bazel/phase33_maintainer_decision_inputs.py --quick --phase32-handoff build/ci-evidence/phase32/downstream-handoff-manifest.json --output-dir build/ci-evidence/phase33`, and `just phase33-verify`. Create `tools/bazel/phase33_maintainer_decision_inputs_test.py` using Python `unittest`, `tempfile`, `json`, and `pathlib`, with test methods named `test_contract_lists_all_decision_axes_and_artifacts`, `test_quick_without_maintainer_input_writes_template_and_blocked_handoffs`, `test_retained_and_residual_decisions_require_explicit_metadata_and_owner_signoff`, `test_hard_blocker_problem_kinds_reject_normal_acceptance`, `test_exception_approval_requires_exact_row_ref_and_gate_match`, `test_rejected_exception_remains_in_exception_register`, `test_readiness_approval_rejects_uncovered_critical_blocker`, `test_readiness_block_handoff_preserves_blocker_refs`, `test_demotion_decision_is_separate_from_readiness_and_evidence`, `test_green_evidence_does_not_create_any_approval`, `test_stale_lifecycle_unknown_type_unresolved_ref_and_malformed_ref_fail_closed`, `test_security_scan_rejects_secret_fields_path_traversal_and_approval_overclaims`, and `test_wiring_requires_bazel_root_workflow_and_just_entries`. Each test must use explicit `# Arrange`, `# Act`, and `# Assert` comments unless the test is a one-line contract assertion. The RED state should fail because `tools/bazel/phase33_maintainer_decision_inputs.py` is not implemented yet; do not weaken assertions to make the test pass before Task 2.</action>
  <verify>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q</automated>
    <expected>RED: exits nonzero before Task 2, with failures or errors tied to missing Phase 33 verifier behavior.</expected>
  </verify>
  <acceptance_criteria>
    - `grep -q '"id": "phase33_maintainer_decision_inputs_contract"' tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `grep -q '"DECIDE-01"' tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `grep -q '"retained_code"' tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `grep -q '"reference_demotion"' tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `grep -q '"demotion_allowed"' tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json`
    - `grep -q 'test_green_evidence_does_not_create_any_approval' tools/bazel/phase33_maintainer_decision_inputs_test.py`
    - `grep -q 'test_exception_approval_requires_exact_row_ref_and_gate_match' tools/bazel/phase33_maintainer_decision_inputs_test.py`
    - `grep -q '# Arrange' tools/bazel/phase33_maintainer_decision_inputs_test.py`
  </acceptance_criteria>
  <done>Contract exists with every D-01 through D-20 policy surface represented, and RED tests describe DECIDE-01, DECIDE-02, DECIDE-03 behavior before production implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 33 verifier and generated handoff bundle</name>
  <files>tools/bazel/phase33_maintainer_decision_inputs.py, tools/bazel/phase33_maintainer_decision_inputs_test.py, tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json</files>
  <read_first>
    - tools/bazel/phase33_maintainer_decision_inputs.py (if present)
    - tools/bazel/phase33_maintainer_decision_inputs_test.py
    - tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
    - tools/bazel/phase32_blocker_register_triage.py
    - tools/bazel/phase27_retained_code_acceptance_decisions.py
    - tools/bazel/phase28_final_readiness_packet.py
    - tools/bazel/manifests/phase32_blocker_register_triage_contract.json
    - .planning/phases/33-maintainer-decision-inputs/33-CONTEXT.md
    - .planning/phases/33-maintainer-decision-inputs/33-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `--contract-only` validates the contract exactly and prints `phase33_maintainer_decision_inputs_contract ok`.
    - Test 2: `--quick` with no `--maintainer-decisions` writes all Phase 33 artifacts, sets all handoff states to blocked or pending input, and records `maintainer_input_supplied: false`.
    - Test 3: explicit retained-code and residual-risk decisions normalize into typed registers only when required metadata and source rows are valid.
    - Test 4: hard blocker rows with `redaction_failed`, `source_ref_failed`, `secret_tainted`, `lifecycle_mismatch`, or `unsafe_ref` cannot be accepted through retained-code, residual-risk, or exception approval.
    - Test 5: exception approval covers readiness only when source row refs exactly match Phase 32 blocker rows and `affected_gates` contains each row's `affected_gate`.
    - Test 6: readiness approval fails closed when uncovered critical blockers remain; readiness block remains valid and preserves blocker refs.
    - Test 7: demotion approval or rejection is written only to `demotion-decision-handoff.json` and never emits `demotion_allowed` or final readiness status.
  </behavior>
  <action>Implement `tools/bazel/phase33_maintainer_decision_inputs.py` as a Python standard-library CLI with constants `PHASE = "33-maintainer-decision-inputs"`, `PHASE_LIFECYCLE_ID = "33-2026-07-04T01-36-41"`, `PHASE32_LIFECYCLE_ID = "32-2026-07-03T14-13-51"`, `CONTRACT_MANIFEST = Path("tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json")`, `DEFAULT_PHASE32_HANDOFF = Path("build/ci-evidence/phase32/downstream-handoff-manifest.json")`, and `DEFAULT_OUTPUT_DIR = Path("build/ci-evidence/phase33")`. Add modes `--contract-only`, `--quick`, `--security-only`, and `--wiring-only`, plus optional `--maintainer-decisions`, `--phase32-handoff`, and `--output-dir`. Implement repo-relative path validation that rejects absolute paths, `..`, and symlink output-root escapes before file reads or writes. Implement `load_phase32_handoff()` so it loads only `downstream-handoff-manifest.json`, validates `phase_lifecycle_id == "32-2026-07-03T14-13-51"`, resolves `canonical_register_ref` under `build/ci-evidence/phase32`, loads `blocker-register.json`, and builds a `row_id -> row` map without reclassifying `row_problem_kind`, `blocker_kind`, `severity`, `owner_ref`, `required_next_action`, or `proof_eligibility` per D-01. Implement maintainer input parsing for a top-level JSON object with `schema_version: "1"`, `phase: "33-maintainer-decision-inputs"`, `phase_lifecycle_id: "33-2026-07-04T01-36-41"`, and `decisions: []`; reject unknown decision types, invalid values, duplicate `decision_id`, missing required fields, malformed `source_row_refs`, unresolved Phase 32 row ids, stale lifecycle ids, secret-bearing fields, and path traversal refs per D-02 and D-05. Accept only row refs shaped as `build/ci-evidence/phase32/blocker-register.json#<row_id>`. Implement decision normalization by axis: `retained_code` values `accept`, `reject`, `exception_approve`; `residual_risk` values `accept`, `reject`; `exception` values `approve`, `reject`; `readiness` values `approve`, `block`; `reference_demotion` values `approve`, `reject` per D-03, D-06, D-07, D-09, D-12, and D-15. Require every accepted or rejected decision to retain `decision_id`, `decision_type`, `decision_value`, `source_row_refs`, `maintainer_identity_ref`, `maintainer_role`, `owner_signoff_ref`, `decision_timestamp`, `rationale`, `evidence_refs`, and `artifact_refs`; validate timestamps as ISO UTC ending in `Z`. Reject retained-code `accept` or `exception_approve`, residual-risk `accept`, and exception `approve` when any source row has `row_problem_kind` in `redaction_failed`, `source_ref_failed`, `secret_tainted`, `lifecycle_mismatch`, or `unsafe_ref` per D-08. For exception `approve`, require every source row `blocker_kind == "exception_request"`, require `linked_blocker_refs` to equal `source_row_refs`, and require `affected_gates` to contain every referenced row's `affected_gate`; broad, empty, or unmatched scope is invalid per D-10. Preserve rejected retained-code, residual-risk, exception, readiness, and demotion rows in `normalized-decision-records.json` and the typed registers per D-11 and D-20. Implement readiness approval validation so `decision_value: "approve"` is invalid when any critical Phase 32 row remains uncovered by approved exception coverage or accepted residual-risk coverage; implement readiness `block` as valid when it preserves `blocked_source_row_refs` and `rationale` in `readiness-decision-handoff.json`; do not emit `final_readiness_status` because Phase 33 only creates a handoff per D-13 and D-14. Implement demotion handoff so `reference_demotion approve` records authorization data with `authorization_state: "approved-input-recorded"` and `phase34_must_validate_readiness: true`; `reject` records `authorization_state: "rejected"`; missing or invalid input records `authorization_state: "blocked"`; never emit `demotion_allowed` per D-15, D-16, and D-17. Write all generated artifacts listed in the contract under `build/ci-evidence/phase33`, including snapshots of the Phase 33 contract, Phase 32 contract, Phase 27 contract, Phase 28 contract, Phase 32 handoff, and Phase 32 blocker register. Write `downstream-handoff-manifest.json` with `phase`, `phase_lifecycle_id`, `artifact_name`, `maintainer_input_supplied`, `source_inputs.phase32_handoff_ref`, `source_inputs.phase32_canonical_register_ref`, `raw_evidence_consumed: false`, refs to every generated register, and `downstream_consumers: ["phase34-final-readiness-and-demotion-dry-run", "phase35-cutover-decision-artifact"]` per D-19 and D-20. Derive `redacted-maintainer-decision-report.md` from normalized JSON records only. Add `run_security_scan()` that scans maintainer input and generated outputs for forbidden field names and text markers including `access_token`, `api_key`, `authorization_header`, `certificate_private_material`, `client_secret`, `connect_token`, `credential_value`, `demotion_allowed`, `password`, `private_key`, `raw_crash_dump`, `raw_release_log`, `secret`, `secret_value`, `service_payload`, `signing_key_value`, `signing_payload_bytes`, `tls_keylog`, `token`, `token_value`, `wifi_credential`, `wifi_password`, private-key blocks, bearer tokens, `reference demotion approved`, `final readiness approved`, `final_readiness_status: "unblocked"`, `cutover verdict approved`, and `accepted by evidence alone`. Keep pure validation helpers separate from file I/O helpers where practical, with early returns and `maybe_` naming for optional locals under Bright Builds code-shape guidance.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase33_maintainer_decision_inputs.py tools/bazel/phase33_maintainer_decision_inputs_test.py</automated>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q</automated>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs.py --contract-only</automated>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs.py --security-only</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q` exits 0.
    - `python3 tools/bazel/phase33_maintainer_decision_inputs.py --contract-only` prints `phase33_maintainer_decision_inputs_contract ok`.
    - `grep -q 'PHASE32_LIFECYCLE_ID = "32-2026-07-03T14-13-51"' tools/bazel/phase33_maintainer_decision_inputs.py`
    - `grep -q 'DEFAULT_PHASE32_HANDOFF' tools/bazel/phase33_maintainer_decision_inputs.py`
    - `grep -q 'canonical_register_ref' tools/bazel/phase33_maintainer_decision_inputs.py`
    - `grep -q 'raw_evidence_consumed' tools/bazel/phase33_maintainer_decision_inputs.py`
    - `grep -q 'demotion_allowed' tools/bazel/phase33_maintainer_decision_inputs.py`
    - `test -f build/ci-evidence/phase33/downstream-handoff-manifest.json` after `--quick` runs.
    - `python3 - <<'PY'` script can load `build/ci-evidence/phase33/downstream-handoff-manifest.json` and assert `raw_evidence_consumed is False`, `phase_lifecycle_id == "33-2026-07-04T01-36-41"`, and `phase34-final-readiness-and-demotion-dry-run` is in `downstream_consumers`.
  </acceptance_criteria>
  <done>Phase 33 verifier passes the RED tests, writes the complete secret-safe handoff bundle, and preserves explicit authorization boundaries for DECIDE-01, DECIDE-02, and DECIDE-03.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Bazel, root aliases, workflow facade, just facade, and validation sign-off</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, .planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md, tools/bazel/phase33_maintainer_decision_inputs.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - .planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md
    - tools/bazel/phase33_maintainer_decision_inputs.py
    - tools/bazel/phase32_blocker_register_triage.py
    - .planning/phases/32-blocker-register-and-evidence-triage/32-01-SUMMARY.md
  </read_first>
  <action>Add `filegroup(name = "phase33_source_ref_manifests", srcs = [...])` to `tools/bazel/BUILD.bazel` with exact srcs `manifests/phase27_retained_code_acceptance_decisions_contract.json`, `manifests/phase28_final_readiness_packet_contract.json`, `manifests/phase32_blocker_register_triage_contract.json`, and `manifests/phase33_maintainer_decision_inputs_contract.json`. Add `shell_binary(name = "phase33_verify", src = "rust_workflow.sh", data = [...])` with exact data entries `phase31_final_evidence_intake.py`, `phase26_release_signing_upstream_evidence.py`, `phase27_retained_code_acceptance_decisions.py`, `phase28_final_readiness_packet.py`, `phase32_blocker_register_triage.py`, `phase33_maintainer_decision_inputs.py`, `manifests/phase33_maintainer_decision_inputs_contract.json`, `:phase33_source_ref_manifests`, and `//:phase33_maintainer_decision_inputs_docs`. Add `shell_binary(name = "phase33_verify_tests", src = "rust_workflow.sh", data = ["phase33_maintainer_decision_inputs.py", "phase33_maintainer_decision_inputs_test.py", "manifests/phase33_maintainer_decision_inputs_contract.json", ":phase33_source_ref_manifests"])`. In root `BUILD.bazel`, add `filegroup(name = "phase33_maintainer_decision_inputs_docs", srcs = [".planning/phases/33-maintainer-decision-inputs/33-CONTEXT.md", ".planning/phases/33-maintainer-decision-inputs/33-RESEARCH.md", ".planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md", ".planning/phases/33-maintainer-decision-inputs/33-01-PLAN.md"])`, plus aliases `phase33_verify` and `phase33_verify_tests` pointing to `//tools/bazel:phase33_verify` and `//tools/bazel:phase33_verify_tests`. In `tools/bazel/rust_workflow.sh`, add a `phase33_verify)` case arm that runs these commands in order: `python3 tools/bazel/phase31_final_evidence_intake.py --quick --output-dir build/ci-evidence/phase31`; `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26`; `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27`; `python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28`; `python3 tools/bazel/phase32_blocker_register_triage.py --quick --phase31-output-dir build/ci-evidence/phase31 --phase27-output-dir build/ci-evidence/phase27 --phase28-output-dir build/ci-evidence/phase28 --output-dir build/ci-evidence/phase32`; `python3 tools/bazel/phase33_maintainer_decision_inputs.py --wiring-only`; and `python3 tools/bazel/phase33_maintainer_decision_inputs.py --quick --phase32-handoff build/ci-evidence/phase32/downstream-handoff-manifest.json --output-dir build/ci-evidence/phase33`. Add `phase33_verify_tests)` that runs `python3 tools/bazel/phase33_maintainer_decision_inputs_test.py`. In `justfile`, add `phase33-verify:` with `bazel run //tools/bazel:phase33_verify_tests` followed by `bazel run //tools/bazel:phase33_verify`. Update `tools/bazel/phase33_maintainer_decision_inputs.py --wiring-only` so it verifies the exact snippets above, including just command order. After all verification below passes, update `.planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md` frontmatter to `nyquist_compliant: true`, `wave_0_complete: true`, and `status: verified`; update the Phase 33 task rows from `pending` to `green` only for rows whose automated command has passed.</action>
  <verify>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs.py --wiring-only</automated>
    <automated>bazel run //tools/bazel:phase33_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase33_verify</automated>
    <automated>just phase33-verify</automated>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs.py --security-only</automated>
    <automated>git diff --check</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "phase33_source_ref_manifests"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase33_verify"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase33_verify_tests"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase33_maintainer_decision_inputs_docs"' BUILD.bazel`
    - `grep -q 'actual = "//tools/bazel:phase33_verify"' BUILD.bazel`
    - `grep -q 'actual = "//tools/bazel:phase33_verify_tests"' BUILD.bazel`
    - `grep -q 'phase33_verify)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase33_verify_tests)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase33-verify:' justfile`
    - `grep -q 'nyquist_compliant: true' .planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md`
    - `grep -q 'wave_0_complete: true' .planning/phases/33-maintainer-decision-inputs/33-VALIDATION.md`
    - `python3 tools/bazel/phase33_maintainer_decision_inputs.py --wiring-only` exits 0 and prints a Phase 33 wiring success message.
    - `just phase33-verify` exits 0 and regenerates `build/ci-evidence/phase33/downstream-handoff-manifest.json`.
  </acceptance_criteria>
  <done>Bazel labels, root aliases, `rust_workflow.sh`, `just phase33-verify`, security scan, validation metadata, and Rust-project pre-commit verification are all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer JSON input -> Phase 33 verifier | Untrusted maintainer decision rows enter local normalization and can carry malformed refs, stale lifecycle ids, overclaim text, or secret-bearing fields. |
| Phase 32 handoff -> Phase 33 verifier | Phase 33 consumes prior generated rows and must reject stale lifecycle or unresolved source row replay instead of trusting arbitrary paths. |
| Phase 33 verifier -> build/ci-evidence/phase33 | Generated JSON and Markdown become downstream inputs for readiness and cutover decisions. |
| Phase 33 reports -> human maintainers | Markdown reports can overclaim authorization if not derived from machine rows and scanned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `load_maintainer_decisions`, `run_security_scan`, generated outputs | mitigate | Reject forbidden secret-bearing field names and text markers including tokens, private keys, service payloads, raw crash dumps, raw release logs, signing material, TLS key logs, and WiFi credentials before writing downstream artifacts. |
| T-33-02 | Tampering | Path arguments and generated output directory | mitigate | Enforce repo-relative paths under expected roots, reject `..`, reject absolute paths, reject symlink output-root escapes, and snapshot only approved Phase 32/27/28 contract files. |
| T-33-03 | Spoofing/Tampering | Phase 32 handoff and source row refs | mitigate | Validate Phase 32 lifecycle id `32-2026-07-03T14-13-51`, require row refs shaped as `build/ci-evidence/phase32/blocker-register.json#<row_id>`, and resolve every row id against the canonical Phase 32 register. |
| T-33-04 | Elevation of Privilege | Decision-axis normalization | mitigate | Keep `retained_code`, `residual_risk`, `exception`, `readiness`, and `reference_demotion` as orthogonal axes; reject attempts to infer one approval from another or from green evidence. |
| T-33-05 | Elevation of Privilege | Exception approval coverage | mitigate | Require exception source row refs to exactly match Phase 32 exception-request rows and require `affected_gates` to contain every referenced row's `affected_gate`; broad or unmatched coverage is invalid. |
| T-33-06 | Repudiation/Elevation of Privilege | Readiness and demotion handoffs | mitigate | Emit readiness input only as `readiness-decision-handoff.json`, emit demotion input only as `demotion-decision-handoff.json`, do not emit `final_readiness_status: "unblocked"` or `demotion_allowed`, and preserve rejected decisions as auditable rows. |
| T-33-07 | Denial of Service | JSON parsing and generated reports | mitigate | Require top-level objects/lists where expected, fail fast on malformed JSON, validate duplicate ids, and keep quick generation deterministic from Phase 32 rows. |
</threat_model>

<verification>
Run the Phase 33 validation strategy commands:

```bash
python3 -m py_compile tools/bazel/phase33_maintainer_decision_inputs.py tools/bazel/phase33_maintainer_decision_inputs_test.py
python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q
python3 tools/bazel/phase33_maintainer_decision_inputs.py --contract-only
python3 tools/bazel/phase33_maintainer_decision_inputs.py --security-only
python3 tools/bazel/phase33_maintainer_decision_inputs.py --wiring-only
bazel run //tools/bazel:phase33_verify_tests
bazel run //tools/bazel:phase33_verify
just phase33-verify
git diff --check
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- `tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json` documents all DECIDE-01, DECIDE-02, and DECIDE-03 inputs, enums, generated artifacts, source contracts, and prohibited approval semantics.
- `tools/bazel/phase33_maintainer_decision_inputs.py --quick --phase32-handoff build/ci-evidence/phase32/downstream-handoff-manifest.json --output-dir build/ci-evidence/phase33` writes every D-18 generated artifact.
- Retained-code, residual-risk, exception, readiness, and reference-demotion decisions are separate normalized axes with rejected decisions preserved.
- Hard blocker problem kinds cannot become accepted retained-code, accepted residual-risk, approved exceptions, readiness approval, or demotion approval.
- `downstream-handoff-manifest.json` points Phase 34 and Phase 35 at Phase 33 normalized outputs and records `raw_evidence_consumed: false`.
- `just phase33-verify` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/33-maintainer-decision-inputs/33-01-SUMMARY.md`.
</output>
