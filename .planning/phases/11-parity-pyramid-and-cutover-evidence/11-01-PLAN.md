---
phase: 11-parity-pyramid-and-cutover-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/phase11_verify.py
  - tools/bazel/phase11_verify_test.py
  - tools/bazel/manifests/phase11_parity_pyramid.json
autonomous: true
requirements:
  - VERF-01
requirements_addressed:
  - VERF-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-06-14T18-48-49
generated_at: 2026-06-14T19:05:56Z
must_haves:
  truths:
    - "Developer can inspect a source-backed Phase 11 parity pyramid with every required verification layer from D-01."
    - "Every parity pyramid row uses one of the D-02 proof scopes: local, ci, simulator, hardware-smoke, manual-hardware-required, or retained-code-justification."
    - "Local rows are limited to deterministic verifier, Rust, manifest, drift, and fixture checks; simulator, hardware, manual, retained-code, and release-candidate proof is not marked local per D-03."
    - "A repo-owned Phase 11 verifier exists and can run `--pyramid-only` per D-04."
    - "The verifier implements the first working mode validators for `--requirements-only`, `--comparison-only`, `--rust-only`, `--cutover-only`, and `--security-only` before Plans 11-02, 11-03, and 11-04 use them."
  artifacts:
    - path: "tools/bazel/manifests/phase11_parity_pyramid.json"
      provides: "VERF-01 parity pyramid manifest"
      contains: "pyramid-hardware-smoke-manual-gates"
    - path: "tools/bazel/phase11_verify.py"
      provides: "Phase 11 verifier foundation and `--pyramid-only` mode"
      contains: "PHASE_LIFECYCLE_ID = \"11-2026-06-14T18-48-49\""
    - path: "tools/bazel/phase11_verify_test.py"
      provides: "Regression tests for pyramid schema, source paths, proof scopes, redaction, and overclaim guards"
      contains: "Phase11VerifierTest"
  key_links:
    - from: "tools/bazel/phase11_verify.py"
      to: "tools/bazel/manifests/phase11_parity_pyramid.json"
      via: "`--pyramid-only` manifest load"
      pattern: "phase11_parity_pyramid.json"
    - from: "tools/bazel/manifests/phase11_parity_pyramid.json"
      to: ".planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md"
      via: "source_artifacts row references"
      pattern: "11-CONTEXT.md"
---

<objective>
Create the Phase 11 parity pyramid manifest and verifier foundation.

Purpose: Satisfy VERF-01 by making the complete evidence taxonomy source-backed, machine-checkable, and honest about local versus non-local proof.
Output: `phase11_parity_pyramid.json`, `phase11_verify.py`, and initial verifier regression tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-RESEARCH.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-VALIDATION.md
@tools/bazel/phase10_verify.py
@tools/bazel/phase10_verify_test.py
@tools/bazel/manifests/phase6_printing_core.json
@tools/bazel/manifests/phase7_generated_outputs.json
@tools/bazel/manifests/phase8_gui_workflows.json
@tools/bazel/manifests/phase9_connect_contracts.json
@tools/bazel/manifests/phase10_auxiliary_controllers.json

<interfaces>
Required Phase 11 proof scopes from D-02:
```text
local
ci
simulator
hardware-smoke
manual-hardware-required
retained-code-justification
```

Required parity pyramid row IDs from D-01 and D-02:
```text
pyramid-rust-unit-tests
pyramid-adapter-domain-contract-tests
pyramid-generated-drift-checks
pyramid-reference-fixture-comparisons
pyramid-simulator-flows
pyramid-network-tls-api-checks
pyramid-release-artifact-checks
pyramid-hardware-smoke-manual-gates
pyramid-retained-code-justifications
```

Forbidden evidence strings for D-12 and D-18 guards:
```text
password_value, token_value, certificate_bytes, private_key, SIGNING_KEY_VALUE,
raw_crash_dump, firmware_payload, hardware verified locally,
simulator passed locally, byte-identical firmware, cutover complete,
reference path removed
```
</interfaces>

<standards>
Loaded Bright Builds canonical standards: `standards/index.md`, `core/architecture.md`, `core/code-shape.md`, `core/verification.md`, `core/testing.md`, and `languages/rust.md` at commit `05f8d7a6c9c2e157ec4f922a05273e72dab97676`.
</standards>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 11 verifier foundation and pyramid tests</name>
  <files>tools/bazel/phase11_verify.py, tools/bazel/phase11_verify_test.py</files>
  <read_first>
    - tools/bazel/phase11_verify.py (create if missing)
    - tools/bazel/phase11_verify_test.py (create if missing)
    - tools/bazel/phase10_verify.py
    - tools/bazel/phase10_verify_test.py
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-RESEARCH.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-VALIDATION.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <behavior>
    - RED: A temp-root test with a parity row using `proof_scope: "hardware-smoke"` and `local_status: "passed-local"` fails.
    - RED: A temp-root test with source path `../outside` fails.
    - RED: A temp-root test containing `token_value` or `byte-identical firmware` fails.
    - GREEN: A complete `phase11_parity_pyramid.json` with the nine required row IDs passes `--pyramid-only`.
  </behavior>
  <action>
    Create `tools/bazel/phase11_verify.py` using standard-library Python only: `argparse`, `json`, `pathlib`, `subprocess`, `tempfile`, `unittest` only where needed by tests, and no external dependencies. Follow the error style from `tools/bazel/phase10_verify.py`: define `VerificationError`, `require_string`, `load_json`, and fail fast with a nonzero exit code.

    Add constants:
    - `PHASE = "11-parity-pyramid-and-cutover-evidence"`
    - `PHASE_LIFECYCLE_ID = "11-2026-06-14T18-48-49"` per D-19.
    - `PARITY_PYRAMID_MANIFEST = Path("tools/bazel/manifests/phase11_parity_pyramid.json")`.
    - `ALLOWED_PROOF_SCOPES = {"local", "ci", "simulator", "hardware-smoke", "manual-hardware-required", "retained-code-justification"}` per D-02.
    - `LOCAL_ONLY_EVIDENCE_CLASSES = {"rust-unit-test", "adapter-contract-test", "generated-drift-check", "reference-fixture-comparison", "source-audit", "static-verifier"}`.
    - `NON_LOCAL_PROOF_SCOPES = {"ci", "simulator", "hardware-smoke", "manual-hardware-required", "retained-code-justification"}` per D-03.
    - `REQUIRED_PYRAMID_ROW_IDS` with the nine row IDs listed in `<interfaces>`.
    - `FORBIDDEN_MARKERS` containing the exact D-12 sensitive markers in `<interfaces>`.
    - `OVERCLAIM_STRINGS` containing the exact D-18 strings in `<interfaces>`.

    Implement `parse_args()` with modes `--quick`, `--all`, `--pyramid-only`, `--requirements-only`, `--comparison-only`, `--cutover-only`, `--security-only`, `--rust-only`, `--wiring-only`, and `--repo-root`. In this task, make `--pyramid-only` execute the complete pyramid checks and implement the first working validators for every later mode that Plans 11-02, 11-03, and 11-04 will call. `--quick` must execute `--pyramid-only` plus security scans for files that already exist. For manifest modes whose owning manifests do not exist yet, raise a clear `VerificationError` naming the missing manifest path instead of silently passing.

    Implement pyramid checks for T-11-01-01 through T-11-01-05:
    - Every row has `id`, `layer`, `requirement_id`, `proof_scope`, `evidence_class`, `local_status`, `cutover_status`, `source_artifacts`, `verifier_commands`, `required_non_local_evidence`, `secret_handling`, `overclaim_guard`, and `phase_lifecycle_id`.
    - Every `id` is printable ASCII, path-free, and at most 96 bytes.
    - Every source artifact is repo-relative, has no absolute path, has no `..` component, and exists.
    - Every row lifecycle equals `11-2026-06-14T18-48-49`.
    - Rows using `proof_scope` other than `local` must not use `local_status: "passed-local"`.
    - Rows with `proof_scope: "local"` must use an evidence class in `LOCAL_ONLY_EVIDENCE_CLASSES`.
    - Whole manifest text rejects all `FORBIDDEN_MARKERS` and `OVERCLAIM_STRINGS`.

    Implement downstream mode validators now, not in Plan 11-05:
    - `check_requirements(...)` for `--requirements-only`: load `tools/bazel/manifests/phase11_requirement_evidence.json`; require the 30 v1 requirement IDs from `.planning/REQUIREMENTS.md`; require Plan 11-02 fields `owning_phase`, `source_artifacts`, `verifier_command_or_evidence_class`, `current_status`, `cutover_status`, `intentional_delta_status`, `retained_code_justification`, `required_non_local_evidence`, `cutover_blocker`, `proof_scope`, and `phase_lifecycle_id`; reject roadmap-only evidence; reject missing pending-requirement handling for BAZL-03, BAZL-05, VERF-01, VERF-03, VERF-04, and VERF-05.
    - `check_comparisons(...)` for `--comparison-only`: load `tools/bazel/manifests/phase11_reference_comparisons.json`; require the nine D-09 row IDs; require `comparison_kind`, `normalization_rule`, `byte_identity_claim`, `reference_command_policy`, `guard_environment`, `source_artifacts`, `required_non_local_evidence`, `secret_handling`, `proof_scope`, and lifecycle fields; reject `byte_identity_claim: true` unless fixture and normalization fields are present; require guarded reference command policy for reference-only commands.
    - `check_cutover(...)` for `--cutover-only`: load `tools/bazel/manifests/phase11_cutover_readiness.json` and `phase11_retained_code_justifications.json`; require the seven D-13 criterion rows and eight D-15 retained-code rows; require `criteria-reference-demotion-blocked` to keep `demotion_allowed: false`; require retained-code dispositions to be `accepted`, `blocked`, or `deferred`.
    - `check_security(...)` for `--security-only`: scan all existing Phase 11 manifests, `11-VALIDATION.md`, and existing Phase 11 summaries for `FORBIDDEN_MARKERS` and `OVERCLAIM_STRINGS`; skip only files that do not exist yet.
    - `check_rust(...)` for `--rust-only`: verify `rust/crates/domain/src/cutover.rs` contains the required API strings from Plan 11-03, verify `pub mod cutover;` in `rust/crates/domain/src/lib.rs`, and reject unsafe patterns in `cutover.rs`.
    - `check_wiring(...)` for `--wiring-only`: define the wiring checks for `tools/bazel/BUILD.bazel`, `tools/bazel/rust_workflow.sh`, root `BUILD.bazel`, and `justfile`; before Plan 11-05 wires those files, missing labels must produce explicit `VerificationError`s naming the missing file or label.

    Create `tools/bazel/phase11_verify_test.py` with `unittest` tests named:
    - `test_pyramid_only_accepts_complete_manifest`
    - `test_pyramid_only_rejects_local_hardware_overclaim`
    - `test_pyramid_only_rejects_source_path_escape`
    - `test_pyramid_only_rejects_secret_marker`
    - `test_pyramid_only_rejects_missing_required_row`
    - `test_requirements_only_reports_missing_manifest`
    - `test_comparison_only_reports_missing_manifest`
    - `test_cutover_only_reports_missing_manifest`
    - `test_rust_only_reports_missing_contract`
    Use temp roots copied from the Phase 10 verifier-test pattern. Each test must use Arrange, Act, Assert comments per Bright Builds testing guidance.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase11_verify_test.py</automated>
    <automated>python3 -m py_compile tools/bazel/phase11_verify.py tools/bazel/phase11_verify_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'PHASE_LIFECYCLE_ID = "11-2026-06-14T18-48-49"' tools/bazel/phase11_verify.py`
    - `rg 'REQUIRED_PYRAMID_ROW_IDS|pyramid-rust-unit-tests|pyramid-hardware-smoke-manual-gates|pyramid-retained-code-justifications' tools/bazel/phase11_verify.py`
    - `rg 'ALLOWED_PROOF_SCOPES|hardware-smoke|manual-hardware-required|retained-code-justification' tools/bazel/phase11_verify.py`
    - `rg 'FORBIDDEN_MARKERS|token_value|certificate_bytes|SIGNING_KEY_VALUE|raw_crash_dump|firmware_payload' tools/bazel/phase11_verify.py`
    - `rg 'OVERCLAIM_STRINGS|hardware verified locally|byte-identical firmware|cutover complete|reference path removed' tools/bazel/phase11_verify.py`
    - `rg 'def check_requirements|def check_comparisons|def check_cutover|def check_security|def check_rust|def check_wiring|--requirements-only|--comparison-only|--rust-only|--cutover-only|--security-only' tools/bazel/phase11_verify.py`
    - `rg 'phase11_requirement_evidence.json|phase11_reference_comparisons.json|phase11_cutover_readiness.json|phase11_retained_code_justifications.json|rust/crates/domain/src/cutover.rs' tools/bazel/phase11_verify.py`
    - `rg 'test_pyramid_only_rejects_local_hardware_overclaim|test_pyramid_only_rejects_source_path_escape|test_pyramid_only_rejects_secret_marker|test_pyramid_only_rejects_missing_required_row|test_requirements_only_reports_missing_manifest|test_comparison_only_reports_missing_manifest|test_cutover_only_reports_missing_manifest|test_rust_only_reports_missing_contract' tools/bazel/phase11_verify_test.py`
    - Both commands in `<verify>` exit 0.
  </acceptance_criteria>
  <done>`phase11_verify.py` and `phase11_verify_test.py` exist, reject dishonest evidence rows, validate the parity pyramid, and provide working downstream verifier modes before later plans depend on them.</done>
</task>

<task type="auto">
  <name>Task 2: Create source-backed parity pyramid manifest</name>
  <files>tools/bazel/manifests/phase11_parity_pyramid.json</files>
  <read_first>
    - tools/bazel/manifests/phase11_parity_pyramid.json (create if missing)
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-RESEARCH.md
    - .planning/phases/01-reference-baseline-and-safety-envelope/01-VERIFICATION.md
    - .planning/phases/03-artifact-and-generator-parity/03-VERIFICATION.md
    - .planning/phases/04-rust-architecture-and-invariant-model/04-VERIFICATION.md
    - .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-VERIFICATION.md
    - .planning/phases/06-printing-core-safety-and-feature-gates/06-VERIFICATION.md
    - .planning/phases/07-persistence-storage-and-resource-compatibility/07-VERIFICATION.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-VERIFICATION.md
    - .planning/phases/09-network-web-services-and-transfers/09-VERIFICATION.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VERIFICATION.md
    - tools/bazel/manifests/phase6_printing_core.json
    - tools/bazel/manifests/phase7_generated_outputs.json
    - tools/bazel/manifests/phase8_gui_workflows.json
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase10_auxiliary_controllers.json
  </read_first>
  <action>
    Create `tools/bazel/manifests/phase11_parity_pyramid.json` with top-level fields:
    - `schema_version`: `"1"`
    - `phase`: `"11-parity-pyramid-and-cutover-evidence"`
    - `phase_lifecycle_id`: `"11-2026-06-14T18-48-49"`
    - `parity_pyramid`: array containing exactly the nine row IDs in Task 1.

    For D-01, use these exact row values:
    - `pyramid-rust-unit-tests`: `layer: "rust-unit-tests"`, `proof_scope: "local"`, `evidence_class: "rust-unit-test"`, `verifier_commands` includes `cargo test --all-features` and `bazel run //tools/bazel:rust_unit_tests`.
    - `pyramid-adapter-domain-contract-tests`: `layer: "adapter-domain-contract-tests"`, `proof_scope: "local"`, `evidence_class: "adapter-contract-test"`, `verifier_commands` includes `python3 tools/bazel/phase11_verify.py --rust-only`.
    - `pyramid-generated-drift-checks`: `layer: "generated-drift-checks"`, `proof_scope: "local"`, `evidence_class: "generated-drift-check"`, `verifier_commands` includes `bazel run //tools/bazel:generated_check`.
    - `pyramid-reference-fixture-comparisons`: `layer: "reference-fixture-comparisons"`, `proof_scope: "local"`, `evidence_class: "reference-fixture-comparison"`, `verifier_commands` includes `python3 tools/bazel/phase11_verify.py --comparison-only`.
    - `pyramid-simulator-flows`: `layer: "simulator-flows"`, `proof_scope: "simulator"`, `evidence_class: "simulator-flow"`, `local_status: "pending-simulator"`.
    - `pyramid-network-tls-api-checks`: `layer: "network-tls-api-checks"`, `proof_scope: "ci"`, `evidence_class: "network-tls-api-check"`, `local_status: "pending-ci"`.
    - `pyramid-release-artifact-checks`: `layer: "release-artifact-checks"`, `proof_scope: "ci"`, `evidence_class: "release-artifact-check"`, `local_status: "pending-ci"`.
    - `pyramid-hardware-smoke-manual-gates`: `layer: "hardware-smoke-or-manual-gates"`, `proof_scope: "manual-hardware-required"`, `evidence_class: "manual-hardware-required"`, `local_status: "manual-hardware-required"`.
    - `pyramid-retained-code-justifications`: `layer: "retained-code-justifications"`, `proof_scope: "retained-code-justification"`, `evidence_class: "retained-code-justification"`, `local_status: "accepted-retained-code"`.

    Every row must include `requirement_id: "VERF-01"`, `cutover_status`, `source_artifacts`, `required_non_local_evidence`, `secret_handling: "name-only-or-redacted"`, `overclaim_guard: "enforced-by-phase11-verifier"`, and `phase_lifecycle_id: "11-2026-06-14T18-48-49"`. For D-03, simulator, hardware, manual, CI-only, and retained-code rows must name at least one non-local artifact requirement and must not use `local_status: "passed-local"`. For D-12, do not store tokens, passwords, certificate bytes, signing keys, raw crash dumps, or firmware payload bytes.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase11_verify.py --pyramid-only</automated>
    <automated>python3 tools/bazel/phase11_verify_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/bazel/manifests/phase11_parity_pyramid.json`
    - `rg '"schema_version": "1"|"phase_lifecycle_id": "11-2026-06-14T18-48-49"' tools/bazel/manifests/phase11_parity_pyramid.json`
    - `rg 'pyramid-rust-unit-tests|pyramid-adapter-domain-contract-tests|pyramid-generated-drift-checks|pyramid-reference-fixture-comparisons|pyramid-simulator-flows|pyramid-network-tls-api-checks|pyramid-release-artifact-checks|pyramid-hardware-smoke-manual-gates|pyramid-retained-code-justifications' tools/bazel/manifests/phase11_parity_pyramid.json`
    - `rg '"proof_scope": "local"|"proof_scope": "ci"|"proof_scope": "simulator"|"proof_scope": "hardware-smoke"|"proof_scope": "manual-hardware-required"|"proof_scope": "retained-code-justification"' tools/bazel/manifests/phase11_parity_pyramid.json`
    - `python3 tools/bazel/phase11_verify.py --pyramid-only` exits 0.
    - `python3 tools/bazel/phase11_verify_test.py` exits 0.
  </acceptance_criteria>
  <done>The parity pyramid manifest has all D-01 layers, D-02 proof scopes, D-03 non-local boundaries, D-12 redaction posture, and D-19 lifecycle metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest JSON -> verifier | Untrusted checked-in evidence rows are parsed before local proof is accepted. |
| Source artifact paths -> repository filesystem | Manifest rows name paths that could escape the repo or point at sensitive files. |
| Local verifier result -> cutover interpretation | A passing local check could be misread as simulator, hardware, release, or cutover approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information Disclosure | `phase11_parity_pyramid.json` | mitigate | Reject `password_value`, `token_value`, `certificate_bytes`, `private_key`, `SIGNING_KEY_VALUE`, `raw_crash_dump`, and `firmware_payload`; require `secret_handling: "name-only-or-redacted"` per D-12. |
| T-11-01-02 | Tampering | `source_artifacts` in manifest rows | mitigate | Reject absolute paths and `..` components; require every source path to exist inside the repo. |
| T-11-01-03 | Spoofing / Repudiation | local proof classification | mitigate | Reject non-local proof scopes with `local_status: "passed-local"` and scan for local hardware/simulator overclaim strings per D-03 and D-18. |
| T-11-01-04 | Elevation of Privilege / Tampering | CMake/C++ reference path status | mitigate | Do not add demotion or deletion actions; reject `cutover complete` and `reference path removed` wording. |
| T-11-01-05 | Repudiation / Tampering | reference comparison wording | mitigate | Reject `byte-identical firmware` claims in pyramid evidence; Plan 11-03 owns normalized comparison rows. |
</threat_model>

<verification>
Run:
- `python3 tools/bazel/phase11_verify_test.py`
- `python3 tools/bazel/phase11_verify.py --pyramid-only`
</verification>

<success_criteria>
VERF-01 is represented by a complete, source-backed parity pyramid manifest and a verifier mode that rejects missing rows, invalid proof scopes, source path escapes, sensitive evidence values, local proof overclaims, premature CMake/C++ demotion wording, and byte-identity claims.
</success_criteria>

<output>
After completion, create `.planning/phases/11-parity-pyramid-and-cutover-evidence/11-01-SUMMARY.md`.
</output>
