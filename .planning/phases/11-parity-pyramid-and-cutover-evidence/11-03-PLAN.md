---
phase: 11-parity-pyramid-and-cutover-evidence
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - tools/bazel/manifests/phase11_reference_comparisons.json
  - rust/crates/domain/src/cutover.rs
  - rust/crates/domain/src/lib.rs
autonomous: true
requirements:
  - VERF-01
  - VERF-03
requirements_addressed:
  - VERF-01
  - VERF-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-06-14T18-48-49
generated_at: 2026-06-14T19:05:56Z
must_haves:
  truths:
    - "Developer can inspect explicit reference-comparison rows for every VERF-03 surface named in D-09."
    - "Rows use normalized semantic comparison contracts unless a deterministic fixture and normalization rule exists per D-10."
    - "CMake/C++ reference commands remain labeled reference-only and guarded from default local execution per D-11."
    - "Pure Rust cutover/evidence types reject invalid proof scopes, comparison kinds, statuses, and row IDs without unsafe code."
  artifacts:
    - path: "tools/bazel/manifests/phase11_reference_comparisons.json"
      provides: "VERF-03 reference comparison manifest"
      contains: "ref-release-metadata"
    - path: "rust/crates/domain/src/cutover.rs"
      provides: "Pure Rust cutover and reference-comparison contracts"
      contains: "ReferenceComparisonContract"
    - path: "rust/crates/domain/src/lib.rs"
      provides: "buddy-domain export for cutover contracts"
      contains: "pub mod cutover;"
  key_links:
    - from: "tools/bazel/phase11_verify.py"
      to: "tools/bazel/manifests/phase11_reference_comparisons.json"
      via: "`--comparison-only` manifest load"
      pattern: "phase11_reference_comparisons.json"
    - from: "tools/bazel/phase11_verify.py"
      to: "rust/crates/domain/src/cutover.rs"
      via: "`--rust-only` API and unsafe-free checks"
      pattern: "ReferenceComparisonContract"
---

<objective>
Create reference-output comparison rows and pure Rust cutover/evidence contracts.

Purpose: Satisfy VERF-03 and strengthen VERF-01 by making comparison claims explicit, normalized, guarded, and represented as typed domain values.
Output: `phase11_reference_comparisons.json`, `cutover.rs`, and `lib.rs` export wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-RESEARCH.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-VALIDATION.md
@.planning/phases/11-parity-pyramid-and-cutover-evidence/11-01-PLAN.md
@tools/bazel/phase11_verify.py
@tools/bazel/artifact_metadata_compare.py
@tools/bazel/generated_drift.py
@tools/bazel/reference_contract.sh
@tools/bazel/manifests/phase7_generated_outputs.json
@tools/bazel/manifests/phase7_config_store.json
@tools/bazel/manifests/phase8_gui_workflows.json
@tools/bazel/manifests/phase9_connect_contracts.json
@tools/bazel/manifests/phase10_auxiliary_controllers.json
@rust/crates/domain/src/artifact.rs
@rust/crates/domain/src/gui.rs
@rust/crates/domain/src/network.rs
@rust/crates/domain/src/auxiliary.rs
@rust/crates/domain/src/lib.rs

<interfaces>
Required reference comparison row IDs from D-09:
```text
ref-product-artifacts
ref-generated-resources
ref-storage-migrations
ref-protocol-traces
ref-gcode-behavior-fixtures
ref-ui-display-state-fixtures
ref-network-tls-api-behavior
ref-auxiliary-controller-flows
ref-release-metadata
```

Required Rust API strings:
```text
CutoverEvidenceRowId
ProofScope
EvidenceClass
CutoverStatus
ReferenceComparisonKind
ReferenceComparisonContract
CutoverCriterion
RetainedCodeDisposition
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference comparison manifest</name>
  <files>tools/bazel/manifests/phase11_reference_comparisons.json</files>
  <read_first>
    - tools/bazel/manifests/phase11_reference_comparisons.json (create if missing)
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-RESEARCH.md
    - .planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
    - .planning/phases/03-artifact-and-generator-parity/03-VERIFICATION.md
    - .planning/phases/07-persistence-storage-and-resource-compatibility/07-VERIFICATION.md
    - .planning/phases/08-local-interface-and-workflow-parity/08-VERIFICATION.md
    - .planning/phases/09-network-web-services-and-transfers/09-VERIFICATION.md
    - .planning/phases/10-auxiliary-controllers-and-expansion-ecosystem/10-VERIFICATION.md
    - tools/bazel/artifact_metadata_compare.py
    - tools/bazel/generated_drift.py
    - tools/bazel/reference_contract.sh
    - tools/bazel/manifests/phase7_generated_outputs.json
    - tools/bazel/manifests/phase8_gui_workflows.json
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase10_auxiliary_controllers.json
  </read_first>
  <action>
    Create `tools/bazel/manifests/phase11_reference_comparisons.json` with top-level fields:
    - `schema_version`: `"1"`
    - `phase`: `"11-parity-pyramid-and-cutover-evidence"`
    - `phase_lifecycle_id`: `"11-2026-06-14T18-48-49"`
    - `reference_comparisons`: exactly the nine row IDs listed in `<interfaces>`.

    Every row must include `id`, `requirement_id: "VERF-03"`, `reference_surface`, `rust_or_bazel_surface`, `comparison_kind`, `normalization_rule`, `byte_identity_claim`, `reference_command_policy`, `guard_environment`, `source_artifacts`, `required_non_local_evidence`, `secret_handling`, `proof_scope`, `phase_lifecycle_id`.

    Use these exact comparison surfaces for D-09:
    - `ref-product-artifacts`: product artifacts `.bin/.bbf/.dfu/map/provenance`.
    - `ref-generated-resources`: generated resources, translations, fonts, icons, littlefs images, WUI assets, ESP blobs, language packs, generated headers.
    - `ref-storage-migrations`: persistent config, deprecated item IDs, defaults, credentials by name, journal hash, storage migration fixtures.
    - `ref-protocol-traces`: protocol traces from reference capture, Connect/WUI command fixtures, auxiliary transport traces by name.
    - `ref-gcode-behavior-fixtures`: G-code behavior fixtures and Buddy-specific G/M-code routing fixtures.
    - `ref-ui-display-state-fixtures`: GUI workflow and display-state fixtures for 240x320 and 480x320 surfaces.
    - `ref-network-tls-api-behavior`: Connect, WUI, TLS, API, telemetry, transfer, and local service behavior.
    - `ref-auxiliary-controller-flows`: puppy, Dwarf, ModularBed, xBuddy Extension, MMU2, Modbus/RS485, toolchanger, update flows.
    - `ref-release-metadata`: release metadata, package status, provenance, signing-sensitive status names.

    For D-10, set `comparison_kind: "normalized-semantic"` and `byte_identity_claim: false` for every row unless the row names a deterministic fixture and a concrete normalization rule. Do not write the phrase `byte-identical firmware`. For D-11, any CMake/Python reference command must use `reference_command_policy: "reference-only-guarded"` and `guard_environment: "BUDDY_BAZEL_EXECUTE_REFERENCE=1"`. For D-12, use `secret_handling: "name-only-or-redacted"` and do not store Wi-Fi passwords, PrusaLink passwords, Connect tokens, certificate bytes, signing key values, raw crash dumps, or firmware payload bytes.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase11_verify.py --comparison-only</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/bazel/manifests/phase11_reference_comparisons.json`
    - `rg 'ref-product-artifacts|ref-generated-resources|ref-storage-migrations|ref-protocol-traces|ref-gcode-behavior-fixtures|ref-ui-display-state-fixtures|ref-network-tls-api-behavior|ref-auxiliary-controller-flows|ref-release-metadata' tools/bazel/manifests/phase11_reference_comparisons.json`
    - `rg '"comparison_kind": "normalized-semantic"|"byte_identity_claim": false|"reference_command_policy": "reference-only-guarded"|"guard_environment": "BUDDY_BAZEL_EXECUTE_REFERENCE=1"' tools/bazel/manifests/phase11_reference_comparisons.json`
    - `python3 tools/bazel/phase11_verify.py --comparison-only` exits 0.
  </acceptance_criteria>
  <done>The VERF-03 comparison manifest covers all D-09 surfaces without byte-identity, secret, or default CMake/C++ execution overclaims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure Rust cutover evidence contracts</name>
  <files>rust/crates/domain/src/cutover.rs, rust/crates/domain/src/lib.rs</files>
  <read_first>
    - rust/crates/domain/src/cutover.rs (create if missing)
    - rust/crates/domain/src/lib.rs
    - rust/crates/domain/src/artifact.rs
    - rust/crates/domain/src/gui.rs
    - rust/crates/domain/src/network.rs
    - rust/crates/domain/src/auxiliary.rs
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-CONTEXT.md
    - .planning/phases/11-parity-pyramid-and-cutover-evidence/11-RESEARCH.md
  </read_first>
  <behavior>
    - Test rejects empty, path-like, control-character, non-printable, and over-96-byte `CutoverEvidenceRowId` values.
    - Test rejects `ProofScope::Local` combined with simulator, hardware-smoke, manual-hardware-required, or retained-code-only evidence classes.
    - Test accepts `ReferenceComparisonContract` only when `comparison_kind` is normalized semantic or has an explicit deterministic fixture and normalization rule.
    - Test keeps all cutover contracts free of `unsafe` code.
  </behavior>
  <action>
    Add `rust/crates/domain/src/cutover.rs` with pure domain types:
    - `CutoverEvidenceRowId` newtype with `try_new(&str) -> Result<Self, InvariantError>`.
    - `ProofScope` enum with variants `Local`, `Ci`, `Simulator`, `HardwareSmoke`, `ManualHardwareRequired`, and `RetainedCodeJustification`.
    - `EvidenceClass` enum with variants `RustUnitTest`, `AdapterContractTest`, `GeneratedDriftCheck`, `ReferenceFixtureComparison`, `SimulatorFlow`, `NetworkTlsApiCheck`, `ReleaseArtifactCheck`, `HardwareSmoke`, `ManualHardwareRequired`, `RetainedCodeJustification`, `SourceAudit`, and `StaticVerifier`.
    - `CutoverStatus` enum with variants `PassedLocal`, `PendingCi`, `PendingSimulator`, `PendingHardwareSmoke`, `ManualHardwareRequired`, `AcceptedRetainedCode`, `Blocked`, and `NotCutoverReady`.
    - `ReferenceComparisonKind` enum with variants `NormalizedSemantic` and `ByteIdentityWithFixture`.
    - `ReferenceComparisonContract` struct with fields `row_id`, `proof_scope`, `evidence_class`, `comparison_kind`, `maybe_fixture_id`, `maybe_normalization_rule`, and `byte_identity_claim`.
    - `CutoverCriterion` enum with variants `AllV1RequirementsMapped`, `LocalVerifierPassed`, `NonLocalGatesIdentified`, `RetainedCodeJustificationsAccepted`, `IntentionalDeltasDocumented`, `OverclaimScanClean`, and `ReferenceDemotionBlocked`.
    - `RetainedCodeDisposition` enum with variants `Accepted`, `Blocked`, and `Deferred`.

    Extend the existing invariant error enum or local error type following `rust/crates/domain/src/gui.rs` and `auxiliary.rs` patterns. Use `maybe_` for optional Rust fields per Bright Builds. Add focused unit tests in `cutover.rs` with Arrange, Act, Assert comments. Do not add `unsafe`, FFI, file I/O, network I/O, CMake command execution, hardware calls, or simulator calls.

    Update `rust/crates/domain/src/lib.rs` with `pub mod cutover;`.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --all-features</automated>
    <automated>python3 tools/bazel/phase11_verify.py --rust-only</automated>
  </verify>
  <acceptance_criteria>
    - `test -f rust/crates/domain/src/cutover.rs`
    - `rg 'pub struct CutoverEvidenceRowId|pub enum ProofScope|pub enum EvidenceClass|pub enum CutoverStatus|pub enum ReferenceComparisonKind|pub struct ReferenceComparisonContract|pub enum CutoverCriterion|pub enum RetainedCodeDisposition' rust/crates/domain/src/cutover.rs`
    - `rg 'maybe_fixture_id|maybe_normalization_rule' rust/crates/domain/src/cutover.rs`
    - `rg 'pub mod cutover;' rust/crates/domain/src/lib.rs`
    - `! rg 'unsafe \\{|unsafe fn|unsafe trait|unsafe impl|unsafe extern|#\\[allow\\(unsafe_code\\)\\]' rust/crates/domain/src/cutover.rs`
    - All commands in `<verify>` exit 0.
  </acceptance_criteria>
  <done>Rust domain contracts encode Phase 11 proof scopes, comparison rules, statuses, criteria, and retained-code dispositions with tests and no unsafe code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reference comparison manifest -> verifier | Comparison rows can overstate reference parity unless schema checks enforce normalized proof. |
| Reference commands -> local machine | CMake/Python reference commands may be heavy, signing-sensitive, or not Bazel-authoritative. |
| Rust domain inputs -> cutover contracts | Raw strings and optional fields must become typed values before being trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | comparison and Rust contracts | mitigate | Require `secret_handling: "name-only-or-redacted"` and no token, password, certificate, signing key, crash dump, or firmware payload byte fields. |
| T-11-03-02 | Tampering | `source_artifacts` | mitigate | Reuse verifier path checks for repo-relative existing paths and reject absolute or escaping paths. |
| T-11-03-03 | Repudiation | local comparison status | mitigate | Non-local simulator, hardware, live TLS, release-candidate, media, MMU, RS485, and toolchanger evidence remains pending unless a real artifact is cited. |
| T-11-03-04 | Elevation of Privilege / Tampering | reference oracle command policy | mitigate | Mark CMake/Python reference commands as `reference-only-guarded` with `BUDDY_BAZEL_EXECUTE_REFERENCE=1`; do not demote CMake/C++ in this plan. |
| T-11-03-05 | Spoofing / Repudiation | byte-identity claims | mitigate | Use `comparison_kind: "normalized-semantic"` and `byte_identity_claim: false` unless a fixture and normalization rule are named; reject `byte-identical firmware` wording. |
</threat_model>

<verification>
Run:
- `python3 tools/bazel/phase11_verify.py --comparison-only`
- `python3 tools/bazel/phase11_verify.py --rust-only`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --all-features`
</verification>

<success_criteria>
VERF-03 has source-backed, normalized reference comparison rows for all required surfaces, and VERF-01 gains typed Rust cutover/evidence contracts that prevent invalid proof-scope and byte-identity states.
</success_criteria>

<output>
After completion, create `.planning/phases/11-parity-pyramid-and-cutover-evidence/11-03-SUMMARY.md`.
</output>
