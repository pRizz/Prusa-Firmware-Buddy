---
phase: 16-live-network-and-transfer-qualification
plan: 01
plan_id: 16-01
type: execute
wave: 1
depends_on:
  - 13-ci-evidence-orchestration
  - 14-simulator-evidence-gates
dependencies:
  - "Phase 13 CI evidence retention is complete and provides the ignored generated-artifact model."
  - "Phase 14 simulator evidence gates are complete and provide pending-input proof boundaries."
  - "Phase 15 hardware evidence is complete and provides the closest operator-evidence verifier/wiring pattern."
files_modified:
  - tools/bazel/manifests/phase16_live_network_evidence_contract.json
  - tools/bazel/phase16_live_network_evidence.py
  - tools/bazel/phase16_live_network_evidence_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03
requirements_addressed:
  - LIVE-01
  - LIVE-02
  - LIVE-03
requirement_ids:
  - LIVE-01
  - LIVE-02
  - LIVE-03
deliverables:
  - "Phase-16-owned live network evidence contract under tools/bazel/manifests/"
  - "Stdlib Python verifier/collector with contract, security, quick, operator-evidence, and wiring modes"
  - "Ignored build/ci-evidence/phase16 run manifest, normalized scenario results, redacted network summary, source contract snapshot, operator evidence echo, and log references"
  - "Bazel labels and just phase16-verify facade"
  - "Focused stdlib unittest coverage for row completeness, source refs, redaction, path guards, overclaim guards, operator evidence, generated artifacts, and workflow wiring"
verification:
  - "python3 -m json.tool tools/bazel/manifests/phase16_live_network_evidence_contract.json >/dev/null"
  - "python3 tools/bazel/phase16_live_network_evidence_test.py"
  - "python3 tools/bazel/phase16_live_network_evidence.py --contract-only"
  - "python3 tools/bazel/phase16_live_network_evidence.py --security-only"
  - "python3 tools/bazel/phase16_live_network_evidence.py --quick"
  - "python3 tools/bazel/phase16_live_network_evidence.py --wiring-only"
  - "bazel run //tools/bazel:phase16_verify_tests"
  - "bazel run //tools/bazel:phase16_verify"
  - "just phase16-verify"
  - "git diff --check"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-06-18T01-09-34
generated_at: 2026-06-18T01:34:54Z
must_haves:
  truths:
    - "Maintainer can inspect row-level live or controlled-service evidence requirements for Connect registration, telemetry, WebSocket commands, token/fingerprint behavior, and proxy limitations."
    - "Maintainer can inspect row-level live or controlled-service evidence requirements for PrusaLink/WUI HTTP API, digest/API-key auth, SNTP, mDNS, syslog, metrics, and transfer behavior."
    - "Maintainer can inspect TLS, certificate, credential-redaction, negative protocol, long-transfer, and crash-dump upload evidence without raw secrets or payloads in retained artifacts."
    - "Local verification validates contract schema, source refs, dry-run artifacts, redaction, overclaim guards, path containment, and workflow wiring without claiming live-service proof."
    - "Rows without supplied live/control-service evidence remain pending or blocked by explicit status, never passed."
  artifacts:
    - path: "tools/bazel/manifests/phase16_live_network_evidence_contract.json"
      provides: "Phase 16 row-level live network evidence contract"
      contains: "live-connect-registration-token-fingerprint"
    - path: "tools/bazel/phase16_live_network_evidence.py"
      provides: "Verifier, dry-run evidence writer, optional operator JSON validator, redaction scanner, and wiring checker"
      exports: ["main"]
    - path: "tools/bazel/phase16_live_network_evidence_test.py"
      provides: "Regression tests for Phase 16 contract, operator evidence, artifacts, security, and wiring"
    - path: "tools/bazel/BUILD.bazel"
      provides: "phase16_verify, phase16_verify_tests, and Phase 16 source-ref manifest runfiles"
      contains: "phase16_verify"
    - path: "BUILD.bazel"
      provides: "Root aliases and Phase 16 evidence docs filegroup"
      contains: "phase16_live_network_evidence_docs"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase16-verify"
  key_links:
    - from: "tools/bazel/phase16_live_network_evidence.py"
      to: "tools/bazel/manifests/phase16_live_network_evidence_contract.json"
      via: "CONTRACT_MANIFEST constant and JSON validation"
      pattern: "phase16_live_network_evidence_contract\\.json"
    - from: "tools/bazel/phase16_live_network_evidence.py"
      to: "build/ci-evidence/phase16"
      via: "DEFAULT_OUTPUT_DIR and repo-relative path guard"
      pattern: "build/ci-evidence/phase16"
    - from: "tools/bazel/phase16_live_network_evidence.py"
      to: "operator evidence JSON"
      via: "--operator-evidence validation path"
      pattern: "operator_evidence"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase16_live_network_evidence.py"
      via: "phase16_verify dispatch"
      pattern: "phase16_verify"
    - from: "justfile"
      to: "//tools/bazel:phase16_verify"
      via: "phase16-verify recipe"
      pattern: "bazel run //tools/bazel:phase16_verify"
---

<objective>
Implement Phase 16 "Live Network and Transfer Qualification" as a Phase-16-owned evidence contract, verifier/collector, regression tests, and Bazel/just workflow gate.

Purpose: Maintainers need a durable way to review live or controlled-service proof for Connect, PrusaLink/WUI, TLS, telemetry, proxy behavior, and transfers without leaking credentials or confusing dry-run output for live qualification.

Output: JSON contract, Python verifier/collector, stdlib tests, Bazel labels, just facade, and ignored `build/ci-evidence/phase16` artifact behavior. Do not mutate archived v1.0, Phase 9, Phase 11, Phase 13, Phase 14, or Phase 15 manifests. Do not create or depend on `UI-SPEC.md`. Do not edit `.planning/config.json`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-live-network-and-transfer-qualification/16-CONTEXT.md
@.planning/phases/16-live-network-and-transfer-qualification/16-RESEARCH.md
@.planning/phases/16-live-network-and-transfer-qualification/16-VALIDATION.md
@.planning/phases/13-ci-evidence-orchestration/13-01-SUMMARY.md
@.planning/phases/14-simulator-evidence-gates/14-01-SUMMARY.md
@.planning/phases/15-hardware-safety-and-media-qualification/15-01-SUMMARY.md
@tools/bazel/phase13_ci_evidence.py
@tools/bazel/phase13_ci_evidence_test.py
@tools/bazel/manifests/phase13_ci_evidence_contract.json
@tools/bazel/phase14_simulator_evidence.py
@tools/bazel/phase14_simulator_evidence_test.py
@tools/bazel/manifests/phase14_simulator_evidence_contract.json
@tools/bazel/phase15_hardware_evidence.py
@tools/bazel/phase15_hardware_evidence_test.py
@tools/bazel/manifests/phase15_hardware_evidence_contract.json
@tools/bazel/manifests/phase9_connect_contracts.json
@tools/bazel/manifests/phase9_wui_contracts.json
@tools/bazel/manifests/phase9_network_service_contracts.json
@tools/bazel/manifests/phase9_transfer_contracts.json
@tools/bazel/manifests/phase9_network_concern_dispositions.json
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/manifests/phase11_parity_pyramid.json
@tools/bazel/manifests/phase11_reference_comparisons.json
@tools/bazel/manifests/phase11_requirement_evidence.json
@tools/bazel/manifests/phase11_retained_code_justifications.json
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@doc/proxy_support.md
@doc/metrics.md
@doc/prusa_printer_settings.ini

<interfaces>
Contract top-level shape to create in `tools/bazel/manifests/phase16_live_network_evidence_contract.json`:
```json
{
  "schema_version": "1",
  "id": "phase16_live_network_evidence_contract",
  "phase": "16-live-network-and-transfer-qualification",
  "phase_lifecycle_id": "16-2026-06-18T01-09-34",
  "output_root": "build/ci-evidence/phase16",
  "artifact_name": "phase16-live-network-evidence",
  "status_vocabulary": [
    "passed",
    "failed",
    "pending-live-input",
    "manual-live-service-required",
    "controlled-service-required",
    "blocked-credentials-unavailable",
    "blocked-endpoint-unavailable",
    "not-applicable-with-justification",
    "source-contract-passed",
    "rejected-redaction",
    "rejected-overclaim"
  ],
  "required_artifact_kinds": [
    "machine-readable-run-manifest",
    "normalized-scenario-results",
    "redacted-network-summary",
    "source-contract-snapshot",
    "live-log-reference",
    "operator-evidence-input",
    "external-artifact-reference"
  ],
  "operator_input_schema": {
    "required_fields": [
      "device",
      "firmware_build",
      "operator",
      "timestamp",
      "scenario_id",
      "result",
      "evidence_type",
      "service_surface",
      "mode",
      "artifact_refs",
      "redaction_summary",
      "residual_risk"
    ],
    "allowed_results": [
      "passed",
      "failed",
      "manual-live-service-required",
      "controlled-service-required",
      "blocked-credentials-unavailable",
      "blocked-endpoint-unavailable",
      "not-applicable-with-justification"
    ],
    "artifact_ref_root": "build/ci-evidence/phase16"
  },
  "scenarios": []
}
```

Each `scenarios[]` row must include these fields:
```json
{
  "id": "live-connect-registration-token-fingerprint",
  "title": "Connect registration, token, and fingerprint live qualification",
  "requirement_ids": ["LIVE-01", "LIVE-03"],
  "v1_requirement_ids": ["IFCE-02", "VERF-03"],
  "source_contract_refs": [
    "tools/bazel/manifests/phase9_connect_contracts.json#connect-registration-token-fingerprint",
    "tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-network-tls-api-checks",
    "tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified"
  ],
  "source_doc_refs": [],
  "service_surface": "connect-registration",
  "mode": "live-or-controlled-service",
  "required_input_kind": "approved-connect-service-or-controlled-compatible-service",
  "proof_scope": "live-service-observation",
  "expected_pass_semantics": "Operator evidence shows registration flow reaches service-accepted state with token/fingerprint behavior summarized through redacted metadata only.",
  "expected_failure_semantics": "Failure row names scenario, requirement IDs, service surface, mode, device/build, artifact refs, reason, and residual risk.",
  "expected_artifact_path": "build/ci-evidence/phase16/logs/live-connect-registration-token-fingerprint.log",
  "retained_artifact_kind": "live-log-reference",
  "allowed_statuses": [
    "pending-live-input",
    "manual-live-service-required",
    "controlled-service-required",
    "blocked-credentials-unavailable",
    "blocked-endpoint-unavailable",
    "not-applicable-with-justification",
    "passed",
    "failed"
  ],
  "operator_metadata_required": [
    "device",
    "firmware_build",
    "operator",
    "timestamp",
    "scenario_id",
    "result",
    "evidence_type",
    "service_surface",
    "mode",
    "artifact_refs",
    "redaction_summary",
    "residual_risk"
  ],
  "redaction_required": true,
  "credential_boundary": "connect-token-fingerprint-registration-code-redacted",
  "residual_non_live_gates": [
    "release-candidate-artifact-and-signing",
    "retained-code-acceptance",
    "final-reference-demotion-review"
  ],
  "unsupported_claims": [
    "claim-live-proof-from-dry-run",
    "claim-production-connect-validated-without-operator-evidence",
    "claim-final-cutover-or-reference-demotion"
  ]
}
```

CLI contract to implement in `tools/bazel/phase16_live_network_evidence.py`:
```text
--contract-only              validate checked-in contract, source refs, status vocabulary, required rows, operator schema, and expected paths
--security-only              scan checked-in contract and generated Phase 16 artifacts for secrets, raw logs, crash dumps, payloads, path traversal, and overclaims
--wiring-only                verify Bazel root/tools labels, rust_workflow dispatch, and just phase16-verify recipe
--quick                      contract + security + deterministic dry-run evidence write under build/ci-evidence/phase16
--operator-evidence PATH     validate operator-supplied JSON evidence and merge valid statuses into generated artifacts
--output-dir PATH            defaults to build/ci-evidence/phase16 and must stay under that root
```
</interfaces>
</context>

<decision_coverage>
| Decision | Coverage | Task | Notes |
|----------|----------|------|-------|
| D-01 | Full | 1 | New Phase 16 contract; no mutation of Phase 9, 11, 13, 14, or 15 manifests. |
| D-02 | Full | 1, 2 | Row-level scenarios include ID, surface, mode, required input kind, artifact path, status semantics, redaction, source refs, and residual gates. |
| D-03 | Full | 1 | Required rows cover Connect, WUI, TLS, telemetry, proxy, transfers, negative behavior, long transfers, and crash-dump upload. |
| D-04 | Full | 1, 2 | Rows default to explicit pending/blocked statuses; local quick mode never marks live rows `passed`. |
| D-05 | Full | 2 | Generated artifacts live under ignored `build/ci-evidence/phase16`. |
| D-06 | Full | 2 | Generated run manifest, normalized results, redacted summary, contract snapshot, operator echo, and log refs are named. |
| D-07 | Full | 1, 2 | Secret-bearing material is excluded by contract fields and enforced scanners. |
| D-08 | Full | 2 | Verifier guards reject secret markers, raw logs/dumps, and overclaim wording. |
| D-09 | Full | 2 | Dedicated stdlib Python verifier/collector over checked-in JSON contract. |
| D-10 | Full | 3 | Exact Phase 16 script, test, manifest, Bazel, root alias, rust_workflow, and just surfaces. |
| D-11 | Full | 2 | Deterministic local verification validates structure, dry-run artifacts, redaction, path guards, source refs, and wiring without live credentials. |
| D-12 | Full | 2 | JSON contract, explicit statuses, small helpers, `subprocess.run(..., shell=False)` in tests, and stdlib-only implementation. |
| D-13 | Full | 1 | Every row maps to LIVE IDs plus v1.0 and prior phase source refs. |
| D-14 | Full | 1, 2 | Phase 14/15 evidence can support readiness but cannot satisfy live rows. |
| D-15 | Full | 2, 3 | Artifact retention mirrors Phase 13, but CI without live inputs stays pending. |
| D-16 | Full | 1, 2, 3 | Lifecycle ID `16-2026-06-18T01-09-34` appears in contract, plan, and generated artifacts. |
</decision_coverage>

<dependency_graph>
| Task | Needs | Creates | Parallelizable |
|------|-------|---------|----------------|
| 1 | Phase 16 context/research/validation, prior source manifests, Phase 15 pattern | Contract, required scenario rows, source-ref and schema tests | No, Task 2 validates and generates from this contract |
| 2 | Task 1 contract and test fixture | Verifier/collector modes, generated artifacts, operator-evidence validation, security tests | No, same runner/test files |
| 3 | Task 2 CLI names and tests | Bazel labels, root aliases, rust_workflow dispatch, just facade | No, wiring depends on final CLI contract |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 16 row-level live network evidence contract</name>
  <files>tools/bazel/manifests/phase16_live_network_evidence_contract.json, tools/bazel/phase16_live_network_evidence.py, tools/bazel/phase16_live_network_evidence_test.py</files>
  <read_first>
    - tools/bazel/manifests/phase16_live_network_evidence_contract.json (planned new file; confirm absence before creating)
    - tools/bazel/phase16_live_network_evidence.py (planned new file; confirm absence before creating)
    - tools/bazel/phase16_live_network_evidence_test.py (planned new file; confirm absence before creating)
    - .planning/phases/16-live-network-and-transfer-qualification/16-CONTEXT.md
    - .planning/phases/16-live-network-and-transfer-qualification/16-RESEARCH.md
    - .planning/phases/16-live-network-and-transfer-qualification/16-VALIDATION.md
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/phase15_hardware_evidence_test.py
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase9_wui_contracts.json
    - tools/bazel/manifests/phase9_network_service_contracts.json
    - tools/bazel/manifests/phase9_transfer_contracts.json
    - tools/bazel/manifests/phase9_network_concern_dispositions.json
    - tools/bazel/manifests/phase11_cutover_readiness.json
    - tools/bazel/manifests/phase11_parity_pyramid.json
    - tools/bazel/manifests/phase11_reference_comparisons.json
    - tools/bazel/manifests/phase11_requirement_evidence.json
    - .planning/codebase/INTEGRATIONS.md
    - .planning/codebase/CONCERNS.md
    - doc/proxy_support.md
    - doc/metrics.md
  </read_first>
  <behavior>
    - Test `contract_accepts_complete_contract`: complete Phase 16 contract validates with `--contract-only`.
    - Test `contract_requires_all_live_rows`: removing any required `live-*` scenario ID fails and names the missing row.
    - Test `contract_requires_live_requirements`: removing `LIVE-01`, `LIVE-02`, or `LIVE-03` coverage fails.
    - Test `contract_requires_surface_coverage`: removing Connect registration, telemetry, WebSocket, proxy, WUI API, digest auth, API-key auth, SNTP, mDNS, syslog, metrics, transfers, TLS, negative protocol, long-transfer, or crash-dump upload surface coverage fails and names the surface.
    - Test `contract_rejects_bad_source_ref`: unknown or path-traversing `source_contract_refs` fail.
    - Test `contract_rejects_passed_default_status`: live-service rows cannot default to `passed`.
    - Test `contract_requires_redaction_and_residual_gates`: every row requires redaction, credential boundary, residual non-live gates, and unsupported claims.
  </behavior>
  <action>
Create `tools/bazel/manifests/phase16_live_network_evidence_contract.json` using the top-level shape in `<interfaces>`, per D-01 through D-04, D-07, D-10, D-13, D-14, and D-16.

Use exactly this `status_vocabulary`: `passed`, `failed`, `pending-live-input`, `manual-live-service-required`, `controlled-service-required`, `blocked-credentials-unavailable`, `blocked-endpoint-unavailable`, `not-applicable-with-justification`, `source-contract-passed`, `rejected-redaction`, `rejected-overclaim`.

Use exactly this `operator_input_schema.required_fields`: `device`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `evidence_type`, `service_surface`, `mode`, `artifact_refs`, `redaction_summary`, and `residual_risk`. The schema must list `allowed_results` as `passed`, `failed`, `manual-live-service-required`, `controlled-service-required`, `blocked-credentials-unavailable`, `blocked-endpoint-unavailable`, and `not-applicable-with-justification`.

Create exactly these required scenario IDs. The verifier must fail if any of these IDs is missing or if any required surface family below lacks coverage:

1. `live-connect-registration-token-fingerprint` - `LIVE-01`, `LIVE-03`; surface `connect-registration`; mode `live-or-controlled-service`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-registration-token-fingerprint`, `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-network-tls-api-checks`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified`.
2. `live-connect-telemetry-events` - `LIVE-01`; surface `connect-telemetry-events`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-telemetry-events`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-network-tls-api-behavior`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-gcode-file-print-telemetry`.
3. `live-connect-websocket-command-channel` - `LIVE-01`; surface `connect-command-channel`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-command-polling-websocket`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-network-tls-api-behavior`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified`.
4. `live-connect-proxy-limitation-tunnel` - `LIVE-01`, `LIVE-03`; surface `connect-proxy`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-proxy-minimal-limitations`, `tools/bazel/manifests/phase9_network_concern_dispositions.json#concern-phase9-proxy-limitations`; `source_doc_refs` must include `doc/proxy_support.md#Minimal proxy support`.
5. `live-connect-transfer-download-command` - `LIVE-01`, `LIVE-03`; surface `connect-transfer-download`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-transfer-download-integration`, `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-connect-command-initiation`, `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-range-request`.
6. `live-wui-prusalink-api-v1` - `LIVE-02`; surface `prusalink-api-v1`; refs `tools/bazel/manifests/phase9_wui_contracts.json#prusalink-api-v1-status-job-files-transfer`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-storage-resource-wui-list-delete`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-network-tls-api-behavior`.
7. `live-wui-digest-auth` - `LIVE-02`, `LIVE-03`; surface `wui-digest-auth`; refs `tools/bazel/manifests/phase9_wui_contracts.json#wui-digest-auth-nonce-stale`, `tools/bazel/manifests/phase11_cutover_readiness.json#concern-phase11-secret-redaction`.
8. `live-wui-api-key-auth` - `LIVE-02`, `LIVE-03`; surface `wui-api-key-auth`; refs `tools/bazel/manifests/phase9_wui_contracts.json#wui-api-key-auth`, `tools/bazel/manifests/phase11_cutover_readiness.json#concern-phase11-secret-redaction`.
9. `live-network-sntp-client` - `LIVE-02`; surface `sntp-client`; refs `tools/bazel/manifests/phase9_network_service_contracts.json#sntp-client-default-server`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-task-readiness-home-wui`.
10. `live-network-mdns-announcement` - `LIVE-02`; surface `mdns-responder`; refs `tools/bazel/manifests/phase9_network_service_contracts.json#mdns-optional-announcement`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified`.
11. `live-network-syslog-metrics-udp` - `LIVE-02`; surface `syslog-and-metrics`; refs `tools/bazel/manifests/phase9_network_service_contracts.json#metrics-runtime-config-udp`, `tools/bazel/manifests/phase9_network_service_contracts.json#metrics-line-protocol-throttling`, `tools/bazel/manifests/phase9_network_service_contracts.json#syslog-udp-destination`; `source_doc_refs` must include `doc/metrics.md#Metrics`.
12. `live-wui-upload-transfer` - `LIVE-02`, `LIVE-03`; surface `wui-upload-transfer`; refs `tools/bazel/manifests/phase9_wui_contracts.json#wui-upload-transfer-renderer`, `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-wui-upload-api-integration`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-transfer-negative-and-conflict`.
13. `live-tls-certificate-verification` - `LIVE-01`, `LIVE-03`; surface `connect-tls-policy`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-tls-required-verification-policy`, `tools/bazel/manifests/phase9_network_concern_dispositions.json#concern-phase9-weak-digest-modules`, `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-network-tls-api-checks`.
14. `live-custom-ca-certificate-boundary` - `LIVE-03`; surface `custom-connect-ca`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-config-host-token-proxy-tls`, `tools/bazel/manifests/phase9_network_concern_dispositions.json#concern-phase9-custom-der-cert-read`, `tools/bazel/manifests/phase11_cutover_readiness.json#concern-phase11-secret-redaction`.
15. `live-negative-protocol-wui-http` - `LIVE-02`, `LIVE-03`; surface `wui-negative-protocol`; refs `tools/bazel/manifests/phase9_wui_contracts.json#wui-unknown-request-error`, `tools/bazel/manifests/phase9_wui_contracts.json#wui-server-resource-model`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-transfer-negative-and-conflict`.
16. `live-negative-protocol-connect-command` - `LIVE-01`, `LIVE-03`; surface `connect-negative-command`; refs `tools/bazel/manifests/phase9_connect_contracts.json#connect-command-polling-websocket`, `tools/bazel/manifests/phase9_network_concern_dispositions.json#concern-phase9-whole-response-shared-buffers`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-network-tls-api-behavior`.
17. `live-long-transfer-connect-download` - `LIVE-01`, `LIVE-03`; surface `connect-long-transfer`; refs `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-single-active-slot`, `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-range-request`, `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-recovery-and-changed-path`.
18. `live-long-transfer-wui-upload` - `LIVE-02`, `LIVE-03`; surface `wui-long-transfer`; refs `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-wui-upload-api-integration`, `tools/bazel/manifests/phase9_transfer_contracts.json#transfer-partial-file-direct-sector`, `tools/bazel/manifests/phase9_network_concern_dispositions.json#concern-phase9-transfer-media-races`.
19. `live-crash-dump-upload-redaction` - `LIVE-03`; surface `crash-dump-upload`; refs `tools/bazel/manifests/phase9_network_concern_dispositions.json#concern-phase9-crash-dump-upload-boundary`, `tools/bazel/manifests/phase11_cutover_readiness.json#concern-phase11-secret-redaction`, `tools/bazel/manifests/phase15_hardware_evidence_contract.json#hard-safety-watchdog-crash-recovery`.
20. `live-contract-traceability-redaction-boundary` - `LIVE-01`, `LIVE-02`, `LIVE-03`; surface `contract-traceability-redaction`; proof_scope `source-contract`; refs `tools/bazel/manifests/phase13_ci_evidence_contract.json#ciev-03-redacted-summary`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-traceability-non-simulator-boundaries`, `tools/bazel/manifests/phase15_hardware_evidence_contract.json#hard-contract-traceability-and-redaction-boundary`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-overclaim-scan-clean`.

For rows 1-19, use `proof_scope: "live-service-observation"` and `mode: "live-or-controlled-service"`. For row 20, use `proof_scope: "source-contract"` and `mode: "local-contract-validation"`.

For live-service rows, `allowed_statuses` must include `pending-live-input`, `manual-live-service-required`, `controlled-service-required`, `blocked-credentials-unavailable`, `blocked-endpoint-unavailable`, `not-applicable-with-justification`, `passed`, and `failed`, but local quick mode must not use `passed`. Source-contract rows may use `source-contract-passed`, `failed`, `rejected-redaction`, and `rejected-overclaim`.

Every row must include `source_doc_refs` as a list, `redaction_required: true`, a non-empty `credential_boundary`, non-empty `residual_non_live_gates`, non-empty `unsupported_claims`, and `expected_artifact_path` under `build/ci-evidence/phase16/logs/`.

Create the initial `tools/bazel/phase16_live_network_evidence.py` with stdlib-only constants and pure validation helpers modeled on Phase 15: `PHASE`, `PHASE_LIFECYCLE_ID`, `CONTRACT_MANIFEST`, `DEFAULT_OUTPUT_DIR`, `REQUIRED_REQUIREMENT_IDS`, `REQUIRED_SCENARIO_IDS`, `REQUIRED_SERVICE_SURFACES`, `REQUIRED_OPERATOR_FIELDS`, `VerificationError`, JSON loading, field validation, row ID search, structured `file#row-id` source-ref resolution, `source_doc_refs` validation by file existence and heading/text anchor, artifact path guarding, surface coverage validation, and `check_contract(root)`.

Create `tools/bazel/phase16_live_network_evidence_test.py` with stdlib `unittest`, temp-root copy helpers, Arrange/Act/Assert comments, and tests for the behaviors above. Tests must use `subprocess.run(["python3", verifier, ...], shell=False)`.
  </action>
  <acceptance_criteria>
    - `test -f tools/bazel/manifests/phase16_live_network_evidence_contract.json` exits 0.
    - `jq -r '.phase_lifecycle_id' tools/bazel/manifests/phase16_live_network_evidence_contract.json` prints `16-2026-06-18T01-09-34`.
    - `jq -r '.operator_input_schema.required_fields[]' tools/bazel/manifests/phase16_live_network_evidence_contract.json` prints `device`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `evidence_type`, `service_surface`, `mode`, `artifact_refs`, `redaction_summary`, and `residual_risk`.
    - `jq -r '.scenarios[].id' tools/bazel/manifests/phase16_live_network_evidence_contract.json` prints every required `live-*` scenario ID listed in the action.
    - `jq -r '.scenarios[].service_surface' tools/bazel/manifests/phase16_live_network_evidence_contract.json` covers `connect-registration`, `connect-telemetry-events`, `connect-command-channel`, `connect-proxy`, `connect-transfer-download`, `prusalink-api-v1`, `wui-digest-auth`, `wui-api-key-auth`, `sntp-client`, `mdns-responder`, `syslog-and-metrics`, `wui-upload-transfer`, `connect-tls-policy`, `custom-connect-ca`, `wui-negative-protocol`, `connect-negative-command`, `connect-long-transfer`, `wui-long-transfer`, `crash-dump-upload`, and `contract-traceability-redaction`.
    - `python3 tools/bazel/phase16_live_network_evidence.py --contract-only` exits 0.
    - `python3 tools/bazel/phase16_live_network_evidence_test.py` exits 0 and includes tests named `test_contract_accepts_complete_contract`, `test_contract_requires_all_live_rows`, `test_contract_requires_live_requirements`, `test_contract_requires_surface_coverage`, and `test_contract_rejects_bad_source_ref`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m json.tool tools/bazel/manifests/phase16_live_network_evidence_contract.json &gt;/dev/null</automated>
    <automated>python3 tools/bazel/phase16_live_network_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase16_live_network_evidence_test.py</automated>
  </verify>
  <done>Contract exists, covers `LIVE-01`, `LIVE-02`, and `LIVE-03`, names every required Connect/WUI/TLS/proxy/transfer/negative/crash-dump surface, resolves prior evidence refs, and cannot represent local dry-run output as live proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the verifier, operator evidence validation, generated artifacts, and security guards</name>
  <files>tools/bazel/phase16_live_network_evidence.py, tools/bazel/phase16_live_network_evidence_test.py</files>
  <read_first>
    - tools/bazel/phase16_live_network_evidence.py
    - tools/bazel/phase16_live_network_evidence_test.py
    - tools/bazel/manifests/phase16_live_network_evidence_contract.json
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/phase15_hardware_evidence_test.py
    - tools/bazel/phase14_simulator_evidence.py
    - tools/bazel/phase13_ci_evidence.py
    - .planning/phases/16-live-network-and-transfer-qualification/16-VALIDATION.md
    - .planning/codebase/CONCERNS.md
  </read_first>
  <behavior>
    - Test `quick_writes_phase16_artifacts`: `--quick` writes `run-manifest.json`, `normalized-scenario-results.json`, `redacted-network-summary.json`, `source-contract-snapshots/phase16_live_network_evidence_contract.json`, `operator-evidence-input.json`, and `logs/{scenario_id}.log`.
    - Test `quick_keeps_live_rows_pending`: all `live-service-observation` rows are `pending-live-input` when no operator evidence is supplied; only `source-contract` rows may be `source-contract-passed`.
    - Test `operator_evidence_accepts_complete_pass`: complete operator JSON for a live row can mark that row `passed` in generated results.
    - Test `operator_evidence_accepts_top_level_list`: operator evidence may be a top-level list or an object with `evidence_rows`.
    - Test `operator_evidence_rejects_missing_metadata`: missing any required operator field fails and names the field.
    - Test `operator_evidence_rejects_unknown_scenario_or_status`: unknown `scenario_id` or unsupported `result` fails.
    - Test `operator_evidence_rejects_artifact_path_traversal`: `artifact_refs` cannot escape `build/ci-evidence/phase16`.
    - Test `security_rejects_secret_markers`: private key, certificate material, Connect token, registration code, printer fingerprint value, Wi-Fi credential, PrusaLink password, API key, Authorization/Cookie headers, raw HTTP/TLS log, raw crash dump, raw production payload, signing key, `.bin` payload, `.bbf` payload, and `.dfu` payload markers fail.
    - Test `security_rejects_overclaim_wording`: exact overclaim wording fails in operator evidence or generated evidence fixtures; valid `unsupported_claims` deny-list identifiers do not fail merely because they describe rejected claim categories.
    - Test `output_dir_rejects_traversal`: `--output-dir` cannot escape `build/ci-evidence/phase16`.
  </behavior>
  <action>
Extend `tools/bazel/phase16_live_network_evidence.py` with the CLI contract from `<interfaces>`, per D-04 through D-12, D-14 through D-16.

Implement these modes:

- `--contract-only`: run `check_contract(root)` and exit.
- `--security-only`: run contract validation and scan the checked-in contract plus any generated files under `build/ci-evidence/phase16`.
- `--wiring-only`: call `check_wiring(root)`; Task 3 will finish the wiring requirements, but the function and tests belong here.
- `--quick`: validate contract and security, optionally load `--operator-evidence`, write deterministic artifacts, then re-run security.
- `--operator-evidence PATH`: validate a JSON file containing either an object with `evidence_rows` or a top-level list of evidence row objects. Every row must include `device`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `evidence_type`, `service_surface`, `mode`, `artifact_refs`, `redaction_summary`, and `residual_risk`. `scenario_id` must match a contract row. `result` must be one of the contract `operator_input_schema.allowed_results`. `artifact_refs` must be a non-empty list of repo-relative paths under `build/ci-evidence/phase16` or external reference handles starting with `external://` or `artifact://`; filesystem paths must be contained under the output root. Evidence text must pass redaction and overclaim scans.

Use exact constants:

```python
PHASE = "16-live-network-and-transfer-qualification"
PHASE_LIFECYCLE_ID = "16-2026-06-18T01-09-34"
CONTRACT_MANIFEST = Path("tools/bazel/manifests/phase16_live_network_evidence_contract.json")
DEFAULT_OUTPUT_DIR = Path("build/ci-evidence/phase16")
REQUIRED_REQUIREMENT_IDS = {"LIVE-01", "LIVE-02", "LIVE-03"}
```

Use forbidden marker regexes covering these case-insensitive terms, raw headers, or PEM blocks: `-----BEGIN PRIVATE KEY-----`, `-----BEGIN CERTIFICATE-----`, `certificate_pem`, `certificate_bytes`, `private_key`, `signing_key`, `token_value`, `connect_token`, `Connect token`, `registration_code`, `Fingerprint:`, `fingerprint_value`, `wifi_password`, `Wi-Fi credential`, `prusalink_password`, `api_key`, `x-api-key`, `Authorization:`, `Cookie:`, `Set-Cookie:`, `raw_http_log`, `raw_tls_log`, `tls_keylog`, `SSLKEYLOGFILE`, `raw_crash_dump`, `raw_ram_dump`, `memory_dump`, `raw_production_payload`, `firmware_payload`, `bbf_payload`, `dfu_payload`, `.bin payload`, `.bbf payload`, and `.dfu payload`.

Use overclaim strings covering these exact phrases in operator evidence and generated evidence content: `live service passed locally`, `live network verified locally`, `production connect validated`, `production Connect validated`, `production PrusaLink validated`, `tls proof complete without operator evidence`, `proxy fully supported`, `proxy authentication supported`, `crash dump upload safe`, `raw crash dump retained`, `final cutover complete`, `cutover complete`, `release readiness proven`, `release-candidate passed locally`, `signing proof complete`, `retained-code accepted by maintainer`, `reference demotion approved`, and `reference removal complete`.

For checked-in contract JSON, parse the document and validate `unsupported_claims` as denied-claim metadata. Valid baseline values must use non-phrase identifiers such as `claim-live-proof-from-dry-run`, `claim-production-connect-validated-without-operator-evidence`, `claim-proxy-full-support`, and `claim-final-cutover-or-reference-demotion`. The security scan must reject exact forbidden phrases in ordinary contract fields, operator evidence, generated summaries, and generated logs, but must not reject valid `unsupported_claims` identifiers that describe capabilities the row refuses to prove.

`--quick` must write these generated artifacts under the guarded output root:

- `run-manifest.json`: phase, lifecycle ID, artifact name, command mode, generated timestamp, output root, scenario results, requirement coverage, status counts, source contract snapshot path, and an explicit `live_inputs_supplied` boolean.
- `normalized-scenario-results.json`: one normalized row per scenario with `scenario_id`, `status`, `requirement_ids`, `v1_requirement_ids`, `service_surface`, `mode`, `proof_scope`, `source_contract_refs`, `source_doc_refs`, `artifact_refs`, `operator_metadata_present`, `redaction_summary`, and `residual_risk`.
- `redacted-network-summary.json`: maintainer-facing scenario status, credential boundaries, unsupported claims, operator evidence summary, pending live input rows, and no raw payloads or secrets.
- `source-contract-snapshots/phase16_live_network_evidence_contract.json`: sanitized copy of the checked-in contract.
- `operator-evidence-input.json`: sanitized echo of accepted operator rows, or `{"evidence_rows": []}` when absent.
- `logs/{scenario_id}.log`: dry-run log references saying live/control-service evidence is pending unless operator evidence supplied that row.

When no operator evidence is supplied, all rows with `proof_scope == "live-service-observation"` must be `pending-live-input`; `live-contract-traceability-redaction-boundary` may be `source-contract-passed`. If operator evidence is supplied, only matching scenario rows may take supplied `result` values. CI/local quick validation without operator evidence must still exit 0 because pending live input is the truthful state, not a verifier failure.

Keep implementation stdlib-only. Do not import requests, aiohttp, pytest, Docker SDKs, lab-control SDKs, serial libraries, or network clients. Do not execute live network calls. Do not call subprocess except for tests or controlled wiring checks; if subprocess is needed, use argument lists and `shell=False`.
  </action>
  <acceptance_criteria>
    - `python3 tools/bazel/phase16_live_network_evidence.py --quick` exits 0.
    - `test -f build/ci-evidence/phase16/run-manifest.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase16/normalized-scenario-results.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase16/redacted-network-summary.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase16/source-contract-snapshots/phase16_live_network_evidence_contract.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase16/operator-evidence-input.json` exits 0 after quick mode.
    - `jq -r '.phase_lifecycle_id' build/ci-evidence/phase16/run-manifest.json` prints `16-2026-06-18T01-09-34`.
    - `jq -r '.live_inputs_supplied' build/ci-evidence/phase16/run-manifest.json` prints `false` when no operator evidence is supplied.
    - `jq -r '.scenarios[] | select(.proof_scope == "live-service-observation") | .status' build/ci-evidence/phase16/run-manifest.json | sort -u` prints only `pending-live-input` when no operator evidence is supplied.
    - `python3 tools/bazel/phase16_live_network_evidence.py --security-only` exits 0 after quick mode.
    - `python3 tools/bazel/phase16_live_network_evidence_test.py` exits 0 and includes tests named `test_operator_evidence_rejects_missing_metadata`, `test_security_rejects_secret_markers`, `test_security_rejects_overclaim_wording`, and `test_output_dir_rejects_traversal`.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase16_live_network_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase16_live_network_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase16_live_network_evidence.py --security-only</automated>
    <automated>python3 tools/bazel/phase16_live_network_evidence.py --quick</automated>
  </verify>
  <done>`--quick` produces deterministic, redacted Phase 16 artifacts under `build/ci-evidence/phase16`; optional operator JSON can validate real live/control-service evidence; local absent-service mode preserves pending rows rather than false pass evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Phase 16 into Bazel and just, then run the phase gate</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase16_live_network_evidence.py, tools/bazel/phase16_live_network_evidence_test.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase16_live_network_evidence.py
    - tools/bazel/phase16_live_network_evidence_test.py
    - tools/bazel/manifests/phase16_live_network_evidence_contract.json
    - .planning/phases/16-live-network-and-transfer-qualification/16-VALIDATION.md
  </read_first>
  <behavior>
    - Test `wiring_accepts_phase16_surface`: `--wiring-only` accepts final Bazel labels, source-ref filegroup, root docs filegroup, root aliases, rust workflow dispatch, and `just phase16-verify`.
    - Test `wiring_rejects_missing_bazel_label`: removing `phase16_verify` or `phase16_verify_tests` fails.
    - Test `wiring_rejects_missing_source_ref_manifest`: removing a required Phase 9/11/13/14/15 source-ref manifest from Bazel runfiles fails.
    - Test `wiring_rejects_verifier_before_tests`: `just phase16-verify` must run tests before verifier.
  </behavior>
  <action>
Add Phase 16 workflow integration per D-09 through D-12, D-15, and D-16.

In `tools/bazel/BUILD.bazel`, add:

```python
filegroup(
    name = "phase16_source_ref_manifests",
    srcs = [
        "manifests/phase9_connect_contracts.json",
        "manifests/phase9_wui_contracts.json",
        "manifests/phase9_network_service_contracts.json",
        "manifests/phase9_transfer_contracts.json",
        "manifests/phase9_network_concern_dispositions.json",
        "manifests/phase11_cutover_readiness.json",
        "manifests/phase11_parity_pyramid.json",
        "manifests/phase11_reference_comparisons.json",
        "manifests/phase11_requirement_evidence.json",
        "manifests/phase11_retained_code_justifications.json",
        "manifests/phase13_ci_evidence_contract.json",
        "manifests/phase14_simulator_evidence_contract.json",
        "manifests/phase15_hardware_evidence_contract.json",
    ],
)

shell_binary(
    name = "phase16_verify",
    src = "rust_workflow.sh",
    data = [
        "phase16_live_network_evidence.py",
        "manifests/phase16_live_network_evidence_contract.json",
        ":phase16_source_ref_manifests",
        "//:phase16_live_network_evidence_docs",
        "//:phase11_cutover_evidence_docs",
    ],
)

shell_binary(
    name = "phase16_verify_tests",
    src = "rust_workflow.sh",
    data = [
        "phase16_live_network_evidence.py",
        "phase16_live_network_evidence_test.py",
        "manifests/phase16_live_network_evidence_contract.json",
        ":phase16_source_ref_manifests",
    ],
)
```

In root `BUILD.bazel`, add:

```python
filegroup(
    name = "phase16_live_network_evidence_docs",
    srcs = [
        ".planning/phases/16-live-network-and-transfer-qualification/16-CONTEXT.md",
        ".planning/phases/16-live-network-and-transfer-qualification/16-RESEARCH.md",
        ".planning/phases/16-live-network-and-transfer-qualification/16-VALIDATION.md",
        ".planning/phases/16-live-network-and-transfer-qualification/16-01-PLAN.md",
    ],
)

alias(
    name = "phase16_verify",
    actual = "//tools/bazel:phase16_verify",
)

alias(
    name = "phase16_verify_tests",
    actual = "//tools/bazel:phase16_verify_tests",
)
```

In `tools/bazel/rust_workflow.sh`, add dispatch cases:

```bash
phase16_verify)
  python3 tools/bazel/phase16_live_network_evidence.py --wiring-only
  python3 tools/bazel/phase16_live_network_evidence.py --quick
  ;;
phase16_verify_tests)
  python3 tools/bazel/phase16_live_network_evidence_test.py
  ;;
```

In `justfile`, add:

```make
phase16-verify:
    bazel run //tools/bazel:phase16_verify_tests
    bazel run //tools/bazel:phase16_verify
```

Update `check_wiring(root)` in `tools/bazel/phase16_live_network_evidence.py` to require exact strings for every label, dispatch case, runfile, and recipe line above. It must also verify `justfile` runs `bazel run //tools/bazel:phase16_verify_tests` before `bazel run //tools/bazel:phase16_verify`.

After wiring works, do a simplification pass:

- Keep validation helpers pure and file/subprocess operations in thin shell functions.
- Remove duplicated fixture constants when the contract is already the source of truth without weakening tests.
- Keep generated artifacts ignored under `build/ci-evidence/phase16`; do not stage generated evidence.
- Confirm no Phase 17 release/signing evidence, Phase 18 retained-code/final demotion approval, broad lab automation, or UI implementation was added.
  </action>
  <acceptance_criteria>
    - `rg 'name = "phase16_verify"' tools/bazel/BUILD.bazel` exits 0.
    - `rg 'name = "phase16_verify_tests"' tools/bazel/BUILD.bazel` exits 0.
    - `rg 'name = "phase16_source_ref_manifests"' tools/bazel/BUILD.bazel` exits 0.
    - `rg 'phase16_live_network_evidence_docs' BUILD.bazel` exits 0.
    - `rg 'phase16_verify\\)' tools/bazel/rust_workflow.sh` exits 0.
    - `rg 'phase16_verify_tests\\)' tools/bazel/rust_workflow.sh` exits 0.
    - `rg 'phase16-verify:' justfile` exits 0.
    - `python3 tools/bazel/phase16_live_network_evidence.py --wiring-only` exits 0.
    - `bazel query "//tools/bazel:phase16_verify + //tools/bazel:phase16_verify_tests + //:phase16_verify + //:phase16_verify_tests"` exits 0.
    - `just phase16-verify` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase16_live_network_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase16_live_network_evidence.py --wiring-only</automated>
    <automated>bazel run //tools/bazel:phase16_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase16_verify</automated>
    <automated>just phase16-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>`just phase16-verify` runs Phase 16 tests before the verifier through Bazel, generated evidence remains ignored, and the implementation stays stdlib-only, secret-safe, and no-overclaim.</done>
</task>

</tasks>

<threat_model>
Security enforcement is enabled. Use ASVS L1 posture for input validation, evidence redaction, command safety, and auditable proof boundaries. Block on high-severity threats; do not mark Phase 16 complete if a high-severity leakage, path traversal, or overclaim path remains.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in JSON contract -> verifier | Contract rows are parsed as untrusted data and must be schema-validated before use. |
| Prior evidence refs -> Phase 16 contract | `file#row-id` refs must resolve to existing source-backed rows without mutating archived or prior-phase artifacts. |
| Operator JSON -> generated live evidence | Operator evidence is external input and must require metadata, valid statuses, path guards, redaction, artifact refs, and residual risk. |
| Contract/operator text -> retained summaries/logs | Evidence text can leak secrets, raw HTTP/TLS logs, raw crash dumps, production payloads, or overclaim proof if not scanned before retention. |
| CLI output path -> filesystem | `--output-dir` and artifact refs could write outside ignored `build/ci-evidence/phase16` without path containment. |
| Dry-run quick mode -> maintainer interpretation | Local deterministic artifacts could be mistaken for live service qualification if status semantics are not enforced. |
| Proxy/TLS summaries -> maintainer decisions | Evidence can misrepresent proxy authentication, printer-to-proxy encryption, custom certificate behavior, or TLS proof scope if row semantics are loose. |
| Bazel/just facade -> maintainer workflow | Incorrect wiring could skip tests or falsely present CI/local validation as live network proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Operator evidence and generated artifacts | High | mitigate | Reject Connect tokens, registration codes, fingerprint values, PrusaLink passwords, API keys, Authorization/Cookie headers, Wi-Fi credentials, private keys, certificates, signing keys, raw production payloads, and firmware payload markers before writing or accepting retained evidence. |
| T-16-02 | Information Disclosure | HTTP/TLS logs | High | mitigate | Reject `raw_http_log`, `raw_tls_log`, TLS keylog, Authorization, Cookie, Set-Cookie, token, fingerprint, API-key, and password markers; retain only redacted summaries and external artifact refs. |
| T-16-03 | Information Disclosure | Crash-dump upload evidence | High | mitigate | Reject raw crash/RAM/memory dump markers; require crash-dump rows to retain only redacted summaries, hashes, consent/redaction notes, and external artifact refs. |
| T-16-04 | Spoofing / Repudiation | Local quick mode | High | mitigate | Quick mode sets live-service rows to `pending-live-input`; only operator-supplied evidence can mark live/control-service rows `passed` or `failed`. |
| T-16-05 | Tampering | Output paths and artifact refs | High | mitigate | Require repo-relative filesystem paths under `build/ci-evidence/phase16`; reject absolute paths and `..` traversal in contract rows, output dirs, and operator `artifact_refs`; allow only explicit `external://` or `artifact://` reference handles outside the filesystem root. |
| T-16-06 | Repudiation | Operator live evidence rows | Medium | mitigate | Require `device`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `evidence_type`, `service_surface`, `mode`, `artifact_refs`, `redaction_summary`, and `residual_risk` before rows can leave pending state. |
| T-16-07 | Tampering | Source refs | Medium | mitigate | Resolve every structured `source_contract_refs` entry as a repo-relative `file#row-id` against Phase 9, 11, 13, 14, and 15 manifests; validate documentation refs separately by file existence and anchor text. |
| T-16-08 | Spoofing / Repudiation | Proxy/TLS evidence | High | mitigate | Required proxy/TLS rows must cite Phase 9 proxy/TLS concerns and reject overclaims for full proxy support, proxy authentication, production Connect validation, or TLS proof without operator evidence. |
| T-16-09 | Denial of Service | Live service availability | Low | accept | Missing credentials/endpoints remain `pending-live-input`, `manual-live-service-required`, `controlled-service-required`, `blocked-credentials-unavailable`, or `blocked-endpoint-unavailable`; this is a truthful qualification state, not a local verifier failure. |
| T-16-10 | Elevation of Privilege | Verifier command execution | Medium | mitigate | Keep verifier stdlib-only and avoid subprocess except controlled wiring checks; if subprocess is used, use argument lists and `shell=False`. |
</threat_model>

<goal_backward_check>
Phase 16 goal: Maintainers can review live or controlled-service evidence for Connect, PrusaLink/WUI, TLS, telemetry, proxy behavior, and transfers with secret-safe artifacts.

Observable truths achieved by this plan:

1. Maintainers can review Connect evidence because Task 1 creates rows for registration, telemetry, WebSocket commands, token/fingerprint behavior, proxy limitations, and Connect-initiated transfers.
2. Maintainers can review WUI/PrusaLink evidence because Task 1 creates rows for API v1, digest auth, API-key auth, SNTP, mDNS, syslog, metrics, and WUI upload transfers.
3. Maintainers can review TLS, certificate, negative protocol, long-transfer, and crash-dump upload evidence because Task 1 creates those rows and Task 2 validates operator evidence shape for them.
4. Maintainers can run deterministic local validation because Task 2 provides `--contract-only`, `--security-only`, and `--quick`, and Task 3 exposes them through Bazel and `just phase16-verify`.
5. Maintainers cannot confuse dry-run artifacts with live proof because live-service rows remain `pending-live-input` unless explicit operator evidence is supplied.
6. Maintainers cannot retain secret/raw artifacts or overclaims because Task 2 scans checked-in and generated evidence and Task 3 wires those checks into the standard phase gate.
</goal_backward_check>

<verification>
Run these local checks before marking the plan complete:

```bash
python3 -m json.tool tools/bazel/manifests/phase16_live_network_evidence_contract.json >/dev/null
python3 tools/bazel/phase16_live_network_evidence_test.py
python3 tools/bazel/phase16_live_network_evidence.py --contract-only
python3 tools/bazel/phase16_live_network_evidence.py --security-only
python3 tools/bazel/phase16_live_network_evidence.py --quick
python3 tools/bazel/phase16_live_network_evidence.py --wiring-only
bazel run //tools/bazel:phase16_verify_tests
bazel run //tools/bazel:phase16_verify
just phase16-verify
git diff --check
```

Also preserve the Phase 16 validation strategy command:

```bash
python3 tools/bazel/phase16_live_network_evidence_test.py && python3 tools/bazel/phase16_live_network_evidence.py --quick
```

Do not require real Connect, WUI, TLS, proxy, transfer, or crash-dump upload execution for local completion. If operator evidence is not supplied, the generated manifest must say live-service-observation rows are `pending-live-input`.
</verification>

<success_criteria>
- `tools/bazel/manifests/phase16_live_network_evidence_contract.json` exists and covers `LIVE-01`, `LIVE-02`, and `LIVE-03`.
- Contract rows include Connect registration/token/fingerprint, telemetry, WebSocket command channel, proxy limitations, PrusaLink/WUI API, digest auth, API-key auth, SNTP, mDNS, syslog, metrics, Connect transfer, WUI upload transfer, TLS/certificate behavior, negative protocol behavior, long transfers, and crash-dump upload redaction.
- `tools/bazel/phase16_live_network_evidence.py --quick` writes secret-safe generated artifacts under `build/ci-evidence/phase16`.
- `tools/bazel/phase16_live_network_evidence.py --operator-evidence <path>` validates complete operator JSON and rejects malformed, secret-bearing, path-traversing, or overclaiming evidence.
- `just phase16-verify` runs Phase 16 tests before the verifier through Bazel.
- No source or planning artifact includes secrets, raw HTTP/TLS logs, raw crash dumps, raw production payloads, private keys, certificates, Connect tokens, registration codes, fingerprint values, Wi-Fi credentials, PrusaLink passwords, API keys, release readiness claims, signing proof, reference demotion approval, or final cutover completion.
</success_criteria>

<residual_risks>
- Real live/control-service qualification still requires approved Connect endpoints or controlled compatible services, operator credentials, networked printer/simulator fixtures, and external artifact storage; absent rows remain pending or blocked by input availability.
- Proxy evidence must preserve current documented limitations: no proxy authentication, printer-to-proxy leg not encrypted, and proxy active only when Connect TLS is enabled.
- Custom CA evidence must preserve the Phase 9 concern boundary around `/internal/connect/connect.der` until operator evidence or a separate code fix proves safe certificate loading behavior.
- Release-candidate `.bin`, `.bbf`, `.dfu`, map/provenance, resources, WUI, ESP, MMU, auxiliary packages, and signing evidence remain owned by Phase 17.
- Retained-code acceptance and final reference-demotion approval remain owned by Phase 18.
</residual_risks>

<output>
After completion, create `.planning/phases/16-live-network-and-transfer-qualification/16-01-SUMMARY.md` summarizing implementation, commands run, generated artifact paths, operator evidence status, live rows that remain pending, and residual evidence boundaries.
</output>
