---
phase: 18-retained-code-acceptance-and-cutover-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase18_cutover_review_contract.json
  - tools/bazel/phase18_cutover_review.py
  - tools/bazel/phase18_cutover_review_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-06-20T14-27-15
generated_at: 2026-06-20T14:50:51.688Z
user_setup: []
must_haves:
  truths:
    - "Maintainers can review Phase 18 retained-code acceptance packets that cover all retained C, C++, ASM, generated, vendor, HAL, RTOS, network, filesystem, signing, release-artifact, resource, MMU, auxiliary-controller, and runtime surfaces."
    - "Maintainers can inspect a final-demotion checklist linking CI, simulator, hardware, live-service, release, retained-code, and residual-risk evidence without converting pending upstream evidence into approval."
    - "Maintainers can supply approve, reject, or exception decisions with auditable metadata, rationale, evidence refs, and residual-risk handling."
    - "Local quick verification keeps demotion_allowed false unless valid maintainer decision input is supplied and every required final criterion is passed, exception-approved, or validly not-applicable."
    - "Generated readiness material is redacted review output; machine-readable gate rows and decision input are the authority."
    - "Bazel labels and just phase18-verify run tests before the verifier."
  artifacts:
    - path: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      provides: "Authoritative retained-code packet, final-demotion criterion, source-ref, and decision schema contract"
      contains: "phase18_cutover_review_contract"
    - path: "tools/bazel/phase18_cutover_review.py"
      provides: "Stdlib contract verifier, decision-input validator, demotion evaluator, quick artifact writer, security scan, source-ref resolver, and wiring checker"
      exports: ["--contract-only", "--security-only", "--quick", "--decision-input", "--output-dir", "--wiring-only"]
    - path: "tools/bazel/phase18_cutover_review_test.py"
      provides: "Unit coverage for retained coverage, final evidence links, demotion semantics, approval/exception metadata, redaction, path guards, generated artifacts, and wiring"
      contains: "Phase18CutoverReviewTest"
    - path: "tools/bazel/BUILD.bazel"
      provides: "phase18_source_ref_manifests, phase18_verify, and phase18_verify_tests labels"
      contains: "phase18_verify"
    - path: "BUILD.bazel"
      provides: "Root Phase 18 docs filegroup and verifier aliases"
      contains: "phase18_cutover_review_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "phase18_verify and phase18_verify_tests dispatch cases"
      contains: "phase18_cutover_review.py --quick"
    - path: "justfile"
      provides: "Developer facade recipe"
      contains: "phase18-verify:"
  key_links:
    - from: "tools/bazel/phase18_cutover_review.py"
      to: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      via: "CONTRACT_MANIFEST constant"
      pattern: "phase18_cutover_review_contract.json"
    - from: "tools/bazel/phase18_cutover_review.py"
      to: "build/ci-evidence/phase18"
      via: "DEFAULT_OUTPUT_DIR constant and path guard"
      pattern: "build/ci-evidence/phase18"
    - from: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      to: "tools/bazel/manifests/phase11_retained_code_justifications.json"
      via: "retained source collection file#row-id refs"
      pattern: "phase11_retained_code_justifications.json"
    - from: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      to: "tools/bazel/manifests/phase13_ci_evidence_contract.json"
      via: "final-demotion evidence index refs"
      pattern: "phase13_ci_evidence_contract.json"
    - from: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      to: "tools/bazel/manifests/phase17_release_candidate_evidence_contract.json"
      via: "release/signing final-demotion evidence refs"
      pattern: "phase17_release_candidate_evidence_contract.json"
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/rust_workflow.sh"
      via: "shell_binary src"
      pattern: "src = \"rust_workflow.sh\""
    - from: "justfile"
      to: "//tools/bazel:phase18_verify_tests"
      via: "phase18-verify recipe first command"
      pattern: "bazel run //tools/bazel:phase18_verify_tests"
---

<objective>
Create the Phase 18 retained-code acceptance and final cutover review gate.

Purpose: Turn retained-code acceptance, final reference-demotion criteria, maintainer exceptions, and final cutover readiness into explicit machine-verifiable review inputs without claiming local approval or demoting the C/C++ reference path.

Output: A checked-in Phase 18 JSON contract, stdlib Python verifier/tests, deterministic redacted quick artifacts under `build/ci-evidence/phase18`, Bazel labels/root aliases, `rust_workflow.sh` dispatch, and `just phase18-verify`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-retained-code-acceptance-and-cutover-review/18-CONTEXT.md
@.planning/phases/18-retained-code-acceptance-and-cutover-review/18-RESEARCH.md
@.planning/phases/18-retained-code-acceptance-and-cutover-review/18-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md

@.planning/phases/13-ci-evidence-orchestration/13-01-SUMMARY.md
@.planning/phases/14-simulator-evidence-gates/14-01-SUMMARY.md
@.planning/phases/15-hardware-safety-and-media-qualification/15-01-SUMMARY.md
@.planning/phases/16-live-network-and-transfer-qualification/16-01-SUMMARY.md
@.planning/phases/17-release-candidate-artifact-and-signing-gates/17-01-SUMMARY.md
@.planning/milestones/v1.0-phases/05-foreign-code-unsafe-and-runtime-boundary/05-01-SUMMARY.md
@.planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-01-SUMMARY.md
@.planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-04-SUMMARY.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

@tools/bazel/phase17_release_candidate_evidence.py
@tools/bazel/phase17_release_candidate_evidence_test.py
@tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
@tools/bazel/phase16_live_network_evidence.py
@tools/bazel/phase15_hardware_evidence.py
@tools/bazel/phase14_simulator_evidence.py
@tools/bazel/phase13_ci_evidence.py
@tools/bazel/phase11_verify.py
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/manifests/phase11_retained_code_justifications.json
@tools/bazel/manifests/foreign_code_inventory.json
@tools/bazel/manifests/unsafe_boundary_audit.json
@tools/bazel/manifests/phase13_ci_evidence_contract.json
@tools/bazel/manifests/phase14_simulator_evidence_contract.json
@tools/bazel/manifests/phase15_hardware_evidence_contract.json
@tools/bazel/manifests/phase16_live_network_evidence_contract.json
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
Existing Phase verifier dispatch pattern:
```bash
phase17_verify)
  python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only
  python3 tools/bazel/phase17_release_candidate_evidence.py --quick
  ;;
phase17_verify_tests)
  python3 tools/bazel/phase17_release_candidate_evidence_test.py
  ;;
```

Existing just ordering pattern:
```make
phase17-verify:
    bazel run //tools/bazel:phase17_verify_tests
    bazel run //tools/bazel:phase17_verify
```

Phase 18 retained packet status vocabulary per D-04:
```text
pending-evidence
pending-maintainer-review
accepted
rejected
blocked
deferred-approved-exception
rejected-redaction
rejected-overclaim
```

Phase 18 final demotion criterion status vocabulary per D-08:
```text
pending
passed
failed
blocked
exception-requested
exception-approved
exception-rejected
not-applicable
rejected-redaction
rejected-overclaim
```

Only these final demotion statuses may contribute to `demotion_allowed: true`:
```text
passed
exception-approved
not-applicable
```

Approved exception records must require these fields per D-09:
```text
scope
rationale
approver
approver_role
affected_printer_or_release_surface
mitigation_or_follow_up
expiry_or_review_trigger
evidence_refs
```

Source-ref collection map the verifier must support:
```text
tools/bazel/manifests/phase11_retained_code_justifications.json | retained_code_justifications | id
tools/bazel/manifests/foreign_code_inventory.json | components | id
tools/bazel/manifests/unsafe_boundary_audit.json | surfaces | surface_id
tools/bazel/manifests/phase11_cutover_readiness.json | cutover_criteria | id
tools/bazel/manifests/phase13_ci_evidence_contract.json | gates | id
tools/bazel/manifests/phase14_simulator_evidence_contract.json | scenarios | id
tools/bazel/manifests/phase15_hardware_evidence_contract.json | scenarios | id
tools/bazel/manifests/phase16_live_network_evidence_contract.json | scenarios | id
tools/bazel/manifests/phase17_release_candidate_evidence_contract.json | rows | id
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
|----------|------|------|----------|-------|
| D-01 | 18-01 | 1 | Full | Creates a Phase 18-owned row-level JSON acceptance packet contract. |
| D-02 | 18-01 | 1 | Full | Verifier computes required coverage from Phase 11 retained rows, foreign-code inventory, and unsafe-boundary audit. |
| D-03 | 18-01 | 1 | Full | Packet schema requires identity, taxonomy, source/evidence refs, owner, approver role, status, rationale, residual risk, blocker/deferred/exception fields, secret policy, and unsupported claims. |
| D-04 | 18-01 | 1,2 | Full | Uses exact retained-code status vocabulary and separates evidence collection from maintainer review. |
| D-05 | 18-01 | 2 | Full | Quick mode generates a maintainer-readable summary from machine rows; JSON remains authoritative. |
| D-06 | 18-01 | 1,2 | Full | Models final review as evidence index plus maintainer decision packet, not prose-only approval. |
| D-07 | 18-01 | 1,2 | Full | Final criteria link CI, simulator, hardware, live-service, release, retained-code, residual-risk, local proof, and non-local evidence. |
| D-08 | 18-01 | 2 | Full | `demotion_allowed` is deterministic and false unless all final criteria are `passed`, `exception-approved`, or valid `not-applicable`. |
| D-09 | 18-01 | 2 | Full | Exception validation requires scope, rationale, approver, affected surface, mitigation/follow-up, expiry/review trigger, and evidence links. |
| D-10 | 18-01 | 2 | Full | Evaluator is explicit close-to-schema Python logic, not a broad policy mini-engine. |
| D-11 | 18-01 | 2,3 | Full | Uses checked-in contract, verifier, dry-run artifacts, Bazel labels, and `just phase18-verify`; generated output is under `build/ci-evidence/phase18`. |
| D-12 | 18-01 | 2 | Full | Human-readable report is generated review material; machine rows and decision input determine status. |
| D-13 | 18-01 | 2 | Full | Quick artifacts include run manifest, normalized final-demotion results, retained-code summary, residual-risk register, redacted report, source snapshot, and decision template. |
| D-14 | 18-01 | 2 | Full | Verifier rejects secrets, payloads, crash dumps, credential values, key/cert material, path traversal, stale refs, overclaims, and approval without decision input. |
| D-15 | 18-01 | 1 | Full | Every Phase 18 row maps to REV IDs plus archived v1.0, Phase 11, and Phase 13-17 evidence refs. |
| D-16 | 18-01 | 1,2 | Full | Adds `tools/bazel/phase18_cutover_review.py` and `tools/bazel/phase18_cutover_review_test.py`. |
| D-17 | 18-01 | 3 | Full | Exposes Phase 18 through contract manifest, Bazel labels, root aliases/docs, `rust_workflow.sh`, and `just phase18-verify`. |
| D-18 | 18-01 | 1,2,3 | Full | Local verification is deterministic and covers schema, rows, refs, artifacts, redaction, path guards, approvals, exceptions, demotion, and wiring without real sign-off. |
| D-19 | 18-01 | 1,2 | Full | Preserves Phase 13-17 boundaries by treating their evidence as inputs, not final cutover approval. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 18 retained-code and final-demotion contract</name>
  <requirements>REV-01, REV-02, REV-03</requirements>
  <files>tools/bazel/manifests/phase18_cutover_review_contract.json, tools/bazel/phase18_cutover_review.py, tools/bazel/phase18_cutover_review_test.py</files>
  <read_first>
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-CONTEXT.md
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-RESEARCH.md
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-VALIDATION.md
    - tools/bazel/phase17_release_candidate_evidence.py
    - tools/bazel/phase17_release_candidate_evidence_test.py
    - tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
    - tools/bazel/manifests/phase11_cutover_readiness.json
    - tools/bazel/manifests/phase11_retained_code_justifications.json
    - tools/bazel/manifests/foreign_code_inventory.json
    - tools/bazel/manifests/unsafe_boundary_audit.json
    - tools/bazel/manifests/phase13_ci_evidence_contract.json
    - tools/bazel/manifests/phase14_simulator_evidence_contract.json
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json
    - tools/bazel/manifests/phase16_live_network_evidence_contract.json
  </read_first>
  <behavior>
    - Test 1: `--contract-only` passes for the complete Phase 18 contract.
    - Test 2: removing any retained packet ID fails and names the missing packet.
    - Test 3: removing any Phase 11 retained row, foreign-code component, or unsafe-boundary surface from packet coverage fails and names the uncovered source row.
    - Test 4: removing any final evidence family (`ci`, `simulator`, `hardware`, `live-service`, `release`, `retained-code`, `residual-risk`, `maintainer-decision`) fails.
    - Test 5: using a retained packet status outside D-04 vocabulary fails.
    - Test 6: using a final criterion status outside D-08 vocabulary fails.
    - Test 7: every packet and final criterion row maps to at least one of `REV-01`, `REV-02`, or `REV-03`.
    - Test 8: every `source_refs` entry resolves as an approved `file#row-id` source ref.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase18_cutover_review_contract.json` with top-level fields `schema_version: "1"`, `id: "phase18_cutover_review_contract"`, `phase: "18-retained-code-acceptance-and-cutover-review"`, `phase_lifecycle_id: "18-2026-06-20T14-27-15"`, `artifact_name: "phase18-cutover-review"`, `output_root: "build/ci-evidence/phase18"`, `retained_packet_status_vocabulary`, `final_criterion_status_vocabulary`, `review_decision_vocabulary`, `allowed_demotion_statuses`, `retained_source_collections`, `retained_code_acceptance_packet_schema`, `final_decision_schema`, `retained_code_acceptance_packets`, `final_demotion_criteria`, and `generated_artifacts`.

    Use this exact `retained_packet_status_vocabulary`: `pending-evidence`, `pending-maintainer-review`, `accepted`, `rejected`, `blocked`, `deferred-approved-exception`, `rejected-redaction`, `rejected-overclaim`.

    Use this exact `final_criterion_status_vocabulary`: `pending`, `passed`, `failed`, `blocked`, `exception-requested`, `exception-approved`, `exception-rejected`, `not-applicable`, `rejected-redaction`, `rejected-overclaim`. Use `allowed_demotion_statuses` exactly as `passed`, `exception-approved`, `not-applicable`. Use `review_decision_vocabulary` exactly as `approve`, `reject`, `exception`.

    Define `retained_source_collections` exactly for these source files and row collections: `tools/bazel/manifests/phase11_retained_code_justifications.json` collection `retained_code_justifications` key `id`; `tools/bazel/manifests/foreign_code_inventory.json` collection `components` key `id`; `tools/bazel/manifests/unsafe_boundary_audit.json` collection `surfaces` key `surface_id`; `tools/bazel/manifests/phase11_cutover_readiness.json` collection `cutover_criteria` key `id`; `tools/bazel/manifests/phase13_ci_evidence_contract.json` collection `gates` key `id`; `tools/bazel/manifests/phase14_simulator_evidence_contract.json` collection `scenarios` key `id`; `tools/bazel/manifests/phase15_hardware_evidence_contract.json` collection `scenarios` key `id`; `tools/bazel/manifests/phase16_live_network_evidence_contract.json` collection `scenarios` key `id`; `tools/bazel/manifests/phase17_release_candidate_evidence_contract.json` collection `rows` key `id`.

    Define `retained_code_acceptance_packet_schema.required_fields` exactly as `id`, `title`, `requirement_ids`, `taxonomy_tags`, `retained_source_refs`, `prior_phase_refs`, `required_evidence_refs`, `supplied_evidence_result_refs`, `owner`, `approver_role`, `approval_metadata`, `status`, `rationale`, `residual_risk`, `blocker_or_deferred_action`, `exception_ref`, `secret_handling_policy`, and `unsupported_claims`.

    Define `final_decision_schema.required_fields` exactly as `decision_id`, `criterion_id`, `decision`, `status`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `evidence_refs`, `residual_risk`, `exception`, and `redaction_summary`. The nested `exception.required_fields` must be exactly `scope`, `rationale`, `approver`, `approver_role`, `affected_printer_or_release_surface`, `mitigation_or_follow_up`, `expiry_or_review_trigger`, and `evidence_refs`.

    Add these required retained packet IDs and taxonomy coverage:
    `packet-hal-cmsis-startup-asm` with taxonomy tags `C`, `ASM`, `vendor`, `HAL`, `runtime`;
    `packet-freertos-runtime` with `C`, `vendor`, `RTOS`, `runtime`;
    `packet-marlin-cpp-print-core-oracle` with `C++`, `vendor`, `runtime`;
    `packet-network-lwip-mbedtls-wui` with `C`, `C++`, `vendor`, `network`;
    `packet-filesystem-fatfs-littlefs-libsysbase` with `C`, `C++`, `vendor`, `filesystem`;
    `packet-usb-tinyusb-and-media` with `C`, `vendor`, `filesystem`, `runtime`;
    `packet-generated-assets-resource-pipeline` with `generated`, `resource`, `runtime`;
    `packet-release-signing-and-packaging` with `signing`, `release-artifact`, `generated`;
    `packet-mmu-modbus-auxiliary-controllers` with `C`, `C++`, `vendor`, `MMU`, `auxiliary-controller`, `runtime`;
    `packet-runtime-safety-crashdump-watchdog` with `C`, `C++`, `ASM`, `runtime`.

    Each packet must set `requirement_ids` to include `REV-01`; packets that influence final demotion may also include `REV-02` and `REV-03`. Each packet must use one of the D-04 statuses, default to `pending-evidence` or `pending-maintainer-review`, include `secret_handling_policy: "name-only-or-redacted"`, and include unsupported claims `claim-local-proof-is-maintainer-acceptance`, `claim-reference-demotion-without-decision-input`, and `claim-sensitive-payload-retained`.

    Packet coverage must cover every existing ID from `tools/bazel/manifests/phase11_retained_code_justifications.json`: `retained-hal-cmsis-vendor`, `retained-freertos-rtos`, `retained-marlin-print-core-oracle`, `retained-lwip-mbedtls-wui`, `retained-fatfs-littlefs`, `retained-tinyusb-usb`, `retained-auxiliary-mmu-modbus`, and `retained-generated-assets-tools`. Packet coverage must also cover every current `components[].id` in `tools/bazel/manifests/foreign_code_inventory.json` and every current `surfaces[].surface_id` in `tools/bazel/manifests/unsafe_boundary_audit.json` through `retained_source_refs` or a contract-level `coverage_mappings` object that maps source refs to packet IDs. Do not hardcode the foreign/unsafe source ID lists in Python; derive them from the source manifests at validation time so future retained-source drift fails loudly.

    Add these required `final_demotion_criteria` IDs: `final-ci-evidence`, `final-simulator-evidence`, `final-hardware-safety-media-evidence`, `final-live-network-transfer-evidence`, `final-release-artifact-signing-evidence`, `final-retained-code-acceptance`, `final-residual-risk-review`, `final-maintainer-decision`, and `final-reference-demotion-allowed`.

    Each final criterion must include `id`, `title`, `requirement_ids`, `evidence_family`, `source_refs`, `required_decision`, `default_status`, `allowed_statuses`, `maintainer_decision_required`, `exception_allowed`, `blocks_demotion`, `residual_risk_ref`, `local_proof_boundary`, `non_local_evidence_boundary`, and `unsupported_claims`. Set `default_status` to `pending` for all criteria except `final-reference-demotion-allowed`, which must default to `blocked`. Set `blocks_demotion: true` for every final criterion. Set `maintainer_decision_required: true` for `final-retained-code-acceptance`, `final-residual-risk-review`, `final-maintainer-decision`, and `final-reference-demotion-allowed`.

    `final-ci-evidence` source refs must include `tools/bazel/manifests/phase13_ci_evidence_contract.json#ciev-01-aggregate-cutover-verifier`, `#ciev-02-run-manifest`, and `#ciev-03-redacted-summary`.
    `final-simulator-evidence` source refs must include `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-startup-bootstrap-ready` and `#sim-traceability-non-simulator-boundaries`.
    `final-hardware-safety-media-evidence` source refs must include `tools/bazel/manifests/phase15_hardware_evidence_contract.json#hard-safety-watchdog-crash-recovery`, `#hard-safety-thermal-motion-emergency-stop`, and `#hard-contract-traceability-and-redaction-boundary`.
    `final-live-network-transfer-evidence` source refs must include `tools/bazel/manifests/phase16_live_network_evidence_contract.json#live-connect-registration-token-fingerprint`, `#live-tls-certificate-verification`, and `#live-contract-traceability-redaction-boundary`.
    `final-release-artifact-signing-evidence` source refs must include `tools/bazel/manifests/phase17_release_candidate_evidence_contract.json#rel-bin-firmware-image`, `#rel-signing-key-identity`, and `#rel-reference-comparison-report`.
    `final-retained-code-acceptance` source refs must include `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-retained-code-justifications-accepted` and all eight Phase 11 retained-code refs listed above.
    `final-residual-risk-review` source refs must include `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-intentional-deltas-documented`, `#criteria-overclaim-scan-clean`, and `#criteria-non-local-gates-identified`.
    `final-maintainer-decision` source refs must include `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-reference-demotion-blocked`.
    `final-reference-demotion-allowed` source refs must include `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-reference-demotion-blocked` and `#criteria-all-v1-requirements-mapped`.

    Add `generated_artifacts` entries for `run-manifest.json`, `normalized-final-demotion-results.json`, `retained-code-acceptance-summary.json`, `residual-risk-register.json`, `redacted-readiness-report.md`, `source-contract-snapshots/phase18_cutover_review_contract.json`, and `maintainer-decision-input-template.json`.

    Create `tools/bazel/phase18_cutover_review.py` as a standard-library script with constants `PHASE`, `PHASE_LIFECYCLE_ID`, `CONTRACT_MANIFEST`, `DEFAULT_OUTPUT_DIR`, `REQUIRED_REQUIREMENT_IDS`, `REQUIRED_RETAINED_PACKET_IDS`, `REQUIRED_FINAL_CRITERION_IDS`, `RETAINED_PACKET_STATUS_VOCABULARY`, `FINAL_CRITERION_STATUS_VOCABULARY`, `ALLOWED_DEMOTION_STATUSES`, `REVIEW_DECISION_VOCABULARY`, `SOURCE_REF_ROW_COLLECTIONS`, `REQUIRED_PACKET_FIELDS`, and `REQUIRED_FINAL_CRITERION_FIELDS`. Implement `--contract-only` with JSON loading, top-level field validation, vocabulary validation, duplicate ID rejection, required packet and criterion checks, requirement coverage, source-ref resolution, retained-source coverage across the three retained inventories, final evidence family coverage, and repo-relative path validation for generated artifact entries.

    Create `tools/bazel/phase18_cutover_review_test.py` with stdlib `unittest`, temp-root fixtures, Arrange/Act/Assert comments for non-trivial tests, and a `Phase18CutoverReviewTest` class. Start with tests for the behaviors listed above, then implement only enough contract validation in `tools/bazel/phase18_cutover_review.py` for `--contract-only` to pass.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase18_cutover_review_test.py</automated>
    <automated>python3 tools/bazel/phase18_cutover_review.py --contract-only</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `test -f tools/bazel/phase18_cutover_review.py`
    - `test -f tools/bazel/phase18_cutover_review_test.py`
    - `grep -q '"id": "phase18_cutover_review_contract"' tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `grep -q '"phase_lifecycle_id": "18-2026-06-20T14-27-15"' tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `grep -q '"pending-maintainer-review"' tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `grep -q '"exception-approved"' tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `grep -q '"packet-release-signing-and-packaging"' tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `grep -q '"final-reference-demotion-allowed"' tools/bazel/manifests/phase18_cutover_review_contract.json`
    - `grep -q 'Phase18CutoverReviewTest' tools/bazel/phase18_cutover_review_test.py`
    - `python3 tools/bazel/phase18_cutover_review.py --contract-only` exits 0.
  </acceptance_criteria>
  <done>The Phase 18 contract exists, validates every retained source collection and final evidence family, resolves concrete source refs, covers REV-01 through REV-03, and establishes the authoritative row schemas for later quick and decision modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement decision evaluation, quick artifacts, and security guards</name>
  <requirements>REV-01, REV-02, REV-03</requirements>
  <files>tools/bazel/phase18_cutover_review.py, tools/bazel/phase18_cutover_review_test.py, tools/bazel/manifests/phase18_cutover_review_contract.json</files>
  <read_first>
    - tools/bazel/phase18_cutover_review.py
    - tools/bazel/phase18_cutover_review_test.py
    - tools/bazel/manifests/phase18_cutover_review_contract.json
    - tools/bazel/phase17_release_candidate_evidence.py
    - tools/bazel/phase16_live_network_evidence.py
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/phase14_simulator_evidence.py
    - tools/bazel/phase13_ci_evidence.py
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-CONTEXT.md
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `--quick` with no `--decision-input` writes all generated artifacts and keeps `demotion_allowed` false.
    - Test 2: a decision input can mark a final criterion `passed` only with complete approver, approver role, UTC timestamp, rationale, evidence refs, and redaction summary.
    - Test 3: `exception-approved` requires the full D-09 exception object; missing scope, affected surface, mitigation/follow-up, expiry/review trigger, or evidence refs fails.
    - Test 4: `demotion_allowed` becomes true only when every final criterion is `passed`, `exception-approved`, or valid `not-applicable`.
    - Test 5: any `pending`, `failed`, `blocked`, `exception-requested`, `exception-rejected`, `rejected-redaction`, or `rejected-overclaim` criterion keeps `demotion_allowed` false.
    - Test 6: retained packet status `accepted` requires approval metadata, rationale, supplied evidence result refs, and no unsupported claim guard violation.
    - Test 7: decision input with absolute paths, `..`, or artifact refs outside `build/ci-evidence/phase18` or `external://phase18/...` fails.
    - Test 8: decision input, checked-in contract, and generated artifacts containing forbidden secret, payload, crash-dump, credential, key/cert, or overclaim markers fail `--security-only`.
    - Test 9: generated report contains the exact sentence `Review material only; machine-readable gate rows and maintainer decision input determine final status.`
    - Test 10: the verifier source contains no `shell=True`, `bash -c`, `python -c`, or `node -e`.
  </behavior>
  <action>
    Extend `tools/bazel/phase18_cutover_review.py` with `--quick`, `--decision-input`, `--output-dir`, and `--security-only`.

    Decision input JSON may be either a top-level object with `decision_packet` or a top-level object with `retained_code_reviews` and `final_criterion_decisions`. Require `decision_packet.phase` to equal `18-retained-code-acceptance-and-cutover-review` when present and `decision_packet.phase_lifecycle_id` to equal `18-2026-06-20T14-27-15`. Require each `final_criterion_decisions[]` item to include `decision_id`, `criterion_id`, `decision`, `status`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `evidence_refs`, `residual_risk`, `exception`, and `redaction_summary`. Require each `retained_code_reviews[]` item to include `packet_id`, `status`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `supplied_evidence_result_refs`, `residual_risk`, `blocker_or_deferred_action`, `exception_ref`, and `redaction_summary`.

    Accept `decision` values only from `approve`, `reject`, and `exception`. Accept final criterion statuses only from the D-08 vocabulary. Accept retained packet statuses only from the D-04 vocabulary. Require `decision_timestamp` to be ISO-8601 UTC ending in `Z`. Require all `evidence_refs`, `supplied_evidence_result_refs`, and generated artifact refs to either be repo-relative under `build/ci-evidence/phase18` or start with `external://phase18/`; source refs still use the approved `file#row-id` resolver from Task 1.

    Implement `demotion_allowed` as an explicit pure helper close to the row schema. It must return true only when every required final criterion has status `passed`, `exception-approved`, or valid `not-applicable`, and only when decision input was supplied. A `not-applicable` final criterion is valid only when it has `decision: "exception"`, non-empty rationale, non-empty evidence refs, and an exception object with all D-09 fields. Without decision input, quick mode must always produce `decision_inputs_supplied: false` and `demotion_allowed: false`.

    For retained-code packet review, `accepted` requires complete approver metadata, non-empty rationale, non-empty `supplied_evidence_result_refs`, and no redaction/overclaim failure. `deferred-approved-exception` requires `exception_ref`, `blocker_or_deferred_action`, complete approver metadata, and non-empty rationale. `rejected`, `blocked`, `rejected-redaction`, and `rejected-overclaim` require non-empty rationale and owner/approver role metadata. `pending-evidence` and `pending-maintainer-review` may appear in quick output but must keep demotion false.

    Generate these files under `build/ci-evidence/phase18` during `--quick`: `run-manifest.json`, `normalized-final-demotion-results.json`, `retained-code-acceptance-summary.json`, `residual-risk-register.json`, `redacted-readiness-report.md`, `source-contract-snapshots/phase18_cutover_review_contract.json`, and `maintainer-decision-input-template.json`.

    `run-manifest.json` must include `phase`, `phase_lifecycle_id`, `artifact_name`, `command_mode: "quick"`, `output_root`, `decision_inputs_supplied`, `demotion_allowed`, `requirement_coverage`, `status_counts`, `retained_packet_status_counts`, `final_criterion_status_counts`, `source_contract_snapshot_path`, and `generated_artifacts`.

    `normalized-final-demotion-results.json` must include one result per final criterion with `id`, `requirement_ids`, `evidence_family`, `status`, `decision`, `maintainer_decision_required`, `exception_allowed`, `blocks_demotion`, `source_refs`, `evidence_refs`, `residual_risk`, and `demotion_blocking_reason`.

    `retained-code-acceptance-summary.json` must include one entry per retained packet with `id`, `taxonomy_tags`, `status`, `owner`, `approver_role`, `retained_source_refs`, `required_evidence_refs`, `supplied_evidence_result_refs`, `residual_risk`, `blocker_or_deferred_action`, and `exception_ref`.

    `residual-risk-register.json` must include entries derived from every final criterion and retained packet whose status is not `passed`, `accepted`, `exception-approved`, or valid `not-applicable`, with fields `id`, `source`, `status`, `risk`, `owner`, `required_action`, and `evidence_refs`.

    `redacted-readiness-report.md` must be generated from normalized rows and must include the exact sentence `Review material only; machine-readable gate rows and maintainer decision input determine final status.` It must include a `demotion_allowed: false` line in no-decision quick mode and must not claim final approval. `maintainer-decision-input-template.json` must include empty/example entries for `retained_code_reviews` and `final_criterion_decisions`, but example values must not contain credentials, private key material, raw firmware payloads, raw crash dumps, or production service data.

    Implement security scanning for the checked-in contract, optional decision input, and generated artifacts. Reject these forbidden field names anywhere in input JSON: `private_key`, `signing_key_value`, `certificate_private_material`, `raw_key_bytes`, `certificate_pem`, `certificate_bytes`, `firmware_payload`, `raw_firmware_payload`, `bbf_payload`, `dfu_payload`, `raw_crash_dump`, `token`, `password`, `secret`, `credential_value`, `wifi_password`, `connect_token`, and `prusalink_password`.

    Reject these forbidden text markers in contract/input/generated text: `BEGIN PRIVATE KEY`, `BEGIN RSA PRIVATE KEY`, `BEGIN EC PRIVATE KEY`, `firmware payload`, `raw firmware payload`, `raw crash dump`, `password=`, `token=`, `secret=`, `reference demotion approved`, `final cutover complete`, `cutover readiness proven`, `retained-code accepted by maintainer`, `retained code accepted by maintainer`, `maintainer approval complete`, and `local proof approved demotion`.

    Reject local proof overclaims by failing when no decision input is supplied and generated output tries to set `demotion_allowed` true or a final criterion status to `passed`, `exception-approved`, or `not-applicable`. Reject retained-code acceptance overclaims by failing when a packet is `accepted` or `deferred-approved-exception` without validated retained review input.

    If subprocesses are needed, use only `subprocess.run([...], cwd=ROOT, stdout=..., stderr=..., text=True, check=False, shell=False)`. Prefer no subprocess calls in Phase 18 verifier logic; add a unit test that scans `tools/bazel/phase18_cutover_review.py` for `shell=True`, `bash -c`, `python -c`, and `node -e` and fails if any are present.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase18_cutover_review_test.py</automated>
    <automated>python3 tools/bazel/phase18_cutover_review.py --contract-only</automated>
    <automated>python3 tools/bazel/phase18_cutover_review.py --quick</automated>
    <automated>python3 tools/bazel/phase18_cutover_review.py --security-only</automated>
  </verify>
  <acceptance_criteria>
    - `test -f build/ci-evidence/phase18/run-manifest.json`
    - `test -f build/ci-evidence/phase18/normalized-final-demotion-results.json`
    - `test -f build/ci-evidence/phase18/retained-code-acceptance-summary.json`
    - `test -f build/ci-evidence/phase18/residual-risk-register.json`
    - `test -f build/ci-evidence/phase18/redacted-readiness-report.md`
    - `test -f build/ci-evidence/phase18/source-contract-snapshots/phase18_cutover_review_contract.json`
    - `test -f build/ci-evidence/phase18/maintainer-decision-input-template.json`
    - `jq -e '.decision_inputs_supplied == false' build/ci-evidence/phase18/run-manifest.json`
    - `jq -e '.demotion_allowed == false' build/ci-evidence/phase18/run-manifest.json`
    - `grep -q 'Review material only; machine-readable gate rows and maintainer decision input determine final status.' build/ci-evidence/phase18/redacted-readiness-report.md`
    - `grep -q 'demotion_allowed: false' build/ci-evidence/phase18/redacted-readiness-report.md`
    - `python3 tools/bazel/phase18_cutover_review.py --security-only` exits 0 after `--quick`.
    - `grep -R "reference demotion approved\\|final cutover complete\\|retained-code accepted by maintainer\\|raw crash dump\\|BEGIN PRIVATE KEY" build/ci-evidence/phase18` finds no matches.
    - `grep -R "shell=True\\|bash -c\\|python -c\\|node -e" tools/bazel/phase18_cutover_review.py` finds no matches.
  </acceptance_criteria>
  <done>The verifier produces deterministic redacted review artifacts, validates maintainer decision semantics and exceptions, rejects unsafe inputs and overclaims, and keeps local quick demotion blocked unless valid decision input proves every final gate status is allowed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Phase 18 into Bazel, root aliases, rust workflow, and just</name>
  <requirements>REV-01, REV-02, REV-03</requirements>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase18_cutover_review.py, tools/bazel/phase18_cutover_review_test.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase18_cutover_review.py
    - tools/bazel/phase18_cutover_review_test.py
    - tools/bazel/manifests/phase18_cutover_review_contract.json
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-CONTEXT.md
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-RESEARCH.md
    - .planning/phases/18-retained-code-acceptance-and-cutover-review/18-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `--wiring-only` fails in a temp root when any Phase 18 Bazel label, root alias, docs filegroup, source-ref filegroup, `rust_workflow.sh` case, or `just` recipe line is missing.
    - Test 2: `just phase18-verify` runs `bazel run //tools/bazel:phase18_verify_tests` before `bazel run //tools/bazel:phase18_verify`.
    - Test 3: Bazel can query `//tools/bazel:phase18_verify`, `//tools/bazel:phase18_verify_tests`, `//:phase18_verify`, and `//:phase18_verify_tests`.
    - Test 4: `bazel run //tools/bazel:phase18_verify_tests` and `bazel run //tools/bazel:phase18_verify` both pass.
  </behavior>
  <action>
    Add `filegroup(name = "phase18_source_ref_manifests", srcs = [...])` to `tools/bazel/BUILD.bazel` with these exact srcs: `manifests/phase11_cutover_readiness.json`, `manifests/phase11_retained_code_justifications.json`, `manifests/foreign_code_inventory.json`, `manifests/unsafe_boundary_audit.json`, `manifests/phase13_ci_evidence_contract.json`, `manifests/phase14_simulator_evidence_contract.json`, `manifests/phase15_hardware_evidence_contract.json`, `manifests/phase16_live_network_evidence_contract.json`, and `manifests/phase17_release_candidate_evidence_contract.json`.

    Add `shell_binary(name = "phase18_verify", src = "rust_workflow.sh", data = ["phase18_cutover_review.py", "manifests/phase18_cutover_review_contract.json", ":phase18_source_ref_manifests", "//:phase18_cutover_review_docs", "//:phase11_cutover_evidence_docs", "//:phase13_ci_evidence_docs", "//:phase14_simulator_evidence_docs", "//:phase15_hardware_evidence_docs", "//:phase16_live_network_evidence_docs", "//:phase17_release_candidate_evidence_docs"])` to `tools/bazel/BUILD.bazel`.

    Add `shell_binary(name = "phase18_verify_tests", src = "rust_workflow.sh", data = ["phase18_cutover_review.py", "phase18_cutover_review_test.py", "manifests/phase18_cutover_review_contract.json", ":phase18_source_ref_manifests"])` to `tools/bazel/BUILD.bazel`.

    In root `BUILD.bazel`, add `filegroup(name = "phase18_cutover_review_docs", srcs = [".planning/phases/18-retained-code-acceptance-and-cutover-review/18-CONTEXT.md", ".planning/phases/18-retained-code-acceptance-and-cutover-review/18-RESEARCH.md", ".planning/phases/18-retained-code-acceptance-and-cutover-review/18-VALIDATION.md", ".planning/phases/18-retained-code-acceptance-and-cutover-review/18-01-PLAN.md"])`, plus aliases `phase18_verify` and `phase18_verify_tests` to the matching `//tools/bazel` targets.

    In `tools/bazel/rust_workflow.sh`, add dispatch cases:
    `phase18_verify)` runs `python3 tools/bazel/phase18_cutover_review.py --wiring-only` then `python3 tools/bazel/phase18_cutover_review.py --quick`.
    `phase18_verify_tests)` runs `python3 tools/bazel/phase18_cutover_review_test.py`.

    In `justfile`, add exactly:
    `phase18-verify:`
    `    bazel run //tools/bazel:phase18_verify_tests`
    `    bazel run //tools/bazel:phase18_verify`

    Extend `tools/bazel/phase18_cutover_review.py --wiring-only` and tests so they require the exact strings above, require `phase18_source_ref_manifests` in `tools/bazel/BUILD.bazel`, require `phase18_cutover_review_docs` in root `BUILD.bazel`, require root aliases for both Phase 18 labels, require `phase18_verify)` and `phase18_verify_tests)` in `rust_workflow.sh`, and fail if `just phase18-verify` orders the verifier before tests.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase18_cutover_review_test.py</automated>
    <automated>python3 tools/bazel/phase18_cutover_review.py --wiring-only</automated>
    <automated>bazel query "//tools/bazel:phase18_verify + //tools/bazel:phase18_verify_tests + //:phase18_verify + //:phase18_verify_tests"</automated>
    <automated>bazel run //tools/bazel:phase18_verify_tests &amp;&amp; bazel run //tools/bazel:phase18_verify</automated>
    <automated>just phase18-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "phase18_source_ref_manifests"' tools/bazel/BUILD.bazel`
    - `grep -q 'manifests/phase18_cutover_review_contract.json' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase18_verify"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase18_verify_tests"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase18_cutover_review_docs"' BUILD.bazel`
    - `grep -q 'name = "phase18_verify"' BUILD.bazel`
    - `grep -q 'name = "phase18_verify_tests"' BUILD.bazel`
    - `grep -q 'phase18_verify)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase18_verify_tests)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase18-verify:' justfile`
    - `bazel query "//tools/bazel:phase18_verify + //tools/bazel:phase18_verify_tests + //:phase18_verify + //:phase18_verify_tests"` exits 0.
    - `just phase18-verify` exits 0.
  </acceptance_criteria>
  <done>Phase 18 is exposed through Bazel and the developer facade, with tests-before-verifier ordering enforced by the verifier and `just phase18-verify` proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer decision JSON -> verifier | Untrusted approval, rejection, and exception metadata enters `tools/bazel/phase18_cutover_review.py` through `--decision-input`. |
| checked-in contract JSON -> verifier | The contract controls packet coverage, source refs, final criteria, generated artifact names, and status vocabulary. |
| Phase 11/13/14/15/16/17 manifests -> Phase 18 evidence index | Prior phase evidence rows become source refs for final review but must not be upgraded to maintainer approval. |
| verifier -> filesystem | Generated review artifacts are written under `build/ci-evidence/phase18`. |
| generated readiness material -> maintainer review | Redacted reports are consumed by humans but are not authoritative approval records. |
| Bazel/just workflow -> local verification | Developer commands execute the verifier and tests and must preserve tests-before-verifier ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Spoofing / Elevation of Privilege | maintainer decision input | mitigate | Require approver, approver role, UTC timestamp, rationale, evidence refs, redaction summary, and valid decision/status vocabulary before any decision can affect final criterion status. |
| T-18-02 | Tampering | source refs and retained coverage | mitigate | Whitelist source manifests, resolve `file#row-id` refs against exact collection/key mappings, reject missing rows, and derive retained-source coverage from Phase 11 retained rows, foreign-code inventory, and unsafe-boundary audit. |
| T-18-03 | Information Disclosure | decision input and generated readiness artifacts | mitigate | Reject private key/certificate fields, credential fields, raw firmware payload fields, raw crash dump fields, token/password/secret assignment text, and forbidden PEM markers before writing or accepting artifacts. |
| T-18-04 | Repudiation | approved exceptions and retained acceptance | mitigate | Exceptions require scope, rationale, approver, approver role, affected surface, mitigation/follow-up, expiry or review trigger, and evidence links; retained packet acceptance requires supplied evidence result refs and rationale. |
| T-18-05 | Elevation of Privilege / Tampering | demotion computation | mitigate | Implement `demotion_allowed` as explicit deterministic logic: false without decision input, true only when every required final criterion is `passed`, `exception-approved`, or valid `not-applicable`. |
| T-18-06 | Tampering / Information Disclosure | output paths and artifact refs | mitigate | Require generated outputs under `build/ci-evidence/phase18`; accept external refs only under `external://phase18/`; reject absolute paths, parent traversal, and resolved path escapes. |
| T-18-07 | Repudiation / Process Integrity | local proof overclaim | mitigate | Security scan rejects reference-demotion approval, final cutover completion, retained-code acceptance, and local proof approval phrases without validated decision input. |
| T-18-08 | Denial of Service | malformed JSON and duplicate rows | mitigate | Parse boundary JSON once, reject duplicate packet/criterion IDs and duplicate decision rows, return bounded validation errors, and do not write artifacts after failed input validation. |
| T-18-09 | Process Integrity | Bazel/just wiring | mitigate | `--wiring-only` requires Phase 18 labels, root aliases, docs filegroup, source-ref filegroup, workflow dispatch, and just recipe ordering with tests before verifier. |
</threat_model>

<verification>
Run these commands after all tasks:

1. `python3 tools/bazel/phase18_cutover_review_test.py`
2. `python3 tools/bazel/phase18_cutover_review.py --contract-only`
3. `python3 tools/bazel/phase18_cutover_review.py --quick`
4. `python3 tools/bazel/phase18_cutover_review.py --security-only`
5. `python3 tools/bazel/phase18_cutover_review.py --wiring-only`
6. `bazel query "//tools/bazel:phase18_verify + //tools/bazel:phase18_verify_tests + //:phase18_verify + //:phase18_verify_tests"`
7. `bazel run //tools/bazel:phase18_verify_tests && bazel run //tools/bazel:phase18_verify`
8. `just phase18-verify`
9. `node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 18 --expect-id 18-2026-06-20T14-27-15 --expect-mode yolo --require-plans`
10. `git diff --check`

Expected generated files after `--quick`:
- `build/ci-evidence/phase18/run-manifest.json`
- `build/ci-evidence/phase18/normalized-final-demotion-results.json`
- `build/ci-evidence/phase18/retained-code-acceptance-summary.json`
- `build/ci-evidence/phase18/residual-risk-register.json`
- `build/ci-evidence/phase18/redacted-readiness-report.md`
- `build/ci-evidence/phase18/source-contract-snapshots/phase18_cutover_review_contract.json`
- `build/ci-evidence/phase18/maintainer-decision-input-template.json`

Expected local quick truth:
- `jq -e '.decision_inputs_supplied == false' build/ci-evidence/phase18/run-manifest.json`
- `jq -e '.demotion_allowed == false' build/ci-evidence/phase18/run-manifest.json`
</verification>

<success_criteria>
- `tools/bazel/manifests/phase18_cutover_review_contract.json` exists and validates with REV-01, REV-02, and REV-03 coverage.
- Retained-code packet coverage includes all eight Phase 11 retained-code rows and every current `foreign_code_inventory.components[].id` and `unsafe_boundary_audit.surfaces[].surface_id` through packets or explicit source-to-packet mappings.
- Final-demotion criteria cover CI, simulator, hardware, live-service, release, retained-code, residual-risk, maintainer decision, and final demotion allowed rows.
- `tools/bazel/phase18_cutover_review.py` supports `--contract-only`, `--security-only`, `--quick`, `--decision-input`, `--output-dir`, and `--wiring-only`.
- `tools/bazel/phase18_cutover_review_test.py` proves contract coverage, source refs, decision metadata, exception metadata, demotion semantics, redaction guards, path guards, generated artifacts, and wiring.
- Local quick mode writes the Phase 18 generated artifact bundle and keeps `demotion_allowed` false when no maintainer decision input is supplied.
- Decision-input validation can approve, reject, or exception each final criterion while rejecting incomplete approvals, incomplete exceptions, redaction failures, source-ref tampering, and local proof overclaims.
- `bazel run //tools/bazel:phase18_verify_tests && bazel run //tools/bazel:phase18_verify` passes.
- `just phase18-verify` passes and runs tests before verifier.
- No committed source or generated Phase 18 artifact contains private signing material, raw firmware payload bytes, raw crash dumps, credentials, retained-code acceptance overclaims, reference-demotion approval claims, or final cutover completion claims.
</success_criteria>

<output>
After completion, create `.planning/phases/18-retained-code-acceptance-and-cutover-review/18-01-SUMMARY.md`.
</output>
