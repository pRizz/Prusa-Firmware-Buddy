---
phase: 15-hardware-safety-and-media-qualification
plan: 01
plan_id: 15-01
type: execute
wave: 1
depends_on:
  - 14-simulator-evidence-gates
dependencies:
  - "Phase 14 simulator evidence gate is complete and provides the closest contract/verifier/wiring pattern."
  - "Phase 13 CI evidence retention is complete and provides the generated artifact model."
files_modified:
  - tools/bazel/manifests/phase15_hardware_evidence_contract.json
  - tools/bazel/phase15_hardware_evidence.py
  - tools/bazel/phase15_hardware_evidence_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - HARD-01
  - HARD-02
  - HARD-03
requirements_addressed:
  - HARD-01
  - HARD-02
  - HARD-03
requirement_ids:
  - HARD-01
  - HARD-02
  - HARD-03
deliverables:
  - "Phase-15-owned hardware evidence contract under tools/bazel/manifests/"
  - "Stdlib Python verifier/collector with contract, operator-evidence, dry-run, security, and wiring modes"
  - "Ignored build/ci-evidence/phase15 run manifest, normalized scenario results, redacted hardware summary, source contract snapshot, and log references"
  - "Bazel labels and just phase15-verify facade"
  - "Focused stdlib unittest coverage for row completeness, metadata, redaction, path guards, overclaim guards, generated artifacts, operator evidence, and wiring"
verification:
  - "python3 -m json.tool tools/bazel/manifests/phase15_hardware_evidence_contract.json >/dev/null"
  - "python3 tools/bazel/phase15_hardware_evidence_test.py"
  - "python3 tools/bazel/phase15_hardware_evidence.py --contract-only"
  - "python3 tools/bazel/phase15_hardware_evidence.py --security-only"
  - "python3 tools/bazel/phase15_hardware_evidence.py --quick"
  - "python3 tools/bazel/phase15_hardware_evidence.py --wiring-only"
  - "bazel run //tools/bazel:phase15_verify_tests"
  - "bazel run //tools/bazel:phase15_verify"
  - "just phase15-verify"
  - "git diff --check"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-06-17T22-53-45
generated_at: 2026-06-17T23:12:00.563Z
must_haves:
  truths:
    - "Maintainer can inspect row-level hardware smoke, board readiness, storage-media, physical UI input, MMU, RS485/Modbus, toolchanger, and auxiliary-controller evidence requirements."
    - "Maintainer can record hardware safety evidence for watchdog, thermal/motion safety, emergency stop, safe-output, crash recovery, physical UI input, MMU faults, RS485/Modbus faults, and toolchanger fault/calibration scenarios."
    - "Maintainer can review generated Phase 15 evidence that names device, printer family, board, firmware build, operator, timestamp, scenario, result, artifact reference, and residual risk."
    - "Local verification validates contracts, source refs, redaction, operator input shape, generated artifact shape, and workflow wiring without claiming physical hardware proof."
    - "Generated and operator evidence cannot leak secrets, raw crash/RAM dumps, firmware payloads, private keys/certificates/tokens, path traversal, or final cutover/release/signing/reference-demotion overclaims."
  artifacts:
    - path: "tools/bazel/manifests/phase15_hardware_evidence_contract.json"
      provides: "Phase 15 row-level hardware evidence contract"
      contains: "hard-supported-printer-smoke-coreone-xbuddy"
    - path: "tools/bazel/phase15_hardware_evidence.py"
      provides: "Verifier, dry-run evidence writer, optional operator JSON validator, redaction scanner, and wiring checker"
      exports: ["main"]
    - path: "tools/bazel/phase15_hardware_evidence_test.py"
      provides: "Regression tests for Phase 15 contract, operator evidence, artifacts, security, and wiring"
    - path: "tools/bazel/BUILD.bazel"
      provides: "phase15_verify, phase15_verify_tests, and Phase 15 source-ref manifest runfiles"
      contains: "phase15_verify"
    - path: "BUILD.bazel"
      provides: "Root aliases and Phase 15 evidence docs filegroup"
      contains: "phase15_hardware_evidence_docs"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase15-verify"
  key_links:
    - from: "tools/bazel/phase15_hardware_evidence.py"
      to: "tools/bazel/manifests/phase15_hardware_evidence_contract.json"
      via: "CONTRACT_MANIFEST constant and JSON validation"
      pattern: "phase15_hardware_evidence_contract\\.json"
    - from: "tools/bazel/phase15_hardware_evidence.py"
      to: "build/ci-evidence/phase15"
      via: "DEFAULT_OUTPUT_DIR and repo-relative path guard"
      pattern: "build/ci-evidence/phase15"
    - from: "tools/bazel/phase15_hardware_evidence.py"
      to: "operator evidence JSON"
      via: "--operator-evidence validation path"
      pattern: "operator_evidence"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase15_hardware_evidence.py"
      via: "phase15_verify dispatch"
      pattern: "phase15_verify"
    - from: "justfile"
      to: "//tools/bazel:phase15_verify"
      via: "phase15-verify recipe"
      pattern: "bazel run //tools/bazel:phase15_verify"
---

<objective>
Implement Phase 15 "Hardware Safety and Media Qualification" as a Phase-15-owned hardware evidence contract, verifier/collector, regression tests, and Bazel/just workflow gate.

Purpose: Maintainers need a durable way to review the physical hardware, safety, storage-media, UI-input, MMU, RS485, toolchanger, and auxiliary-controller evidence required for cutover readiness without confusing deterministic local checks for physical proof.

Output: JSON contract, Python verifier/collector, stdlib tests, Bazel labels, just facade, and ignored `build/ci-evidence/phase15` artifact behavior. Do not mutate archived v1.0, Phase 11, Phase 13, or Phase 14 manifests. Do not edit `.planning/config.json`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/15-hardware-safety-and-media-qualification/15-CONTEXT.md
@.planning/phases/15-hardware-safety-and-media-qualification/15-RESEARCH.md
@.planning/phases/15-hardware-safety-and-media-qualification/15-VALIDATION.md
@.planning/phases/13-ci-evidence-orchestration/13-01-SUMMARY.md
@.planning/phases/14-simulator-evidence-gates/14-01-SUMMARY.md
@.planning/phases/14-simulator-evidence-gates/14-VERIFICATION.md
@tools/bazel/phase13_ci_evidence.py
@tools/bazel/phase13_ci_evidence_test.py
@tools/bazel/manifests/phase13_ci_evidence_contract.json
@tools/bazel/phase14_simulator_evidence.py
@tools/bazel/phase14_simulator_evidence_test.py
@tools/bazel/manifests/phase14_simulator_evidence_contract.json
@tools/bazel/manifests/phase6_safety_gates.json
@tools/bazel/manifests/phase7_storage_media.json
@tools/bazel/manifests/phase8_gui_workflows.json
@tools/bazel/manifests/phase8_display_layouts.json
@tools/bazel/manifests/phase10_auxiliary_controllers.json
@tools/bazel/manifests/phase10_mmu_transport.json
@tools/bazel/manifests/phase10_modbus_rs485.json
@tools/bazel/manifests/phase10_toolchanger_dock_offsets.json
@tools/bazel/manifests/phase10_auxiliary_build_update.json
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/manifests/phase11_parity_pyramid.json
@tools/bazel/manifests/phase11_reference_comparisons.json
@tools/bazel/manifests/phase11_requirement_evidence.json
@tools/bazel/manifests/phase11_retained_code_justifications.json
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile
@ProjectOptions.cmake
@utils/presets/presets.json
@include/printers.h
@include/device/board.h
@.planning/codebase/STRUCTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

<interfaces>
Contract top-level shape to create in `tools/bazel/manifests/phase15_hardware_evidence_contract.json`:
```json
{
  "schema_version": "1",
  "id": "phase15_hardware_evidence_contract",
  "phase": "15-hardware-safety-and-media-qualification",
  "phase_lifecycle_id": "15-2026-06-17T22-53-45",
  "output_root": "build/ci-evidence/phase15",
  "artifact_name": "phase15-hardware-evidence",
  "status_vocabulary": [
    "passed",
    "failed",
    "pending-hardware-input",
    "manual-hardware-required",
    "blocked-hardware-unavailable",
    "not-applicable",
    "source-contract-passed",
    "rejected-redaction",
    "rejected-overclaim"
  ],
  "required_artifact_kinds": [
    "machine-readable-run-manifest",
    "normalized-scenario-results",
    "redacted-hardware-summary",
    "source-contract-snapshot",
    "hardware-log-reference",
    "operator-evidence-input"
  ],
  "operator_input_schema": {
    "required_fields": [
      "device",
      "printer_family",
      "board",
      "firmware_build",
      "operator",
      "timestamp",
      "scenario_id",
      "result",
      "artifact_ref",
      "residual_risk"
    ],
    "allowed_results": ["passed", "failed", "blocked-hardware-unavailable"],
    "artifact_ref_root": "build/ci-evidence/phase15"
  },
  "scenarios": []
}
```

Each `scenarios[]` row must include these fields:
```json
{
  "id": "hard-storage-usb-fatfs-removable-media",
  "title": "USB FatFs removable-media physical qualification",
  "requirement_ids": ["HARD-01", "HARD-03"],
  "v1_requirement_ids": ["IFCE-04", "VERF-03", "VERF-05"],
  "source_contract_refs": [
    "tools/bazel/manifests/phase7_storage_media.json#filesystem-usb-fatfs",
    "tools/bazel/manifests/phase11_reference_comparisons.json#ref-storage-migrations",
    "tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified"
  ],
  "printer_family": "MINI",
  "board": "BUDDY",
  "media_surface": "usb-fatfs-removable-media",
  "auxiliary_surface": "none",
  "proof_scope": "hardware-observation",
  "expected_pass_semantics": "Operator evidence shows insert, mount, list, read, write/delete, safe removal, and residual media risk for the named firmware build.",
  "expected_failure_semantics": "Failure row names scenario, requirement IDs, device, board, firmware build, artifact ref, result, reason, and residual risk.",
  "expected_artifact_path": "build/ci-evidence/phase15/logs/hard-storage-usb-fatfs-removable-media.log",
  "retained_artifact_kind": "hardware-log-reference",
  "allowed_statuses": ["pending-hardware-input", "manual-hardware-required", "blocked-hardware-unavailable", "not-applicable", "passed", "failed"],
  "operator_metadata_required": [
    "device",
    "printer_family",
    "board",
    "firmware_build",
    "operator",
    "timestamp",
    "scenario_id",
    "result",
    "artifact_ref",
    "residual_risk"
  ],
  "residual_risk_required": true,
  "unsupported_claims": ["claim-hardware-proof-from-local-checks", "claim-release-readiness", "claim-reference-demotion"]
}
```

CLI contract to implement in `tools/bazel/phase15_hardware_evidence.py`:
```text
--contract-only              validate checked-in contract, source refs, status vocabulary, and required rows
--security-only              scan checked-in contract and generated Phase 15 artifacts for secrets, raw dumps, payloads, path traversal, and overclaims
--wiring-only                verify Bazel root/tools labels, rust_workflow dispatch, and just phase15-verify recipe
--quick                      contract + security + deterministic dry-run evidence write under build/ci-evidence/phase15
--operator-evidence PATH     validate operator-supplied JSON evidence and merge valid statuses into generated artifacts
--output-dir PATH            defaults to build/ci-evidence/phase15 and must stay under that root
```
</interfaces>
</context>

<decision_coverage>
| Decision | Coverage | Task | Notes |
|----------|----------|------|-------|
| D-01 | Full | 1 | New Phase 15 contract; no mutation of Phase 11, 13, or 14 manifests. |
| D-02 | Full | 1, 2 | Row-level scenarios with device, board, firmware, scenario, artifact, operator metadata, and residual risk. |
| D-03 | Full | 1 | All supported printer families, board families, storage-media surfaces, and auxiliary combinations are explicit active, pending, blocked, or not-applicable scenario rows. |
| D-04 | Full | 1, 2 | Hardware availability statuses are required and quick mode never marks physical rows `passed`. |
| D-05 | Full | 1 | Safety rows cover watchdog, thermal/motion, emergency stop, safe-output, crash recovery, UI input, MMU, RS485, and toolchanger. |
| D-06 | Full | 1, 2 | Proof scope separates `hardware-observation` from `source-contract`. |
| D-07 | Full | 1, 2 | Redaction scanners reject raw crash/RAM dumps, credentials, tokens, certs, signing keys, and payload markers. |
| D-08 | Full | 1, 2 | Every row requires residual risk; passed operator rows still preserve gaps. |
| D-09 | Full | 2 | Generated artifacts live under ignored `build/ci-evidence/phase15`. |
| D-10 | Full | 2 | Generated manifest, normalized results, redacted summary, snapshot, and log refs are named. |
| D-11 | Full | 1, 2 | Operator metadata field list is enforced by contract and validator. |
| D-12 | Full | 1, 2 | Forbidden marker and overclaim guards are explicit tests and verifier checks. |
| D-13 | Full | 2 | Dedicated stdlib Python verifier/collector. |
| D-14 | Full | 3 | Exact Phase 15 script, test, manifest, Bazel, root alias, rust_workflow, and just surfaces. |
| D-15 | Full | 2 | Optional operator JSON input is validated; local absent-hardware mode remains deterministic. |
| D-16 | Full | 1, 2 | JSON contracts, explicit statuses, path guards, stdlib tests, and no lab automation rewrite. |
| D-17 | Full | 1 | Every row maps to HARD IDs plus v1.0 and prior phase source refs. |
| D-18 | Full | 1, 2 | Phase 14 refs may support readiness but cannot satisfy physical rows. |
| D-19 | Full | 2, 3 | Artifact retention mirrors Phase 13, but CI without hardware remains pending input. |
| D-20 | Full | 1, 2, 3 | Lifecycle ID `15-2026-06-17T22-53-45` appears in contract, plan, and generated artifacts. |
</decision_coverage>

<dependency_graph>
| Task | Needs | Creates | Parallelizable |
|------|-------|---------|----------------|
| 1 | Phase 15 context/research/validation, prior source manifests, Phase 14 pattern | Contract, row coverage, source-ref and schema tests | No, Task 2 validates and generates from this contract |
| 2 | Task 1 contract and test fixture | Verifier/collector modes, generated artifacts, operator-evidence validation, security tests | No, same runner/test files |
| 3 | Task 2 CLI names and tests | Bazel labels, root aliases, rust_workflow dispatch, just facade | No, wiring depends on final CLI contract |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 15 row-level hardware evidence contract</name>
  <files>tools/bazel/manifests/phase15_hardware_evidence_contract.json, tools/bazel/phase15_hardware_evidence.py, tools/bazel/phase15_hardware_evidence_test.py</files>
  <read_first>
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json (planned new file; confirm absence before creating)
    - tools/bazel/phase15_hardware_evidence.py (planned new file; confirm absence before creating)
    - tools/bazel/phase15_hardware_evidence_test.py (planned new file; confirm absence before creating)
    - .planning/phases/15-hardware-safety-and-media-qualification/15-CONTEXT.md
    - .planning/phases/15-hardware-safety-and-media-qualification/15-RESEARCH.md
    - .planning/phases/15-hardware-safety-and-media-qualification/15-VALIDATION.md
    - tools/bazel/phase14_simulator_evidence.py
    - tools/bazel/phase14_simulator_evidence_test.py
    - tools/bazel/manifests/phase14_simulator_evidence_contract.json
    - tools/bazel/manifests/phase6_safety_gates.json
    - tools/bazel/manifests/phase7_storage_media.json
    - tools/bazel/manifests/phase8_gui_workflows.json
    - tools/bazel/manifests/phase10_auxiliary_controllers.json
    - tools/bazel/manifests/phase10_mmu_transport.json
    - tools/bazel/manifests/phase10_modbus_rs485.json
    - tools/bazel/manifests/phase10_toolchanger_dock_offsets.json
    - tools/bazel/manifests/phase11_cutover_readiness.json
    - tools/bazel/manifests/phase11_parity_pyramid.json
    - tools/bazel/manifests/phase11_requirement_evidence.json
    - tools/bazel/manifests/phase11_reference_comparisons.json
  </read_first>
  <behavior>
    - Test `contract_accepts_complete_contract`: complete Phase 15 contract validates with `--contract-only`.
    - Test `contract_requires_all_hardware_rows`: removing any required `hard-*` scenario ID fails and names the missing row.
    - Test `contract_requires_supported_family_surface_coverage`: removing coverage for any supported printer family, board family, or storage-media surface fails and names the missing family or surface.
    - Test `contract_requires_all_hard_requirements`: removing `HARD-01`, `HARD-02`, or `HARD-03` coverage fails.
    - Test `contract_rejects_bad_source_ref`: unknown or path-traversing `source_contract_refs` fail.
    - Test `contract_rejects_invalid_pass_status_without_operator_metadata`: hardware-observation rows cannot default to `passed` and must require all operator metadata fields.
    - Test `contract_rejects_missing_residual_risk`: every row must require residual risk.
  </behavior>
  <action>
Create `tools/bazel/manifests/phase15_hardware_evidence_contract.json` using the top-level shape in `<interfaces>`, per D-01 through D-06, D-08, D-11, D-16, D-17, D-18, and D-20.

Use exactly this `status_vocabulary`: `passed`, `failed`, `pending-hardware-input`, `manual-hardware-required`, `blocked-hardware-unavailable`, `not-applicable`, `source-contract-passed`, `rejected-redaction`, `rejected-overclaim`.

Use exactly this `operator_input_schema.required_fields`: `device`, `printer_family`, `board`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `artifact_ref`, `residual_risk`. The schema must list `allowed_results` as `passed`, `failed`, and `blocked-hardware-unavailable`.

Create at least these required scenario IDs, then add any extra coverage rows required so the contract fully satisfies HARD-01. This list is not an exhaustive ceiling: the checked-in contract and tests must fail if any supported printer family, board family, or storage-media surface below is missing active, pending, blocked, or not-applicable row coverage with residual risk.

Supported printer families that must appear in the matrix: `COREONE`, `MINI`, `MK4`, `MK3.5`, `XL`, `iX`, and `XL_DEV_KIT`.

Supported board families that must appear in the matrix: `BUDDY`, `XBUDDY`, `XLBUDDY`, `DWARF`, `MODULARBED`, `XL_DEV_KIT_XLB`, and `XBUDDY_EXTENSION`.

Storage-media surfaces that must appear in the matrix: `usb-fatfs-removable-media`, `internal-littlefs`, `bbf-littlefs`, `eeprom-config-store`, `semihosting`, and `root-libsysbase-dispatch`.

1. `hard-supported-printer-smoke-coreone-xbuddy` - `HARD-01`, `HARD-03`; printer `COREONE`; board `XBUDDY`; media `none`; auxiliary `xbuddy-extension-optional`; refs `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-hardware-smoke-manual-gates`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-01`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-02`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-startup-bootstrap-ready`.
2. `hard-supported-printer-smoke-mini-buddy` - `HARD-01`, `HARD-03`; printer `MINI`; board `BUDDY`; media `usb-fatfs`; auxiliary `none`; refs `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-hardware-smoke-manual-gates`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-base-01`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-ifce-04`, `tools/bazel/manifests/phase7_storage_media.json#filesystem-usb-fatfs`.
3. `hard-board-startup-readiness-mk4-xbuddy` - `HARD-01`, `HARD-03`; printer `MK4`; board `XBUDDY`; refs `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-01`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-02`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-task-readiness-home-wui`.
4. `hard-board-startup-readiness-xl-xlbuddy-puppies` - `HARD-01`, `HARD-03`; printer `XL`; board `XLBUDDY`; auxiliary `DWARF+MODULARBED`; refs `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-dwarf`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-modular-bed`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#dwarf-runtime-fifo-loadcell-toolhead`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-ifce-06`.
5. `hard-supported-printer-smoke-mk35-xbuddy` - `HARD-01`, `HARD-03`; printer `MK3.5`; board `XBUDDY`; media `usb-fatfs-removable-media`; auxiliary `MMU2-optional`; refs `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-hardware-smoke-manual-gates`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-base-01`, `tools/bazel/manifests/phase7_storage_media.json#filesystem-usb-fatfs`.
6. `hard-supported-printer-smoke-ix-xbuddy` - `HARD-01`, `HARD-03`; printer `iX`; board `XBUDDY`; media `internal-littlefs`; auxiliary `DWARF+MODULARBED-if-configured`; refs `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-hardware-smoke-manual-gates`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-01`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-dwarf`.
7. `hard-supported-printer-smoke-xl-dev-kit-xlb` - `HARD-01`, `HARD-03`; printer `XL_DEV_KIT`; board `XL_DEV_KIT_XLB`; media `internal-littlefs`; auxiliary `not-applicable-unless-dev-kit-fixtured`; refs `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-hardware-smoke-manual-gates`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-01`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-startup-bootstrap-ready`.
8. `hard-board-startup-readiness-dwarf` - `HARD-01`, `HARD-02`, `HARD-03`; printer `XL`; board `DWARF`; auxiliary `Dwarf`; refs `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-dwarf`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#dwarf-runtime-fifo-loadcell-toolhead`, `tools/bazel/manifests/phase10_auxiliary_build_update.json#startup-flashing-bootloader-gates`.
9. `hard-board-startup-readiness-modularbed` - `HARD-01`, `HARD-02`, `HARD-03`; printer `XL`; board `MODULARBED`; auxiliary `ModularBed`; refs `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-modular-bed`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#modular-bed-runtime-bedlet-faults`, `tools/bazel/manifests/phase10_auxiliary_build_update.json#startup-flashing-bootloader-gates`.
10. `hard-board-startup-readiness-xl-dev-kit-xlb` - `HARD-01`, `HARD-03`; printer `XL_DEV_KIT`; board `XL_DEV_KIT_XLB`; auxiliary `dev-kit-board`; refs `tools/bazel/manifests/phase11_parity_pyramid.json#pyramid-hardware-smoke-manual-gates`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-01`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-startup-bootstrap-ready`.
11. `hard-storage-usb-fatfs-removable-media` - `HARD-01`, `HARD-03`; printer `MINI`; board `BUDDY`; media `usb-fatfs-removable-media`; refs `tools/bazel/manifests/phase7_storage_media.json#filesystem-usb-fatfs`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-storage-migrations`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified`.
12. `hard-storage-internal-flash-littlefs` - `HARD-01`, `HARD-03`; printer `MK4`; board `XBUDDY`; media `internal-littlefs`; refs `tools/bazel/manifests/phase7_storage_media.json#filesystem-internal-littlefs`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-ifce-05`.
13. `hard-storage-bbf-littlefs-resource-image` - `HARD-01`, `HARD-03`; printer `XL`; board `XLBUDDY`; media `bbf-littlefs`; refs `tools/bazel/manifests/phase7_storage_media.json#filesystem-bbf-littlefs`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-storage-migrations`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-ifce-05`.
14. `hard-storage-eeprom-config-store` - `HARD-01`, `HARD-03`; printer `all-supported`; board `all-master-boards`; media `eeprom-config-store`; refs `tools/bazel/manifests/phase7_storage_media.json#storage-driver-eeprom`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-storage-migrations`, `tools/bazel/manifests/phase11_retained_code_justifications.json#retained-fatfs-littlefs`.
15. `hard-storage-semihosting` - `HARD-01`, `HARD-03`; printer `debug-supported`; board `debug-supported`; media `semihosting`; refs `tools/bazel/manifests/phase7_storage_media.json#filesystem-semihosting`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified`.
16. `hard-storage-root-libsysbase-dispatch` - `HARD-01`, `HARD-03`; printer `all-supported`; board `all-supported`; media `root-libsysbase-dispatch`; refs `tools/bazel/manifests/phase7_storage_media.json#filesystem-root-listing`, `tools/bazel/manifests/phase7_storage_media.json#libsysbase-devoptab-dispatch`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-storage-migrations`.
17. `hard-ui-physical-input-encoder-touch` - `HARD-02`, `HARD-03`; printer `COREONE`; board `XBUDDY`; media `none`; auxiliary `none`; refs `tools/bazel/manifests/phase8_gui_workflows.json#screen-stack-home-bootstrap`, `tools/bazel/manifests/phase8_gui_workflows.json#menu-settings-and-home-entry`, `tools/bazel/manifests/phase8_display_layouts.json#large-480x320-gui-defaults`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-gui-filebrowser-navigation`.
18. `hard-safety-watchdog-crash-recovery` - `HARD-02`, `HARD-03`; refs `tools/bazel/manifests/phase6_safety_gates.json#watchdog-and-crash-dump-boundary`, `tools/bazel/manifests/phase6_safety_gates.json#power-panic-recovery`, `tools/bazel/manifests/phase11_cutover_readiness.json#concern-phase11-non-local-hardware-proof`; expected semantics must say raw crash/RAM dumps are never committed.
19. `hard-safety-thermal-motion-emergency-stop` - `HARD-02`, `HARD-03`; refs `tools/bazel/manifests/phase6_safety_gates.json#thermal-safety-transitions`, `tools/bazel/manifests/phase6_safety_gates.json#motion-safe-output-and-emergency-stop`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-selected-thermal-failures`.
20. `hard-safe-output-fatal-redscreen-bsod` - `HARD-02`, `HARD-03`; refs `tools/bazel/manifests/phase6_safety_gates.json#fatal-redscreen-bsod-assert`, `tools/bazel/manifests/phase6_safety_gates.json#motion-safe-output-and-emergency-stop`, `tools/bazel/manifests/phase11_requirement_evidence.json#req-core-04`.
21. `hard-mmu-fault-handling` - `HARD-01`, `HARD-02`, `HARD-03`; printer `MK4`; board `XBUDDY`; auxiliary `MMU2`; refs `tools/bazel/manifests/phase10_mmu_transport.json#mmu2-availability-reporting-stub`, `tools/bazel/manifests/phase10_mmu_transport.json#mmu2-usemmu-config-runtime-state`, `tools/bazel/manifests/phase10_mmu_transport.json#mmu2-uart-transport`, `tools/bazel/manifests/phase11_retained_code_justifications.json#retained-auxiliary-mmu-modbus`.
22. `hard-rs485-modbus-fault-handling` - `HARD-01`, `HARD-02`, `HARD-03`; printer `XL`; board `XLBUDDY`; auxiliary `RS485/Modbus puppies`; refs `tools/bazel/manifests/phase10_modbus_rs485.json#puppy-modbus-master-request-retry-timeout`, `tools/bazel/manifests/phase10_modbus_rs485.json#puppy-rs485-flow-control`, `tools/bazel/manifests/phase10_modbus_rs485.json#puppy-modbus-register-block-limits`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-auxiliary-controller-flows`.
23. `hard-toolchanger-dock-offset-calibration` - `HARD-01`, `HARD-02`, `HARD-03`; printer `XL`; board `XLBUDDY`; auxiliary `toolchanger+dock+offset`; refs `tools/bazel/manifests/phase10_toolchanger_dock_offsets.json#toolchanger-dock-identity-dwarf1-6`, `tools/bazel/manifests/phase10_toolchanger_dock_offsets.json#tool-offset-selftest-flow`, `tools/bazel/manifests/phase10_toolchanger_dock_offsets.json#tool-offset-nozzle-settings-ui`.
24. `hard-auxiliary-controller-combination-coreone-xbe` - `HARD-01`, `HARD-03`; printer `COREONE`; board `XBUDDY_EXTENSION`; auxiliary `xBuddy Extension`; refs `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-xbuddy-extension`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#xbuddy-extension-runtime-h503-special`, `tools/bazel/manifests/phase10_auxiliary_build_update.json#aux-build-xbuddy-extension-external-project`.
25. `hard-auxiliary-controller-combination-xl-dwarf-modularbed` - `HARD-01`, `HARD-03`; printer `XL`; board `XLBUDDY`; auxiliary `Dwarf+ModularBed`; refs `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-dwarf`, `tools/bazel/manifests/phase10_auxiliary_controllers.json#aux-controller-family-modular-bed`, `tools/bazel/manifests/phase10_auxiliary_build_update.json#startup-flashing-bootloader-gates`.
26. `hard-contract-traceability-and-redaction-boundary` - `HARD-03`; printer `all-supported`; board `all-supported`; proof_scope `source-contract`; refs `tools/bazel/manifests/phase13_ci_evidence_contract.json#ciev-03-redacted-summary`, `tools/bazel/manifests/phase14_simulator_evidence_contract.json#sim-traceability-non-simulator-boundaries`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-reference-demotion-blocked`, `tools/bazel/manifests/phase11_cutover_readiness.json#concern-phase11-secret-redaction`.

Use `proof_scope: "hardware-observation"` for every physical hardware, board, storage, media, UI, MMU, RS485, toolchanger, and auxiliary scenario row, and `proof_scope: "source-contract"` only for the traceability/redaction boundary row. For hardware-observation rows, `allowed_statuses` must include `pending-hardware-input`, `manual-hardware-required`, `blocked-hardware-unavailable`, `not-applicable`, `passed`, and `failed`, but local quick mode will not use `passed`. Source-contract rows may use `source-contract-passed` and `failed`.

Create the initial `tools/bazel/phase15_hardware_evidence.py` with stdlib-only constants and pure validation helpers modeled on Phase 14: `PHASE`, `PHASE_LIFECYCLE_ID`, `CONTRACT_MANIFEST`, `DEFAULT_OUTPUT_DIR`, `REQUIRED_REQUIREMENT_IDS`, `REQUIRED_SCENARIO_IDS`, `REQUIRED_SUPPORTED_PRINTER_FAMILIES`, `REQUIRED_SUPPORTED_BOARD_FAMILIES`, `REQUIRED_STORAGE_MEDIA_SURFACES`, `REQUIRED_OPERATOR_FIELDS`, `VerificationError`, JSON loading, field validation, row ID search, `file#row-id` source-ref resolution, artifact path guarding, support-matrix coverage validation, and `check_contract(root)`.

Create `tools/bazel/phase15_hardware_evidence_test.py` with stdlib `unittest`, temp-root copy helpers, Arrange/Act/Assert comments, and tests for the behaviors above. Tests must use `subprocess.run(["python3", verifier, ...], shell=False)`.
  </action>
  <acceptance_criteria>
    - `test -f tools/bazel/manifests/phase15_hardware_evidence_contract.json` exits 0.
    - `jq -r '.phase_lifecycle_id' tools/bazel/manifests/phase15_hardware_evidence_contract.json` prints `15-2026-06-17T22-53-45`.
    - `jq -r '.operator_input_schema.required_fields[]' tools/bazel/manifests/phase15_hardware_evidence_contract.json` prints `device`, `printer_family`, `board`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `artifact_ref`, and `residual_risk`.
    - `jq -r '.scenarios[].id' tools/bazel/manifests/phase15_hardware_evidence_contract.json` prints every required `hard-*` scenario ID listed in the action.
    - `jq -r '.scenarios[].printer_family' tools/bazel/manifests/phase15_hardware_evidence_contract.json` covers `COREONE`, `MINI`, `MK4`, `MK3.5`, `XL`, `iX`, and `XL_DEV_KIT` either directly or through explicit all-supported/not-applicable rows with residual risk.
    - `jq -r '.scenarios[].board' tools/bazel/manifests/phase15_hardware_evidence_contract.json` covers `BUDDY`, `XBUDDY`, `XLBUDDY`, `DWARF`, `MODULARBED`, `XL_DEV_KIT_XLB`, and `XBUDDY_EXTENSION` either directly or through explicit all-supported/not-applicable rows with residual risk.
    - `jq -r '.scenarios[].media_surface' tools/bazel/manifests/phase15_hardware_evidence_contract.json` covers `usb-fatfs-removable-media`, `internal-littlefs`, `bbf-littlefs`, `eeprom-config-store`, `semihosting`, and `root-libsysbase-dispatch`.
    - `python3 tools/bazel/phase15_hardware_evidence.py --contract-only` exits 0.
    - `python3 tools/bazel/phase15_hardware_evidence_test.py` exits 0 and includes tests named `test_contract_accepts_complete_contract`, `test_contract_requires_all_hardware_rows`, `test_contract_requires_supported_family_surface_coverage`, and `test_contract_rejects_bad_source_ref`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m json.tool tools/bazel/manifests/phase15_hardware_evidence_contract.json &gt;/dev/null</automated>
    <automated>python3 tools/bazel/phase15_hardware_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase15_hardware_evidence_test.py</automated>
  </verify>
  <done>Contract exists, covers `HARD-01`, `HARD-02`, and `HARD-03`, names every required hardware/safety/media/UI/MMU/RS485/toolchanger/auxiliary scenario family, resolves prior evidence refs, and cannot represent hardware pass evidence without required operator metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the verifier, operator evidence validation, artifacts, and security guards</name>
  <files>tools/bazel/phase15_hardware_evidence.py, tools/bazel/phase15_hardware_evidence_test.py</files>
  <read_first>
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/phase15_hardware_evidence_test.py
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json
    - tools/bazel/phase14_simulator_evidence.py
    - tools/bazel/phase14_simulator_evidence_test.py
    - tools/bazel/phase13_ci_evidence.py
    - tools/bazel/phase13_ci_evidence_test.py
    - .planning/phases/15-hardware-safety-and-media-qualification/15-VALIDATION.md
    - .planning/codebase/CONCERNS.md
  </read_first>
  <behavior>
    - Test `quick_writes_phase15_artifacts`: `--quick` writes `run-manifest.json`, `normalized-scenario-results.json`, `redacted-hardware-summary.json`, `source-contract-snapshots/phase15_hardware_evidence_contract.json`, and `logs/{scenario_id}.log`.
    - Test `quick_keeps_physical_rows_pending`: all `hardware-observation` rows are `pending-hardware-input` when no operator evidence is supplied; only `source-contract` rows may be `source-contract-passed`.
    - Test `operator_evidence_accepts_complete_pass`: complete operator JSON for a hardware row can mark that row `passed` in generated results.
    - Test `operator_evidence_rejects_missing_metadata`: missing any required operator field fails and names the field.
    - Test `operator_evidence_rejects_unknown_scenario_or_status`: unknown `scenario_id` or unsupported `result` fails.
    - Test `security_rejects_secret_markers`: private key, certificate, token, password, Wi-Fi credential, signing key, raw crash dump, raw RAM dump, `.bin` payload, `.bbf` payload, and `.dfu` payload markers fail.
    - Test `security_rejects_overclaim_wording`: exact overclaim wording fails when injected into operator evidence or generated evidence fixtures; valid contract `unsupported_claims` deny-list identifiers do not fail merely because they describe rejected claim categories.
    - Test `path_guard_rejects_traversal`: output and operator artifact refs cannot escape `build/ci-evidence/phase15`.
  </behavior>
  <action>
Extend `tools/bazel/phase15_hardware_evidence.py` with the CLI contract from `<interfaces>`, per D-04, D-06 through D-16, D-18 through D-20.

Implement these modes:

- `--contract-only`: run `check_contract(root)` and exit.
- `--security-only`: run contract validation and scan the checked-in contract plus any generated files under `build/ci-evidence/phase15`.
- `--wiring-only`: call `check_wiring(root)`; Task 3 will finish the wiring requirements, but the function and tests belong here.
- `--quick`: validate contract and security, optionally load `--operator-evidence`, write deterministic artifacts, then re-run security.
- `--operator-evidence PATH`: validate a JSON file containing either an object with `evidence_rows` or a top-level list of evidence row objects. Every row must include `device`, `printer_family`, `board`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `artifact_ref`, and `residual_risk`. `scenario_id` must match a contract row. `result` must be `passed`, `failed`, or `blocked-hardware-unavailable`. `artifact_ref` must be repo-relative and under `build/ci-evidence/phase15`. Evidence text must pass redaction and overclaim scans. If a row is `passed` for a `hardware-observation` scenario, the generated manifest must preserve `operator`, timestamp, artifact ref, and residual risk.

Use exact constants:

```python
PHASE = "15-hardware-safety-and-media-qualification"
PHASE_LIFECYCLE_ID = "15-2026-06-17T22-53-45"
CONTRACT_MANIFEST = Path("tools/bazel/manifests/phase15_hardware_evidence_contract.json")
DEFAULT_OUTPUT_DIR = Path("build/ci-evidence/phase15")
REQUIRED_REQUIREMENT_IDS = {"HARD-01", "HARD-02", "HARD-03"}
```

Use forbidden marker regexes covering these case-insensitive terms or PEM blocks: `-----BEGIN PRIVATE KEY-----`, `-----BEGIN CERTIFICATE-----`, `token_value`, `password_value`, `wifi_password`, `Wi-Fi credential`, `Connect token`, `certificate_bytes`, `private_key`, `signing_key`, `raw_crash_dump`, `raw_ram_dump`, `memory_dump`, `firmware_payload`, `bbf_payload`, `dfu_payload`, `.bin payload`, `.bbf payload`, `.dfu payload`.

Use overclaim strings covering these exact phrases in operator evidence and generated evidence content: `hardware verified locally`, `local hardware proof`, `hardware qualification passed locally`, `final cutover complete`, `cutover complete`, `release readiness proven`, `release-candidate passed locally`, `signing proof complete`, `signing verified locally`, `retained-code accepted by maintainer`, `reference demotion approved`, `reference removal complete`.

For checked-in contract JSON, parse the document and validate `unsupported_claims` as denied-claim metadata. Valid baseline values must use non-phrase identifiers such as `claim-hardware-proof-from-local-checks`, `claim-release-readiness`, and `claim-reference-demotion`. The security scan must reject exact forbidden phrases in ordinary contract fields, operator evidence, generated summaries, and generated logs, but must not reject valid `unsupported_claims` identifiers that describe capabilities the row refuses to prove. Keep the overclaim rejection test by writing exact forbidden phrases into negative operator/generated evidence fixtures, not into the valid contract.

`--quick` must write these generated artifacts under the guarded output root:

- `run-manifest.json`: phase, lifecycle ID, artifact name, command mode, generated timestamp, output root, scenario results, requirement coverage, and source-contract snapshot path.
- `normalized-scenario-results.json`: one normalized row per scenario with `scenario_id`, `status`, `requirement_ids`, `v1_requirement_ids`, `source_contract_refs`, `artifact_refs`, `operator_metadata_present`, and `residual_risk`.
- `redacted-hardware-summary.json`: maintainer-facing scenario status, unsupported boundaries, operator evidence summary, and no raw payloads or secrets.
- `source-contract-snapshots/phase15_hardware_evidence_contract.json`: sanitized copy of the checked-in contract.
- `logs/{scenario_id}.log`: dry-run log references saying physical hardware evidence is pending unless operator evidence supplied that row.

When no operator evidence is supplied, all rows with `proof_scope == "hardware-observation"` must be `pending-hardware-input`; `hard-contract-traceability-and-redaction-boundary` may be `source-contract-passed`. If operator evidence is supplied, only the matching scenario rows may take `passed`, `failed`, or `blocked-hardware-unavailable`. CI/local quick validation without operator evidence must still exit 0 because pending hardware input is the truthful state, not a failure.

Keep implementation stdlib-only. Do not import pytest, lab-control SDKs, USB libraries, serial libraries, or network clients. Do not call subprocess except for wiring checks if needed; if subprocess is needed, use argument lists and `shell=False`.
  </action>
  <acceptance_criteria>
    - `python3 tools/bazel/phase15_hardware_evidence.py --quick` exits 0.
    - `test -f build/ci-evidence/phase15/run-manifest.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase15/normalized-scenario-results.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase15/redacted-hardware-summary.json` exits 0 after quick mode.
    - `test -f build/ci-evidence/phase15/source-contract-snapshots/phase15_hardware_evidence_contract.json` exits 0 after quick mode.
    - `jq -r '.phase_lifecycle_id' build/ci-evidence/phase15/run-manifest.json` prints `15-2026-06-17T22-53-45`.
    - `jq -r '.scenarios[] | select(.proof_scope == "hardware-observation") | .status' build/ci-evidence/phase15/run-manifest.json | sort -u` prints only `pending-hardware-input` when no operator evidence is supplied.
    - `python3 tools/bazel/phase15_hardware_evidence.py --security-only` exits 0 after quick mode.
    - `python3 tools/bazel/phase15_hardware_evidence_test.py` exits 0 and includes tests named `test_operator_evidence_rejects_missing_metadata`, `test_security_rejects_secret_markers`, and `test_security_rejects_overclaim_wording`.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase15_hardware_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase15_hardware_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase15_hardware_evidence.py --security-only</automated>
    <automated>python3 tools/bazel/phase15_hardware_evidence.py --quick</automated>
  </verify>
  <done>`--quick` produces deterministic, redacted Phase 15 artifacts under `build/ci-evidence/phase15`; optional operator JSON can validate real hardware evidence later; local absent-hardware mode preserves pending rows rather than false pass evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Phase 15 into Bazel and just, then run the phase gate</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase15_hardware_evidence.py, tools/bazel/phase15_hardware_evidence_test.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/phase15_hardware_evidence_test.py
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json
    - .planning/phases/15-hardware-safety-and-media-qualification/15-VALIDATION.md
  </read_first>
  <behavior>
    - Test `wiring_accepts_phase15_surface`: `--wiring-only` accepts final Bazel labels, source-ref filegroup, root docs filegroup, root aliases, rust workflow dispatch, and `just phase15-verify`.
    - Test `wiring_rejects_missing_bazel_label`: removing `phase15_verify` or `phase15_verify_tests` fails.
    - Test `wiring_rejects_missing_source_ref_manifest`: removing a required Phase 6/7/8/10/11/13/14 source-ref manifest from Bazel runfiles fails.
    - Test `wiring_rejects_verifier_before_tests`: `just phase15-verify` must run tests before verifier.
  </behavior>
  <action>
Add Phase 15 workflow integration per D-13, D-14, D-16, D-19, and D-20.

In `tools/bazel/BUILD.bazel`, add:

```python
filegroup(
    name = "phase15_source_ref_manifests",
    srcs = [
        "manifests/phase6_safety_gates.json",
        "manifests/phase7_storage_media.json",
        "manifests/phase8_gui_workflows.json",
        "manifests/phase8_display_layouts.json",
        "manifests/phase10_auxiliary_controllers.json",
        "manifests/phase10_mmu_transport.json",
        "manifests/phase10_modbus_rs485.json",
        "manifests/phase10_toolchanger_dock_offsets.json",
        "manifests/phase10_auxiliary_build_update.json",
        "manifests/phase11_cutover_readiness.json",
        "manifests/phase11_parity_pyramid.json",
        "manifests/phase11_reference_comparisons.json",
        "manifests/phase11_requirement_evidence.json",
        "manifests/phase11_retained_code_justifications.json",
        "manifests/phase13_ci_evidence_contract.json",
        "manifests/phase14_simulator_evidence_contract.json",
    ],
)

shell_binary(
    name = "phase15_verify",
    src = "rust_workflow.sh",
    data = [
        "phase15_hardware_evidence.py",
        "manifests/phase15_hardware_evidence_contract.json",
        ":phase15_source_ref_manifests",
        "//:phase15_hardware_evidence_docs",
        "//:phase11_cutover_evidence_docs",
    ],
)

shell_binary(
    name = "phase15_verify_tests",
    src = "rust_workflow.sh",
    data = [
        "phase15_hardware_evidence.py",
        "phase15_hardware_evidence_test.py",
        "manifests/phase15_hardware_evidence_contract.json",
        ":phase15_source_ref_manifests",
    ],
)
```

In root `BUILD.bazel`, add:

```python
filegroup(
    name = "phase15_hardware_evidence_docs",
    srcs = [
        ".planning/phases/15-hardware-safety-and-media-qualification/15-CONTEXT.md",
        ".planning/phases/15-hardware-safety-and-media-qualification/15-RESEARCH.md",
        ".planning/phases/15-hardware-safety-and-media-qualification/15-VALIDATION.md",
        ".planning/phases/15-hardware-safety-and-media-qualification/15-01-PLAN.md",
    ],
)

alias(
    name = "phase15_verify",
    actual = "//tools/bazel:phase15_verify",
)

alias(
    name = "phase15_verify_tests",
    actual = "//tools/bazel:phase15_verify_tests",
)
```

In `tools/bazel/rust_workflow.sh`, add dispatch cases:

```bash
phase15_verify)
  python3 tools/bazel/phase15_hardware_evidence.py --wiring-only
  python3 tools/bazel/phase15_hardware_evidence.py --quick
  ;;
phase15_verify_tests)
  python3 tools/bazel/phase15_hardware_evidence_test.py
  ;;
```

In `justfile`, add:

```make
phase15-verify:
    bazel run //tools/bazel:phase15_verify_tests
    bazel run //tools/bazel:phase15_verify
```

Update `check_wiring(root)` in `tools/bazel/phase15_hardware_evidence.py` to require exact strings for every label, dispatch case, runfile, and recipe line above. It must also verify `justfile` runs `bazel run //tools/bazel:phase15_verify_tests` before `bazel run //tools/bazel:phase15_verify`.

After wiring works, do a simplification pass:

- Keep validation helpers pure and file/subprocess operations in thin shell functions.
- Remove duplicated fixture constants when the contract is already the source of truth without weakening tests.
- Keep generated artifacts ignored under `build/ci-evidence/phase15`; do not stage generated evidence.
- Confirm no deferred Phase 16 live network/TLS/transfer evidence, Phase 17 release/signing evidence, or Phase 18 retained-code/final demotion approval is implemented.
  </action>
  <acceptance_criteria>
    - `rg 'name = "phase15_verify"' tools/bazel/BUILD.bazel` exits 0.
    - `rg 'name = "phase15_verify_tests"' tools/bazel/BUILD.bazel` exits 0.
    - `rg 'name = "phase15_source_ref_manifests"' tools/bazel/BUILD.bazel` exits 0.
    - `rg 'phase15_hardware_evidence_docs' BUILD.bazel` exits 0.
    - `rg 'phase15_verify\\)' tools/bazel/rust_workflow.sh` exits 0.
    - `rg 'phase15_verify_tests\\)' tools/bazel/rust_workflow.sh` exits 0.
    - `rg 'phase15-verify:' justfile` exits 0.
    - `python3 tools/bazel/phase15_hardware_evidence.py --wiring-only` exits 0.
    - `bazel query "//tools/bazel:phase15_verify + //tools/bazel:phase15_verify_tests + //:phase15_verify + //:phase15_verify_tests"` exits 0.
    - `just phase15-verify` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase15_hardware_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase15_hardware_evidence.py --wiring-only</automated>
    <automated>bazel run //tools/bazel:phase15_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase15_verify</automated>
    <automated>just phase15-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>`just phase15-verify` runs Phase 15 tests before the verifier through Bazel, Phase 15 generated evidence remains ignored, and the implementation stays stdlib-only and no-overclaim.</done>
</task>

</tasks>

<threat_model>
Security enforcement is enabled. Use ASVS L1 posture for input validation, evidence redaction, command safety, and auditable proof boundaries. Block on high-severity threats; do not mark Phase 15 complete if a high-severity leakage or overclaim path is discovered.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in JSON contract -> verifier | Contract rows are parsed as untrusted data and must be schema-validated before use. |
| Prior evidence refs -> Phase 15 contract | `file#row-id` refs must resolve to existing source-backed rows without mutating archived or prior-phase artifacts. |
| Operator JSON -> generated hardware evidence | Operator evidence is external input and must require metadata, valid statuses, path guards, redaction, and residual risk. |
| Contract/operator text -> retained summaries/logs | Evidence text can leak secrets, raw dumps, firmware payloads, or overclaim proof if not scanned before retention. |
| CLI output path -> filesystem | `--output-dir` and artifact refs could write outside ignored `build/ci-evidence/phase15` without path containment. |
| Bazel/just facade -> maintainer workflow | Incorrect wiring could skip tests or falsely present CI/local validation as hardware proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | Operator evidence and generated artifacts | High | mitigate | Reject private keys, certificates, tokens, passwords, Wi-Fi credentials, raw crash dumps, raw RAM dumps, memory dumps, `.bin`/`.bbf`/`.dfu` payload markers, and signing key markers before writing or accepting retained evidence. |
| T-15-02 | Tampering | Output paths and artifact refs | High | mitigate | Require repo-relative paths under `build/ci-evidence/phase15`; reject absolute paths and `..` traversal in contract rows, output dirs, and operator `artifact_ref`. |
| T-15-03 | Repudiation | Operator hardware rows | Medium | mitigate | Require `device`, `printer_family`, `board`, `firmware_build`, `operator`, `timestamp`, `scenario_id`, `result`, `artifact_ref`, and `residual_risk` before hardware rows can become `passed` or `failed`. |
| T-15-04 | Spoofing / Repudiation | Local quick mode | High | mitigate | Quick mode sets hardware-observation rows to `pending-hardware-input`; only operator-supplied evidence can mark physical rows `passed` or `failed`. |
| T-15-05 | Repudiation | Cutover/release/signing proof claims | High | mitigate | Reject overclaim phrases for local hardware proof, final cutover completion, release readiness, signing proof, retained-code acceptance, and reference demotion. |
| T-15-06 | Tampering | Source refs | Medium | mitigate | Resolve every `source_contract_refs` entry as a repo-relative `file#row-id` against Phase 6, 7, 8, 10, 11, 13, and 14 manifests. |
| T-15-07 | Information Disclosure | Crash/fault evidence | High | mitigate | Preserve crash/fault evidence as redacted summaries and log references only; do not commit raw crash dumps, RAM dumps, credential regions, unsafe operational payloads, or firmware packages. |
| T-15-08 | Denial of Service | Hardware availability | Low | accept | Missing lab hardware remains `pending-hardware-input`, `manual-hardware-required`, or `blocked-hardware-unavailable`; this is a truthful qualification state, not a local verifier failure. |
| T-15-09 | Elevation of Privilege | Verifier command execution | Medium | mitigate | Keep verifier stdlib-only and avoid subprocess except controlled Bazel/just wiring checks; if subprocess is used, use argument lists and `shell=False`. |
</threat_model>

<goal_backward_check>
Phase 15 goal: Maintainers can evaluate hardware, safety, storage-media, UI-input, MMU, RS485, and toolchanger evidence required for cutover readiness.

Observable truths achieved by this plan:

1. Maintainers can execute a hardware smoke matrix because Task 1 creates row-level scenarios for supported printer families, boards, storage media, and auxiliary-controller combinations.
2. Maintainers can record physical safety evidence because Task 1 defines the required safety/fault scenarios and Task 2 validates operator evidence fields and statuses.
3. Maintainers can review device, firmware build, operator, timestamp, scenario, result, artifact ref, and residual risk because Task 1 makes those fields required and Task 2 emits them into generated manifests.
4. Maintainers can run deterministic local validation because Task 2 provides `--contract-only`, `--security-only`, and `--quick`, and Task 3 exposes them through Bazel and `just phase15-verify`.
5. Maintainers cannot confuse local verification with hardware proof because hardware-observation rows remain `pending-hardware-input` unless explicit operator evidence is supplied.
6. Maintainers cannot retain secret/raw/unsafe artifacts or final cutover overclaims because Task 2 scans checked-in and generated evidence and Task 3 wires those checks into the standard phase gate.
</goal_backward_check>

<verification>
Run these local checks before marking the plan complete:

```bash
python3 -m json.tool tools/bazel/manifests/phase15_hardware_evidence_contract.json >/dev/null
python3 tools/bazel/phase15_hardware_evidence_test.py
python3 tools/bazel/phase15_hardware_evidence.py --contract-only
python3 tools/bazel/phase15_hardware_evidence.py --security-only
python3 tools/bazel/phase15_hardware_evidence.py --quick
python3 tools/bazel/phase15_hardware_evidence.py --wiring-only
bazel run //tools/bazel:phase15_verify_tests
bazel run //tools/bazel:phase15_verify
just phase15-verify
git diff --check
```

Also preserve the Phase 15 validation strategy command:

```bash
python3 tools/bazel/phase15_hardware_evidence_test.py && python3 tools/bazel/phase15_hardware_evidence.py --quick
```

Do not require real hardware execution for local completion. If operator evidence is not supplied, the generated manifest must say hardware-observation rows are `pending-hardware-input`.
</verification>

<success_criteria>
- `tools/bazel/manifests/phase15_hardware_evidence_contract.json` exists and covers `HARD-01`, `HARD-02`, and `HARD-03`.
- Contract rows include supported-printer smoke, board startup/readiness, storage media, physical UI input, watchdog/crash recovery, thermal/motion/emergency stop, safe-output, MMU, RS485/Modbus, toolchanger, and auxiliary-controller combinations.
- `tools/bazel/phase15_hardware_evidence.py --quick` writes secret-safe generated artifacts under `build/ci-evidence/phase15`.
- `tools/bazel/phase15_hardware_evidence.py --operator-evidence <path>` validates complete operator JSON and rejects malformed, secret-bearing, path-traversing, or overclaiming evidence.
- `just phase15-verify` runs Phase 15 tests before the verifier through Bazel.
- No source or planning artifact includes secrets, raw crash dumps, RAM dumps, firmware payloads, private keys, certificates, tokens, signing keys, Wi-Fi credentials, release readiness claims, signing proof, reference demotion approval, or final cutover completion.
</success_criteria>

<residual_risks>
- Real physical hardware qualification still requires lab devices, operators, firmware builds, media, MMU, RS485, and toolchanger combinations; absent rows remain pending or blocked by hardware availability.
- Long-run soak, environmental extremes, unsupported removable media, and unavailable auxiliary boards remain residual risk until operator evidence or maintainer exception records them.
- Live Connect/WUI/TLS, telemetry, proxy, long-transfer, and crash-dump upload evidence remain owned by Phase 16.
- Release-candidate `.bin`, `.bbf`, `.dfu`, map/provenance, resources, WUI, ESP, MMU, auxiliary packages, and signing evidence remain owned by Phase 17.
- Retained-code acceptance and final reference-demotion approval remain owned by Phase 18.
</residual_risks>

<output>
After completion, create `.planning/phases/15-hardware-safety-and-media-qualification/15-01-SUMMARY.md` summarizing implementation, commands run, generated artifact paths, operator evidence status, hardware rows that remain pending, and residual evidence boundaries.
</output>
