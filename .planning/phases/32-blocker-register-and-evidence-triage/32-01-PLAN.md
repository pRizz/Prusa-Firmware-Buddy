---
phase: 32-blocker-register-and-evidence-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase32_blocker_register_triage_contract.json
  - tools/bazel/phase32_blocker_register_triage.py
  - tools/bazel/phase32_blocker_register_triage_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
  - .planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md
autonomous: true
requirements:
  - TRIAGE-01
  - TRIAGE-02
  - TRIAGE-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 32-2026-07-03T14-13-51
generated_at: 2026-07-03T14:34:43Z
must_haves:
  truths:
    - "Maintainer can aggregate Phase 31 final-intake rejections, quarantines, accepted receipt row refs, retained-code handoffs, and readiness blockers into one canonical blocker register."
    - "Each blocker row has row_id, source_stream, source_ref, requirement_ids, affected_gate, row_problem_kind, blocker_kind, severity, owner_ref, required_next_action, decision_impact, proof_eligibility, and evidence_refs."
    - "Quick/default placeholder, smoke fixture, local dry-run, prose-only, row-only, stale, redaction-failed, source-ref-failed, unsafe-ref, and secret-tainted rows are visible for triage but proof-ineligible."
    - "Unknown or unmapped source statuses fail closed as critical unresolved decision blockers."
    - "Phase 32 outputs do not imply maintainer approval, exception approval, retained-code acceptance, final-readiness approval, reference-demotion authorization, or cutover verdict."
  artifacts:
    - path: "tools/bazel/manifests/phase32_blocker_register_triage_contract.json"
      provides: "Phase 32 schema, source refs, policy map, generated artifacts, and verification commands"
      contains: "phase32_blocker_register_triage_contract"
    - path: "tools/bazel/phase32_blocker_register_triage.py"
      provides: "Thin CLI shell plus pure blocker classifier and handoff writer"
      exports:
        - "main"
    - path: "tools/bazel/phase32_blocker_register_triage_test.py"
      provides: "Regression coverage for aggregation, taxonomy, fail-closed security, derived views, and wiring"
    - path: "build/ci-evidence/phase32/blocker-register.json"
      provides: "Canonical source of truth for Phase 32 blocker rows"
    - path: "build/ci-evidence/phase32/decision-impact-index.json"
      provides: "Decision-impact index derived from canonical blocker rows"
    - path: "build/ci-evidence/phase32/exception-request-register.json"
      provides: "Exception-request view derived from canonical blocker rows"
    - path: "build/ci-evidence/phase32/residual-risk-request-register.json"
      provides: "Residual-risk request view derived from canonical blocker rows"
    - path: "build/ci-evidence/phase32/downstream-handoff-manifest.json"
      provides: "Machine-readable Phase 33-35 handoff over canonical row ids"
    - path: "build/ci-evidence/phase32/redacted-blocker-register-report.md"
      provides: "Human-readable redacted report derived from canonical blocker rows"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase32_blocker_register_triage_contract.json"
      provides: "Phase 32 contract snapshot used by the handoff bundle"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase31_final_evidence_intake_contract.json"
      provides: "Phase 31 source contract snapshot used by the handoff bundle"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase23_simulator_evidence_execution_contract.json"
      provides: "Phase 23 simulator source contract snapshot"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase24_hardware_media_safety_evidence_execution_contract.json"
      provides: "Phase 24 hardware/media/safety source contract snapshot"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase25_live_service_evidence_execution_contract.json"
      provides: "Phase 25 live-service source contract snapshot"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase26_release_signing_upstream_evidence_contract.json"
      provides: "Phase 26 release/signing/upstream source contract snapshot"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase27_retained_code_acceptance_decisions_contract.json"
      provides: "Phase 27 retained-code/decision source contract snapshot"
    - path: "build/ci-evidence/phase32/contract-snapshots/phase28_final_readiness_packet_contract.json"
      provides: "Phase 28 final-readiness source contract snapshot"
  key_links:
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase31/final-intake-manifest.json"
      via: "--phase31-output-dir loads manifest before any source-row detail"
      pattern: "final-intake-manifest.json"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "build/ci-evidence/phase31/rejected-submissions.json"
      via: "--phase31-output-dir makes rejected and quarantined rows visible but ineligible"
      pattern: "rejected-submissions.json"
    - from: "tools/bazel/phase32_blocker_register_triage.py"
      to: "consumed_upstream_row_refs"
      via: "accepted-final receipt row refs are followed after Phase 31 receipt loading"
      pattern: "consumed_upstream_row_refs"
    - from: "build/ci-evidence/phase32/exception-request-register.json"
      to: "build/ci-evidence/phase32/blocker-register.json"
      via: "derived rows include canonical row_id backreferences"
      pattern: "row_id"
    - from: "build/ci-evidence/phase32/residual-risk-request-register.json"
      to: "build/ci-evidence/phase32/blocker-register.json"
      via: "derived rows include canonical row_id backreferences"
      pattern: "row_id"
    - from: "build/ci-evidence/phase32/decision-impact-index.json"
      to: "build/ci-evidence/phase32/blocker-register.json"
      via: "decision impact entries are derived from canonical row_id values"
      pattern: "row_id"
    - from: "build/ci-evidence/phase32/redacted-blocker-register-report.md"
      to: "build/ci-evidence/phase32/blocker-register.json"
      via: "redacted report is generated from canonical blocker rows, not raw evidence"
      pattern: "blocker-register.json"
    - from: "justfile"
      to: "//tools/bazel:phase32_verify"
      via: "phase32-verify recipe runs tests before verifier"
      pattern: "phase32-verify"
---

<objective>
Create the Phase 32 blocker register and evidence triage handoff over Phase 31 final-intake outputs and existing v1.2/v1.3 evidence artifacts.

Purpose: let maintainers see every cutover-blocking consumed row in one machine-readable register with explicit owner, severity, affected gate, next action, proof eligibility, and decision impact while preserving Phase 31 as the finality/provenance boundary.

Output: `tools/bazel/phase32_blocker_register_triage.py`, its contract and tests, Bazel/root/just wiring, updated Nyquist validation metadata, and generated Phase 32 handoff artifacts under `build/ci-evidence/phase32`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-blocker-register-and-evidence-triage/32-CONTEXT.md
@.planning/phases/32-blocker-register-and-evidence-triage/32-RESEARCH.md
@.planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md
@.planning/phases/31-final-evidence-intake/31-01-SUMMARY.md
@.planning/phases/31-final-evidence-intake/31-VERIFICATION.md
@tools/bazel/manifests/phase31_final_evidence_intake_contract.json
@tools/bazel/phase31_final_evidence_intake.py
@tools/bazel/phase31_final_evidence_intake_test.py
@tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
@tools/bazel/manifests/phase28_final_readiness_packet_contract.json
@tools/bazel/phase28_final_readiness_packet.py
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
Existing Phase 31 finality boundary:

```text
Manifest: build/ci-evidence/phase31/final-intake-manifest.json
Rejected submissions: build/ci-evidence/phase31/rejected-submissions.json
Receipt refs field: receipt_refs
Accepted receipt status: accepted-final
Rejected/quarantined statuses: rejected-final, quarantined-non-final
Accepted receipt upstream refs field: consumed_upstream_row_refs
```

Phase 32 canonical enums required by D-07 and D-08:

```text
blocker_kind: repair_item, exception_request, unresolved_decision_blocker
row_problem_kind: failed, missing, stale, malformed, redaction_failed, source_ref_failed, secret_tainted, lifecycle_mismatch, unsafe_ref, exception_requested, non_final_placeholder, smoke_fixture, local_dry_run, prose_attestation, row_only_submission, unknown_unclassified
severity: critical, high, medium
proof_eligibility: eligible, ineligible
```

Phase 32 owner defaults to encode in the contract:

```text
simulator -> simulator-maintainer
hardware-media-safety -> safety-maintainer
live-service -> network-security-maintainer
release-signing -> release-maintainer
upstream-result -> release-maintainer
retained-code -> retained-code-maintainer
readiness -> readiness-maintainer
unknown -> cutover-maintainer
```

Phase 32 generated artifacts required by D-16:

```text
build/ci-evidence/phase32/blocker-register.json
build/ci-evidence/phase32/decision-impact-index.json
build/ci-evidence/phase32/exception-request-register.json
build/ci-evidence/phase32/residual-risk-request-register.json
build/ci-evidence/phase32/downstream-handoff-manifest.json
build/ci-evidence/phase32/redacted-blocker-register-report.md
build/ci-evidence/phase32/contract-snapshots/phase32_blocker_register_triage_contract.json
build/ci-evidence/phase32/contract-snapshots/phase31_final_evidence_intake_contract.json
build/ci-evidence/phase32/contract-snapshots/phase23_simulator_evidence_execution_contract.json
build/ci-evidence/phase32/contract-snapshots/phase24_hardware_media_safety_evidence_execution_contract.json
build/ci-evidence/phase32/contract-snapshots/phase25_live_service_evidence_execution_contract.json
build/ci-evidence/phase32/contract-snapshots/phase26_release_signing_upstream_evidence_contract.json
build/ci-evidence/phase32/contract-snapshots/phase27_retained_code_acceptance_decisions_contract.json
build/ci-evidence/phase32/contract-snapshots/phase28_final_readiness_packet_contract.json
```
</interfaces>

Important boundaries:
- Implement D-01 through D-20 exactly. Phase 31 remains authoritative for finality and provenance; Phase 32 must not scan Phase 23-26 rows as the primary intake source.
- Do not reopen or edit Phase 31 unless a test fixture proves an actual final-intake rejection cannot be classified from current reason/finality metadata.
- Do not approve exceptions, accept retained code, accept residual risk, approve final readiness, authorize reference demotion, or publish a go/no-go cutover verdict.
- Do not generate a UI spec; Phase 32 is a CLI/generated-artifact workflow.
</context>

<dependency_graph>
One plan is sufficient because all work is one vertical slice over the same contract/script/test/build files.

| Task | Needs | Creates | Parallelizable |
|------|-------|---------|----------------|
| 1 | Phase 32 context, research, validation strategy, Phase 31/27/28 contracts | Contract, CLI/test skeleton, RED tests for policy and generated artifacts | No, downstream tasks depend on exact enum and file names |
| 2 | Task 1 contract and tests | Classifier, Phase 31-first loader, derived bundle writer, security scan | No, same Python files |
| 3 | Task 2 verifier modes | Bazel/root/just wiring, validation metadata, full verification path | No, wiring validates the same files |
</dependency_graph>

<decision_coverage>
| Decision | Plan | Task | Full/Partial | Notes |
|----------|------|------|--------------|-------|
| D-01 | 32-01 | 1, 2 | Full | Contract and loader define a small Phase 32 adapter over Phase 31 outputs and referenced rows. |
| D-02 | 32-01 | 2, 3 | Full | `phase32_verify` runs Phase 31 quick first; loader reads manifest, rejections, receipts, then `consumed_upstream_row_refs`. |
| D-03 | 32-01 | 2 | Full | Primary source is Phase 31; source rows are followed only through accepted-final receipt refs. |
| D-04 | 32-01 | 2 | Full | Accepted receipts must load row-level status, criterion, artifact, and failure detail before classification. |
| D-05 | 32-01 | 2 | Full | Retained-code/readiness rows are consumed only through Phase 27/28 handoff artifacts. |
| D-06 | 32-01 | 1, 2 | Full | Canonical row schema includes all required orthogonal fields. |
| D-07 | 32-01 | 1, 2 | Full | `blocker_kind` enum is exactly `repair_item`, `exception_request`, `unresolved_decision_blocker`. |
| D-08 | 32-01 | 1, 2 | Full | `row_problem_kind` enum includes every specified cause. |
| D-09 | 32-01 | 1, 2 | Full | Unknown/unmapped inputs fail closed as critical unresolved decision blockers. |
| D-10 | 32-01 | 1, 2 | Full | Owner and next-action policy requires explicit `owner_ref` and `required_next_action`. |
| D-11 | 32-01 | 2 | Full | Phase 31 rejected/quarantined submissions become visible proof-ineligible rows. |
| D-12 | 32-01 | 2 | Full | Only accepted-final receipts can reach row-detail loading before proof eligibility is considered. |
| D-13 | 32-01 | 2 | Full | Non-final, unsafe, stale, redaction/source-ref failed, and secret-tainted rows map to `proof_eligibility: ineligible`. |
| D-14 | 32-01 | 2 | Full | Report and JSON distinguish triage visibility from cutover proof eligibility. |
| D-15 | 32-01 | 2 | Full | No Phase 31 edit unless an actual unclassifiable rejection is proven; unknown reasons still fail closed. |
| D-16 | 32-01 | 2 | Full | Emits the normalized handoff bundle and contract snapshots. |
| D-17 | 32-01 | 2 | Full | Phase 33 can consume exception, retained-code, residual-risk, and unresolved decision blocker rows without raw evidence. |
| D-18 | 32-01 | 2 | Full | Phase 34 can consume blocker state, exception refs, residual-risk refs, and proof eligibility without demotion approval. |
| D-19 | 32-01 | 2 | Full | Phase 35 can link blockers, exceptions, residual risks, evidence packets, readiness, and demotion decision refs by stable ids. |
| D-20 | 32-01 | 2 | Full | Derived views are generated from canonical register rows and include `row_id` backreferences. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - Define Phase 32 contract and failing policy tests</name>
  <files>tools/bazel/manifests/phase32_blocker_register_triage_contract.json, tools/bazel/phase32_blocker_register_triage.py, tools/bazel/phase32_blocker_register_triage_test.py</files>
  <read_first>
    - `tools/bazel/manifests/phase31_final_evidence_intake_contract.json`
    - `tools/bazel/phase31_final_evidence_intake.py`
    - `tools/bazel/phase31_final_evidence_intake_test.py`
    - `tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json`
    - `tools/bazel/manifests/phase28_final_readiness_packet_contract.json`
    - `.planning/phases/32-blocker-register-and-evidence-triage/32-CONTEXT.md`
    - `.planning/phases/32-blocker-register-and-evidence-triage/32-RESEARCH.md`
    - `.planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md`
  </read_first>
  <behavior>
    - Test 1: `--contract-only` accepts a contract with Phase 31/23/24/25/26/27/28 source refs, canonical row fields, enum values, policy map, generated artifact list, and verification commands.
    - Test 2: Contract validation fails if `blocker_kind`, `row_problem_kind`, `severity`, `proof_eligibility`, owner defaults, or generated artifacts omit any Phase 32 required value.
    - Test 3: Pure policy tests prove unknown signals classify as `row_problem_kind: unknown_unclassified`, `blocker_kind: unresolved_decision_blocker`, `severity: critical`, and `proof_eligibility: ineligible`.
    - Test 4: Pure policy tests prove quick/default finality reasons classify as `non_final_placeholder` and remain proof-ineligible.
    - Test 5: Pure policy tests prove smoke fixture and local dry-run signals classify as `smoke_fixture` and `local_dry_run`, use `blocker_kind: repair_item`, and remain `proof_eligibility: ineligible`.
  </behavior>
  <action>Create the Phase 32 contract with `id: phase32_blocker_register_triage_contract`, `phase: 32-blocker-register-and-evidence-triage`, `phase_lifecycle_id: 32-2026-07-03T14-13-51`, `artifact_name: phase32-blocker-register-triage`, `output_root: build/ci-evidence/phase32`, and `requirement_ids: [TRIAGE-01, TRIAGE-02, TRIAGE-03]`. Add `source_contracts` for Phase 31, Phase 23, Phase 24, Phase 25, Phase 26, Phase 27, and Phase 28. Add `canonical_row_schema.required_fields` with `row_id`, `source_stream`, `source_ref`, `requirement_ids`, `affected_gate`, `row_problem_kind`, `blocker_kind`, `severity`, `owner_ref`, `required_next_action`, `decision_impact`, `proof_eligibility`, and `evidence_refs`. Add exact enum lists from the `<interfaces>` block. Add `decision_impact` values `repair_required_before_cutover`, `exception_decision_required`, `residual_risk_decision_required`, `retained_code_decision_required`, `final_readiness_blocked`, `demotion_decision_required`, and `cutover_verdict_blocked`. Add `generated_artifacts` exactly as listed in context. Create the Python script with working `--contract-only`, a `VerificationError`, JSON boundary helpers, and pure policy functions with enough behavior for the RED tests to fail before implementation and pass after contract logic is present. Create explicit tests for `non_final_placeholder`, `smoke_fixture`, `local_dry_run`, `prose_attestation`, `row_only_submission`, and `lifecycle_mismatch` signals, each asserting `proof_eligibility: ineligible` and an appropriate non-passing `blocker_kind`. Create the unittest file using standard-library `unittest`, temp roots, and explicit `# Arrange`, `# Act`, `# Assert` comments for non-trivial tests. Do not add new dependencies.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py</automated>
    <automated>python3 tools/bazel/phase32_blocker_register_triage.py --contract-only</automated>
    <automated>python3 tools/bazel/phase32_blocker_register_triage_test.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `test -f tools/bazel/phase32_blocker_register_triage.py`
    - `test -f tools/bazel/phase32_blocker_register_triage_test.py`
    - `rg -n '"repair_item"|"exception_request"|"unresolved_decision_blocker"' tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `rg -n '"unknown_unclassified"|"secret_tainted"|"local_dry_run"|"row_only_submission"' tools/bazel/manifests/phase32_blocker_register_triage_contract.json`
    - `python3 tools/bazel/phase32_blocker_register_triage.py --contract-only` exits 0.
  </acceptance_criteria>
  <done>Wave 0 files exist, the contract defines all Phase 32 enums/artifacts/source refs, and focused tests cover fail-closed classification before broader file I/O is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 31-first aggregation, classification, and handoff bundle</name>
  <files>tools/bazel/phase32_blocker_register_triage.py, tools/bazel/phase32_blocker_register_triage_test.py, tools/bazel/manifests/phase32_blocker_register_triage_contract.json</files>
  <read_first>
    - `build/ci-evidence/phase31/final-intake-manifest.json`
    - `build/ci-evidence/phase31/rejected-submissions.json`
    - `tools/bazel/phase31_final_evidence_intake.py`
    - `tools/bazel/phase27_retained_code_acceptance_decisions.py`
    - `tools/bazel/phase28_final_readiness_packet.py`
    - `.planning/milestones/v1.2-phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-SUMMARY.md`
    - `.planning/milestones/v1.2-phases/28-final-readiness-packet-and-demotion-gate/28-01-SUMMARY.md`
    - `.planning/milestones/v1.2-phases/29-upstream-evidence-flow-closure/29-01-SUMMARY.md`
  </read_first>
  <behavior>
    - Test 1: `--quick --phase31-output-dir build/ci-evidence/phase31 --phase27-output-dir build/ci-evidence/phase27 --phase28-output-dir build/ci-evidence/phase28 --output-dir build/ci-evidence/phase32` writes all Phase 32 generated artifacts.
    - Test 2: Loader reads Phase 31 manifest and rejected submissions before following any accepted receipt `consumed_upstream_row_refs`; a receipt-only fixture without row detail produces a fail-closed blocker rather than a proof claim.
    - Test 3: Phase 31 rejected/quarantined rows produce canonical rows with stable `row_id`, owner/action/severity, `row_problem_kind: non_final_placeholder` for quick/default reasons, and `proof_eligibility: ineligible`.
    - Test 3b: Phase 31 rejection reasons containing smoke fixture, local-only dry run, prose-only attestation, upstream-row-only submission, or stale lifecycle wording produce `row_problem_kind` values `smoke_fixture`, `local_dry_run`, `prose_attestation`, `row_only_submission`, or `lifecycle_mismatch` and remain `proof_eligibility: ineligible`.
    - Test 4: Accepted-final receipt refs load row-level detail and classify failed, missing, stale, malformed, redaction-failed, source-ref-failed, unsafe-ref, secret-tainted, exception-requested, and unknown signals into the required taxonomy.
    - Test 5: Phase 27 residual-risk and exception rows plus Phase 28 readiness blockers are included only as existing handoff/readiness inputs; Phase 32 does not create approved exception, approved retained-code, readiness approval, demotion approval, or cutover verdict fields.
    - Test 6: `decision-impact-index.json`, `exception-request-register.json`, `residual-risk-request-register.json`, `downstream-handoff-manifest.json`, and `redacted-blocker-register-report.md` are generated from canonical `blocker-register.json` rows and every derived row has a canonical `row_id` backreference.
    - Test 7: `--security-only` rejects generated artifacts containing private key, token, certificate private material, service payload, raw crash dump, raw release log, TLS keylog, Wi-Fi credential, `demotion_allowed: true`, `reference demotion approved`, `final_readiness_status: unblocked`, or `cutover verdict approved`.
  </behavior>
  <action>Implement `--quick`, `--phase31-output-dir`, `--phase27-output-dir`, `--phase28-output-dir`, `--output-dir`, `--security-only`, and `--wiring-only` in `tools/bazel/phase32_blocker_register_triage.py`. Keep I/O in thin CLI functions and put policy logic in pure functions such as `classify_problem_kind(...)`, `blocker_policy_for(...)`, `build_blocker_row(...)`, `build_derived_views(...)`, and `run_security_scan(...)`. In `--quick`, load `final-intake-manifest.json`, then `rejected-submissions.json`, then accepted receipts listed by `receipt_refs`, then each accepted receipt's `consumed_upstream_row_refs`; do not scan Phase 23-26 outputs directly except through those accepted receipt refs. Also load Phase 27 `residual-risk-register.json`, `exception-decision-register.json`, `phase28-handoff-manifest.json` when present under `--phase27-output-dir`, and Phase 28 `blocker-summary.json`, `exception-residual-risk-summary.json`, `reference-demotion-authorization-record.json` when present under `--phase28-output-dir`. Missing optional Phase 27/28 quick inputs should become explicit `missing` blocker rows with owner/action/severity, not silent skips. Write `blocker-register.json` as the canonical source of truth; generate all other JSON/Markdown views only from canonical rows. Use stable row ids with source prefixes such as `phase31-rejection-<stream>-<sha12>`, `phase31-receipt-row-<stream>-<sha12>`, `phase27-exception-<sha12>`, `phase27-residual-risk-<sha12>`, and `phase28-readiness-<sha12>`. Copy contract snapshots listed in the contract. Ensure every blocker row has non-empty `owner_ref`, `required_next_action`, `decision_impact`, and `evidence_refs`, and ensure unknown/unmapped signals always fail closed per D-09.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py</automated>
    <automated>python3 tools/bazel/phase32_blocker_register_triage_test.py -q</automated>
    <automated>python3 tools/bazel/phase32_blocker_register_triage.py --contract-only</automated>
    <automated>python3 tools/bazel/phase32_blocker_register_triage.py --security-only</automated>
    <automated>python3 tools/bazel/phase31_final_evidence_intake.py --quick --output-dir build/ci-evidence/phase31</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28</automated>
    <automated>python3 tools/bazel/phase32_blocker_register_triage.py --quick --phase31-output-dir build/ci-evidence/phase31 --phase27-output-dir build/ci-evidence/phase27 --phase28-output-dir build/ci-evidence/phase28 --output-dir build/ci-evidence/phase32</automated>
  </verify>
  <acceptance_criteria>
    - `test -f build/ci-evidence/phase32/blocker-register.json`
    - `test -f build/ci-evidence/phase32/downstream-handoff-manifest.json`
    - `test -f build/ci-evidence/phase32/redacted-blocker-register-report.md`
    - `python3 - <<'PY'\nimport json\nrows=json.load(open('build/ci-evidence/phase32/blocker-register.json'))['rows']\nrequired={'row_id','source_stream','source_ref','requirement_ids','affected_gate','row_problem_kind','blocker_kind','severity','owner_ref','required_next_action','decision_impact','proof_eligibility','evidence_refs'}\nassert rows and all(required <= set(row) for row in rows)\nassert all(row['proof_eligibility'] == 'ineligible' for row in rows if row['row_problem_kind'] != 'unknown_unclassified' or row['blocker_kind'] == 'unresolved_decision_blocker')\nPY`
    - `python3 - <<'PY'\nimport json\nregister={row['row_id'] for row in json.load(open('build/ci-evidence/phase32/blocker-register.json'))['rows']}\nfor path in ['exception-request-register.json','residual-risk-request-register.json']:\n    data=json.load(open('build/ci-evidence/phase32/'+path))\n    assert all(row['row_id'] in register for row in data.get('rows', []))\nPY`
    - `! rg -n 'demotion_allowed\"?\\s*:\\s*true|reference demotion approved|final_readiness_status\"?\\s*:\\s*\"unblocked\"|cutover verdict approved' build/ci-evidence/phase32`
  </acceptance_criteria>
  <done>Phase 32 generates a secret-safe canonical register and derived handoff bundle from Phase 31/27/28 inputs, with fail-closed taxonomy and no approval semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 32 into Bazel, just, and Nyquist validation</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, .planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md</files>
  <read_first>
    - `tools/bazel/BUILD.bazel`
    - `BUILD.bazel`
    - `tools/bazel/rust_workflow.sh`
    - `justfile`
    - `.planning/phases/31-final-evidence-intake/31-01-PLAN.md`
    - `.planning/phases/31-final-evidence-intake/31-VERIFICATION.md`
    - `.planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md`
  </read_first>
  <action>Add root `filegroup(name = "phase32_blocker_register_triage_docs", srcs = [".planning/phases/32-blocker-register-and-evidence-triage/32-CONTEXT.md", ".planning/phases/32-blocker-register-and-evidence-triage/32-RESEARCH.md", ".planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md", ".planning/phases/32-blocker-register-and-evidence-triage/32-01-PLAN.md"])`. Add root aliases `phase32_verify` and `phase32_verify_tests` pointing to `//tools/bazel:phase32_verify` and `//tools/bazel:phase32_verify_tests`. Add `tools/bazel/BUILD.bazel` `filegroup(name = "phase32_source_ref_manifests", srcs = ["manifests/phase23_simulator_evidence_execution_contract.json", "manifests/phase24_hardware_media_safety_evidence_execution_contract.json", "manifests/phase25_live_service_evidence_execution_contract.json", "manifests/phase26_release_signing_upstream_evidence_contract.json", "manifests/phase27_retained_code_acceptance_decisions_contract.json", "manifests/phase28_final_readiness_packet_contract.json", "manifests/phase31_final_evidence_intake_contract.json", "manifests/phase32_blocker_register_triage_contract.json"])`. Add `shell_binary(name = "phase32_verify", src = "rust_workflow.sh", data = ["phase31_final_evidence_intake.py", "phase26_release_signing_upstream_evidence.py", "phase27_retained_code_acceptance_decisions.py", "phase28_final_readiness_packet.py", "phase32_blocker_register_triage.py", "manifests/phase32_blocker_register_triage_contract.json", ":phase32_source_ref_manifests", "//:phase32_blocker_register_triage_docs"])` and `shell_binary(name = "phase32_verify_tests", src = "rust_workflow.sh", data = ["phase32_blocker_register_triage.py", "phase32_blocker_register_triage_test.py", "manifests/phase32_blocker_register_triage_contract.json", ":phase32_source_ref_manifests"])`. Add `rust_workflow.sh` case `phase32_verify_tests)` running `python3 tools/bazel/phase32_blocker_register_triage_test.py`, and case `phase32_verify)` running Phase 31 quick, Phase 26 quick, Phase 27 quick, Phase 28 quick, `python3 tools/bazel/phase32_blocker_register_triage.py --wiring-only`, and Phase 32 quick with exact flags from Task 2. Add `justfile` recipe `phase32-verify` that runs `bazel run //tools/bazel:phase32_verify_tests` before `bazel run //tools/bazel:phase32_verify`. Update `32-VALIDATION.md` only after evidence passes: set `nyquist_compliant: true`, `wave_0_complete: true`, mark each task row `passed`, and record the executed commands without adding standalone markdown body separators.</action>
  <verify>
    <automated>python3 tools/bazel/phase32_blocker_register_triage.py --wiring-only</automated>
    <automated>bazel run //tools/bazel:phase32_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase32_verify</automated>
    <automated>just phase32-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'phase32_blocker_register_triage_docs|phase32_verify|phase32_verify_tests' BUILD.bazel`
    - `rg -n 'phase32_source_ref_manifests|phase32_blocker_register_triage.py|phase32_blocker_register_triage_test.py' tools/bazel/BUILD.bazel`
    - `rg -n 'phase32_verify\\)|phase32_verify_tests\\)' tools/bazel/rust_workflow.sh`
    - `rg -n '^phase32-verify:' justfile`
    - `rg -n 'nyquist_compliant: true|wave_0_complete: true' .planning/phases/32-blocker-register-and-evidence-triage/32-VALIDATION.md`
    - `just phase32-verify` exits 0.
  </acceptance_criteria>
  <done>Phase 32 has direct Python, Bazel, and `just` verification paths, validation metadata reflects passing Wave 0 evidence, and whitespace diff checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 31 generated evidence -> Phase 32 loader | Local JSON artifacts and receipt refs cross into Phase 32 classification and must be treated as untrusted until parsed and classified. |
| Accepted receipt refs -> referenced source rows | `consumed_upstream_row_refs` can point at row details; Phase 32 must enforce repo-relative/external-ref policy and never load arbitrary secret-bearing payloads. |
| Canonical register -> derived handoff/report artifacts | Derived queues and Markdown can overclaim, drift, or leak data unless generated only from sanitized canonical rows. |
| Phase 32 outputs -> Phase 33-35 consumers | Downstream phases consume Phase 32 as input, so ambiguous fields can imply approvals or verdicts that Phase 32 is not authorized to make. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | S/E | `proof_eligibility` classifier | mitigate | Block if any rejected, quarantined, quick/default, smoke, local-dry-run, prose, row-only, stale, redaction-failed, source-ref-failed, unsafe-ref, or secret-tainted row has `proof_eligibility` other than `ineligible`; cover in Task 2 tests and `--security-only`. |
| T-32-02 | I | `redacted-blocker-register-report.md` and JSON outputs | mitigate | Generate reports only from canonical sanitized rows; scan generated outputs for private key, token, certificate private material, service payload, raw crash dump, raw release log, TLS keylog, Wi-Fi credential, and signing payload markers in `--security-only`. |
| T-32-03 | T/E | `classify_problem_kind(...)` policy map | mitigate | Unknown or unmapped source signals must emit `unknown_unclassified`, `unresolved_decision_blocker`, `critical`, and `ineligible`; tests must include an unknown status/reason fixture. |
| T-32-04 | E/R | Phase 32 handoff fields consumed by Phase 33-35 | mitigate | Security scan rejects `demotion_allowed: true`, `reference demotion approved`, `final_readiness_status: unblocked`, and cutover verdict approval wording; contract must state Phase 32 does not approve exceptions, retained code, readiness, demotion, or cutover. |
| T-32-05 | T/R | Derived exception/residual-risk/decision-impact views | mitigate | Build derived artifacts from `blocker-register.json` only and require every derived row to carry a canonical `row_id`; Task 2 tests compare derived ids against the canonical register. |
| T-32-06 | T/I | Receipt/source ref loading | mitigate | Require repo-relative paths under allowed evidence roots or `external://phaseXX/` refs; reject traversal, absolute paths, and symlink/output-root escapes before reading or writing generated artifacts. |

High severity threats T-32-01 through T-32-04 are blocking: the executor must not mark the plan complete if their automated tests or `--security-only` checks fail.
</threat_model>

<verification>
Overall phase checks after Task 3:

```bash
python3 -m py_compile tools/bazel/phase32_blocker_register_triage.py tools/bazel/phase32_blocker_register_triage_test.py
python3 tools/bazel/phase32_blocker_register_triage_test.py -q
python3 tools/bazel/phase32_blocker_register_triage.py --contract-only
python3 tools/bazel/phase32_blocker_register_triage.py --security-only
python3 tools/bazel/phase32_blocker_register_triage.py --wiring-only
python3 tools/bazel/phase31_final_evidence_intake.py --quick --output-dir build/ci-evidence/phase31
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26
python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27
python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28
python3 tools/bazel/phase32_blocker_register_triage.py --quick --phase31-output-dir build/ci-evidence/phase31 --phase27-output-dir build/ci-evidence/phase27 --phase28-output-dir build/ci-evidence/phase28 --output-dir build/ci-evidence/phase32
bazel run //tools/bazel:phase32_verify_tests
bazel run //tools/bazel:phase32_verify
just phase32-verify
git diff --check
```

Before any git commit in this Rust repository, run the repo-required sequence:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- `tools/bazel/manifests/phase32_blocker_register_triage_contract.json` documents the Phase 32 source refs, taxonomy, policy map, generated artifacts, owner defaults, decision-impact values, and verification commands.
- `tools/bazel/phase32_blocker_register_triage.py` reads Phase 31 manifest/rejections/receipts before source-row refs, classifies blockers fail-closed, writes the Phase 32 handoff bundle, and supports `--contract-only`, `--security-only`, `--wiring-only`, and `--quick`.
- `tools/bazel/phase32_blocker_register_triage_test.py` proves TRIAGE-01, TRIAGE-02, and TRIAGE-03 with focused tests.
- `build/ci-evidence/phase32/blocker-register.json` is the canonical register and all derived Phase 32 artifacts reference canonical `row_id` values.
- Phase 32 generated artifacts contain no secret-bearing data and no text or fields implying approvals, readiness unblocking, demotion authorization, or cutover verdict.
- `just phase32-verify` passes after running Bazel tests before the verifier.
</success_criteria>

<output>
After completion, create `.planning/phases/32-blocker-register-and-evidence-triage/32-01-SUMMARY.md`.
</output>
