---
phase: 40-file-length-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .bright-builds-rules-checks.tsv
  - doc/file_length_policy.md
  - tools/bazel/phase40_file_length_policy.py
  - tools/bazel/phase40_file_length_policy_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - justfile
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-09, D-12]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-27T17:17:18.946Z
must_haves:
  truths:
    - "The managed checker is green with exactly 933 classified exact-path exceptions: 838 frozen permanent provenance/declarative paths and 95 shrink-only temporary owned paths."
    - "Only the three locked owned paths may ever become permanent owned exceptions, and terminal mode requires their exact union with the frozen 838-path set."
    - "One repository command validates ledger syntax, policy, managed-checker output, and terminal reconciliation without modifying the managed checker or CI workflow."
  artifacts:
    - path: .bright-builds-rules-checks.tsv
      provides: "Canonical sorted exact-path exception ledger"
    - path: tools/bazel/phase40_file_length_policy.py
      provides: "Shrink-only and terminal policy verifier"
    - path: tools/bazel/phase40_file_length_policy_test.py
      provides: "Malformed, growth, reclassification, and exact-terminal regressions"
    - path: doc/file_length_policy.md
      provides: "Reason-prefix, deletion-test, and campaign evidence contract"
  key_links:
    - from: justfile
      to: tools/bazel/phase40_file_length_policy.py
      via: "phase40-verify recipe"
      pattern: "phase40-verify"
    - from: tools/bazel/phase40_file_length_policy.py
      to: .bright-builds-rules-checks.tsv
      via: "strict TSV parsing and exact-set validation"
      pattern: "bright-builds-rules-checks"
---

<objective>
Establish the immutable baseline and shrink-only policy gate for the full refactoring campaign.

Purpose: Make the checker green immediately while preventing temporary debt growth or unauthorized permanence.
Output: Canonical 933-row ledger, tested policy verifier, policy documentation, Bazel labels, and `just phase40-verify`.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/40-file-length-refactoring/40-CONTEXT.md
@.planning/phases/40-file-length-refactoring/40-RESEARCH.md
@.planning/phases/40-file-length-refactoring/40-VALIDATION.md
@scripts/bright-builds-check.ts
@justfile
</context>

<decision-coverage>
| Decisions | Task | Coverage |
| --- | --- | --- |
| D-01, D-02 | 1 | Seed the only checker-consumed ledger without editing managed checker/workflow code. |
| D-03, D-04 | 2 | Enforce shrink-only temporary membership, the three locked conversions, and exact terminal set. |
| D-09, D-12 | 2 | Provide the authoritative serial campaign gate and evidence vocabulary. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed the exact baseline ledger</name>
  <read-first>
    <file>scripts/bright-builds-check.ts</file>
    <file>.planning/phases/40-file-length-refactoring/40-RESEARCH.md</file>
    <file>.planning/phases/40-file-length-refactoring/40-CONTEXT.md</file>
  </read-first>
  <files>.bright-builds-rules-checks.tsv, doc/file_length_policy.md</files>
  <behavior>
    - "The current 933 findings partition into exactly 838 approved permanent provenance/declarative paths and the exact 95 owned campaign paths."
    - "Rows are sorted, unique, three-field TSV entries with approved, non-empty reason prefixes."
  </behavior>
  <action>Per D-01 and D-02, capture the live managed-checker findings mechanically, classify only against the researched exact path categories, and commit the sorted TSV as the sole authority. Include `src/connect/planner.cpp` in the six-path network/media campaign even though RESEARCH names its conversion in prose. Document permanent reason classes, temporary campaign IDs, deletion-test evidence, atomic removal, and the prohibition on editing `scripts/bright-builds-check.ts` or `.github/workflows/bright-builds-checks.yml`. Do not introduce a second manifest or merge-base dependency.</action>
  <verify>
    <automated>bun scripts/bright-builds-check.ts file-lengths</automated>
    <expected>`SUMMARY file-lengths ... exceptions=933 findings=0`.</expected>
  </verify>
  <acceptance-criteria>
    - "TSV has exactly 933 unique sorted paths: 838 permanent and 95 temporary."
    - "Managed checker and workflow remain byte-for-byte untouched."
    - "The policy doc names the exact three allowed permanent-owned conversions and campaign evidence fields from D-12."
  </acceptance-criteria>
  <done>The managed checker is green through the canonical exact-path ledger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the fail-closed Phase 40 policy gate</name>
  <read-first>
    <file>.bright-builds-rules-checks.tsv</file>
    <file>tools/bazel/BUILD.bazel</file>
    <file>BUILD.bazel</file>
    <file>justfile</file>
  </read-first>
  <files>tools/bazel/phase40_file_length_policy.py, tools/bazel/phase40_file_length_policy_test.py, tools/bazel/BUILD.bazel, BUILD.bazel, justfile</files>
  <behavior>
    - "Malformed rows, duplicates, unapproved reasons, temporary-set growth, provenance-to-temporary conversion, and unauthorized owned permanence fail."
    - "Terminal mode accepts only the exact frozen 838 plus three locked owned paths and no temporary reasons."
  </behavior>
  <action>Write one-concern Arrange/Act/Assert tests first, then implement a standard-library boundary parser and pure set policy per D-03/D-04. The verifier may embed the locked original path sets as policy constants but must treat the TSV as the only active exception authority. Add Bazel test/binary labels, root aliases, and `just phase40-verify`, which runs policy tests, policy validation, and `bun scripts/bright-builds-check.ts all`; support an explicit terminal flag. Keep all new files below 629 lines.</action>
  <verify>
    <automated>just phase40-verify</automated>
    <expected>Policy tests pass and managed checks report zero findings with the initial 933 exceptions.</expected>
  </verify>
  <acceptance-criteria>
    - "Negative tests cover every five-point policy assertion in RESEARCH."
    - "`just phase40-verify` is the single authoritative campaign/wave gate."
    - "Terminal mode is present and intentionally fails against the initial temporary baseline."
  </acceptance-criteria>
  <done>Phase 40 has a tested fail-closed baseline and terminal reconciliation command.</done>
</task>

</tasks>

<threat-model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-01 | Tampering | Exception ledger | mitigate | Exact path sets, strict TSV parsing, unique sorted rows, and terminal exact-set equality. |
| T-40-02 | Elevation | Permanent reason classification | mitigate | Allow owned permanence only for the three D-03 paths with deletion-test reasons. |
</threat-model>

<verification>
Run `just phase40-verify` and `git diff --check`; confirm the managed checker/workflow have no diff.
</verification>

<success-criteria>
The initial state is exactly 838 permanent plus 95 temporary exceptions, zero findings, and a tested terminal mode that cannot pass until all campaign work is complete.
</success-criteria>

<output>
After completion, create `.planning/phases/40-file-length-refactoring/40-01-SUMMARY.md`.
</output>
