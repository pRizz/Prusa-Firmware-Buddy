---
phase: 40-file-length-refactoring
plan: 16
type: execute
wave: 16
depends_on: [40-15]
files_modified:
  - tools/bazel/phase33_maintainer_decision_inputs_test.py
  - tools/bazel/phase33_maintainer_decision_inputs_cases_test.py
  - tools/bazel/phase33_maintainer_decision_inputs_failure_test.py
  - tools/bazel/phase33_maintainer_decision_inputs_security_test.py
autonomous: true
gap_closure: true
requirements: [D-05, D-06, D-08, D-11, D-12, D-13]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-28T03:30:00Z
must_haves:
  truths:
    - "Phase 33 tests are ordinary, reviewable Python mixins rather than executable source stored in strings."
    - "The stable Phase 33 public entrypoint uses ordinary imports and inheritance while preserving its exact 40 test-method inventory and Bazel labels."
    - "All four touched files are below 629 physical lines and contain no source-reconstruction seam."
  artifacts:
    - path: tools/bazel/phase33_maintainer_decision_inputs_cases_test.py
      provides: "Ordinary Phase 33 contract and accepted-path mixin"
      contains: "class "
    - path: tools/bazel/phase33_maintainer_decision_inputs_failure_test.py
      provides: "Ordinary Phase 33 failure-domain mixin"
      contains: "class "
    - path: tools/bazel/phase33_maintainer_decision_inputs_security_test.py
      provides: "Ordinary Phase 33 security mixin"
      contains: "class "
  key_links:
    - from: tools/bazel/phase33_maintainer_decision_inputs_test.py
      to: tools/bazel/phase33_maintainer_decision_inputs_cases_test.py
      via: "ordinary mixin import and inheritance"
      pattern: "from phase33_maintainer_decision_inputs_cases_test import"
---

<objective>
Close the Phase 33 portion of the Phase 40 architecture gap by materializing the three encoded test payloads as ordinary phase-local mixins.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/40-file-length-refactoring/40-VERIFICATION.md
@.planning/phases/40-file-length-refactoring/40-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the Phase 33 decision-input suites</name>
  <files>tools/bazel/phase33_maintainer_decision_inputs_test.py, tools/bazel/phase33_maintainer_decision_inputs_cases_test.py, tools/bazel/phase33_maintainer_decision_inputs_failure_test.py, tools/bazel/phase33_maintainer_decision_inputs_security_test.py</files>
  <action>Before editing, load the public entrypoint with `unittest`, recursively enumerate every `_testMethodName`, assert exactly 40 unique names, and save the sorted names to a temporary baseline outside the repository. Convert each `TEST_METHODS` payload byte-for-byte into an ordinary named behavior mixin in its existing phase-local file, adding explicit imports for names used by its methods. Keep shared fixture construction in `Phase33MaintainerDecisionInputsTest`, which must inherit the three mixins plus `unittest.TestCase` through ordinary imports. Preserve every test body, assertion, fixture, direct-script behavior, and the legitimate `importlib.util` production-verifier loader. Remove `TEST_METHODS`, `textwrap`, `importlib.import_module`, and every `exec()` assembly path. After editing, enumerate the same public method names and fail unless the sorted set matches the baseline exactly, remains 40 names, and has no duplicates. The deletion test is explicit: removing a mixin removes its named behavior cluster from the public suite.</action>
  <verify>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs_test.py &amp;&amp; bazel run //tools/bazel:phase33_verify_tests &amp;&amp; bazel run //tools/bazel:phase33_verify</automated>
    <expected>All 40 tests and both unchanged Phase 33 labels pass.</expected>
  </verify>
  <done>Phase 33 has exact inventory parity through ordinary mixins and no reconstructed source.</done>
</task>

</tasks>

<verification>
1. Run the exact before/after method-name set comparison and duplicate assertion described above.
2. Run `python3 -m py_compile` on the four files.
3. Fail if a scoped scan finds `TEST_METHODS`, `TEST_CLASSES`, `textwrap`, `importlib.import_module`, or `exec(` in the four files; allow only the fixture's legitimate `importlib.util` loader.
4. Use an executable physical-line scan to require each file to be below 629 lines.
5. Run `just phase33-verify`, `just phase38-verify`, `just phase40-verify --terminal`, `bun scripts/bright-builds-check.ts all`, and `git diff --check`.
6. Review the diff for test-body, public-entrypoint, label, runfile, or unrelated changes.
7. Before committing, run in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
</verification>

<success-criteria>
The exact 40 Phase 33 test names execute through ordinary imports and mixins, all four files are below 629 lines, forbidden reconstruction patterns are absent, and focused/downstream/terminal gates pass.
</success-criteria>

<output>
Create `.planning/phases/40-file-length-refactoring/40-16-SUMMARY.md`.
</output>
