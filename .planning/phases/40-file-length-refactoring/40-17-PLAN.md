---
phase: 40-file-length-refactoring
plan: 17
type: execute
wave: 17
depends_on: [40-16]
files_modified:
  - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
  - tools/bazel/phase34_final_readiness_cases_test.py
  - tools/bazel/phase34_final_readiness_demotion_failure_test.py
  - tools/bazel/phase34_final_readiness_source_failure_test.py
  - tools/bazel/phase34_publication_state_test.py
autonomous: true
gap_closure: true
requirements: [D-05, D-06, D-08, D-11, D-12, D-13]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-28T03:30:00Z
must_haves:
  truths:
    - "Phase 34 tests are ordinary behavior/failure mixins and classes, not encoded executable strings."
    - "The stable public entrypoint preserves its exact 68 test-method inventory, direct execution, and Bazel labels."
    - "All five touched files are below 629 lines and contain no dynamic source reconstruction."
  artifacts:
    - path: tools/bazel/phase34_final_readiness_cases_test.py
      provides: "Ordinary Phase 34 readiness cases mixin"
      contains: "class "
    - path: tools/bazel/phase34_publication_state_test.py
      provides: "Ordinary publication-state security test class"
      contains: "class Phase34PublicationStateSecurityTests"
  key_links:
    - from: tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
      to: tools/bazel/phase34_publication_state_test.py
      via: "ordinary class import for unittest discovery"
      pattern: "from phase34_publication_state_test import"
---

<objective>
Close the Phase 34 portion of the Phase 40 architecture gap by materializing readiness, failure, and publication tests as ordinary phase-local Python modules.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/40-file-length-refactoring/40-16-SUMMARY.md
@.planning/phases/40-file-length-refactoring/40-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the Phase 34 readiness and publication suites</name>
  <files>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py, tools/bazel/phase34_final_readiness_cases_test.py, tools/bazel/phase34_final_readiness_demotion_failure_test.py, tools/bazel/phase34_final_readiness_source_failure_test.py, tools/bazel/phase34_publication_state_test.py</files>
  <action>Before editing, load the public entrypoint with `unittest`, recursively enumerate every `_testMethodName`, assert exactly 68 unique names, and save the sorted names to a temporary baseline outside the repository. Convert the three `TEST_METHODS` payloads into ordinary cases, demotion/failure, and source-failure mixins using the existing `phase34_test_support.py` boundary. Materialize `Phase34PublicationStateSecurityTests` directly in its existing module. Replace the public entrypoint's dynamic loop/global execution with explicit imports, mixin composition with `Phase34TestSupport`, and an ordinary imported publication-state class discoverable by `unittest`. Preserve every test body, assertion, failure replacement, marker behavior, direct-script behavior, and runfile contract. After editing, fail unless the sorted public method-name set exactly matches the saved baseline, remains 68 names, and contains no duplicates. Do not enlarge support modules or create cross-phase infrastructure.</action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py &amp;&amp; bazel run //tools/bazel:phase34_verify_tests &amp;&amp; bazel run //tools/bazel:phase34_verify</automated>
    <expected>All 68 tests and both unchanged Phase 34 labels pass.</expected>
  </verify>
  <done>Phase 34 has exact inventory parity through ordinary modules and no reconstructed source.</done>
</task>

</tasks>

<verification>
1. Run the exact before/after method-name set comparison and duplicate assertion.
2. Run `python3 -m py_compile` on the five files.
3. Fail if a scoped scan finds `TEST_METHODS`, `TEST_CLASSES`, `textwrap`, `importlib.import_module`, or `exec(`.
4. Require every touched file to have fewer than 629 physical lines.
5. Run `just phase34-verify`, `just phase38-verify`, `just phase40-verify --terminal`, `bun scripts/bright-builds-check.ts all`, and `git diff --check`.
6. Review the diff for test-body, public-entrypoint, label, runfile, or unrelated changes.
7. Before committing, run in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
</verification>

<success-criteria>
The exact 68 Phase 34 test names execute through ordinary imports/classes, all five files are below 629 lines, forbidden reconstruction patterns are absent, and focused/downstream/terminal gates pass.
</success-criteria>

<output>
Create `.planning/phases/40-file-length-refactoring/40-17-SUMMARY.md`.
</output>
