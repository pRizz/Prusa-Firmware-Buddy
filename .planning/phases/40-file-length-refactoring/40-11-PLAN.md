---
phase: 40-file-length-refactoring
plan: 11
type: execute
wave: 11
depends_on: [40-10]
files_modified:
  - .bright-builds-rules-checks.tsv
  - src/persistent_stores/journal/backend.cpp
  - src/persistent_stores/journal/backend_state.cpp
  - src/persistent_stores/journal/CMakeLists.txt
  - src/persistent_stores/store_instances/config_store/store_definition.cpp
  - src/persistent_stores/store_instances/config_store/store_migrations.cpp
  - src/persistent_stores/store_instances/config_store/CMakeLists.txt
autonomous: true
requirements: [D-05, D-07, D-08, D-09, D-11, D-12, D-14, D-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-27T17:17:18.946Z
must_haves:
  truths:
    - "Journal state policy is separated from flash effects without changing stored layout, hashes, recovery, migrations, or defaults."
    - "Both original storage files and new private sources are below 629 lines with two rows removed."
  artifacts:
    - path: src/persistent_stores/journal/backend_state.cpp
      provides: "Pure journal bank/transaction state policy"
    - path: src/persistent_stores/store_instances/config_store/store_migrations.cpp
      provides: "Config migration implementation behind stable definition surface"
  key_links:
    - from: src/persistent_stores/journal/CMakeLists.txt
      to: src/persistent_stores/journal/backend_state.cpp
      via: "existing storage target"
      pattern: "backend_state"
---

<objective>
Refactor journal and config-store implementation while preserving every persisted byte-level contract.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/40-file-length-refactoring/40-10-SUMMARY.md
@tests/unit/persistent_stores/CMakeLists.txt
</context>

<decision-coverage>
| Decisions | Task | Coverage |
| --- | --- | --- |
| D-05, D-07, D-08 | 1-2 | State policy/effect boundary, same target, deletion-test seams, sub-629 files. |
| D-09, D-11, D-12, D-14, D-15 | 1-2 | Ordered persistence campaign, atomic rows, generated/build/simulator evidence. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate journal state policy from storage effects</name>
  <read-first><file>src/persistent_stores/journal/backend.cpp</file><file>tests/unit/persistent_stores/EEPROM_journal_test.cpp</file><file>src/persistent_stores/journal/CMakeLists.txt</file></read-first>
  <files>src/persistent_stores/journal/backend.cpp, src/persistent_stores/journal/backend_state.cpp, src/persistent_stores/journal/CMakeLists.txt, .bright-builds-rules-checks.tsv</files>
  <behavior>
    - "Bank selection, transactions, CRC/recovery, flash failures, and journal API behavior remain exact."
  </behavior>
  <action>Per D-05, D-07, D-08, D-11, D-12, and D-14, extract only deterministic bank/transaction state policy; retain EEPROM/flash I/O, synchronization, public symbols, and initialization in `backend.cpp`. Use Plan 08 recovery characterization, preserve target ordering, and remove the row after host tests, generated checks, build, and simulator parity pass.</action>
  <verify><automated>set -euo pipefail; BUDDY_BAZEL_EXECUTE_REFERENCE=1 just test; binary=build-tests/tests/unit/persistent_stores/eeprom_unit_tests; test -x "$binary"; suite_dir="$(dirname "$binary")"; suite_name="$(basename "$binary")"; test_list="$(cd "$suite_dir" &amp;&amp; "./$suite_name" --list-tests)"; printf '%s\n' "$test_list"; printf '%s\n' "$test_list" | grep -Eq '[1-9][0-9]* test cases?'; (cd "$suite_dir" &amp;&amp; "./$suite_name")</automated></verify>
  <acceptance-criteria>
    - "Original/new files are sub-threshold and journal bytes/recovery compare equal."
    - "One row is removed."
  </acceptance-criteria>
  <done>Journal policy is deep and effects remain stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract config-store migration implementation</name>
  <read-first><file>src/persistent_stores/store_instances/config_store/store_definition.cpp</file><file>src/persistent_stores/store_instances/config_store/store_definition.hpp</file><file>src/persistent_stores/store_instances/config_store/CMakeLists.txt</file></read-first>
  <files>src/persistent_stores/store_instances/config_store/store_definition.cpp, src/persistent_stores/store_instances/config_store/store_migrations.cpp, src/persistent_stores/store_instances/config_store/CMakeLists.txt, .bright-builds-rules-checks.tsv</files>
  <behavior>
    - "Persisted item ordering/layout, defaults, migrations, generated hashes, and public definitions remain exact."
  </behavior>
  <action>Per D-05, D-07, D-08, D-11, D-12, D-14, and D-15, move cohesive migration implementation into the named source while leaving registry/definition ownership and public header unchanged. Compare generated hashes and layout artifacts before/after, run storage tests, `just generated-check`, representative build and executed simulator parity, then remove the row and run Phase 40 gate.</action>
  <verify><automated>set -euo pipefail; just generated-check; BUDDY_BAZEL_EXECUTE_REFERENCE=1 just test; binary=build-tests/tests/unit/persistent_stores/eeprom_unit_tests; test -x "$binary"; suite_dir="$(dirname "$binary")"; suite_name="$(basename "$binary")"; test_list="$(cd "$suite_dir" &amp;&amp; "./$suite_name" --list-tests)"; printf '%s\n' "$test_list"; printf '%s\n' "$test_list" | grep -Eq '[1-9][0-9]* test cases?'; (cd "$suite_dir" &amp;&amp; "./$suite_name")</automated></verify>
  <acceptance-criteria>
    - "Definition/migration sources are sub-threshold."
    - "Hashes, layout, defaults, and migrations compare equal."
    - "Both persistence rows are absent."
  </acceptance-criteria>
  <done>Persistent storage contracts remain byte-compatible.</done>
</task>

</tasks>

<threat-model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-08 | Tampering | Persisted layout/migrations | mitigate | Hash/layout comparison, recovery tests, generated checks. |
| T-40-10 | Denial | Journal recovery | mitigate | Failure/recovery characterization plus simulator parity. |
</threat-model>

<verification>
Run storage tests, `just generated-check`, representative build, executed simulator parity, `just phase40-verify`, and `git diff --check`.
</verification>

<success-criteria>
Both storage originals are sub-threshold, persisted layout/migrations are identical, and both temporary rows are removed.
</success-criteria>

<output>
After completion, create `.planning/phases/40-file-length-refactoring/40-11-SUMMARY.md`.
</output>
