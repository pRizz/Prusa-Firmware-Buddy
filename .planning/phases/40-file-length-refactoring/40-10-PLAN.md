---
phase: 40-file-length-refactoring
plan: 10
type: execute
wave: 10
depends_on: [40-09]
files_modified:
  - .bright-builds-rules-checks.tsv
  - src/buddy/CMakeLists.txt
  - src/buddy/filesystem_fatfs.cpp
  - src/buddy/filesystem_fatfs_dir.cpp
  - src/common/media_prefetch/media_prefetch.cpp
  - src/common/media_prefetch/media_prefetch_policy.cpp
  - src/common/CMakeLists.txt
  - src/connect/connect.cpp
  - src/connect/connect_commands.cpp
  - src/connect/marlin_printer.cpp
  - src/connect/marlin_printer_state.cpp
  - src/connect/CMakeLists.txt
  - src/transfers/transfer.cpp
  - src/transfers/transfer_recovery.cpp
  - src/transfers/CMakeLists.txt
  - src/connect/planner.cpp
autonomous: true
requirements: [D-03, D-05, D-07, D-08, D-09, D-11, D-12, D-14, D-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-27T17:17:18.946Z
must_haves:
  truths:
    - "Filesystem, prefetch, Connect, Marlin-printer adapter, and transfer behavior remain compatible across host, build, and simulator evidence."
    - "Five refactors are sub-threshold and planner.cpp is the only conversion in this campaign."
  artifacts:
    - path: src/transfers/transfer_recovery.cpp
      provides: "Transfer recovery policy implementation"
  key_links:
    - from: src/transfers/CMakeLists.txt
      to: src/transfers/transfer_recovery.cpp
      via: "existing firmware target"
      pattern: "transfer_recovery"
---

<objective>
Refactor the five network/media sources and convert the locked Connect planner exception.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/40-file-length-refactoring/40-09-SUMMARY.md
@tests/unit/media_prefetch/CMakeLists.txt
</context>

<decision-coverage>
| Decisions | Task | Coverage |
| --- | --- | --- |
| D-05, D-07, D-08 | 1-2 | Pure policy versus effect adapters, same targets, deletion-test seams. |
| D-03 | 3 | Planner is the sole locked conversion. |
| D-09, D-11, D-12, D-14, D-15 | 1-3 | Serial high-risk gate, atomic ledger, simulator/build evidence. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor filesystem and media prefetch</name>
  <read-first><file>src/buddy/filesystem_fatfs.cpp</file><file>src/common/media_prefetch/media_prefetch.cpp</file><file>tests/unit/media_prefetch/media_prefetch_tests.cpp</file></read-first>
  <files>src/buddy/filesystem_fatfs.cpp, src/buddy/filesystem_fatfs_dir.cpp, src/buddy/CMakeLists.txt, src/common/media_prefetch/media_prefetch.cpp, src/common/media_prefetch/media_prefetch_policy.cpp, src/common/CMakeLists.txt, .bright-builds-rules-checks.tsv</files>
  <action>Per D-05, D-07, D-08, D-11, D-12, and D-14, keep FatFS calls and async I/O in façades; extract directory/devoptab operations and pure prefetch state policy into named sources. Preserve filenames, mount/device behavior, read/seek/error semantics, option guards, and target ownership. Remove two rows after media characterization and representative build pass.</action>
  <verify><automated>set -euo pipefail; BUDDY_BAZEL_EXECUTE_REFERENCE=1 just test; binary=build-tests/tests/unit/media_prefetch/media_prefetch_tests; test -x "$binary"; suite_dir="$(dirname "$binary")"; suite_name="$(basename "$binary")"; test_list="$(cd "$suite_dir" &amp;&amp; "./$suite_name" --list-tests)"; printf '%s\n' "$test_list"; printf '%s\n' "$test_list" | grep -Eq '[1-9][0-9]* test cases?'; (cd "$suite_dir" &amp;&amp; "./$suite_name")</automated></verify>
  <acceptance-criteria>
    - "Both originals/new sources are sub-threshold and media/filesystem behavior compares equal."
    - "Two rows are removed."
  </acceptance-criteria>
  <done>Filesystem and prefetch seams preserve media behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Connect, Marlin adapter, and transfers</name>
  <read-first><file>src/connect/connect.cpp</file><file>src/connect/marlin_printer.cpp</file><file>src/transfers/transfer.cpp</file></read-first>
  <files>src/connect/connect.cpp, src/connect/connect_commands.cpp, src/connect/marlin_printer.cpp, src/connect/marlin_printer_state.cpp, src/connect/CMakeLists.txt, src/transfers/transfer.cpp, src/transfers/transfer_recovery.cpp, src/transfers/CMakeLists.txt, .bright-builds-rules-checks.tsv</files>
  <action>Per D-05, D-07, D-08, D-11, D-12, D-14, and D-15, extract command/state decisions and transfer recovery while keeping network, filesystem, Marlin calls, public symbols, protocol statuses, filenames, retries, and recovery effects in stable adapters. Preserve compile guards and target ordering. Run host tests, representative build, and simulator parity; if wrapper prints, execute underlying simulator command. Remove three rows atomically.</action>
  <verify><automated>set -euo pipefail; BUDDY_BAZEL_EXECUTE_REFERENCE=1 just test; for binary in build-tests/tests/unit/connect/connect_tests build-tests/tests/unit/connect/connect_planner_tests build-tests/tests/unit/connect/connect_registration_tests build-tests/tests/unit/transfers/transfers_tests; do test -x "$binary"; suite_dir="$(dirname "$binary")"; suite_name="$(basename "$binary")"; test_list="$(cd "$suite_dir" &amp;&amp; "./$suite_name" --list-tests)"; printf '%s\n' "$test_list"; printf '%s\n' "$test_list" | grep -Eq '^[[:space:]]*[1-9][0-9]* test cases?$'; (cd "$suite_dir" &amp;&amp; "./$suite_name"); done</automated></verify>
  <acceptance-criteria>
    - "Three originals/new sources are sub-threshold; protocol/recovery behavior compares equal."
    - "Three rows are removed and simulator proof records actual execution."
  </acceptance-criteria>
  <done>Network and transfer behavior remains compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the locked planner exception</name>
  <read-first><file>src/connect/planner.cpp</file><file>doc/file_length_policy.md</file></read-first>
  <files>src/connect/planner.cpp, .bright-builds-rules-checks.tsv</files>
  <action>Per D-03, D-04, D-11, D-12, and D-14, replace only `src/connect/planner.cpp` temporary reason with the approved permanent-owned deletion-test reason, without editing the source. Prove the planner remains a cohesive stateful deep module and run its host tests, representative build, simulator parity, and Phase 40 gate.</action>
  <verify><automated>just phase40-verify</automated></verify>
  <acceptance-criteria>
    - "Planner is the third and final permanent-owned conversion."
    - "Five refactor rows are removed; no other path is reclassified."
  </acceptance-criteria>
  <done>The network/media campaign ends with five refactors and one locked conversion.</done>
</task>

</tasks>

<threat-model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-07 | Tampering | Protocol/media recovery | mitigate | Characterization, host tests, build, simulator parity. |
| T-40-09 | Elevation | Planner permanence | mitigate | Exact path allowlist and deletion-test evidence. |
</threat-model>

<verification>
Run focused host tests, representative firmware build, executed simulator parity, `just phase40-verify`, and `git diff --check`.
</verification>

<success-criteria>
Five network/media originals are sub-threshold, planner is the third exact conversion, and protocols/recovery remain compatible.
</success-criteria>

<output>
After completion, create `.planning/phases/40-file-length-refactoring/40-10-SUMMARY.md`.
</output>
