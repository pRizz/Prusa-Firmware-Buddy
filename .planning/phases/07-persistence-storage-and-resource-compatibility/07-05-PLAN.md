---
phase: 07-persistence-storage-and-resource-compatibility
plan: 05
type: execute
wave: 3
depends_on:
  - 07-04
files_modified:
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - BUILD.bazel
  - justfile
  - .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
autonomous: true
requirements:
  - IFCE-04
  - IFCE-05
requirements_addressed:
  - IFCE-04
  - IFCE-05
must_haves:
  truths:
    - "Developer can run `just phase7-verify` to execute Phase 7 verifier tests and the aggregate Phase 7 verifier through Bazel."
    - "Developer can query root and tools Bazel labels for `phase7_verify`, `phase7_verify_tests`, and `phase7_persistence_storage_resource_docs`."
    - "Developer can query a root storage migration fixture filegroup and `just phase7-verify` validates the redacted migration catalog without running full generators, simulator flows, or hardware/media checks."
    - "Nyquist validation records all Phase 7 task IDs, automated commands, non-local evidence classes, and final local evidence without marking hardware/media/generator/release parity as locally passed."
    - "Rust pre-commit checks required by repo instructions are run before completion."
  artifacts:
    - path: "tools/bazel/BUILD.bazel"
      provides: "Bazel shell_binary labels for Phase 7 verifier and tests"
      contains: "phase7_verify"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Dispatch for Phase 7 verifier labels"
      contains: "phase7_verify)"
    - path: "BUILD.bazel"
      provides: "Root aliases and phase doc filegroup"
      contains: "phase7_storage_migration_fixtures"
    - path: "justfile"
      provides: "Developer facade recipe"
      contains: "phase7-verify:"
    - path: ".planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md"
      provides: "Nyquist validation sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "justfile"
      to: "//tools/bazel:phase7_verify"
      via: "`phase7-verify` recipe"
      pattern: "bazel run //tools/bazel:phase7_verify"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase7_verify.py"
      via: "case dispatch"
      pattern: "python3 tools/bazel/phase7_verify.py --all"
    - from: "BUILD.bazel"
      to: ".planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md"
      via: "phase docs filegroup"
      pattern: "phase7_persistence_storage_resource_docs"
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json"
      via: "`phase7_verify` data dependency"
      pattern: "redacted_migration_catalog.json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-06T04-24-25
generated_at: 2026-06-06T04:45:46Z
---

<objective>
Wire Phase 7 verification through Bazel/just and complete Nyquist validation sign-off.

Purpose: Phase 7 closes with a developer-visible local gate that verifies IFCE-04 and IFCE-05 compatibility contracts while recording which storage/resource evidence remains simulator, hardware, manual, bootstrap, or later-phase work.
Output: Bazel labels, `just phase7-verify`, passing aggregate checks, and updated `07-VALIDATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
@tools/bazel/phase7_verify.py
@tools/bazel/phase7_verify_test.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@tools/bazel/manifests/phase7_config_store.json
@tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json
@tools/bazel/manifests/phase7_storage_media.json
@tools/bazel/manifests/phase7_resources.json
@tools/bazel/manifests/phase7_generated_outputs.json
@tools/bazel/manifests/phase7_concern_dispositions.json
@rust/crates/domain/src/storage.rs
@rust/crates/domain/src/resource.rs
@rust/crates/domain/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 7 verifier into Bazel and just</name>
  <read_first>
    tools/bazel/BUILD.bazel
    tools/bazel/rust_workflow.sh
    BUILD.bazel
    justfile
    tools/bazel/phase7_verify.py
    tools/bazel/phase7_verify_test.py
    tools/bazel/manifests/phase7_config_store.json
    tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json
    tools/bazel/manifests/phase7_storage_media.json
    tools/bazel/manifests/phase7_resources.json
    tools/bazel/manifests/phase7_generated_outputs.json
    tools/bazel/manifests/phase7_concern_dispositions.json
    rust/crates/domain/src/storage.rs
    rust/crates/domain/src/resource.rs
    rust/crates/domain/src/lib.rs
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
  </read_first>
  <files>tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, BUILD.bazel, justfile</files>
  <action>Update `tools/bazel/BUILD.bazel` per D-13, D-14, D-15, and resolved research question 3 to add `shell_binary(name = "phase7_verify", src = "rust_workflow.sh", data = [...])` and `shell_binary(name = "phase7_verify_tests", src = "rust_workflow.sh", data = [...])`. The `phase7_verify` data list must include `phase7_verify.py`, all five Phase 7 manifests, `tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json`, `//:phase7_persistence_storage_resource_docs`, `//:phase7_storage_migration_fixtures`, and `//:rust_workspace_sources`. The `phase7_verify_tests` data list must include `phase7_verify.py`, `phase7_verify_test.py`, and `tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json`. Update `tools/bazel/rust_workflow.sh` with case labels `phase7_verify)` running `python3 tools/bazel/phase7_verify.py --all` and `phase7_verify_tests)` running `python3 tools/bazel/phase7_verify_test.py`; do not add full generator, simulator, hardware, or media execution to these dispatches. Update root `BUILD.bazel` with a `filegroup(name = "phase7_persistence_storage_resource_docs", srcs = [".planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md", ".planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md", ".planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md"])`, a `filegroup(name = "phase7_storage_migration_fixtures", srcs = ["tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json"])`, plus aliases `phase7_verify` and `phase7_verify_tests`. Update `justfile` with recipe `phase7-verify:` that runs `bazel run //tools/bazel:phase7_verify_tests` then `bazel run //tools/bazel:phase7_verify`; keep it as deterministic static verifier/test/Rust checks and do not call generator update/check labels or hardware/media flows from this recipe. Keep existing Phase 2-6 recipes unchanged.</action>
  <verify>
    <automated>bazel query "//tools/bazel:phase7_verify + //tools/bazel:phase7_verify_tests + //:phase7_verify + //:phase7_verify_tests + //:phase7_persistence_storage_resource_docs + //:phase7_storage_migration_fixtures" &amp;&amp; bazel run //tools/bazel:phase7_verify_tests &amp;&amp; bazel run //tools/bazel:phase7_verify</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query "//tools/bazel:phase7_verify + //tools/bazel:phase7_verify_tests + //:phase7_verify + //:phase7_verify_tests + //:phase7_persistence_storage_resource_docs + //:phase7_storage_migration_fixtures"` exits 0.
    - `bazel run //tools/bazel:phase7_verify_tests` exits 0.
    - `bazel run //tools/bazel:phase7_verify` exits 0.
    - `rg "phase7_verify|phase7_verify_tests|phase7_persistence_storage_resource_docs|phase7_storage_migration_fixtures|redacted_migration_catalog.json|phase7_config_store.json|phase7_storage_media.json|phase7_resources.json|phase7_generated_outputs.json|phase7_concern_dispositions.json" tools/bazel/BUILD.bazel BUILD.bazel` finds every string.
    - `rg "phase7_verify\\)|python3 tools/bazel/phase7_verify.py --all|phase7_verify_tests\\)|python3 tools/bazel/phase7_verify_test.py" tools/bazel/rust_workflow.sh` finds every dispatch string.
    - `rg "phase7-verify:|bazel run //tools/bazel:phase7_verify_tests|bazel run //tools/bazel:phase7_verify" justfile` finds every facade string.
  </acceptance_criteria>
  <done>Phase 7 verifier and tests are queryable through Bazel and runnable through `just phase7-verify`.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Phase 7 validation evidence and Rust pre-commit checks</name>
  <read_first>
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
    tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json
    tools/bazel/phase7_verify.py
    tools/bazel/phase7_verify_test.py
    tools/bazel/BUILD.bazel
    BUILD.bazel
    justfile
    rust/crates/domain/src/storage.rs
    rust/crates/domain/src/resource.rs
    rust/crates/domain/src/lib.rs
  </read_first>
  <files>.planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md</files>
  <action>Update `.planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md` per D-07, D-13, D-14, D-15, and resolved research question 3 after verification commands actually pass. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`. In the Per-Task Verification Map, replace `TBD` plan/wave values with exact plan IDs and waves: `07-W0-01` -> Plan 01 Wave 1, `07-W0-02` -> Plan 01 Wave 1, `07-W0-03` -> Plan 02 Wave 1, `07-W0-04` -> Plan 04 Wave 2, `07-W0-05` -> Plan 01 Wave 1, and `07-FINAL` -> Plan 05 Wave 3. Add additional rows for `07-01-03`, `07-02-01`, `07-02-02`, `07-02-03`, `07-03-01`, `07-03-02`, `07-04-01`, `07-04-02`, `07-05-01`, and `07-05-02`, each with requirement IDs, threat refs, automated command, and status `green` only if that command passed. The `07-01-03` row must verify `tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json` and exact strings `selftest-calibration-state`, `Selftest Result`, `selftest_result`, `calibration`, and `selftest`. Add a `## Final Automated Evidence` section listing exact commands and outcomes for `python3 tools/bazel/phase7_verify.py --quick`, `python3 tools/bazel/phase7_verify_test.py`, `bazel run //tools/bazel:phase7_verify_tests`, `bazel run //tools/bazel:phase7_verify`, `just phase7-verify`, `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`. Note that `just phase7-verify` is deterministic static verifier/test/Rust evidence; preserve the Manual-Only Verifications table and do not mark actual USB media, flash wear, filesystem mount timing, crash-dump persistence, full generator execution, full release artifact byte parity, simulator flows, or hardware media proof as locally passed. Keep `phase_lifecycle_id: 7-2026-06-06T04-24-25` unchanged.</action>
  <verify>
    <automated>python3 tools/bazel/phase7_verify.py --quick &amp;&amp; python3 tools/bazel/phase7_verify_test.py &amp;&amp; bazel run //tools/bazel:phase7_verify_tests &amp;&amp; bazel run //tools/bazel:phase7_verify &amp;&amp; just phase7-verify &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase7_verify.py --quick` exits 0.
    - `python3 tools/bazel/phase7_verify_test.py` exits 0.
    - `bazel run //tools/bazel:phase7_verify_tests` exits 0.
    - `bazel run //tools/bazel:phase7_verify` exits 0.
    - `just phase7-verify` exits 0.
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
    - `cargo build --all-targets --all-features` exits 0.
    - `cargo test --all-features` exits 0.
    - `rg "nyquist_compliant: true|wave_0_complete: true|Final Automated Evidence|07-01-03|07-W0-05|redacted_migration_catalog.json|selftest-calibration-state|Selftest Result|selftest_result|calibration|selftest|07-05-02|just phase7-verify|cargo clippy --all-targets --all-features -- -D warnings" .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md` finds every string.
    - `rg "manual-hardware-required|hardware-smoke|simulator-flow|Full release artifact byte parity|Actual USB media mount/read/write timing" .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md` still finds the non-local evidence wording.
  </acceptance_criteria>
  <done>Phase 7 Nyquist validation is complete, local aggregate verification passes, Rust pre-commit checks pass, and non-local evidence remains explicitly classified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel/just facade -> verifier scripts | Developer commands must call the correct Phase 7 verifier and tests, not stale phase labels. |
| Bazel data graph -> storage fixture catalog | The redacted storage migration catalog must be present in verifier data dependencies and root filegroups. |
| Aggregate verification -> evidence record | Validation status must be updated only from commands that actually ran and passed. |
| Local checks -> non-local proof | Passing local gates must not be recorded as physical media, simulator, heavy generator, release artifact, or hardware proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Bazel label wiring | mitigate | Query exact root and tools labels and require Phase 7 manifests/docs/Rust sources in data dependencies. |
| T-07-05-02 | Repudiation | `just phase7-verify` facade | mitigate | Require recipe to run verifier tests before aggregate verifier and validate through `just phase7-verify`. |
| T-07-05-03 | Repudiation | Validation sign-off | mitigate | `07-VALIDATION.md` must list exact command outcomes and task IDs before setting `nyquist_compliant: true`. |
| T-07-05-04 | Repudiation | Non-local evidence classification | mitigate | Preserve manual/simulator/hardware evidence rows and forbid local proof claims for media, generator, release, simulator, and hardware flows. |
| T-07-05-05 | Tampering | Storage migration fixture wiring | mitigate | Query `//:phase7_storage_migration_fixtures` and require `redacted_migration_catalog.json` in Phase 7 verifier Bazel data. |
</threat_model>

<verification>
- `bazel query "//tools/bazel:phase7_verify + //tools/bazel:phase7_verify_tests + //:phase7_verify + //:phase7_verify_tests + //:phase7_persistence_storage_resource_docs + //:phase7_storage_migration_fixtures"`
- `bazel run //tools/bazel:phase7_verify_tests`
- `bazel run //tools/bazel:phase7_verify`
- `just phase7-verify`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
</verification>

<success_criteria>
Plan 05 succeeds when Phase 7 verifier labels and the storage migration fixture filegroup are queryable, `just phase7-verify` passes as a deterministic static verifier/test/Rust gate, Rust pre-commit checks pass, `07-VALIDATION.md` is Nyquist-compliant, and non-local storage/resource evidence remains explicitly classified.
</success_criteria>

<output>
After completion, create `.planning/phases/07-persistence-storage-and-resource-compatibility/07-05-SUMMARY.md`.
</output>
