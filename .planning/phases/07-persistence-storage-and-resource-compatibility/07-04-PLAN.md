---
phase: 07-persistence-storage-and-resource-compatibility
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - tools/bazel/phase7_verify.py
  - tools/bazel/phase7_verify_test.py
autonomous: true
requirements:
  - IFCE-04
  - IFCE-05
requirements_addressed:
  - IFCE-04
  - IFCE-05
must_haves:
  truths:
    - "Developer can run `python3 tools/bazel/phase7_verify.py --quick` to validate Phase 7 manifests, source paths, lifecycle metadata, Rust API surface, redaction policy, evidence classes, concern dispositions, generated-label references, and overclaim boundaries."
    - "Developer can run the quick verifier and see redacted storage migration catalog coverage for old EEPROM versions, current schema, settings import/export, credential redaction, selftest/calibration state, and journal hash facts."
    - "Verifier regression tests prove failures for missing manifests, invalid lifecycle IDs, unredacted credential evidence, missing source paths, invalid generated labels, missing Rust API strings, unsafe domain code, and local hardware/release overclaims."
    - "The quick verifier remains static and deterministic; full Rust checks are reserved for `--all`."
  artifacts:
    - path: "tools/bazel/phase7_verify.py"
      provides: "Phase 7 static and aggregate verifier"
      contains: "check_storage_migration_catalog"
    - path: "tools/bazel/phase7_verify_test.py"
      provides: "Phase 7 verifier regression tests"
      contains: "Phase7VerifierTest"
  key_links:
    - from: "tools/bazel/phase7_verify.py"
      to: "tools/bazel/manifests/phase7_config_store.json"
      via: "config manifest validation"
      pattern: "phase7_config_store.json"
    - from: "tools/bazel/phase7_verify.py"
      to: "tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json"
      via: "redacted storage migration catalog validation"
      pattern: "redacted_migration_catalog.json"
    - from: "tools/bazel/phase7_verify.py"
      to: "rust/crates/domain/src/storage.rs"
      via: "Rust API surface validation"
      pattern: "ReferenceHashName"
    - from: "tools/bazel/phase7_verify.py"
      to: "tools/bazel/generated_drift.py"
      via: "generated label coverage validation"
      pattern: "generated_drift.py"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-06T04-24-25
generated_at: 2026-06-06T04:45:46Z
---

<objective>
Create the repo-owned Phase 7 verifier and verifier regression tests.

Purpose: The Phase 7 manifests and Rust contracts need a local gate that proves compatibility coverage without depending on full firmware builds, heavy generator execution, simulator flows, or hardware media checks.
Output: `tools/bazel/phase7_verify.py` and `tools/bazel/phase7_verify_test.py`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
@tools/bazel/phase6_verify.py
@tools/bazel/phase6_verify_test.py
@tools/bazel/manifests/phase7_config_store.json
@tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json
@tools/bazel/manifests/phase7_storage_media.json
@tools/bazel/manifests/phase7_resources.json
@tools/bazel/manifests/phase7_generated_outputs.json
@tools/bazel/manifests/phase7_concern_dispositions.json
@rust/crates/domain/src/lib.rs
@rust/crates/domain/src/storage.rs
@rust/crates/domain/src/resource.rs
@tools/bazel/generated_drift.py
@tools/bazel/BUILD.bazel
@BUILD.bazel
@justfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 7 verifier regression tests</name>
  <read_first>
    tools/bazel/phase7_verify_test.py
    tools/bazel/phase6_verify_test.py
    tools/bazel/phase7_verify.py
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
    tools/bazel/manifests/phase7_config_store.json
    tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json
    tools/bazel/manifests/phase7_storage_media.json
    tools/bazel/manifests/phase7_resources.json
    tools/bazel/manifests/phase7_generated_outputs.json
    tools/bazel/manifests/phase7_concern_dispositions.json
    rust/crates/domain/src/lib.rs
    rust/crates/domain/src/storage.rs
    rust/crates/domain/src/resource.rs
  </read_first>
  <files>tools/bazel/phase7_verify_test.py</files>
  <behavior>
    - Test 1: `--help` lists `--quick`, `--all`, `--manifests-only`, `--config-only`, `--storage-only`, `--resources-only`, `--generated-only`, `--concerns-only`, and `--rust-only`.
    - Test 2: `--manifests-only` fails when `tools/bazel/manifests/phase7_config_store.json` is missing.
    - Test 3: `--quick` fails when any Phase 7 manifest has `phase_lifecycle_id` other than `7-2026-06-06T04-24-25`.
    - Test 4: `--quick` fails when `phase7_config_store.json` contains credential value markers such as `password_value`, `token_value`, `BEGIN PRIVATE KEY`, or `raw_eeprom`.
    - Test 5: `--quick` fails when `redacted_migration_catalog.json` is missing `selftest-calibration-state`, `Selftest Result`, `selftest_result`, `calibration`, `selftest`, `old-eeprom-v4-migration`, or `journal-hash-facts`.
    - Test 6: `--quick` fails when `redacted_migration_catalog.json` contains credential value markers or byte-material markers such as `password_value`, `token_value`, `BEGIN PRIVATE KEY`, `raw_eeprom`, `byte_array`, or `eeprom_bytes`.
    - Test 7: `--quick` fails when a hardware/media storage row uses `evidence_class: "hardware verified locally"`.
    - Test 8: `--quick` fails when `phase7_generated_outputs.json` omits `//tools/bazel:generated_resources_check` or uses a check label not ending in `_check`.
    - Test 9: `--quick` fails when `rust/crates/domain/src/storage.rs` lacks `ReferenceHashName` or `CredentialRedactionPolicy`.
    - Test 10: `--quick` fails when `rust/crates/domain/src/resource.rs` lacks `ResourceSurface` or `GeneratedOutputOwnership`.
    - Test 11: `--quick` fails when storage/resource domain modules contain `unsafe {` in code after comments and strings are stripped.
    - Test 12: `--all` runs quick checks and invokes Cargo with exact arguments `fmt --all -- --check`, `clippy --all-targets --all-features -- -D warnings`, `build --all-targets --all-features`, and `test --all-features`.
  </behavior>
  <action>Create `tools/bazel/phase7_verify_test.py` per D-13, D-14, and D-15 using Python `unittest`, following the temp-root pattern in `tools/bazel/phase6_verify_test.py`. Include helper methods named `make_temp_root`, `write_file`, `write_source_paths`, `write_config_manifest`, `write_migration_catalog`, `write_storage_manifest`, `write_resources_manifest`, `write_generated_outputs_manifest`, `write_concern_manifest`, `write_rust_api_surface`, `write_facade_files`, and `write_phase7_quick_surface`. The helper manifests/catalog must include the required Phase 7 row IDs from Plans 01 and 02, the required lifecycle ID `7-2026-06-06T04-24-25`, `selftest-calibration-state`, `Selftest Result`, `selftest_result`, `calibration`, `selftest`, old EEPROM rows, and source stub files so missing-source tests are deliberate. Add the behavior tests listed above with assertions on exact stderr/stdout substrings such as `phase_lifecycle_id`, `credential`, `selftest-calibration-state`, `Selftest Result`, `raw_eeprom`, `hardware verified locally`, `generated_resources_check`, `ReferenceHashName`, `ResourceSurface`, and `unsafe block`. Tests must be written before the verifier implementation is completed; initial RED failure is acceptable until Task 2 implements `phase7_verify.py`.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase7_verify_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile tools/bazel/phase7_verify_test.py` exits 0.
    - `rg "class Phase7VerifierTest|write_config_manifest|write_migration_catalog|write_storage_manifest|write_resources_manifest|write_generated_outputs_manifest|write_concern_manifest|write_rust_api_surface|test_help_lists_phase7_modes|test_quick_rejects_unredacted_credential_material|test_all_runs_quick_checks_and_cargo_verification" tools/bazel/phase7_verify_test.py` finds every string.
    - `rg "ReferenceHashName|CredentialRedactionPolicy|ResourceSurface|GeneratedOutputOwnership|generated_resources_check|selftest-calibration-state|Selftest Result|selftest_result|calibration|selftest|hardware verified locally|BEGIN PRIVATE KEY|raw_eeprom|unsafe block" tools/bazel/phase7_verify_test.py` finds every string.
  </acceptance_criteria>
  <done>Phase 7 verifier regression tests exist and specify the failure modes the verifier must enforce.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 7 static verifier</name>
  <read_first>
    tools/bazel/phase7_verify.py
    tools/bazel/phase7_verify_test.py
    tools/bazel/phase6_verify.py
    tools/bazel/generated_drift.py
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
    tools/bazel/manifests/phase7_config_store.json
    tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json
    tools/bazel/manifests/phase7_storage_media.json
    tools/bazel/manifests/phase7_resources.json
    tools/bazel/manifests/phase7_generated_outputs.json
    tools/bazel/manifests/phase7_concern_dispositions.json
    rust/crates/domain/src/lib.rs
    rust/crates/domain/src/storage.rs
    rust/crates/domain/src/resource.rs
    BUILD.bazel
    tools/bazel/BUILD.bazel
    tools/bazel/rust_workflow.sh
    justfile
  </read_first>
  <files>tools/bazel/phase7_verify.py</files>
  <behavior>
    - Test 1: `python3 tools/bazel/phase7_verify.py --quick` passes against the real repo after Plans 01-03 are complete.
    - Test 2: `python3 tools/bazel/phase7_verify.py --config-only` validates `phase7_config_store.json` only and rejects secrets.
    - Test 3: `python3 tools/bazel/phase7_verify.py --storage-only` validates `phase7_storage_media.json` and `redacted_migration_catalog.json`, rejects local hardware overclaims, and rejects catalog byte-material/secret markers.
    - Test 4: `python3 tools/bazel/phase7_verify.py --resources-only` validates `phase7_resources.json` only.
    - Test 5: `python3 tools/bazel/phase7_verify.py --generated-only` validates `phase7_generated_outputs.json` only and requires all Phase 3 labels.
    - Test 6: `python3 tools/bazel/phase7_verify.py --concerns-only` validates `phase7_concern_dispositions.json` only and requires all D-11 concern rows.
    - Test 7: `python3 tools/bazel/phase7_verify.py --rust-only` validates `storage.rs`, `resource.rs`, and `lib.rs` API strings and unsafe-free posture only.
    - Test 8: `python3 tools/bazel/phase7_verify.py --all` runs quick checks plus Cargo fmt, clippy, build, and tests.
  </behavior>
  <action>Create `tools/bazel/phase7_verify.py` per D-05, D-07, D-10, D-13, D-14, and D-15 from the Phase 6 verifier shape, but update constants and validation to Phase 7. Set `PHASE = "07-persistence-storage-and-resource-compatibility"` and `PHASE_LIFECYCLE_ID = "7-2026-06-06T04-24-25"`. Define manifest paths `phase7_config_store.json`, `phase7_storage_media.json`, `phase7_resources.json`, `phase7_generated_outputs.json`, and `phase7_concern_dispositions.json`, plus fixture catalog path `tools/bazel/fixtures/phase7_storage/redacted_migration_catalog.json`. Allow evidence classes exactly `manifest-check`, `source-audit`, `static-source-audit`, `host-test`, `rust-host-test`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`. Implement functions named `check_config_store_manifest`, `check_storage_migration_catalog`, `check_storage_media_manifest`, `check_resources_manifest`, `check_generated_outputs_manifest`, `check_concern_manifest`, `check_rust_api_surface`, `check_bazel_surface`, `check_just_surface`, `check_validation_contract`, `check_no_phase7_overclaim`, `check_quick`, and `check_rust_toolchain`. Required row IDs must match Plans 01 and 02 exactly. `check_config_store_manifest` must require `CurrentStore::newest_config_version = 5`, `WIFI AP Password`, `Connect Token`, `name-only-redacted`, `DeprecatedStore`, `selftest-calibration-state`, `Selftest Result`, `selftest_result`, `calibration`, `selftest`, `0x3FFF`, and old EEPROM versions `v4`, `v6`, `v7`, `v9`, `v10`, `v11`, `v12`, `v22`, `v32787`, `v32789`, and `current`. `check_storage_migration_catalog` must require catalog rows `old-eeprom-v4-migration`, `old-eeprom-v6-migration`, `old-eeprom-v7-migration`, `old-eeprom-v9-migration`, `old-eeprom-v10-migration`, `old-eeprom-v11-migration`, `old-eeprom-v12-migration`, `old-eeprom-v22-migration`, `old-eeprom-v32787-migration`, `old-eeprom-v32789-migration`, `current-schema-v5`, `settings-import-export`, `credential-redaction`, `selftest-calibration-state`, and `journal-hash-facts`; require strings `Selftest Result`, `selftest_result`, `calibration`, `selftest`, `CurrentStore::newest_config_version = 5`, `journal::hash`, `0x3FFF`, `duplicate detection`, `synthetic-redacted`, `name-only-redacted`, and `byte_material_policy`; and reject secret/byte-material markers `password_value`, `token_value`, `secret_value`, `BEGIN PRIVATE KEY`, `certificate_bytes`, `raw_eeprom`, `byte_array`, and `eeprom_bytes`. `check_storage_media_manifest` must require rows for `/usb`, `/internal`, `/bbf`, `/semihosting`, `/`, `POSIX-like devoptab`, and `EEPROM/internal flash`, and reject hardware/media rows whose `evidence_class` is not `manual-hardware-required`, `hardware-smoke`, or `simulator-flow` unless the row is source-only. `check_generated_outputs_manifest` must require every check/update label from Plan 02 and require check labels to end in `_check` and update labels to end in `_update`. `check_concern_manifest` must require all eight D-11 concern row IDs and `preserve-with-explicit-risk` unless `intentional_delta` is not `none`. `check_rust_api_surface` must require `#![forbid(unsafe_code)]`, storage API strings `ReferenceHashName`, `JournalHashFact`, `CredentialRedactionPolicy`, `EvidenceClass`, `FilesystemSurface`, `StorageCompatibilitySurface`, and `FixtureIdentity`, and resource API strings `ResourceRuntimePath`, `ResourceSurface`, `GeneratedOutputOwnership`, `BazelLabel`, and `GeneratedSurface`; it must reject unsafe syntax after stripping comments and strings using the Phase 6 scanner. `check_no_phase7_overclaim` must scan Phase 7 manifests, the redacted migration catalog, and `07-*-SUMMARY.md` files when present and reject lowercase phrases `hardware-safe`, `hardware passed`, `hardware verified locally`, `locally passed hardware`, `byte-for-byte firmware parity`, `full release artifact parity`, `gui parity implemented`, `wui api implemented`, `connect tls implemented`, `auxiliary runtime parity implemented`, and `cutover evidence complete`. `--quick` and `--storage-only` must include `check_storage_migration_catalog`; `--all` must run the exact Cargo commands from the repo Rust pre-commit requirements.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase7_verify.py &amp;&amp; python3 tools/bazel/phase7_verify_test.py &amp;&amp; python3 tools/bazel/phase7_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile tools/bazel/phase7_verify.py` exits 0.
    - `python3 tools/bazel/phase7_verify_test.py` exits 0.
    - `python3 tools/bazel/phase7_verify.py --quick` exits 0 and prints `Phase 7 persistence storage and resource compatibility verification passed`.
    - `rg "check_config_store_manifest|check_storage_migration_catalog|check_storage_media_manifest|check_resources_manifest|check_generated_outputs_manifest|check_concern_manifest|check_rust_api_surface|check_no_phase7_overclaim" tools/bazel/phase7_verify.py` finds every function name.
    - `rg "CurrentStore::newest_config_version = 5|WIFI AP Password|Connect Token|name-only-redacted|0x3FFF|ReferenceHashName|CredentialRedactionPolicy|ResourceSurface|GeneratedOutputOwnership|byte-for-byte firmware parity|redacted_migration_catalog.json|selftest-calibration-state|Selftest Result|selftest_result|calibration|selftest|journal-hash-facts" tools/bazel/phase7_verify.py` finds every string.
  </acceptance_criteria>
  <done>The Phase 7 verifier enforces manifests, source paths, lifecycle metadata, redaction, evidence classes, generated labels, Rust API shape, unsafe-free posture, facade checks, validation contract checks, and scope-overclaim boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON manifests -> verifier trust | The verifier must parse untrusted JSON manifests and reject missing, malformed, unredacted, or overclaiming rows. |
| Redacted fixture catalog -> verifier trust | The verifier must parse synthetic fixture catalog rows and reject missing selftest/calibration, migration, redaction, or hash coverage. |
| Rust domain source -> verifier trust | The verifier must inspect source text for required API strings and unsafe syntax without being fooled by comments or strings. |
| Local verifier -> aggregate evidence | Quick checks must not invoke heavy generator, simulator, hardware, or release parity flows by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | Manifest validation | mitigate | Required row IDs, fields, lifecycle ID, source paths, requirement IDs, and evidence classes are checked before quick verification passes. |
| T-07-04-02 | Information Disclosure | Credential and fixture evidence | mitigate | Verifier rejects private-key, token, certificate, raw EEPROM, and credential value markers. |
| T-07-04-03 | Tampering | Generated label coverage | mitigate | Verifier requires exact Phase 3 check/update labels and suffixes `_check` and `_update`. |
| T-07-04-04 | Elevation of privilege | Rust pure domain modules | mitigate | Verifier strips comments/strings and rejects unsafe syntax in `storage.rs` and `resource.rs`. |
| T-07-04-05 | Repudiation | Scope overclaims | mitigate | Verifier rejects local hardware, release byte parity, GUI, WUI, Connect/TLS, auxiliary runtime, and cutover overclaim phrases. |
| T-07-04-06 | Tampering | Storage migration catalog validation | mitigate | Verifier requires catalog rows for old EEPROM versions, current schema, settings import/export, credential redaction, `selftest-calibration-state`, and journal hash facts. |
</threat_model>

<verification>
- `python3 -m py_compile tools/bazel/phase7_verify.py tools/bazel/phase7_verify_test.py`
- `python3 tools/bazel/phase7_verify_test.py`
- `python3 tools/bazel/phase7_verify.py --quick`
- `rg "check_storage_migration_catalog|redacted_migration_catalog.json|selftest-calibration-state|Selftest Result|selftest_result|calibration|selftest" tools/bazel/phase7_verify.py tools/bazel/phase7_verify_test.py`
</verification>

<success_criteria>
Plan 04 succeeds when the Phase 7 verifier test suite passes, `--quick` passes in the real repo, the redacted storage migration catalog is validated, selftest/calibration state is enforced in config and fixture coverage, and the verifier rejects the Phase 7 threat and overclaim cases described in the tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-persistence-storage-and-resource-compatibility/07-04-SUMMARY.md`.
</output>
