---
phase: 06-printing-core-safety-and-feature-gates
plan: 05
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - tools/bazel/phase6_verify.py
  - .planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05
requirements_addressed:
  - CORE-03
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "Developer can run one aggregate Phase 6 gate through `just phase6-verify`."
    - "The verifier checks manifest schema, lifecycle ID, source paths, concern dispositions, Rust API shape, Bazel labels, just recipe, Rust fmt/lint/build/tests, and evidence wording."
    - "No Phase 6 artifact can claim local hardware proof, GUI parity, storage parity, network/auth/crypto implementation, auxiliary runtime parity, or cutover evidence."
    - "Nyquist validation status reflects that Wave 0 verifier setup exists and automated checks are available for every remaining Phase 6 task."
  artifacts:
    - path: "tools/bazel/phase6_verify.py"
      provides: "Full aggregate Phase 6 verifier"
      contains: "check_rust_api_surface"
    - path: ".planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md"
      provides: "Updated Phase 6 validation sign-off and Nyquist status"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tools/bazel/phase6_verify.py"
      to: "rust/crates/domain/src/print.rs"
      via: "Rust API shape check"
      pattern: "PrintJobState"
    - from: "tools/bazel/phase6_verify.py"
      to: "rust/crates/domain/src/safety.rs"
      via: "Rust API shape check"
      pattern: "SafetyPolicySurface"
    - from: "tools/bazel/phase6_verify.py"
      to: "rust/crates/domain/src/feature.rs"
      via: "Rust API shape check"
      pattern: "Phase6FeatureGates"
    - from: ".planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md"
      to: "just phase6-verify"
      via: "validation sign-off"
      pattern: "Full suite command"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-04T09-48-48
generated_at: 2026-06-04T10:06:26Z
---

<objective>
Harden the Phase 6 aggregate verifier after the print, safety, and feature-gate Rust surfaces exist, then update the Nyquist validation record.

Purpose: Phase 6 closes with a single enforceable local gate that proves CORE-03, CORE-04, and CORE-05 policy and manifest coverage without overclaiming non-local hardware or later-phase behavior.
Output: Hardened `tools/bazel/phase6_verify.py`, passing `just phase6-verify`, and updated `06-VALIDATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-01-PLAN.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-02-PLAN.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-03-PLAN.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-04-PLAN.md
@tools/bazel/phase6_verify.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@tools/bazel/manifests/phase6_printing_core.json
@tools/bazel/manifests/phase6_safety_gates.json
@tools/bazel/manifests/phase6_feature_gates.json
@tools/bazel/manifests/phase6_concern_dispositions.json
@rust/crates/domain/src/lib.rs
@rust/crates/domain/src/print.rs
@rust/crates/domain/src/safety.rs
@rust/crates/domain/src/feature.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden aggregate verifier for Rust API shape and scope claims</name>
  <read_first>
    tools/bazel/phase6_verify.py
    tools/bazel/BUILD.bazel
    tools/bazel/rust_workflow.sh
    BUILD.bazel
    justfile
    tools/bazel/manifests/phase6_printing_core.json
    tools/bazel/manifests/phase6_safety_gates.json
    tools/bazel/manifests/phase6_feature_gates.json
    tools/bazel/manifests/phase6_concern_dispositions.json
    rust/crates/domain/src/lib.rs
    rust/crates/domain/src/print.rs
    rust/crates/domain/src/safety.rs
    rust/crates/domain/src/feature.rs
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
  </read_first>
  <files>tools/bazel/phase6_verify.py</files>
  <behavior>
    - Test 1: `--quick` fails if `rust/crates/domain/src/print.rs` lacks `FixtureId`, `PrintJobState`, `PrintSource`, `PrintCommand`, `PlannerFlowState`, `CommandRoute`, `route_gcode_mnemonic`, or `transition_print_state`.
    - Test 2: `--quick` fails if `rust/crates/domain/src/safety.rs` lacks `SafetyFlow`, `SafetyAction`, `EvidenceClass`, `FatalPathPolicy`, `SafetyPolicySurface`, or `classify_safety_flow`.
    - Test 3: `--quick` fails if `rust/crates/domain/src/feature.rs` lacks `Phase6FeatureGate`, `Phase6FeatureGates`, `BurstSteppingMode`, `GateState`, `HasLoadcellHx717`, or `OutOfScopePhase10`.
    - Test 4: `--quick` fails if any Phase 6 manifest or summary contains local hardware overclaim strings or out-of-scope implementation claims.
    - Test 5: `--all` runs quick checks plus Cargo format, clippy, build, and tests.
  </behavior>
  <action>Extend `tools/bazel/phase6_verify.py` with functions named `check_rust_api_surface`, `check_bazel_surface`, `check_just_surface`, `check_no_phase6_overclaim`, and `check_validation_contract`. `check_rust_api_surface` must require exact files `rust/crates/domain/src/print.rs`, `rust/crates/domain/src/safety.rs`, and `rust/crates/domain/src/feature.rs`, and must require the strings named in the behavior tests. It must also require `#![forbid(unsafe_code)]` in `rust/crates/domain/src/lib.rs` and reject any Rust unsafe operation in `rust/crates/domain/src/print.rs`, `safety.rs`, or `feature.rs` after stripping comments and strings using the Phase 5 scanner pattern. `check_bazel_surface` must require `phase6_verify` in root `BUILD.bazel`, `tools/bazel/BUILD.bazel`, and `tools/bazel/rust_workflow.sh`, and must require the exact workflow command `python3 tools/bazel/phase6_verify.py --all`. `check_just_surface` must require `phase6-verify:` and `bazel run //tools/bazel:phase6_verify` in `justfile`. `check_no_phase6_overclaim` must scan Phase 6 manifests and `06-*-SUMMARY.md` files when present and fail on exact lowercase strings `hardware-safe`, `hardware passed`, `hardware verified locally`, `locally passed hardware`, `network parity implemented`, `auth implemented`, `crypto implemented`, `gui parity implemented`, `storage parity implemented`, `auxiliary runtime parity implemented`, and `cutover evidence complete`. `check_validation_contract` must require `.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md` to contain `Quick run command`, `python3 tools/bazel/phase6_verify.py --quick`, `Full suite command`, and `just phase6-verify`. `--quick` must run all static checks including manifests, source paths, lifecycle, concern dispositions, Rust API surface, Bazel/just surface, validation contract, and overclaim guard. `--all` must run quick checks plus `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`. Preserve `--printing-only`, `--safety-only`, `--features-only`, and `--concerns-only` modes.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase6_verify.py &amp;&amp; python3 tools/bazel/phase6_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile tools/bazel/phase6_verify.py` exits 0.
    - `python3 tools/bazel/phase6_verify.py --quick` exits 0 and prints `Phase 6 printing core safety and feature gate verification passed`.
    - `rg "check_rust_api_surface|check_bazel_surface|check_just_surface|check_no_phase6_overclaim|check_validation_contract" tools/bazel/phase6_verify.py` finds all five function names.
    - `rg "PrintJobState|SafetyPolicySurface|Phase6FeatureGates|OutOfScopePhase10" tools/bazel/phase6_verify.py` finds all four strings.
  </acceptance_criteria>
  <done>The Phase 6 verifier enforces Rust API shape, pure-domain unsafe posture, manifest coverage, facade wiring, validation contract, and phase-scope wording boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Nyquist validation sign-off</name>
  <read_first>
    .planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
    tools/bazel/phase6_verify.py
    justfile
    tools/bazel/BUILD.bazel
    BUILD.bazel
    rust/crates/domain/src/print.rs
    rust/crates/domain/src/safety.rs
    rust/crates/domain/src/feature.rs
  </read_first>
  <files>.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md</files>
  <action>After `python3 tools/bazel/phase6_verify.py --quick` passes, update `.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md` frontmatter to `nyquist_compliant: true` and `wave_0_complete: true`. In the Per-Task Verification Map, update Phase 6 local task statuses to `green` only for checks that actually passed during execution; leave non-local simulator, hardware-smoke, and manual-hardware-required rows as required evidence rather than passed. Add a `## Final Automated Evidence` section containing exact command lines and outcomes for `python3 tools/bazel/phase6_verify.py --quick`, `just phase6-verify`, `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`. Keep the Manual-Only Verifications table intact and do not mark hardware thermal, motion, watchdog, crash recovery, emergency stop, safe-output, full Marlin planner, TMC driver, auxiliary controller, or MMU runtime behavior as locally passed.</action>
  <verify>
    <automated>python3 tools/bazel/phase6_verify.py --quick &amp;&amp; just phase6-verify</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase6_verify.py --quick` exits 0.
    - `just phase6-verify` exits 0.
    - `rg "nyquist_compliant: true|wave_0_complete: true|Final Automated Evidence|just phase6-verify" .planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md` finds all four strings.
    - `rg "manual-hardware-required|simulator-flow|hardware-smoke" .planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md` still finds all three evidence classes.
  </acceptance_criteria>
  <done>Nyquist validation records Wave 0 completion, the aggregate local verifier has passed, and non-local safety evidence remains explicitly classified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 6 verifier -> Rust domain crate | Static validation must ensure required pure Rust API surfaces exist and remain unsafe-free. |
| Local phase gate -> non-local evidence | Aggregate local success must not imply hardware, simulator, GUI, storage, network, auxiliary, or cutover evidence has passed. |
| Validation artifact -> execution claims | `06-VALIDATION.md` can incorrectly mark non-local evidence green unless updated from actual command results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Rust API surface drift | mitigate | `check_rust_api_surface` must require exact print, safety, and feature type/function strings before quick verification passes. |
| T-06-05-02 | Elevation of privilege / Safety | Pure domain unsafe posture | mitigate | Verifier must require `#![forbid(unsafe_code)]` and reject unsafe syntax in Phase 6 domain modules. |
| T-06-05-03 | Repudiation | Hardware and later-phase overclaims | mitigate | `check_no_phase6_overclaim` must reject exact overclaim strings in manifests and summaries. |
| T-06-05-04 | Tampering | Developer facade drift | mitigate | `check_bazel_surface` and `check_just_surface` must require direct Bazel and `just` wiring to `phase6_verify`. |
| T-06-05-05 | Repudiation | Nyquist validation status | mitigate | Validation sign-off must list actual command outcomes and keep non-local evidence classes visible. |
</threat_model>

<verification>
- `python3 -m py_compile tools/bazel/phase6_verify.py`
- `python3 tools/bazel/phase6_verify.py --quick`
- `just phase6-verify`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
</verification>

<success_criteria>
Plan 05 succeeds when the hardened quick verifier passes, `just phase6-verify` passes, Rust fmt/clippy/build/tests pass through the aggregate gate, `06-VALIDATION.md` is Nyquist-compliant, and non-local evidence remains classified rather than locally passed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-printing-core-safety-and-feature-gates/06-05-SUMMARY.md`.
</output>
