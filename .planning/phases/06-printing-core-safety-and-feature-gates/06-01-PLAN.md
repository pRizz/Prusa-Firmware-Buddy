---
phase: 06-printing-core-safety-and-feature-gates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/bazel/phase6_verify.py
  - tools/bazel/manifests/phase6_printing_core.json
  - tools/bazel/manifests/phase6_safety_gates.json
  - tools/bazel/manifests/phase6_feature_gates.json
  - tools/bazel/manifests/phase6_concern_dispositions.json
  - BUILD.bazel
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05
requirements_addressed:
  - CORE-03
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "Developer can run Phase 6 manifest validation before pure Rust policy work depends on it."
    - "Printing, safety, feature-gate, and known-concern evidence rows carry requirement IDs, lifecycle ID, source paths, evidence classes, and explicit Rust surfaces."
    - "Known Phase 6 concerns are recorded as preserved behavior or intentional deltas before Rust behavior can be accepted."
    - "Phase 6 verification is reachable through direct Python, Bazel, and `just` commands."
  artifacts:
    - path: "tools/bazel/phase6_verify.py"
      provides: "Phase 6 verifier schema, lifecycle, source-path, evidence-class, and facade checks"
      contains: "Phase 6 printing core safety and feature gate verification passed"
    - path: "tools/bazel/manifests/phase6_printing_core.json"
      provides: "CORE-03 reference printing contract rows"
      contains: "print-gcode-routing"
    - path: "tools/bazel/manifests/phase6_safety_gates.json"
      provides: "CORE-04 safety and recovery evidence rows"
      contains: "fatal-redscreen-bsod-assert"
    - path: "tools/bazel/manifests/phase6_feature_gates.json"
      provides: "CORE-05 typed feature-gate coverage rows"
      contains: "phase-burst-stepping-gates"
    - path: "tools/bazel/manifests/phase6_concern_dispositions.json"
      provides: "Phase 6 known concern disposition rows"
      contains: "CL-007"
    - path: "justfile"
      provides: "Developer facade for aggregate Phase 6 verification"
      contains: "phase6-verify:"
  key_links:
    - from: "justfile"
      to: "//tools/bazel:phase6_verify"
      via: "phase6-verify recipe"
      pattern: "bazel run //tools/bazel:phase6_verify"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase6_verify.py"
      via: "phase6_verify case"
      pattern: "python3 tools/bazel/phase6_verify.py --all"
    - from: "tools/bazel/phase6_verify.py"
      to: "tools/bazel/manifests/phase6_printing_core.json"
      via: "manifest schema validation"
      pattern: "phase6_printing_core.json"
    - from: "tools/bazel/phase6_verify.py"
      to: "tools/bazel/manifests/phase6_safety_gates.json"
      via: "manifest schema validation"
      pattern: "phase6_safety_gates.json"
    - from: "tools/bazel/phase6_verify.py"
      to: "tools/bazel/manifests/phase6_feature_gates.json"
      via: "manifest schema validation"
      pattern: "phase6_feature_gates.json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-04T09-48-48
generated_at: 2026-06-04T10:06:26Z
---

<objective>
Create the Wave 0 validation foundation for Phase 6: a standard-library verifier, manifest schemas, required reference rows, concern dispositions, and Bazel/`just` entrypoints.

Purpose: Later Phase 6 tasks can add Rust print, safety, and feature-gate policy code against an executable evidence contract instead of prose-only expectations.
Output: `tools/bazel/phase6_verify.py`, four Phase 6 manifests, Bazel labels, root alias, workflow dispatch, and `just phase6-verify`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md
@tools/bazel/phase5_verify.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@ProjectOptions.cmake
@lib/AddMarlin.cmake
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 6 verifier schema gates</name>
  <read_first>
    tools/bazel/phase5_verify.py
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md
    AGENTS.md
    AGENTS.bright-builds.md
    standards-overrides.md
  </read_first>
  <files>tools/bazel/phase6_verify.py</files>
  <behavior>
    - Test 1: `--help` lists `--quick`, `--all`, `--manifests-only`, `--printing-only`, `--safety-only`, `--features-only`, and `--concerns-only`.
    - Test 2: `--manifests-only` fails with a readable missing-file error before manifests exist.
    - Test 3: each manifest mode validates `schema_version`, `phase`, `phase_lifecycle_id`, row uniqueness, requirement IDs, source paths, and allowed evidence classes.
    - Test 4: overclaim guard fails on `hardware-safe`, `hardware passed`, `hardware verified locally`, `locally passed hardware`, `network parity implemented`, `GUI parity implemented`, `storage parity implemented`, `auxiliary runtime parity implemented`, or `cutover evidence complete` inside Phase 6 manifests and execution summaries.
  </behavior>
  <action>Create `tools/bazel/phase6_verify.py` as a Python standard-library verifier modeled on `tools/bazel/phase5_verify.py`. Define `PHASE = "06-printing-core-safety-and-feature-gates"` and `PHASE_LIFECYCLE_ID = "6-2026-06-04T09-48-48"`. Define manifest paths for `tools/bazel/manifests/phase6_printing_core.json`, `phase6_safety_gates.json`, `phase6_feature_gates.json`, and `phase6_concern_dispositions.json`. Define allowed evidence classes exactly as `manifest-check`, `source-audit`, `static-source-audit`, `host-test`, `rust-host-test`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`. Define required printing fields exactly as `id`, `requirement`, `source_paths`, `reference_behavior`, `print_surface`, `evidence_class`, `rust_surface`, and `intentional_delta`. Define required safety fields exactly as `id`, `requirement`, `source_paths`, `safety_flow`, `preserved_behavior`, `evidence_class`, `rust_surface`, and `non_local_evidence`. Define required feature fields exactly as `id`, `requirement`, `source_paths`, `gate`, `profile_keys`, `expected_state`, `evidence_class`, and `rust_surface`. Define required concern fields exactly as `id`, `concern_id`, `requirement`, `source_paths`, `disposition`, `phase6_handling`, `evidence_class`, and `intentional_delta`. Add required row ID constants: printing rows `print-gcode-routing`, `print-serial-start-pause-resume-cancel`, `print-file-start-preview-stream-recovery`, `print-planner-visible-flow`, and `print-buddy-gmcode-handlers`; safety rows `thermal-safety-transitions`, `motion-safe-output-and-emergency-stop`, `selftest-calibration-crash-recovery`, `power-panic-recovery`, `fatal-redscreen-bsod-assert`, `watchdog-and-crash-dump-boundary`, and `probe-loadcell-classification`; feature rows `filament-sensor-gates`, `tmc-motion-driver-gates`, `precise-homing-gates`, `input-shaper-gates`, `phase-burst-stepping-gates`, `loadcell-hx717-gates`, `bed-chamber-door-gates`, `mmu2-gates`, `nfc-leds-gates`, and `toolchanger-xbuddy-extension-gates`; concern rows `concern-cl-007-probe-analysis`, `concern-cl-008-home-screen-flash-start`, `concern-cl-011-crash-dump-handling`, `concern-cl-014-rng-fallback`, `concern-cl-024-stm32g0-irq`, `concern-cl-002-mmu-reporting`, and `concern-tmc-motion-driver-retention`. Implement `--quick` as manifest, source-path, lifecycle, Bazel/just, and overclaim checks; implement `--all` as `--quick` plus Cargo format, clippy, build, and tests; implement manifest-only modes named in Test 1. Default to `--quick` when no mode is supplied. The success line must be exactly `Phase 6 printing core safety and feature gate verification passed`. Use no third-party Python packages.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase6_verify.py &amp;&amp; python3 tools/bazel/phase6_verify.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile tools/bazel/phase6_verify.py` exits 0.
    - `python3 tools/bazel/phase6_verify.py --help` output contains `--quick`, `--all`, `--manifests-only`, `--printing-only`, `--safety-only`, `--features-only`, and `--concerns-only`.
    - `rg "REQUIRED_PRINTING_ROW_IDS|REQUIRED_SAFETY_ROW_IDS|REQUIRED_FEATURE_ROW_IDS|REQUIRED_CONCERN_ROW_IDS|PHASE_LIFECYCLE_ID|Phase 6 printing core safety and feature gate verification passed" tools/bazel/phase6_verify.py` finds all six strings.
  </acceptance_criteria>
  <done>The verifier exists, compiles, exposes all Phase 6 validation modes, and has executable schema constants for CORE-03, CORE-04, CORE-05, and D-10 concern coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 6 evidence manifests</name>
  <read_first>
    tools/bazel/phase6_verify.py
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-CONCERN-LEDGER.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md
    .planning/codebase/CONCERNS.md
    ProjectOptions.cmake
    lib/AddMarlin.cmake
    src/common/marlin_server.cpp
    src/common/marlin_client.cpp
    src/common/marlin_server_request.hpp
    src/common/serial_printing.cpp
    src/marlin_stubs/gcode.cpp
    src/common/probe_analysis.cpp
    src/common/crash_dump/dump.cpp
    src/common/Pin.cpp
    src/mmu2/mmu2_reporting.cpp
  </read_first>
  <files>tools/bazel/manifests/phase6_printing_core.json, tools/bazel/manifests/phase6_safety_gates.json, tools/bazel/manifests/phase6_feature_gates.json, tools/bazel/manifests/phase6_concern_dispositions.json</files>
  <action>Create the four JSON manifests with top-level `schema_version: 1`, `phase: "06-printing-core-safety-and-feature-gates"`, and `phase_lifecycle_id: "6-2026-06-04T09-48-48"`. `phase6_printing_core.json` must contain a `printing_contracts` list with the five required row IDs from Task 1; every row must set `requirement: "CORE-03"`, use existing `source_paths`, and include reference paths for retained Marlin/Buddy behavior. Required source coverage: `lib/Marlin/`, `lib/AddMarlin.cmake`, `src/common/marlin_server.cpp`, `src/common/marlin_client.cpp`, `src/common/marlin_server_request.hpp`, `src/common/marlin_client_queue.hpp`, `src/common/marlin_vars.cpp`, `src/common/serial_printing.cpp`, `src/common/gcode/`, and `src/marlin_stubs/gcode.cpp`. `phase6_safety_gates.json` must contain a `safety_gates` list with the seven required row IDs from Task 1; every row must set `requirement: "CORE-04"` and use evidence classes from the allow-list. Required source coverage: `src/common/safe_state.cpp`, `src/common/feature/safety_timer/`, `src/common/power_panic.cpp`, `src/common/crash_dump/`, `src/common/feature/emergency_stop/`, `src/common/selftest/`, `src/common/probe_analysis.cpp`, `src/common/Pin.cpp`, `src/common/random_hw.cpp`, and `rust/crates/runtime-adapter/src/panic_boundary.rs`. `phase6_feature_gates.json` must contain a `feature_gates` list with the ten required row IDs from Task 1; every row must set `requirement: "CORE-05"` and derive `profile_keys` from `ProjectOptions.cmake`, `utils/presets/presets.json`, `lib/AddMarlin.cmake`, and Phase 1/5 evidence. Required gate coverage strings: `HAS_TRINAMIC`, `HAS_TMC_UART`, `HAS_PRECISE_HOMING`, `HAS_PRECISE_HOMING_COREXY`, `HAS_INPUT_SHAPER_CALIBRATION`, `HAS_PHASE_STEPPING`, `HAS_BURST_STEPPING`, `HAS_LOADCELL`, `HAS_LOADCELL_HX717`, `HAS_LOCAL_BED`, `HAS_MODULAR_BED`, `HAS_REMOTE_BED`, `HAS_CHAMBER_API`, `HAS_DOOR_SENSOR`, `HAS_MMU2`, `HAS_NFC`, `HAS_LEDS`, `HAS_SIDE_LEDS`, `HAS_TOOLCHANGER`, and `HAS_XBUDDY_EXTENSION`. `phase6_concern_dispositions.json` must contain a `concerns` list with the seven required concern row IDs from Task 1. Use `concern_id` values `CL-007`, `CL-008`, `CL-011`, `CL-014`, `CL-024`, `CL-002`, and `phase6-tmc-motion-driver-retention`. For `CL-007`, set `disposition: "preserve-temporarily"` and reference `src/common/probe_analysis.cpp`. For `CL-011`, reference `src/common/crash_dump/dump.cpp` and `src/common/crash_dump/crash_dump_distribute.cpp`, set `evidence_class: "manual-hardware-required"` or `source-audit`, and state that crash dump handling preserves the Phase 5 panic boundary. For `CL-014`, reference `src/common/random_hw.cpp` and `src/connect/tls/hardware_rng.cpp` but do not implement Phase 9 network or crypto behavior. For `CL-002`, record only MMU gate facts needed by CORE-05 and state that MMU runtime parity remains Phase 10. For `phase6-tmc-motion-driver-retention`, reference `lib/TMCStepper/` and `lib/AddTMCStepper.cmake` and set the handling to retained reference contract. Do not add GUI workflow, persistence, network/auth, transfer, auxiliary runtime, or cutover-evidence implementation claims.</action>
  <verify>
    <automated>python3 tools/bazel/phase6_verify.py --manifests-only</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase6_verify.py --manifests-only` exits 0.
    - `rg "CORE-03|print-gcode-routing|print-serial-start-pause-resume-cancel|print-file-start-preview-stream-recovery|src/common/serial_printing.cpp" tools/bazel/manifests/phase6_printing_core.json` finds all five strings.
    - `rg "CORE-04|fatal-redscreen-bsod-assert|watchdog-and-crash-dump-boundary|manual-hardware-required|src/common/probe_analysis.cpp" tools/bazel/manifests/phase6_safety_gates.json` finds all five strings.
    - `rg "CORE-05|phase-burst-stepping-gates|HAS_LOADCELL_HX717|HAS_TOOLCHANGER|HAS_XBUDDY_EXTENSION" tools/bazel/manifests/phase6_feature_gates.json` finds all five strings.
    - `rg "CL-007|CL-011|CL-014|CL-024|CL-002|phase6-tmc-motion-driver-retention" tools/bazel/manifests/phase6_concern_dispositions.json` finds all six strings.
  </acceptance_criteria>
  <done>All Phase 6 evidence manifests exist, carry lifecycle metadata, cover CORE-03/04/05, include D-10 known concerns, and pass manifest-only validation.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 6 Bazel and just verification</name>
  <read_first>
    tools/bazel/BUILD.bazel
    BUILD.bazel
    tools/bazel/rust_workflow.sh
    justfile
    tools/bazel/shell_rules.bzl
    tools/bazel/phase6_verify.py
    tools/bazel/manifests/phase6_printing_core.json
    tools/bazel/manifests/phase6_safety_gates.json
    tools/bazel/manifests/phase6_feature_gates.json
    tools/bazel/manifests/phase6_concern_dispositions.json
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
  </read_first>
  <files>BUILD.bazel, tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, justfile</files>
  <action>In root `BUILD.bazel`, add `filegroup(name = "phase6_printing_safety_docs", srcs = [".planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md", ".planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md", ".planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md"])` and add `alias(name = "phase6_verify", actual = "//tools/bazel:phase6_verify")`. In `tools/bazel/BUILD.bazel`, add `shell_binary(name = "phase6_verify", src = "rust_workflow.sh", data = ["phase6_verify.py", "manifests/phase6_printing_core.json", "manifests/phase6_safety_gates.json", "manifests/phase6_feature_gates.json", "manifests/phase6_concern_dispositions.json", "//:phase6_printing_safety_docs", "//:rust_workspace_sources"])`. In `tools/bazel/rust_workflow.sh`, add a `phase6_verify)` case that runs exactly `python3 tools/bazel/phase6_verify.py --all`. In `justfile`, add `phase6-verify:` with exactly `bazel run //tools/bazel:phase6_verify`. Preserve existing Phase 2 through Phase 5 labels and recipes.</action>
  <verify>
    <automated>python3 tools/bazel/phase6_verify.py --quick &amp;&amp; bazel query "//tools/bazel:phase6_verify + //:phase6_verify" &amp;&amp; just --list</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase6_verify.py --quick` exits 0 and prints `Phase 6 printing core safety and feature gate verification passed`.
    - `bazel query "//tools/bazel:phase6_verify + //:phase6_verify"` exits 0 and prints both labels.
    - `just --list` output contains `phase6-verify`.
    - `rg "phase6_verify|phase6_printing_safety_docs" BUILD.bazel tools/bazel/BUILD.bazel tools/bazel/rust_workflow.sh` finds both strings.
    - `rg "phase6-verify:|bazel run //tools/bazel:phase6_verify" justfile` finds both strings.
  </acceptance_criteria>
  <done>Phase 6 validation is reachable through direct Python, Bazel, and `just`, and Wave 0 quick verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest JSON -> verifier | Repo-local JSON rows are data inputs that can omit source paths, requirements, or evidence classes unless checked. |
| Verifier -> reference source tree | Static checks must validate existing retained source paths without interpreting C/C++ behavior as locally proven hardware behavior. |
| Bazel/just facade -> verifier | Developer-facing commands must execute the same aggregate verifier as direct Python invocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `phase6_printing_core.json` | mitigate | `phase6_verify.py` must require schema version, lifecycle ID, CORE-03 rows, existing source paths, and allowed evidence classes. |
| T-06-01-02 | Tampering / Safety | `phase6_safety_gates.json` | mitigate | Safety rows must include CORE-04, named flows, non-local evidence fields, and source paths for fatal, crash, probe, power-panic, and emergency references. |
| T-06-01-03 | Tampering / Safety | `phase6_feature_gates.json` | mitigate | Feature rows must include CORE-05 and exact reference gate strings from `ProjectOptions.cmake` and `lib/AddMarlin.cmake`. |
| T-06-01-04 | Repudiation | Evidence classification | mitigate | Overclaim guard must reject local hardware-pass wording and out-of-phase GUI/storage/network/auxiliary/cutover implementation claims. |
| T-06-01-05 | Information Disclosure | Crash dump concern row | mitigate | `CL-011` row must preserve Phase 5 crash-dump boundary, avoid crash memory contents, and classify crash evidence as source-audit or non-local. |
| T-06-01-06 | Spoofing / Tampering | Bazel and just facade | mitigate | Bazel query and `just --list` acceptance criteria must prove `phase6_verify` and `phase6-verify` route to the same verifier. |
</threat_model>

<verification>
- `python3 -m py_compile tools/bazel/phase6_verify.py`
- `python3 tools/bazel/phase6_verify.py --manifests-only`
- `python3 tools/bazel/phase6_verify.py --quick`
- `bazel query "//tools/bazel:phase6_verify + //:phase6_verify"`
- `just --list`
</verification>

<success_criteria>
Wave 0 succeeds when Phase 6 verifier infrastructure exists, all four manifests pass schema/source/evidence validation, required concern dispositions are present, Bazel and `just` expose the verifier, and quick validation does not overclaim simulator or hardware evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-printing-core-safety-and-feature-gates/06-01-SUMMARY.md`.
</output>
