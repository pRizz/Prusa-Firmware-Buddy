---
phase: 06-printing-core-safety-and-feature-gates
plan: 04
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - rust/crates/domain/src/feature.rs
  - tools/bazel/manifests/phase6_feature_gates.json
autonomous: true
requirements:
  - CORE-05
requirements_addressed:
  - CORE-05
must_haves:
  truths:
    - "Developer can derive Phase 6 feature-gate facts from validated `ProductProfile` values."
    - "Unsupported printer, board, or feature combinations are rejected or reported as disabled instead of accepted through unchecked primitives."
    - "Filament sensors, TMC paths, precise homing, input shaper, phase/burst stepping, loadcell/HX717, beds, chamber, door, MMU2, NFC, LEDs, toolchanger, and xBuddy Extension gates are all represented."
    - "Feature-gate rows remain scoped to printing and safety facts and do not implement auxiliary runtime parity."
  artifacts:
    - path: "rust/crates/domain/src/feature.rs"
      provides: "ProductProfile-keyed Phase 6 feature-gate model and tests"
      contains: "Phase6FeatureGate"
    - path: "tools/bazel/manifests/phase6_feature_gates.json"
      provides: "CORE-05 source-backed feature-gate rows"
      contains: "toolchanger-xbuddy-extension-gates"
  key_links:
    - from: "rust/crates/domain/src/feature.rs"
      to: "rust/crates/domain/src/product.rs"
      via: "ProductProfile-keyed gate construction"
      pattern: "ProductProfile"
    - from: "tools/bazel/manifests/phase6_feature_gates.json"
      to: "ProjectOptions.cmake"
      via: "source_paths and expected gate names"
      pattern: "ProjectOptions.cmake"
    - from: "tools/bazel/manifests/phase6_feature_gates.json"
      to: "rust/crates/domain/src/feature.rs"
      via: "rust_surface field"
      pattern: "Phase6FeatureGates"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-04T09-48-48
generated_at: 2026-06-04T10:06:26Z
---

<objective>
Extend the Rust feature model with Phase 6 printer-specific gate facts for CORE-05 and bind those facts to reference CMake/preset/source-selection rows.

Purpose: Printing and safety policy code can consume typed feature availability keyed by validated product profiles rather than freehand duplicated gate booleans, per D-07 through D-09.
Output: `Phase6FeatureGate`/`Phase6FeatureGates` Rust policy data, tests, and updated feature-gate manifest rows.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-01-PLAN.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-FOREIGN-CODE-INVENTORY.md
@tools/bazel/manifests/phase6_feature_gates.json
@tools/bazel/manifests/phase6_concern_dispositions.json
@tools/bazel/phase6_verify.py
@rust/crates/domain/src/feature.rs
@rust/crates/domain/src/product.rs
@ProjectOptions.cmake
@utils/presets/presets.json
@lib/AddMarlin.cmake
@lib/AddTMCStepper.cmake
@lib/AddMMU2.cmake
</context>

<interfaces>
From `rust/crates/domain/src/product.rs`:
```rust
pub enum PrinterKind { CoreOne, Mini, Mk4, Mk35, Xl, Ix, XlDevKit }
pub enum BoardKind { Buddy, XBuddy, XlBuddy, Dwarf, ModularBed, XlDevKitXlBuddy, XBuddyExtension }
pub struct ProductProfile;
impl ProductProfile {
    pub fn printer(&self) -> PrinterKind;
    pub fn board(&self) -> BoardKind;
    pub fn features(&self) -> &FeatureSet;
    pub fn is_auxiliary(&self) -> bool;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProductProfile-keyed Phase 6 gate facts</name>
  <read_first>
    rust/crates/domain/src/feature.rs
    rust/crates/domain/src/product.rs
    tools/bazel/manifests/phase6_feature_gates.json
    tools/bazel/manifests/phase6_concern_dispositions.json
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    ProjectOptions.cmake
    utils/presets/presets.json
    lib/AddMarlin.cmake
    lib/AddTMCStepper.cmake
    lib/AddMMU2.cmake
  </read_first>
  <files>rust/crates/domain/src/feature.rs</files>
  <behavior>
    - Test 1: CORE One xBuddy master profile enables `HasXBuddyExtension`, `HasDoorSensor`, `HasChamberApi`, `HasNfc`, `HasMmu2`, `HasLeds`, and `HasEmergencyStop`.
    - Test 2: MINI Buddy master profile enables `HasFilamentSensorBinary`, `HasTmcUart`, `HasLocalBed`, `HasSerialPrint`, and disables `HasMmu2`.
    - Test 3: XL XLBuddy master profile enables `HasToolchanger`, `HasModularBed`, `HasRemoteBed`, `HasPhaseStepping`, `HasLoadcell`, `HasChamberApi`, and disables `HasNfc`.
    - Test 4: xBuddy Extension auxiliary profile exposes only `HasXBuddyExtension` gate facts needed by Phase 6 and reports behavior parity as out of scope for Phase 10.
    - Test 5: burst stepping is enabled only when `BurstSteppingMode::Enabled` is supplied and the printer is XL, MK4, iX, or CORE One on a master board.
    - Test 6: HX717 loadcell gate is enabled for `BoardKind::Dwarf` and for `BoardKind::XBuddy` except MK3.5; it is disabled for MK3.5 xBuddy.
  </behavior>
  <action>Extend `rust/crates/domain/src/feature.rs` with Phase 6 gate types without adding dependencies. Add `Phase6FeatureGate` enum variants exactly named `HasFilamentSensorBinary`, `HasFilamentSensorAdc`, `HasSideFilamentSensor`, `HasTrinamic`, `HasTmcUart`, `HasPreciseHoming`, `HasPreciseHomingCorexy`, `HasInputShaperCalibration`, `HasPhaseStepping`, `HasPhaseSteppingCalibration`, `HasBurstStepping`, `HasLoadcell`, `HasLoadcellHx717`, `HasLocalBed`, `HasModularBed`, `HasRemoteBed`, `HasChamberApi`, `HasChamberFiltrationApi`, `HasDoorSensor`, `HasMmu2`, `HasNfc`, `HasLeds`, `HasSideLeds`, `HasToolchanger`, `HasXBuddyExtension`, `HasSerialPrint`, and `HasEmergencyStop`. Add `GateState::{Enabled, Disabled, OutOfScopePhase10}` and `BurstSteppingMode::{Enabled, Disabled}`. Add `Phase6FeatureGates` with constructor `Phase6FeatureGates::from_profile(profile: &ProductProfile, burst_stepping_mode: BurstSteppingMode) -> Self` and method `gate_state(&self, gate: Phase6FeatureGate) -> GateState`. Encode exact gate sets from `ProjectOptions.cmake`: filament binary `MINI`, `MK3.5`; filament ADC `MK4`, `XL`, `iX`, `XL_DEV_KIT`, `COREONE`; `HAS_TRINAMIC` for all supported master printers; `HAS_TMC_UART` only `MINI`; precise homing `MK4`, `MK3.5`; precise homing CoreXY `iX`, `XL`, `XL_DEV_KIT`, `COREONE`; input shaper calibration `MK4`, `MK3.5`, `XL`, `XL_DEV_KIT`, `COREONE`; phase stepping `XL`, `iX`, `COREONE`; phase stepping calibration `XL`, `iX`, `COREONE`; burst stepping only `XL`, `MK4`, `iX`, `COREONE` when `BurstSteppingMode::Enabled`; loadcell `MK4`, `iX`, `XL`, `XL_DEV_KIT`, `COREONE` on master boards; HX717 for `BoardKind::Dwarf` and `BoardKind::XBuddy` except MK3.5; local bed `COREONE`, `MINI`, `MK4`, `MK3.5`; modular/remote bed `iX`, `XL`, `XL_DEV_KIT`; chamber API `XL`, `COREONE`; door sensor `COREONE`, `MK4`; MMU2 `MK4`, `MK3.5`, `COREONE`; NFC `MK3.5`, `MK4`, `COREONE`; LEDs `MK4`, `MK3.5`, `XL`, `iX`, `COREONE`; side LEDs `XL`, `iX`, `COREONE`; toolchanger `XL`, `XL_DEV_KIT`; xBuddy Extension `COREONE`; serial print `MK4`, `MK3.5`, `XL`, `iX`, `MINI`, `COREONE`; emergency stop `COREONE`. For auxiliary boards, expose only gate facts needed by printing/safety and return `OutOfScopePhase10` for auxiliary behavior gates rather than accepting runtime parity. Use `ProductProfile` accessors instead of raw strings.</action>
  <verify>
    <automated>cargo test --all-features -p buddy-domain feature</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --all-features -p buddy-domain feature` exits 0.
    - `rg "Phase6FeatureGate|Phase6FeatureGates|BurstSteppingMode|GateState|HasLoadcellHx717|OutOfScopePhase10" rust/crates/domain/src/feature.rs` finds all six strings.
    - `rg "HasXBuddyExtension|HasToolchanger|HasMmu2|HasNfc|HasTmcUart|HasBurstStepping" rust/crates/domain/src/feature.rs` finds all six strings.
    - `rg "// Arrange|// Act|// Assert" rust/crates/domain/src/feature.rs` finds all three test-section markers.
  </acceptance_criteria>
  <done>Phase 6 feature gates are represented as tested, ProductProfile-keyed Rust policy data with exact reference-derived printer and board sets.</done>
</task>

<task type="auto">
  <name>Task 2: Bind feature gates to reference rows</name>
  <read_first>
    tools/bazel/manifests/phase6_feature_gates.json
    tools/bazel/manifests/phase6_concern_dispositions.json
    tools/bazel/phase6_verify.py
    rust/crates/domain/src/feature.rs
    rust/crates/domain/src/product.rs
    ProjectOptions.cmake
    utils/presets/presets.json
    lib/AddMarlin.cmake
    lib/AddTMCStepper.cmake
    lib/AddMMU2.cmake
  </read_first>
  <files>tools/bazel/manifests/phase6_feature_gates.json</files>
  <action>Update each `feature_gates` row in `tools/bazel/manifests/phase6_feature_gates.json` so `rust_surface` names the exact Rust gate type or constructor that implements the local policy claim. Use these exact pairings: `filament-sensor-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasFilamentSensorBinary`; `tmc-motion-driver-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasTrinamic`; `precise-homing-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasPreciseHoming`; `input-shaper-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasInputShaperCalibration`; `phase-burst-stepping-gates` -> `rust/crates/domain/src/feature.rs::BurstSteppingMode`; `loadcell-hx717-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasLoadcellHx717`; `bed-chamber-door-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasChamberApi`; `mmu2-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasMmu2`; `nfc-leds-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasNfc`; `toolchanger-xbuddy-extension-gates` -> `rust/crates/domain/src/feature.rs::Phase6FeatureGate::HasXBuddyExtension`. Keep `ProjectOptions.cmake`, `utils/presets/presets.json`, and `lib/AddMarlin.cmake` in source paths. The `mmu2-gates` row must reference `CL-002` through concern disposition linkage and state gate facts only. The TMC row must reference `lib/TMCStepper/`, `lib/AddTMCStepper.cmake`, and retained reference contract handling. Do not add Modbus, MMU runtime, toolchanger runtime, puppy update, or auxiliary protocol behavior parity to Phase 6 rows.</action>
  <verify>
    <automated>python3 tools/bazel/phase6_verify.py --features-only &amp;&amp; cargo test --all-features -p buddy-domain feature</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase6_verify.py --features-only` exits 0.
    - `cargo test --all-features -p buddy-domain feature` exits 0.
    - `rg "Phase6FeatureGate::HasFilamentSensorBinary|Phase6FeatureGate::HasTrinamic|BurstSteppingMode|Phase6FeatureGate::HasLoadcellHx717|Phase6FeatureGate::HasXBuddyExtension" tools/bazel/manifests/phase6_feature_gates.json` finds all five strings.
    - `rg "ProjectOptions.cmake|utils/presets/presets.json|lib/AddMarlin.cmake|lib/AddTMCStepper.cmake|CL-002" tools/bazel/manifests/phase6_feature_gates.json` finds all five strings.
  </acceptance_criteria>
  <done>CORE-05 manifest rows identify exact Rust gate surfaces, reference-derived source paths, and Phase 6 scope boundaries for MMU/TMC/toolchanger/xBuddy Extension gate facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProductProfile -> feature gates | Validated product identity enters gate construction; raw printer strings must not drive policy decisions. |
| Reference CMake/preset data -> Rust gate data | Gate facts must preserve reference source-selection behavior without freehand drift. |
| Feature gate facts -> auxiliary runtime scope | Phase 6 gate facts must not claim Modbus, MMU runtime, toolchanger runtime, puppy update, or auxiliary protocol parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering / Safety | Feature gate construction | mitigate | `Phase6FeatureGates::from_profile` must use validated `ProductProfile` accessors and tested exact printer/board sets. |
| T-06-04-02 | Tampering | Reference gate drift | mitigate | Manifest rows must cite `ProjectOptions.cmake`, `utils/presets/presets.json`, and `lib/AddMarlin.cmake` plus exact `HAS_*` gate strings. |
| T-06-04-03 | Denial of service / Safety | Unsupported combinations | mitigate | Auxiliary and unsupported behavior gates return `Disabled` or `OutOfScopePhase10`; they are not accepted as runtime parity. |
| T-06-04-04 | Repudiation | MMU and TMC retained behavior | mitigate | `mmu2-gates` and TMC rows must link `CL-002` or retained TMC reference handling instead of silently changing behavior. |
| T-06-04-05 | Tampering / Safety | Burst stepping override | mitigate | `HasBurstStepping` requires both supported printer set and explicit `BurstSteppingMode::Enabled`. |
</threat_model>

<verification>
- `cargo test --all-features -p buddy-domain feature`
- `python3 tools/bazel/phase6_verify.py --features-only`
- `python3 tools/bazel/phase6_verify.py --quick`
</verification>

<success_criteria>
Plan 04 succeeds when CORE-05 has tested ProductProfile-keyed gate facts, all required Phase 6 feature groups are represented, gate data is tied to reference source paths, and auxiliary/MMU/toolchanger behavior scope stays outside Phase 6 except for printing/safety gate facts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-printing-core-safety-and-feature-gates/06-04-SUMMARY.md`.
</output>
