---
phase: 06-printing-core-safety-and-feature-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - rust/crates/domain/src/lib.rs
  - rust/crates/domain/src/safety.rs
  - tools/bazel/manifests/phase6_safety_gates.json
autonomous: true
requirements:
  - CORE-04
requirements_addressed:
  - CORE-04
must_haves:
  truths:
    - "Developer can name each Phase 6 thermal, motion, selftest, calibration, crash, power panic, emergency, fatal, watchdog, and recovery safety surface."
    - "Developer can distinguish locally testable safety classification from simulator, hardware-smoke, and manual-hardware-required evidence."
    - "Fatal and crash-dump policies preserve Phase 5 panic/watchdog/crash-dump boundary contracts."
    - "Safety claims are source-backed and do not locally pass hardware behavior by wording alone."
  artifacts:
    - path: "rust/crates/domain/src/safety.rs"
      provides: "Typed safety flow, action, evidence, and fatal-path policy surfaces"
      contains: "SafetyPolicySurface"
    - path: "rust/crates/domain/src/lib.rs"
      provides: "Public domain exports for safety policy types"
      contains: "pub mod safety"
    - path: "tools/bazel/manifests/phase6_safety_gates.json"
      provides: "CORE-04 source-backed safety evidence rows"
      contains: "watchdog-and-crash-dump-boundary"
  key_links:
    - from: "rust/crates/domain/src/lib.rs"
      to: "rust/crates/domain/src/safety.rs"
      via: "public module export"
      pattern: "pub mod safety"
    - from: "tools/bazel/manifests/phase6_safety_gates.json"
      to: "rust/crates/runtime-adapter/src/panic_boundary.rs"
      via: "Phase 5 fatal boundary source path"
      pattern: "panic_boundary.rs"
    - from: "tools/bazel/manifests/phase6_safety_gates.json"
      to: "rust/crates/domain/src/safety.rs"
      via: "rust_surface field"
      pattern: "SafetyPolicySurface"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-04T09-48-48
generated_at: 2026-06-04T10:06:26Z
---

<objective>
Add typed Rust safety and recovery policy surfaces for CORE-04 and bind them to evidence-classed retained source references.

Purpose: The Rust port can test safety classifications locally while preserving a hard boundary between pure decisions and HAL/RTOS/fatal-path effects per D-04 through D-06.
Output: `rust/crates/domain/src/safety.rs`, domain exports, focused unit tests, and updated safety manifest rows.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-01-PLAN.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
@.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
@tools/bazel/manifests/phase6_safety_gates.json
@tools/bazel/manifests/phase6_concern_dispositions.json
@tools/bazel/phase6_verify.py
@rust/crates/domain/src/lib.rs
@rust/crates/runtime-adapter/src/panic_boundary.rs
@src/common/safe_state.cpp
@src/common/feature/safety_timer/
@src/common/power_panic.cpp
@src/common/crash_dump/dump.cpp
@src/common/crash_dump/crash_dump_distribute.cpp
@src/common/feature/emergency_stop/
@src/common/selftest/
@src/common/probe_analysis.cpp
@src/common/Pin.cpp
@src/common/random_hw.cpp
</context>

<interfaces>
From `.planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md`:
```text
Evidence classes: source-audit, host-test, simulator-flow, hardware-smoke, manual-hardware-required
```

From `.planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md`:
```text
panic-bsod-assert-boundary
crash-dump-memory-boundary
watchdog-boundary
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add safety flow and evidence policy model</name>
  <read_first>
    rust/crates/domain/src/lib.rs
    tools/bazel/manifests/phase6_safety_gates.json
    tools/bazel/manifests/phase6_concern_dispositions.json
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-SAFETY-ENVELOPE.md
    .planning/phases/05-foreign-code-unsafe-and-runtime-boundary/05-UNSAFE-BOUNDARY-AUDIT.md
    rust/crates/runtime-adapter/src/panic_boundary.rs
    src/common/safe_state.cpp
    src/common/feature/safety_timer/
    src/common/power_panic.cpp
    src/common/crash_dump/dump.cpp
    src/common/feature/emergency_stop/
    src/common/selftest/
    src/common/probe_analysis.cpp
    src/common/Pin.cpp
    src/common/random_hw.cpp
  </read_first>
  <files>rust/crates/domain/src/lib.rs, rust/crates/domain/src/safety.rs</files>
  <behavior>
    - Test 1: every `SafetyFlow` variant maps to a `SafetyPolicySurface` with a non-empty retained source path list.
    - Test 2: `SafetyFlow::FatalBoundary` maps to `SafetyAction::EnterFatalBoundary`, `allows_allocation: false`, and audit surface IDs `panic-bsod-assert-boundary`, `crash-dump-memory-boundary`, and `watchdog-boundary`.
    - Test 3: `SafetyFlow::CrashDump` and `SafetyFlow::Watchdog` classify evidence as non-local and do not return `EvidenceClass::RustHostTest`.
    - Test 4: `SafetyFlow::ProbeLoadcellClassification` includes `CL-007` and preserves current classifier behavior unless an intentional delta row is present.
    - Test 5: `SafetyFlow::EmergencyStop` maps to `SafetyAction::EnterSafeOutput` or `SafetyAction::DisableMotionAndHeat` and carries retained emergency-stop source paths.
  </behavior>
  <action>Create `rust/crates/domain/src/safety.rs` with safe pure Rust types only; keep `#![forbid(unsafe_code)]` in `rust/crates/domain/src/lib.rs`. Add `pub mod safety` and public re-exports for `EvidenceClass`, `FatalPathPolicy`, `SafetyAction`, `SafetyFlow`, `SafetyPolicySurface`, and `classify_safety_flow`. Implement `EvidenceClass::{ManifestCheck, SourceAudit, StaticSourceAudit, HostTest, RustHostTest, SimulatorFlow, HardwareSmoke, ManualHardwareRequired}`. Implement `SafetyFlow::{ThermalTransition, MotionSafeOutput, Selftest, Calibration, CrashDetection, PowerPanic, EmergencyStop, FatalBoundary, CrashDump, Watchdog, Recovery, ProbeLoadcellClassification}`. Implement `SafetyAction::{ContinueMonitoring, HoldMotion, DisableHeaters, DisableMotors, DisableMotionAndHeat, EnterSafeOutput, RequestRecovery, EnterFatalBoundary}`. Implement `FatalPathPolicy` with fields `allows_allocation: bool`, `preserves_crash_evidence: bool`, and `audit_surface_ids: &'static [&'static str]`; `classify_safety_flow(SafetyFlow::FatalBoundary)` must set `allows_allocation: false` and include audit surface IDs `panic-bsod-assert-boundary`, `crash-dump-memory-boundary`, and `watchdog-boundary` per D-06. `SafetyPolicySurface` must include `flow`, `action`, `evidence_class`, `source_paths`, `maybe_concern_id`, and `maybe_fatal_path_policy`; use `maybe_` names for optional fields. Do not allocate dynamically inside policy construction; use static slices for source paths and audit surface IDs. Do not call retained C/C++ functions, HAL, RTOS, watchdog, crash dump, or safe-output effects from this pure module.</action>
  <verify>
    <automated>cargo test --all-features -p buddy-domain safety</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --all-features -p buddy-domain safety` exits 0.
    - `rg "pub mod safety|SafetyFlow|SafetyAction|EvidenceClass|FatalPathPolicy|classify_safety_flow" rust/crates/domain/src/lib.rs rust/crates/domain/src/safety.rs` finds all six strings.
    - `rg "panic-bsod-assert-boundary|crash-dump-memory-boundary|watchdog-boundary|allows_allocation: false|CL-007" rust/crates/domain/src/safety.rs` finds all five strings.
    - `rg "// Arrange|// Act|// Assert" rust/crates/domain/src/safety.rs` finds all three test-section markers.
  </acceptance_criteria>
  <done>Safety flows, actions, evidence classes, fatal-path constraints, and known concern links are represented as tested Rust domain policy data.</done>
</task>

<task type="auto">
  <name>Task 2: Bind safety policies to evidence rows</name>
  <read_first>
    tools/bazel/manifests/phase6_safety_gates.json
    tools/bazel/manifests/phase6_concern_dispositions.json
    tools/bazel/phase6_verify.py
    rust/crates/domain/src/safety.rs
    rust/crates/runtime-adapter/src/panic_boundary.rs
    src/common/safe_state.cpp
    src/common/feature/safety_timer/
    src/common/power_panic.cpp
    src/common/crash_dump/dump.cpp
    src/common/crash_dump/crash_dump_distribute.cpp
    src/common/feature/emergency_stop/
    src/common/selftest/
    src/common/probe_analysis.cpp
    src/common/Pin.cpp
    src/common/random_hw.cpp
  </read_first>
  <files>tools/bazel/manifests/phase6_safety_gates.json</files>
  <action>Update each `safety_gates` row in `tools/bazel/manifests/phase6_safety_gates.json` so `rust_surface` names the exact Rust type or function that implements the local policy claim. Use these exact pairings: `thermal-safety-transitions` -> `rust/crates/domain/src/safety.rs::SafetyFlow::ThermalTransition`; `motion-safe-output-and-emergency-stop` -> `rust/crates/domain/src/safety.rs::SafetyAction::EnterSafeOutput`; `selftest-calibration-crash-recovery` -> `rust/crates/domain/src/safety.rs::SafetyFlow::Selftest`; `power-panic-recovery` -> `rust/crates/domain/src/safety.rs::SafetyFlow::PowerPanic`; `fatal-redscreen-bsod-assert` -> `rust/crates/domain/src/safety.rs::FatalPathPolicy`; `watchdog-and-crash-dump-boundary` -> `rust/crates/domain/src/safety.rs::SafetyFlow::CrashDump`; `probe-loadcell-classification` -> `rust/crates/domain/src/safety.rs::SafetyFlow::ProbeLoadcellClassification`. Keep hardware, watchdog, crash-dump, and safe-output effects classified as `simulator-flow`, `hardware-smoke`, or `manual-hardware-required` where Phase 1/5 evidence requires it; do not describe those effects as locally passed. Ensure the `probe-loadcell-classification` row references `CL-007`, `src/common/probe_analysis.cpp`, and preserve-temporarily behavior unless an intentional delta row exists.</action>
  <verify>
    <automated>python3 tools/bazel/phase6_verify.py --safety-only &amp;&amp; cargo test --all-features -p buddy-domain safety</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase6_verify.py --safety-only` exits 0.
    - `cargo test --all-features -p buddy-domain safety` exits 0.
    - `rg "SafetyFlow::ThermalTransition|SafetyAction::EnterSafeOutput|FatalPathPolicy|SafetyFlow::CrashDump|SafetyFlow::ProbeLoadcellClassification" tools/bazel/manifests/phase6_safety_gates.json` finds all five strings.
    - `rg "CL-007|src/common/probe_analysis.cpp|rust/crates/runtime-adapter/src/panic_boundary.rs|manual-hardware-required" tools/bazel/manifests/phase6_safety_gates.json` finds all four strings.
  </acceptance_criteria>
  <done>CORE-04 manifest rows identify the exact Rust safety policy surfaces, retained safety source paths, non-local evidence classes, and known concern linkage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust safety policy -> retained fatal/HAL/RTOS effects | Pure classifications must not execute or hide retained fatal, watchdog, crash-dump, or safe-output effects. |
| Host tests -> hardware safety behavior | Unit tests prove classification only and must not claim physical thermal, motion, watchdog, or crash recovery proof. |
| Safety manifests -> sensitive crash-dump surfaces | Crash dump rows can reveal sensitive handling assumptions unless constrained to source paths and evidence classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Repudiation / Safety | Evidence classes | mitigate | `EvidenceClass` and manifest rows must keep simulator/hardware/manual evidence distinct from Rust host tests. |
| T-06-03-02 | Denial of service | Fatal boundary policy | mitigate | `FatalPathPolicy` must set `allows_allocation: false` and name Phase 5 panic, crash-dump, and watchdog audit surfaces. |
| T-06-03-03 | Information Disclosure | Crash dump handling | mitigate | Manifest rows reference source paths and CL-011 handling without copying crash dump memory contents or secrets. |
| T-06-03-04 | Tampering | Probe/loadcell classification | mitigate | `SafetyFlow::ProbeLoadcellClassification` and manifest rows must cite `CL-007` and retained `src/common/probe_analysis.cpp` behavior. |
| T-06-03-05 | Denial of service / Safety | Emergency stop and safe-output classification | mitigate | `SafetyAction` must include safe-output and disable-motion/heat actions with retained emergency-stop source coverage. |
</threat_model>

<verification>
- `cargo test --all-features -p buddy-domain safety`
- `python3 tools/bazel/phase6_verify.py --safety-only`
- `python3 tools/bazel/phase6_verify.py --quick`
</verification>

<success_criteria>
Plan 03 succeeds when CORE-04 has tested Rust safety policy data, fatal and crash-dump flows preserve Phase 5 boundaries, known safety concerns are named, and all safety manifest rows carry source-backed evidence classes without local hardware overclaims.
</success_criteria>

<output>
After completion, create `.planning/phases/06-printing-core-safety-and-feature-gates/06-03-SUMMARY.md`.
</output>
