---
phase: 20-release-candidate-artifact-production
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
  - tools/bazel/phase20_release_candidate_artifacts.py
  - tools/bazel/phase20_release_candidate_artifacts_test.py
  - tools/bazel/phase17_release_candidate_evidence.py
  - tools/bazel/phase17_release_candidate_evidence_test.py
autonomous: true
requirements: [REL-01, REL-02, REL-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-06-21T12-40-17
generated_at: 2026-06-21T13:06:55Z
must_haves:
  truths:
    - "`//tools/bazel:phase17_release_candidate_artifacts` is non-empty and points to the Phase 20 release-environment input manifest label, not local smoke artifacts."
    - "`phase17_representative_release_smoke`, `representative_release_artifacts`, and `phase3_verify` remain separate from production release proof."
    - "`just phase20-verify` runs Bazel verifier tests before the Phase 20 verifier."
    - "Phase 17 wiring checks still reject empty release identity targets and representative smoke wrapping after Phase 20 changes the target identity."
  artifacts:
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 20 Bazel labels and non-empty Phase 17 release artifact identity"
      contains: "phase20_release_environment_input_manifest"
    - path: "BUILD.bazel"
      provides: "Root Phase 20 docs filegroup and verifier aliases"
      contains: "phase20_release_candidate_artifacts_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Phase 20 verifier dispatch"
      contains: "phase20_verify"
    - path: "justfile"
      provides: "Developer facade for Phase 20 verification"
      contains: "phase20-verify"
    - path: "tools/bazel/phase17_release_candidate_evidence.py"
      provides: "Phase 17 compatibility guard for non-empty production-safe release identity"
      contains: "phase20_release_environment_input_manifest"
  key_links:
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/manifests/phase20_release_environment_inputs.template.json"
      via: "filegroup phase20_release_environment_input_manifest"
      pattern: "phase20_release_environment_input_manifest"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase20_release_candidate_artifacts.py"
      via: "phase20_verify dispatch"
      pattern: "phase20_release_candidate_artifacts\\.py --quick"
    - from: "justfile"
      to: "//tools/bazel:phase20_verify_tests"
      via: "phase20-verify recipe"
      pattern: "bazel run //tools/bazel:phase20_verify_tests"
---

<objective>
Wire Phase 20 into the Bazel/root/just verification surface and harden the Phase 17 release identity guard against the audited empty-target gap.

Purpose: Make release managers use a non-empty Bazel-owned release identity while preserving the no-overclaim boundary between production release evidence and representative smoke fixtures.
Output: Phase 20 Bazel targets, root aliases/docs, workflow dispatch, `just phase20-verify`, and Phase 17 compatibility checks.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md
@.planning/phases/20-release-candidate-artifact-production/20-RESEARCH.md
@.planning/phases/20-release-candidate-artifact-production/20-VALIDATION.md
@.planning/phases/17-release-candidate-artifact-and-signing-gates/17-01-SUMMARY.md
@.planning/phases/19-aggregate-cutover-evidence-ci/19-SUMMARY.md
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile
@tools/bazel/phase17_release_candidate_evidence.py
@tools/bazel/phase17_release_candidate_evidence_test.py
@tools/bazel/phase20_release_candidate_artifacts.py
@tools/bazel/phase20_release_candidate_artifacts_test.py

<interfaces>
Current `tools/bazel/BUILD.bazel` release identity:
```python
filegroup(
    name = "phase17_release_candidate_artifacts",
    # Wire only real release-candidate outputs here; keep local smoke fixtures separate.
    srcs = [],
)

filegroup(
    name = "phase17_representative_release_smoke",
    srcs = [":representative_release_artifacts"],
)
```

Current `tools/bazel/rust_workflow.sh` dispatch pattern:
```bash
phase19_verify)
  python3 tools/bazel/phase19_aggregate_ci_evidence.py --wiring-only
  python3 tools/bazel/phase19_aggregate_ci_evidence.py --ci --output-dir build/ci-evidence/phase19
  ;;
phase19_verify_tests)
  python3 tools/bazel/phase19_aggregate_ci_evidence_test.py
  ;;
```

Current `justfile` ordering pattern:
```make
phase19-verify:
    bazel run //tools/bazel:phase19_verify_tests
    bazel run //tools/bazel:phase19_verify
```
</interfaces>

Decision coverage:
- D-01 full: `phase17_release_candidate_artifacts` becomes non-empty.
- D-02 full: the target points at explicit release-environment input metadata while private signing stays external.
- D-03 full: representative smoke labels remain separate and are rejected by Phase 17 and Phase 20 wiring checks.
- D-07 full: wiring tests reject empty targets and smoke-wrapped targets.
- D-13, D-14 full: Phase 20 verifier remains release authority; Phase 19 is not changed to decide REL pass/fail.
- D-15 full: Bazel labels, root docs/aliases, `rust_workflow.sh`, and `just phase20-verify` are added for discoverability and retention.
- D-16 full: generated Phase 20 result manifests remain consumable by Phase 21 without implementing Phase 21 policy.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Phase 20 Bazel labels, root aliases, workflow dispatch, and just facade</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase20_release_candidate_artifacts.py, tools/bazel/phase20_release_candidate_artifacts_test.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase20_release_candidate_artifacts.py
    - tools/bazel/phase20_release_candidate_artifacts_test.py
    - tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json
    - tools/bazel/manifests/phase20_release_environment_inputs.template.json
    - tools/bazel/phase17_release_candidate_evidence.py
    - .planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md
  </read_first>
  <behavior>
    - Bazel query finds `//tools/bazel:phase20_verify`, `//tools/bazel:phase20_verify_tests`, `//:phase20_verify`, and `//:phase20_verify_tests`.
    - `//tools/bazel:phase17_release_candidate_artifacts` has `srcs = [":phase20_release_environment_input_manifest"]`.
    - `rust_workflow.sh` dispatches `phase20_verify` to `--wiring-only` then `--quick`.
    - `just phase20-verify` runs `bazel run //tools/bazel:phase20_verify_tests` before `bazel run //tools/bazel:phase20_verify`.
  </behavior>
  <action>Modify `tools/bazel/BUILD.bazel` by adding `filegroup(name = "phase20_release_environment_input_manifest", srcs = ["manifests/phase20_release_environment_inputs.template.json"])`. Change the existing `filegroup(name = "phase17_release_candidate_artifacts", ...)` to `srcs = [":phase20_release_environment_input_manifest"]` and keep `phase17_representative_release_smoke` unchanged with `srcs = [":representative_release_artifacts"]`. Add `filegroup(name = "phase20_source_ref_manifests", srcs = ["manifests/phase17_release_candidate_evidence_contract.json", "manifests/phase19_aggregate_ci_evidence_contract.json", "manifests/phase20_release_candidate_artifacts_contract.json", "manifests/phase20_release_environment_inputs.template.json", "manifests/phase11_reference_comparisons.json", "manifests/representative_products.json"])`. Add `shell_binary(name = "phase20_verify", src = "rust_workflow.sh", data = ["phase20_release_candidate_artifacts.py", "manifests/phase20_release_candidate_artifacts_contract.json", "manifests/phase20_release_environment_inputs.template.json", ":phase20_source_ref_manifests", ":phase17_release_candidate_artifacts", ":phase17_representative_release_smoke", "//:phase20_release_candidate_artifacts_docs", "//:phase17_release_candidate_evidence_docs", "//:phase19_aggregate_ci_evidence_docs"])`. Add `shell_binary(name = "phase20_verify_tests", src = "rust_workflow.sh", data = ["phase20_release_candidate_artifacts.py", "phase20_release_candidate_artifacts_test.py", "manifests/phase20_release_candidate_artifacts_contract.json", "manifests/phase20_release_environment_inputs.template.json", ":phase20_source_ref_manifests", ":phase17_release_candidate_artifacts", ":phase17_representative_release_smoke"])`.

Modify root `BUILD.bazel` by adding `filegroup(name = "phase20_release_candidate_artifacts_docs", srcs = [".planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md", ".planning/phases/20-release-candidate-artifact-production/20-RESEARCH.md", ".planning/phases/20-release-candidate-artifact-production/20-VALIDATION.md", ".planning/phases/20-release-candidate-artifact-production/20-01-PLAN.md", ".planning/phases/20-release-candidate-artifact-production/20-02-PLAN.md"])`, plus aliases `phase20_verify` and `phase20_verify_tests` pointing to `//tools/bazel:phase20_verify` and `//tools/bazel:phase20_verify_tests`.

Modify `tools/bazel/rust_workflow.sh` by adding `phase20_verify)` with commands `python3 tools/bazel/phase20_release_candidate_artifacts.py --wiring-only` and `python3 tools/bazel/phase20_release_candidate_artifacts.py --quick`; add `phase20_verify_tests)` with command `python3 tools/bazel/phase20_release_candidate_artifacts_test.py`.

Modify `justfile` by adding recipe `phase20-verify:` with commands `bazel run //tools/bazel:phase20_verify_tests` then `bazel run //tools/bazel:phase20_verify`.

Extend `tools/bazel/phase20_release_candidate_artifacts.py` with `--wiring-only` that checks the exact labels, filegroups, aliases, dispatch commands, and `justfile` ordering above. Extend tests with `test_wiring_requires_phase20_identity_manifest`, `test_wiring_rejects_phase17_empty_release_target`, `test_wiring_rejects_smoke_release_target`, and `test_just_phase20_verify_runs_tests_before_verifier`.</action>
  <verify>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts_test.py</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --wiring-only</automated>
    <automated>bazel query "//tools/bazel:phase20_verify + //tools/bazel:phase20_verify_tests + //:phase20_verify + //:phase20_verify_tests + //tools/bazel:phase17_release_candidate_artifacts + //:phase17_release_candidate_artifacts"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "phase20_release_environment_input_manifest"' tools/bazel/BUILD.bazel` returns one match.
    - `rg -n 'srcs = \\[":phase20_release_environment_input_manifest"\\]' tools/bazel/BUILD.bazel` returns one match inside `phase17_release_candidate_artifacts`.
    - `rg -n 'phase20_verify|phase20_verify_tests|phase20_release_candidate_artifacts_docs' tools/bazel/BUILD.bazel BUILD.bazel tools/bazel/rust_workflow.sh justfile` returns matches in all four files.
    - `python3 tools/bazel/phase20_release_candidate_artifacts.py --wiring-only` prints `Phase 20 release candidate artifact wiring passed`.
    - `bazel query "//tools/bazel:phase20_verify + //tools/bazel:phase20_verify_tests + //:phase20_verify + //:phase20_verify_tests"` exits 0.
  </acceptance_criteria>
  <done>Phase 20 is discoverable through Bazel root aliases and `just phase20-verify`, and the production release identity target is non-empty without depending on representative smoke labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden Phase 17 compatibility checks and run the full Phase 20 gate</name>
  <files>tools/bazel/phase17_release_candidate_evidence.py, tools/bazel/phase17_release_candidate_evidence_test.py, tools/bazel/phase20_release_candidate_artifacts.py, tools/bazel/phase20_release_candidate_artifacts_test.py</files>
  <read_first>
    - tools/bazel/phase17_release_candidate_evidence.py
    - tools/bazel/phase17_release_candidate_evidence_test.py
    - tools/bazel/phase20_release_candidate_artifacts.py
    - tools/bazel/phase20_release_candidate_artifacts_test.py
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-01-SUMMARY.md
    - .planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md
  </read_first>
  <behavior>
    - Phase 17 `--wiring-only` fails for an empty `phase17_release_candidate_artifacts` filegroup.
    - Phase 17 `--wiring-only` passes when the target source is exactly `:phase20_release_environment_input_manifest`.
    - Phase 17 `--wiring-only` still fails when the target wraps `:phase17_representative_release_smoke`, `:representative_release_artifacts`, or `//tools/bazel:phase3_verify`.
    - Full Phase 20 verification passes through direct Python, Bazel, and `just`.
  </behavior>
  <action>Update `tools/bazel/phase17_release_candidate_evidence.py` function `check_release_candidate_artifact_target` so it returns an error when the `phase17_release_candidate_artifacts` filegroup has an empty `srcs` list, returns an error when `srcs` contains `:phase17_representative_release_smoke`, `:representative_release_artifacts`, `//tools/bazel:phase17_representative_release_smoke`, `//tools/bazel:representative_release_artifacts`, or `//tools/bazel:phase3_verify`, and accepts `:phase20_release_environment_input_manifest` as the production-safe source for Phase 20. Keep existing Phase 17 release evidence row validation unchanged except for accepting the non-empty release identity wiring.

Update `tools/bazel/phase17_release_candidate_evidence_test.py` with tests named `test_release_candidate_target_rejects_empty_filegroup`, `test_release_candidate_target_accepts_phase20_release_input_manifest`, and `test_release_candidate_target_rejects_representative_smoke_wrapping`. Keep the existing Phase 17 tests for release evidence input, redaction, source refs, and smoke separation passing.

Run the full Phase 20 verification sequence after Phase 17 tests pass: direct tests, Phase 20 contract/security/quick/wiring modes, Bazel verifier test target, Bazel verifier target, `just phase20-verify`, and `git diff --check`.</action>
  <verify>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts_test.py</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --contract-only</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --security-only</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --quick</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --wiring-only</automated>
    <automated>bazel run //tools/bazel:phase20_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase20_verify</automated>
    <automated>just phase20-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'test_release_candidate_target_rejects_empty_filegroup|test_release_candidate_target_accepts_phase20_release_input_manifest|test_release_candidate_target_rejects_representative_smoke_wrapping' tools/bazel/phase17_release_candidate_evidence_test.py` returns all three test names.
    - `rg -n 'phase20_release_environment_input_manifest' tools/bazel/phase17_release_candidate_evidence.py tools/bazel/phase17_release_candidate_evidence_test.py` returns matches in both files.
    - `python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only` prints `Phase 17 release candidate evidence wiring passed`.
    - `bazel run //tools/bazel:phase20_verify_tests` exits 0.
    - `bazel run //tools/bazel:phase20_verify` exits 0.
    - `just phase20-verify` exits 0.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Phase 17 and Phase 20 both reject the audited empty/smoke release-identity failure modes, and Phase 20 passes direct, Bazel, and just verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel target graph -> release manager | The target name is a release identity; wrong deps can spoof production proof. |
| local smoke labels -> release identity | Representative smoke outputs are useful fixtures but cannot cross into production release evidence. |
| workflow facade -> verifier | `rust_workflow.sh` and `justfile` must execute tests before verifier output is trusted. |
| Phase 17 compatibility guard -> Phase 20 identity | Prior release verifier must reject the audited empty target and smoke wrapping after Phase 20 wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing / Repudiation | `tools/bazel/BUILD.bazel` release identity filegroup | mitigate | Require `srcs = [":phase20_release_environment_input_manifest"]` and reject smoke labels in Phase 17 and Phase 20 wiring checks. |
| T-20-02 | Information Disclosure | Phase 20 release-environment manifest label | mitigate | The checked-in manifest is metadata-only; private keys and payloads remain rejected by Plan 01 verifier security scans. |
| T-20-03 | Tampering | Bazel/root/workflow/just wiring | mitigate | `--wiring-only` checks exact filegroup names, aliases, shell dispatch commands, and `just` command order. |
| T-20-04 | Repudiation | `just phase20-verify` and Bazel verifier labels | mitigate | `just phase20-verify` and Bazel dispatch run tests before verifier quick output. |
| T-20-05 | Process Integrity | Phase 17 compatibility tests | mitigate | Add regression tests for empty filegroup rejection, Phase 20 manifest acceptance, and smoke wrapping rejection. |
</threat_model>

<verification>
Run:
- `python3 tools/bazel/phase17_release_candidate_evidence_test.py`
- `python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only`
- `python3 tools/bazel/phase20_release_candidate_artifacts_test.py`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --contract-only`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --security-only`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --quick`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --wiring-only`
- `bazel run //tools/bazel:phase20_verify_tests`
- `bazel run //tools/bazel:phase20_verify`
- `just phase20-verify`
- `git diff --check`
</verification>

<success_criteria>
- `//tools/bazel:phase17_release_candidate_artifacts` is non-empty and uses the Phase 20 release-environment input manifest label.
- Representative smoke labels remain separate and fail if used as production release identity deps.
- Phase 20 has Bazel package targets, root aliases, workflow dispatch, and a `just phase20-verify` facade.
- Phase 17 wiring checks fail the old empty target shape and pass the new Phase 20 release input identity.
- Direct, Bazel, and just verification commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/20-release-candidate-artifact-production/20-02-SUMMARY.md`
</output>
