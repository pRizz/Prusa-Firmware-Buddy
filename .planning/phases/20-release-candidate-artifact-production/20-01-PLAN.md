---
phase: 20-release-candidate-artifact-production
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json
  - tools/bazel/manifests/phase20_release_environment_inputs.template.json
  - tools/bazel/phase20_release_candidate_artifacts.py
  - tools/bazel/phase20_release_candidate_artifacts_test.py
autonomous: true
requirements: [REL-01, REL-02, REL-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-06-21T12-40-17
generated_at: 2026-06-21T13:06:55Z
must_haves:
  truths:
    - "Release managers can inspect a Phase 20 release-result manifest with REL-01, REL-02, and REL-03 row statuses."
    - "Local quick verification writes pending release evidence under build/ci-evidence/phase20 without marking release rows passed when no approved release input is supplied."
    - "Release signing/provenance inputs record key identity, build input identity, subject digests, retention refs, timestamp, and verification outcome without private key or payload material."
    - "Every release comparison mismatch is classified as pass, intentional-delta, blocker, or deferred-retained-code-issue with reason, owner phase, affected surface, and residual risk."
  artifacts:
    - path: "tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json"
      provides: "Phase 20 release identity, proof class, status, signing/provenance, retention, and comparison contract"
      contains: "phase20_release_candidate_artifacts"
    - path: "tools/bazel/manifests/phase20_release_environment_inputs.template.json"
      provides: "Source-backed release-environment evidence input template"
      contains: "external://phase20/"
    - path: "tools/bazel/phase20_release_candidate_artifacts.py"
      provides: "Stdlib verifier and result writer"
      exports: ["--contract-only", "--security-only", "--quick", "--release-input", "--output-dir"]
    - path: "tools/bazel/phase20_release_candidate_artifacts_test.py"
      provides: "Regression tests for contract, no-overclaim, redaction, path, and comparison behavior"
      contains: "Phase20ReleaseCandidateArtifactsTest"
  key_links:
    - from: "tools/bazel/phase20_release_candidate_artifacts.py"
      to: "tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json"
      via: "CONTRACT_MANIFEST constant"
      pattern: "phase20_release_candidate_artifacts_contract\\.json"
    - from: "tools/bazel/phase20_release_candidate_artifacts.py"
      to: "build/ci-evidence/phase20/release-result-manifest.json"
      via: "quick artifact writer"
      pattern: "release-result-manifest\\.json"
    - from: "tools/bazel/phase20_release_candidate_artifacts.py"
      to: "external://phase20/"
      via: "release input ref guard"
      pattern: "external://phase20/"
---

<objective>
Create the Phase 20 release artifact contract, release-environment input template, verifier/result writer, and focused regression tests.

Purpose: Close REL-01, REL-02, and REL-03 at the evidence-contract layer without treating local smoke products or template-only data as production release proof.
Output: Phase 20 source contract, release input template, verifier, tests, and generated quick artifacts under `build/ci-evidence/phase20/`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md
@.planning/phases/20-release-candidate-artifact-production/20-RESEARCH.md
@.planning/phases/20-release-candidate-artifact-production/20-VALIDATION.md
@.planning/phases/17-release-candidate-artifact-and-signing-gates/17-01-SUMMARY.md
@.planning/phases/19-aggregate-cutover-evidence-ci/19-SUMMARY.md
@tools/bazel/phase17_release_candidate_evidence.py
@tools/bazel/phase17_release_candidate_evidence_test.py
@tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
@tools/bazel/manifests/phase19_aggregate_ci_evidence_contract.json

<interfaces>
From `tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`:
```json
{
  "release_workflow_identities": {
    "phase17_release_candidate_artifacts": {
      "artifact_outputs": [".bin", ".bbf", ".dfu", ".map", ".provenance.json", "resource-image", "resource-package", "language-bundle", "wui-assets", "esp-package", "mmu-package", "dwarf-firmware", "modularbed-firmware", "xbuddy-extension-firmware", "package-manifest", "signing-summary", "provenance-summary", "retention-manifest", "comparison-report"],
      "bazel_label": "//tools/bazel:phase17_release_candidate_artifacts",
      "release_command": "bazel build //tools/bazel:phase17_release_candidate_artifacts",
      "release_run_required": true
    }
  },
  "mismatch_class_vocabulary": ["pass", "intentional-delta", "blocker", "deferred-retained-code-issue"]
}
```

From `tools/bazel/phase17_release_candidate_evidence.py`:
```python
DEFAULT_OUTPUT_DIR = Path("build/ci-evidence/phase17")
def validated_release_rows(root: Path, contract: dict[str, Any], path: str | None) -> dict[str, dict[str, Any]]: ...
def write_quick_artifacts(root: Path, contract: dict[str, Any], output_dir: Path, release_rows: dict[str, dict[str, Any]]) -> None: ...
```
</interfaces>

Decision coverage:
- D-04 full: contract lists every required release surface exactly.
- D-05 full: contract/verifier uses proof classes `release-candidate`, `approved-release-run`, `external-release-key-evidence`, `local-smoke`, and `template-only`.
- D-06 full: quick mode validates schema, redaction, path, and result shape while keeping missing release inputs pending.
- D-07 full: tests reject smoke labels, empty release data, dry-run/template-only rows, and local representative products as REL proof.
- D-08 full: generated runtime evidence uses `build/ci-evidence/phase20/`; checked-in files are contracts, template, verifier, and tests only.
- D-09, D-10, D-11, D-12 full: verifier enforces repo-native JSON signing, provenance, retention, and comparison metadata.
- D-13, D-16 full: `release-result-manifest.json` is the Phase 20 machine-readable release result source for Phase 21.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED Phase 20 contract and verifier tests</name>
  <files>tools/bazel/phase20_release_candidate_artifacts_test.py</files>
  <read_first>
    - tools/bazel/phase20_release_candidate_artifacts_test.py
    - tools/bazel/phase17_release_candidate_evidence_test.py
    - tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
    - .planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md
    - .planning/phases/20-release-candidate-artifact-production/20-VALIDATION.md
  </read_first>
  <behavior>
    - Test `test_contract_lists_all_release_surfaces`: missing any required surface or row ID fails.
    - Test `test_quick_without_release_input_writes_pending_result_manifest`: no release input writes a result manifest and no release row has `status == "passed"`.
    - Test `test_passed_result_rejects_local_smoke_and_template_only_proof`: proof classes `local-smoke` and `template-only` cannot satisfy REL rows.
    - Test `test_redaction_rejects_private_key_and_payload_fields`: private key, raw payload, token, password, and credential fields fail validation.
    - Test `test_release_refs_reject_absolute_and_parent_traversal_paths`: `/tmp/...`, `../...`, and refs outside `build/ci-evidence/phase20/` or `external://phase20/...` fail.
    - Test `test_comparison_rows_require_exact_classification_metadata`: each mismatch needs one allowed class plus reason, owner phase, affected surface, and residual risk.
  </behavior>
  <action>Create `tools/bazel/phase20_release_candidate_artifacts_test.py` using Python stdlib `unittest`, `tempfile`, `subprocess`, `json`, and `pathlib`. Define constants `REQUIRED_SURFACES`, `REQUIRED_ROW_IDS`, `PROOF_CLASSES`, `STATUS_VOCABULARY`, and `MISMATCH_CLASSES` with the exact values named in Task 2. Build temporary repo fixtures in the tests rather than mutating real checked-in files. Use explicit Arrange, Act, Assert comments in each test. The first run is RED because `tools/bazel/phase20_release_candidate_artifacts.py` and the Phase 20 contract do not exist yet.</action>
  <verify>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class Phase20ReleaseCandidateArtifactsTest" tools/bazel/phase20_release_candidate_artifacts_test.py` returns one match.
    - `rg -n "test_contract_lists_all_release_surfaces|test_quick_without_release_input_writes_pending_result_manifest|test_passed_result_rejects_local_smoke_and_template_only_proof|test_redaction_rejects_private_key_and_payload_fields|test_release_refs_reject_absolute_and_parent_traversal_paths|test_comparison_rows_require_exact_classification_metadata" tools/bazel/phase20_release_candidate_artifacts_test.py` returns all six test names.
    - `rg -n "REQUIRED_SURFACES = \\[" tools/bazel/phase20_release_candidate_artifacts_test.py` returns one match.
    - Running `python3 tools/bazel/phase20_release_candidate_artifacts_test.py` before Task 2 exits nonzero for missing Phase 20 implementation, not for Python syntax errors.
  </acceptance_criteria>
  <done>The RED test file exists, names the required behaviors, and fails only because the Phase 20 verifier/contract implementation is not present yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 20 contract, input template, verifier, and result writer</name>
  <files>tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json, tools/bazel/manifests/phase20_release_environment_inputs.template.json, tools/bazel/phase20_release_candidate_artifacts.py, tools/bazel/phase20_release_candidate_artifacts_test.py</files>
  <read_first>
    - tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json
    - tools/bazel/manifests/phase20_release_environment_inputs.template.json
    - tools/bazel/phase20_release_candidate_artifacts.py
    - tools/bazel/phase20_release_candidate_artifacts_test.py
    - tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
    - tools/bazel/phase17_release_candidate_evidence.py
    - tools/bazel/manifests/phase19_aggregate_ci_evidence_contract.json
    - .planning/phases/20-release-candidate-artifact-production/20-CONTEXT.md
    - .planning/phases/20-release-candidate-artifact-production/20-RESEARCH.md
  </read_first>
  <behavior>
    - `--contract-only` validates contract schema, row IDs, surfaces, proof classes, status vocabulary, mismatch classes, and release input schema.
    - `--security-only` scans checked-in Phase 20 contract/template files for forbidden field names and forbidden text markers.
    - `--quick` writes Phase 20 generated artifacts under `build/ci-evidence/phase20/`.
    - `--release-input <path>` accepts complete approved release input rows and rejects incomplete, secret-bearing, path-escaping, smoke, or template-only pass claims.
  </behavior>
  <action>Create `tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json` with `schema_version: "1"`, `phase: "20-release-candidate-artifact-production"`, `phase_lifecycle_id: "20-2026-06-21T12-40-17"`, `output_root: "build/ci-evidence/phase20"`, `release_identity_label: "//tools/bazel:phase17_release_candidate_artifacts"`, `release_identity_command: "bazel build //tools/bazel:phase17_release_candidate_artifacts"`, `required_artifact_outputs` exactly `[".bin", ".bbf", ".dfu", ".map", ".provenance.json", "resource-image", "resource-package", "language-bundle", "wui-assets", "esp-package", "mmu-package", "dwarf-firmware", "modularbed-firmware", "xbuddy-extension-firmware", "package-manifest", "signing-summary", "provenance-summary", "retention-manifest", "comparison-report"]`, `proof_class_vocabulary` exactly `["release-candidate", "approved-release-run", "external-release-key-evidence", "local-smoke", "template-only"]`, `status_vocabulary` exactly `["pending-release-input", "release-run-required", "external-signing-required", "blocked-signing-key-unavailable", "source-contract-passed", "passed", "failed", "rejected-redaction", "rejected-overclaim"]`, and `mismatch_class_vocabulary` exactly `["pass", "intentional-delta", "blocker", "deferred-retained-code-issue"]`. Include `rows` for `rel-bin-firmware-image`, `rel-bbf-firmware-package`, `rel-dfu-update-package`, `rel-map-and-provenance`, `rel-resource-image-package`, `rel-language-bundles`, `rel-wui-assets`, `rel-esp-packages`, `rel-mmu-package`, `rel-auxiliary-dwarf-firmware`, `rel-auxiliary-modularbed-firmware`, `rel-auxiliary-xbuddy-extension-firmware`, `rel-package-manifests`, `rel-signing-key-identity`, `rel-build-input-identity`, `rel-artifact-retention`, `rel-reference-comparison-report`, and `rel-contract-traceability-redaction-boundary`. Each row must include `id`, `title`, `requirement_ids`, `artifact_surface`, `artifact_outputs`, `proof_class_allowed`, `default_status`, `required_input_kind`, `release_metadata_required`, `signing_metadata_required`, `provenance_metadata_required`, `comparison_metadata_required`, `retention_metadata_required`, `source_contract_refs`, `owner_phase: "20-release-candidate-artifact-production"`, and `residual_cutover_gates`.

Create `tools/bazel/manifests/phase20_release_environment_inputs.template.json` with top-level `schema_version: "1"`, `phase_lifecycle_id: "20-2026-06-21T12-40-17"`, `bazel_label: "//tools/bazel:phase17_release_candidate_artifacts"`, `builder_command: "bazel build //tools/bazel:phase17_release_candidate_artifacts"`, `allowed_external_ref_root: "external://phase20/"`, and `evidence_rows` entries that use `proof_class: "template-only"` and `status: "pending-release-input"` until release managers replace them with approved release-run metadata.

Create `tools/bazel/phase20_release_candidate_artifacts.py` as a stdlib-only verifier. It must expose `--contract-only`, `--security-only`, `--quick`, `--release-input`, and `--output-dir`; default output is `build/ci-evidence/phase20`. `--quick` must write `release-result-manifest.json`, `normalized-release-results.json`, `redacted-signing-provenance-summary.json`, `comparison-classification-report.json`, `release-environment-input-template.json`, `target-source-snapshot.json`, and `logs/phase20-release-candidate-artifacts.log`. Passed rows must require proof class `release-candidate`, `approved-release-run`, or `external-release-key-evidence`; non-empty `subject_digests`; non-empty `build_input_identity`; non-empty `retention_refs`; non-empty `verification_outcome`; and comparison metadata with `mismatch_class`, `mismatch_reason`, `owner_phase`, `affected_artifact_surface`, and `residual_risk`. Reject field names `private_key`, `signing_key_value`, `raw_key_bytes`, `private_certificate`, `token`, `password`, `credential`, `raw_firmware_payload`, `firmware_payload_bytes`, `signing_payload_bytes`, and `crash_dump_bytes`. Reject refs unless they start with `external://phase20/` or are repo-relative under `build/ci-evidence/phase20/` with no `..` segments.</action>
  <verify>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts_test.py</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --contract-only</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --security-only</automated>
    <automated>python3 tools/bazel/phase20_release_candidate_artifacts.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase20_release_candidate_artifacts_test.py` exits 0.
    - `python3 tools/bazel/phase20_release_candidate_artifacts.py --contract-only` prints `Phase 20 release candidate artifact contract passed`.
    - `python3 tools/bazel/phase20_release_candidate_artifacts.py --security-only` prints `Phase 20 release candidate artifact security scan passed`.
    - `python3 tools/bazel/phase20_release_candidate_artifacts.py --quick` creates `build/ci-evidence/phase20/release-result-manifest.json`.
    - `rg -n '"release_inputs_supplied": false' build/ci-evidence/phase20/release-result-manifest.json` returns one match after quick mode without `--release-input`.
    - `rg -n '"status": "passed"' build/ci-evidence/phase20/release-result-manifest.json` returns no matches after quick mode without `--release-input`.
    - `rg -n '"proof_class_vocabulary"|release-candidate|approved-release-run|external-release-key-evidence|local-smoke|template-only' tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json` returns matches for every proof class value.
    - `rg -n 'external://phase20/' tools/bazel/manifests/phase20_release_environment_inputs.template.json tools/bazel/phase20_release_candidate_artifacts.py` returns matches in both files.
  </acceptance_criteria>
  <done>Phase 20 contract, template, verifier, and tests pass direct local validation and generate only pending release results without approved release input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release input JSON -> Phase 20 verifier | Release managers or CI supply untrusted metadata that may overclaim proof or contain secrets. |
| verifier -> build/ci-evidence/phase20 | Generated evidence becomes retained CI/final-review input and must stay redacted and path-confined. |
| signing metadata -> repository artifacts | Key identity may be recorded, but private key material and raw signing payloads must never cross into repo files. |
| archived reference comparison -> mismatch report | Comparison rows drive release decision evidence and must not omit or misclassify mismatches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing / Repudiation | `phase20_release_candidate_artifacts.py` proof-class validation | mitigate | Reject `passed` rows unless proof class is `release-candidate`, `approved-release-run`, or `external-release-key-evidence` and release metadata is complete. |
| T-20-02 | Information Disclosure | contract/template/input/generated artifact scans | mitigate | Reject private key, raw key, private certificate, token, password, credential, raw firmware payload, signing payload, and crash-dump field names or text markers. |
| T-20-03 | Tampering / Information Disclosure | release refs and output paths | mitigate | Allow refs only under `external://phase20/` or repo-relative `build/ci-evidence/phase20/`; reject absolute paths and parent traversal. |
| T-20-04 | Repudiation | quick result manifest | mitigate | Default quick rows to pending statuses and emit `release_inputs_supplied: false` when no approved release input exists. |
| T-20-05 | Tampering / Process Integrity | comparison classification report | mitigate | Require each mismatch to include exactly one allowed class plus reason, owner phase, affected artifact surface, and residual risk. |
</threat_model>

<verification>
Run:
- `python3 tools/bazel/phase20_release_candidate_artifacts_test.py`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --contract-only`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --security-only`
- `python3 tools/bazel/phase20_release_candidate_artifacts.py --quick`
</verification>

<success_criteria>
- Phase 20 source contract and release input template exist with REL-01, REL-02, and REL-03 coverage.
- Quick output exists at `build/ci-evidence/phase20/release-result-manifest.json`.
- Quick output contains no `passed` rows unless an approved release input was supplied and validated.
- Forbidden key, credential, payload, and path escape inputs fail tests.
- Comparison classes are restricted to `pass`, `intentional-delta`, `blocker`, and `deferred-retained-code-issue`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-release-candidate-artifact-production/20-01-SUMMARY.md`
</output>
