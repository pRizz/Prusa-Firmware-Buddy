---
phase: 28-final-readiness-packet-and-demotion-gate
plan: "01"
plan_id: "28-01"
name: "Final Readiness Packet and Demotion Gate"
status: planned
type: execute
wave: 1
depends_on:
  - "27"
files_modified:
  - tools/bazel/manifests/phase28_final_readiness_packet_contract.json
  - tools/bazel/phase28_final_readiness_packet.py
  - tools/bazel/phase28_final_readiness_packet_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-03
requirements_addressed:
  - READ-01
  - READ-02
  - READ-03
lifecycle_id: 28-2026-06-25T03-31-49
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-06-25T03-31-49
generated_by: gsd-plan-phase
generated_at: 2026-06-25T03:56:59Z
user_setup: []
verification_commands:
  - python3 tools/bazel/phase28_final_readiness_packet_test.py
  - python3 tools/bazel/phase28_final_readiness_packet.py --contract-only
  - python3 tools/bazel/phase28_final_readiness_packet.py --security-only
  - python3 tools/bazel/phase28_final_readiness_packet.py --wiring-only
  - python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26
  - python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27
  - python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28
  - just phase28-verify
  - git diff --check
  - cargo fmt --all
  - cargo clippy --all-targets --all-features -- -D warnings
  - cargo build --all-targets --all-features
  - cargo test --all-features
must_haves:
  truths:
    - "Maintainer can generate a final readiness packet that links Phase 26 upstream rows, Phase 27 retained-code decisions, exception records, residual-risk rows, hard blockers, and artifact references for every canonical Phase 18 final criterion."
    - "The machine-readable packet is the authority, and the redacted Markdown report is derived from the same normalized packet rows."
    - "Final readiness starts blocked and remains blocked unless every required gate passes or is covered by a valid approved exception with no hard blockers."
    - "Hard blockers for redaction, overclaim, lifecycle mismatch, source-ref failure, unsafe refs, or secret taint cannot be converted into accepted residual risk by exception metadata."
    - "Reference demotion authorization is a separate top-level verdict that remains blocked unless an explicit Phase 28 demotion decision input is valid and final readiness is not blocked."
    - "Phase 28 quick verification writes secret-safe blocked decision fixtures and never claims real maintainer demotion approval."
  artifacts:
    - path: "tools/bazel/manifests/phase28_final_readiness_packet_contract.json"
      provides: "Phase 28 final-readiness packet, source-contract, generated-output, exception, hard-blocker, and demotion authorization policy"
      contains: "phase28_final_readiness_packet_contract"
    - path: "tools/bazel/phase28_final_readiness_packet.py"
      provides: "Phase 28 aggregate verifier, output writer, secret scanner, demotion input validator, and CLI"
      contains: "DEFAULT_OUTPUT_DIR = Path(\"build/ci-evidence/phase28\")"
    - path: "tools/bazel/phase28_final_readiness_packet_test.py"
      provides: "Regression coverage for READ-01, READ-02, READ-03, hard blockers, no implied demotion, retained outputs, and wiring"
      contains: "class Phase28FinalReadinessPacketTest"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 28 Bazel shell targets and source-ref runfiles"
      contains: "phase28_verify"
    - path: "BUILD.bazel"
      provides: "Root Phase 28 docs filegroup and aliases"
      contains: "phase28_final_readiness_packet_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Phase 28 workflow dispatch"
      contains: "phase28_verify)"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase28-verify:"
  key_links:
    - from: "tools/bazel/phase28_final_readiness_packet.py"
      to: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      via: "runtime contract loading for canonical final criteria, statuses, exception fields, and hard blocker policy"
      pattern: "phase18_cutover_review_contract"
    - from: "tools/bazel/phase28_final_readiness_packet.py"
      to: "build/ci-evidence/phase26/upstream-result-row-table.json"
      via: "Phase 26 upstream row consumption"
      pattern: "upstream-result-row-table"
    - from: "tools/bazel/phase28_final_readiness_packet.py"
      to: "build/ci-evidence/phase27/phase28-handoff-manifest.json"
      via: "Phase 27 handoff and demotion starting-state validation"
      pattern: "phase28-handoff-manifest"
    - from: "justfile"
      to: "//tools/bazel:phase28_verify"
      via: "phase28-verify recipe"
      pattern: "bazel run //tools/bazel:phase28_verify"
---

<objective>
Create the Phase 28 final-readiness packet and explicit reference-demotion authorization gate.

Purpose: Maintainers need one decision-ready packet that aggregates retained Phase 26 upstream evidence rows and Phase 27 acceptance decisions while keeping final readiness fail-closed and reference demotion blocked unless Phase 28 receives an explicit valid authorization decision.

Output: A Phase 28 JSON contract, Python aggregate verifier, unit/security/wiring test suite, retained output writer under `build/ci-evidence/phase28`, Bazel labels, root aliases, `rust_workflow.sh` dispatch, and `just phase28-verify` facade.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-final-readiness-packet-and-demotion-gate/28-CONTEXT.md
@.planning/phases/28-final-readiness-packet-and-demotion-gate/28-RESEARCH.md
@.planning/phases/28-final-readiness-packet-and-demotion-gate/28-VALIDATION.md
@.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-SUMMARY.md
@.planning/phases/26-release-signing-and-upstream-result-evidence/26-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@tools/bazel/manifests/phase18_cutover_review_contract.json
@tools/bazel/phase18_cutover_review.py
@tools/bazel/phase18_cutover_review_test.py
@tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
@tools/bazel/phase26_release_signing_upstream_evidence.py
@tools/bazel/phase26_release_signing_upstream_evidence_test.py
@tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
@tools/bazel/phase27_retained_code_acceptance_decisions.py
@tools/bazel/phase27_retained_code_acceptance_decisions_test.py
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/manifests/phase11_retained_code_justifications.json
@tools/bazel/manifests/foreign_code_inventory.json
@tools/bazel/manifests/unsafe_boundary_audit.json
@BUILD.bazel
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
Material local guidance: `AGENTS.md`, `AGENTS.bright-builds.md`, `standards-overrides.md`, `standards/core/architecture.md`, `standards/core/code-shape.md`, `standards/core/testing.md`, `standards/core/verification.md`, and `standards/languages/rust.md` require Bright Builds standards, functional-core/imperative-shell verifier structure, focused unit tests with Arrange/Act/Assert comments, repo-native verification, and the required Cargo sequence before commits. Do not edit `.planning/config.json`; an unrelated local modification may already exist.

Canonical Phase 18 upstream criteria:
`final-ci-evidence`, `final-simulator-evidence`, `final-hardware-safety-media-evidence`, `final-live-network-transfer-evidence`, `final-release-artifact-signing-evidence`, `final-retained-code-acceptance`, `final-residual-risk-review`, `final-maintainer-decision`, `final-reference-demotion-allowed`.

Phase 26 upstream row table path:
`build/ci-evidence/phase26/upstream-result-row-table.json`.

Phase 27 handoff path:
`build/ci-evidence/phase27/phase28-handoff-manifest.json`.

Phase 27 retained outputs to consume or link:
`acceptance-run-manifest.json`, `normalized-retained-code-decisions.json`, `residual-risk-register.json`, `exception-decision-register.json`, `final-readiness-decision-summary.json`, `phase28-handoff-manifest.json`, `decision-row-table.json`, `maintainer-acceptance-input-template.json`, `artifact-reference-summary.json`, `contract-snapshots/phase18_cutover_review_contract.json`, `contract-snapshots/phase26_release_signing_upstream_evidence_contract.json`, and `contract-snapshots/phase26-upstream-result-row-table.json`.

Phase 28 generated outputs required by D-11:
`final-readiness-run-manifest.json`, `final-readiness-packet.json`, `normalized-readiness-criteria-table.json`, `blocker-summary.json`, `exception-residual-risk-summary.json`, `reference-demotion-authorization-record.json`, `demotion-decision-input-template.json`, `redacted-readiness-report.md`, `artifact-reference-summary.json`, `contract-snapshots/phase18_cutover_review_contract.json`, `contract-snapshots/phase26_release_signing_upstream_evidence_contract.json`, `contract-snapshots/phase27_retained_code_acceptance_decisions_contract.json`, `contract-snapshots/phase26-upstream-result-row-table.json`, and `contract-snapshots/phase27-phase28-handoff-manifest.json`.
</interfaces>
</context>

<commit_verification>
Before every task commit and before any final GSD/docs commit in this Rust workspace, run the root required pre-commit sequence in this exact order:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```

Do not commit an intentionally failing RED state. If a task writes tests before implementation, keep the failing-test state uncommitted and commit only after implementation plus all required Rust checks and Phase 28 checks pass. Do not edit `.planning/config.json` and do not revert unrelated worktree changes.
</commit_verification>

<tasks>

<task id="28-T1" type="auto" tdd="true">
  <name>Task 1: Define Phase 28 contract and regression test surface</name>
  <depends_on>none</depends_on>
  <files>tools/bazel/manifests/phase28_final_readiness_packet_contract.json, tools/bazel/phase28_final_readiness_packet.py, tools/bazel/phase28_final_readiness_packet_test.py</files>
  <write_scope>
    <file>tools/bazel/manifests/phase28_final_readiness_packet_contract.json</file>
    <file>tools/bazel/phase28_final_readiness_packet.py</file>
    <file>tools/bazel/phase28_final_readiness_packet_test.py</file>
  </write_scope>
  <read_first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/core/verification.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-CONTEXT.md</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-RESEARCH.md</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-VALIDATION.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-SUMMARY.md</file>
    <file>tools/bazel/manifests/phase18_cutover_review_contract.json</file>
    <file>tools/bazel/phase18_cutover_review.py</file>
    <file>tools/bazel/phase18_cutover_review_test.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions.py</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions_test.py</file>
    <file>tools/bazel/manifests/phase11_cutover_readiness.json</file>
    <file>tools/bazel/manifests/phase11_retained_code_justifications.json</file>
    <file>tools/bazel/manifests/foreign_code_inventory.json</file>
    <file>tools/bazel/manifests/unsafe_boundary_audit.json</file>
    <file>tools/bazel/manifests/phase28_final_readiness_packet_contract.json if present</file>
    <file>tools/bazel/phase28_final_readiness_packet.py if present</file>
    <file>tools/bazel/phase28_final_readiness_packet_test.py if present</file>
  </read_first>
  <behavior>
    - Test 1: `--contract-only` exact-matches the Phase 18 final criterion IDs listed in this plan and fails if Phase 28 contract criteria drift from Phase 18.
    - Test 2: The Phase 28 contract declares `final_readiness_status` and `reference_demotion_authorization` as separate top-level verdicts; no test fixture may use `demotion_allowed: true`.
    - Test 3: The Phase 28 contract declares generated artifacts exactly as listed in the `<interfaces>` block.
    - Test 4: The Phase 28 contract declares hard blockers `redaction-failed`, `overclaim-failed`, `lifecycle-mismatch`, `source-ref-failed`, `unsafe-ref`, and `secret-tainted`, and contract tests assert these outrank exception coverage.
    - Test 5: The Phase 28 contract declares required demotion approval input fields `phase`, `phase_lifecycle_id`, `demotion_authorization`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `scope`, and `evidence_refs`.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase28_final_readiness_packet_contract.json` with `schema_version: "1"`, `id: "phase28_final_readiness_packet_contract"`, `phase: "28-final-readiness-packet-and-demotion-gate"`, `phase_lifecycle_id: "28-2026-06-25T03-31-49"`, `artifact_name: "phase28-final-readiness-packet"`, and `output_root: "build/ci-evidence/phase28"`.

    Add `requirements` with exactly `READ-01`, `READ-02`, and `READ-03`. Add `source_contracts` for `tools/bazel/manifests/phase18_cutover_review_contract.json`, `tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json`, `tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json`, `tools/bazel/manifests/phase11_cutover_readiness.json`, `tools/bazel/manifests/phase11_retained_code_justifications.json`, `tools/bazel/manifests/foreign_code_inventory.json`, and `tools/bazel/manifests/unsafe_boundary_audit.json`.

    Add `required_inputs` with `phase26_upstream_rows: "build/ci-evidence/phase26/upstream-result-row-table.json"` and `phase27_handoff: "build/ci-evidence/phase27/phase28-handoff-manifest.json"`. Add optional `demotion_decision_input` path support for CLI input. Do not redefine simulator, hardware, live-service, release, upstream, retained-code, or residual-risk evidence semantics; Phase 28 must aggregate Phase 26 and Phase 27 outputs per D-10.

    Add `readiness_policy` with `default_final_readiness_status: "blocked"`, `pass_statuses: ["passed"]`, `exception_statuses: ["exception-approved"]`, `exception_coverable_statuses: ["failed", "blocked", "exception-requested"]`, `hard_blocker_reasons: ["redaction-failed", "overclaim-failed", "lifecycle-mismatch", "source-ref-failed", "unsafe-ref", "secret-tainted"]`, and `hard_blockers_outrank_exceptions: true` per D-04, D-05, and D-06.

    Add `demotion_authorization_policy` with `default: "blocked"`, `approval_requires_explicit_phase28_input: true`, `evidence_status_never_implies_approval: true`, `requires_final_readiness_unblocked: true`, and `allowed_authorizations: ["blocked", "approved"]` per D-07 and D-08. Add `phase27_handoff_policy` requiring `demotion_authorization: "blocked"`, `phase27_may_authorize_demotion: false`, and `phase28_required_decision: "explicit-maintainer-reference-demotion-decision"` per D-09.

    Add `demotion_decision_required_fields` with exactly `phase`, `phase_lifecycle_id`, `demotion_authorization`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `scope`, and `evidence_refs`. Add `exception_required_fields` with `scope`, `owner`, `approver`, `approver_role`, `rationale`, `affected_printer_or_release_surface`, `evidence_refs`, `residual_risk`, `mitigation_or_follow_up`, and `expiry_or_review_trigger` per D-06.

    Add `generated_artifacts` exactly as listed in the `<interfaces>` block for Phase 28 outputs. These artifacts implement D-01, D-02, D-03, and D-11.

    Create `tools/bazel/phase28_final_readiness_packet.py` with a minimal standard-library CLI skeleton exposing `--contract-only`, `--security-only`, `--wiring-only`, `--quick`, `--phase26-upstream-rows PATH`, `--phase27-handoff PATH`, `--demotion-decision PATH`, and `--output-dir PATH`. Implement `--contract-only` in this task. Include path constants only, not canonical Phase 18 criterion constants as authority; load Phase 18 criteria at runtime and assert Phase 28 contract criteria match them.

    Create `tools/bazel/phase28_final_readiness_packet_test.py` with `unittest` and class `Phase28FinalReadinessPacketTest`. Write focused tests for the behavior list above, using Arrange, Act, Assert comments for non-trivial tests. Tests must cover READ-01 criteria coverage, READ-02 blocked-by-default policy, READ-03 separate demotion verdict policy, and D-01 through D-12 contract declarations.
  </action>
  <acceptance_criteria>
    - `tools/bazel/manifests/phase28_final_readiness_packet_contract.json` exists and contains `phase28_final_readiness_packet_contract`, `build/ci-evidence/phase28`, `READ-01`, `READ-02`, `READ-03`, `final_readiness_status`, and `reference_demotion_authorization`.
    - `rg -n "final-ci-evidence|final-simulator-evidence|final-hardware-safety-media-evidence|final-live-network-transfer-evidence|final-release-artifact-signing-evidence|final-retained-code-acceptance|final-residual-risk-review|final-maintainer-decision|final-reference-demotion-allowed" tools/bazel/manifests/phase28_final_readiness_packet_contract.json tools/bazel/phase28_final_readiness_packet_test.py` returns all nine canonical criteria.
    - `rg -n "demotion_allowed\\s*.*true|demotion_authorization\\s*.*allowed" tools/bazel/phase28_final_readiness_packet.py tools/bazel/phase28_final_readiness_packet_test.py tools/bazel/manifests/phase28_final_readiness_packet_contract.json` returns no matches.
    - `python3 tools/bazel/phase28_final_readiness_packet.py --contract-only` exits 0.
    - `python3 tools/bazel/phase28_final_readiness_packet_test.py` exits 0 and includes assertions for exact generated artifact names.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase28_final_readiness_packet_test.py</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --contract-only</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>Phase 28 has a source-backed contract and test surface proving READ-01, READ-02, READ-03, D-01 through D-12, generated output names, hard-blocker precedence, and separate readiness versus demotion verdicts.</done>
</task>

<task id="28-T2" type="auto" tdd="true">
  <name>Task 2: Implement aggregate verifier, retained outputs, and demotion input validation</name>
  <depends_on>28-T1</depends_on>
  <files>tools/bazel/manifests/phase28_final_readiness_packet_contract.json, tools/bazel/phase28_final_readiness_packet.py, tools/bazel/phase28_final_readiness_packet_test.py</files>
  <write_scope>
    <file>tools/bazel/manifests/phase28_final_readiness_packet_contract.json</file>
    <file>tools/bazel/phase28_final_readiness_packet.py</file>
    <file>tools/bazel/phase28_final_readiness_packet_test.py</file>
  </write_scope>
  <read_first>
    <file>tools/bazel/manifests/phase28_final_readiness_packet_contract.json</file>
    <file>tools/bazel/phase28_final_readiness_packet.py</file>
    <file>tools/bazel/phase28_final_readiness_packet_test.py</file>
    <file>tools/bazel/manifests/phase18_cutover_review_contract.json</file>
    <file>tools/bazel/phase18_cutover_review.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions.py</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions_test.py</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-SUMMARY.md</file>
    <file>.planning/phases/26-release-signing-and-upstream-result-evidence/26-01-SUMMARY.md</file>
  </read_first>
  <behavior>
    - Test 1: Quick mode emits exactly one normalized criteria row for every canonical Phase 18 final criterion and links each row to requirement IDs, Phase 26 upstream status, Phase 27 decision state, exception refs, residual risk refs, blocker refs, artifact refs, and retained source refs.
    - Test 2: Missing Phase 26 row table or Phase 27 handoff keeps final readiness blocked and records actionable missing-input blocker reasons.
    - Test 3: A row with any hard blocker remains blocked even when exception metadata is complete.
    - Test 4: A row with a coverable failing status and complete approved exception metadata can be counted as exception-covered only when there are no hard blockers.
    - Test 5: All green or exception-covered readiness rows keep `reference_demotion_authorization` blocked without `--demotion-decision`.
    - Test 6: Explicit demotion approval is rejected when final readiness is blocked, when Phase 27 handoff lifecycle/source refs drift, when approval metadata is incomplete, or when approval is implied by evidence instead of supplied through `--demotion-decision`.
    - Test 7: Generated `redacted-readiness-report.md` is derived from `final-readiness-packet.json` and includes the same final readiness status, demotion authorization, requirement coverage counts, blocker counts, exception counts, and residual-risk counts.
    - Test 8: Output containment rejects absolute paths, parent traversal, and symlink escape before deleting or writing any Phase 28 retained output.
    - Test 9: Security scans reject private keys, private certificates, signing payload bytes, raw firmware payloads, crash dump bytes, token values, password values, credential values, secret values, unsafe raw refs, and overclaim phrases.
  </behavior>
  <action>
    Implement the Phase 28 functional core in `tools/bazel/phase28_final_readiness_packet.py` as pure functions that accept parsed dictionaries and return normalized packet dictionaries before any filesystem writes. Keep CLI parsing, JSON file I/O, output deletion, and output writing in thin imperative-shell functions per Bright Builds architecture guidance.

    Implement input parsing at the boundary. Load Phase 18, Phase 26, Phase 27, and Phase 28 contracts. Load `--phase26-upstream-rows`, defaulting to `build/ci-evidence/phase26/upstream-result-row-table.json`. Load `--phase27-handoff`, defaulting to `build/ci-evidence/phase27/phase28-handoff-manifest.json`. When files are missing outside quick mode, fail closed with messages naming the missing path and these commands: `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26` and `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27`.

    Implement `--quick` to generate safe local inputs by running only Phase 28 logic against existing Phase 26 and Phase 27 quick outputs when present, or by producing blocked missing-input rows that clearly state `real_external_evidence_supplied: false`, `real_maintainer_demotion_approval_supplied: false`, `final_readiness_status: "blocked"`, and `reference_demotion_authorization: "blocked"`. Do not mark release, signing, hardware, live-service, retained-code, residual-risk, maintainer approval, or demotion authorization as passed from quick mode alone.

    Normalize one row per Phase 18 criterion. Each row must include `criterion_id`, `requirement_ids`, `phase26_upstream_row_ref`, `phase26_status`, `phase27_decision_refs`, `exception_refs`, `residual_risk_refs`, `hard_blocker_refs`, `retained_artifact_refs`, `readiness_effect`, and `demotion_effect`. For D-01 traceability, link `READ-01`, `READ-02`, and `READ-03` at packet level and link the most relevant READ ID per row: evidence and traceability rows to READ-01, blocked-by-default and exception rows to READ-02, and `final-reference-demotion-allowed` to READ-03.

    Derive `final_readiness_status` independently from `reference_demotion_authorization`. `final_readiness_status` starts `blocked`; it can become `passed` only when every non-demotion required criterion has status `passed` or valid exception coverage and no hard blockers. `final-reference-demotion-allowed` must not make final readiness circular; treat it as a separate demotion row with `demotion_effect` and keep its readiness contribution blocked until explicit Phase 28 demotion input exists. This implements D-04, D-07, D-08, D-09, READ-02, and READ-03.

    Validate exceptions using Phase 28 contract fields. Valid exceptions may cover only statuses in `exception_coverable_statuses`, must include `scope`, `owner`, `approver`, `approver_role`, `rationale`, `affected_printer_or_release_surface`, `evidence_refs`, `residual_risk`, `mitigation_or_follow_up`, and `expiry_or_review_trigger`, and must be ignored for rows with hard blockers. This implements D-05 and D-06.

    Validate optional `--demotion-decision`. The input must include `phase: "28-final-readiness-packet-and-demotion-gate"`, `phase_lifecycle_id: "28-2026-06-25T03-31-49"`, `demotion_authorization: "approved"` or `"blocked"`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `scope`, and non-empty `evidence_refs`. Approval requires final readiness not blocked, Phase 27 handoff values `demotion_authorization: "blocked"` and `phase27_may_authorize_demotion: false`, and no lifecycle/source-ref drift. Without this input, emit `reference_demotion_authorization: "blocked"` and `real_maintainer_demotion_approval_supplied: false`.

    Implement output containment for `--output-dir`. Only allow repo-relative paths resolving under `build/ci-evidence/phase28`; reject absolute paths, `..`, and symlink escapes before deleting or writing. Write exactly the Phase 28 generated outputs named in `<interfaces>`, including `contract-snapshots/phase26-upstream-result-row-table.json` and `contract-snapshots/phase27-phase28-handoff-manifest.json`. Do not copy raw external logs, firmware binaries, signing payloads, crash dumps, credentials, or production payloads into Phase 28 outputs.

    Implement `--security-only` to scan contract inputs, optional decision inputs, and generated outputs for forbidden fields and text patterns. Forbidden fields and text patterns must cover private signing keys, private certificates, raw key bytes, signing payload bytes, raw firmware payloads, raw logs, binary dumps, crash dump bytes, token values, password values, credential values, secret values, unsafe raw refs, and overclaim phrases including `reference demotion approved`, `demotion allowed`, `final readiness approved`, and `accepted by evidence alone`.

    Add or extend tests in `tools/bazel/phase28_final_readiness_packet_test.py` for every behavior item above. Use temporary directories for output and symlink escape tests. Ensure report tests load `final-readiness-packet.json` first, then assert `redacted-readiness-report.md` reflects packet values instead of raw input-only values.
  </action>
  <acceptance_criteria>
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26` exits 0 before Phase 27 quick mode is run.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27` exits 0 before Phase 28 quick mode is run.
    - `python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28` exits 0.
    - All Phase 28 generated artifact paths from `<interfaces>` exist under `build/ci-evidence/phase28`.
    - `build/ci-evidence/phase28/final-readiness-packet.json` contains top-level `final_readiness_status`, `reference_demotion_authorization`, `criteria`, `requirements`, and `real_maintainer_demotion_approval_supplied`.
    - `build/ci-evidence/phase28/final-readiness-packet.json` contains all nine canonical Phase 18 criterion IDs and `reference_demotion_authorization` is `blocked` when no `--demotion-decision` is supplied.
    - `rg -n '"demotion_allowed"\\s*:\\s*true|"reference_demotion_authorization"\\s*:\\s*"approved"' build/ci-evidence/phase28` returns no matches after quick mode.
    - `python3 tools/bazel/phase28_final_readiness_packet.py --security-only` exits 0 after quick mode.
    - `python3 tools/bazel/phase28_final_readiness_packet_test.py` includes tests for missing inputs, hard-block-before-exception, valid exception coverage without hard blockers, no implied demotion, explicit demotion rejection while readiness is blocked, lifecycle/source-ref drift rejection, incomplete demotion metadata rejection, report-from-packet behavior, output-root symlink escape, and secret/overclaim scan failures.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet_test.py</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --contract-only</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --security-only</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28</automated>
    <automated>git diff --check</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>Phase 28 can generate the final readiness packet, normalized criteria table, summaries, demotion authorization record, redacted report, artifact refs, and contract snapshots under `build/ci-evidence/phase28`; final readiness and reference demotion remain separate fail-closed verdicts.</done>
</task>

<task id="28-T3" type="auto" tdd="true">
  <name>Task 3: Wire Bazel, workflow dispatch, and just facade</name>
  <depends_on>28-T2</depends_on>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase28_final_readiness_packet.py, tools/bazel/phase28_final_readiness_packet_test.py</files>
  <write_scope>
    <file>tools/bazel/BUILD.bazel</file>
    <file>BUILD.bazel</file>
    <file>tools/bazel/rust_workflow.sh</file>
    <file>justfile</file>
    <file>tools/bazel/phase28_final_readiness_packet.py</file>
    <file>tools/bazel/phase28_final_readiness_packet_test.py</file>
  </write_scope>
  <read_first>
    <file>tools/bazel/BUILD.bazel</file>
    <file>BUILD.bazel</file>
    <file>tools/bazel/rust_workflow.sh</file>
    <file>justfile</file>
    <file>tools/bazel/manifests/phase28_final_readiness_packet_contract.json</file>
    <file>tools/bazel/phase28_final_readiness_packet.py</file>
    <file>tools/bazel/phase28_final_readiness_packet_test.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions.py</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-CONTEXT.md</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-RESEARCH.md</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-VALIDATION.md</file>
    <file>.planning/phases/28-final-readiness-packet-and-demotion-gate/28-01-PLAN.md</file>
  </read_first>
  <behavior>
    - Test 1: `--wiring-only` verifies root docs filegroup, root aliases, tools Bazel targets, workflow cases, and `just phase28-verify`.
    - Test 2: `just phase28-verify` runs `bazel run //tools/bazel:phase28_verify_tests` before `bazel run //tools/bazel:phase28_verify`.
    - Test 3: `phase28_verify` dispatch runs Phase 28 wiring validation, then Phase 26 quick generation, then Phase 27 quick generation from the Phase 26 row table, then Phase 28 quick generation from Phase 26 and Phase 27 outputs.
    - Test 4: Bazel runfiles include Phase 18, Phase 26, Phase 27, and Phase 28 contracts; Phase 26, Phase 27, and Phase 28 verifiers; Phase 28 tests; Phase 28 context/research/validation/plan docs; and source-ref manifests.
  </behavior>
  <action>
    Add a root `filegroup` in `BUILD.bazel` named `phase28_final_readiness_packet_docs` with these `srcs`: `.planning/phases/28-final-readiness-packet-and-demotion-gate/28-CONTEXT.md`, `.planning/phases/28-final-readiness-packet-and-demotion-gate/28-RESEARCH.md`, `.planning/phases/28-final-readiness-packet-and-demotion-gate/28-VALIDATION.md`, and `.planning/phases/28-final-readiness-packet-and-demotion-gate/28-01-PLAN.md`. Add root aliases `phase28_verify` and `phase28_verify_tests` pointing to `//tools/bazel:phase28_verify` and `//tools/bazel:phase28_verify_tests`.

    Add `tools/bazel/BUILD.bazel` filegroup `phase28_source_ref_manifests` with `manifests/phase11_cutover_readiness.json`, `manifests/phase11_retained_code_justifications.json`, `manifests/foreign_code_inventory.json`, `manifests/unsafe_boundary_audit.json`, `manifests/phase18_cutover_review_contract.json`, `manifests/phase26_release_signing_upstream_evidence_contract.json`, `manifests/phase27_retained_code_acceptance_decisions_contract.json`, and `manifests/phase28_final_readiness_packet_contract.json`.

    Add `shell_binary` targets `phase28_verify` and `phase28_verify_tests` using `rust_workflow.sh`. The `phase28_verify` target data must include `phase26_release_signing_upstream_evidence.py`, `phase27_retained_code_acceptance_decisions.py`, `phase28_final_readiness_packet.py`, `manifests/phase28_final_readiness_packet_contract.json`, `:phase26_source_ref_manifests`, `:phase27_source_ref_manifests`, `:phase28_source_ref_manifests`, `//:phase26_release_signing_upstream_evidence_docs`, `//:phase27_retained_code_acceptance_decisions_docs`, and `//:phase28_final_readiness_packet_docs`. The `phase28_verify_tests` target data must include `phase28_final_readiness_packet.py`, `phase28_final_readiness_packet_test.py`, `manifests/phase28_final_readiness_packet_contract.json`, and `:phase28_source_ref_manifests`.

    Add `tools/bazel/rust_workflow.sh` cases `phase28_verify)` and `phase28_verify_tests)`. The `phase28_verify` case must run exactly these commands in order: `python3 tools/bazel/phase28_final_readiness_packet.py --wiring-only`; `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26`; `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27`; `python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28`. The `phase28_verify_tests` case must run `python3 tools/bazel/phase28_final_readiness_packet_test.py`.

    Add a `justfile` recipe `phase28-verify:` that runs `bazel run //tools/bazel:phase28_verify_tests` followed by `bazel run //tools/bazel:phase28_verify`. Extend `--wiring-only` tests in `tools/bazel/phase28_final_readiness_packet_test.py` to copy or synthesize the relevant wiring files in a temp root and verify exact strings from this task. This implements D-12 and verifies workflow wiring drift cannot silently skip Phase 26 or Phase 27 preconditions.
  </action>
  <acceptance_criteria>
    - `rg -n "phase28_final_readiness_packet_docs|phase28_verify|phase28_verify_tests" BUILD.bazel` returns all three root surfaces.
    - `rg -n "phase28_source_ref_manifests|phase28_final_readiness_packet.py|phase28_final_readiness_packet_test.py|phase28_final_readiness_packet_contract.json" tools/bazel/BUILD.bazel` returns all Phase 28 runfile surfaces.
    - `rg -n "phase28_verify\\)|phase28_verify_tests\\)|phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26|phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27|phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28" tools/bazel/rust_workflow.sh` returns all required dispatch strings.
    - `rg -n "phase28-verify:|bazel run //tools/bazel:phase28_verify_tests|bazel run //tools/bazel:phase28_verify" justfile` returns the Phase 28 facade and ordering.
    - `python3 tools/bazel/phase28_final_readiness_packet.py --wiring-only` exits 0.
    - `just phase28-verify` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase28_final_readiness_packet_test.py</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --contract-only</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --security-only</automated>
    <automated>python3 tools/bazel/phase28_final_readiness_packet.py --wiring-only</automated>
    <automated>just phase28-verify</automated>
    <automated>git diff --check</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>Phase 28 is exposed through Bazel, `rust_workflow.sh`, and `just phase28-verify`; the facade runs tests before verifier execution and regenerates Phase 26 and Phase 27 preconditions before Phase 28 packet generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 26 retained rows -> Phase 28 verifier | Generated upstream row data from `build/ci-evidence/phase26` influences final readiness status. |
| Phase 27 handoff -> Phase 28 verifier | Generated retained-code, residual-risk, exception, and demotion starting-state data influences the final packet. |
| Maintainer demotion decision input -> Phase 28 verifier | Untrusted machine-readable authorization input can influence reference demotion state. |
| Phase 28 verifier -> build output filesystem | Phase 28 writes retained evidence under `build/ci-evidence/phase28` and must not escape that tree. |
| Machine packet -> derived report | The human-readable report must not overclaim beyond the machine-readable packet. |
| Workflow wiring -> maintainer command surface | Bazel and just wrappers determine whether maintainers actually run tests and precondition generators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | Phase 28 contract and Phase 18 criteria loading | High | mitigate | Implement `--contract-only` and tests that load `tools/bazel/manifests/phase18_cutover_review_contract.json` at runtime and exact-match all nine final criterion IDs, status vocabularies, exception fields, and hard blocker reasons before packet generation. Schema drift is a blocker. |
| T-28-02 | Information Disclosure | `final-readiness-packet.json`, report, snapshots, artifact summary | High | mitigate | Reject raw evidence, private keys, certificates, signing payload bytes, firmware payload bytes, raw logs, production payloads, crash dump bytes, token/password/credential/secret values, and unsafe raw refs before writing outputs and again in `--security-only`. Secret or unsafe reference leakage is a blocker. |
| T-28-03 | Elevation of Privilege | Exception handling over hard blockers | High | mitigate | Evaluate `redaction-failed`, `overclaim-failed`, `lifecycle-mismatch`, `source-ref-failed`, `unsafe-ref`, and `secret-tainted` before exception coverage; rows with hard blockers remain blocked even with complete exception metadata. Hard blocker bypass by exception is a blocker. |
| T-28-04 | Repudiation | `redacted-readiness-report.md` | Medium | mitigate | Generate the report only from `final-readiness-packet.json` and assert packet/report parity for readiness status, demotion authorization, requirement coverage, blockers, exceptions, and residual risks. Scan for overclaim phrases including `final readiness approved`, `reference demotion approved`, and `demotion allowed`. Report overclaim is a blocker when it implies approval not present in packet data. |
| T-28-05 | Tampering | `--output-dir` retained writer | High | mitigate | Reject absolute paths, parent traversal, non-`build/ci-evidence/phase28` roots, and symlink escapes before deleting or writing output. Output-root escape is a blocker. |
| T-28-06 | Elevation of Privilege | `reference_demotion_authorization` | High | mitigate | Keep `reference_demotion_authorization` separate from `final_readiness_status`; default blocked; require explicit Phase 28 demotion decision input with approver, role, timestamp, rationale, scope, and evidence refs; reject approval inferred from green evidence. Forged or implied demotion approval is a blocker. |
| T-28-07 | Tampering | Phase lifecycle and source-ref validation | High | mitigate | Validate `phase_lifecycle_id: 28-2026-06-25T03-31-49`, Phase 27 handoff values, Phase 26 source lifecycle fields when present, approved source roots, and source contract snapshots. Lifecycle/source-ref drift is a blocker. |
| T-28-08 | Denial of Service | Missing Phase 26 or Phase 27 inputs | Medium | mitigate | Fail closed with actionable missing-input blocker rows and commands to regenerate Phase 26 and Phase 27 quick outputs. Do not write partial pass claims when required inputs are absent. |
| T-28-09 | Tampering | Bazel/root/just workflow wiring | High | mitigate | Implement `--wiring-only` checks and tests for root aliases, docs filegroup, tools targets, runfiles, `rust_workflow.sh` order, and `just phase28-verify` ordering. Workflow wiring drift that skips tests or Phase 26/27 preconditions is a blocker. |
</threat_model>

<verification>
Overall Phase 28 checks after all tasks:

```bash
python3 tools/bazel/phase28_final_readiness_packet_test.py
python3 tools/bazel/phase28_final_readiness_packet.py --contract-only
python3 tools/bazel/phase28_final_readiness_packet.py --security-only
python3 tools/bazel/phase28_final_readiness_packet.py --wiring-only
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26
python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27
python3 tools/bazel/phase28_final_readiness_packet.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --phase27-handoff build/ci-evidence/phase27/phase28-handoff-manifest.json --output-dir build/ci-evidence/phase28
just phase28-verify
git diff --check
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<decision_coverage_matrix>
| Decision | Plan | Task | Coverage | Notes |
|----------|------|------|----------|-------|
| D-01 | 28-01 | 1,2 | Full | Criteria-centric rows link Phase 18 criteria to READ IDs, Phase 26 rows, Phase 27 decisions, exceptions, residual risks, hard blockers, and artifact refs. |
| D-02 | 28-01 | 1,2 | Full | `final-readiness-packet.json` is authority; report derives from packet rows and tests assert parity. |
| D-03 | 28-01 | 1,2 | Full | Secret/unsafe raw evidence is excluded by contract and scanner before retained writes. |
| D-04 | 28-01 | 1,2 | Full | Two-verdict fail-closed model with `final_readiness_status` and `reference_demotion_authorization`. |
| D-05 | 28-01 | 1,2 | Full | Hard blockers evaluated before exception coverage and treated as blockers. |
| D-06 | 28-01 | 1,2 | Full | Exception required fields and coverable-status policy are contract-backed and tested. |
| D-07 | 28-01 | 1,2 | Full | Reference demotion remains separate explicit maintainer decision. |
| D-08 | 28-01 | 1,2 | Full | Default demotion authorization remains blocked and implied approval is rejected. |
| D-09 | 28-01 | 1,2 | Full | Phase 18 demotion criterion and Phase 27 handoff are aligned through handoff validation. |
| D-10 | 28-01 | 2 | Full | Phase 28 consumes retained Phase 26 and Phase 27 outputs instead of redefining evidence producers. |
| D-11 | 28-01 | 1,2 | Full | All named Phase 28 retained outputs are generated under `build/ci-evidence/phase28`. |
| D-12 | 28-01 | 1,2,3 | Full | Tests and workflow wiring cover schema drift, input provenance, fail-closed behavior, exceptions, hard blockers, no-implied-demotion, secret/overclaim guards, retained outputs, and Bazel/just wiring. |
</decision_coverage_matrix>

<dependency_graph>
| Task | Needs | Creates | Wave |
|------|-------|---------|------|
| 28-T1 | Phase 18, Phase 26, Phase 27 contracts and Phase 28 context/research/validation | Phase 28 contract skeleton, CLI skeleton, contract tests | 1 |
| 28-T2 | 28-T1 contract and tests; Phase 26/27 output contracts | Phase 28 aggregate verifier, retained outputs, security checks, demotion input validation | 1 |
| 28-T3 | 28-T2 verifier and tests; existing Bazel/just patterns | Bazel/root/workflow/just Phase 28 wiring | 1 |
</dependency_graph>

<success_criteria>
Phase 28 is complete when:

- `just phase28-verify` passes and runs tests before verifier execution.
- `build/ci-evidence/phase28` contains every generated artifact listed in this plan.
- `final-readiness-packet.json` has one criteria row for each of the nine canonical Phase 18 final criteria.
- Final readiness is blocked by default unless every required row is passed or valid-exception-covered and no hard blockers exist.
- `reference_demotion_authorization` is blocked without an explicit valid Phase 28 demotion decision input, even when evidence rows are green.
- The redacted Markdown report derives from `final-readiness-packet.json` and does not overclaim approval.
- Security scans reject secret-bearing, unsafe-ref, and overclaim inputs or outputs.
- READ-01, READ-02, and READ-03 are represented in plan frontmatter and proven by automated verifier/test commands.
</success_criteria>

<output>
After completion, create `.planning/phases/28-final-readiness-packet-and-demotion-gate/28-01-SUMMARY.md`.
</output>
