---
phase: 13-ci-evidence-orchestration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/bazel/manifests/phase13_ci_evidence_contract.json
  - tools/bazel/phase13_ci_evidence.py
  - tools/bazel/phase13_ci_evidence_test.py
  - .github/workflows/ci-evidence.yml
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - BUILD.bazel
  - justfile
  - .planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md
autonomous: true
requirements:
  - CIEV-01
  - CIEV-02
  - CIEV-03
requirements_addressed:
  - CIEV-01
  - CIEV-02
  - CIEV-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-06-16T14-21-01
generated_at: 2026-06-16T14:44:28.522Z
must_haves:
  truths:
    - "Maintainer can run CI evidence from a repo-owned GitHub Actions workflow for pull requests touching Rust, Bazel, verifier, manifest, planning, workflow, or release-evidence surfaces, and can run it manually with workflow_dispatch."
    - "Maintainer can inspect a run-specific JSON manifest with gate id, requirement_id, owning_phase, command, proof_scope, artifact_path, status, and failure_reason for every Phase 13 gate."
    - "Maintainer can download verifier logs, manifest snapshots, normalized comparison snapshots, and redacted evidence summaries from a non-hidden CI artifact directory."
    - "The workflow and manifest preserve simulator, hardware, live-service, release-candidate, signing, retained-code acceptance, maintainer approval, and reference-demotion evidence as pending-non-local, not passed."
    - "Phase 13 verification is exposed through Python, Bazel labels, root aliases, and just phase13-verify."
  artifacts:
    - path: "tools/bazel/manifests/phase13_ci_evidence_contract.json"
      provides: "Checked-in Phase 13 CI evidence schema and required gate contract"
      contains: "phase13_ci_evidence_contract"
    - path: "tools/bazel/phase13_ci_evidence.py"
      provides: "Phase 13 contract, workflow, security, wiring, and CI evidence writer"
      contains: "Verify Phase 13 CI evidence orchestration"
    - path: "tools/bazel/phase13_ci_evidence_test.py"
      provides: "Standard-library regression tests for contract, workflow, redaction, overclaim, output, and wiring guards"
      contains: "Phase13CiEvidenceTest"
    - path: ".github/workflows/ci-evidence.yml"
      provides: "Repo-owned PR/manual CI evidence workflow"
      contains: "name: CI Evidence"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 13 verifier and verifier-test Bazel labels"
      contains: "phase13_verify"
    - path: "BUILD.bazel"
      provides: "Root Phase 13 docs filegroup and aliases"
      contains: "phase13_ci_evidence_docs"
    - path: "justfile"
      provides: "Developer facade for Phase 13 verification"
      contains: "phase13-verify:"
    - path: ".planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md"
      provides: "Nyquist validation sign-off for Phase 13 local evidence"
      contains: "wave_0_complete: true"
  key_links:
    - from: ".github/workflows/ci-evidence.yml"
      to: "tools/bazel/phase13_ci_evidence.py"
      via: "workflow run step"
      pattern: "python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13"
    - from: "tools/bazel/phase13_ci_evidence.py"
      to: "tools/bazel/manifests/phase13_ci_evidence_contract.json"
      via: "contract loader"
      pattern: "phase13_ci_evidence_contract.json"
    - from: "tools/bazel/phase13_ci_evidence.py"
      to: "tools/bazel/phase11_verify.py"
      via: "aggregate cutover verifier command"
      pattern: "python3 tools/bazel/phase11_verify.py --quick"
    - from: "justfile"
      to: "//tools/bazel:phase13_verify_tests"
      via: "phase13-verify recipe runs tests first"
      pattern: "bazel run //tools/bazel:phase13_verify_tests"
---

<objective>
Make aggregate cutover evidence executable and reviewable from CI while preserving every later non-local cutover gate as pending.

Purpose: Phase 13 turns the Phase 11 local aggregate verifier and v1.0 archived evidence boundaries into a repo-owned CI evidence contract, CI workflow, run manifest, artifact bundle, and local verification facade. This addresses CIEV-01, CIEV-02, and CIEV-03 without implementing simulator, hardware, live-service, release-candidate, signing, retained-code acceptance, or reference-demotion approval evidence.
Output: One Phase 13 CI evidence contract JSON, Python verifier/writer with tests, repo-owned GitHub Actions workflow, Bazel/just wiring, generated CI output under `build/ci-evidence/phase13/`, and completed local validation metadata.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/verification.md
@standards/core/testing.md
@standards/core/code-shape.md
@standards/core/architecture.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-ci-evidence-orchestration/13-CONTEXT.md
@.planning/phases/13-ci-evidence-orchestration/13-RESEARCH.md
@.planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/milestones/v1.0-ROADMAP.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@.planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-VERIFICATION.md
@.planning/milestones/v1.0-phases/12-milestone-evidence-hygiene/12-VERIFICATION.md
@tools/bazel/phase11_verify.py
@tools/bazel/phase11_verify_test.py
@tools/bazel/manifests/phase11_parity_pyramid.json
@tools/bazel/manifests/phase11_requirement_evidence.json
@tools/bazel/manifests/phase11_reference_comparisons.json
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/manifests/phase11_retained_code_justifications.json
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@.github/workflows/stale.yml
@.github/workflows/bright-builds-auto-update.yml
@utils/holly/build-pr.jenkins
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

<interfaces>
Existing Phase 11 aggregate verifier entrypoints to reuse:
```text
python3 tools/bazel/phase11_verify_test.py
python3 tools/bazel/phase11_verify.py --quick
python3 tools/bazel/phase11_verify.py --comparison-only
python3 tools/bazel/phase11_verify.py --security-only
python3 tools/bazel/phase11_verify.py --wiring-only
bazel run //tools/bazel:phase11_verify_tests
bazel run //tools/bazel:phase11_verify
just phase11-verify
```

Phase 13 entrypoints this plan must create:
```text
python3 tools/bazel/phase13_ci_evidence_test.py
python3 tools/bazel/phase13_ci_evidence.py --contract-only
python3 tools/bazel/phase13_ci_evidence.py --workflow-only
python3 tools/bazel/phase13_ci_evidence.py --security-only
python3 tools/bazel/phase13_ci_evidence.py --wiring-only
python3 tools/bazel/phase13_ci_evidence.py --quick
python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13
bazel run //tools/bazel:phase13_verify_tests
bazel run //tools/bazel:phase13_verify
just phase13-verify
```

Phase 13 generated output contract:
```text
build/ci-evidence/phase13/run-manifest.json
build/ci-evidence/phase13/logs/phase11-quick.log
build/ci-evidence/phase13/logs/phase13-contract.log
build/ci-evidence/phase13/manifest-snapshots/phase13_ci_evidence_contract.json
build/ci-evidence/phase13/manifest-snapshots/phase11_requirement_evidence.json
build/ci-evidence/phase13/normalized-comparisons/phase11_reference_comparisons.json
build/ci-evidence/phase13/redacted-summary.json
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 13-01 | 2 | Full: creates `.github/workflows/ci-evidence.yml` and does not edit `.github/workflows/bright-builds-auto-update.yml`. |
| D-02 | 13-01 | 2 | Full: workflow has `pull_request.paths` for Rust, Bazel, verifier, manifest, planning, workflow, and release-evidence surfaces plus `workflow_dispatch`. |
| D-03 | 13-01 | 2 | Full: workflow is self-contained; Jenkins/Holly is cited only as context. |
| D-04 | 13-01 | 2,3 | Full: workflow runs repo-owned Python entrypoint; Bazel/just wiring exposes the same verifier. |
| D-05 | 13-01 | 1 | Full: contract records required gate fields and status semantics. |
| D-06 | 13-01 | 1,2 | Full: checked-in contract plus generated `build/ci-evidence/phase13/run-manifest.json`; build output remains ignored. |
| D-07 | 13-01 | 1,2 | Full: gates map to CIEV-01/02/03 and source archived v1.0 / Phase 11 evidence refs. |
| D-08 | 13-01 | 1,2 | Full: failed/skipped rows must include command, requirement/evidence row, owner phase, artifact path, and failure reason. |
| D-09 | 13-01 | 2 | Full: generated output includes logs, manifest snapshots, normalized comparison snapshot, and redacted summary. |
| D-10 | 13-01 | 1,2 | Full: verifier rejects secret, certificate, key, token, firmware payload, and raw crash dump markers. |
| D-11 | 13-01 | 2 | Full: workflow uses `actions/upload-artifact` on `build/ci-evidence/phase13/` with explicit retention and no-files error. |
| D-12 | 13-01 | 1,2 | Full: non-local gates use `pending-non-local`, never pass claims. |
| D-13 | 13-01 | 1,2,3 | Full: Phase 13 verifier and tests validate contract, workflow, artifacts, redaction, overclaim, Bazel/just, and lifecycle. |
| D-14 | 13-01 | 3 | Full: Bazel labels and `just phase13-verify` are added. |
| D-15 | 13-01 | 1,2 | Full: wrapper runs Phase 11 aggregate verifier and preserves later evidence as pending-non-local. |
| D-16 | 13-01 | 1,2,3 | Full: lifecycle ID `13-2026-06-16T14-21-01` is enforced in contract, verifier, tests, workflow evidence, and validation. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create Phase 13 contract and verifier test harness</name>
  <files>tools/bazel/manifests/phase13_ci_evidence_contract.json, tools/bazel/phase13_ci_evidence.py, tools/bazel/phase13_ci_evidence_test.py</files>
  <read_first>
    - tools/bazel/manifests/phase13_ci_evidence_contract.json (create if missing)
    - tools/bazel/phase13_ci_evidence.py (create if missing)
    - tools/bazel/phase13_ci_evidence_test.py (create if missing)
    - .planning/phases/13-ci-evidence-orchestration/13-CONTEXT.md
    - .planning/phases/13-ci-evidence-orchestration/13-RESEARCH.md
    - .planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md
    - tools/bazel/phase11_verify.py
    - tools/bazel/phase11_verify_test.py
    - tools/bazel/manifests/phase11_parity_pyramid.json
    - tools/bazel/manifests/phase11_requirement_evidence.json
    - tools/bazel/manifests/phase11_reference_comparisons.json
    - tools/bazel/manifests/phase11_cutover_readiness.json
    - tools/bazel/manifests/phase11_retained_code_justifications.json
    - .planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-VERIFICATION.md
    - standards/core/testing.md
    - standards/core/code-shape.md
  </read_first>
  <behavior>
    - Contract validation accepts the required Phase 13 schema and rejects missing lifecycle, missing gate fields, path traversal, unsupported statuses, and empty failure semantics.
    - Contract rows map directly to CIEV-01, CIEV-02, and CIEV-03.
    - Contract rows preserve archived v1.0 / Phase 11 evidence refs and do not use roadmap-only proof.
    - Regression tests use standard-library `unittest`, temp roots, subprocess verifier calls, and explicit Arrange/Act/Assert comments.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase13_ci_evidence_contract.json` as the checked-in source-of-truth contract per D-05, D-06, D-07, D-08, D-12, D-13, and D-16. Use valid JSON with these exact top-level fields:
    - `"schema_version": "1"`
    - `"id": "phase13_ci_evidence_contract"`
    - `"phase": "13-ci-evidence-orchestration"`
    - `"phase_lifecycle_id": "13-2026-06-16T14-21-01"`
    - `"output_root": "build/ci-evidence/phase13"`
    - `"artifact_name": "phase13-ci-evidence"`
    - `"retention_days": 30`
    - `"status_vocabulary": ["passed", "failed", "skipped", "pending-non-local"]`
    - `"required_artifact_kinds": ["verifier-log", "manifest-snapshot", "normalized-comparison-output", "redacted-evidence-summary", "machine-readable-ci-manifest"]`
    - `"forbidden_generated_artifacts": ["firmware packages", "raw crash dumps", "private certificates", "signing keys", "Connect tokens", "Wi-Fi credentials", "credential values"]`
    - `"gates": [...]`

    Populate `"gates"` with exactly these five gate objects:
    1. `ciev-01-pr-path-trigger`: `requirement_id: "CIEV-01"`, `owning_phase: "13-ci-evidence-orchestration"`, `command: "python3 tools/bazel/phase13_ci_evidence.py --workflow-only"`, `proof_scope: "ci-orchestration"`, `expected_artifact_path: "build/ci-evidence/phase13/logs/phase13-workflow.log"`, `retained_artifact_kind: "verifier-log"`, `allowed_statuses: ["passed", "failed"]`, `failure_reason_semantics: "failed row must name workflow path, missing trigger or path filter, owner phase, artifact path, and reason"`.
    2. `ciev-01-aggregate-cutover-verifier`: `requirement_id: "CIEV-01"`, `command: "python3 tools/bazel/phase11_verify.py --quick"`, `proof_scope: "ci-local-aggregate"`, `expected_artifact_path: "build/ci-evidence/phase13/logs/phase11-quick.log"`, `retained_artifact_kind: "verifier-log"`, `allowed_statuses: ["passed", "failed"]`, `failure_reason_semantics: "failed row must name Phase 11 verifier command, owner phase, requirement or evidence row, artifact path, and reason"`.
    3. `ciev-02-run-manifest`: `requirement_id: "CIEV-02"`, `command: "python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13"`, `proof_scope: "ci-generated-manifest"`, `expected_artifact_path: "build/ci-evidence/phase13/run-manifest.json"`, `retained_artifact_kind: "machine-readable-ci-manifest"`, `allowed_statuses: ["passed", "failed"]`, `failure_reason_semantics: "failed or skipped row must include command, requirement_id, owning_phase, artifact_path, and failure_reason"`.
    4. `ciev-03-manifest-and-comparison-snapshots`: `requirement_id: "CIEV-03"`, `command: "python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13"`, `proof_scope: "ci-artifact-retention"`, `expected_artifact_path: "build/ci-evidence/phase13/manifest-snapshots/phase13_ci_evidence_contract.json"`, `retained_artifact_kind: "manifest-snapshot"`, `allowed_statuses: ["passed", "failed", "pending-non-local"]`, `failure_reason_semantics: "pending-non-local rows must name later phase evidence, source snapshot, artifact path, and reason"`.
    5. `ciev-03-redacted-summary`: `requirement_id: "CIEV-03"`, `command: "python3 tools/bazel/phase13_ci_evidence.py --security-only"`, `proof_scope: "ci-artifact-retention"`, `expected_artifact_path: "build/ci-evidence/phase13/redacted-summary.json"`, `retained_artifact_kind: "redacted-evidence-summary"`, `allowed_statuses: ["passed", "failed", "pending-non-local"]`, `failure_reason_semantics: "failed row must name forbidden marker, source path, owner phase, artifact path, and reason"`.

    Every gate must include `source_evidence_refs` with at least `.planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-VERIFICATION.md`, `tools/bazel/manifests/phase11_requirement_evidence.json`, and `tools/bazel/manifests/phase11_cutover_readiness.json`.

    Create `tools/bazel/phase13_ci_evidence.py` as a standard-library Python 3 script with:
    - constants `PHASE = "13-ci-evidence-orchestration"` and `PHASE_LIFECYCLE_ID = "13-2026-06-16T14-21-01"`;
    - `CONTRACT_MANIFEST = Path("tools/bazel/manifests/phase13_ci_evidence_contract.json")`;
    - `DEFAULT_OUTPUT_DIR = Path("build/ci-evidence/phase13")`;
    - `FORBIDDEN_TEXT_PATTERNS` covering private key headers, certificate headers, `certificate_pem`, `password_value`, `token_value`, `certificate_bytes`, `private_key`, `signing_key_value`, `raw_crash_dump`, `firmware_payload`, `Connect token`, `Wi-Fi credential`, and `credential value`;
    - `OVERCLAIM_STRINGS` covering `hardware verified locally`, `local hardware proof`, `simulator passed locally`, `live service passed locally`, `release-candidate passed locally`, `signing verified locally`, `retained-code accepted by maintainer`, `reference demotion approved`, `reference removal complete`, and `cutover complete`;
    - helpers named `read_text`, `load_json`, `require_string`, `require_list_of_strings`, `require_fields`, `require_repo_relative_under`, `reject_forbidden_text`, `check_contract`, `check_security`, `parse_args`, `selected_checks`, and `main`;
    - CLI flags `--repo-root`, `--quick`, `--contract-only`, `--workflow-only`, `--security-only`, `--wiring-only`, `--ci`, and `--output-dir`.

    In this task, make `--contract-only` fully validate the JSON contract and make `--quick` run `check_contract` plus `check_security`. It is acceptable for `--workflow-only`, `--wiring-only`, and `--ci` to exist and return explicit missing-surface errors until Tasks 2 and 3 create those surfaces; do not make them pass by ignoring missing files.

    Create `tools/bazel/phase13_ci_evidence_test.py` with `unittest.TestCase` class `Phase13CiEvidenceTest`, temp-root fixture helpers, `run_verifier(args, maybe_root=None)`, and these tests:
    - `test_contract_only_accepts_complete_contract`
    - `test_contract_only_rejects_missing_required_gate`
    - `test_contract_only_rejects_path_traversal_artifact_path`
    - `test_contract_only_rejects_unknown_requirement_id`
    - `test_contract_only_rejects_unsupported_status`
    - `test_security_only_rejects_secret_marker`
    - `test_security_only_rejects_non_local_overclaim`
  </action>
  <verify>
    <automated>python3 tools/bazel/phase13_ci_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase13_ci_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase13_ci_evidence.py --security-only</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m json.tool tools/bazel/manifests/phase13_ci_evidence_contract.json >/dev/null` exits 0.
    - `rg '"phase_lifecycle_id": "13-2026-06-16T14-21-01"' tools/bazel/manifests/phase13_ci_evidence_contract.json`
    - `rg '"ciev-01-pr-path-trigger"|"ciev-01-aggregate-cutover-verifier"|"ciev-02-run-manifest"|"ciev-03-manifest-and-comparison-snapshots"|"ciev-03-redacted-summary"' tools/bazel/manifests/phase13_ci_evidence_contract.json`
    - `rg 'def check_contract|def check_security|--contract-only|--workflow-only|--wiring-only|--ci|--output-dir' tools/bazel/phase13_ci_evidence.py`
    - `rg 'test_contract_only_accepts_complete_contract|test_contract_only_rejects_missing_required_gate|test_contract_only_rejects_path_traversal_artifact_path|test_contract_only_rejects_unknown_requirement_id|test_contract_only_rejects_unsupported_status|test_security_only_rejects_secret_marker|test_security_only_rejects_non_local_overclaim' tools/bazel/phase13_ci_evidence_test.py`
    - Every command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>Wave 0 validation files for the Phase 13 contract and verifier test harness exist and pass contract/security checks without implementing later non-local evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned CI workflow and generated evidence writer</name>
  <files>.github/workflows/ci-evidence.yml, tools/bazel/phase13_ci_evidence.py, tools/bazel/phase13_ci_evidence_test.py, tools/bazel/manifests/phase13_ci_evidence_contract.json</files>
  <read_first>
    - .github/workflows/ci-evidence.yml (create if missing)
    - tools/bazel/phase13_ci_evidence.py
    - tools/bazel/phase13_ci_evidence_test.py
    - tools/bazel/manifests/phase13_ci_evidence_contract.json
    - .github/workflows/stale.yml
    - .github/workflows/bright-builds-auto-update.yml
    - utils/holly/build-pr.jenkins
    - tools/bazel/phase11_verify.py
    - tools/bazel/manifests/phase11_reference_comparisons.json
    - tools/bazel/manifests/phase11_requirement_evidence.json
    - .planning/phases/13-ci-evidence-orchestration/13-CONTEXT.md
    - .planning/phases/13-ci-evidence-orchestration/13-RESEARCH.md
    - .planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md
  </read_first>
  <behavior>
    - Workflow has `pull_request.paths` coverage for Rust, Bazel, verifier, manifest, planning, workflow, and release-evidence surfaces.
    - Workflow has `workflow_dispatch`, minimum read-only permissions, a thin repo-owned Python command, and artifact upload from `build/ci-evidence/phase13/`.
    - CI writer creates machine-readable run manifest, logs, manifest snapshots, normalized comparison snapshot, and redacted summary before returning failure.
    - Generated artifacts never include raw secrets, firmware packages, raw crash dumps, private certificates, signing keys, Connect tokens, Wi-Fi credentials, or credential values.
    - Non-local evidence from Phases 14-18 remains `pending-non-local`.
  </behavior>
  <action>
    Create `.github/workflows/ci-evidence.yml` per D-01 through D-04, D-09 through D-12, and D-15. Do not edit `.github/workflows/bright-builds-auto-update.yml`.

    The workflow must use these exact structural values:
    - `name: CI Evidence`
    - `on.pull_request.paths` includes:
      - `.github/workflows/ci-evidence.yml`
      - `.github/workflows/**`
      - `BUILD.bazel`
      - `MODULE.bazel`
      - `.bazelrc`
      - `platforms/**`
      - `tools/bazel/**`
      - `tools/bazel/manifests/**`
      - `Cargo.toml`
      - `Cargo.lock`
      - `rust/**`
      - `.planning/PROJECT.md`
      - `.planning/ROADMAP.md`
      - `.planning/REQUIREMENTS.md`
      - `.planning/STATE.md`
      - `.planning/phases/**`
      - `.planning/milestones/**`
      - `CMakeLists.txt`
      - `ProjectOptions.cmake`
      - `cmake/**`
      - `utils/build.py`
      - `utils/pack_fw.py`
      - `utils/dfu.py`
      - `utils/presets/**`
      - `src/resources/**`
      - `src/lang/**`
      - `lib/Add*.cmake`
    - `workflow_dispatch:`
    - top-level or job-level `permissions: contents: read`
    - one job named `phase13-ci-evidence` on `ubuntu-latest`
    - checkout step `uses: actions/checkout@v6`
    - run step command exactly `python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13`
    - upload step `uses: actions/upload-artifact@v7`
    - upload step `if: always()`
    - upload step `name: phase13-ci-evidence-${{ github.run_id }}-${{ github.run_attempt }}`
    - upload step `path: build/ci-evidence/phase13/`
    - upload step `retention-days: 30`
    - upload step `if-no-files-found: error`

    Keep workflow YAML thin: no multiline shell program, no `bash -c`, no `python -c`, no `node -e`, no secret env vars, and no write permissions.

    Extend `tools/bazel/phase13_ci_evidence.py` so `--workflow-only` validates the workflow by source text using standard-library checks:
    - file path `.github/workflows/ci-evidence.yml` exists;
    - all path filters listed above appear in the workflow;
    - `workflow_dispatch`, `permissions:`, `contents: read`, `actions/checkout@v6`, `actions/upload-artifact@v7`, `if: always()`, `retention-days: 30`, `if-no-files-found: error`, and `build/ci-evidence/phase13/` appear;
    - workflow does not contain `contents: write`, `pull-requests: write`, `id-token: write`, `secrets.`, `bash -c`, `python -c`, `node -e`, `run: |`, or `run: >`.

    Extend `--ci` so it creates the deterministic output tree under the supplied output directory, always writes evidence files, and returns nonzero only after writing artifacts if a required local gate fails:
    - create `logs/`, `manifest-snapshots/`, and `normalized-comparisons/`;
    - run `python3 tools/bazel/phase13_ci_evidence.py --contract-only`, capture output to `logs/phase13-contract.log`, and record gate `ciev-02-run-manifest`;
    - run `python3 tools/bazel/phase13_ci_evidence.py --workflow-only`, capture output to `logs/phase13-workflow.log`, and record gate `ciev-01-pr-path-trigger`;
    - run `python3 tools/bazel/phase11_verify.py --quick`, capture output to `logs/phase11-quick.log`, and record gate `ciev-01-aggregate-cutover-verifier`;
    - copy `tools/bazel/manifests/phase13_ci_evidence_contract.json`, `tools/bazel/manifests/phase11_requirement_evidence.json`, `tools/bazel/manifests/phase11_cutover_readiness.json`, and `tools/bazel/manifests/phase11_retained_code_justifications.json` into `manifest-snapshots/`;
    - copy `tools/bazel/manifests/phase11_reference_comparisons.json` into `normalized-comparisons/phase11_reference_comparisons.json`;
    - write `redacted-summary.json` with `schema_version`, `phase`, `phase_lifecycle_id`, `generated_at_utc`, `summary`, `gates`, `pending_non_local_evidence`, and no credential values;
    - write `run-manifest.json` with `schema_version`, `phase`, `phase_lifecycle_id`, `generated_at_utc`, `output_root`, `artifact_name`, `retention_days`, and `gates`.

    Every generated gate row must include `id`, `requirement_id`, `owning_phase`, `command`, `proof_scope`, `artifact_path`, `retained_artifact_kind`, `status`, and `failure_reason`. For passed rows, use `failure_reason: ""`. For failed rows, include the command exit code or missing validation detail. For pending rows, include the later-phase evidence class and reason.

    The generated `pending_non_local_evidence` list must include exactly these strings: `simulator evidence (Phase 14)`, `hardware safety and media evidence (Phase 15)`, `live network and transfer evidence (Phase 16)`, `release-candidate artifact and signing evidence (Phase 17)`, `retained-code acceptance and cutover review evidence (Phase 18)`.

    Extend tests with:
    - `test_workflow_only_accepts_ci_evidence_workflow`
    - `test_workflow_only_rejects_missing_artifact_upload`
    - `test_workflow_only_rejects_write_permissions`
    - `test_workflow_only_rejects_hidden_planning_upload_path`
    - `test_workflow_only_rejects_inline_shell_logic`
    - `test_ci_writes_manifest_logs_snapshots_and_redacted_summary`
    - `test_ci_manifest_records_failed_gate_after_logs_are_written`
    - `test_ci_manifest_preserves_pending_non_local_evidence`
  </action>
  <verify>
    <automated>python3 tools/bazel/phase13_ci_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase13_ci_evidence.py --workflow-only --security-only</automated>
    <automated>python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/ci-evidence.yml`
    - `rg 'name: CI Evidence|workflow_dispatch:|contents: read|actions/checkout@v6|actions/upload-artifact@v7|retention-days: 30|if-no-files-found: error|build/ci-evidence/phase13/' .github/workflows/ci-evidence.yml`
    - `rg 'rust/\\*\\*|tools/bazel/\\*\\*|tools/bazel/manifests/\\*\\*|\\.planning/phases/\\*\\*|\\.planning/milestones/\\*\\*|utils/pack_fw.py|utils/dfu.py|src/resources/\\*\\*|src/lang/\\*\\*' .github/workflows/ci-evidence.yml`
    - `! rg 'contents: write|pull-requests: write|id-token: write|secrets\\.|bash -c|python -c|node -e|run: \\||run: >' .github/workflows/ci-evidence.yml`
    - `rg 'def check_workflow|def write_ci_evidence|run-manifest.json|redacted-summary.json|pending_non_local_evidence|phase11_reference_comparisons.json' tools/bazel/phase13_ci_evidence.py`
    - `test -f build/ci-evidence/phase13/run-manifest.json`
    - `test -f build/ci-evidence/phase13/redacted-summary.json`
    - `test -f build/ci-evidence/phase13/logs/phase11-quick.log`
    - `test -f build/ci-evidence/phase13/manifest-snapshots/phase13_ci_evidence_contract.json`
    - `test -f build/ci-evidence/phase13/normalized-comparisons/phase11_reference_comparisons.json`
    - Every command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>The repo-owned CI workflow runs Phase 13 evidence orchestration, uploads the deterministic evidence directory, and the local verifier proves trigger/path coverage, artifact retention, redaction, and pending non-local semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Bazel, just, and validation sign-off</name>
  <files>tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, BUILD.bazel, justfile, .planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md, tools/bazel/phase13_ci_evidence.py, tools/bazel/phase13_ci_evidence_test.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
    - .planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md
    - tools/bazel/phase13_ci_evidence.py
    - tools/bazel/phase13_ci_evidence_test.py
    - tools/bazel/manifests/phase13_ci_evidence_contract.json
    - .github/workflows/ci-evidence.yml
    - .planning/phases/13-ci-evidence-orchestration/13-CONTEXT.md
    - .planning/phases/13-ci-evidence-orchestration/13-RESEARCH.md
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
  </read_first>
  <action>
    Wire Phase 13 through existing repo-owned Bazel and `just` entrypoints per D-04, D-13, D-14, and D-16.

    In `tools/bazel/BUILD.bazel`, add:
    - `shell_binary(name = "phase13_verify", src = "rust_workflow.sh", data = ["phase13_ci_evidence.py", "manifests/phase13_ci_evidence_contract.json", "//:phase13_ci_evidence_docs"])`
    - `shell_binary(name = "phase13_verify_tests", src = "rust_workflow.sh", data = ["phase13_ci_evidence.py", "phase13_ci_evidence_test.py", "manifests/phase13_ci_evidence_contract.json"])`

    In root `BUILD.bazel`, add:
    - `filegroup(name = "phase13_ci_evidence_docs", srcs = [".planning/phases/13-ci-evidence-orchestration/13-CONTEXT.md", ".planning/phases/13-ci-evidence-orchestration/13-RESEARCH.md", ".planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md", ".planning/phases/13-ci-evidence-orchestration/13-01-PLAN.md", ".github/workflows/ci-evidence.yml"])`
    - `alias(name = "phase13_verify", actual = "//tools/bazel:phase13_verify")`
    - `alias(name = "phase13_verify_tests", actual = "//tools/bazel:phase13_verify_tests")`

    In `tools/bazel/rust_workflow.sh`, add cases before `*)`:
    - `phase13_verify)` runs `python3 tools/bazel/phase13_ci_evidence.py --wiring-only` then `python3 tools/bazel/phase13_ci_evidence.py --quick`
    - `phase13_verify_tests)` runs `python3 tools/bazel/phase13_ci_evidence_test.py`

    In `justfile`, add:
    - `phase13-verify:`
    - `    bazel run //tools/bazel:phase13_verify_tests`
    - `    bazel run //tools/bazel:phase13_verify`

    Extend `tools/bazel/phase13_ci_evidence.py --wiring-only` to require these exact strings:
    - in `tools/bazel/BUILD.bazel`: `name = "phase13_verify"`, `name = "phase13_verify_tests"`, `phase13_ci_evidence.py`, `phase13_ci_evidence_test.py`, `phase13_ci_evidence_contract.json`;
    - in `tools/bazel/rust_workflow.sh`: `phase13_verify)`, `python3 tools/bazel/phase13_ci_evidence.py --wiring-only`, `python3 tools/bazel/phase13_ci_evidence.py --quick`, `phase13_verify_tests)`, `python3 tools/bazel/phase13_ci_evidence_test.py`;
    - in root `BUILD.bazel`: `name = "phase13_ci_evidence_docs"`, `name = "phase13_verify"`, `name = "phase13_verify_tests"`;
    - in `justfile`: `phase13-verify:`, `bazel run //tools/bazel:phase13_verify_tests`, `bazel run //tools/bazel:phase13_verify`.

    Extend tests with:
    - `test_wiring_only_accepts_phase13_bazel_and_just_surface`
    - `test_wiring_only_rejects_missing_just_recipe`
    - `test_wiring_only_rejects_missing_rust_workflow_dispatch`

    After all commands pass, update `.planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md`:
    - frontmatter `status: local-signoff`
    - frontmatter `wave_0_complete: true`
    - Per-Task Verification Map rows `13-01-01`, `13-01-02`, `13-01-03`, and `13-01-04` status changed from `pending` to `green`
    - Validation Sign-Off `**Approval:** local-signoff`
    Do not change manual-only verifications into automated pass claims.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase13_ci_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase13_ci_evidence.py --wiring-only</automated>
    <automated>python3 tools/bazel/phase13_ci_evidence.py --quick</automated>
    <automated>just phase13-verify</automated>
    <automated>test "$(node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 13 --require-plans --raw)" = valid</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'name = "phase13_verify"|name = "phase13_verify_tests"|phase13_ci_evidence.py|phase13_ci_evidence_test.py|phase13_ci_evidence_contract.json' tools/bazel/BUILD.bazel`
    - `rg 'phase13_verify\\)|phase13_verify_tests\\)|python3 tools/bazel/phase13_ci_evidence.py --wiring-only|python3 tools/bazel/phase13_ci_evidence.py --quick|python3 tools/bazel/phase13_ci_evidence_test.py' tools/bazel/rust_workflow.sh`
    - `rg 'name = "phase13_ci_evidence_docs"|name = "phase13_verify"|name = "phase13_verify_tests"|\\.github/workflows/ci-evidence.yml' BUILD.bazel`
    - `rg 'phase13-verify:|bazel run //tools/bazel:phase13_verify_tests|bazel run //tools/bazel:phase13_verify' justfile`
    - `rg 'status: local-signoff|wave_0_complete: true|13-01-01 \\| 01 \\| 0 \\| CIEV-02 .* green|13-01-04 \\| 01 \\| 0 \\| CIEV-01, CIEV-02, CIEV-03 .* green|\\*\\*Approval:\\*\\* local-signoff' .planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md`
    - Every command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>Phase 13 evidence verification is available through Python, Bazel, and `just phase13-verify`, and validation metadata records local sign-off without converting later non-local gates into pass claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull request diff -> GitHub Actions workflow | Untrusted PR changes decide whether CI evidence orchestration runs through path filters. |
| Checked-in contract/workflow -> Phase 13 verifier | Source-controlled JSON and YAML are parsed as evidence contracts and must not hide logic, path traversal, or overclaim wording. |
| Verifier subprocess -> generated CI artifacts | Command output is captured into retained logs and manifests; failure must be recorded before process exit. |
| Source evidence -> uploaded artifact bundle | Planning docs and manifests are copied into a non-hidden artifact directory after redaction checks. |
| GitHub Actions token -> repository checkout/upload actions | Workflow token permissions must stay read-only and must not expose secrets or write scopes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | `tools/bazel/phase13_ci_evidence.py` redaction scan and generated artifacts | mitigate | Reject private-key and certificate headers, token/password/private-key/signing-key/raw-crash-dump/firmware-payload markers, and generated artifacts containing credential values before writing `redacted-summary.json` or returning success. |
| T-13-02 | Tampering | `expected_artifact_path`, manifest snapshots, and output directory handling | mitigate | Validate every contract artifact path is repo-relative, contains no `..`, and stays under `build/ci-evidence/phase13`; copy only named manifest snapshots and normalized comparison snapshots. |
| T-13-03 | Spoofing / Repudiation | Gate status vocabulary and non-local evidence rows | mitigate | Allow only `passed`, `failed`, `skipped`, and `pending-non-local`; reject overclaim strings such as local hardware proof, simulator passed locally, release-candidate passed locally, signing verified locally, retained-code accepted by maintainer, reference demotion approved, reference removal complete, and cutover complete. |
| T-13-04 | Elevation of Privilege | `.github/workflows/ci-evidence.yml` GitHub token permissions | mitigate | Require `permissions: contents: read`; reject `contents: write`, `pull-requests: write`, `id-token: write`, `secrets.`, and custom secret environment usage in the Phase 13 workflow. |
| T-13-05 | Tampering | Workflow YAML command surface | mitigate | Keep workflow YAML as thin orchestration with one repo-owned Python command; verifier rejects `run: |`, `run: >`, `bash -c`, `python -c`, and `node -e` so substantive logic stays in reviewed Python. |
| T-13-06 | Denial of Service | Artifact retention step | mitigate | Upload `build/ci-evidence/phase13/` with `if: always()`, `if-no-files-found: error`, and explicit `retention-days: 30` so failed verifier runs still retain diagnosis artifacts and missing artifacts fail clearly. |
</threat_model>

<verification>
Run these commands after Task 2 completes:

```bash
python3 tools/bazel/phase13_ci_evidence_test.py
python3 tools/bazel/phase13_ci_evidence.py --contract-only
python3 tools/bazel/phase13_ci_evidence.py --workflow-only --security-only
python3 tools/bazel/phase13_ci_evidence.py --wiring-only
python3 tools/bazel/phase13_ci_evidence.py --quick
python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13
just phase13-verify
git diff --check
test "$(node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 13 --require-plans --raw)" = valid
```

The `--ci` command must leave `build/ci-evidence/phase13/run-manifest.json`, `logs/phase11-quick.log`, `manifest-snapshots/phase13_ci_evidence_contract.json`, `normalized-comparisons/phase11_reference_comparisons.json`, and `redacted-summary.json` on disk even if a required verifier gate fails.
</verification>

<success_criteria>
- `.github/workflows/bright-builds-auto-update.yml` is unchanged.
- `.github/workflows/ci-evidence.yml` exists, runs on path-filtered pull requests and `workflow_dispatch`, uses read-only permissions, calls `python3 tools/bazel/phase13_ci_evidence.py --ci --output-dir build/ci-evidence/phase13`, and uploads `build/ci-evidence/phase13/`.
- `tools/bazel/manifests/phase13_ci_evidence_contract.json` maps every gate to `CIEV-01`, `CIEV-02`, or `CIEV-03`, includes lifecycle `13-2026-06-16T14-21-01`, and rejects path traversal, overclaim, and forbidden secret markers through tests.
- `build/ci-evidence/phase13/run-manifest.json` records status, owning phase, command, artifact path, and failure reason for each cutover gate.
- Downloadable artifact directory includes verifier logs, manifest snapshots, normalized comparison snapshot, and redacted summary.
- `bazel run //tools/bazel:phase13_verify_tests`, `bazel run //tools/bazel:phase13_verify`, and `just phase13-verify` pass.
- `.planning/phases/13-ci-evidence-orchestration/13-VALIDATION.md` records local sign-off for Phase 13 only and keeps later non-local evidence manual/pending.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ci-evidence-orchestration/13-01-SUMMARY.md`.
</output>
