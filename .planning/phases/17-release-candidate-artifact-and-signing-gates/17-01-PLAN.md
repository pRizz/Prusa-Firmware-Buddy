---
phase: 17-release-candidate-artifact-and-signing-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
  - tools/bazel/phase17_release_candidate_evidence.py
  - tools/bazel/phase17_release_candidate_evidence_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-06-19T13-57-17
generated_at: 2026-06-19T14:16:15Z
user_setup: []
must_haves:
  truths:
    - "Release managers can inspect one Phase 17 contract that enumerates every required release artifact family instead of an umbrella release pass."
    - "Every release artifact evidence row names a Bazel-owned workflow identity and release command, while representative artifact labels remain local smoke only."
    - "Local quick verification writes deterministic redacted evidence under build/ci-evidence/phase17 while keeping production release and signing rows pending without supplied release evidence."
    - "Signing and provenance evidence records key identity metadata, build input identity, artifact digest, timestamp, retention path, and verification outcome without private key material or firmware payload bytes."
    - "Every release comparison row cites archived reference evidence and uses exactly one mismatch class: pass, intentional-delta, blocker, or deferred-retained-code-issue."
    - "Bazel labels and just phase17-verify run verifier tests before the verifier."
  artifacts:
    - path: "tools/bazel/manifests/phase17_release_candidate_evidence_contract.json"
      provides: "Row-level release artifact, signing, provenance, retention, and comparison evidence contract"
      contains: "phase17_release_candidate_evidence_contract"
    - path: "tools/bazel/phase17_release_candidate_evidence.py"
      provides: "Stdlib verifier, release evidence validator, quick artifact writer, security scan, and wiring check"
      exports: ["--contract-only", "--security-only", "--quick", "--release-evidence", "--wiring-only"]
    - path: "tools/bazel/phase17_release_candidate_evidence_test.py"
      provides: "Unit coverage for contract rows, release evidence inputs, redaction, path guards, mismatch classes, generated artifacts, and wiring"
      contains: "Phase17ReleaseCandidateEvidenceTest"
    - path: "tools/bazel/BUILD.bazel"
      provides: "phase17_release_candidate_artifacts workflow identity, phase17_source_ref_manifests filegroup, and phase17_verify / phase17_verify_tests labels"
      contains: "phase17_release_candidate_artifacts"
    - path: "BUILD.bazel"
      provides: "Root Phase 17 docs filegroup, phase17_release_candidate_artifacts alias, and verifier aliases"
      contains: "phase17_release_candidate_evidence_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "phase17_verify and phase17_verify_tests dispatch cases"
      contains: "phase17_release_candidate_evidence.py --quick"
    - path: "justfile"
      provides: "Developer facade recipe"
      contains: "phase17-verify:"
  key_links:
    - from: "tools/bazel/phase17_release_candidate_evidence.py"
      to: "tools/bazel/manifests/phase17_release_candidate_evidence_contract.json"
      via: "CONTRACT_MANIFEST constant"
      pattern: "phase17_release_candidate_evidence_contract.json"
    - from: "tools/bazel/phase17_release_candidate_evidence.py"
      to: "build/ci-evidence/phase17"
      via: "DEFAULT_OUTPUT_DIR constant and path guard"
      pattern: "build/ci-evidence/phase17"
    - from: "tools/bazel/phase17_release_candidate_evidence.py"
      to: "tools/bazel/manifests/phase11_reference_comparisons.json"
      via: "source refs and mismatch taxonomy"
      pattern: "phase11_reference_comparisons.json"
    - from: "tools/bazel/manifests/phase17_release_candidate_evidence_contract.json"
      to: "//tools/bazel:phase17_release_candidate_artifacts"
      via: "bazel_label and release_command fields on release artifact rows"
      pattern: "phase17_release_candidate_artifacts"
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/rust_workflow.sh"
      via: "shell_binary src"
      pattern: "src = \"rust_workflow.sh\""
    - from: "justfile"
      to: "//tools/bazel:phase17_verify_tests"
      via: "phase17-verify recipe first command"
      pattern: "bazel run //tools/bazel:phase17_verify_tests"
---

<objective>
Create the Phase 17 release-candidate artifact and signing evidence gate.

Purpose: Convert release artifact, signing, provenance, retention, and archived reference-comparison blockers into a Bazel-owned, redaction-safe, machine-verifiable workflow without claiming local production release approval.

Output: A checked-in Phase 17 JSON contract, stdlib Python verifier/tests, Bazel-owned release artifact workflow identity, root aliases/docs filegroup, `rust_workflow.sh` dispatch, and `just phase17-verify`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-release-candidate-artifact-and-signing-gates/17-CONTEXT.md
@.planning/phases/17-release-candidate-artifact-and-signing-gates/17-RESEARCH.md
@.planning/phases/17-release-candidate-artifact-and-signing-gates/17-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md

@.planning/phases/13-ci-evidence-orchestration/13-01-SUMMARY.md
@.planning/phases/15-hardware-safety-and-media-qualification/15-01-SUMMARY.md
@.planning/phases/16-live-network-and-transfer-qualification/16-01-SUMMARY.md
@.planning/milestones/v1.0-phases/03-artifact-and-generator-parity/03-01-SUMMARY.md
@.planning/milestones/v1.0-phases/03-artifact-and-generator-parity/03-02-SUMMARY.md
@.planning/milestones/v1.0-phases/03-artifact-and-generator-parity/03-03-SUMMARY.md
@.planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-01-SUMMARY.md
@.planning/milestones/v1.0-phases/11-parity-pyramid-and-cutover-evidence/11-03-SUMMARY.md

@tools/bazel/phase16_live_network_evidence.py
@tools/bazel/phase16_live_network_evidence_test.py
@tools/bazel/manifests/phase16_live_network_evidence_contract.json
@tools/bazel/phase15_hardware_evidence.py
@tools/bazel/phase13_ci_evidence.py
@tools/bazel/phase11_verify.py
@tools/bazel/manifests/phase11_reference_comparisons.json
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/manifests/representative_products.json
@tools/bazel/artifact_packager.py
@tools/bazel/artifact_manifest.py
@tools/bazel/artifact_metadata_compare.py
@tools/bazel/artifact_rules.bzl
@tools/bazel/phase3_artifacts.sh
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
Existing Phase verifier dispatch pattern:
```bash
phase16_verify)
  python3 tools/bazel/phase16_live_network_evidence.py --wiring-only
  python3 tools/bazel/phase16_live_network_evidence.py --quick
  ;;
phase16_verify_tests)
  python3 tools/bazel/phase16_live_network_evidence_test.py
  ;;
```

Existing just ordering pattern:
```make
phase16-verify:
    bazel run //tools/bazel:phase16_verify_tests
    bazel run //tools/bazel:phase16_verify
```

Existing artifact helper vocabularies:
```python
ARTIFACT_KINDS = ["bin", "map", "bbf", "dfu", "resource-image", "resource-package", "provenance", "auxiliary-manifest"]
EVIDENCE_CLASSES = ["local-smoke", "bootstrap-required", "ci-only", "reference-only", "manual-hardware-required", "release-candidate"]
SIGNING_MODES = ["unsigned-local", "test-key", "external-release-key", "not-applicable"]
```

Phase 17 release workflow identity contract:
```text
Bazel-owned release workflow identity: //tools/bazel:phase17_release_candidate_artifacts
Approved release-run command identity: bazel build //tools/bazel:phase17_release_candidate_artifacts
Local smoke labels only: //tools/bazel:representative_release_artifacts, //tools/bazel:representative_package_surface_smoke, //tools/bazel:representative_reference_format_artifacts, //tools/bazel:phase3_verify
```

Phase 17 must use this mismatch class enum exactly:
```text
pass
intentional-delta
blocker
deferred-retained-code-issue
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
|----------|------|------|----------|-------|
| D-01 | 17-01 | 1 | Full | Phase-owned contract file, not mutation of earlier manifests. |
| D-02 | 17-01 | 1 | Full | Required artifact family row IDs enumerate each release surface and the Bazel-owned workflow identity for that surface. |
| D-03 | 17-01 | 1,2 | Full | Status vocabulary and workflow identity fields separate local source contract/smoke labels from release-run pass evidence. |
| D-04 | 17-01 | 1,2 | Full | Contract cites Phase 3 helpers and verifier validates helper/source refs. |
| D-05 | 17-01 | 1,2 | Full | Signing schema and security scan require metadata only. |
| D-06 | 17-01 | 1,2 | Full | External release-key identity accepted by name/fingerprint only. |
| D-07 | 17-01 | 1,2 | Full | Provenance rows validate build input identity and artifact hashes without byte-identity claims. |
| D-08 | 17-01 | 2 | Full | Forbidden marker and overclaim guards reject secrets, payload bytes, and approval claims. |
| D-09 | 17-01 | 1,2 | Full | Comparison rows require exact mismatch class enum. |
| D-10 | 17-01 | 1,2 | Full | Comparison output fields name surface, profile, refs, normalized fields, owner, risk. |
| D-11 | 17-01 | 1,2 | Full | Contract cites Phase 11 reference comparisons and cutover readiness rows. |
| D-12 | 17-01 | 1,2 | Full | Verifier fails missing comparison surfaces or unclassified mismatches. |
| D-13 | 17-01 | 2 | Full | Generated root is build/ci-evidence/phase17. |
| D-14 | 17-01 | 2 | Full | Quick artifacts include manifest, normalized results, signing/provenance summary, classification report, snapshot, template, logs. |
| D-15 | 17-01 | 1,2 | Full | CI retention is referenced, but CI without inputs remains non-release proof. |
| D-16 | 17-01 | 2 | Full | Artifact refs restricted to build/ci-evidence/phase17 or external://phase17/... references. |
| D-17 | 17-01 | 1,2 | Full | Adds stdlib Python verifier and test file. |
| D-18 | 17-01 | 3 | Full | Adds Bazel release artifact workflow label, verifier labels, root aliases/docs, rust_workflow dispatch, and just recipe. |
| D-19 | 17-01 | 1,2,3 | Full | Local verification is deterministic and private-key-free. |
| D-20 | 17-01 | 2 | Full | Uses JSON, explicit enums, stdlib helpers, shell=False subprocess calls only. |
| D-21 | 17-01 | 1,2 | Full | Every row maps to REL IDs plus archived reference and prior phase evidence refs. |
| D-22 | 17-01 | 1,2 | Full | Phase 15 and Phase 16 rows remain supporting boundaries, not release proof. |
| D-23 | 17-01 | 1,2,3 | Full | Lifecycle ID is fixed to 17-2026-06-19T13-57-17. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 17 release evidence contract and contract verifier</name>
  <requirements>REL-01, REL-02, REL-03</requirements>
  <files>tools/bazel/manifests/phase17_release_candidate_evidence_contract.json, tools/bazel/phase17_release_candidate_evidence.py, tools/bazel/phase17_release_candidate_evidence_test.py</files>
  <read_first>
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-CONTEXT.md
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-RESEARCH.md
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-VALIDATION.md
    - tools/bazel/phase16_live_network_evidence.py
    - tools/bazel/phase16_live_network_evidence_test.py
    - tools/bazel/manifests/phase16_live_network_evidence_contract.json
    - tools/bazel/manifests/phase11_reference_comparisons.json
    - tools/bazel/manifests/phase11_cutover_readiness.json
    - tools/bazel/manifests/representative_products.json
    - tools/bazel/artifact_manifest.py
    - tools/bazel/artifact_metadata_compare.py
    - tools/bazel/artifact_packager.py
    - ProjectOptions.cmake
  </read_first>
  <behavior>
    - Test 1: `--contract-only` passes for the complete Phase 17 contract.
    - Test 2: removing any required release row fails and names the missing row ID.
    - Test 3: removing any of REL-01, REL-02, or REL-03 coverage fails and names the missing requirement.
    - Test 4: using any mismatch class outside `pass`, `intentional-delta`, `blocker`, or `deferred-retained-code-issue` fails.
    - Test 5: `passed` as a default status for a release-run or external-signing row fails.
    - Test 6: removing `bazel_label`, `release_command`, `artifact_outputs`, or `release_run_required` from any release artifact family row fails and names the missing workflow field.
    - Test 7: a `release_run_required: true` row using `//tools/bazel:representative_release_artifacts` or another local smoke label as its production `bazel_label` fails.
    - Test 8: removing any supported product (`COREONE`, `MINI`, `MK4`, `MK3.5`, `XL`, `iX`, `XL_DEV_KIT`) or auxiliary board surface (`DWARF`, `MODULARBED`, `XBUDDY_EXTENSION`) from the contract fails.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase17_release_candidate_evidence_contract.json` with top-level fields `schema_version: "1"`, `id: "phase17_release_candidate_evidence_contract"`, `phase: "17-release-candidate-artifact-and-signing-gates"`, `phase_lifecycle_id: "17-2026-06-19T13-57-17"`, `output_root: "build/ci-evidence/phase17"`, `artifact_name: "phase17-release-candidate-evidence"`, `status_vocabulary`, `mismatch_class_vocabulary`, `required_artifact_kinds`, `release_workflow_identities`, `local_smoke_workflow_identities`, `supported_release_products`, `supported_release_boards`, `release_input_schema`, and `rows`.

    Use this exact status vocabulary: `pending-release-input`, `release-run-required`, `external-signing-required`, `blocked-signing-key-unavailable`, `source-contract-passed`, `passed`, `failed`, `rejected-redaction`, `rejected-overclaim`. Use this exact mismatch class vocabulary: `pass`, `intentional-delta`, `blocker`, `deferred-retained-code-issue`.

    Define `supported_release_products` exactly as `COREONE`, `MINI`, `MK4`, `MK3.5`, `XL`, `iX`, and `XL_DEV_KIT` from `ProjectOptions.cmake`. Define `supported_release_boards` exactly as `BUDDY`, `XBUDDY`, `XLBUDDY`, `XL_DEV_KIT_XLB`, `DWARF`, `MODULARBED`, and `XBUDDY_EXTENSION`. Include surface metadata showing resources for `MINI`, `MK4`, `MK3.5`, `XL`, `iX`, and `COREONE`; translations for `COREONE`, `MK4`, `MK3.5`, `XL`, and `MINI`; MMU for `MK4`, `MK3.5`, and `COREONE`; puppy/auxiliary firmware for `XL`, `iX`, `XL_DEV_KIT`, and `COREONE`; and xBuddy Extension for `COREONE`.

    Define `release_workflow_identities` with an entry `phase17_release_candidate_artifacts` containing `bazel_label: "//tools/bazel:phase17_release_candidate_artifacts"`, `release_command: "bazel build //tools/bazel:phase17_release_candidate_artifacts"`, `artifact_outputs` covering `.bin`, `.bbf`, `.dfu`, `.map`, `.provenance.json`, resource image/package, language bundle, WUI assets, ESP package, MMU package, Dwarf firmware, ModularBed firmware, xBuddy Extension firmware, package manifest, signing summary, provenance summary, retention manifest, and comparison report, and `release_run_required: true`. Define `local_smoke_workflow_identities` for `//tools/bazel:representative_release_artifacts`, `//tools/bazel:representative_package_surface_smoke`, `//tools/bazel:representative_reference_format_artifacts`, and `//tools/bazel:phase3_verify` with `local_smoke_only: true` and `release_run_required: false`.

    Add these required row IDs and artifact surfaces:
    `rel-bin-firmware-image` (`.bin`), `rel-bbf-firmware-package` (`.bbf`), `rel-dfu-update-package` (`.dfu`), `rel-map-and-provenance` (`map/provenance`), `rel-resource-image-package` (`resource`), `rel-language-bundles` (`language`), `rel-wui-assets` (`wui`), `rel-esp-packages` (`esp`), `rel-mmu-package` (`mmu`), `rel-auxiliary-dwarf-firmware` (`dwarf`), `rel-auxiliary-modularbed-firmware` (`modularbed`), `rel-auxiliary-xbuddy-extension-firmware` (`xbuddy-extension`), `rel-package-manifests` (`package-manifest`), `rel-signing-key-identity` (`signing-key-identity`), `rel-build-input-identity` (`build-input-identity`), `rel-artifact-retention` (`artifact-retention`), `rel-reference-comparison-report` (`reference-comparison`), and `rel-contract-traceability-redaction-boundary` (`contract-traceability-redaction`).

    Every row must include `id`, `title`, `requirement_ids`, `artifact_surface`, `product_profile`, `source_contract_refs`, `source_doc_refs`, `proof_scope`, `required_input_kind`, `bazel_label`, `release_command`, `artifact_outputs`, `release_run_required`, `expected_artifact_path`, `retained_artifact_kind`, `allowed_statuses`, `release_metadata_required`, `signing_metadata_required`, `provenance_metadata_required`, `comparison_metadata_required`, `mismatch_class`, `mismatch_reason_required`, `owner_phase`, `residual_cutover_gates`, `unsupported_claims`, and `redaction_required`. Rows covering release artifacts map to REL-01, signing/provenance/retention rows map to REL-02, and comparison rows map to REL-03; overlap rows may list multiple REL IDs. Release artifact rows with `release_run_required: true` must use `//tools/bazel:phase17_release_candidate_artifacts` as `bazel_label` and `bazel build //tools/bazel:phase17_release_candidate_artifacts` as `release_command`; smoke-only rows may cite representative labels only when they cannot satisfy production release proof.

    `expected_artifact_path` values must be repo-relative under `build/ci-evidence/phase17`, for example `build/ci-evidence/phase17/logs/rel-bin-firmware-image.log`. `source_contract_refs` must cite concrete prior manifests such as `tools/bazel/manifests/representative_products.json#mini_boot`, `tools/bazel/manifests/phase7_generated_outputs.json#generated-resources`, `tools/bazel/manifests/phase10_auxiliary_build_update.json#auxiliary-build-update-release-artifacts`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-product-artifacts`, `tools/bazel/manifests/phase11_reference_comparisons.json#ref-release-metadata`, `tools/bazel/manifests/phase11_cutover_readiness.json#criteria-non-local-gates-identified`, `tools/bazel/manifests/phase13_ci_evidence_contract.json#ciev-03-redacted-summary`, `tools/bazel/manifests/phase15_hardware_evidence_contract.json#hard-contract-traceability-and-redaction-boundary`, and `tools/bazel/manifests/phase16_live_network_evidence_contract.json#live-contract-traceability-redaction-boundary`. If an exact row ID differs, use the real existing row ID from the referenced manifest and make the verifier resolve it.

    Create `tools/bazel/phase17_release_candidate_evidence.py` as a standard-library verifier with constants `PHASE`, `PHASE_LIFECYCLE_ID`, `CONTRACT_MANIFEST`, `DEFAULT_OUTPUT_DIR`, `REQUIRED_REQUIREMENT_IDS`, `REQUIRED_ROW_IDS`, `REQUIRED_ARTIFACT_SURFACES`, `REQUIRED_RELEASE_PRODUCTS`, `REQUIRED_RELEASE_BOARDS`, `RELEASE_WORKFLOW_IDENTITIES`, `LOCAL_SMOKE_WORKFLOW_IDENTITIES`, `STATUS_VOCABULARY`, `MISMATCH_CLASS_VOCABULARY`, `REQUIRED_ARTIFACT_KINDS`, `SOURCE_REF_MANIFESTS`, and `REQUIRED_ROW_FIELDS`. Implement `--contract-only` first with JSON loading, row field validation, requirement coverage, row coverage, artifact surface coverage, supported product/board coverage, status vocabulary validation, release workflow identity validation, mismatch class validation, source ref resolution using `file#row-id`, doc ref validation using `file#anchor`, and repo-relative path validation. Keep the verifier import-free outside Python stdlib.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence.py --contract-only</automated>
    <automated>python3 -m json.tool tools/bazel/manifests/phase17_release_candidate_evidence_contract.json >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"phase_lifecycle_id": "17-2026-06-19T13-57-17"' tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`
    - `grep -q '"rel-bbf-firmware-package"' tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`
    - `grep -q '"rel-signing-key-identity"' tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`
    - `grep -q '"bazel_label": "//tools/bazel:phase17_release_candidate_artifacts"' tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`
    - `grep -q '"release_command": "bazel build //tools/bazel:phase17_release_candidate_artifacts"' tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`
    - `grep -q '"deferred-retained-code-issue"' tools/bazel/manifests/phase17_release_candidate_evidence_contract.json`
    - `python3 tools/bazel/phase17_release_candidate_evidence.py --contract-only` exits 0 for the checked-in contract.
  </acceptance_criteria>
  <done>The Phase 17 contract exists, all required artifact/signing/provenance/comparison rows are source-backed, lifecycle-tagged, tied to Bazel-owned workflow identities, and contract-only verification proves REL-01, REL-02, and REL-03 coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement release evidence ingestion, quick artifacts, and security guards</name>
  <requirements>REL-01, REL-02, REL-03</requirements>
  <files>tools/bazel/phase17_release_candidate_evidence.py, tools/bazel/phase17_release_candidate_evidence_test.py</files>
  <read_first>
    - tools/bazel/phase17_release_candidate_evidence.py
    - tools/bazel/phase17_release_candidate_evidence_test.py
    - tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
    - tools/bazel/phase16_live_network_evidence.py
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/phase13_ci_evidence.py
    - tools/bazel/artifact_packager.py
    - tools/bazel/artifact_manifest.py
    - tools/bazel/artifact_metadata_compare.py
  </read_first>
  <behavior>
    - Test 1: `--quick` writes all required generated artifacts under `build/ci-evidence/phase17`.
    - Test 2: without release evidence, release-run and external-signing rows remain `pending-release-input`, `release-run-required`, `external-signing-required`, or `blocked-signing-key-unavailable`, and source-contract rows become `source-contract-passed`.
    - Test 3: complete redacted release evidence can mark an eligible row `passed` only when `evidence_type` is `approved-release-run`, `approved-release-signing-evidence`, or `approved-release-comparison`.
    - Test 4: path traversal, absolute paths, generated outputs outside `build/ci-evidence/phase17`, and non-`external://phase17/...` external references fail before writing artifacts.
    - Test 5: forbidden private-key, credential, raw payload, and overclaim markers fail the security scan and do not leak marker values into error output.
    - Test 6: any subprocess helper call in the verifier uses `subprocess.run([...], shell=False)` or is absent.
    - Test 7: supplied release evidence fails when `bazel_label`, `release_command`, or `artifact_outputs` do not match the contract workflow identity for the row.
    - Test 8: supplied release evidence using representative smoke labels fails for rows where `release_run_required` is true, even if the result says `passed`.
  </behavior>
  <action>
    Extend `tools/bazel/phase17_release_candidate_evidence.py` with `--security-only`, `--quick`, `--release-evidence`, and `--output-dir`. `--quick` must call contract validation, optionally load release evidence JSON, write deterministic artifacts, then scan the checked-in contract and generated artifacts.

    The release evidence JSON may be either a top-level list or an object with `evidence_rows`. Each row must require `release_run_id`, `artifact_surface`, `product_profile`, `build_input_identity`, `operator`, `timestamp`, `result`, `evidence_type`, `bazel_label`, `release_command`, `artifact_outputs`, `release_run_required`, `signing_mode`, `key_identity_ref`, `artifact_digest_sha256`, `artifact_refs`, `provenance_refs`, `comparison_refs`, `retention_path`, `verification_outcome`, `mismatch_class`, `mismatch_reason`, `owner_phase`, `residual_risk`, and `redaction_summary`. `timestamp` must be ISO-8601 UTC. `artifact_digest_sha256` must be a 64-character lowercase hex digest or an empty string only for rows whose proof scope is `source-contract`. `key_identity_ref` must be a name or fingerprint string only; reject fields named `private_key`, `signing_key_value`, `certificate_private_material`, `raw_key_bytes`, `certificate_pem`, `certificate_bytes`, `firmware_payload`, `bbf_payload`, `dfu_payload`, `token`, `password`, or `secret`.

    Cross-check each supplied evidence row against the matching contract row before status calculation: `bazel_label`, `release_command`, `artifact_outputs`, and `release_run_required` must match exactly. Rows with `release_run_required: true` can be marked `passed` only when the evidence type is `approved-release-run`, `approved-release-signing-evidence`, or `approved-release-comparison`, the workflow identity is `//tools/bazel:phase17_release_candidate_artifacts`, the release command is `bazel build //tools/bazel:phase17_release_candidate_artifacts`, and all required digest, retention, provenance, signing, and comparison fields are present. Evidence from `//tools/bazel:representative_release_artifacts`, `//tools/bazel:representative_package_surface_smoke`, `//tools/bazel:representative_reference_format_artifacts`, or `//tools/bazel:phase3_verify` must remain local smoke/source-contract evidence and cannot satisfy production release proof.

    Accept `artifact_refs`, `provenance_refs`, and `comparison_refs` only when each entry is repo-relative under `build/ci-evidence/phase17` or starts with `external://phase17/`. Do not accept `artifact://` for Phase 17 unless it also starts with `external://phase17/`; this keeps release evidence references explicit.

    Generate these files under `build/ci-evidence/phase17`: `run-manifest.json`, `normalized-release-results.json`, `redacted-signing-provenance-summary.json`, `comparison-classification-report.json`, `source-contract-snapshots/phase17_release_candidate_evidence_contract.json`, `release-operator-evidence-input.json`, and `logs/{row_id}.log`. The run manifest must include `phase`, `phase_lifecycle_id`, `artifact_name`, `command_mode: "quick"`, `output_root`, `release_inputs_supplied`, `requirement_coverage`, `status_counts`, `source_contract_snapshot_path`, and row summaries. The normalized results must include one result per contract row with status, artifact surface, proof scope, product profile, `bazel_label`, `release_command`, `artifact_outputs`, `release_run_required`, artifact refs, signing mode, key identity ref, digest, retention path, verification outcome, mismatch class, mismatch reason, owner phase, and residual risk. The comparison report must include every comparison row with `artifact_surface`, `product_profile`, `reference_source`, `rust_bazel_surface`, `normalized_fields_compared`, `artifact_refs`, `mismatch_class`, `mismatch_reason`, `owner_phase`, and `residual_risk`.

    Security scan must reject checked-in contract text, release evidence input, and generated artifacts containing private key blocks, certificate private material, `signing_key_value`, `private_key`, `raw_key_bytes`, `certificate_pem`, `certificate_bytes`, `firmware_payload`, `raw_firmware_payload`, `bbf_payload`, `dfu_payload`, `.bin payload`, `.bbf payload`, `.dfu payload`, token/password/secret assignment forms, `release readiness proven`, `release-candidate passed locally`, `production signing proof complete`, `signing proof complete`, `retained-code accepted by maintainer`, `reference demotion approved`, and `final cutover complete`.

    If the verifier invokes any existing helper, use `subprocess.run([...], cwd=ROOT, stdout=..., stderr=..., text=True, check=False, shell=False)` and sanitize captured text before retaining it. If no helper invocation is needed, add a test that no `shell=True`, `bash -c`, `python -c`, or `node -e` string exists in `tools/bazel/phase17_release_candidate_evidence.py`.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence.py --security-only</automated>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `test -f build/ci-evidence/phase17/run-manifest.json`
    - `test -f build/ci-evidence/phase17/normalized-release-results.json`
    - `test -f build/ci-evidence/phase17/redacted-signing-provenance-summary.json`
    - `test -f build/ci-evidence/phase17/comparison-classification-report.json`
    - `test -f build/ci-evidence/phase17/release-operator-evidence-input.json`
    - `grep -q '"bazel_label": "//tools/bazel:phase17_release_candidate_artifacts"' build/ci-evidence/phase17/normalized-release-results.json`
    - `grep -q '"release_run_required": true' build/ci-evidence/phase17/normalized-release-results.json`
    - `python3 tools/bazel/phase17_release_candidate_evidence.py --security-only` exits 0 after `--quick`.
    - `grep -R "release-candidate passed locally\\|signing proof complete\\|reference demotion approved" build/ci-evidence/phase17` finds no matches.
  </acceptance_criteria>
  <done>The verifier writes deterministic redacted Phase 17 evidence, accepts only safe release metadata references from the contracted Bazel workflow identities, rejects secret/payload/overclaim/path/workflow-spoofing risks, and preserves production release/signing rows as pending until supplied evidence is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Phase 17 into Bazel, root aliases, rust workflow, and just</name>
  <requirements>REL-01, REL-02, REL-03</requirements>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase17_release_candidate_evidence_test.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase16_live_network_evidence.py
    - tools/bazel/phase16_live_network_evidence_test.py
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-CONTEXT.md
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-RESEARCH.md
    - .planning/phases/17-release-candidate-artifact-and-signing-gates/17-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `--wiring-only` fails in a temp root when any Phase 17 Bazel label, root alias, docs filegroup, rust_workflow dispatch case, or just recipe line is missing.
    - Test 2: `just phase17-verify` runs `bazel run //tools/bazel:phase17_verify_tests` before `bazel run //tools/bazel:phase17_verify`.
    - Test 3: Bazel can run both Phase 17 labels.
    - Test 4: `bazel query //tools/bazel:phase17_release_candidate_artifacts` and `bazel build //tools/bazel:phase17_release_candidate_artifacts` succeed as the Bazel-owned artifact workflow identity, while verifier output still classifies local builds as smoke until release evidence is supplied.
  </behavior>
  <action>
    Add `filegroup(name = "phase17_release_candidate_artifacts", srcs = [":representative_release_artifacts"])` to `tools/bazel/BUILD.bazel`. This target is the Bazel-owned release-candidate artifact workflow identity that release-run evidence must cite; because local execution is limited to representative artifacts, the verifier must keep local builds of this target in smoke/source-contract status unless approved release evidence is supplied.

    Add `filegroup(name = "phase17_representative_release_smoke", srcs = [":representative_release_artifacts"])` to `tools/bazel/BUILD.bazel` so local smoke coverage remains visibly separate from production release proof. Use the existing `phase3_release_artifacts` / `representative_release_artifacts` patterns; do not add a new BBF/DFU encoder or private signing path.

    Add `phase17_source_ref_manifests` to `tools/bazel/BUILD.bazel` with `srcs` including `manifests/representative_products.json`, `manifests/phase7_generated_outputs.json`, `manifests/phase7_storage_media.json`, `manifests/phase10_auxiliary_build_update.json`, `manifests/phase10_auxiliary_controllers.json`, `manifests/phase11_cutover_readiness.json`, `manifests/phase11_parity_pyramid.json`, `manifests/phase11_reference_comparisons.json`, `manifests/phase11_requirement_evidence.json`, `manifests/phase11_retained_code_justifications.json`, `manifests/phase13_ci_evidence_contract.json`, `manifests/phase15_hardware_evidence_contract.json`, and `manifests/phase16_live_network_evidence_contract.json`.

    Add `shell_binary(name = "phase17_verify", src = "rust_workflow.sh", data = ["phase17_release_candidate_evidence.py", "manifests/phase17_release_candidate_evidence_contract.json", ":phase17_release_candidate_artifacts", ":phase17_representative_release_smoke", ":phase17_source_ref_manifests", "//:phase17_release_candidate_evidence_docs", "//:phase11_cutover_evidence_docs"])`.

    Add `shell_binary(name = "phase17_verify_tests", src = "rust_workflow.sh", data = ["phase17_release_candidate_evidence.py", "phase17_release_candidate_evidence_test.py", "manifests/phase17_release_candidate_evidence_contract.json", ":phase17_release_candidate_artifacts", ":phase17_representative_release_smoke", ":phase17_source_ref_manifests"])`.

    In root `BUILD.bazel`, add `filegroup(name = "phase17_release_candidate_evidence_docs", srcs = [".planning/phases/17-release-candidate-artifact-and-signing-gates/17-CONTEXT.md", ".planning/phases/17-release-candidate-artifact-and-signing-gates/17-RESEARCH.md", ".planning/phases/17-release-candidate-artifact-and-signing-gates/17-VALIDATION.md", ".planning/phases/17-release-candidate-artifact-and-signing-gates/17-01-PLAN.md"])`, plus aliases `phase17_release_candidate_artifacts`, `phase17_verify`, and `phase17_verify_tests` to the matching `//tools/bazel` targets.

    In `tools/bazel/rust_workflow.sh`, add dispatch cases:
    `phase17_verify)` runs `python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only` then `python3 tools/bazel/phase17_release_candidate_evidence.py --quick`.
    `phase17_verify_tests)` runs `python3 tools/bazel/phase17_release_candidate_evidence_test.py`.

    In `justfile`, add:
    `phase17-verify:`
    `    bazel run //tools/bazel:phase17_verify_tests`
    `    bazel run //tools/bazel:phase17_verify`
    `phase17-release-artifacts-smoke:`
    `    bazel build //tools/bazel:phase17_release_candidate_artifacts`

    Extend `tools/bazel/phase17_release_candidate_evidence.py --wiring-only` and tests so they require the exact strings above, require `phase17_release_candidate_artifacts` in both root and `tools/bazel` Bazel files, require the just smoke recipe, and fail if just ordering places the verifier before tests.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only</automated>
    <automated>bazel query "//tools/bazel:phase17_release_candidate_artifacts + //:phase17_release_candidate_artifacts"</automated>
    <automated>bazel build //tools/bazel:phase17_release_candidate_artifacts</automated>
    <automated>bazel run //tools/bazel:phase17_verify_tests &amp;&amp; bazel run //tools/bazel:phase17_verify</automated>
    <automated>just phase17-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query "//tools/bazel:phase17_release_candidate_artifacts + //:phase17_release_candidate_artifacts + //tools/bazel:phase17_verify + //tools/bazel:phase17_verify_tests + //:phase17_verify + //:phase17_verify_tests"` exits 0.
    - `bazel build //tools/bazel:phase17_release_candidate_artifacts` exits 0.
    - `grep -q 'name = "phase17_release_candidate_artifacts"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase17_source_ref_manifests"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase17_release_candidate_evidence_docs"' BUILD.bazel`
    - `grep -q 'name = "phase17_release_candidate_artifacts"' BUILD.bazel`
    - `grep -q 'phase17_verify)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase17-verify:' justfile`
    - `grep -q 'phase17-release-artifacts-smoke:' justfile`
    - `just phase17-verify` exits 0.
  </acceptance_criteria>
  <done>Phase 17 is exposed through Bazel and the developer facade, with an explicit release artifact workflow identity, smoke-only representative artifact build, tests-before-verifier ordering enforced by the verifier, and `just phase17-verify` proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release evidence JSON -> verifier | Untrusted operator/release metadata enters `tools/bazel/phase17_release_candidate_evidence.py` through `--release-evidence`. |
| contract JSON -> verifier | Checked-in contract data drives row status, artifact paths, source refs, and mismatch classes. |
| verifier -> filesystem | Generated evidence is written under `build/ci-evidence/phase17`. |
| verifier -> optional helper subprocess | Any helper invocation crosses from Python into external commands. |
| generated evidence -> CI retention | Redacted outputs may be retained by CI artifact workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | signing evidence input and generated summaries | mitigate | Block high severity leakage by rejecting private key blocks, `private_key`, `signing_key_value`, `raw_key_bytes`, certificate private material, tokens, passwords, and secrets before accepting release evidence or writing retained artifacts. |
| T-17-02 | Information Disclosure | release artifact references and generated logs | mitigate | Reject firmware payload markers including `firmware_payload`, `raw_firmware_payload`, `bbf_payload`, `dfu_payload`, `.bin payload`, `.bbf payload`, and `.dfu payload`; retain digests, names, and external refs only. |
| T-17-03 | Tampering / Information Disclosure | artifact refs, output dir, source refs | mitigate | Require repo-relative paths under `build/ci-evidence/phase17` or `external://phase17/...`; reject absolute paths, `..`, symlink escapes by resolved output root check, and bad `file#row-id` source refs. |
| T-17-04 | Repudiation | release result and mismatch classification | mitigate | Require complete operator metadata, ISO-8601 UTC timestamp, owner phase, mismatch reason, residual risk, and exactly one mismatch class from `pass`, `intentional-delta`, `blocker`, or `deferred-retained-code-issue`. |
| T-17-05 | Spoofing / Repudiation | local dry-run release status | mitigate | Default release-run and signing rows cannot be `passed`; pass requires supplied release evidence with approved evidence type and safe artifact refs. Local quick output must report `release_inputs_supplied: false` when absent. |
| T-17-06 | Tampering / Elevation of Privilege | helper subprocess execution | mitigate | Use `subprocess.run([...], shell=False)` only; tests reject `shell=True`, `bash -c`, `python -c`, and `node -e` in the verifier. |
| T-17-07 | Repudiation / Process Integrity | release overclaiming | mitigate | Security scan rejects `release readiness proven`, `release-candidate passed locally`, `production signing proof complete`, `signing proof complete`, retained-code approval, reference-demotion approval, and final cutover wording. |
| T-17-08 | Denial of Service | malformed JSON and duplicate rows | mitigate | Parse boundary JSON once, require top-level objects/lists, reject duplicate row IDs and duplicate release evidence rows, and return bounded validation errors without writing artifacts after failed input validation. |
| T-17-09 | Spoofing / Tampering | release workflow identity evidence | mitigate | Require release evidence `bazel_label`, `release_command`, `artifact_outputs`, and `release_run_required` to match the contract row; reject representative smoke labels for production release proof. |
</threat_model>

<verification>
Run these commands after the three tasks:

1. `python3 tools/bazel/phase17_release_candidate_evidence_test.py`
2. `python3 tools/bazel/phase17_release_candidate_evidence.py --contract-only`
3. `python3 tools/bazel/phase17_release_candidate_evidence.py --security-only`
4. `python3 tools/bazel/phase17_release_candidate_evidence.py --quick`
5. `python3 tools/bazel/phase17_release_candidate_evidence.py --wiring-only`
6. `bazel query "//tools/bazel:phase17_release_candidate_artifacts + //:phase17_release_candidate_artifacts"`
7. `bazel build //tools/bazel:phase17_release_candidate_artifacts`
8. `bazel run //tools/bazel:phase17_verify_tests && bazel run //tools/bazel:phase17_verify`
9. `just phase17-verify`
10. `git diff --check`

Expected generated files after `--quick`:
- `build/ci-evidence/phase17/run-manifest.json`
- `build/ci-evidence/phase17/normalized-release-results.json`
- `build/ci-evidence/phase17/redacted-signing-provenance-summary.json`
- `build/ci-evidence/phase17/comparison-classification-report.json`
- `build/ci-evidence/phase17/source-contract-snapshots/phase17_release_candidate_evidence_contract.json`
- `build/ci-evidence/phase17/release-operator-evidence-input.json`
- `build/ci-evidence/phase17/logs/rel-bin-firmware-image.log`
</verification>

<success_criteria>
- `tools/bazel/manifests/phase17_release_candidate_evidence_contract.json` exists and validates with REL-01, REL-02, and REL-03 coverage.
- Contract rows for release artifact families include `bazel_label`, `release_command`, `artifact_outputs`, and `release_run_required`, and the verifier rejects evidence that does not match the Bazel-owned workflow identity.
- `//tools/bazel:phase17_release_candidate_artifacts` and `//:phase17_release_candidate_artifacts` exist as the release artifact workflow identity; local representative builds remain smoke-only unless approved release evidence is supplied.
- `tools/bazel/phase17_release_candidate_evidence.py` supports `--contract-only`, `--security-only`, `--quick`, `--release-evidence`, `--output-dir`, and `--wiring-only`.
- `tools/bazel/phase17_release_candidate_evidence_test.py` proves required rows, statuses, mismatch classes, path guards, redaction guards, generated outputs, release evidence validation, and wiring.
- `bazel run //tools/bazel:phase17_verify_tests && bazel run //tools/bazel:phase17_verify` passes.
- `just phase17-verify` passes and runs tests before verifier.
- No committed source or generated Phase 17 artifact contains private signing material, raw firmware payload bytes, credentials, release overclaims, retained-code acceptance claims, or reference-demotion approval claims.
</success_criteria>

<output>
After completion, create `.planning/phases/17-release-candidate-artifact-and-signing-gates/17-01-SUMMARY.md`.
</output>
