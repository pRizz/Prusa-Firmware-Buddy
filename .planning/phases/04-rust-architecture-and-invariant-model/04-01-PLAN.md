---
phase: 04-rust-architecture-and-invariant-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - BUILD.bazel
  - justfile
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - tools/bazel/phase4_verify.py
  - rust/crates/domain/**
  - rust/crates/application/**
  - rust/crates/board-adapter/**
  - rust/crates/runtime-adapter/**
autonomous: true
requirements:
  - RUST-01
  - RUST-02
  - RUST-05
  - VERF-02
requirements_addressed:
  - RUST-01
  - RUST-02
  - RUST-05
  - VERF-02
must_haves:
  truths:
    - "Developer can inspect Rust crates that separate pure domain logic from application, board adapter, and runtime adapter code."
    - "Domain constructors reject unsupported printer/board/MCU/bootloader/feature combinations and invalid storage, artifact, and protocol values."
    - "Rust format, clippy, build, unit-test, doc, and aggregate verification run through Bazel/just."
    - "Pure domain and application policy tests use focused Arrange/Act/Assert sections."
  artifacts:
    - path: "rust/crates/domain/src/product.rs"
      provides: "Typed product, board, MCU, bootloader, and feature invariants"
      contains: "ProductProfile::new"
    - path: "rust/crates/domain/src/storage.rs"
      provides: "Typed storage key, schema version, and migration window invariants"
      contains: "MigrationWindow"
    - path: "rust/crates/domain/src/artifact.rs"
      provides: "Typed artifact kind, file-name, and suffix validation"
      contains: "ArtifactSuffixMismatch"
    - path: "rust/crates/domain/src/protocol.rs"
      provides: "Typed registration and connection state machine"
      contains: "Disconnected"
    - path: "tools/bazel/phase4_verify.py"
      provides: "Phase 4 verifier for Rust architecture and toolchain checks"
      contains: "Phase 4 Rust architecture verification passed"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-06-03T12-43-57
generated_at: 2026-06-03T12:43:57Z
---

<objective>
Create the Phase 4 Rust workspace and invariant model, wire it through Bazel and `just`, and verify the pure Rust architecture with Bright Builds Rust checks.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Rust workspace and pure domain crate</name>
  <files>Cargo.toml, Cargo.lock, rust/crates/domain/**</files>
  <action>Add a dependency-free Cargo workspace and `buddy-domain` crate. Implement enums, newtypes, and constructors for product, board, MCU, bootloader, feature, storage, artifact, and protocol invariants. Keep the crate `unsafe`-free and test pure logic with Arrange/Act/Assert.</action>
  <verify>cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-features</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add architecture boundary crates</name>
  <files>rust/crates/application/**, rust/crates/board-adapter/**, rust/crates/runtime-adapter/**</files>
  <action>Add thin `buddy-application`, `buddy-board-adapter`, and `buddy-runtime-adapter` crates. Keep application policy pure and adapter crates typed but effect-free so Phase 5 can add explicit runtime/foreign-code boundaries without primitive leakage.</action>
  <verify>cargo build --workspace --all-features && cargo test --all-features</verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire Rust checks through Bazel and just</name>
  <files>BUILD.bazel, tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, tools/bazel/phase4_verify.py, justfile</files>
  <action>Add Bazel labels for `phase4_verify`, `rust_format_check`, `rust_lint`, `rust_unit_tests`, `rust_docs`, `rust_build`, and real `rust_firmware`. Add matching `just` recipes. Implement a verifier that checks the static architecture surface and runs Rust fmt, clippy, build, test, and docs.</action>
  <verify>python3 tools/bazel/phase4_verify.py --all && bazel query "//tools/bazel:phase4_verify + //tools/bazel:rust_format_check + //tools/bazel:rust_lint + //tools/bazel:rust_unit_tests + //tools/bazel:rust_docs + //tools/bazel:rust_build + //tools/bazel:rust_firmware" && just phase4-verify</verify>
</task>

</tasks>

<acceptance_criteria>

- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
- `cargo build --workspace --all-features` exits 0.
- `cargo test --all-features` exits 0.
- `cargo doc --workspace --all-features --no-deps` exits 0.
- `python3 tools/bazel/phase4_verify.py --all` exits 0.
- `just phase4-verify` exits 0.

</acceptance_criteria>

<done>
Phase 4 is complete when all acceptance criteria pass, lifecycle validation sees a plan and passed verification, and RUST-01/RUST-02/RUST-05/VERF-02 are marked complete.
</done>
