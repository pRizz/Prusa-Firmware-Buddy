---
phase: 29-upstream-evidence-flow-closure
plan: "01"
plan_id: "29-01"
name: "Phase 26 Upstream Row Ingestion"
status: planned
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/phase26_release_signing_upstream_evidence.py
  - tools/bazel/phase26_release_signing_upstream_evidence_test.py
  - tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
  - tools/bazel/phase28_final_readiness_packet.py
  - tools/bazel/phase28_final_readiness_packet_test.py
  - .planning/phases/29-upstream-evidence-flow-closure/29-01-SUMMARY.md
autonomous: true
requirements:
  - ACPT-01
  - READ-01
  - READ-02
requirements_addressed:
  - ACPT-01
  - READ-01
  - READ-02
lifecycle_id: 29-2026-06-25T20-26-39
phase_lifecycle_id: 29-2026-06-25T20-26-39
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: 2026-06-25T20:58:00Z
verification_commands:
  - python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py
  - python3 tools/bazel/phase28_final_readiness_packet_test.py
  - just phase26-verify
  - just phase28-verify
  - git diff --check
  - cargo fmt --all
  - cargo clippy --all-targets --all-features -- -D warnings
  - cargo build --all-targets --all-features
  - cargo test --all-features
must_haves:
  truths:
    - "Phase 26 quick mode accepts supplied Phase 23, Phase 24, and Phase 25 upstream row files through explicit quick-only flags."
    - "Absent Phase 23, Phase 24, or Phase 25 row flags keep the existing fail-closed default rows for the matching criteria."
    - "Consumed Phase 26 rows carry EVID-01, EVID-02, and EVID-03 with ACPT-01, producer manifest refs, input row paths, and producer artifact refs."
    - "Invalid upstream row identity, requirement IDs, phase, lifecycle, status, source-ref, redaction, or artifact refs cannot become passing Phase 26 evidence."
    - "Phase 28 final readiness packet rows expose consumed Phase 26 refs and keep reference_demotion_authorization blocked without explicit Phase 28 demotion input."
  artifacts:
    - path: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      provides: "Quick-only Phase 23/24/25 upstream row input ingestion and canonical Phase 26 row normalization"
      contains: "--phase23-simulator-row"
    - path: "tools/bazel/phase26_release_signing_upstream_evidence_test.py"
      provides: "Regression coverage for consumed rows, absent defaults, Phase 25 mapping, and invalid row guards"
      contains: "test_consumed_upstream_rows_replace_default_pending_rows"
    - path: "tools/bazel/phase28_final_readiness_packet_test.py"
      provides: "Packet propagation and demotion separation regression coverage"
      contains: "test_packet_carries_consumed_phase23_24_25_refs_from_phase26_rows"
  key_links:
    - from: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      to: "tools/bazel/phase23_simulator_evidence_execution.py"
      via: "consumed upstream-simulator-result-row.json input"
      pattern: "--phase23-simulator-row"
    - from: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      to: "tools/bazel/phase24_hardware_media_safety_evidence_execution.py"
      via: "consumed upstream-hardware-media-safety-result-row.json input"
      pattern: "--phase24-hardware-media-safety-row"
    - from: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      to: "tools/bazel/phase25_live_service_evidence_execution.py"
      via: "explicit final-live-service-evidence to final-live-network-transfer-evidence mapping"
      pattern: "final-live-service-evidence.*final-live-network-transfer-evidence"
---

<objective>
Implement the code path that lets Phase 26 consume Phase 23, Phase 24, and Phase 25 upstream row artifacts, then prove Phase 28 receives the propagated evidence through Phase 26 rows.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/29-upstream-evidence-flow-closure/29-CONTEXT.md
@.planning/phases/29-upstream-evidence-flow-closure/29-RESEARCH.md
@.planning/phases/29-upstream-evidence-flow-closure/29-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@tools/bazel/manifests/phase18_cutover_review_contract.json
@tools/bazel/manifests/phase23_simulator_evidence_execution_contract.json
@tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json
@tools/bazel/manifests/phase25_live_service_evidence_execution_contract.json
@tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
@tools/bazel/manifests/phase28_final_readiness_packet_contract.json
@tools/bazel/phase23_simulator_evidence_execution.py
@tools/bazel/phase24_hardware_media_safety_evidence_execution.py
@tools/bazel/phase25_live_service_evidence_execution.py
@tools/bazel/phase26_release_signing_upstream_evidence.py
@tools/bazel/phase26_release_signing_upstream_evidence_test.py
@tools/bazel/phase28_final_readiness_packet.py
@tools/bazel/phase28_final_readiness_packet_test.py
</context>

<commit_verification>
Before any commit in this Rust repository, run these commands in this exact order:
1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo build --all-targets --all-features`
4. `cargo test --all-features`

Do not commit an intentionally failing RED state. Focused Python tests, phase verify commands, `git diff --check`, and the Cargo sequence must all pass before committing this plan.
</commit_verification>

<threat_model>
| Threat ID | Category | Component | Mitigation |
|-----------|----------|-----------|------------|
| T-29-01 | Spoofing | Phase 26 upstream row inputs | Validate `criterion_id`, `phase`, exact producer `requirement_ids`, non-empty `phase_lifecycle_id`, and Phase 25 compact-to-canonical mapping before canonical row construction. |
| T-29-02 | Tampering | Evidence refs and artifact refs | Require repo-relative row paths and allow only `build/ci-evidence/phase23/`, `build/ci-evidence/phase24/`, `build/ci-evidence/phase25/`, `external://phase23/`, `external://phase24/`, and `external://phase25/` refs. |
| T-29-03 | Information Disclosure | Raw upstream evidence | Reuse existing forbidden field/text guards and keep raw evidence in ignored `build/ci-evidence/...` or external refs, never tracked planning docs. |
| T-29-04 | Elevation of Privilege | Reference demotion | Add Phase 28 regression coverage proving consumed upstream pass rows do not authorize demotion without explicit Phase 28 demotion input. |
</threat_model>

<tasks>

<task id="29-01-01" type="auto">
  <name>Add Phase 26 consumed upstream row inputs</name>
  <files>
    tools/bazel/phase26_release_signing_upstream_evidence.py
    tools/bazel/phase26_release_signing_upstream_evidence_test.py
    tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
  </files>
  <read_first>
    @tools/bazel/phase26_release_signing_upstream_evidence.py
    @tools/bazel/phase26_release_signing_upstream_evidence_test.py
    @tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
    @tools/bazel/manifests/phase18_cutover_review_contract.json
    @tools/bazel/phase23_simulator_evidence_execution.py
    @tools/bazel/phase24_hardware_media_safety_evidence_execution.py
    @tools/bazel/phase25_live_service_evidence_execution.py
  </read_first>
  <action>
    Add quick-only optional CLI flags exactly named `--phase23-simulator-row`, `--phase24-hardware-media-safety-row`, and `--phase25-live-service-row`; reject them unless `--quick` is selected, matching `--release-input`.

    Add a descriptor table with these exact entries:
    - Phase 23: source phase `23-simulator-evidence-execution`, source criterion `final-simulator-evidence`, canonical criterion `final-simulator-evidence`, producer requirements `["EVID-01"]`, input root `build/ci-evidence/phase23/`, external root `external://phase23/`.
    - Phase 24: source phase `24-hardware-media-and-safety-evidence-execution`, source criterion `final-hardware-safety-media-evidence`, canonical criterion `final-hardware-safety-media-evidence`, producer requirements `["EVID-02"]`, input root `build/ci-evidence/phase24/`, external root `external://phase24/`.
    - Phase 25: source phase `25-live-service-evidence-execution`, source criterion `final-live-service-evidence`, canonical criterion `final-live-network-transfer-evidence`, producer requirements `["EVID-03"]`, input root `build/ci-evidence/phase25/`, external root `external://phase25/`.

    Implement pure helpers to load supplied row files, validate compact producer rows, and canonicalize compact rows into `UPSTREAM_RESULT_ROW_FIELDS`. The validator must require exact source criterion, exact producer requirements, exact source phase, non-empty `phase_lifecycle_id`, status in the Phase 18 upstream status vocabulary, non-empty `artifact_refs`, safe `manifest_ref` when present, and an input row path under the descriptor input root. `redaction_status != "passed"` or `source_ref_status != "passed"` must become blocked through `normalize_upstream_row(...)`, not pass.

    Canonical consumed rows must set `criterion_id` to the descriptor canonical criterion; `requirement_ids` to producer EVID plus `ACPT-01`; `source_requirement_ids`, `owning_phase`, `source_lifecycle_id`, and `evidence_family` from the matching Phase 18 requirement; `source_lifecycle_status` to `current`; `evidence_refs` to the source `manifest_ref` when present plus the input row path; `artifact_refs` to producer artifact refs plus the input row path; `generated_at_utc` to the Phase 26 `generated_at` value.

    Change `build_upstream_rows(...)` to accept a mapping of canonical criterion ID to consumed row. Criteria without consumed rows must keep the existing `default_upstream_status(...)`, `phase26_requirement_ids(...)`, `evidence_refs_for_criterion(...)`, and `artifact_refs_for_criterion(...)` behavior. Always call `normalize_upstream_row(row, requirement)` before appending.

    Update `phase26_release_signing_upstream_evidence_contract.json` with `upstream_policy.upstream_row_inputs` declaring the three flags, source phases, source criteria, canonical criteria, producer requirement IDs, input roots, and external roots. Update `check_contract(...)` so drift in this policy fails `--contract-only`.

    Add tests named `test_consumed_upstream_rows_replace_default_pending_rows`, `test_absent_upstream_rows_keep_fail_closed_defaults`, `test_phase25_compact_live_service_row_maps_to_phase18_live_network_criterion`, and `test_invalid_upstream_row_guards_block_or_reject`.
  </action>
  <acceptance_criteria>
    - `rg -n -- "--phase23-simulator-row|--phase24-hardware-media-safety-row|--phase25-live-service-row" tools/bazel/phase26_release_signing_upstream_evidence.py` finds all three CLI flags.
    - `rg -n "test_consumed_upstream_rows_replace_default_pending_rows|test_absent_upstream_rows_keep_fail_closed_defaults|test_phase25_compact_live_service_row_maps_to_phase18_live_network_criterion|test_invalid_upstream_row_guards_block_or_reject" tools/bazel/phase26_release_signing_upstream_evidence_test.py` finds all four tests.
    - `rg -n "upstream_row_inputs|build/ci-evidence/phase23/|external://phase25/" tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json` finds the declared policy.
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26</automated>
  </verify>
  <done>
    `python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py` passes; Phase 26 exposes the three quick-only upstream row flags; the contract declares `upstream_row_inputs`; valid supplied rows replace the simulator, hardware/media/safety, and live-service defaults; absent rows keep fail-closed defaults; invalid upstream row guards reject or block malformed evidence.
  </done>
</task>

<task id="29-01-02" type="auto">
  <name>Prove Phase 28 propagation and demotion separation</name>
  <files>
    tools/bazel/phase28_final_readiness_packet.py
    tools/bazel/phase28_final_readiness_packet_test.py
  </files>
  <read_first>
    @tools/bazel/phase28_final_readiness_packet.py
    @tools/bazel/phase28_final_readiness_packet_test.py
    @tools/bazel/phase26_release_signing_upstream_evidence.py
    @tools/bazel/manifests/phase18_cutover_review_contract.json
    @tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
    @tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
  </read_first>
  <action>
    Add fixture support in `phase28_final_readiness_packet_test.py` for consumed Phase 26 rows containing:
    - `final-simulator-evidence` with `requirement_ids: ["EVID-01", "ACPT-01"]`, `build/ci-evidence/phase23/simulator-result-manifest.json`, `build/ci-evidence/phase23/upstream-simulator-result-row.json`, and `external://phase23/simulator/startup-log.json`.
    - `final-hardware-safety-media-evidence` with `requirement_ids: ["EVID-02", "ACPT-01"]`, `build/ci-evidence/phase24/hardware-media-safety-result-manifest.json`, `build/ci-evidence/phase24/upstream-hardware-media-safety-result-row.json`, and `external://phase24/hardware/safety-report.json`.
    - `final-live-network-transfer-evidence` with `requirement_ids: ["EVID-03", "ACPT-01"]`, `build/ci-evidence/phase25/live-service-result-manifest.json`, `build/ci-evidence/phase25/upstream-live-service-result-row.json`, and `external://phase25/live-service/connect-report.json`.

    Add `test_packet_carries_consumed_phase23_24_25_refs_from_phase26_rows` and assert the Phase 28 packet criteria rows carry each EVID ID, producer manifest refs, producer artifact refs, and input row paths.

    Add `test_consumed_upstream_rows_do_not_authorize_reference_demotion_without_decision` and assert `final_readiness_status == "unblocked"`, `reference_demotion_authorization == "blocked"`, `real_maintainer_demotion_approval_supplied is False`, and the demotion row has `demotion_gate_effect == "requires-explicit-phase28-decision"` when no demotion input is supplied.

    Run the tests before changing `phase28_final_readiness_packet.py`. If they already pass, leave the implementation untouched. If Phase 28 drops Phase 26 evidence refs when Phase 27 rows also contain refs, change only `normalize_readiness_criteria(...)` so `evidence_refs` is a sorted union of string refs from `phase26_row["evidence_refs"]` and `phase27_row["evidence_refs"]`.
  </action>
  <acceptance_criteria>
    - `rg -n "test_packet_carries_consumed_phase23_24_25_refs_from_phase26_rows|test_consumed_upstream_rows_do_not_authorize_reference_demotion_without_decision" tools/bazel/phase28_final_readiness_packet_test.py` finds both tests.
    - `python3 tools/bazel/phase28_final_readiness_packet_test.py` passes.
    - The packet propagation test asserts exact refs containing `build/ci-evidence/phase23/upstream-simulator-result-row.json`, `build/ci-evidence/phase24/upstream-hardware-media-safety-result-row.json`, and `build/ci-evidence/phase25/upstream-live-service-result-row.json`.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase28_final_readiness_packet_test.py</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py</automated>
    <automated>just phase26-verify</automated>
    <automated>just phase28-verify</automated>
    <automated>git diff --check</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>
    `python3 tools/bazel/phase28_final_readiness_packet_test.py` passes; Phase 28 packet tests assert consumed Phase 23, Phase 24, and Phase 25 requirement IDs plus source/artifact refs appear through Phase 26 rows; demotion authorization remains blocked without explicit Phase 28 demotion input; `just phase26-verify`, `just phase28-verify`, `git diff --check`, and the required Cargo sequence pass before the plan commit.
  </done>
</task>

</tasks>

<verification>
Run:
1. `python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py`
2. `python3 tools/bazel/phase28_final_readiness_packet_test.py`
3. `just phase26-verify`
4. `just phase28-verify`
5. `git diff --check`
6. `cargo fmt --all`
7. `cargo clippy --all-targets --all-features -- -D warnings`
8. `cargo build --all-targets --all-features`
9. `cargo test --all-features`
</verification>

<success_criteria>
- Phase 26 accepts valid supplied Phase 23, Phase 24, and Phase 25 upstream row files only through `--quick` flags.
- Missing Phase 23, Phase 24, or Phase 25 row flags preserve fail-closed default Phase 26 rows.
- Invalid producer row identity, requirement IDs, phase, lifecycle, status, source-ref, redaction, or artifact refs cannot produce passing Phase 26 evidence.
- Phase 25 `final-live-service-evidence` maps only to Phase 18 `final-live-network-transfer-evidence`.
- Phase 28 packet criteria expose EVID-01, EVID-02, EVID-03, producer evidence refs, and producer artifact refs from consumed Phase 26 rows.
- `reference_demotion_authorization` remains `blocked` without a valid explicit Phase 28 demotion input.
</success_criteria>

<output>
Create `.planning/phases/29-upstream-evidence-flow-closure/29-01-SUMMARY.md`.
</output>
