---
phase: 02-bazel-authority-and-developer-facade
plan: 01
type: execute
wave: 1
depends_on:
  - 01-reference-baseline-and-safety-envelope
files_modified:
  - MODULE.bazel
  - MODULE.bazel.lock
  - .bazelrc
  - .gitignore
  - BUILD.bazel
  - platforms/BUILD.bazel
  - tools/bazel/BUILD.bazel
  - tools/bazel/shell_rules.bzl
  - tools/bazel/reference_contract.sh
  - tools/bazel/phase2_verify.sh
  - tools/bazel/phase2_verify.py
  - tools/bazel/toolchains/BUILD.bazel
  - tools/bazel/toolchains/reference_toolchain.bzl
  - justfile
autonomous: true
requirements: [BAZL-01, BAZL-02, BAZL-04]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-06-02T20-31-42
generated_at: 2026-06-02T20:31:42.861Z
must_haves:
  truths:
    - BAZL-01 is satisfied for Phase 2 by a root Bzlmod entrypoint, Bazel config, registered firmware toolchain labels, and explicit host/product platforms.
    - BAZL-02 is satisfied for Phase 2 by queryable Bazel workflow labels for Rust firmware, retained foreign code, generated assets, host tools, unit tests, simulator inputs, and release packages.
    - BAZL-04 is satisfied by a root checked `justfile` facade that calls Bazel labels for the required developer workflows.
    - Phase 2 verification is lightweight and cannot accidentally run heavy reference builds unless `BUDDY_BAZEL_EXECUTE_REFERENCE=1` is set.
  artifacts:
    - MODULE.bazel
    - MODULE.bazel.lock
    - .bazelrc
    - .gitignore
    - BUILD.bazel
    - platforms/BUILD.bazel
    - tools/bazel/BUILD.bazel
    - tools/bazel/shell_rules.bzl
    - tools/bazel/reference_contract.sh
    - tools/bazel/phase2_verify.py
    - tools/bazel/toolchains/BUILD.bazel
    - tools/bazel/toolchains/reference_toolchain.bzl
    - justfile
  key_links:
    - `MODULE.bazel` registers `//tools/bazel/toolchains:*_firmware_toolchain` labels.
    - `.bazelrc` maps host and product configs to `//platforms:*` labels.
    - `tools/bazel/phase2_verify.py` checks the Phase 2 Bazel, `just`, and Bazel-output ignore contract.
    - `justfile` routes developer workflows through `bazel run //tools/bazel:*`.
---

<objective>
Create the Phase 2 Bazel authority and developer facade.

Purpose: Developers need a root Bazel workspace and stable `just` recipes before later phases move generators, artifacts, Rust crates, retained code, and parity checks behind those labels.
Output: Queryable Bazel root graph, product platforms, registered toolchain labels, workflow targets, root `justfile`, and a phase verifier.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-CONTEXT.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-RESEARCH.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-VALIDATION.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Bazel root, product platforms, and toolchain labels</name>
  <files>MODULE.bazel, MODULE.bazel.lock, .bazelrc, BUILD.bazel, platforms/BUILD.bazel, tools/bazel/toolchains/BUILD.bazel, tools/bazel/toolchains/reference_toolchain.bzl</files>
  <action>Add a root Bzlmod module, checked lockfile, Bazel configs for host/product platforms, root filegroups/aliases, constraint settings for runtime/printer/board/MCU, representative supported product platforms, and registered Rust/C/C++/ASM/asset toolchain labels.</action>
  <verify>bazel query "//tools/bazel/... + //platforms/..."</verify>
  <done>Bazel can query host and embedded product platforms plus registered toolchain labels.</done>
</task>

<task type="auto">
  <name>Task 2: Add workflow targets and developer facade</name>
  <files>tools/bazel/BUILD.bazel, tools/bazel/shell_rules.bzl, tools/bazel/reference_contract.sh, tools/bazel/phase2_verify.sh, justfile</files>
  <action>Add repo-local shell executable support, Bazel workflow labels for the required command classes, a guarded reference dispatcher, and root `justfile` recipes that call Bazel labels.</action>
  <verify>just --list</verify>
  <done>Developers can discover and invoke the required workflows through `just` and Bazel labels.</done>
</task>

<task type="auto">
  <name>Task 3: Add and run Phase 2 verification</name>
  <files>tools/bazel/phase2_verify.py</files>
  <action>Add a standard-library verifier that checks required files, required strings, Bazel queryability, and `just --list` when tools are available.</action>
  <verify>python3 tools/bazel/phase2_verify.py && git diff --check</verify>
  <done>Phase 2 verification passes locally without running heavy firmware reference commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 tools/bazel/phase2_verify.py`
- [ ] `bazel query "//tools/bazel/... + //platforms/..."`
- [ ] `just --list`
- [ ] `git diff --check`
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 2 --require-plans --require-verification --raw`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No heavy reference build or hardware-bound command runs by default
- BAZL-01, BAZL-02, and BAZL-04 are each linked to concrete files and labels
- Later phases have explicit Bazel labels to extend instead of inventing new entrypoints

</success_criteria>

<output>
After completion, create `.planning/phases/02-bazel-authority-and-developer-facade/02-01-SUMMARY.md`
</output>
