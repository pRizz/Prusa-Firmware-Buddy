---
phase: 38-fail-closed-cutover-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
  - tools/bazel/phase34_final_readiness_demotion_dry_run.py
  - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
  - tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
  - tools/bazel/phase35_cutover_decision_artifact.py
  - tools/bazel/phase35_cutover_decision_artifact_test.py
autonomous: true
requirements:
  - READY-02
  - READY-03
  - CUTOVER-01
  - CUTOVER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 38-2026-07-26T16-29-23
generated_at: 2026-07-26T16:55:00.000Z
must_haves:
  truths:
    - "Every invalid Phase 31 or Phase 33 source causes Phase 34 to publish and validate a durable blocked replacement before returning nonzero."
    - "Phase 35 staged-install failure never deletes the only recoverable prior bundle or revives stale approved authority."
    - "A durable Phase 35 authority guard is blocking before canonical mutation and remains blocking through failed install, validation, restore, or cleanup."
    - "Readiness, cutover verdict, production-cutover route, and demotion authorization remain separate authority states."
  artifacts:
    - path: tools/bazel/phase34_final_readiness_demotion_dry_run.py
      provides: "Complete Phase 31/33 source-failure publication boundary"
    - path: tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
      provides: "Seeded stale-approval replacement regressions for every source boundary"
    - path: tools/bazel/phase35_cutover_decision_artifact.py
      provides: "Guarded staged installation and compensating restore"
    - path: tools/bazel/phase35_cutover_decision_artifact_test.py
      provides: "Focused guard, rename, validation, restore, and cleanup fault tests"
  key_links:
    - from: tools/bazel/phase34_final_readiness_demotion_dry_run.py
      to: build/ci-evidence/phase34
      via: "staged blocked source-failure bundle installed before nonzero return"
      pattern: "blocked"
    - from: tools/bazel/phase35_cutover_decision_artifact.py
      to: build/ci-evidence/phase35
      via: "blocking guard checked before canonical bundle authority is accepted"
      pattern: "guard"
---

<objective>
Make Phase 34 source failure publication complete and make Phase 35 staged installation recoverable without reviving stale authority.

Purpose: Close both local publication gaps that allow invalid inputs or rename failures to preserve or destroy authoritative cutover state.
Output: Contract-defined blocked Phase 34 fallbacks, a durable Phase 35 authority guard, compensating restore, and focused fault-injection regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md
@.planning/phases/38-fail-closed-cutover-workflow/38-RESEARCH.md
@.planning/phases/38-fail-closed-cutover-workflow/38-VALIDATION.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-CONTEXT.md
@.planning/phases/35-cutover-decision-artifact/35-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish a blocked Phase 34 replacement for every invalid Phase 31 or Phase 33 source</name>
  <read_first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md</file>
    <file>.planning/v1.3-MILESTONE-AUDIT.md</file>
    <file>tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run.py</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</file>
    <file>tools/bazel/phase34_decision_reconciliation_integration_test.py</file>
  </read_first>
  <files>tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json, tools/bazel/phase34_final_readiness_demotion_dry_run.py, tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</files>
  <behavior>
    - "Per D-02, missing, malformed, stale, lifecycle-mismatched, unsafe, secret-tainted, digest-mismatched, or unreadable Phase 31/33 inputs replace a seeded prior unblocked/open Phase 34 bundle with a blocked bundle."
    - "Per D-04, the blocked replacement is installed and passes generated-output/security validation before the verifier returns the original nonzero operational status and safe reason category."
    - "The command retains a safe stable reason code and never copies raw source payloads into fallback artifacts."
    - "Per D-05, fallback readiness_state and demotion gate_state are blocked; no prior readiness approval, cutover approval, production-cutover route, or open demotion-gate projection survives, and the four authority states remain separate."
  </behavior>
  <action>
Per D-01, harden the existing Phase 34 readiness bundle and Phase 35 cutover bundle authority surfaces without adding a generation pointer or parallel authority schema. Add focused tests first by seeding a valid unblocked/open Phase 34 bundle and mutating one Phase 31 or Phase 33 loading boundary per test: Phase 31 manifest/receipt loading, Phase 33 handoff loading, normalized decisions, readiness input, register refs/digests, Phase 32 blocker register, and demotion handoff. Per D-02, extend the contract with the exact safe source-failure reason vocabulary and required blocked fallback fields, and refactor `run_quick` so all untrusted Phase 31/33 loads and validations execute inside one failure-publication boundary, including failures before demotion-handoff loading. On `VerificationError`, map only to contract-listed safe reason categories, build the complete minimal blocked readiness/demotion bundle in a staging directory, install it at `build/ci-evidence/phase34`, and run generated-output and security validation. Per D-04, preserve the original operational status and safe category as the nonzero result after publication. Per D-05, clear every readiness, cutover, route, and demotion authority projection rather than conflating those states. Do not catch programming errors, return success for fallback publication, infer demotion approval, or retain any field that grants readiness, cutover, route, or demotion authority. After the narrow Phase 34 test passes and before committing, run the mandatory gate in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `git diff --check`.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q</automated>
  </verify>
  <acceptance_criteria>
    - "The Phase 34 test suite contains a seeded-prior-authority regression for each Phase 31/33 source family named in B3."
    - "Every tested source failure observes a complete canonical Phase 34 bundle with `readiness_state: blocked` and `gate_state: blocked` before the command returns nonzero."
    - "Fallback output validation and security scanning execute for source-failure bundles."
    - "No raw evidence, token, key, certificate, crash dump, service payload, or caller-supplied authority flag is written."
    - "Before committing, the executor first runs the narrow automated command, then runs `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `git diff --check` in that order."
  </acceptance_criteria>
  <done>Phase 34 durably revokes stale readiness and demotion authority for every invalid Phase 31/33 source before reporting failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard Phase 35 authority and restore canonical data across staged-install faults</name>
  <read_first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md</file>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-RESEARCH.md</file>
    <file>.planning/v1.3-MILESTONE-AUDIT.md</file>
    <file>tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json</file>
    <file>tools/bazel/phase35_cutover_decision_artifact.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact_test.py</file>
  </read_first>
  <files>tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json, tools/bazel/phase35_cutover_decision_artifact.py, tools/bazel/phase35_cutover_decision_artifact_test.py</files>
  <behavior>
    - "Per D-06, a contract-defined guard adjacent to the canonical Phase 35 output becomes blocking before any canonical directory rename and remains authoritative through mutation and recovery faults."
    - "Guard presence, malformed shape, stale lifecycle, unsafe path, or unreadable state makes Phase 35 authority blocked."
    - "Per D-07, if staged rename or post-install validation fails, the prior bundle is restored for availability only while the guard remains blocking."
    - "Per D-08, the guard clears only after canonical generated-output, security, verdict, route, and demotion projections validate as one safe bundle; validation or guard-cleanup failure remains blocked and returns nonzero."
    - "Per D-09, every recovery ends with either a validated blocked replacement or restored canonical data under the blocking guard, without deleting the only recoverable bundle or reviving prior approval."
  </behavior>
  <action>
Per D-10, write separate one-concern fault-injection tests before implementation for guard creation/write failure, moving the prior canonical bundle, staged-to-canonical rename after backup, canonical post-install validation, compensating restore, backup cleanup, and guard cleanup. To fully mitigate T-38-03, add focused regressions for each mutation target (guard, stage, backup, and canonical) covering absolute-path substitution, parent-traversal substitution, symlink escape, wrong-root resolution, and non-directory substitution. Extend the Phase 35 contract with one guard schema containing a lifecycle ID, blocking authority state, safe reason code, attempted output root, and no caller-controlled approval fields. Per D-06, refactor `install_staged_bundle` into explicit guarded steps with small injectable filesystem call seams and publish and validate the guard before mutation. Strictly validate type, canonical root containment, traversal absence, and symlink resolution immediately before every filesystem mutation: guard publication, prior-to-backup rename, stage-to-canonical rename, compensating restore, staging cleanup, backup cleanup, and guard cleanup. Per D-07 and D-09, restore backup when available only under the retained blocking guard, discard only validated non-authoritative staging data, and raise `VerificationError`. Per D-08, clear the guard and remove backup only after the installed canonical bundle passes generated-output, security, verdict, route, and demotion validation. Make every Phase 35 canonical-bundle reader introduced or touched in this phase enforce the guard. Never unconditionally restore stale authority, delete backup in the failure handler, or allow a test-only CLI flag to bypass guard semantics. After the narrow Phase 35 test passes and before committing, run the mandatory gate in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `git diff --check`.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase35_cutover_decision_artifact_test.py -q</automated>
  </verify>
  <acceptance_criteria>
    - "The contract defines exactly one blocking authority-guard artifact with lifecycle and safe reason fields and no approval/verdict override."
    - "Fault tests cover guard write, old-bundle rename, stage rename, post-install validation, restore, backup cleanup, and guard cleanup as separate concerns."
    - "T-38-03 regressions cover absolute paths, parent traversal, symlink escape, wrong roots, and non-directory substitutions for guard, stage, backup, and canonical targets, and assert strict validation occurs immediately before every mutation."
    - "After every injected failure, either a validated blocked canonical bundle exists or restored canonical data exists while a validated blocking guard remains."
    - "No injected failure leaves prior `approved`, production-cutover planning, or open demotion authority observable as current."
    - "Before committing, the executor first runs the narrow automated command, then runs `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `git diff --check` in that order."
  </acceptance_criteria>
  <done>Phase 35 publication is recoverable and authority-monotonic across every staged-install failure boundary.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 31/33 inputs to Phase 34 | Untrusted or stale retained artifacts may attempt to preserve prior readiness or demotion authority. |
| Phase 34 fallback to canonical output | Partial or unsafe fallback publication may claim revocation without replacing authority. |
| Phase 35 stage/backup/guard to canonical output | Rename, restore, validation, or cleanup faults may expose stale approval or delete recoverable state. |
| Canonical readers to authority guard | A reader that ignores the guard can bypass fail-closed recovery. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-38-01 | Spoofing / Elevation of Privilege | high | Seeded prior Phase 34/35 approval | mitigate | Replace every invalid-source path with validated blocked artifacts and assert prior authority disappears before nonzero return. |
| T-38-02 | Elevation of Privilege | high | Phase 35 authority guard | mitigate | Make guard enforcement mandatory at every touched reader; malformed, unreadable, stale, or present guard blocks authority. |
| T-38-03 | Tampering | high | Guard, stage, backup, and canonical paths | mitigate | Reuse strict root/path/symlink validation before mutation and test unsafe path substitutions. |
| T-38-04 | Tampering / Denial of Service | high | Staged publication and rollback | mitigate | Guard before mutation, compensating restore on failure, post-install validation before guard clear, and focused fault tests at every step. |
| T-38-05 | Information Disclosure / Repudiation | high | Blocked fallback diagnostics | mitigate | Emit only contract-listed safe categories, lifecycle IDs, counts, booleans, and sanitized refs; retain nonzero status and audit reason. |

No high-severity threat may remain unresolved. The affected focused suite and mandatory Rust sequence must pass before each task commit.
</threat-model>

<verification>
Run the Phase 34 suite after Task 1 and the Phase 35 suite after Task 2. Before every task commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then `git diff --check`. Review the final diff for guard bypasses, swallowed errors, unsafe path mutation, stale approval restoration, and diagnostic leakage.
</verification>

<success-criteria>
- Every B3 Phase 31/33 invalid-source family durably blocks Phase 34 before failure is reported.
- Phase 35 staged installation restores recoverable data without making stale approval authoritative.
- Guard and fallback artifacts are contract-defined, safe, lifecycle-bound, and validated.
- All high-severity threats are enforced by focused automated regressions.
</success-criteria>

<output>
After completion, create `.planning/phases/38-fail-closed-cutover-workflow/38-01-SUMMARY.md`.
</output>
