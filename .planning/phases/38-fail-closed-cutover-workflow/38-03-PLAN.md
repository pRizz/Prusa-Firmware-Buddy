---
phase: 38-fail-closed-cutover-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 38-01
  - 38-02
files_modified:
  - tools/bazel/phase34_final_readiness_demotion_dry_run.py
  - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
  - tools/bazel/phase35_cutover_decision_artifact.py
  - tools/bazel/phase35_cutover_decision_artifact_test.py
  - tools/bazel/phase38_cutover_workflow.py
  - tools/bazel/phase38_cutover_workflow_test.py
  - tools/bazel/phase38_cutover_workflow_integration_test.py
autonomous: true
gap_closure: true
requirements:
  - READY-02
  - READY-03
  - CUTOVER-01
  - CUTOVER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 38-2026-07-26T16-29-23
generated_at: 2026-07-27T01:18:01Z
must_haves:
  truths:
    - "A Phase 35 guard-creation failure before the guard file exists leaves a durable blocking workflow marker, and every canonical Phase 35 reader rejects seeded prior approval."
    - "An invalid Phase 31 or Phase 33 source followed by Phase 34 blocked-replacement installation failure cannot restore authoritative unblocked readiness or republish an approved production-cutover route."
    - "After a nonzero Phase 34 result, Phase 35 may clear its publication guard only when persisted Phase 34 authority is blocked and correlated to that exact failed workflow attempt."
    - "Default blocked, complete approved, named targeted-repair, READY-03 demotion separation, and CUTOVER-01/CUTOVER-03 routing remain unchanged."
  artifacts:
    - path: tools/bazel/phase34_final_readiness_demotion_dry_run.py
      provides: "Attempt-correlated blocking authority across Phase 34 staging, install, validation, rollback, and cleanup failures"
    - path: tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
      provides: "Focused seeded-approval regression for blocked-replacement installation failure"
    - path: tools/bazel/phase35_cutover_decision_artifact.py
      provides: "Canonical-reader enforcement of durable workflow revocation and guarded Phase 34 consumption"
    - path: tools/bazel/phase35_cutover_decision_artifact_test.py
      provides: "True pre-create guard-failure and canonical-reader regressions"
    - path: tools/bazel/phase38_cutover_workflow.py
      provides: "Attempt-correlated coordinator revocation and exact nonzero Phase 34 authority gate"
    - path: tools/bazel/phase38_cutover_workflow_test.py
      provides: "Coordinator sequencing, persisted-authority, and guard-clear unit regressions"
    - path: tools/bazel/phase38_cutover_workflow_integration_test.py
      provides: "Actual Phase 31-through-35 producer fault regressions for both verifier reproductions"
  key_links:
    - from: tools/bazel/phase38_cutover_workflow.py
      to: tools/bazel/phase35_cutover_decision_artifact.py
      via: "A durable attempt-correlated workflow revocation exists before Phase 35 guard creation and is enforced by every canonical reader"
      pattern: "attempt.*guard|guard.*attempt"
    - from: tools/bazel/phase34_final_readiness_demotion_dry_run.py
      to: tools/bazel/phase38_cutover_workflow.py
      via: "The Phase 34 blocked bundle or blocking publication state carries the same attempt identity evaluated after nonzero return"
      pattern: "attempt"
    - from: tools/bazel/phase38_cutover_workflow.py
      to: tools/bazel/phase35_cutover_decision_artifact.py
      via: "Phase 35 finalization consumes a failed Phase 34 run only after its persisted effective authority validates blocked for that attempt"
      pattern: "blocked"
---

<objective>
Close the two remaining stale-authority publication gaps with one attempt-correlated fail-closed workflow boundary.

Purpose: Ensure no nonzero Phase 34/35 workflow outcome can leave seeded prior readiness or cutover approval readable, even when the failure occurs before Phase 35 guard creation or while installing the Phase 34 blocked replacement.
Output: RED fault reproductions, durable attempt-correlated authority revocation, exact failed-run validation, canonical-reader enforcement, and a green authoritative Phase 38 gate.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md
@.planning/phases/38-fail-closed-cutover-workflow/38-RESEARCH.md
@.planning/phases/38-fail-closed-cutover-workflow/38-VERIFICATION.md
@.planning/phases/38-fail-closed-cutover-workflow/38-01-SUMMARY.md
@.planning/phases/38-fail-closed-cutover-workflow/38-02-SUMMARY.md
@.planning/phases/38-fail-closed-cutover-workflow/38-REVIEW.md
@.planning/phases/38-fail-closed-cutover-workflow/38-REVIEW-FIX.md

<interfaces>
From tools/bazel/phase34_final_readiness_demotion_dry_run.py:
```python
def replace_output_with_staging(full_output: Path, staging_output: Path) -> None
def publish_source_failure_bundle(
    root: Path,
    relative_output: Path,
    full_output: Path,
    reason_code: str,
    approval_validation_state: str = "invalid",
) -> None
def run_quick(
    root: Path,
    phase31_output: str,
    phase33_handoff: str,
    output_arg: str,
) -> str | None
```

From tools/bazel/phase35_cutover_decision_artifact.py:
```python
def ensure_canonical_authority(root: Path, relative_output: Path) -> None
def publish_authority_guard(root: Path) -> None
def install_staged_bundle(
    root: Path,
    stage: Path,
    canonical_output: Path,
    validate_installed: Callable[[Path], None],
) -> None
def run_quick(root: Path, phase34_arg: str, output_arg: str) -> None
```

From tools/bazel/phase38_cutover_workflow.py:
```python
@dataclass(frozen=True)
class CommandOutcome:
    status: int
    reason_category: str

def load_final_authority(root: Path) -> FinalAuthority
def _phase34_authority_is_valid(root: Path) -> bool
def coordinate_workflow(root: Path = ROOT) -> WorkflowResult
```
</interfaces>
</context>

<decision-coverage>
| Decisions | Task | Coverage |
| --- | --- | --- |
| D-01, D-05, D-11, D-12, D-13, D-14, D-15 | 1 and 2 | Keep the existing Phase 34/35 public bundles and coordinator, prove both actual-producer faults, and preserve the four normal routing/demotion paths without a generation pointer or Cartesian test matrix. |
| D-02 | 1 and 2 | Implement the durable Phase 34 publication-state primitive, then use it to persist blocked authority for every source failure. |
| D-03, D-04 | 2 | Finalize Phase 35 from exact persisted authority before return while preserving the original nonzero status and safe category. |
| D-06, D-07, D-08, D-09, D-10 | 1 and 2 | Reproduce the uncovered pre-create/install faults, retain blocking authority through recovery, and clear protection only after exact installed-authority validation. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 GREEN boundary: Implement durable marker primitives and focused path defenses</name>
  <read-first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-VERIFICATION.md</file>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run.py</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact_test.py</file>
    <file>tools/bazel/phase38_cutover_workflow.py</file>
    <file>tools/bazel/phase38_cutover_workflow_test.py</file>
    <file>tools/bazel/phase38_cutover_workflow_integration_test.py</file>
  </read-first>
  <files>tools/bazel/phase34_final_readiness_demotion_dry_run.py, tools/bazel/phase34_final_readiness_demotion_dry_run_test.py, tools/bazel/phase35_cutover_decision_artifact.py, tools/bazel/phase35_cutover_decision_artifact_test.py, tools/bazel/phase38_cutover_workflow.py, tools/bazel/phase38_cutover_workflow_test.py</files>
  <behavior>
    - "Both the workflow-attempt marker and Phase 34 publication-state marker establish durable blocking authority before their structured payload is created or atomically installed; injected pre-create or atomic-publication failure leaves that authority blocking."
    - "For each marker surface, missing required fields, malformed JSON, unreadable state, absolute/traversal/wrong-root substitution, symlink substitution, wrong-type or non-directory targets, and cleanup/removal failure are rejected by canonical readers."
    - "Every marker fault retains durable blocking authority, reports only a safe bounded reason category, and prevents seeded prior Phase 34/35 approval from becoming readable."
    - "The two markers remain private revocation metadata adjacent to the existing Phase 34/35 bundles; they do not create a second public decision schema or a generation pointer."
  </behavior>
  <action>
Per D-01, D-06, D-08, and D-11, keep `build/ci-evidence/phase34` and `build/ci-evidence/phase35` as the only public authority bundles and do not add a generation pointer or alternate decision schema. Define one private fixed workflow-attempt marker surface and one private fixed Phase 34 publication-state marker surface adjacent to those bundles. Each surface must establish an unmistakable durable blocking shell before attempting to create or atomically replace its structured payload, so a payload pre-create failure or temp-to-canonical atomic replacement failure leaves the shell itself blocking. Marker absence after a completed cleanup is allowed only through the existing validated success transition; a present shell with a missing payload, missing field, malformed payload, or unreadable payload is blocking.

Start with RED tests and record their expected failures without committing. Add two focused test classes, one per marker surface, with one Arrange/Act/Assert test per concern. For both surfaces cover: structured-payload pre-create failure after the blocking shell exists; atomic replacement failure; each required field missing individually; malformed JSON; unreadable state; absolute path; `..` traversal; wrong-root substitution; symlinked marker or parent; regular-file/FIFO/other wrong-type marker shell; non-directory parent or canonical target; and cleanup/removal failure. Keep substitution tests as explicit cases rather than assuming the existing Phase 35 path matrix covers the new paths. Every case must seed prior valid unblocked/approved authority, invoke the production canonical reader, assert that authority remains rejected, assert at least one durable blocking shell/state remains, and assert diagnostics are bounded safe categories without attacker-controlled path text or raw payloads. The selected Task 1 command must be run once RED before implementation and fail only on the new marker contract, never on fixture setup; do not commit this RED state.

Implement the smallest shared policy or phase-local adapters needed to make those focused tests GREEN. Per D-02, D-07, D-09, and D-10, validate repository-root containment, exact expected fixed paths, parent identity, directory/file type, and symlink rejection before every marker read, write, rename, or removal. Treat `lstat`/read/parse/type/permission failures as blocked. Permit only phase, lifecycle, opaque attempt identity, blocked state, fixed canonical output ref, and a closed safe reason category in marker payloads and diagnostics; exclude evidence payloads, verdict/route overrides, demotion approval, tokens, keys, certificates, crash dumps, service data, raw exception strings, and unbounded attacker-controlled paths. Make `ensure_canonical_authority`, `run_security_scan`, `load_final_authority`, and the Phase 34 effective-authority evaluator reject seeded approval whenever either relevant marker is present, malformed, unreadable, unsafe, wrong-type, or cannot be safely cleaned up.

After the selected Task 1 test classes are GREEN, run `just phase38-verify`; run the mandatory Rust commands in this exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; and finally run `git diff --check`. Only then create the first atomic task commit. Do not commit if any selected test, authoritative gate, Rust command, or diff check fails.
  </action>
  <verify>
    <automated>python3 -m unittest tools.bazel.phase34_final_readiness_demotion_dry_run_test.Phase34PublicationStateSecurityTests tools.bazel.phase38_cutover_workflow_test.WorkflowAttemptMarkerSecurityTests</automated>
    <expected>GREEN in under 30 seconds: every one-concern marker publication, malformed/unreadable state, path substitution, wrong-type, and cleanup/removal regression passes for both new marker surfaces.</expected>
  </verify>
  <acceptance-criteria>
    - "Both marker surfaces have separate one-concern tests for pre-create and atomic publication failure, malformed and missing fields, unreadable state, absolute/traversal/wrong-root substitution, symlink substitution, wrong-type/non-directory targets, and cleanup/removal failure."
    - "Every new marker regression seeds prior positive authority and proves durable blocking authority remains, the production canonical reader rejects, and diagnostics are safe and bounded."
    - "The Phase 34 marker payload and workflow marker payload contain only fixed lifecycle/correlation/reason metadata and cannot act as a second public readiness, cutover, route, or demotion schema."
    - "The selected Task 1 test classes are first observed RED without a commit, then pass GREEN in under 30 seconds."
    - "Before the Task 1 GREEN commit, `just phase38-verify`, the four Rust commands in the mandated order, and `git diff --check` all pass."
  </acceptance-criteria>
  <done>Both new marker primitives and their authority/path boundaries are implemented and committed only in a fully green state, with explicit T-38-03/T-38-04 regressions for each surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN boundary: Close coordinator and actual-producer stale-authority reproductions</name>
  <read-first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-VERIFICATION.md</file>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-01-SUMMARY.md</file>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-02-SUMMARY.md</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run.py</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact_test.py</file>
    <file>tools/bazel/phase38_cutover_workflow.py</file>
    <file>tools/bazel/phase38_cutover_workflow_test.py</file>
    <file>tools/bazel/phase38_cutover_workflow_integration_test.py</file>
    <file>justfile</file>
  </read-first>
  <files>tools/bazel/phase34_final_readiness_demotion_dry_run.py, tools/bazel/phase34_final_readiness_demotion_dry_run_test.py, tools/bazel/phase35_cutover_decision_artifact.py, tools/bazel/phase35_cutover_decision_artifact_test.py, tools/bazel/phase38_cutover_workflow.py, tools/bazel/phase38_cutover_workflow_test.py, tools/bazel/phase38_cutover_workflow_integration_test.py</files>
  <behavior>
    - "The coordinator persists a blocking workflow-attempt marker before calling Phase 35 guard publication; a Phase 35 pre-create failure leaves that marker intact."
    - "Phase 34 source-failure publication persists an attempt-correlated blocked state before moving prior canonical output and retains it across staged rename, validation, rollback, and cleanup failures."
    - "A nonzero Phase 34 outcome is consumable by Phase 35 only when the persisted Phase 34 bundle or retained blocking state is blocked, lifecycle-valid, reason-consistent, and correlated to the coordinator's current attempt."
    - "Phase 35 clears workflow/publication protection only after a complete installed blocked or normal bundle validates and exact Phase 34 failed-run correlation has been checked."
    - "Operational status remains nonzero for either injected fault; approved, blocked, targeted-repair, and orthogonal demotion behavior remain unchanged when operations succeed."
  </behavior>
  <action>
Preserve TDD without committing failure. First add or complete the focused verifier reproductions and two actual-producer regressions described below, run the selected Task 2 command, and record RED only when the tests reach the intended injected boundary and fail on missing current-attempt/monotonic-authority behavior. Do not commit while any selected or relevant test is failing. Then implement the coordinator and producer wiring until the same selected command is GREEN.

Per D-01, keep `build/ci-evidence/phase34` and `build/ci-evidence/phase35` as the only public readiness/cutover authority bundles and do not add a generation pointer. Generate one opaque safe attempt identity inside `coordinate_workflow` for each invocation; it is correlation metadata, never caller-supplied authorization. Before calling `phase35.publish_authority_guard`, establish and validate the Task 1 workflow-attempt blocking marker. Replace the current Phase 35 “creation interruption” fixture with a true pre-create failure: the injected `touch_guard` seam must raise without calling `Path.touch`, the Phase 35 guard path must be absent, and the earlier workflow marker must remain durable. Seed a valid approved/production-cutover canonical bundle and call `ensure_canonical_authority`, `run_security_scan`, and `load_final_authority` separately; every reader must reject it. Candidate-bundle validators may inspect output while protection is active, but no function may report candidate data as current authority or bypass the public reader.

Per D-02, D-06, D-07, D-08, and D-09, thread the internally generated attempt identity into the callable Phase 34/35 publication boundary without adding a CLI authority override. Before Phase 34 replaces source-failure output, persist and validate a blocked publication state containing the attempt identity and safe source-failure reason. Retain that state through prior-to-backup rename, blocked stage rename, canonical validation, compensating restore, backup cleanup, and guard cleanup. Clear the Phase 34 publication state only after the installed canonical blocked bundle validates, and record the same attempt identity in its safe run manifest so successful blocked installation is correlated. If rollback restores prior unblocked data, keep the Phase 34 state blocking; never classify the restored content itself as current unblocked authority.

Per D-03 and D-04, replace `_phase34_authority_is_valid(root)` with an evaluator that receives the current attempt and `CommandOutcome`. For status zero, require the existing lifecycle-valid normal authority invariants. For nonzero status, require effective persisted authority to be blocked for the same attempt and safe reason: either the installed Phase 34 manifest/packet/demotion bundle is blocked and carries that attempt identity, or the retained Phase 34 publication state is valid and blocking for it. Reject stale blocking artifacts from earlier attempts. When Phase 34 is nonzero and effective authority is not exact and blocked, do not run Phase 35 and retain the workflow marker. When it is exact and blocked, Phase 35 must treat any restored unblocked canonical Phase 34 bundle behind the publication state as invalid source authority and publish a fresh blocked Phase 35 bundle; it must never derive approval from that restored data.

Per D-05, keep readiness, cutover verdict, production route, and demotion authorization separate. Per D-08, the code path that clears the existing Phase 35 guard and coordinator workflow marker must first validate: the installed Phase 35 generated artifacts and security projection; explicit verdict/route consistency; independent demotion fields; and, after nonzero Phase 34, the exact persisted blocked Phase 34 attempt. If validation, marker removal, restore, or cleanup fails, keep at least one durable blocking marker and return nonzero. Per D-10, satisfy every focused fault regression without a test-only production flag, swallowed exception, `|| true`, raw-payload copy, or unconditional stale restore. Per D-11, retain `phase38_cutover_workflow.py` as the single production coordinator called by the shell. Per D-12 through D-15, keep the actual-producer baseline, small one-fault mutations, existing four routing paths, and separate demotion predicates.

Per D-12, D-13, and D-15, add two real-producer tests to `Phase38ActualProducerWorkflowTest` using its canonical actual Phase 31-through-35 baseline and one fault mutation per test. The first seeds approved Phase 35 output and injects the true pre-create Phase 35 guard failure. The second seeds approved Phase 34/35 output, makes the actual Phase 31 manifest invalid, and injects only the Phase 34 blocked-stage install failure after prior authority moved. Execute the real producer callables in the temp root so filesystem seams can be patched; do not replace producer outputs with handwritten decision snapshots. Both cases must assert nonzero status, no production-cutover planning, no demotion authorization, no readable prior approval through any canonical reader, safe bounded diagnostics, and persisted correlation to the current attempt. Per D-05 and D-14, leave the established verdict/route/readiness/demotion truth tables intact.

After the selected Task 2 command is GREEN, run `just phase38-verify` as the authoritative repository gate and confirm it executes all focused/integration tests before default blocked publication. Perform a simplification pass: keep attempt-state parsing at the boundary, keep final authority evaluation pure, use small filesystem helpers, and avoid duplicating Phase 34/35 bundle validation. Before the second GREEN atomic commit, run the mandatory Rust commands in this exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run `git diff --check` and review the scoped diff for stale approval replay, guard bypass, partial rollback, unsafe marker paths, and diagnostic leakage. Do not commit if any selected test, authoritative gate, Rust command, or diff check fails.
  </action>
  <verify>
    <automated>python3 -m unittest tools.bazel.phase34_final_readiness_demotion_dry_run_test.Phase34FinalReadinessDemotionDryRunTest.test_source_failure_stage_rename_failure_keeps_seeded_prior_authority_guarded tools.bazel.phase35_cutover_decision_artifact_test.Phase35GuardedPublicationTest.test_guard_precreation_failure_blocks_seeded_prior_authority_for_all_readers tools.bazel.phase38_cutover_workflow_test.CoordinatorTests.test_guard_precreation_failure_keeps_seeded_prior_authority_persistently_blocked tools.bazel.phase38_cutover_workflow_integration_test.Phase38ActualProducerWorkflowTest.test_phase35_guard_precreation_failure_blocks_seeded_approval_for_every_reader tools.bazel.phase38_cutover_workflow_integration_test.Phase38ActualProducerWorkflowTest.test_phase34_blocked_install_failure_never_republishes_seeded_approval</automated>
    <expected>GREEN in under 30 seconds: both verifier reproductions pass through focused and actual-producer paths with exact-attempt blocking authority.</expected>
  </verify>
  <acceptance-criteria>
    - "With seeded prior approved Phase 35 authority, an injected `touch_guard` failure before file creation returns nonzero, leaves the Phase 35 guard absent, leaves a validated durable workflow-attempt marker present, and causes `ensure_canonical_authority`, `run_security_scan`, and `load_final_authority` to reject the prior bundle."
    - "With seeded unblocked Phase 34 and approved Phase 35 authority, invalid Phase 31 plus blocked-stage rename failure returns nonzero; restored Phase 34 JSON is governed by an exact-attempt blocked state, Phase 35 never republishes readable approval, and production-cutover planning and demotion authorization are false."
    - "After nonzero Phase 34, Phase 35 protection clears only when persisted Phase 34 effective authority is blocked for the current attempt and the installed Phase 35 bundle validates; stale lifecycle, stale attempt, reason mismatch, unblocked restored content without a guard, or malformed state keeps protection blocking."
    - "T-38-01 stale approval replay, T-38-02 guard bypass, and T-38-04 partial publication/rollback failure each have focused unit coverage and an actual-producer acceptance case."
    - "Existing default blocked, complete approved, named targeted-repair, invalid Phase 31/33, approved-with-missing/rejected-demotion, and blocked-readiness-with-approved-demotion cases remain green."
    - "The selected Task 2 regressions are first observed RED without a commit, then pass GREEN in under 30 seconds."
    - "`just phase38-verify` passes and remains the authoritative tests-before-publication gate; no Bazel, shell, root alias, or justfile change is required."
    - "Before the Task 2 GREEN commit, the executor runs the selected Python command, `just phase38-verify`, `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `git diff --check`; all commands pass."
  </acceptance-criteria>
  <done>Both verifier reproductions fail closed durably for the exact workflow attempt, every canonical reader rejects stale approval, and the full Phase 38 route/demotion matrix passes.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Coordinator attempt start to Phase 35 guard creation | Phase 35 guard creation can fail before the guard file exists while stale canonical approval is present. |
| Invalid Phase 31/33 source to Phase 34 fallback installation | Blocked staging, rename, validation, rollback, or cleanup can fail after prior unblocked authority moved. |
| Nonzero Phase 34 outcome to Phase 35 finalization | A restored prior Phase 34 bundle must not be mistaken for the current failed run's persisted authority. |
| Persisted guards and bundles to canonical readers | Readers must not accept absent Phase 35 guard as sufficient when a workflow or Phase 34 publication attempt remains blocked. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-38-01 | Spoofing / Elevation of Privilege | high | Seeded prior Phase 34/35 approval | mitigate | Task 1 proves each marker primitive makes seeded approval unreadable; Task 2 adds the focused and actual-producer verifier reproductions and requires an exact current-attempt blocked Phase 34 state before Phase 35 consumes a nonzero run or clears protection. |
| T-38-02 | Elevation of Privilege | high | Phase 35 pre-create guard boundary and canonical readers | mitigate | Task 1 makes the earlier workflow marker durably blocking even when its structured publication fails; Task 2 injects failure before the Phase 35 guard exists and separately proves every canonical reader rejects seeded approval. |
| T-38-03 | Tampering | high | Workflow-attempt and Phase 34 publication-state marker paths | mitigate | Task 1 adds separate one-concern absolute, traversal, wrong-root, symlink, wrong-type/non-directory, unreadable, malformed/missing-field, and cleanup/removal regressions for both new paths and applies exact-root/type checks before every marker mutation or read. |
| T-38-04 | Tampering / Denial of Service | high | Marker publication plus Phase 34 staged publication and compensating rollback | mitigate | Task 1 covers pre-create, atomic-install, and cleanup/removal failure on both marker surfaces; Task 2 reproduces blocked-stage rename failure after backup and retains blocked attempt authority through install, validation, rollback, and cleanup. |
| T-38-05 | Information Disclosure / Repudiation | high | Attempt and failure diagnostics | mitigate | Task 1 asserts safe marker fields; Task 2 permits only opaque attempt IDs, fixed lifecycle/output refs, safe reason categories, booleans, and blocked state, with no raw evidence or secret-bearing data. |
</threat-model>

<verification>
Each task preserves RED-GREEN TDD by running and recording its selected Python regressions RED before implementation without committing, then making that same command GREEN in under 30 seconds. Task 1 closes both marker authority/path surfaces; Task 2 closes the two verifier reproductions through focused and actual-producer cases. Before either atomic GREEN commit, the selected task command and `just phase38-verify` must pass; then run in exact order `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`, followed by `git diff --check`. The authoritative gate must continue to execute focused and integration tests before default blocked publication.
</verification>

<success-criteria>
- Both `gaps:` entries and all three failed verification truths are closed by durable persisted authority, not transient coordinator booleans.
- The two verifier reproductions pass as focused and actual-producer regressions with seeded prior approval.
- READY-02 and CUTOVER-03 can no longer be bypassed by Phase 34 blocked-install rollback.
- READY-03 and CUTOVER-01 retain their existing normal approved, blocked, targeted-repair, and independent demotion behavior.
- T-38-01 through T-38-05 have no unresolved path after the authoritative gate passes, including explicit path and failure coverage for both new marker surfaces.
</success-criteria>

<output>
After completion, create `.planning/phases/38-fail-closed-cutover-workflow/38-03-SUMMARY.md`.
</output>
