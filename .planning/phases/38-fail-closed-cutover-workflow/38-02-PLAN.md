---
phase: 38-fail-closed-cutover-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - tools/bazel/phase38_cutover_workflow.py
  - tools/bazel/phase38_cutover_workflow_test.py
  - tools/bazel/phase38_cutover_workflow_integration_test.py
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - BUILD.bazel
  - justfile
autonomous: true
requirements:
  - READY-02
  - READY-03
  - CUTOVER-01
  - CUTOVER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 38-2026-07-26T16-29-23
generated_at: 2026-07-26T16:55:00.000Z
must_haves:
  truths:
    - "The production workflow always finalizes Phase 35 after Phase 34 has published canonical output, including blocked output followed by nonzero Phase 34 status."
    - "The full actual-producer Phase 31-through-35 workflow covers default blocked, complete approved, targeted repair, invalid Phase 31, and invalid Phase 33 paths."
    - "Production-cutover planning is reachable only from a validated approved cutover verdict."
    - "Reference-demotion authority remains an orthogonal explicit predicate and never follows from the cutover verdict."
    - "The authoritative Phase 38 gate runs focused and integration regressions before publishing default blocked outputs."
  artifacts:
    - path: tools/bazel/phase38_cutover_workflow.py
      provides: "Production coordinator and final authority truth table"
    - path: tools/bazel/phase38_cutover_workflow_test.py
      provides: "Coordinator status, authority, route, and demotion unit regressions"
    - path: tools/bazel/phase38_cutover_workflow_integration_test.py
      provides: "Actual-producer Phase 31-through-35 workflow matrix"
    - path: tools/bazel/rust_workflow.sh
      provides: "Thin dispatch to the coordinator"
    - path: justfile
      provides: "Authoritative `phase38-verify` developer facade"
  key_links:
    - from: tools/bazel/rust_workflow.sh
      to: tools/bazel/phase38_cutover_workflow.py
      via: "single coordinator invocation rather than implicit set-e Phase 34/35 sequencing"
      pattern: "phase38"
    - from: tools/bazel/phase38_cutover_workflow_integration_test.py
      to: "actual Phase 31, Phase 32, Phase 33, Phase 34, and Phase 35 producer entrypoints"
      via: "real retained artifacts and canonical publication paths"
      pattern: "production-cutover-planning"
---

<objective>
Add one testable production coordinator, prove the complete Phase 31-through-35 route matrix with actual producer outputs, and wire an authoritative Phase 38 verification gate.

Purpose: Close B3 end to end so operational failure can return nonzero only after stale readiness and cutover authority have been durably replaced.
Output: Coordinator, focused and real-producer regressions, Bazel/runfiles wiring, thin shell dispatch, root alias, and `just phase38-verify`.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md
@.planning/phases/38-fail-closed-cutover-workflow/38-RESEARCH.md
@.planning/phases/38-fail-closed-cutover-workflow/38-VALIDATION.md
@.planning/phases/38-fail-closed-cutover-workflow/38-01-SUMMARY.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-02-SUMMARY.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-VERIFICATION.md
@tools/bazel/phase34_decision_reconciliation_integration_test.py
@tools/bazel/phase35_cutover_decision_artifact_test.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement a fail-closed Phase 34-to-35 production coordinator</name>
  <read_first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md</file>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-01-SUMMARY.md</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact.py</file>
    <file>tools/bazel/rust_workflow.sh</file>
  </read_first>
  <files>tools/bazel/phase38_cutover_workflow.py, tools/bazel/phase38_cutover_workflow_test.py</files>
  <behavior>
    - "Per D-03, Phase 35 finalization runs after Phase 34 whenever Phase 34 has installed a validated canonical success or blocked bundle, even when Phase 34 status is nonzero."
    - "Per D-04, the coordinator preserves the first nonzero operational status and safe category after final authority validation; fallback publication never changes failure to success."
    - "Final validation accepts production-cutover planning only from a validated Phase 35 `approved` verdict and rejects a present, malformed, unreadable, lifecycle-stale, or unsafe-path Phase 35 guard."
    - "Per D-05, demotion approval/gate state is reported separately and cannot upgrade cutover verdict or route."
    - "Coordinator output contains only safe statuses, reason categories, booleans, lifecycle IDs, and canonical sanitized refs."
  </behavior>
  <action>
Per D-11, create `phase38_cutover_workflow_test.py` first and implement one small production coordinator called by the shell and integration suite. Use separate one-concern Arrange/Act/Assert tests for the Phase 34 status × Phase 35 status truth table; missing, blocked, and approved final authority; and each Phase 35 guard state: present-valid, malformed, unreadable, lifecycle-stale, absolute, traversal, symlink-escape, wrong-root, and non-directory unsafe-path. Add separate tests for targeted-repair versus production-cutover route and each of the three orthogonal demotion cases. Implement `phase38_cutover_workflow.py` as a standard-library imperative shell around explicit callable Phase 34 and Phase 35 entrypoints or subprocess commands, with a pure final-status evaluator. Per D-01, use only the existing contract-defined Phase 34/35 output roots and public authority bundles; do not add a generation pointer or second schema. Validate canonical outputs and guard state after each step. Per D-03, invoke Phase 35 after a nonzero Phase 34 result only when the blocked Phase 34 replacement validates, and return only after Phase 35 finalization. Per D-04, retain the earliest failure status and safe category. Per D-05, validate route, verdict, readiness, and demotion as separate authority fields. Do not accept caller-supplied authority flags, duplicate Phase 34/35 business logic, parse raw evidence, or swallow status codes. After the narrow coordinator test passes and before committing, run the mandatory gate in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `git diff --check`.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase38_cutover_workflow_test.py -q</automated>
  </verify>
  <acceptance_criteria>
    - "`tools/bazel/phase38_cutover_workflow.py` contains a production coordinator and a pure final-status evaluator."
    - "Focused tests prove Phase 35 runs after validated blocked Phase 34 publication and the overall result remains nonzero."
    - "Separate coordinator tests reject present-valid, malformed, unreadable, lifecycle-stale, absolute, traversal, symlink-escape, wrong-root, and non-directory Phase 35 guard states."
    - "Only `approved` plus `production-cutover-planning` passes route authority validation."
    - "Demotion missing/rejected with approved cutover stays closed, demotion approved with blocked readiness stays closed, and only valid approve plus unblocked readiness opens."
    - "Before committing, the executor first runs the narrow automated command, then runs `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `git diff --check` in that order."
  </acceptance_criteria>
  <done>The production coordinator finalizes authority deterministically and reports failure only after the final Phase 34/35 state is safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the actual-producer Phase 31-through-35 route and stale-authority matrix</name>
  <read_first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-CONTEXT.md</file>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-RESEARCH.md</file>
    <file>tools/bazel/phase34_decision_reconciliation_integration_test.py</file>
    <file>tools/bazel/phase35_cutover_decision_artifact_test.py</file>
    <file>tools/bazel/phase38_cutover_workflow.py</file>
  </read_first>
  <files>tools/bazel/phase38_cutover_workflow_integration_test.py</files>
  <behavior>
    - "Per D-12, the baseline executes actual Phase 31, 32, 33, 34, and 35 producer/loading/publication paths rather than handwritten snapshot-only authority fixtures."
    - "Default quick inputs publish blocked readiness, blocked cutover, targeted repair, and no implied demotion."
    - "Complete valid inputs publish unblocked readiness, approved cutover, and production-cutover planning while demotion remains independently evaluated."
    - "One exact evidence or decision defect publishes blocked cutover and a named targeted-repair scope requiring a fresh decision."
    - "Per D-13, invalid Phase 31 and invalid Phase 33 cases start from seeded prior Phase 34/35 approval, replace both authority bundles, and return nonzero after proving the four required primary routes."
  </behavior>
  <action>
Per D-12, extend the Phase 37 real-producer fixture pattern through actual Phase 35 production rather than copying handwritten JSON snapshots. Execute actual producers through Phase 35 using the Phase 38 coordinator. Per D-15, build one canonical valid baseline and derive small one-concern mutations: default quick blocked, complete approved, one exact targeted-repair mutation, one invalid Phase 31 source, and one invalid Phase 33 source; keep staged-install fault tests separate and do not build a Cartesian-product shell suite. Per D-13, seed a prior unblocked/open Phase 34 bundle and approved/production-cutover Phase 35 bundle before each invalid-source case, then prove the four primary routes, exact named repair scope, fresh-decision requirement, durable replacement, and nonzero exit. Per D-14, add separate focused assertions that approved cutover with missing or rejected demotion stays closed, valid demotion with blocked readiness stays closed, and only unblocked readiness plus explicit valid demotion approval opens the dry run. Keep all files temp-scoped and sanitized and use actual producer contracts as the only schema authority. After the narrow real-producer integration test passes and before committing, run the mandatory gate in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `git diff --check`.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase38_cutover_workflow_integration_test.py -q</automated>
  </verify>
  <acceptance_criteria>
    - "The integration test executes actual Phase 31-through-35 producer and publication entrypoints."
    - "Five required primary cases pass: default blocked, complete approved, targeted repair, invalid Phase 31, and invalid Phase 33."
    - "Both invalid-source cases seed prior approval and assert durable Phase 34/35 blocked replacement before nonzero return."
    - "Approved cutover never implies demotion authority and targeted repair always requires a fresh decision."
    - "Before committing, the executor first runs the narrow automated command, then runs `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `git diff --check` in that order."
  </acceptance_criteria>
  <done>The actual producer workflow proves every required route and replaces seeded stale authority for upstream failures.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the coordinator and regressions into the authoritative Phase 38 gate</name>
  <read_first>
    <file>.planning/phases/38-fail-closed-cutover-workflow/38-VALIDATION.md</file>
    <file>tools/bazel/phase38_cutover_workflow.py</file>
    <file>tools/bazel/phase38_cutover_workflow_test.py</file>
    <file>tools/bazel/phase38_cutover_workflow_integration_test.py</file>
    <file>tools/bazel/BUILD.bazel</file>
    <file>tools/bazel/rust_workflow.sh</file>
    <file>BUILD.bazel</file>
    <file>justfile</file>
  </read_first>
  <files>tools/bazel/phase38_cutover_workflow_test.py, tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, BUILD.bazel, justfile</files>
  <action>
Per D-11, add hermetic Bazel targets/runfiles for the Phase 38 coordinator, coordinator unit suite, and actual-producer integration suite, and make `rust_workflow.sh` a thin dispatch to that coordinator. Add `phase38_verify_tests` and `phase38_verify` workflow case arms so focused Phase 34 tests, focused Phase 35 tests, Phase 38 coordinator tests, and Phase 38 integration tests run before the coordinator publishes the default blocked output. Per D-03, replace the Phase 35 arm's direct Phase 34-then-Phase 35 `set -e` sequence with explicit coordinator status capture and finalization while preserving nonzero failure propagation. Add root aliases consistent with existing phase aliases and one `just phase38-verify` recipe that runs tests before publication. Put exact wiring assertions in the owned `tools/bazel/phase38_cutover_workflow_test.py`: assert Bazel target/runfile labels, the shell-to-coordinator invocation, explicit status propagation, root aliases, and `just phase38-verify` test-before-publication ordering. Do not use `|| true`, `set +e` without explicit status handling, swallowed subprocess results, direct raw-evidence reads, or any production cutover/demotion mutation. After the narrow wiring test and shell syntax check pass, run `just phase38-verify`; before committing, run the mandatory gate in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `git diff --check`.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase38_cutover_workflow_test.py -q &amp;&amp; bash -n tools/bazel/rust_workflow.sh</automated>
  </verify>
  <acceptance_criteria>
    - "Bazel targets include all Phase 38 code/test runfiles and actual producer dependencies."
    - "`rust_workflow.sh` invokes one Phase 38 coordinator for Phase 34/35 finalization and propagates its status."
    - "`just phase38-verify` runs focused and integration tests before default blocked publication."
    - "`tools/bazel/phase38_cutover_workflow_test.py` owns exact assertions for Bazel labels/runfiles, shell coordinator dispatch and status propagation, root aliases, and `just` test-before-publication ordering."
    - "Root aliases and wiring tests expose the Phase 38 gate without changing production cutover or reference-demotion policy."
    - "Before committing, the executor first runs the narrow automated command and `just phase38-verify`, then runs `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `git diff --check` in that order."
  </acceptance_criteria>
  <done>The repository-owned Phase 38 gate proves and publishes fail-closed cutover authority through one testable production path.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Coordinator to Phase 34/35 commands | Nonzero operational status must not skip finalization or become success after fallback publication. |
| Canonical Phase 34/35 bundles to final authority evaluator | Stale, partial, guarded, or mismatched artifacts must not authorize a route. |
| Real producer fixtures to integration assertions | Handwritten shortcuts must not hide producer shape or lifecycle incompatibility. |
| Bazel/shell/just gate to publication | Missing tests or swallowed status can publish authority without proof. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-38-01 | Spoofing / Elevation of Privilege | high | Coordinator final authority | mitigate | Seed prior approval, require validated blocked replacement on source failure, and enforce exact approved-verdict/route predicates. |
| T-38-02 | Elevation of Privilege | high | Phase 35 guard consumption | mitigate | Final evaluator rejects present, malformed, unreadable, stale, or unsafe guard state. |
| T-38-03 | Tampering | high | Producer and output paths | mitigate | Reuse production path/symlink validation and temp-scoped real producers; no snapshot-only authority. |
| T-38-04 | Tampering / Denial of Service | high | Phase 34-to-35 sequencing | mitigate | Explicit status capture, mandatory Phase 35 finalization after validated Phase 34 output, and nonzero return after safe authority validation. |
| T-38-05 | Information Disclosure / Repudiation | high | Coordinator result and integration artifacts | mitigate | Safe bounded diagnostics only; actual retained outputs remain sanitized and lifecycle-linked. |

No high-severity threat may remain unresolved. The authoritative Phase 38 gate must fail before publication if any mitigation regression fails.
</threat-model>

<verification>
Run each new focused suite after its task. After Task 3, run `bash -n tools/bazel/rust_workflow.sh` and `just phase38-verify`. Before every task commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then `git diff --check`. Before phase verification, repeat `git diff --check`, `just phase38-verify`, the complete Rust sequence, and lifecycle validation.
</verification>

<success-criteria>
- Workflow orchestration cannot exit while prior Phase 34 or Phase 35 approval remains authoritative.
- Actual producer outputs cover default blocked, complete approved, targeted repair, and invalid-source paths.
- Production-cutover planning is reachable only from a valid approved verdict.
- Demotion remains an independent explicit predicate.
- The authoritative gate mitigates every high-severity Phase 38 threat before publication.
</success-criteria>

<output>
After completion, create `.planning/phases/38-fail-closed-cutover-workflow/38-02-SUMMARY.md`.
</output>
