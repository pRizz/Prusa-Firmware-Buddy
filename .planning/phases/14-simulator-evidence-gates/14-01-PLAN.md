---
phase: 14-simulator-evidence-gates
plan: 01
plan_id: 14-01
type: execute
wave: 1
depends_on:
  - 13-ci-evidence-orchestration
dependencies:
  - "Phase 13 CI evidence contract and workflow are complete and available as the pattern to mirror."
files_modified:
  - tools/bazel/manifests/phase14_simulator_evidence_contract.json
  - tools/bazel/phase14_simulator_evidence.py
  - tools/bazel/phase14_simulator_evidence_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - SIM-01
  - SIM-02
  - SIM-03
requirement_ids:
  - SIM-01
  - SIM-02
  - SIM-03
deliverables:
  - "Phase-14-owned simulator evidence contract under tools/bazel/manifests/"
  - "Deterministic stdlib Python verifier/runner with contract, dry-run, security, wiring, and real simulator command modes"
  - "Ignored build/ci-evidence/phase14 run manifest, normalized scenario summary, log references, contract snapshot, and redacted summary behavior"
  - "Bazel labels and just phase14-verify facade"
  - "Focused stdlib unittest coverage for contract, artifact, overclaim, redaction, command, and wiring semantics"
verification:
  - "python3 -m json.tool tools/bazel/manifests/phase14_simulator_evidence_contract.json >/dev/null"
  - "python3 tools/bazel/phase14_simulator_evidence_test.py"
  - "python3 tools/bazel/phase14_simulator_evidence.py --quick"
  - "python3 tools/bazel/phase14_simulator_evidence.py --wiring-only"
  - "just phase14-verify"
  - "git diff --check"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-06-17T16-11-34
generated_at: 2026-06-17T16:38:01Z
must_haves:
  truths:
    - "Maintainer can inspect a flow-by-flow simulator evidence matrix for startup, task readiness, watchdog-visible startup behavior, G-code, GUI, storage/resource, transfer, and selected failure scenarios."
    - "Maintainer can run deterministic local Phase 14 validation without a firmware image, Mini404/QEMU, or active pytest simulator dependencies."
    - "Maintainer can see the exact external inputs required for real simulator execution and run that path when firmware and simulator prerequisites exist."
    - "Maintainer can map every simulator result to SIM-01, SIM-02, SIM-03, v1.0 evidence rows, and cutover criteria."
    - "Generated evidence never claims physical watchdog timing, thermal/motion safety, physical media/UI behavior, live service/TLS, MMU/RS485/toolchanger, release-candidate readiness, retained-code acceptance, reference demotion, or hardware-only safety."
  artifacts:
    - path: "tools/bazel/manifests/phase14_simulator_evidence_contract.json"
      provides: "Phase 14 simulator scenario contract"
      contains: "sim-startup-bootstrap-ready"
    - path: "tools/bazel/phase14_simulator_evidence.py"
      provides: "Verifier, dry-run evidence writer, optional real simulator runner, and overclaim/secret guards"
      exports: ["main"]
    - path: "tools/bazel/phase14_simulator_evidence_test.py"
      provides: "Regression tests for Phase 14 contract and runner semantics"
    - path: "tools/bazel/BUILD.bazel"
      provides: "phase14_verify and phase14_verify_tests labels"
      contains: "phase14_verify"
    - path: "BUILD.bazel"
      provides: "Root aliases and Phase 14 evidence docs filegroup"
      contains: "phase14_simulator_evidence_docs"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase14-verify"
  key_links:
    - from: "tools/bazel/phase14_simulator_evidence.py"
      to: "tools/bazel/manifests/phase14_simulator_evidence_contract.json"
      via: "CONTRACT_MANIFEST constant and JSON validation"
      pattern: "phase14_simulator_evidence_contract\\.json"
    - from: "tools/bazel/phase14_simulator_evidence.py"
      to: "build/ci-evidence/phase14"
      via: "DEFAULT_OUTPUT_DIR and repo-relative artifact path guard"
      pattern: "build/ci-evidence/phase14"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase14_simulator_evidence.py"
      via: "phase14_verify dispatch"
      pattern: "phase14_verify"
    - from: "justfile"
      to: "//tools/bazel:phase14_verify"
      via: "phase14-verify recipe"
      pattern: "bazel run //tools/bazel:phase14_verify"
---

<objective>
Implement Phase 14 "Simulator Evidence Gates" as a Phase-14-owned contract, verifier/runner, tests, and workflow wiring.

Purpose: Maintainers need simulator evidence that is runnable and traceable without overstating what a simulator can prove. This plan creates reviewable simulator gates for `SIM-01`, `SIM-02`, and `SIM-03`, while preserving hardware, live-service, release, retained-code, and final demotion boundaries as explicit residual gates.

Output: JSON contract, Python verifier/runner, stdlib tests, Bazel labels, just facade, and ignored `build/ci-evidence/phase14` artifact behavior. Do not modify `.planning/config.json`. Do not commit.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-simulator-evidence-gates/14-CONTEXT.md
@.planning/phases/14-simulator-evidence-gates/14-RESEARCH.md
@.planning/phases/14-simulator-evidence-gates/14-VALIDATION.md
@tools/bazel/phase13_ci_evidence.py
@tools/bazel/phase13_ci_evidence_test.py
@tools/bazel/manifests/phase13_ci_evidence_contract.json
@tools/bazel/manifests/phase11_parity_pyramid.json
@tools/bazel/manifests/phase11_requirement_evidence.json
@tools/bazel/manifests/phase11_reference_comparisons.json
@tools/bazel/manifests/phase11_cutover_readiness.json
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile
@tests/integration/README.md
@tests/integration/conftest.py
@tests/integration/test_basic_examples.py
@tests/integration/test_safety.py
@tests/integration/test_prusa_link.py
@utils/simulator/simulator.py

<interfaces>
Contract shape to create:
```json
{
  "schema_version": "1",
  "id": "phase14_simulator_evidence_contract",
  "phase": "14-simulator-evidence-gates",
  "phase_lifecycle_id": "14-2026-06-17T16-11-34",
  "output_root": "build/ci-evidence/phase14",
  "artifact_name": "phase14-simulator-evidence",
  "status_vocabulary": [
    "passed",
    "failed",
    "skipped",
    "pending-simulator-input",
    "pending-simulator-dependency",
    "manual-hardware-required",
    "pending-hardware",
    "pending-live-service",
    "pending-release",
    "pending-review"
  ],
  "required_artifact_kinds": [
    "machine-readable-run-manifest",
    "simulator-log-reference",
    "normalized-scenario-summary",
    "redacted-evidence-summary",
    "contract-snapshot"
  ],
  "external_inputs": {
    "firmware_bin": "required for --run-simulator",
    "firmware_bbf_adjacent": "required by tests/integration/conftest.py bootstrap flow",
    "mini404_qemu": "from utils/bootstrap.py or --simulator",
    "pytest_environment": "repo requirements active",
    "ocr_cache": "downloaded by existing OCR helpers on first real run"
  },
  "scenarios": []
}
```

Each `scenarios[]` row must include:
```json
{
  "id": "sim-gcode-file-print-telemetry",
  "title": "Representative G-code file print telemetry",
  "requirement_ids": ["SIM-01"],
  "v1_requirement_ids": ["CORE-03", "VERF-03"],
  "phase11_source_refs": [
    "tools/bazel/manifests/phase11_requirement_evidence.json#req-core-03",
    "tools/bazel/manifests/phase11_reference_comparisons.json#ref-gcode-behavior-fixtures"
  ],
  "pytest_node_ids": ["tests/integration/test_prusa_link.py::test_printing_telemetry"],
  "skipped_pytest_node_ids": [],
  "proof_scope": "simulator",
  "expected_pass_semantics": "Running node passes and generated summary links logs without claiming motion, thermal, or hardware print quality proof.",
  "expected_failure_semantics": "Failure row names scenario, requirement IDs, pytest node IDs, artifact path, and reason.",
  "expected_artifact_path": "build/ci-evidence/phase14/logs/sim-gcode-file-print-telemetry.log",
  "retained_artifact_kind": "simulator-log-reference",
  "allowed_statuses": ["passed", "failed", "pending-simulator-input", "pending-simulator-dependency"],
  "residual_non_simulator_gates": ["pending-hardware", "pending-release"],
  "unsupported_claims": ["physical motion safety", "thermal safety", "release-candidate readiness"]
}
```

CLI contract to implement in `tools/bazel/phase14_simulator_evidence.py`:
```text
--contract-only       validate checked-in contract and Phase 11 source refs
--security-only       scan checked-in contract and generated Phase 14 artifacts for secrets/overclaims
--wiring-only         verify Bazel root/tools labels, rust_workflow dispatch, and just phase14-verify recipe
--quick               contract + security + deterministic dry-run evidence write under build/ci-evidence/phase14
--run-simulator       execute active pytest node IDs with explicit --firmware and optional --simulator
--firmware PATH       required only with --run-simulator
--simulator PATH      optional simulator binary override for real mode
--output-dir PATH     defaults to build/ci-evidence/phase14 and must stay under that root
```
</interfaces>
</context>

<decision_coverage>
| Decision | Coverage | Task | Notes |
|----------|----------|------|-------|
| D-01 | Full | 1, 2 | Flow-by-flow scenarios, not umbrella pass. |
| D-02 | Full | 1, 2 | Required scenario families are explicit rows. |
| D-03 | Full | 1, 2 | Watchdog row is simulator-observable startup/readiness only. |
| D-04 | Full | 1 | Rows cite Phase 11 parity, requirement, comparison, and cutover source refs. |
| D-05 | Full | 1 | New Phase 14 contract; Phase 11 manifests are read-only refs. |
| D-06 | Full | 1 | Mirrors Phase 13 contract pattern and output root. |
| D-07 | Full | 2 | Run manifest, log refs, normalized summary, redacted summary. |
| D-08 | Full | 1, 2 | Every row maps to `SIM-*` and v1.0/Phase 11 refs. |
| D-09 | Full | 2 | Dedicated Python runner/verifier over existing simulator/pytest surfaces. |
| D-10 | Full | 1, 2, 3 | Exact files, labels, and `just phase14-verify` path. |
| D-11 | Full | 2 | Deterministic dry-run plus explicit real simulator inputs. |
| D-12 | Full | 1, 2 | Uses `tests/integration/` and `utils/simulator/`; no new simulator framework. |
| D-13 | Full | 1, 2 | Contract and artifact guards reject overclaims. |
| D-14 | Full | 1, 2 | Residual status vocabulary includes hardware/live/release/review boundaries. |
| D-15 | Full | 1, 2 | Forbidden markers and generated artifact scans reject secret-bearing content. |
| D-16 | Full | 3 | No Phase 13 retrofit; Phase 14 owns simulator proof. |
</decision_coverage>

<dependency_graph>
| Task | Needs | Creates | Parallelizable |
|------|-------|---------|----------------|
| 1 | Phase 14 context, Phase 11/13 patterns | Contract, validation core, contract/security tests | No, creates runner/test files used by Task 2 |
| 2 | Task 1 contract and validation helpers | Dry-run artifacts, optional real simulator command mode, artifact tests | No, same runner/test files |
| 3 | Task 2 runner CLI and tests | Bazel labels, root aliases, rust_workflow dispatch, just facade | No, wiring depends on final CLI names |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 14 contract and contract verifier semantics</name>
  <files>tools/bazel/manifests/phase14_simulator_evidence_contract.json, tools/bazel/phase14_simulator_evidence.py, tools/bazel/phase14_simulator_evidence_test.py</files>
  <behavior>
    - Test `sim01_contract_rows`: contract contains startup/bootstrap, task readiness, watchdog-visible startup/readiness, and G-code telemetry rows mapped to `SIM-01`.
    - Test `sim02_contract_rows`: contract contains GUI filebrowser, storage/resource WUI list/delete, transfer negative/conflict, and selected thermal failure rows mapped to `SIM-02`.
    - Test `sim03_traceability`: every scenario maps to at least one `SIM-*` ID, at least one v1.0 requirement/reference ID, and at least one resolvable Phase 11 source ref.
    - Test `contract_rejects_overclaim`: verifier rejects claims for physical watchdog timing, hardware proof, live service proof, release readiness, signing proof, retained-code maintainer acceptance, reference demotion, cutover completion, or hardware-only safety.
    - Test `contract_rejects_secret_and_path_issues`: verifier rejects private key/certificate/token/password/Wi-Fi/raw-crash/firmware-package markers and paths outside `build/ci-evidence/phase14`.
    - Test `contract_rejects_skipped_pass_nodes`: skipped pytest nodes cannot appear as pass evidence nodes.
  </behavior>
  <action>
Create `tools/bazel/manifests/phase14_simulator_evidence_contract.json` with the top-level contract described in `<interfaces>`, per D-05, D-06, D-08, D-13, D-14, and D-15. Use exactly these scenario IDs unless a stronger checked-in reason appears during implementation:

1. `sim-startup-bootstrap-ready` - `SIM-01`; v1 refs `CORE-01`, `CORE-02`; cite `phase11_requirement_evidence#req-core-01`, `#req-core-02`, and `phase11_parity_pyramid#pyramid-simulator-flows`; use `tests/integration/test_basic_examples.py::test_filebrowser_shows_files` plus helper ref `tests/integration/actions/utils.py::wait_for_bootstrap`; residual `pending-hardware`.
2. `sim-task-readiness-home-wui` - `SIM-01`; v1 refs `CORE-02`, `IFCE-03`; cite `req-core-02`, `req-ifce-03`, `pyramid-simulator-flows`; pytest nodes `test_idle_version`, `test_idle_printer_api`, and `test_idle_job` from `tests/integration/test_prusa_link.py`; residual `pending-hardware`, `pending-live-service`.
3. `sim-watchdog-visible-startup-readiness` - `SIM-01`; v1 refs `BASE-04`, `CORE-01`, `CORE-04`; cite `req-base-04`, `req-core-01`, `req-core-04`, and `criteria-non-local-gates-identified`; semantics must say simulator startup/readiness evidence only, not physical watchdog timing or safety proof, per D-03.
4. `sim-gcode-file-print-telemetry` - `SIM-01`; v1 refs `CORE-03`, `VERF-03`; cite `req-core-03` and `ref-gcode-behavior-fixtures`; pytest node `tests/integration/test_prusa_link.py::test_printing_telemetry`; residual `pending-hardware`, `pending-release`.
5. `sim-gui-filebrowser-navigation` - `SIM-02`; v1 refs `IFCE-01`, `VERF-03`; cite `req-ifce-01` and `ref-ui-display-state-fixtures`; pytest node `tests/integration/test_basic_examples.py::test_filebrowser_shows_files`; residual `pending-hardware`.
6. `sim-storage-resource-wui-list-delete` - `SIM-02`; v1 refs `IFCE-03`, `IFCE-04`, `VERF-03`; cite `req-ifce-03`, `req-ifce-04`, and `ref-storage-migrations`; pytest nodes `test_list_files` and `test_delete_project`; residual `pending-hardware`.
7. `sim-transfer-negative-and-conflict` - `SIM-02`; v1 refs `IFCE-02`, `IFCE-03`; cite `req-ifce-02`, `req-ifce-03`, `ref-protocol-traces`; active pytest nodes `test_upload_notauth` and `test_delete_project_printing`; skipped nodes `test_printing_job`, `test_download_gcode`, `test_thumbnails`, `test_caching`, and `test_upload` must stay skipped or residual, not pass evidence.
8. `sim-selected-thermal-failures` - `SIM-02`; v1 refs `CORE-04`, `VERF-05`; cite `req-core-04`, `criteria-non-local-gates-identified`, and `concern-phase11-non-local-hardware-proof`; include at least one active min-temp, max-temp, runaway, and preheat pytest node from `tests/integration/test_safety.py`; mark skipped nozzle max-temp as skipped/residual; residual `pending-hardware`.
9. `sim-traceability-non-simulator-boundaries` - `SIM-03`; v1 refs `VERF-01`, `VERF-03`, `VERF-05`; cite `pyramid-simulator-flows`, `criteria-non-local-gates-identified`, `criteria-reference-demotion-blocked`, and `concern-phase11-reference-demotion`; no real pytest node required because this is the contract guard row.

Create `tools/bazel/phase14_simulator_evidence.py` with a stdlib-only functional core for JSON loading, required field checks, Phase 11 `file#row-id` source-ref resolution, status vocabulary validation, scenario coverage validation, repo-relative artifact path validation, skipped-node pass rejection, secret marker scanning, and overclaim scanning. Keep filesystem and subprocess behavior out of the pure validation helpers. Do not import pytest, aiohttp, EasyOCR, PIL, or simulator modules at import time.

Create `tools/bazel/phase14_simulator_evidence_test.py` using stdlib `unittest`, temporary repo roots, and Arrange/Act/Assert comments. Mirror the Phase 13 test fixture style: copy the verifier and contract into temp roots, mutate contracts to prove failures, and use subprocess calls to exercise CLI behavior.
  </action>
  <verify>
    <automated>python3 -m json.tool tools/bazel/manifests/phase14_simulator_evidence_contract.json &gt;/dev/null</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence.py --security-only</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence_test.py -k contract</automated>
  </verify>
  <done>Contract exists, covers all required `SIM-*` requirements and scenario families, resolves Phase 11 source refs, rejects overclaim/secret/path/skipped-pass defects, and local verification does not need simulator dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic dry-run artifacts and real simulator command mode</name>
  <files>tools/bazel/phase14_simulator_evidence.py, tools/bazel/phase14_simulator_evidence_test.py</files>
  <behavior>
    - Test `quick_writes_artifacts`: `--quick` writes `run-manifest.json`, `normalized-scenarios.json`, `redacted-summary.json`, `contract-snapshots/phase14_simulator_evidence_contract.json`, and per-scenario log references under `build/ci-evidence/phase14`.
    - Test `quick_preserves_pending_inputs`: dry-run scenarios with active simulator nodes record `pending-simulator-input`, not `passed`.
    - Test `quick_summary_names_external_inputs`: redacted summary lists firmware `.bin`, adjacent `.bbf`, Mini404/QEMU, pytest environment, OCR cache, and optional simulator binary as real-run inputs.
    - Test `real_mode_requires_firmware`: `--run-simulator` without `--firmware` exits nonzero with an actionable message.
    - Test `real_command_is_argument_list`: command builder uses `sys.executable -m pytest` with validated node IDs, `--firmware`, and optional `--simulator`; no `shell=True`, `bash -c`, `python -c`, or inline foreign-language logic.
    - Test `generated_outputs_are_redacted`: generated outputs do not contain private key, certificate, token, password, Wi-Fi credential, raw crash dump, firmware package payload, or overclaim markers.
  </behavior>
  <action>
Extend `tools/bazel/phase14_simulator_evidence.py` with the CLI contract from `<interfaces>`, per D-07, D-09, D-11, D-12, D-13, D-14, and D-15.

Implement `--quick` as contract validation plus deterministic evidence generation. It must remove/recreate only the selected output directory after verifying it is repo-relative and under `build/ci-evidence/phase14`. It must write:

- `build/ci-evidence/phase14/run-manifest.json` with phase metadata, lifecycle ID, command mode, generated timestamp, scenario results, requirement coverage, external input status, and residual gate summary.
- `build/ci-evidence/phase14/normalized-scenarios.json` with one normalized row per contract scenario, including `scenario_id`, status, `requirement_ids`, `v1_requirement_ids`, `phase11_source_refs`, `artifact_refs`, and `residual_non_simulator_gates`.
- `build/ci-evidence/phase14/redacted-summary.json` with review-focused scenario status, SIM coverage, unsupported/hardware-only boundaries, real-run input names, and no secret or raw artifact content.
- `build/ci-evidence/phase14/contract-snapshots/phase14_simulator_evidence_contract.json` as a sanitized checked-in contract snapshot.
- `build/ci-evidence/phase14/logs/{scenario_id}.log` entries that are log references in dry-run mode and sanitized captured output in real simulator mode.

In dry-run mode, active simulator scenarios must be `pending-simulator-input` unless the row is a contract-only traceability row that can pass on contract validation alone. Missing pytest/Mini404/QEMU/firmware cannot become pass evidence. The command should still exit 0 when all checks are structurally valid and pending statuses are allowed by the contract.

Implement `--run-simulator` as an explicit mode only. Require `--firmware`; validate the firmware path is a file ending in `.bin`; check the adjacent `.bbf` exists because `tests/integration/conftest.py` copies it during bootstrap; accept optional `--simulator` and validate it as a file if provided. Build subprocess commands as argument lists, never a shell string. Run only active pytest node IDs from the contract. If pytest, simulator binary, or firmware inputs are missing, record `pending-simulator-dependency` or exit nonzero for invalid explicit inputs; never record a pass.

Keep redaction conservative. Generated artifacts may include scenario IDs, node IDs, basenames, sanitized command summaries, status, and reasons. They must not include raw firmware bytes, `.bbf`/`.dfu` payloads, raw crash dumps, private certificates, signing keys, Connect tokens, Wi-Fi credentials, credential values, or final cutover claims.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase14_simulator_evidence_test.py -k artifacts</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence_test.py -k command</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence.py --quick</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence.py --security-only</automated>
  </verify>
  <done>`--quick` produces deterministic, secret-safe Phase 14 artifacts under `build/ci-evidence/phase14`; real simulator execution is documented and runnable only with explicit inputs; missing inputs remain pending rather than false pass evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Phase 14 into Bazel and just workflows, then simplify</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase14_simulator_evidence.py, tools/bazel/phase14_simulator_evidence_test.py</files>
  <behavior>
    - Test `wiring_accepts_phase14_surface`: `--wiring-only` accepts the final Bazel labels, root aliases/filegroup, rust workflow dispatch, and `just phase14-verify`.
    - Test `wiring_rejects_missing_just_recipe`: removing the just recipe fails with a message naming `phase14-verify`.
    - Test `wiring_rejects_missing_bazel_labels`: missing `phase14_verify` or `phase14_verify_tests` fails with a label-specific message.
    - Test `wiring_runs_tests_before_verifier`: `justfile` and `rust_workflow.sh` keep tests before verifier execution.
  </behavior>
  <action>
Add Phase 14 Bazel and just workflow integration per D-10 and D-16.

In `tools/bazel/BUILD.bazel`, add:

- `shell_binary(name = "phase14_verify", src = "rust_workflow.sh", data = ["phase14_simulator_evidence.py", "manifests/phase14_simulator_evidence_contract.json", "//:phase14_simulator_evidence_docs", "//:phase11_cutover_evidence_docs"])`
- `shell_binary(name = "phase14_verify_tests", src = "rust_workflow.sh", data = ["phase14_simulator_evidence.py", "phase14_simulator_evidence_test.py", "manifests/phase14_simulator_evidence_contract.json"])`

In root `BUILD.bazel`, add:

- `filegroup(name = "phase14_simulator_evidence_docs", srcs = [".planning/phases/14-simulator-evidence-gates/14-CONTEXT.md", ".planning/phases/14-simulator-evidence-gates/14-RESEARCH.md", ".planning/phases/14-simulator-evidence-gates/14-VALIDATION.md", ".planning/phases/14-simulator-evidence-gates/14-01-PLAN.md"])`
- `alias(name = "phase14_verify", actual = "//tools/bazel:phase14_verify")`
- `alias(name = "phase14_verify_tests", actual = "//tools/bazel:phase14_verify_tests")`

In `tools/bazel/rust_workflow.sh`, add dispatch cases:

```bash
phase14_verify)
  python3 tools/bazel/phase14_simulator_evidence.py --wiring-only
  python3 tools/bazel/phase14_simulator_evidence.py --quick
  ;;
phase14_verify_tests)
  python3 tools/bazel/phase14_simulator_evidence_test.py
  ;;
```

In `justfile`, add:

```make
phase14-verify:
    bazel run //tools/bazel:phase14_verify_tests
    bazel run //tools/bazel:phase14_verify
```

After wiring works, perform a simplification pass:

- Remove duplicated scenario constants from tests when the contract can be the source of truth without weakening assertions.
- Keep helper functions small and single-purpose; split functions that grow beyond the Bright Builds refactor trigger.
- Keep validation helpers pure and filesystem/subprocess work in a thin shell.
- Confirm no new third-party dependency, broad simulator framework, source-code rewrite, or `.planning/config.json` edit was introduced.
- Confirm deferred Phase 15/16/17/18 evidence remains residual and no task implements deferred scope.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase14_simulator_evidence_test.py -k wiring</automated>
    <automated>python3 tools/bazel/phase14_simulator_evidence.py --wiring-only</automated>
    <automated>bazel query "//tools/bazel:phase14_verify + //tools/bazel:phase14_verify_tests + //:phase14_verify + //:phase14_verify_tests"</automated>
    <automated>just phase14-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>`just phase14-verify` runs Phase 14 tests before the verifier through Bazel, generated evidence is under `build/ci-evidence/phase14`, and the code path remains small, stdlib-only, and boundary-preserving.</done>
</task>

</tasks>

<threat_model>
Security enforcement is enabled. Use ASVS L1 posture: validate boundary inputs, avoid leaking secrets, avoid unsafe command execution, and keep evidence claims auditable. No high-severity finding is accepted by this plan; if implementation discovers a high-severity path, stop and redesign before marking Phase 14 complete.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in JSON contract -> verifier | Untrusted schema/content is parsed and must be validated before use. |
| Contract scenario rows -> generated evidence | Scenario text could overclaim proof or reference unsafe artifact paths. |
| CLI args -> subprocess runner | Firmware/simulator paths and mode flags influence real simulator execution. |
| Simulator/pytest output -> retained logs/summaries | External process output may contain secrets, raw dumps, payload names, or misleading proof language. |
| Bazel/just facade -> developer workflow | Wiring can accidentally skip tests, run the wrong verifier, or hide missing dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing / Repudiation | Contract and generated summaries | Medium | mitigate | Reject forbidden overclaim phrases; require residual non-simulator gates for hardware, live service, release, signing, retained-code, and demotion boundaries. |
| T-14-02 | Tampering | Artifact paths and source refs | Medium | mitigate | Require repo-relative non-traversing paths under `build/ci-evidence/phase14`; resolve `file#row-id` refs against existing Phase 11 JSON rows. |
| T-14-03 | Information Disclosure | Logs and redacted summaries | Medium | mitigate | Scan checked-in contract and generated artifacts for private keys, certificates, tokens, passwords, Wi-Fi credentials, raw crash dumps, and firmware payload markers; write only sanitized logs and summaries. |
| T-14-04 | Elevation of Privilege | Real simulator command mode | Medium | mitigate | Build subprocess commands as argument lists from validated paths and known pytest node IDs; never use `shell=True`, `bash -c`, inline `python -c`, or contract-provided executable text. |
| T-14-05 | Tampering / Repudiation | Missing simulator dependencies | Medium | mitigate | Default local mode is deterministic dry-run; missing firmware, pytest, or Mini404/QEMU is `pending-simulator-input` or `pending-simulator-dependency`, never `passed`. |
| T-14-06 | Information Disclosure / Repudiation | Skipped transfer and safety nodes | Low | mitigate | Require skipped pytest nodes to stay skipped/residual; reject skipped nodes in pass evidence fields. |
| T-14-07 | Denial of Service | Real simulator runtime | Low | accept | Real mode can be long-running by nature; deterministic `--quick` remains the default local gate and does not require simulator execution. |
</threat_model>

<goal_backward_check>
Phase 14 goal: Maintainers can review simulator evidence for startup, G-code, GUI, storage, transfer, and selected failure flows without confusing simulator proof for hardware proof.

Observable truths achieved by this plan:

1. Maintainers can inspect scenario-by-scenario simulator gates because Task 1 creates explicit contract rows for each required scenario family.
2. Maintainers can run local validation without unavailable simulator inputs because Task 2 implements deterministic `--quick` dry-run artifacts.
3. Maintainers can execute real simulator flows when prerequisites exist because Task 2 documents and validates explicit `--run-simulator --firmware` inputs and builds pytest commands over existing integration tests.
4. Maintainers can trace results back to `SIM-*`, v1.0 requirements, Phase 11 source rows, and cutover criteria because Task 1 validates those mappings and Task 2 emits them in generated manifests.
5. Maintainers cannot confuse simulator evidence with hardware/live/release/final proof because Tasks 1 and 2 reject overclaims, preserve residual statuses, and scan generated outputs.

Therefore, executing all tasks satisfies `SIM-01`, `SIM-02`, and `SIM-03` without reducing any locked Phase 14 decision or implementing deferred Phase 15-18 scope.
</goal_backward_check>

<verification>
Run these checks before marking the plan complete:

```bash
python3 -m json.tool tools/bazel/manifests/phase14_simulator_evidence_contract.json >/dev/null
python3 tools/bazel/phase14_simulator_evidence_test.py
python3 tools/bazel/phase14_simulator_evidence.py --contract-only
python3 tools/bazel/phase14_simulator_evidence.py --security-only
python3 tools/bazel/phase14_simulator_evidence.py --quick
python3 tools/bazel/phase14_simulator_evidence.py --wiring-only
bazel query "//tools/bazel:phase14_verify + //tools/bazel:phase14_verify_tests + //:phase14_verify + //:phase14_verify_tests"
just phase14-verify
git diff --check
```

Do not require real simulator execution for local completion unless firmware `.bin`, adjacent `.bbf`, Mini404/QEMU, and the repo pytest environment are available. If real simulator mode is not run, the summary must state that simulator execution inputs remain external and that dry-run evidence is contract validation, not proof of firmware behavior.
</verification>

<success_criteria>
- `tools/bazel/manifests/phase14_simulator_evidence_contract.json` exists and covers exactly `SIM-01`, `SIM-02`, and `SIM-03`.
- Contract rows include the required simulator scenario families and Phase 11/v1.0 source refs.
- `tools/bazel/phase14_simulator_evidence.py --quick` writes secret-safe generated artifacts under `build/ci-evidence/phase14`.
- `tools/bazel/phase14_simulator_evidence.py --run-simulator --firmware <firmware.bin>` is a documented explicit real-run path and validates required external inputs.
- `just phase14-verify` runs Phase 14 tests before the verifier through Bazel.
- Overclaim guards block hardware proof, live-service proof, release-candidate readiness, signing proof, retained-code acceptance, final reference demotion, and hardware-only safety claims.
- No source or planning artifact includes secrets, raw crash dumps, or firmware package payloads.
</success_criteria>

<residual_risks>
- Real simulator execution still depends on bootstrapped pytest dependencies, Mini404/QEMU, OCR cache, and a firmware `.bin` with adjacent `.bbf`; deterministic dry-run evidence is intentionally not a real simulator pass.
- Physical watchdog timing, thermal/motion safety, physical storage media, physical UI input, MMU, RS485, toolchanger, and hardware-only safety remain owned by Phase 15 evidence.
- Live Connect/WUI/TLS, telemetry, proxy, long-transfer, and crash-dump upload evidence remain owned by Phase 16.
- Release-candidate artifacts, signing, provenance, resources, and auxiliary packages remain owned by Phase 17.
- Retained-code acceptance and final reference-demotion approval remain owned by Phase 18.
</residual_risks>

<output>
After completion, create `.planning/phases/14-simulator-evidence-gates/14-01-SUMMARY.md` summarizing implementation, commands run, generated artifact paths, simulator real-run status, and residual evidence boundaries.
</output>
