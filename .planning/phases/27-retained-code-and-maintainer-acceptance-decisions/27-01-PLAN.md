---
phase: 27-retained-code-and-maintainer-acceptance-decisions
plan: "01"
type: execute
wave: 1
depends_on:
  - "26"
files_modified:
  - tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json
  - tools/bazel/phase27_retained_code_acceptance_decisions.py
  - tools/bazel/phase27_retained_code_acceptance_decisions_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - ACPT-02
  - ACPT-03
requirements_addressed:
  - ACPT-02
  - ACPT-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-06-25T01-06-06
generated_at: 2026-06-25T01:29:18Z
must_haves:
  truths:
    - "Maintainer can submit machine-readable retained-code decisions for every canonical Phase 18 retained packet, and each decision is accepted, rejected, blocked, or deferred-approved-exception only from explicit maintainer input."
    - "Maintainer can approve, reject, or exception final-readiness criteria through machine-readable rows, not prose-only notes."
    - "Exception approvals require complete Phase 18 exception metadata plus owner or approver, affected scope, residual risk, mitigation or follow-up, and expiry or review trigger."
    - "Redaction failures, overclaim failures, unsafe refs, source-ref failures, and stale lifecycle evidence hard-block before exception evaluation."
    - "Phase 27 outputs distinguish evidence failures, accepted retained-code risks, unresolved residual risks, exception state, final-readiness decision state, and reference-demotion authorization."
    - "Phase 27 never authorizes reference demotion; it emits a Phase 28 handoff with demotion authorization blocked or not approved."
  artifacts:
    - path: "tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json"
      provides: "Phase 27 wrapper contract around Phase 18 acceptance semantics and Phase 26 upstream rows"
      contains: "phase27_retained_code_acceptance_decisions_contract"
    - path: "tools/bazel/phase27_retained_code_acceptance_decisions.py"
      provides: "Phase 27 decision verifier, normalizer, hard-block guard, output writer, and CLI"
      contains: "DEFAULT_OUTPUT_DIR = Path(\"build/ci-evidence/phase27\")"
    - path: "tools/bazel/phase27_retained_code_acceptance_decisions_test.py"
      provides: "Regression coverage for ACPT-02, ACPT-03, hard blockers, no-demotion, retained outputs, and wiring"
      contains: "class Phase27RetainedCodeAcceptanceDecisionsTest"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 27 Bazel shell targets and source-ref runfiles"
      contains: "phase27_verify"
    - path: "BUILD.bazel"
      provides: "Root Phase 27 docs filegroup and aliases"
      contains: "phase27_retained_code_acceptance_decisions_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Phase 27 workflow dispatch"
      contains: "phase27_verify)"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase27-verify:"
  key_links:
    - from: "tools/bazel/phase27_retained_code_acceptance_decisions.py"
      to: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      via: "exact-match loading for packet IDs, final criteria, fields, vocabularies, exceptions, hard blockers, and demotion rules"
      pattern: "phase18_cutover_review_contract"
    - from: "tools/bazel/phase27_retained_code_acceptance_decisions.py"
      to: "build/ci-evidence/phase26/upstream-result-row-table.json"
      via: "Phase 26 upstream row consumption"
      pattern: "upstream-result-row-table"
    - from: "tools/bazel/phase27_retained_code_acceptance_decisions.py"
      to: "build/ci-evidence/phase27"
      via: "retained Phase 27 output writer"
      pattern: "build/ci-evidence/phase27"
    - from: "justfile"
      to: "//tools/bazel:phase27_verify"
      via: "phase27-verify recipe"
      pattern: "bazel run //tools/bazel:phase27_verify"
---

<objective>
Create the Phase 27 retained-code and maintainer acceptance decision gate.

Purpose: Phase 28 needs machine-readable retained-code, residual-risk, exception, final-readiness, and demotion-handoff inputs that preserve Phase 18 semantics and consume Phase 26 upstream rows without converting evidence status into maintainer acceptance.

Output: A Phase 27 contract manifest, Python verifier, unit/security/wiring tests, retained output writer under `build/ci-evidence/phase27`, Bazel labels, `rust_workflow.sh` dispatch, and `just phase27-verify` facade.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-CONTEXT.md
@.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-RESEARCH.md
@.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-VALIDATION.md
@.planning/phases/26-release-signing-and-upstream-result-evidence/26-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@tools/bazel/manifests/phase18_cutover_review_contract.json
@tools/bazel/phase18_cutover_review.py
@tools/bazel/phase18_cutover_review_test.py
@tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
@tools/bazel/phase26_release_signing_upstream_evidence.py
@tools/bazel/phase26_release_signing_upstream_evidence_test.py
@tools/bazel/manifests/phase11_retained_code_justifications.json
@tools/bazel/manifests/foreign_code_inventory.json
@tools/bazel/manifests/unsafe_boundary_audit.json
@tools/bazel/manifests/phase11_cutover_readiness.json
@BUILD.bazel
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
Material local guidance: `AGENTS.md`, `AGENTS.bright-builds.md`, `standards-overrides.md`, `standards/core/architecture.md`, `standards/core/code-shape.md`, `standards/core/testing.md`, `standards/core/verification.md`, and `standards/languages/rust.md` require Bright Builds standards, functional-core/imperative-shell verifier structure, focused unit tests with Arrange/Act/Assert comments, repo-native verification, and the required Cargo sequence before commits.

Canonical Phase 18 retained packet IDs from `tools/bazel/manifests/phase18_cutover_review_contract.json`:
`packet-hal-cmsis-startup-asm`, `packet-freertos-runtime`, `packet-marlin-cpp-print-core-oracle`, `packet-network-lwip-mbedtls-wui`, `packet-filesystem-fatfs-littlefs-libsysbase`, `packet-usb-tinyusb-and-media`, `packet-generated-assets-resource-pipeline`, `packet-release-signing-and-packaging`, `packet-mmu-modbus-auxiliary-controllers`, `packet-runtime-safety-crashdump-watchdog`.

Canonical Phase 18 upstream criteria:
`final-ci-evidence`, `final-simulator-evidence`, `final-hardware-safety-media-evidence`, `final-live-network-transfer-evidence`, `final-release-artifact-signing-evidence`, `final-retained-code-acceptance`, `final-residual-risk-review`, `final-maintainer-decision`, `final-reference-demotion-allowed`.

Phase 18 retained packet required fields:
`id`, `title`, `requirement_ids`, `taxonomy_tags`, `retained_source_refs`, `prior_phase_refs`, `required_evidence_refs`, `supplied_evidence_result_refs`, `owner`, `approver_role`, `approval_metadata`, `status`, `rationale`, `residual_risk`, `blocker_or_deferred_action`, `exception_ref`, `secret_handling_policy`, `unsupported_claims`.

Phase 18 final decision required fields:
`decision_id`, `criterion_id`, `decision`, `status`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `evidence_refs`, `residual_risk`, `exception`, `redaction_summary`.

Phase 18 exception required fields:
`scope`, `rationale`, `approver`, `approver_role`, `affected_printer_or_release_surface`, `mitigation_or_follow_up`, `expiry_or_review_trigger`, `evidence_refs`.

Phase 18 retained packet status vocabulary:
`pending-evidence`, `pending-maintainer-review`, `accepted`, `rejected`, `blocked`, `deferred-approved-exception`, `rejected-redaction`, `rejected-overclaim`.

Phase 18 final criterion status vocabulary:
`pending`, `passed`, `failed`, `blocked`, `exception-requested`, `exception-approved`, `exception-rejected`, `not-applicable`, `rejected-redaction`, `rejected-overclaim`.

Phase 18 review decision vocabulary:
`approve`, `reject`, `exception`.

Hard blocker reasons that must be evaluated before exception coverage:
`redaction-failed`, `overclaim-failed`, `lifecycle-mismatch`, `source-ref-failed`, `unsafe-ref`.

Phase 26 upstream row table path for Phase 27 consumption:
`build/ci-evidence/phase26/upstream-result-row-table.json`.
</interfaces>
</context>

<commit_verification>
Before every task commit and before any final GSD/docs commit in this Rust workspace, run the root required pre-commit sequence in this exact order:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```

Do not commit an intentionally failing RED state. If a task writes tests before implementation, keep the failing-test state uncommitted and commit only after implementation plus all required Rust checks and Phase 27 checks pass.
</commit_verification>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 27 contract and regression test surface</name>
  <files>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json, tools/bazel/phase27_retained_code_acceptance_decisions.py, tools/bazel/phase27_retained_code_acceptance_decisions_test.py</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/core/verification.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-CONTEXT.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-RESEARCH.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-VALIDATION.md</file>
    <file>tools/bazel/manifests/phase18_cutover_review_contract.json</file>
    <file>tools/bazel/phase18_cutover_review.py</file>
    <file>tools/bazel/phase18_cutover_review_test.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>tools/bazel/manifests/phase11_retained_code_justifications.json</file>
    <file>tools/bazel/manifests/foreign_code_inventory.json</file>
    <file>tools/bazel/manifests/unsafe_boundary_audit.json</file>
    <file>tools/bazel/manifests/phase11_cutover_readiness.json</file>
    <file>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json if present</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions.py if present</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions_test.py if present</file>
  </read_first>
  <behavior>
    - Test 1: `--contract-only` exact-matches Phase 18 retained packet IDs, final criterion IDs, retained required fields, final decision required fields, exception required fields, status vocabularies, review decision vocabulary, and hard blocker reasons.
    - Test 2: The Phase 27 contract declares `decision_axes` with exactly `evidence_state`, `maintainer_decision`, `exception_state`, `residual_risk_state`, `hard_failure_state`, and `demotion_authorization`.
    - Test 3: The Phase 27 contract declares generated artifacts: `acceptance-run-manifest.json`, `normalized-retained-code-decisions.json`, `residual-risk-register.json`, `exception-decision-register.json`, `final-readiness-decision-summary.json`, `phase28-handoff-manifest.json`, `decision-row-table.json`, `maintainer-acceptance-input-template.json`, `artifact-reference-summary.json`, `contract-snapshots/phase18_cutover_review_contract.json`, `contract-snapshots/phase26_release_signing_upstream_evidence_contract.json`, and `contract-snapshots/phase26-upstream-result-row-table.json`.
    - Test 4: The safe maintainer input template includes one retained-code decision row for every Phase 18 retained packet ID and one final-readiness decision row for every Phase 18 upstream criterion ID.
    - Test 5: The template rows require `approver`, `approver_role`, `decision_timestamp`, `decision`, `rationale`, `evidence_refs`, `residual_risk`, and `redaction_summary`; exception decisions also require the Phase 18 exception fields plus residual-risk-bearing mitigation metadata.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json` with `schema_version: "1"`, `id: "phase27_retained_code_acceptance_decisions_contract"`, `phase: "27-retained-code-and-maintainer-acceptance-decisions"`, `phase_lifecycle_id: "27-2026-06-25T01-06-06"`, `artifact_name: "phase27-retained-code-acceptance-decisions"`, `output_root: "build/ci-evidence/phase27"`, and `source_contracts` entries for `tools/bazel/manifests/phase18_cutover_review_contract.json`, `tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json`, `tools/bazel/manifests/phase11_retained_code_justifications.json`, `tools/bazel/manifests/foreign_code_inventory.json`, `tools/bazel/manifests/unsafe_boundary_audit.json`, and `tools/bazel/manifests/phase11_cutover_readiness.json`.

    In that contract, add `canonical_policy` fields that point to Phase 18 as canonical for retained packet schema, final decision schema, exception fields, status vocabularies, upstream criteria, hard blocker reasons, and demotion blocking rules per D-01 and D-02. Do not copy Phase 18 packet IDs, field sets, or vocabularies into Python constants as the authority; the verifier must load them from `phase18_cutover_review_contract.json` and assert exact coverage per D-03.

    In that contract, add `decision_axes` with exactly `evidence_state`, `maintainer_decision`, `exception_state`, `residual_risk_state`, `hard_failure_state`, and `demotion_authorization` per D-04. Add `hard_blocker_policy` with `redaction-failed`, `overclaim-failed`, `unsafe-ref`, `source-ref-failed`, and `lifecycle-mismatch` before exception evaluation per D-05 and D-10. Add `phase28_handoff_policy` with `demotion_authorization: "blocked"` and `phase27_may_authorize_demotion: false` per D-07.

    Add `exception_policy` requiring `scope`, `rationale`, `approver`, `approver_role`, `affected_printer_or_release_surface`, `mitigation_or_follow_up`, `expiry_or_review_trigger`, `evidence_refs`, `residual_risk`, and either `owner` or the same `approver` as owner per D-08 and D-09. Add `sensitive_role_policy` mapping `safety`, `hardware`, `watchdog`, `thermal`, `motion`, `safe-output`, and `crash-dump` surfaces to `safety-maintainer`; `release`, `signing`, and `provenance` surfaces to `release-maintainer`; and `tls`, `credential`, `connect`, `wui`, `proxy`, and `transfer` surfaces to `network-security-maintainer` per D-11.

    Add `generated_artifacts` with the exact 12 artifact paths listed in the behavior block. These implement D-12 and D-13. Add `phase26_upstream_rows_path: "build/ci-evidence/phase26/upstream-result-row-table.json"` and `phase26_generation_command: "python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26"` per D-14.

    Create `tools/bazel/phase27_retained_code_acceptance_decisions.py` with a minimal standard-library CLI skeleton exposing `--contract-only`, `--security-only`, `--wiring-only`, `--quick`, `--maintainer-input PATH`, `--phase26-upstream-rows PATH`, and `--output-dir PATH`. Implement `--contract-only` and `--security-only` in this task. Include constants for file paths only, not canonical Phase 18 IDs or vocabularies. `--contract-only` must load Phase 18 and Phase 27 contracts, then assert exact Phase 18 surfaces as described above.

    Create `tools/bazel/phase27_retained_code_acceptance_decisions_test.py` with `unittest` and class `Phase27RetainedCodeAcceptanceDecisionsTest`. Write focused tests for the behavior list above, using Arrange, Act, Assert comments for non-trivial tests. This implements D-15 for Phase 18 schema/vocabulary exact-match checks and retained packet coverage before Task 2 adds decision normalization and retained output checks.
  </action>
  <acceptance_criteria>
    - `tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json` exists and contains `phase27_retained_code_acceptance_decisions_contract`, `build/ci-evidence/phase27`, `phase18_cutover_review_contract.json`, `phase26_release_signing_upstream_evidence_contract.json`, `decision_axes`, and `phase27_may_authorize_demotion`.
    - `rg -n "phase27_may_authorize_demotion.*false|false.*phase27_may_authorize_demotion" tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json tools/bazel/phase27_retained_code_acceptance_decisions_test.py` returns at least one match.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --contract-only` exits 0.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --security-only` exits 0.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py` exits 0 and includes assertions for the 10 Phase 18 retained packet IDs and 9 Phase 18 upstream criterion IDs loaded from Phase 18.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --contract-only</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --security-only</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>Phase 27 has a source-backed wrapper contract and tests that prove it uses Phase 18 as canonical, declares all Phase 27 decision axes and retained artifacts, and keeps demotion authorization blocked in Phase 27.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement decision normalization, hard blockers, and retained outputs</name>
  <files>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json, tools/bazel/phase27_retained_code_acceptance_decisions.py, tools/bazel/phase27_retained_code_acceptance_decisions_test.py</files>
  <read_first>
    <file>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions.py</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions_test.py</file>
    <file>tools/bazel/manifests/phase18_cutover_review_contract.json</file>
    <file>tools/bazel/phase18_cutover_review.py</file>
    <file>tools/bazel/phase18_cutover_review_test.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>.planning/phases/26-release-signing-and-upstream-result-evidence/26-01-SUMMARY.md</file>
  </read_first>
  <behavior>
    - Test 1: explicit retained-code decisions with `decision: "approve"` normalize to `maintainer_decision: "accepted"` only when rationale, residual risk, evidence refs, approver, role, timestamp, and redaction summary are complete.
    - Test 2: retained-code decisions with `decision: "reject"` normalize to `maintainer_decision: "rejected"` with rationale and residual risk preserved.
    - Test 3: retained-code exception decisions normalize to `maintainer_decision: "deferred-approved-exception"` only when every exception metadata field is complete and the row is not hard-blocked.
    - Test 4: redaction failure, overclaim failure, unsafe refs, source-ref failure, or stale lifecycle evidence produces `hard_failure_state: "blocked"` before exception handling and cannot become accepted retained-code risk.
    - Test 5: Phase 26 upstream rows are consumed as status inputs but are not replayed or copied wholesale into Phase 27 outputs; Phase 27 emits decision deltas for retained-code, residual-risk, final-readiness, exception, and handoff state.
    - Test 6: quick mode writes all 12 generated artifacts under `build/ci-evidence/phase27` and snapshots Phase 18, Phase 26, and Phase 26 upstream rows.
    - Test 7: every generated JSON output has no `demotion_allowed: true`, no `demotion_authorization: "allowed"`, and no final-readiness overclaim text.
  </behavior>
  <action>
    Implement a functional core in `tools/bazel/phase27_retained_code_acceptance_decisions.py` with pure functions for loading and checking canonical Phase 18 surfaces, loading Phase 26 upstream rows, validating maintainer decision input, computing hard blockers, validating exception metadata, deriving orthogonal axes, and building output documents. Keep filesystem reads/writes and `argparse` in a thin shell per Bright Builds architecture guidance.

    Add a default safe maintainer input template generated from Phase 18 canonical data. Its top-level object must include `schema_version: "1"`, `phase: "27-retained-code-and-maintainer-acceptance-decisions"`, `source_contract_refs`, `retained_code_decisions`, `final_readiness_decisions`, and `reference_demotion_decision`. For quick mode, emit reviewable decision rows that remain blocked, pending-maintainer-review, or not approved until a maintainer supplies explicit decision input; do not use green evidence alone as acceptance per D-06.

    Implement retained-code decision validation for ACPT-02. Each retained-code decision row must name `packet_id`, `decision`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `evidence_refs`, `residual_risk`, `redaction_summary`, and optional `exception`. Valid decisions are `approve`, `reject`, and `exception` from Phase 18. Derived retained statuses must be limited to Phase 18 retained status vocabulary: `accepted`, `rejected`, `blocked`, or `deferred-approved-exception` for completed maintainer input; hard blockers may derive `rejected-redaction` or `rejected-overclaim` when those exact failures exist. This implements D-04, D-05, D-06, D-08, D-09, and D-10.

    Implement final-readiness decision validation for ACPT-03. Each final decision row must name `criterion_id`, `decision`, `approver`, `approver_role`, `decision_timestamp`, `rationale`, `evidence_refs`, `residual_risk`, `exception`, and `redaction_summary`. Derived final statuses must use Phase 18 final criterion vocabulary and must not set `final-reference-demotion-allowed` to `passed`, `exception-approved`, or any allowing status in Phase 27. The Phase 28 handoff must state `demotion_authorization: "blocked"`, `phase27_may_authorize_demotion: false`, and `phase28_required_decision: "explicit-maintainer-reference-demotion-decision"` per D-07.

    Consume `--phase26-upstream-rows`, defaulting to `build/ci-evidence/phase26/upstream-result-row-table.json`. If the file is absent, fail with a precise message that names the required command: `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26`. When present, validate the row IDs exactly match the 9 Phase 18 upstream criteria and validate the Phase 26 row required fields from `phase26_release_signing_upstream_evidence_contract.json`. Preserve Phase 26 statuses as `evidence_state` inputs and emit only Phase 27-owned `decision_delta` rows per D-14.

    Implement output containment using the Phase 26 pattern: `--output-dir` must be repo-relative, must not contain `..`, must resolve under `build/ci-evidence/phase27`, and must reject symlink escapes before deletion or write. Write the 12 generated artifacts named in Task 1. `contract-snapshots/phase26-upstream-result-row-table.json` must snapshot only the Phase 26 row table, not unrelated evidence directories. This implements D-12 and D-13.

    Add security scans for raw decision input and generated outputs. Reject forbidden field names and text patterns for private signing keys, private certificates, raw key bytes, signing payload bytes, raw firmware payloads, firmware payload bytes, raw logs, binary dumps, crash dump bytes, token values, password values, credential values, and secret values. Reject overclaim phrases that imply `reference demotion approved`, `demotion allowed`, `final readiness approved`, or `retained code accepted by evidence alone`. This completes D-05, D-07, D-11, and D-15.
  </action>
  <acceptance_criteria>
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26` exits 0 before Phase 27 quick mode is run.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --output-dir build/ci-evidence/phase27` exits 0.
    - All 12 generated artifact paths from Task 1 exist under `build/ci-evidence/phase27`.
    - `build/ci-evidence/phase27/phase28-handoff-manifest.json` contains `demotion_authorization` and does not contain `demotion_allowed`.
    - `rg -n '"demotion_allowed"\\s*:\\s*true|"demotion_authorization"\\s*:\\s*"allowed"' build/ci-evidence/phase27` returns no matches.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py` includes tests for explicit approve, reject, exception, hard-block-before-exception, missing Phase 26 row table, output-root symlink escape, generated artifact list, and no-demotion output scanning.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --contract-only</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --security-only</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --output-dir build/ci-evidence/phase27</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>Phase 27 can consume Phase 26 upstream rows, validate explicit maintainer retained-code/final-readiness decision inputs, enforce hard blockers before exceptions, write all retained outputs under `build/ci-evidence/phase27`, and keep reference demotion blocked for Phase 28.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Bazel, workflow dispatch, and just facade</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase27_retained_code_acceptance_decisions.py, tools/bazel/phase27_retained_code_acceptance_decisions_test.py</files>
  <read_first>
    <file>tools/bazel/BUILD.bazel</file>
    <file>BUILD.bazel</file>
    <file>tools/bazel/rust_workflow.sh</file>
    <file>justfile</file>
    <file>tools/bazel/manifests/phase27_retained_code_acceptance_decisions_contract.json</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions.py</file>
    <file>tools/bazel/phase27_retained_code_acceptance_decisions_test.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-CONTEXT.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-RESEARCH.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-VALIDATION.md</file>
    <file>.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-PLAN.md</file>
  </read_first>
  <behavior>
    - Test 1: `--wiring-only` verifies root docs filegroup, root aliases, tools Bazel targets, workflow cases, and `just phase27-verify`.
    - Test 2: `just phase27-verify` runs `bazel run //tools/bazel:phase27_verify_tests` before `bazel run //tools/bazel:phase27_verify`.
    - Test 3: `phase27_verify` dispatch runs Phase 26 quick generation before Phase 27 quick consumption so fresh checkouts can produce `build/ci-evidence/phase26/upstream-result-row-table.json`.
    - Test 4: Bazel runfiles include Phase 18 contract, Phase 26 contract, Phase 27 contract, Phase 27 context/research/validation/plan docs, source-ref manifests, the Phase 27 verifier, and the Phase 27 tests.
  </behavior>
  <action>
    Add a root `filegroup` in `BUILD.bazel` named `phase27_retained_code_acceptance_decisions_docs` with these `srcs`: `.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-CONTEXT.md`, `.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-RESEARCH.md`, `.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-VALIDATION.md`, and `.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-PLAN.md`. Add root aliases `phase27_verify` and `phase27_verify_tests` pointing to `//tools/bazel:phase27_verify` and `//tools/bazel:phase27_verify_tests`.

    Add `tools/bazel/BUILD.bazel` filegroup `phase27_source_ref_manifests` with `manifests/phase18_cutover_review_contract.json`, `manifests/phase26_release_signing_upstream_evidence_contract.json`, `manifests/phase27_retained_code_acceptance_decisions_contract.json`, `manifests/phase11_retained_code_justifications.json`, `manifests/foreign_code_inventory.json`, `manifests/unsafe_boundary_audit.json`, and `manifests/phase11_cutover_readiness.json`. Add `shell_binary` targets `phase27_verify` and `phase27_verify_tests` using `rust_workflow.sh`, mirroring Phase 26's style. Include `phase27_retained_code_acceptance_decisions.py`, `phase27_retained_code_acceptance_decisions_test.py`, the Phase 27 contract, `:phase27_source_ref_manifests`, and `//:phase27_retained_code_acceptance_decisions_docs` as data where appropriate.

    Add `tools/bazel/rust_workflow.sh` cases `phase27_verify)` and `phase27_verify_tests)`. The `phase27_verify` case must run exactly: `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --wiring-only`; `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26`; `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27`. The `phase27_verify_tests` case must run `python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py`.

    Add a `justfile` recipe `phase27-verify:` that runs `bazel run //tools/bazel:phase27_verify_tests` followed by `bazel run //tools/bazel:phase27_verify`. Extend `--wiring-only` tests in `tools/bazel/phase27_retained_code_acceptance_decisions_test.py` to copy or synthesize the relevant wiring files in a temp root and verify exact strings from this task. This completes D-15 for Bazel/just wiring and preserves D-14 by generating Phase 26 quick rows before Phase 27 consumes them.
  </action>
  <acceptance_criteria>
    - `rg -n "phase27_retained_code_acceptance_decisions_docs|phase27_verify|phase27_verify_tests" BUILD.bazel` returns all three root surfaces.
    - `rg -n "phase27_source_ref_manifests|phase27_retained_code_acceptance_decisions.py|phase27_retained_code_acceptance_decisions_test.py|phase27_retained_code_acceptance_decisions_contract.json" tools/bazel/BUILD.bazel` returns all Phase 27 runfile surfaces.
    - `rg -n "phase27_verify\\)|phase27_verify_tests\\)|phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26|phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27" tools/bazel/rust_workflow.sh` returns all required dispatch strings.
    - `rg -n "phase27-verify:|bazel run //tools/bazel:phase27_verify_tests|bazel run //tools/bazel:phase27_verify" justfile` returns the Phase 27 facade and ordering.
    - `python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --wiring-only` exits 0.
    - `just phase27-verify` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py</automated>
    <automated>python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --wiring-only</automated>
    <automated>just phase27-verify</automated>
    <automated>git diff --check</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <done>Phase 27 is exposed through Bazel, `rust_workflow.sh`, and `just phase27-verify`; the facade runs tests before verifier execution, regenerates Phase 26 upstream rows before Phase 27 quick mode, and all wiring drift checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer input -> verifier | Untrusted machine-readable decision rows cross into Phase 27 retained evidence generation. |
| Phase 26 generated rows -> Phase 27 verifier | Generated upstream row data from `build/ci-evidence/phase26` influences Phase 27 decision deltas. |
| verifier -> build output filesystem | Phase 27 writes retained evidence under `build/ci-evidence/phase27` and must not escape that tree. |
| Phase 27 outputs -> Phase 28 final readiness | Phase 28 will consume Phase 27 handoff artifacts; overclaims here can incorrectly influence cutover readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Spoofing | `maintainer-acceptance-input-template.json` and `--maintainer-input` parsing | mitigate | Require explicit `approver`, `approver_role`, `decision_timestamp`, rationale, evidence refs, and sensitive-role policy checks for safety/release/signing/TLS/credential/crash-dump/hardware-adjacent surfaces. This supports ASVS L1 validation and business-logic controls. |
| T-27-02 | Tampering | `--output-dir` retained writer | mitigate | Reject absolute paths, parent traversal, and symlink escape; only allow resolved writes under `build/ci-evidence/phase27` before deleting or writing output. |
| T-27-03 | Repudiation | retained-code and final-readiness decisions | mitigate | Emit `decision-row-table.json`, `acceptance-run-manifest.json`, and `phase28-handoff-manifest.json` with source contract refs, packet IDs, criterion IDs, approver, role, timestamp, rationale, evidence refs, and generated timestamp. |
| T-27-04 | Information Disclosure | raw decision inputs and generated outputs | mitigate | Scan raw text and parsed JSON for private keys, private certificates, raw key bytes, signing payload bytes, raw firmware payloads, raw logs, binary dumps, crash dump bytes, token values, password values, credential values, and secret values before retained writes and after output generation. |
| T-27-05 | Denial of Service | malformed JSON or missing Phase 26 upstream rows | mitigate | Fail closed with `VerificationError` messages that name the missing file and required generation command; do not partially write Phase 27 outputs after validation errors. |
| T-27-06 | Elevation of Privilege | reference demotion handoff | mitigate | Reject `demotion_allowed: true`, reject `demotion_authorization: "allowed"`, and always emit `phase27_may_authorize_demotion: false` with Phase 28 as the explicit demotion gate. |
| T-27-07 | Tampering | exception laundering of hard blockers | mitigate | Evaluate redaction, overclaim, unsafe-ref, source-ref, and lifecycle hard blockers before exception metadata; do not allow exceptions to convert hard blockers into accepted retained-code risk. |
| T-27-08 | Repudiation | schema drift from Phase 18 | mitigate | Load Phase 18 contract at runtime and exact-match packet IDs, final criteria, required fields, status vocabularies, exception fields, hard blocker reasons, and demotion policy in tests and `--contract-only`. |
</threat_model>

<verification>
Overall Phase 27 checks after all tasks:

```bash
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26
python3 tools/bazel/phase27_retained_code_acceptance_decisions_test.py
python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --contract-only
python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --security-only
python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --wiring-only
python3 tools/bazel/phase27_retained_code_acceptance_decisions.py --quick --phase26-upstream-rows build/ci-evidence/phase26/upstream-result-row-table.json --output-dir build/ci-evidence/phase27
just phase27-verify
git diff --check
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<decision_coverage_matrix>
| Decision | Plan | Task | Coverage | Notes |
|----------|------|------|----------|-------|
| D-01 | 27-01 | 1 | Full | Phase-owned wrapper around Phase 18 and Phase 26, no standalone acceptance schema. |
| D-02 | 27-01 | 1 | Full | Phase 18 remains canonical for schemas, vocabularies, criteria, exceptions, and demotion rules. |
| D-03 | 27-01 | 1 | Full | Exact coverage tests for retained packets and upstream criteria. |
| D-04 | 27-01 | 1,2 | Full | Orthogonal axes in contract and generated decision rows. |
| D-05 | 27-01 | 2 | Full | Hard blockers evaluated before exception handling. |
| D-06 | 27-01 | 2 | Full | Acceptance requires explicit maintainer decision input. |
| D-07 | 27-01 | 1,2 | Full | Phase 27 handoff keeps demotion blocked/not approved. |
| D-08 | 27-01 | 1,2 | Full | Typed exception gate uses Phase 18 exception fields. |
| D-09 | 27-01 | 1,2 | Full | Exception approvals require owner/approver, scope, rationale, refs, residual risk, mitigation, and expiry/revisit trigger. |
| D-10 | 27-01 | 2 | Full | Evidence blockers remain distinct from accepted residual risks. |
| D-11 | 27-01 | 1,2 | Full | Sensitive role policy for safety/release/signing/TLS/credential/crash-dump/hardware-adjacent surfaces. |
| D-12 | 27-01 | 2 | Full | Outputs under `build/ci-evidence/phase27`. |
| D-13 | 27-01 | 1,2 | Full | All named retained outputs generated. |
| D-14 | 27-01 | 2,3 | Full | Consumes Phase 26 rows, emits Phase 27 decision deltas and Phase 28 handoff. |
| D-15 | 27-01 | 1,2,3 | Full | Focused tests for contracts, upstream consumption, coverage, exception metadata, hard blockers, no-demotion, output writing, and wiring. |
</decision_coverage_matrix>

<success_criteria>
Phase 27 is complete when:

- `just phase27-verify` passes and runs tests before verifier execution.
- `build/ci-evidence/phase27` contains all 12 generated artifacts listed in this plan.
- `decision-row-table.json` has rows for all 10 Phase 18 retained packets and all 9 Phase 18 final criteria.
- `phase28-handoff-manifest.json` contains `phase27_may_authorize_demotion: false` and does not contain `demotion_allowed: true`.
- Security scans reject secret-bearing decision inputs and no generated output contains demotion approval overclaims.
- ACPT-02 and ACPT-03 are both represented in the plan frontmatter and proven by automated verifier/test commands.
</success_criteria>

<output>
After completion, create `.planning/phases/27-retained-code-and-maintainer-acceptance-decisions/27-01-SUMMARY.md`.
</output>
