---
phase: 09-network-web-services-and-transfers
plan: 04
type: execute
wave: 3
depends_on:
  - 09-03
files_modified:
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - BUILD.bazel
  - justfile
  - .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
autonomous: true
requirements:
  - IFCE-02
  - IFCE-03
requirements_addressed:
  - IFCE-02
  - IFCE-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 9-2026-06-14T02-15-21
generated_at: 2026-06-14T02:45:00Z
must_haves:
  truths:
    - "Developer can run `just phase9-verify` to execute Phase 9 verifier tests and the aggregate Phase 9 verifier through Bazel."
    - "Developer can query root and tools Bazel labels for `phase9_verify`, `phase9_verify_tests`, and `phase9_network_web_services_docs`."
    - "Nyquist validation records Phase 9 task IDs, automated commands, threat refs, lifecycle metadata, and final local evidence without marking live cloud, physical network, real TLS, simulator network, USB/media race, long transfer, crash dump upload, or cutover proof as locally passed."
    - "Rust pre-commit checks required by repo instructions are run before completion."
  artifacts:
    - path: "tools/bazel/BUILD.bazel"
      provides: "Bazel shell_binary labels for Phase 9 verifier and tests"
      contains: "phase9_verify"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Dispatch for Phase 9 verifier labels"
      contains: "phase9_verify)"
    - path: "BUILD.bazel"
      provides: "Root aliases and Phase 9 docs filegroup"
      contains: "phase9_network_web_services_docs"
    - path: "justfile"
      provides: "Developer facade recipe"
      contains: "phase9-verify:"
    - path: ".planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md"
      provides: "Nyquist validation sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "justfile"
      to: "//tools/bazel:phase9_verify"
      via: "`phase9-verify` recipe"
      pattern: "bazel run //tools/bazel:phase9_verify"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase9_verify.py"
      via: "case dispatch"
      pattern: "python3 tools/bazel/phase9_verify.py --all"
    - from: "BUILD.bazel"
      to: ".planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md"
      via: "phase docs filegroup"
      pattern: "phase9_network_web_services_docs"
---

<objective>
Wire Phase 9 verification through Bazel/just and complete Nyquist validation sign-off.

Purpose: Phase 9 closes with a developer-visible local gate that verifies IFCE-02 and IFCE-03 compatibility contracts while recording which cloud, network, TLS, simulator, transfer/media, crash-dump, and cutover evidence remains non-local.
Output: Bazel labels, `just phase9-verify`, passing aggregate checks, and updated `09-VALIDATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md
@.planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md
@.planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
@.planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md
@tools/bazel/phase9_verify.py
@tools/bazel/phase9_verify_test.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh
@BUILD.bazel
@justfile
@tools/bazel/manifests/phase9_connect_contracts.json
@tools/bazel/manifests/phase9_wui_contracts.json
@tools/bazel/manifests/phase9_transfer_contracts.json
@tools/bazel/manifests/phase9_network_service_contracts.json
@tools/bazel/manifests/phase9_network_concern_dispositions.json
@rust/crates/domain/src/network.rs
@rust/crates/domain/src/lib.rs

<interfaces>
From `tools/bazel/rust_workflow.sh`:
```bash
case "$command_name" in
  phase8_verify)
    python3 tools/bazel/phase8_verify.py --all
    ;;
  phase8_verify_tests)
    python3 tools/bazel/phase8_verify_test.py
    ;;
esac
```

From `justfile`:
```make
phase8-verify:
    bazel run //tools/bazel:phase8_verify_tests
    bazel run //tools/bazel:phase8_verify
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 9 verifier into Bazel and just</name>
  <files>tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, BUILD.bazel, justfile</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
    - tools/bazel/phase9_verify.py
    - tools/bazel/phase9_verify_test.py
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase9_wui_contracts.json
    - tools/bazel/manifests/phase9_transfer_contracts.json
    - tools/bazel/manifests/phase9_network_service_contracts.json
    - tools/bazel/manifests/phase9_network_concern_dispositions.json
    - rust/crates/domain/src/network.rs
    - rust/crates/domain/src/lib.rs
    - .planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md
    - .planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md
    - .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
    - .planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md
  </read_first>
  <action>Update `tools/bazel/BUILD.bazel` per D-20, D-21, and D-22 to add `shell_binary(name = "phase9_verify", src = "rust_workflow.sh", data = [...])` and `shell_binary(name = "phase9_verify_tests", src = "rust_workflow.sh", data = [...])`. The `phase9_verify` data list must include `phase9_verify.py`, all five Phase 9 manifests, `//:phase9_network_web_services_docs`, and `//:rust_workspace_sources`. The `phase9_verify_tests` data list must include `phase9_verify.py` and `phase9_verify_test.py`. Update `tools/bazel/rust_workflow.sh` with case labels `phase9_verify)` running `python3 tools/bazel/phase9_verify.py --all` and `phase9_verify_tests)` running `python3 tools/bazel/phase9_verify_test.py`; do not add live cloud, physical network, simulator, Docker metrics collector, media race, long-running transfer, crash-dump upload, or cutover execution to these dispatches. Update root `BUILD.bazel` with a `filegroup(name = "phase9_network_web_services_docs", srcs = [".planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md", ".planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md", ".planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md", ".planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md"])`, plus aliases `phase9_verify` and `phase9_verify_tests`. Update `justfile` with recipe `phase9-verify:` that runs `bazel run //tools/bazel:phase9_verify_tests` before `bazel run //tools/bazel:phase9_verify`. Keep existing Phase 2-8 recipes unchanged.</action>
  <verify>
    <automated>bazel query "//tools/bazel:phase9_verify + //tools/bazel:phase9_verify_tests + //:phase9_verify + //:phase9_verify_tests + //:phase9_network_web_services_docs"</automated>
    <automated>bazel run //tools/bazel:phase9_verify_tests</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query "//tools/bazel:phase9_verify + //tools/bazel:phase9_verify_tests + //:phase9_verify + //:phase9_verify_tests + //:phase9_network_web_services_docs"` exits 0.
    - `bazel run //tools/bazel:phase9_verify_tests` exits 0.
    - `rg 'phase9_verify|phase9_verify_tests|phase9_network_web_services_docs|phase9_connect_contracts.json|phase9_wui_contracts.json|phase9_transfer_contracts.json|phase9_network_service_contracts.json|phase9_network_concern_dispositions.json' tools/bazel/BUILD.bazel BUILD.bazel` finds every label and manifest string.
    - `rg 'phase9_verify\\)|python3 tools/bazel/phase9_verify.py --all|phase9_verify_tests\\)|python3 tools/bazel/phase9_verify_test.py' tools/bazel/rust_workflow.sh` finds both dispatch cases.
    - `rg 'phase9-verify:|bazel run //tools/bazel:phase9_verify_tests|bazel run //tools/bazel:phase9_verify' justfile` finds the facade recipe and command order.
  </acceptance_criteria>
  <done>Phase 9 verifier and tests are queryable through Bazel and runnable through the first half of `just phase9-verify`.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Phase 9 validation evidence and aggregate checks</name>
  <files>.planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
    - .planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md
    - .planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md
    - .planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md
    - tools/bazel/phase9_verify.py
    - tools/bazel/phase9_verify_test.py
    - tools/bazel/BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - BUILD.bazel
    - justfile
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase9_wui_contracts.json
    - tools/bazel/manifests/phase9_transfer_contracts.json
    - tools/bazel/manifests/phase9_network_service_contracts.json
    - tools/bazel/manifests/phase9_network_concern_dispositions.json
    - rust/crates/domain/src/network.rs
    - rust/crates/domain/src/lib.rs
  </read_first>
  <action>Update `.planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md` per D-20, D-21, D-22, IFCE-02, IFCE-03, and the security requirement after verification commands actually pass. Set frontmatter `status: complete`, keep `nyquist_compliant: true`, set `wave_0_complete: true`, keep `phase_lifecycle_id: 9-2026-06-14T02-15-21`, and keep `lifecycle_mode: yolo`. Replace the current pending task rows with exact task IDs and plan/wave mappings: `09-01-01` Plan 01 Wave 1, `09-01-02` Plan 01 Wave 1, `09-01-03` Plan 01 Wave 1, `09-02-01` Plan 02 Wave 1, `09-02-02` Plan 02 Wave 1, `09-03-01` Plan 03 Wave 2, `09-03-02` Plan 03 Wave 2, `09-04-01` Plan 04 Wave 3, and `09-04-02` Plan 04 Wave 3. Each row must list requirement IDs, threat refs, automated command, file existence, and status `green` only if the command passed. Add a `## Final Automated Evidence` section listing exact commands and outcomes for `python3 tools/bazel/phase9_verify.py --manifests-only`, `python3 tools/bazel/phase9_verify.py --rust-only`, `python3 tools/bazel/phase9_verify.py --security-only`, `python3 tools/bazel/phase9_verify_test.py`, `bazel query "//tools/bazel:phase9_verify + //tools/bazel:phase9_verify_tests + //:phase9_verify + //:phase9_verify_tests + //:phase9_network_web_services_docs"`, `bazel run //tools/bazel:phase9_verify_tests`, `bazel run //tools/bazel:phase9_verify`, `just phase9-verify`, `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`. Preserve the Manual-Only Verifications table and explicitly state that live Prusa Connect registration, live WebSocket commands, real TLS handshakes, physical Ethernet/Wi-Fi, simulator network flows, USB/media races, direct-sector media behavior, long-running stalled transfers, crash dump upload approval, and final cutover proof remain non-local evidence unless concrete run artifacts are recorded. Do not mark Docker metrics collector, live metrics service, proxy authentication, transfer concurrency redesign, broader TLS capability, auxiliary-controller network/update behavior, or Phase 11 cutover as locally passed.</action>
  <verify>
    <automated>python3 tools/bazel/phase9_verify.py --manifests-only</automated>
    <automated>python3 tools/bazel/phase9_verify.py --rust-only</automated>
    <automated>python3 tools/bazel/phase9_verify.py --security-only</automated>
    <automated>python3 tools/bazel/phase9_verify_test.py</automated>
    <automated>bazel run //tools/bazel:phase9_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase9_verify</automated>
    <automated>just phase9-verify</automated>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase9_verify.py --quick` exits 0 after validation sign-off.
    - `bazel run //tools/bazel:phase9_verify` exits 0.
    - `just phase9-verify` exits 0 and runs verifier tests before aggregate verifier.
    - `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` all exit 0.
    - `rg 'status: complete|nyquist_compliant: true|wave_0_complete: true|9-2026-06-14T02-15-21|09-01-01|09-02-02|09-03-02|09-04-02|Final Automated Evidence|just phase9-verify|cargo clippy --all-targets --all-features -- -D warnings' .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md` finds every string.
    - `rg 'manual-hardware-required|hardware-smoke|simulator-flow|Live Prusa Connect registration|USB/media race|long-running stalled network transfers|real TLS handshakes|cutover proof' .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md` still finds the non-local evidence wording.
  </acceptance_criteria>
  <done>Phase 9 Nyquist validation is complete, local aggregate verification passes, Rust pre-commit checks pass, and non-local network/TLS/transfer evidence remains explicitly classified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel/just facade -> verifier scripts | Developer commands must call the correct Phase 9 verifier and tests, not stale phase labels. |
| Bazel data graph -> Phase 9 manifests/docs | All manifests, lifecycle docs, validation docs, and Rust workspace sources must be in verifier runfiles. |
| Aggregate verification -> evidence record | Validation status must be updated only from commands that actually ran and passed. |
| Local checks -> non-local proof | Passing local gates must not be recorded as live cloud, physical network, real TLS, simulator, media-race, long-running transfer, crash-dump upload, or cutover proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | Bazel label wiring | mitigate | Query exact root and tools labels and require Phase 9 manifests/docs/Rust sources in data dependencies. |
| T-09-04-02 | Repudiation | `just phase9-verify` facade | mitigate | Require recipe to run verifier tests before aggregate verifier and validate through `just phase9-verify`. |
| T-09-04-03 | Repudiation | Validation sign-off | mitigate | `09-VALIDATION.md` must list exact command outcomes, task IDs, threat refs, and lifecycle metadata before setting `status: complete`. |
| T-09-04-04 | Repudiation | Non-local evidence classification | mitigate | Preserve manual/simulator/hardware evidence rows and forbid local proof claims for live cloud/network/TLS/media/transfer/crash-dump/cutover flows. |
| T-09-04-05 | Information Disclosure | Aggregate validation artifacts | mitigate | The Phase 9 verifier runs secret marker checks before local validation can be marked green. |
</threat_model>

<verification>
- `bazel query "//tools/bazel:phase9_verify + //tools/bazel:phase9_verify_tests + //:phase9_verify + //:phase9_verify_tests + //:phase9_network_web_services_docs"`
- `bazel run //tools/bazel:phase9_verify_tests`
- `bazel run //tools/bazel:phase9_verify`
- `just phase9-verify`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
</verification>

<success_criteria>
Plan 09-04 succeeds when Phase 9 verifier labels are queryable, `just phase9-verify` passes as a deterministic static verifier/test/Rust gate, Rust pre-commit checks pass, `09-VALIDATION.md` is Nyquist-compliant and lifecycle-tagged, and non-local network/TLS/transfer/cloud/media/cutover evidence remains explicitly classified.
</success_criteria>

<output>
After completion, create `.planning/phases/09-network-web-services-and-transfers/09-04-SUMMARY.md`.
</output>
