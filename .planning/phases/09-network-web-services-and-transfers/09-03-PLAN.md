---
phase: 09-network-web-services-and-transfers
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - tools/bazel/phase9_verify.py
  - tools/bazel/phase9_verify_test.py
autonomous: true
requirements:
  - IFCE-02
  - IFCE-03
requirements_addressed:
  - IFCE-02
  - IFCE-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 9-2026-06-14T02-15-21
generated_at: 2026-06-14T02:45:00Z
must_haves:
  truths:
    - "Developer can run Phase 9 verifier regression tests that prove missing manifests, source paths, Rust API strings, redaction, non-local proof, concern dispositions, lifecycle, and overclaim failures are rejected."
    - "Developer can run a deterministic Phase 9 static verifier that checks all five manifests and pure Rust domain surfaces before Bazel/just wiring is added."
    - "The verifier blocks local green claims for live cloud, real TLS, physical network, simulator network, USB/media race, long-running transfer, raw crash dump, and final cutover evidence."
  artifacts:
    - path: "tools/bazel/phase9_verify.py"
      provides: "Static Phase 9 verifier"
      exports: ["--quick", "--all", "--manifests-only", "--rust-only", "--security-only"]
    - path: "tools/bazel/phase9_verify_test.py"
      provides: "Verifier regression tests for required Phase 9 failure modes"
      contains: "Phase9VerifierTest"
  key_links:
    - from: "tools/bazel/phase9_verify.py"
      to: "tools/bazel/manifests/phase9_connect_contracts.json"
      via: "manifest schema, source-path, redaction, and row ID checks"
      pattern: "phase9_connect_contracts.json"
    - from: "tools/bazel/phase9_verify.py"
      to: "rust/crates/domain/src/network.rs"
      via: "Rust API string and unsafe-free checks"
      pattern: "NetworkEvidenceClass|TransferRange|NetworkServiceContract"
    - from: "tools/bazel/phase9_verify.py"
      to: "tools/bazel/manifests/phase9_network_concern_dispositions.json"
      via: "required concern and regression guard checks"
      pattern: "concern-phase9-custom-der-cert-read"
---

<objective>
Add Phase 9 verifier regression tests and the static verifier implementation for manifests, Rust API shape, redaction, evidence boundaries, lifecycle metadata, and overclaim wording.

Purpose: Make IFCE-02/IFCE-03 local evidence executable and self-tested before the final Bazel/just validation facade is wired.
Output: `tools/bazel/phase9_verify_test.py` and `tools/bazel/phase9_verify.py`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md
@.planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md
@.planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
@.planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md
@tools/bazel/phase8_verify.py
@tools/bazel/phase8_verify_test.py
@tools/bazel/manifests/phase9_connect_contracts.json
@tools/bazel/manifests/phase9_wui_contracts.json
@tools/bazel/manifests/phase9_transfer_contracts.json
@tools/bazel/manifests/phase9_network_service_contracts.json
@tools/bazel/manifests/phase9_network_concern_dispositions.json
@rust/crates/domain/src/network.rs
@rust/crates/domain/src/lib.rs

<interfaces>
From `tools/bazel/phase8_verify.py`:
```python
ALLOWED_EVIDENCE_CLASSES = {
    "manifest-check",
    "source-audit",
    "static-source-audit",
    "host-test",
    "rust-host-test",
    "simulator-flow",
    "hardware-smoke",
    "manual-hardware-required",
}
NON_LOCAL_EVIDENCE_CLASSES = {
    "simulator-flow",
    "hardware-smoke",
    "manual-hardware-required",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Phase 9 verifier regression tests</name>
  <files>tools/bazel/phase9_verify_test.py</files>
  <read_first>
    - tools/bazel/phase9_verify_test.py
    - tools/bazel/phase8_verify_test.py
    - tools/bazel/phase8_verify.py
    - .planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md
    - .planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md
    - .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
    - .planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase9_wui_contracts.json
    - tools/bazel/manifests/phase9_transfer_contracts.json
    - tools/bazel/manifests/phase9_network_service_contracts.json
    - tools/bazel/manifests/phase9_network_concern_dispositions.json
    - rust/crates/domain/src/network.rs
    - rust/crates/domain/src/lib.rs
  </read_first>
  <behavior>
    - Test 1: Missing any one Phase 9 manifest fails `--quick`.
    - Test 2: Missing required Connect row IDs `connect-registration-token-fingerprint`, `connect-tls-required-verification-policy`, or `connect-proxy-minimal-limitations` fails `--quick`.
    - Test 3: Missing WUI rows `wui-server-resource-model`, `wui-digest-auth-nonce-stale`, or `wui-api-key-auth` fails `--quick`.
    - Test 4: Missing transfer rows `transfer-single-active-slot`, `transfer-encrypted-aes-ctr-payload`, or `transfer-media-race-non-local` fails `--quick`.
    - Test 5: Missing network service rows `metrics-line-protocol-throttling`, `syslog-udp-destination`, or `network-feature-gates-wui-connect` fails `--quick`.
    - Test 6: Missing concern rows `concern-phase9-custom-der-cert-read`, `concern-phase9-weak-digest-modules`, `concern-phase9-proxy-limitations`, or `concern-phase9-crash-dump-upload-boundary` fails `--quick`.
    - Test 7: Secret value fields or markers `token_value`, `password_value`, `wifi_password`, `certificate_bytes`, `private_key`, `BEGIN PRIVATE KEY`, `raw_crash_dump`, or `crash_dump_payload` fail `--quick`.
    - Test 8: `proof_scope: "local"` paired with `simulator-flow`, `hardware-smoke`, `manual-hardware-required`, live cloud, real TLS, physical network, USB/media race, long-running transfer, or cutover wording fails `--quick`.
    - Test 9: Missing Rust API strings `NetworkEvidenceClass`, `NetworkParityRowId`, `SecretHandling`, `ConnectCommandState`, `ProxyMode`, `WuiAuthMode`, `TransferSlotState`, `TransferRange`, `EncryptedPayloadMetadata`, `NetworkServiceContract`, and `NetworkParityContract` fails `--quick`.
    - Test 10: Missing Bazel labels, root aliases, `rust_workflow.sh` dispatch, or `just phase9-verify` fails `--quick`.
    - Test 11: Missing validation lifecycle ID `9-2026-06-14T02-15-21` or missing `nyquist_compliant: true` fails `--quick`.
  </behavior>
  <action>Create `tools/bazel/phase9_verify_test.py` using the Phase 8 temp-root unittest pattern. Name the test class `Phase9VerifierTest`. Copy the real verifier into a temp root when it exists, write minimal valid Phase 9 manifest/Rust/Bazel/just/validation fixtures in helper methods, then mutate one concern per test. Include tests named `test_requires_all_phase9_manifests`, `test_requires_connect_tls_proxy_rows`, `test_requires_wui_auth_and_resource_rows`, `test_requires_transfer_single_slot_and_media_rows`, `test_requires_network_service_rows`, `test_requires_phase9_concern_rows`, `test_rejects_secret_value_markers`, `test_rejects_non_local_evidence_overclaims`, `test_requires_network_rust_api_surface`, `test_requires_bazel_and_just_wiring`, and `test_requires_validation_lifecycle_contract`. The RED run should fail until Task 2 creates `tools/bazel/phase9_verify.py`.</action>
  <verify>
    <automated>python3 tools/bazel/phase9_verify_test.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'class Phase9VerifierTest|test_requires_all_phase9_manifests|test_requires_connect_tls_proxy_rows|test_requires_wui_auth_and_resource_rows|test_requires_transfer_single_slot_and_media_rows|test_requires_network_service_rows|test_requires_phase9_concern_rows|test_rejects_secret_value_markers|test_rejects_non_local_evidence_overclaims|test_requires_network_rust_api_surface|test_requires_bazel_and_just_wiring|test_requires_validation_lifecycle_contract' tools/bazel/phase9_verify_test.py` finds all required tests.
    - `python3 tools/bazel/phase9_verify_test.py` fails before Task 2 because `tools/bazel/phase9_verify.py` is missing or incomplete.
  </acceptance_criteria>
  <done>Verifier regression tests define the Phase 9 aggregate contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 9 static verifier</name>
  <files>tools/bazel/phase9_verify.py, tools/bazel/phase9_verify_test.py</files>
  <read_first>
    - tools/bazel/phase9_verify.py
    - tools/bazel/phase9_verify_test.py
    - tools/bazel/phase8_verify.py
    - tools/bazel/phase8_verify_test.py
    - .planning/phases/09-network-web-services-and-transfers/09-CONTEXT.md
    - .planning/phases/09-network-web-services-and-transfers/09-RESEARCH.md
    - .planning/phases/09-network-web-services-and-transfers/09-VALIDATION.md
    - .planning/phases/09-network-web-services-and-transfers/09-UI-SPEC.md
    - tools/bazel/manifests/phase9_connect_contracts.json
    - tools/bazel/manifests/phase9_wui_contracts.json
    - tools/bazel/manifests/phase9_transfer_contracts.json
    - tools/bazel/manifests/phase9_network_service_contracts.json
    - tools/bazel/manifests/phase9_network_concern_dispositions.json
    - rust/crates/domain/src/network.rs
    - rust/crates/domain/src/lib.rs
  </read_first>
  <action>Implement `tools/bazel/phase9_verify.py` per D-20, D-21, D-22, IFCE-02, and IFCE-03 using Python standard library only. Define `PHASE = "09-network-web-services-and-transfers"` and `PHASE_LIFECYCLE_ID = "9-2026-06-14T02-15-21"`. Define manifest constants for `phase9_connect_contracts.json`, `phase9_wui_contracts.json`, `phase9_transfer_contracts.json`, `phase9_network_service_contracts.json`, and `phase9_network_concern_dispositions.json`; define `VALIDATION_CONTRACT`, `NETWORK_RUST`, and `RUST_DOMAIN_LIB`. Support `--quick`, `--all`, `--manifests-only`, `--rust-only`, and `--security-only`; default to `--quick`. `--quick` must run manifest schema, canonical `requirement_id` / `reference_sources`, lifecycle, evidence class, proof scope, source-path existence, secret marker, concern disposition, Rust API, unsafe-free network module, validation contract, Bazel/just surface, and overclaim checks. `--all` must run `--quick` plus `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.

Required row ID constants are exactly: Connect rows `connect-registration-token-fingerprint`, `connect-config-host-token-proxy-tls`, `connect-telemetry-events`, `connect-command-polling-websocket`, `connect-host-decompression-connection-reuse`, `connect-tls-required-verification-policy`, `connect-proxy-minimal-limitations`, `connect-transfer-download-integration`, and `connect-sleep-backoff-shared-buffer-limits`; WUI rows `wui-server-resource-model`, `wui-static-assets`, `prusalink-api-v1-status-job-files-transfer`, `octoprint-compatible-api`, `wui-digest-auth-nonce-stale`, `wui-api-key-auth`, `wui-usb-file-storage-paths`, `wui-upload-transfer-renderer`, `wui-unknown-request-error`, and `wui-responsive-static-ui-contract`; transfer rows `transfer-single-active-slot`, `transfer-connect-command-initiation`, `transfer-wui-upload-api-integration`, `transfer-range-request`, `transfer-encrypted-aes-ctr-payload`, `transfer-partial-file-direct-sector`, `transfer-recovery-and-changed-path`, `transfer-error-outcome-mapping`, and `transfer-media-race-non-local`; network service rows `sntp-client-default-server`, `mdns-optional-announcement`, `dns-lwip-network-resolution`, `metrics-runtime-config-udp`, `metrics-line-protocol-throttling`, `syslog-udp-destination`, and `network-feature-gates-wui-connect`; concern rows `concern-phase9-custom-der-cert-read`, `concern-phase9-weak-digest-modules`, `concern-phase9-proxy-limitations`, `concern-phase9-stale-connect-module-tests`, `concern-phase9-whole-response-shared-buffers`, `concern-phase9-transfer-media-races`, `concern-phase9-transfer-monitor-lock-order`, `concern-phase9-crash-dump-upload-boundary`, and `concern-phase9-network-tls-coverage-gaps`.

Allowed evidence classes are exactly `manifest-check`, `source-audit`, `static-source-audit`, `host-test`, `rust-host-test`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`; reject `proof_scope: "local"` for simulator/hardware/manual evidence. Reject forbidden marker strings `token_value`, `password_value`, `wifi_password`, `certificate_bytes`, `private_key`, `BEGIN PRIVATE KEY`, `raw_crash_dump`, and `crash_dump_payload` in Phase 9 manifests and validation. Require the exact Rust API strings from Task 1, plus `NetworkServiceContractInput` and `NetworkParityContractInput`, in `network.rs`, and require `pub mod network;` plus exports in `lib.rs`. Strip Rust comments and string literals before unsafe syntax checks, following Phase 8. Reject overclaim strings `cloud verified locally`, `live Connect passed`, `real TLS handshake passed`, `physical Wi-Fi verified locally`, `physical Ethernet verified locally`, `USB media race passed locally`, `long-running transfer verified locally`, `simulator network flow passed locally`, `raw crash dump upload approved`, and `cutover evidence complete`. Update Task 1 tests if temp fixtures need exact schema alignment, but do not weaken failure cases. It is acceptable for `python3 tools/bazel/phase9_verify.py --quick` against the real repo to fail until Plan 09-04 adds Bazel/just wiring and final validation sign-off; direct manifest/Rust/security modes must pass after Plans 09-01 and 09-02 are complete.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase9_verify.py tools/bazel/phase9_verify_test.py</automated>
    <automated>python3 tools/bazel/phase9_verify_test.py</automated>
    <automated>python3 tools/bazel/phase9_verify.py --manifests-only</automated>
    <automated>python3 tools/bazel/phase9_verify.py --rust-only</automated>
    <automated>python3 tools/bazel/phase9_verify.py --security-only</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'PHASE_LIFECYCLE_ID = "9-2026-06-14T02-15-21"|check_connect_manifest|check_wui_manifest|check_transfer_manifest|check_network_services_manifest|check_concern_dispositions|check_network_rust_api_surface|check_no_phase9_overclaim|check_bazel_surface|check_just_surface|check_validation_contract' tools/bazel/phase9_verify.py` finds all required verifier checks.
    - `rg 'phase9_connect_contracts.json|phase9_wui_contracts.json|phase9_transfer_contracts.json|phase9_network_service_contracts.json|phase9_network_concern_dispositions.json' tools/bazel/phase9_verify.py` finds all five manifest paths.
    - `rg 'NetworkEvidenceClass|NetworkParityRowId|SecretHandling|ConnectCommandState|ProxyMode|WuiAuthMode|TransferSlotState|TransferRange|EncryptedPayloadMetadata|NetworkServiceContract|NetworkParityContract' tools/bazel/phase9_verify.py` finds required Rust API strings.
    - `rg 'token_value|password_value|wifi_password|certificate_bytes|private_key|BEGIN PRIVATE KEY|raw_crash_dump|crash_dump_payload|cloud verified locally|real TLS handshake passed|USB media race passed locally|cutover evidence complete' tools/bazel/phase9_verify.py` finds the forbidden marker and overclaim guard strings.
    - `python3 tools/bazel/phase9_verify_test.py` passes after implementation.
    - `python3 tools/bazel/phase9_verify.py --manifests-only`, `--rust-only`, and `--security-only` pass in the real repo after Plans 09-01 and 09-02 are complete.
  </acceptance_criteria>
  <done>The direct Python verifier and regression suite enforce Phase 9 manifest, Rust, lifecycle, redaction, concern, evidence, and overclaim contracts before final facade wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest files -> verifier | JSON rows are untrusted until schema, lifecycle, source-path, redaction, evidence, and required-row checks pass. |
| Rust domain source -> verifier | Verifier checks exact pure-domain API strings and unsafe-free posture without executing runtime networking code. |
| Local verifier -> evidence claims | Passing static checks must not be recorded as live cloud, physical network, real TLS, USB/media race, simulator, long-transfer, crash-dump, or cutover proof. |
| Bazel/just surface -> aggregate verifier | Final quick verification should fail until Plan 09-04 exposes the correct labels and facade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | Phase 9 manifest schemas | mitigate | Required row IDs, canonical fields, lifecycle metadata, and source-path existence checks reject missing or drifted rows. |
| T-09-03-02 | Information Disclosure | Redaction scanner | mitigate | Verifier rejects token/password/Wi-Fi/certificate/private-key/raw-crash-dump value markers across manifests and validation. |
| T-09-03-03 | Repudiation | Non-local evidence classification | mitigate | Verifier rejects local proof scope for simulator/hardware/manual evidence and rejects overclaim phrases. |
| T-09-03-04 | Elevation of Privilege | Rust API surface | mitigate | Verifier requires typed network/transfer/service contracts and scans `network.rs` for unsafe syntax after stripping comments/strings. |
| T-09-03-05 | Spoofing | TLS/proxy concern dispositions | mitigate | Required concern rows and regression guard strings block green Phase 9 claims without TLS/proxy/custom-cert disposition. |
</threat_model>

<verification>
- `python3 -m py_compile tools/bazel/phase9_verify.py tools/bazel/phase9_verify_test.py`
- `python3 tools/bazel/phase9_verify_test.py`
- `python3 tools/bazel/phase9_verify.py --manifests-only`
- `python3 tools/bazel/phase9_verify.py --rust-only`
- `python3 tools/bazel/phase9_verify.py --security-only`
</verification>

<success_criteria>
Plan 09-03 succeeds when verifier regression tests pass, direct manifest/Rust/security checks pass, the verifier carries all required row IDs and guard strings, secret values are blocked, non-local evidence overclaims are blocked, and quick verification is ready for Plan 09-04 Bazel/just/validation wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/09-network-web-services-and-transfers/09-03-SUMMARY.md`.
</output>
