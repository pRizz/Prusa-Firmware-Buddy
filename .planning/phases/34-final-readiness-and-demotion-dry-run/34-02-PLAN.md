---
phase: 34-final-readiness-and-demotion-dry-run
plan: "02"
type: execute
wave: 2
depends_on:
  - "34-01"
gap_closure: true
files_modified:
  - tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
  - tools/bazel/phase34_final_readiness_demotion_dry_run.py
  - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
autonomous: true
requirements:
  - READY-02
  - READY-03
must_haves:
  truths:
    - "Phase 34 derives the complete required evidence-stream set from the Phase 31 contract, independently of which accepted receipts were submitted."
    - "Every absent required Phase 31 stream creates an explicit blocked ledger row with required-row-missing."
    - "The demotion dry run cannot open when any required evidence stream is absent, even with otherwise valid readiness and approval inputs."
    - "The isolated open fixture supplies all Phase 31 required streams."
  artifacts:
    - path: "tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json"
      provides: "Contract-driven required-stream completeness policy"
      contains: "required_streams_from"
    - path: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      provides: "Required-stream derivation and missing-stream ledger rows"
      exports: ["main"]
    - path: "tools/bazel/phase34_final_readiness_demotion_dry_run_test.py"
      provides: "Partial-intake fail-closed and complete-open regression coverage"
      contains: "required_stream"
  key_links:
    - from: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      to: "tools/bazel/manifests/phase31_final_evidence_intake_contract.json"
      via: "stream_adapters required-set derivation"
      pattern: "stream_adapters"
    - from: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      to: "readiness_state"
      via: "synthetic missing-stream ledger rows"
      pattern: "required-row-missing"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 34-2026-07-25T18-18-48
generated_at: 2026-07-25T20:00:00Z
---

<objective>
Close the Phase 34 verification gap that allows a partial Phase 31 intake to be treated as complete.

Purpose: READY-02 and READY-03 require completeness to be measured against all four contract-required evidence streams, not merely the accepted receipts that happen to be present.
Output: contract-driven missing-stream ledger rows, complete open fixtures, and regression tests proving partial intake remains blocked.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-CONTEXT.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-01-PLAN.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-01-SUMMARY.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-VERIFICATION.md
@tools/bazel/manifests/phase31_final_evidence_intake_contract.json
@tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
@tools/bazel/phase34_final_readiness_demotion_dry_run.py
@tools/bazel/phase34_final_readiness_demotion_dry_run_test.py

<interfaces>
Required stream specification:
```text
stream, requirement_ids, expected_source_ref, expected_gate
```

Missing stream ledger semantics:
```text
row_problem_kind: missing
coverage_state: required-row-missing
proof_eligibility: ineligible
readiness_effect: blocked
reason_codes: [required-row-missing]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contract-driven required-stream completeness</name>
  <files>tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json, tools/bazel/phase34_final_readiness_demotion_dry_run.py</files>
  <action>
    Extend the Phase 34 sparse-overlay policy with an explicit Phase 31 `stream_adapters` required-set source and an absent-stream state of `required-row-missing`.
    Load and validate the Phase 31 contract identity/lifecycle, derive one required-stream specification per adapter, and reject duplicate, unknown, or malformed adapter entries.
    When evaluating coverage, synthesize a deterministic missing ledger row for every required stream that has no accepted receipt. Use the adapter's upstream row or upstream row-table path as its safe expected reference, and keep the existing receipt-derived rows unchanged.
    Missing-stream rows must block directly with `required-row-missing`; they must not require a Phase 32 classification merely to become visible.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --contract-only</automated>
  </verify>
  <done>The pure ledger receives contract-derived required streams and represents every absent stream as an explicit blocked row.</done>
</task>

<task type="auto">
  <name>Task 2: Add partial-intake and complete-open regressions</name>
  <files>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</files>
  <action>
    Add a helper that builds accepted receipts for all four Phase 31 contract streams.
    Change the isolated open-gate fixture to provide the complete required stream set while retaining its exact exception/readiness/demotion authorization scenario.
    Add a regression that removes each required stream in turn while keeping readiness and demotion inputs otherwise valid, then assert readiness and gate remain blocked and the missing stream row carries `required-row-missing`.
    Keep tests Arrange/Act/Assert and preserve existing focused path, security, projection, and sparse-overlay cases.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q</automated>
  </verify>
  <done>The only open fixture is complete and every partial-intake variant is proven fail-closed.</done>
</task>

</tasks>

<threat-model>
## Blocking Threats

| Threat ID | Severity | Boundary | Mitigation |
| --- | --- | --- | --- |
| T-34-08 | critical | Phase 31 required-set completeness | Derive the required set from the validated Phase 31 contract, not from caller-supplied receipts. |
| T-34-09 | high | Missing-stream projection | Emit deterministic, secret-free missing rows that always block and cannot be exception-covered without evidence. |
</threat-model>

<verification>
```bash
python3 -m py_compile tools/bazel/phase34_final_readiness_demotion_dry_run.py tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q
python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --contract-only
python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --security-only
python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --wiring-only
bazel run //tools/bazel:phase34_verify_tests
bazel run //tools/bazel:phase34_verify
just phase34-verify
git diff --check
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- Partial accepted-final intake cannot yield unblocked readiness or an open demotion gate.
- Each absent required stream is visible as a deterministic `required-row-missing` ledger row.
- The isolated open fixture covers all four required Phase 31 streams.
- READY-02 and READY-03 re-verification passes without override or human approval.
</success_criteria>

<output>
After completion, create `.planning/phases/34-final-readiness-and-demotion-dry-run/34-02-SUMMARY.md`.
</output>
