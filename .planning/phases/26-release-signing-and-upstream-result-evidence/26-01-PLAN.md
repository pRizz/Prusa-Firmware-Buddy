---
phase: 26-release-signing-and-upstream-result-evidence
plan: "01"
type: execute
wave: 1
depends_on:
  - "25"
files_modified:
  - tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json
  - tools/bazel/phase26_release_signing_upstream_evidence.py
  - tools/bazel/phase26_release_signing_upstream_evidence_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - EVID-04
  - ACPT-01
requirements_addressed:
  - EVID-04
  - ACPT-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-06-24T13-36-46
generated_at: 2026-06-24T13:56:09Z
must_haves:
  truths:
    - "Release manager can supply sanitized release/signing/provenance metadata for every canonical Phase 20 release row without retaining private keys, credentials, raw payloads, or binary dumps."
    - "Passed Phase 26 release evidence requires artifact digests, build input identity, signing identity references where required, provenance, comparison, retention, verification outcome, operator, timestamp, and release run identity."
    - "Maintainer can inspect one normalized upstream row for every canonical Phase 18 cutover criterion, including simulator, hardware/media/safety, live-service, release/signing, retained-code, residual-risk, final-readiness, and reference-demotion rows."
    - "Each upstream row names Phase 26 requirement IDs, source requirement IDs, owning phase or gate, evidence refs, artifact refs, lifecycle/source-ref/redaction state, exception state, maintainer state, failure reason, and generated timestamp."
    - "Missing, stale, lifecycle-mismatched, source-ref-invalid, failed, blocked, secret-tainted, schema-invalid, or overclaiming rows remain blocked unless the source contract explicitly allows exception coverage."
    - "Quick verification produces blocked or pending review rows from safe checked-in fixtures and never claims retained-code acceptance, final readiness approval, or reference demotion."
  artifacts:
    - path: "tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json"
      provides: "Phase 26 release and upstream-row execution contract"
      contains: "phase26_release_signing_upstream_evidence_contract"
    - path: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      provides: "Phase 26 release evidence validator, upstream row normalizer, secret guard, and retained output writer"
      contains: "DEFAULT_OUTPUT_DIR = Path(\"build/ci-evidence/phase26\")"
    - path: "tools/bazel/phase26_release_signing_upstream_evidence_test.py"
      provides: "Regression coverage for EVID-04, ACPT-01, and security blockers"
      contains: "class Phase26ReleaseSigningUpstreamEvidenceTest"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 26 Bazel shell targets"
      contains: "phase26_verify"
    - path: "BUILD.bazel"
      provides: "Root Phase 26 docs filegroup and aliases"
      contains: "phase26_release_signing_upstream_evidence_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Phase 26 workflow dispatch"
      contains: "phase26_verify)"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase26-verify:"
  key_links:
    - from: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      to: "tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json"
      via: "canonical Phase 20 release row loading"
      pattern: "phase20_release_candidate_artifacts_contract"
    - from: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      to: "tools/bazel/manifests/phase18_cutover_review_contract.json"
      via: "canonical upstream criterion loading"
      pattern: "phase18_cutover_review_contract"
    - from: "tools/bazel/phase26_release_signing_upstream_evidence.py"
      to: "build/ci-evidence/phase26"
      via: "retained output writer"
      pattern: "build/ci-evidence/phase26"
    - from: "justfile"
      to: "//tools/bazel:phase26_verify"
      via: "phase26-verify recipe"
      pattern: "bazel run //tools/bazel:phase26_verify"
---

<objective>
Create the Phase 26 release/signing/provenance and upstream-result evidence gate.

Purpose: Release managers need a secret-safe input path for real release-environment evidence, and maintainers need normalized upstream result rows for every required cutover gate before Phase 27 and Phase 28 decision work can proceed.

Output: A Phase 26 contract manifest, Python verifier, unit/security/wiring tests, retained output writer under `build/ci-evidence/phase26`, Bazel labels, `rust_workflow.sh` dispatch, and `just phase26-verify` facade.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-release-signing-and-upstream-result-evidence/26-CONTEXT.md
@.planning/phases/26-release-signing-and-upstream-result-evidence/26-RESEARCH.md
@.planning/phases/26-release-signing-and-upstream-result-evidence/26-VALIDATION.md
@.planning/phases/23-simulator-evidence-execution/23-01-SUMMARY.md
@.planning/phases/24-hardware-media-and-safety-evidence-execution/24-01-SUMMARY.md
@.planning/phases/25-live-service-evidence-execution/25-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@tools/bazel/manifests/phase17_release_candidate_evidence_contract.json
@tools/bazel/manifests/phase18_cutover_review_contract.json
@tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json
@tools/bazel/manifests/phase20_release_environment_inputs.template.json
@tools/bazel/phase18_cutover_review.py
@tools/bazel/phase18_cutover_review_test.py
@tools/bazel/phase20_release_candidate_artifacts.py
@tools/bazel/phase20_release_candidate_artifacts_test.py
@tools/bazel/phase23_simulator_evidence_execution.py
@tools/bazel/phase24_hardware_media_safety_evidence_execution.py
@tools/bazel/phase25_live_service_evidence_execution.py

<interfaces>
Canonical Phase 20 release row IDs from `tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json`:
`rel-bin-firmware-image`, `rel-bbf-firmware-package`, `rel-dfu-update-package`, `rel-map-and-provenance`, `rel-resource-image-package`, `rel-language-bundles`, `rel-wui-assets`, `rel-esp-packages`, `rel-mmu-package`, `rel-auxiliary-dwarf-firmware`, `rel-auxiliary-modularbed-firmware`, `rel-auxiliary-xbuddy-extension-firmware`, `rel-package-manifests`, `rel-signing-key-identity`, `rel-build-input-identity`, `rel-artifact-retention`, `rel-reference-comparison-report`, `rel-contract-traceability-redaction-boundary`.

Phase 26 pass-capable proof classes per D-06: `approved-release-run`, `external-release-key-evidence`.
Phase 26 non-pass proof classes per D-06: `template-only`, `local-smoke`, `pending-release-input`, `release-run-required`, `external-signing-required`, `blocked-signing-key-unavailable`, and Phase 20's older `release-candidate`.

Canonical Phase 18 upstream criteria from `tools/bazel/manifests/phase18_cutover_review_contract.json`:
`final-ci-evidence`, `final-simulator-evidence`, `final-hardware-safety-media-evidence`, `final-live-network-transfer-evidence`, `final-release-artifact-signing-evidence`, `final-retained-code-acceptance`, `final-residual-risk-review`, `final-maintainer-decision`, `final-reference-demotion-allowed`.

Required Phase 26 upstream row keys per D-11:
`criterion_id`, `evidence_family`, `requirement_ids`, `source_requirement_ids`, `owning_phase`, `source_lifecycle_id`, `source_lifecycle_status`, `evidence_refs`, `artifact_refs`, `status`, `failure_reason`, `redaction_status`, `source_ref_status`, `exception_status`, `maintainer_state`, `generated_at_utc`.
</interfaces>
</context>

<commit_verification>
Before every task commit and before any final GSD/docs commit in this Rust workspace, run the root required pre-commit sequence in this exact order:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```

Do not create RED or otherwise intentionally failing commits. If a task writes tests first, keep the failing-test state uncommitted and commit only after implementation plus all required Rust checks and Phase 26 checks pass.
</commit_verification>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 26 contract and release evidence validator</name>
  <files>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json, tools/bazel/phase26_release_signing_upstream_evidence.py, tools/bazel/phase26_release_signing_upstream_evidence_test.py</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/core/verification.md</file>
    <file>.planning/phases/26-release-signing-and-upstream-result-evidence/26-CONTEXT.md</file>
    <file>.planning/phases/26-release-signing-and-upstream-result-evidence/26-RESEARCH.md</file>
    <file>.planning/phases/26-release-signing-and-upstream-result-evidence/26-VALIDATION.md</file>
    <file>tools/bazel/manifests/phase17_release_candidate_evidence_contract.json</file>
    <file>tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json</file>
    <file>tools/bazel/manifests/phase20_release_environment_inputs.template.json</file>
    <file>tools/bazel/phase20_release_candidate_artifacts.py</file>
    <file>tools/bazel/phase20_release_candidate_artifacts_test.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json if present</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py if present</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py if present</file>
  </read_first>
  <behavior>
    - Test release input coverage: exact Phase 20 `.rows[].id` set is required; missing, duplicate, unknown, or drifted IDs fail closed.
    - Test pass metadata: passed rows require `release_run_id`, `artifact_refs`, `operator`, `timestamp`, `subject_digests`, `build_input_identity`, `retention_refs`, `verification_outcome`, `mismatch_class`, `mismatch_reason`, `owner_phase`, `affected_artifact_surface`, and `residual_risk`; rows with Phase 20 `signing_metadata_required` also require `key_identity_ref` and `signing_mode`.
    - Test proof-class policy: `approved-release-run` and `external-release-key-evidence` can pass; `release-candidate`, `local-smoke`, and `template-only` cannot pass in Phase 26.
    - Test secret rejection before retained writes: private-key markers, raw signing payload bytes, raw firmware payloads, raw logs, token fields, password fields, credential fields, secret fields, and private certificate material abort before `build/ci-evidence/phase26` is created or rewritten.
    - Test output-root containment: absolute paths, `..`, and symlink escapes are rejected before deletion or write operations.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json` with `schema_version: "1"`, `id: "phase26_release_signing_upstream_evidence_contract"`, `artifact_name: "phase26-release-signing-upstream-evidence"`, `phase: "26-release-signing-and-upstream-result-evidence"`, `phase_lifecycle_id: "26-2026-06-24T13-36-46"`, `output_root: "build/ci-evidence/phase26"`, `source_contracts` entries for Phase 17, Phase 20 contract, Phase 20 input template, and Phase 18, and `release_policy` containing the 18 canonical Phase 20 row IDs above. This implements D-01, D-02, D-04, and D-14 without redefining Phase 17 or Phase 20 row IDs.

    Create `tools/bazel/phase26_release_signing_upstream_evidence.py` as a standard-library Python verifier using a functional core and thin CLI/file I/O shell. Add CLI flags `--contract-only`, `--security-only`, `--wiring-only`, `--quick`, `--release-input PATH`, and `--output-dir PATH`. Load Phase 20 rows from `tools/bazel/manifests/phase20_release_candidate_artifacts_contract.json` instead of copying row catalogs into code; load the release input packet from `--release-input` or default to the Phase 20 template for quick-mode blocked rows.

    Validate release evidence as sanitized metadata and artifact references only per D-03 and D-05. Accept `key_identity_ref`, `signing_mode`, signing authority refs, certificate chain refs, artifact refs, digest refs, provenance refs, comparison refs, retention refs, operator, timestamp, and release run identity. Reject raw firmware payloads, signing payload bytes, private keys, private certificates, credentials, raw logs, binary dumps, tokens, passwords, and secret fields. Apply this scan to raw input text and parsed JSON before creating, deleting, or writing any retained output per D-07.

    Add release status normalization per D-06: `approved-release-run` and `external-release-key-evidence` may produce `passed` when all required metadata is present; `template-only`, `local-smoke`, `pending-release-input`, `release-run-required`, `external-signing-required`, `blocked-signing-key-unavailable`, and `release-candidate` produce blocked or pending statuses unless explicit exception metadata is present and the source contract allows that exception. Do not use release proof to accept simulator, hardware/media/safety, live-service, retained-code, residual-risk, final-readiness, or demotion rows per D-08 and D-13.

    Add `tools/bazel/phase26_release_signing_upstream_evidence_test.py` with `unittest` tests for the release row coverage, pass metadata, proof-class, secret guard, output-root containment, and contract shape behaviors above. Use explicit Arrange, Act, Assert comments for non-trivial tests. This starts D-18 coverage for release row coverage, status/proof-class normalization, signing identity redaction, artifact digest requirements, provenance/comparison/retention metadata, secret guards, retained output safety, and wiring checks; Task 2 extends D-18 to upstream row schema and blocker semantics.
  </action>
  <acceptance_criteria>
    - `tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json` exists and contains `phase26_release_signing_upstream_evidence_contract`, `build/ci-evidence/phase26`, all 18 canonical Phase 20 release IDs, `approved-release-run`, and `external-release-key-evidence`.
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --contract-only` exits 0.
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --security-only` exits 0.
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py` exits 0 with tests covering missing release rows, duplicate release rows, unknown release rows, `release-candidate` non-pass policy, secret-tainted input, and symlink output-root escape.
    - `rg -n "release-candidate.*cannot|cannot.*release-candidate|release-candidate" tools/bazel/phase26_release_signing_upstream_evidence_test.py` shows a Phase 26 regression test that prevents Phase 20's older `release-candidate` proof class from passing Phase 26.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --contract-only</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --security-only</automated>
  </verify>
  <done>Phase 26 has a contract-backed release evidence validator that preserves Phase 17 and Phase 20 release identities, rejects secret material before retained writes, and cannot pass template, local-smoke, pending, or `release-candidate` evidence as Phase 26 release proof.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize upstream rows and write retained Phase 26 outputs</name>
  <files>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json, tools/bazel/phase26_release_signing_upstream_evidence.py, tools/bazel/phase26_release_signing_upstream_evidence_test.py</files>
  <read_first>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>tools/bazel/manifests/phase18_cutover_review_contract.json</file>
    <file>tools/bazel/phase18_cutover_review.py</file>
    <file>tools/bazel/phase18_cutover_review_test.py</file>
    <file>tools/bazel/phase23_simulator_evidence_execution.py</file>
    <file>tools/bazel/phase24_hardware_media_safety_evidence_execution.py</file>
    <file>tools/bazel/phase25_live_service_evidence_execution.py</file>
    <file>tools/bazel/manifests/phase20_release_environment_inputs.template.json</file>
    <file>.planning/phases/23-simulator-evidence-execution/23-01-SUMMARY.md</file>
    <file>.planning/phases/24-hardware-media-and-safety-evidence-execution/24-01-SUMMARY.md</file>
    <file>.planning/phases/25-live-service-evidence-execution/25-01-SUMMARY.md</file>
  </read_first>
  <behavior>
    - Test upstream row coverage: generated row table contains exactly the nine canonical Phase 18 criterion IDs.
    - Test row schema: every row has `criterion_id`, `evidence_family`, `requirement_ids`, `source_requirement_ids`, `owning_phase`, `source_lifecycle_id`, `source_lifecycle_status`, `evidence_refs`, `artifact_refs`, `status`, `failure_reason`, `redaction_status`, `source_ref_status`, `exception_status`, `maintainer_state`, and `generated_at_utc`.
    - Test release row mapping: `final-release-artifact-signing-evidence` carries `requirement_ids: ["EVID-04", "ACPT-01"]`; every other row carries `requirement_ids: ["ACPT-01"]`.
    - Test blocker semantics: missing, stale, lifecycle-mismatched, source-ref-invalid, failed, blocked, redaction-failed, schema-invalid, and overclaiming rows stay blocked unless the source contract explicitly marks the status exception-coverable.
    - Test later-phase boundaries: `final-retained-code-acceptance`, `final-residual-risk-review`, `final-maintainer-decision`, and `final-reference-demotion-allowed` are emitted as blocked, pending, or not-required review rows and never as accepted approvals in Phase 26.
  </behavior>
  <action>
    Extend the contract with `upstream_policy` for the nine Phase 18 criteria listed in the context. The policy must load canonical criteria from `tools/bazel/manifests/phase18_cutover_review_contract.json` per D-09 and use explicit compatibility mapping only where necessary, including mapping Phase 25 compact `final-live-service-evidence` inputs to Phase 18 canonical `final-live-network-transfer-evidence`. Do not create a second live-service gate.

    Implement upstream row normalization in `tools/bazel/phase26_release_signing_upstream_evidence.py` per D-10, D-11, D-12, and D-13. Every row must include `criterion_id`, `evidence_family`, `requirement_ids`, `source_requirement_ids`, `owning_phase`, `source_lifecycle_id`, `source_lifecycle_status`, `evidence_refs`, `artifact_refs`, `status`, `failure_reason`, `redaction_status`, `source_ref_status`, `exception_status`, `maintainer_state`, and `generated_at_utc`. Use `source_requirement_ids` for Phase 18 source IDs such as `REV-02` and `REV-03`; use Phase 26 `requirement_ids` as `["EVID-04", "ACPT-01"]` for release/signing and `["ACPT-01"]` for all other review rows.

    Implement fail-closed row states. Use upstream status values from Phase 18: `missing`, `not-required`, `pending`, `pending-ci-input`, `pending-simulator-input`, `pending-hardware-input`, `pending-live-input`, `pending-release-input`, `release-run-required`, `external-signing-required`, `blocked-signing-key-unavailable`, `source-contract-passed`, `passed`, `failed`, `blocked`, `rejected-redaction`, and `rejected-overclaim`. For Phase 26 output, redaction and source-ref status must be `passed` to allow a row to be `passed`; any failed redaction status is a hard blocker and cannot become a normal exception approval per D-07.

    Write retained outputs under `build/ci-evidence/phase26` per D-14 and D-15. Generate these exact files: `release-upstream-run-manifest.json`, `normalized-release-evidence-summary.json`, `upstream-result-row-table.json`, `upstream-result-manifest.json`, `redaction-provenance-summary.json`, `artifact-reference-summary.json`, `operator-release-input-template.json`, `contract-snapshots/phase17_release_candidate_evidence_contract.json`, `contract-snapshots/phase18_cutover_review_contract.json`, `contract-snapshots/phase20_release_candidate_artifacts_contract.json`, and `contract-snapshots/phase20_release_environment_inputs.template.json`. Keep them generated under the ignored build tree per D-16.

    Make `--quick --output-dir build/ci-evidence/phase26` pass using checked-in safe contracts and template evidence while visibly reporting `real_release_evidence_supplied: false`, `maintainer_state: "pending"`, `exception_status: "none"`, and blocked or pending statuses where real release, retained-code, residual-risk, final-readiness, or demotion decision inputs are not supplied per D-19.
  </action>
  <acceptance_criteria>
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26` exits 0 and creates all retained output filenames listed in this task.
    - `python3 - <<'PY'\nimport json\nfrom pathlib import Path\nrows=json.loads(Path('build/ci-evidence/phase26/upstream-result-row-table.json').read_text())['rows']\nexpected={'final-ci-evidence','final-simulator-evidence','final-hardware-safety-media-evidence','final-live-network-transfer-evidence','final-release-artifact-signing-evidence','final-retained-code-acceptance','final-residual-risk-review','final-maintainer-decision','final-reference-demotion-allowed'}\nassert {row['criterion_id'] for row in rows} == expected\nrequired={'criterion_id','evidence_family','requirement_ids','source_requirement_ids','owning_phase','source_lifecycle_id','source_lifecycle_status','evidence_refs','artifact_refs','status','failure_reason','redaction_status','source_ref_status','exception_status','maintainer_state','generated_at_utc'}\nassert all(required <= row.keys() for row in rows)\nassert next(row for row in rows if row['criterion_id']=='final-release-artifact-signing-evidence')['requirement_ids'] == ['EVID-04','ACPT-01']\nassert all(row['maintainer_state'] in {'pending','blocked','not-required'} for row in rows)\nPY` exits 0 after quick mode.
    - `rg -n "demotion_allowed.*true|final approval complete|retained-code accepted|accepted retained-code" build/ci-evidence/phase26` returns no matches after quick mode.
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py` exits 0 with tests for all nine Phase 18 criteria, D-11 row keys, redaction-failed hard blockers, lifecycle/source-ref blockers, exception eligibility, retained output file creation, and no-overclaim wording.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26</automated>
  </verify>
  <done>Phase 26 writes secret-safe retained release summaries and a complete normalized upstream result table for all Phase 18 cutover criteria without approving later retained-code, residual-risk, final-readiness, or demotion decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 26 through Bazel, rust workflow, and just</name>
  <files>BUILD.bazel, tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, tools/bazel/phase26_release_signing_upstream_evidence.py, tools/bazel/phase26_release_signing_upstream_evidence_test.py</files>
  <read_first>
    <file>BUILD.bazel</file>
    <file>tools/bazel/BUILD.bazel</file>
    <file>tools/bazel/rust_workflow.sh</file>
    <file>justfile</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence.py</file>
    <file>tools/bazel/phase26_release_signing_upstream_evidence_test.py</file>
    <file>tools/bazel/manifests/phase26_release_signing_upstream_evidence_contract.json</file>
    <file>tools/bazel/BUILD.bazel sections for `phase23_verify`, `phase24_verify`, and `phase25_verify`</file>
    <file>BUILD.bazel sections for `phase23_simulator_evidence_execution_docs`, `phase24_hardware_media_safety_evidence_execution_docs`, and `phase25_live_service_evidence_execution_docs`</file>
    <file>tools/bazel/rust_workflow.sh cases for `phase23_verify`, `phase24_verify`, and `phase25_verify`</file>
    <file>justfile recipes for `phase23-verify`, `phase24-verify`, and `phase25-verify`</file>
  </read_first>
  <behavior>
    - Test root and tool Bazel labels expose Phase 26 tests before verifier execution.
    - Test `tools/bazel/rust_workflow.sh phase26_verify` runs `--wiring-only` before `--quick --output-dir build/ci-evidence/phase26`.
    - Test `just phase26-verify` runs `bazel run //tools/bazel:phase26_verify_tests` before `bazel run //tools/bazel:phase26_verify`.
  </behavior>
  <action>
    Add `phase26_release_signing_upstream_evidence_docs` to root `BUILD.bazel` with `.planning/phases/26-release-signing-and-upstream-result-evidence/26-CONTEXT.md`, `26-RESEARCH.md`, `26-VALIDATION.md`, and `26-01-PLAN.md`. Add root aliases `phase26_verify` and `phase26_verify_tests` pointing to `//tools/bazel:phase26_verify` and `//tools/bazel:phase26_verify_tests`.

    Add `phase26_source_ref_manifests`, `phase26_verify`, and `phase26_verify_tests` to `tools/bazel/BUILD.bazel` per D-17. `phase26_source_ref_manifests` must include `manifests/phase17_release_candidate_evidence_contract.json`, `manifests/phase18_cutover_review_contract.json`, `manifests/phase19_aggregate_ci_evidence_contract.json`, `manifests/phase20_release_candidate_artifacts_contract.json`, `manifests/phase20_release_environment_inputs.template.json`, `manifests/phase23_simulator_evidence_execution_contract.json`, `manifests/phase24_hardware_media_safety_evidence_execution_contract.json`, `manifests/phase25_live_service_evidence_execution_contract.json`, and `manifests/phase26_release_signing_upstream_evidence_contract.json`. `phase26_verify` data must include the verifier script, contract, source ref filegroup, and root docs filegroup. `phase26_verify_tests` data must include the verifier script, test script, contract, and source ref filegroup.

    Add `phase26_verify)` and `phase26_verify_tests)` cases to `tools/bazel/rust_workflow.sh`. The verify case must run `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --wiring-only` and then `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26`. The test case must run `python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py`.

    Add a `phase26-verify:` recipe to `justfile` that runs `bazel run //tools/bazel:phase26_verify_tests` before `bazel run //tools/bazel:phase26_verify`. Keep existing Phase 17, 18, 19, 20, 23, 24, and 25 labels unchanged.

    Extend `--wiring-only` tests and implementation so missing root docs filegroup, missing aliases, missing `phase26_source_ref_manifests`, wrong `rust_workflow.sh` command order, or wrong `justfile` command order fail with explicit messages.
  </action>
  <acceptance_criteria>
    - `rg -n "phase26_release_signing_upstream_evidence_docs|phase26_verify|phase26_verify_tests" BUILD.bazel` shows the docs filegroup and both root aliases.
    - `rg -n "phase26_source_ref_manifests|phase26_verify|phase26_verify_tests|phase26_release_signing_upstream_evidence_contract" tools/bazel/BUILD.bazel` shows the source-ref filegroup and both shell targets.
    - `rg -n "phase26_verify\\)|phase26_verify_tests\\)|phase26_release_signing_upstream_evidence.py --wiring-only|phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26" tools/bazel/rust_workflow.sh` shows the required dispatch cases and commands.
    - `rg -n "phase26-verify:|bazel run //tools/bazel:phase26_verify_tests|bazel run //tools/bazel:phase26_verify" justfile` shows the developer facade in test-before-verify order.
    - `python3 tools/bazel/phase26_release_signing_upstream_evidence.py --wiring-only` exits 0.
    - `just phase26-verify` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py</automated>
    <automated>python3 tools/bazel/phase26_release_signing_upstream_evidence.py --wiring-only</automated>
    <automated>just phase26-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Phase 26 verification is reachable through direct Python commands, Bazel labels, `rust_workflow.sh`, and `just phase26-verify`, with tests running before verifier execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release-manager JSON -> Phase 26 verifier | Untrusted release/signing/provenance metadata crosses into local validation and retained output generation. |
| Canonical source contracts -> Phase 26 normalizer | Phase 17, Phase 18, and Phase 20 manifests are authoritative inputs; row ID or lifecycle drift must not be silently normalized into success. |
| Phase 26 verifier -> `build/ci-evidence/phase26` | Validated summaries and row tables are written under a repo-local ignored output root. |
| Retained Phase 26 outputs -> maintainer review | Machine-readable rows can influence later exception, final readiness, and demotion decisions. |
| Bazel/just workflow -> evidence outputs | Developer-facing commands create review artifacts and must not skip tests or bypass security checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | Release proof-class normalization | mitigate | Only `approved-release-run` and `external-release-key-evidence` can pass Phase 26 release rows; tests reject `release-candidate`, `local-smoke`, and `template-only` pass attempts. |
| T-26-02 | Tampering | Release row coverage | mitigate | Load canonical Phase 20 `.rows[].id`; reject missing, duplicate, unknown, and drifted release IDs before summary generation. |
| T-26-03 | Information Disclosure | Release input scanning and retained output writer | mitigate | Scan raw text and parsed JSON for private keys, private certificates, signing payload bytes, firmware payload bytes, credentials, tokens, passwords, secrets, raw logs, and binary dumps before creating or writing retained outputs. |
| T-26-04 | Tampering / Information Disclosure | Output root handling | mitigate | Resolve `--output-dir`; require containment under `build/ci-evidence/phase26`; reject absolute paths, `..`, and symlink escapes before deletion or write operations. |
| T-26-05 | Tampering / Repudiation | Lifecycle and source-ref status | mitigate | Preserve `source_lifecycle_id`, `source_lifecycle_status`, and `source_ref_status` in every upstream row; stale, mismatched, missing, or failed source refs block the row. |
| T-26-06 | Elevation of Privilege | Exception and maintainer state handling | mitigate | Redaction failures are hard blockers; Phase 26 writes `exception_status: "none"` and `maintainer_state: "pending"` for later-phase decisions and cannot approve retained code, residual risk, final readiness, or reference demotion. |
| T-26-07 | Spoofing / Elevation of Privilege | Proof overclaim wording | mitigate | Tests scan retained outputs for final approval, retained-code acceptance, and `demotion_allowed: true` claims; quick mode writes blocked or pending review rows only. |
| T-26-08 | Denial of Service | Schema-invalid upstream rows | mitigate | Contract and unit tests require all D-11 row keys, exact nine Phase 18 criteria, and explicit failure reasons so malformed rows fail closed instead of disappearing from review. |
| T-26-09 | Tampering | Bazel/just workflow wiring | mitigate | `--wiring-only` verifies docs filegroup, source-ref filegroup, root aliases, shell dispatch, and `just phase26-verify` order so tests run before verifier execution. |
</threat_model>

<verification>
Run these commands after all tasks:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
python3 tools/bazel/phase26_release_signing_upstream_evidence_test.py
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --contract-only
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --security-only
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --wiring-only
python3 tools/bazel/phase26_release_signing_upstream_evidence.py --quick --output-dir build/ci-evidence/phase26
just phase26-verify
git diff --check
```

Then inspect generated output with:

```bash
python3 - <<'PY'
import json
from pathlib import Path
rows = json.loads(Path("build/ci-evidence/phase26/upstream-result-row-table.json").read_text())["rows"]
expected = {
    "final-ci-evidence",
    "final-simulator-evidence",
    "final-hardware-safety-media-evidence",
    "final-live-network-transfer-evidence",
    "final-release-artifact-signing-evidence",
    "final-retained-code-acceptance",
    "final-residual-risk-review",
    "final-maintainer-decision",
    "final-reference-demotion-allowed",
}
assert {row["criterion_id"] for row in rows} == expected
assert next(row for row in rows if row["criterion_id"] == "final-release-artifact-signing-evidence")["requirement_ids"] == ["EVID-04", "ACPT-01"]
assert all("generated_at_utc" in row for row in rows)
PY
```
</verification>

<success_criteria>
- EVID-04 is satisfied by a release-manager input path that covers all 18 canonical Phase 20 release rows, requires pass metadata, permits only reference-style signing identity, and rejects private keys, credentials, raw payloads, and secret-tainted evidence before retained writes.
- ACPT-01 is satisfied by a normalized upstream row table containing all nine canonical Phase 18 cutover criteria with Phase 26 requirement IDs, source requirement IDs, owning phase or gate, evidence refs, artifact refs, lifecycle status, exception status, maintainer state, and generated timestamp.
- Missing, stale, failed, blocked, secret-tainted, schema-invalid, source-ref-invalid, lifecycle-mismatched, and overclaiming rows remain blocked until corrected or explicitly exception-approved where the canonical source contract permits it.
- Phase 26 generated evidence remains under `build/ci-evidence/phase26`; tracked changes are limited to the source contract, verifier, tests, Bazel wiring, `rust_workflow.sh`, `justfile`, and this plan.
- `just phase26-verify` passes and runs tests before verifier execution.
</success_criteria>

<output>
After completion, create `.planning/phases/26-release-signing-and-upstream-result-evidence/26-01-SUMMARY.md`.
</output>
