---
phase: 24-hardware-media-and-safety-evidence-execution
plan: "01"
type: execute
wave: 0
depends_on:
  - 23
files_modified:
  - tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json
  - tools/bazel/phase24_hardware_media_safety_evidence_execution.py
  - tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
autonomous: true
requirements:
  - EVID-02
requirements_addressed:
  - EVID-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-06-23T19-52-32
generated_at: 2026-06-23T20:19:41Z
must_haves:
  truths:
    - "Maintainer can submit a sanitized hardware/media/safety evidence packet with one row for every canonical Phase 15 scenario."
    - "Every Phase 24 scenario result is normalized to passed, failed, blocked, or exception-requested without letting pending Phase 15 hardware statuses pass."
    - "Storage-media rows preserve specific media surface, observed behavior, failure observations, and residual risk."
    - "Safety, UI-input, MMU, RS485, toolchanger, and auxiliary rows remain failed or blocked when required coverage or residual risk is missing."
    - "Retained outputs under build/ci-evidence/phase24 are secret-safe, machine-readable, and explicit about non-local boundaries."
    - "Bazel labels, rust_workflow dispatch, and justfile facade expose Phase 24 tests and verification."
  artifacts:
    - path: "tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json"
      provides: "Phase 24 evidence execution schema and policy"
      contains: "phase24_hardware_media_safety_evidence_execution_contract"
    - path: "tools/bazel/phase24_hardware_media_safety_evidence_execution.py"
      provides: "Phase 24 evidence validator and retained output writer"
      contains: "DEFAULT_OUTPUT_DIR = Path(\"build/ci-evidence/phase24\")"
    - path: "tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py"
      provides: "Regression coverage for EVID-02 acceptance samples"
      contains: "class Phase24HardwareMediaSafetyEvidenceExecutionTest"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Phase 24 Bazel shell targets"
      contains: "phase24_verify"
    - path: "BUILD.bazel"
      provides: "Root Phase 24 docs filegroup and aliases"
      contains: "phase24_hardware_media_safety_evidence_execution_docs"
    - path: "tools/bazel/rust_workflow.sh"
      provides: "Phase 24 workflow dispatch"
      contains: "phase24_verify)"
    - path: "justfile"
      provides: "Developer facade"
      contains: "phase24-verify:"
  key_links:
    - from: "tools/bazel/phase24_hardware_media_safety_evidence_execution.py"
      to: "tools/bazel/manifests/phase15_hardware_evidence_contract.json"
      via: "canonical scenario catalog load and exact scenario ID set check"
      pattern: "phase15_hardware_evidence_contract"
    - from: "tools/bazel/phase24_hardware_media_safety_evidence_execution.py"
      to: "build/ci-evidence/phase24"
      via: "retained JSON output writer"
      pattern: "hardware-media-safety-result-manifest.json"
    - from: "tools/bazel/phase24_hardware_media_safety_evidence_execution.py"
      to: "tools/bazel/BUILD.bazel"
      via: "wiring-only self-check"
      pattern: "phase24_verify"
    - from: "justfile"
      to: "//tools/bazel:phase24_verify"
      via: "phase24-verify recipe"
      pattern: "bazel run //tools/bazel:phase24_verify"
---

<objective>
Create the Phase 24 hardware, media, and safety evidence execution gate.

Purpose: Maintainers need a secret-safe way to submit and retain real hardware/media/safety evidence for EVID-02 without redefining the Phase 15 hardware scenario catalog or claiming final readiness.

Output: A Phase 24 contract manifest, verifier, regression tests, Bazel labels, workflow dispatch, and `just phase24-verify` facade.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-hardware-media-and-safety-evidence-execution/24-CONTEXT.md
@.planning/phases/24-hardware-media-and-safety-evidence-execution/24-RESEARCH.md
@.planning/phases/24-hardware-media-and-safety-evidence-execution/24-VALIDATION.md
@.planning/phases/24-hardware-media-and-safety-evidence-execution/24-UI-SPEC.md
@.planning/phases/23-simulator-evidence-execution/23-01-SUMMARY.md
@tools/bazel/manifests/phase15_hardware_evidence_contract.json
@tools/bazel/phase15_hardware_evidence.py
@tools/bazel/phase15_hardware_evidence_test.py
@tools/bazel/manifests/phase23_simulator_evidence_execution_contract.json
@tools/bazel/phase23_simulator_evidence_execution.py
@tools/bazel/phase23_simulator_evidence_execution_test.py
@tools/bazel/manifests/phase18_cutover_review_contract.json
@tools/bazel/phase18_cutover_review.py
@tools/bazel/manifests/phase19_aggregate_ci_evidence_contract.json
@tools/bazel/phase19_aggregate_ci_evidence.py
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile

<interfaces>
Phase 15 scenario objects contain these fields and remain the canonical source for Phase 24 scenario identity:

```json
[
  "id",
  "title",
  "proof_scope",
  "printer_family",
  "board",
  "media_surface",
  "auxiliary_surface",
  "operator_metadata_required",
  "residual_risk_required",
  "retained_artifact_kind",
  "source_contract_refs",
  "allowed_statuses",
  "unsupported_claims",
  "requirement_ids",
  "v1_requirement_ids",
  "expected_artifact_path",
  "expected_pass_semantics",
  "expected_failure_semantics"
]
```

The 26 Phase 15 scenario IDs that Phase 24 must require exactly are:

```text
hard-supported-printer-smoke-coreone-xbuddy
hard-supported-printer-smoke-mini-buddy
hard-board-startup-readiness-mk4-xbuddy
hard-board-startup-readiness-xl-xlbuddy-puppies
hard-supported-printer-smoke-mk35-xbuddy
hard-supported-printer-smoke-ix-xbuddy
hard-supported-printer-smoke-xl-dev-kit-xlb
hard-board-startup-readiness-dwarf
hard-board-startup-readiness-modularbed
hard-board-startup-readiness-xl-dev-kit-xlb
hard-storage-usb-fatfs-removable-media
hard-storage-internal-flash-littlefs
hard-storage-bbf-littlefs-resource-image
hard-storage-eeprom-config-store
hard-storage-semihosting
hard-storage-root-libsysbase-dispatch
hard-ui-physical-input-encoder-touch
hard-safety-watchdog-crash-recovery
hard-safety-thermal-motion-emergency-stop
hard-safe-output-fatal-redscreen-bsod
hard-mmu-fault-handling
hard-rs485-modbus-fault-handling
hard-toolchanger-dock-offset-calibration
hard-auxiliary-controller-combination-coreone-xbe
hard-auxiliary-controller-combination-xl-dwarf-modularbed
hard-contract-traceability-and-redaction-boundary
```

The Phase 23 verifier pattern to mirror exposes:

```python
def check_contract(root: Path) -> tuple[dict[str, Any], dict[str, Any]]: ...
def validate_artifact_ref(ref: str, row_name: str) -> str: ...
def load_evidence_rows(root: Path, input_path: Path, source_contract: dict[str, Any]) -> tuple[dict[str, Any], list[dict[str, Any]]]: ...
def quick_rows(root: Path, output_dir: Path, source_contract: dict[str, Any]) -> list[dict[str, Any]]: ...
def write_retained_outputs(root: Path, output_dir: Path, rows: list[dict[str, Any]], command_mode: str, real_input_supplied: bool, maybe_packet: dict[str, Any] | None = None) -> None: ...
def check_security(root: Path) -> None: ...
def check_wiring(root: Path) -> None: ...
```

Phase 24 must keep the same command surface: `--contract-only`, `--security-only`, `--wiring-only`, `--quick`, `--evidence-input`, and `--output-dir`.
</interfaces>
</context>

<decision_coverage>
| Decision | Plan Task | Coverage |
|----------|-----------|----------|
| D-01 | Task 1, Task 2 | Full: contract and tests require exact Phase 15 scenario IDs. |
| D-02 | Task 1, Task 2 | Full: every Phase 15 scenario must have exactly one result row. |
| D-03 | Task 1, Task 2 | Full: sanitized metadata and artifact refs are required; raw payloads and secrets are rejected. |
| D-04 | Task 1, Task 2 | Full: Phase 24 statuses are limited to passed, failed, blocked, and exception-requested. |
| D-05 | Task 1, Task 2 | Full: pending/manual/unavailable Phase 15 statuses cannot pass. |
| D-06 | Task 1, Task 2 | Full: retained outputs preserve simulator, live-service, release/signing, retained-code, review, and demotion boundaries. |
| D-07 | Task 1, Task 2 | Full: hardware rows require family, board, build, operator, timestamp, artifact refs, and residual risk. |
| D-08 | Task 1, Task 2 | Full: storage media rows keep distinct media surface, observed behavior, failure observations, and risk. |
| D-09 | Task 1, Task 2 | Full: safety and auxiliary coverage fails or blocks when missing or unresolved. |
| D-10 | Task 1, Task 3 | Full: outputs are written under build/ci-evidence/phase24 and wired there. |
| D-11 | Task 1, Task 2 | Full: secret, raw payload, and overclaim guards are implemented and tested. |
| D-12 | Task 1, Task 2 | Full: retained manifest, normalized summary, redacted summary, snapshots/refs, artifact summary, template, and upstream row are written and tested. |
| D-13 | Task 3 | Full: root BUILD, tools BUILD, rust_workflow, and justfile are wired. |
| D-14 | Task 2, Task 3 | Full: focused Python tests cover acceptance samples and wiring. |
| D-15 | Task 1, Task 2 | Full: checked-in fixtures and quick blocked rows pass local verification without claiming real hardware proof. |
</decision_coverage>

<validation_samples>
Cover every acceptance sample from `24-VALIDATION.md`:

- Complete input packet with one row per Phase 15 scenario is accepted, retained, and summarized.
- Missing, duplicate, or unknown scenario rows are rejected.
- `source_status` values `pending-hardware-input`, `manual-hardware-required`, and `blocked-hardware-unavailable` cannot pass.
- `exception-requested` rows require owner, rationale, evidence ref, and revisit condition.
- Storage rows require media surface, filesystem/resource behavior, failure observations, and residual risk.
- Safety rows require artifact refs and residual risk.
- Artifact refs outside `build/ci-evidence/phase24/` or `external://phase24/` are rejected.
- Secret-bearing fields/text, raw firmware/crash payload markers, and non-local overclaims are rejected.
- Quick mode without real input is retained as blocked quick evidence and never as passed proof.
</validation_samples>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 24 contract and verifier</name>
  <files>tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json, tools/bazel/phase24_hardware_media_safety_evidence_execution.py</files>
  <read_first>
    - .planning/phases/24-hardware-media-and-safety-evidence-execution/24-CONTEXT.md
    - .planning/phases/24-hardware-media-and-safety-evidence-execution/24-RESEARCH.md
    - .planning/phases/24-hardware-media-and-safety-evidence-execution/24-VALIDATION.md
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json
    - tools/bazel/phase15_hardware_evidence.py
    - tools/bazel/manifests/phase23_simulator_evidence_execution_contract.json
    - tools/bazel/phase23_simulator_evidence_execution.py
    - tools/bazel/manifests/phase18_cutover_review_contract.json
    - tools/bazel/manifests/phase19_aggregate_ci_evidence_contract.json
  </read_first>
  <behavior>
    - A contract-only run fails if the Phase 24 contract scenario ID list does not exactly equal the Phase 15 scenario ID list.
    - A complete evidence input packet emits retained JSON under `build/ci-evidence/phase24`.
    - Quick mode emits blocked rows for all 26 scenarios with `real_hardware_evidence_supplied=false`.
    - Secret-bearing content, raw payload markers, unsafe artifact refs, and non-local overclaim phrases fail before retained outputs are trusted.
  </behavior>
  <action>
    Create `tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json` with id `phase24_hardware_media_safety_evidence_execution_contract`, phase `24-hardware-media-and-safety-evidence-execution`, lifecycle id `24-2026-06-23T19-52-32`, output root `build/ci-evidence/phase24`, artifact name `phase24-hardware-media-safety-evidence-execution`, `requirement_ids=["EVID-02"]`, status vocabulary `passed`, `failed`, `blocked`, `exception-requested`, allowed artifact roots `build/ci-evidence/phase24/` and `external://phase24/`, source contracts for Phase 15/18/19/23, and `upstream_result_row` fields `criterion_id=final-hardware-safety-media-evidence`, `evidence_family=hardware`, and `requirement_ids=["EVID-02"]`.

    The contract must declare required packet fields `evidence_run_id`, `phase_lifecycle_id`, `firmware_identity`, `operator`, `started_at`, `completed_at`, `source_contract_ref`, and `scenario_results`. It must declare required scenario row fields `scenario_id`, `status`, `source_status`, `status_reason`, `artifact_refs`, `redaction_status`, `source_ref_status`, `device`, `printer_family`, `board`, `firmware_build`, `operator`, `timestamp`, and `residual_risk`, plus conditional fields `media_surface`, `auxiliary_surface`, `failure_observations`, and `exception_request`.

    Create `tools/bazel/phase24_hardware_media_safety_evidence_execution.py` as a standard-library Python verifier modeled on Phase 23 and using Phase 15 as the canonical catalog per D-01. Implement modes `--contract-only`, `--security-only`, `--wiring-only`, `--quick`, `--evidence-input`, and `--output-dir`. The verifier must:

    - Load and validate `tools/bazel/manifests/phase15_hardware_evidence_contract.json`; do not fork or rename scenario IDs per D-01.
    - Require exact one-row coverage for all 26 Phase 15 scenarios per D-02.
    - Validate sanitized packet and row metadata per D-03 and D-07.
    - Normalize only to the four Phase 24 statuses per D-04.
    - Reject `status=passed` with `source_status` of `pending-hardware-input`, `manual-hardware-required`, or `blocked-hardware-unavailable` per D-05.
    - Require passed rows to have `redaction_status=passed` and `source_ref_status=passed`.
    - Require `exception_request.owner`, `rationale`, `evidence_ref`, and `revisit_condition` for `exception-requested` per D-14.
    - Preserve non-local boundaries in retained outputs and reject wording that claims live-service proof, release/signing proof, retained-code acceptance, final readiness, or reference demotion per D-06 and D-11.
    - Require storage/media rows to keep their Phase 15 `media_surface`, observed resource/config behavior, failure observations, and residual risk per D-08.
    - Require safety, UI-input, MMU, RS485, toolchanger, and auxiliary-controller rows to remain failed or blocked when artifact refs or residual risk are missing per D-09.
    - Allow artifact refs only under `build/ci-evidence/phase24/` or `external://phase24/` and reject absolute paths, `..`, empty refs, and bare `external://phase24/` refs per D-11.
    - Reject private keys, certificates, tokens, Wi-Fi credentials, raw crash dumps, raw RAM dumps, firmware payload bytes, BBF/DFU payloads, and forbidden field names using case-folded dash/underscore normalization per D-11.
    - Write `hardware-media-safety-result-manifest.json`, `normalized-hardware-media-safety-results.json`, `redacted-hardware-media-safety-summary.json`, `upstream-hardware-result-row.json`, `operator-evidence-input-template.json`, `contract-snapshots/phase15_hardware_evidence_contract.json`, `contract-snapshots/phase24_hardware_media_safety_evidence_execution_contract.json`, and `artifact-summaries/hardware-media-safety-artifact-summary.json` per D-10 and D-12.
    - In quick mode, write blocked quick rows and a blocked aggregate status without setting `real_hardware_evidence_supplied=true` per D-15.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --contract-only</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --security-only</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --quick --output-dir build/ci-evidence/phase24</automated>
  </verify>
  <acceptance_criteria>
    - `tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json` contains `phase24_hardware_media_safety_evidence_execution_contract`.
    - `tools/bazel/phase24_hardware_media_safety_evidence_execution.py` contains `DEFAULT_OUTPUT_DIR = Path("build/ci-evidence/phase24")`.
    - Contract validation proves `required_phase15_scenario_ids` exactly match the 26 Phase 15 scenario IDs.
    - Quick mode writes `build/ci-evidence/phase24/hardware-media-safety-result-manifest.json` with aggregate `status` set to `blocked`, scenario count `26`, and `real_hardware_evidence_supplied` set to `false`.
  </acceptance_criteria>
  <done>Contract, verifier, quick retained outputs, and security/contract checks satisfy D-01 through D-12 and D-15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 24 regression tests</name>
  <files>tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py, tools/bazel/phase24_hardware_media_safety_evidence_execution.py</files>
  <read_first>
    - .planning/phases/24-hardware-media-and-safety-evidence-execution/24-VALIDATION.md
    - tools/bazel/phase23_simulator_evidence_execution_test.py
    - tools/bazel/phase15_hardware_evidence_test.py
    - tools/bazel/manifests/phase15_hardware_evidence_contract.json
    - tools/bazel/manifests/phase24_hardware_media_safety_evidence_execution_contract.json
    - tools/bazel/phase24_hardware_media_safety_evidence_execution.py
  </read_first>
  <behavior>
    - Tests prove the complete-packet happy path writes retained outputs and upstream row fields for EVID-02.
    - Tests prove every rejection sample from `24-VALIDATION.md` fails with a specific reason.
    - Tests prove safe fixtures and quick blocked rows do not claim real hardware proof.
  </behavior>
  <action>
    Add `tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py` using Python `unittest`, temp roots, and Arrange/Act/Assert comments where test bodies are non-trivial. Keep each test focused on one behavior per Bright Builds testing rules.

    The suite must cover:

    - `--contract-only` accepts the complete contract and rejects a contract whose `required_phase15_scenario_ids` drift from Phase 15 per D-01.
    - A complete evidence packet with one row for each Phase 15 scenario is accepted, retained, and summarized per D-02 and EVID-02.
    - Missing, duplicate, and unknown scenario rows are rejected per D-02.
    - Invalid Phase 24 statuses are rejected per D-04.
    - `pending-hardware-input`, `manual-hardware-required`, and `blocked-hardware-unavailable` cannot pass per D-05.
    - `exception-requested` without owner, rationale, evidence ref, or revisit condition is rejected per D-14.
    - Missing device, printer family, board, firmware build, operator, timestamp, artifact refs, or residual risk is rejected per D-03 and D-07.
    - Storage rows without the specific media surface, resource/config behavior observed, failure observations, or residual risk are rejected per D-08.
    - Safety, UI-input, MMU, RS485, toolchanger, and auxiliary rows without artifact refs or residual risk are rejected per D-09.
    - Artifact refs outside `build/ci-evidence/phase24/` or `external://phase24/`, absolute paths, `..`, empty refs, and unsafe external refs are rejected per D-11.
    - Secret-bearing fields, mixed-case secret field names, private key/certificate markers, Wi-Fi credential wording, raw crash/RAM dump markers, firmware payload markers, BBF/DFU payload markers, and overclaim phrases are rejected per D-11.
    - Retained output files include the normalized summary, redacted summary, run manifest, source contract snapshot or reference, operator input template, artifact summary, and upstream result row per D-12.
    - Quick mode writes all rows as blocked quick evidence and never as real hardware proof per D-15.
    - Fixture wiring tests can validate expected `tools/bazel/BUILD.bazel`, root `BUILD.bazel`, `rust_workflow.sh`, and `justfile` snippets before Task 3 edits the real files per D-13.

    If tests reveal verifier gaps, refine `tools/bazel/phase24_hardware_media_safety_evidence_execution.py` in the same task. Do not relax decisions or acceptance samples to make tests pass.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --contract-only</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --quick --output-dir build/ci-evidence/phase24</automated>
  </verify>
  <acceptance_criteria>
    - The test file defines `Phase24HardwareMediaSafetyEvidenceExecutionTest`.
    - Tests include positive complete-packet coverage and every negative sample from `24-VALIDATION.md`.
    - `python3 tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py` exits 0.
    - Retained quick output remains blocked and `real_hardware_evidence_supplied=false`.
  </acceptance_criteria>
  <done>Regression tests pass and prove the Phase 24 acceptance samples and decision constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 24 Bazel and developer commands</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase23_simulator_evidence_execution.py
    - tools/bazel/phase23_simulator_evidence_execution_test.py
    - .planning/phases/23-simulator-evidence-execution/23-01-PLAN.md
  </read_first>
  <action>
    Add Phase 24 wiring consistent with Phase 15 and Phase 23 per D-13.

    In `tools/bazel/BUILD.bazel`, add a `phase24_source_ref_manifests` filegroup containing `manifests/phase24_hardware_media_safety_evidence_execution_contract.json`, `manifests/phase15_hardware_evidence_contract.json`, Phase 18 and Phase 19 contracts, Phase 23 contract, and the Phase 15 source-ref manifest dependencies needed to resolve Phase 15 rows. Add `shell_binary` targets `phase24_verify` and `phase24_verify_tests` using `rust_workflow.sh`. `phase24_verify` data must include the verifier, contract, source-ref filegroup, and root Phase 24 docs filegroup. `phase24_verify_tests` data must include the verifier, test file, contract, and source-ref filegroup.

    In root `BUILD.bazel`, add `phase24_hardware_media_safety_evidence_execution_docs` with `.planning/phases/24-hardware-media-and-safety-evidence-execution/24-CONTEXT.md`, `24-RESEARCH.md`, `24-VALIDATION.md`, `24-UI-SPEC.md`, and `24-01-PLAN.md`. Add aliases `phase24_verify` and `phase24_verify_tests`.

    In `tools/bazel/rust_workflow.sh`, add dispatch cases:

    ```bash
    phase24_verify)
      python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --wiring-only
      python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --quick --output-dir build/ci-evidence/phase24
      ;;
    phase24_verify_tests)
      python3 tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py
      ;;
    ```

    In `justfile`, add:

    ```make
    phase24-verify:
        bazel run //tools/bazel:phase24_verify_tests
        bazel run //tools/bazel:phase24_verify
    ```
  </action>
  <verify>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --wiring-only</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --contract-only</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --security-only</automated>
    <automated>python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --quick --output-dir build/ci-evidence/phase24</automated>
    <automated>just phase24-verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `tools/bazel/BUILD.bazel` contains `name = "phase24_source_ref_manifests"`, `name = "phase24_verify"`, and `name = "phase24_verify_tests"`.
    - Root `BUILD.bazel` contains `name = "phase24_hardware_media_safety_evidence_execution_docs"` plus aliases for `phase24_verify` and `phase24_verify_tests`.
    - `tools/bazel/rust_workflow.sh` contains `phase24_verify)` and `phase24_verify_tests)` dispatch cases.
    - `justfile` contains `phase24-verify:` and runs tests before verifier.
    - Every verification command listed in `24-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <done>Bazel, workflow, and justfile wiring expose Phase 24 verification and all validation commands pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer evidence packet -> Phase 24 verifier | Untrusted JSON input crosses into retained evidence processing. |
| Evidence packet artifact refs -> filesystem/external refs | Untrusted ref strings decide what retained evidence points at. |
| Phase 24 retained outputs -> later acceptance phases | Machine-readable summaries can influence final gate decisions. |
| Safe fixtures/quick rows -> human interpretation | Local quick evidence can be mistaken for real hardware proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `phase24_hardware_media_safety_evidence_execution.py` packet parser and output writer | mitigate | Reject forbidden field names and text for private keys, certificates, tokens, Wi-Fi credentials, raw crash/RAM dumps, firmware payloads, BBF/DFU payloads, and raw service payloads before writing retained outputs. |
| T-24-02 | Tampering / Information Disclosure | Artifact reference validation | mitigate | Accept only repo-relative refs under `build/ci-evidence/phase24/` or vetted `external://phase24/` refs; reject absolute paths, `..`, empty refs, and bare external roots. |
| T-24-03 | Spoofing / Repudiation | Status summaries and redacted summaries | mitigate | Reject overclaim wording for live-service proof, release readiness, signing proof, retained-code acceptance, final readiness, and reference demotion; include unsupported boundary lists in retained summaries. |
| T-24-04 | Repudiation | Scenario coverage aggregation | mitigate | Require exact one-row coverage for every Phase 15 scenario and fail on missing, duplicate, or unknown scenario IDs before aggregate status is computed. |
| T-24-05 | Tampering / Elevation of Privilege | Exception metadata validation | mitigate | Require `exception_request.owner`, `rationale`, `evidence_ref`, and `revisit_condition`; validate exception evidence refs with the same artifact bounds and never count exception-requested as passed. |
</threat_model>

<verification>
Run the Phase 24 validation commands from `24-VALIDATION.md`:

```bash
python3 tools/bazel/phase24_hardware_media_safety_evidence_execution_test.py
python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --contract-only
python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --security-only
python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --wiring-only
python3 tools/bazel/phase24_hardware_media_safety_evidence_execution.py --quick --output-dir build/ci-evidence/phase24
just phase24-verify
git diff --check
```
</verification>

<success_criteria>
- EVID-02 is covered by a Phase 24 contract, verifier, tests, and workflow facade.
- Phase 24 imports Phase 15 scenario identity exactly and does not redefine scenario IDs.
- Complete real evidence packets are accepted and retained; incomplete, unsafe, or overclaimed packets are rejected.
- Storage, safety, UI-input, MMU, RS485, toolchanger, and auxiliary-controller coverage remains row-specific.
- Quick local output stays blocked and cannot be mistaken for real hardware proof.
- Retained outputs are secret-safe and consumable by later acceptance phases.
</success_criteria>

<output>
After completion, create `.planning/phases/24-hardware-media-and-safety-evidence-execution/24-01-SUMMARY.md`.
</output>
