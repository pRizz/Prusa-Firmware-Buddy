---
phase: 03-artifact-and-generator-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/phase3_verify.py
  - tools/bazel/artifact_manifest.py
  - tools/bazel/generated_drift.py
autonomous: true
requirements:
  - BAZL-03
  - BAZL-05
requirements_addressed:
  - BAZL-03
  - BAZL-05
must_haves:
  truths:
    - "Developer can run Phase 3 helper self-tests without private signing keys or bootstrapped firmware toolchains."
    - "Artifact manifests normalize release metadata fields required by D-15 without treating firmware byte parity as a Phase 3 claim."
    - "Generated-file drift checks have explicit read-only check mode and separate source-writing update mode."
  artifacts:
    - path: "tools/bazel/phase3_verify.py"
      provides: "Phase 3 verifier CLI with quick, artifact, manifest, drift, update-target, and facade checks"
      contains: "Phase 3 quick verification passed"
    - path: "tools/bazel/artifact_manifest.py"
      provides: "Normalized artifact manifest writer and self-test"
      contains: "artifact manifest self-test passed"
    - path: "tools/bazel/generated_drift.py"
      provides: "Tracked generated-output drift checker and self-test"
      contains: "generated drift self-test passed"
  key_links:
    - from: "tools/bazel/phase3_verify.py"
      to: "tools/bazel/artifact_manifest.py"
      via: "subprocess or import-level self-test check"
      pattern: "artifact_manifest.py"
    - from: "tools/bazel/phase3_verify.py"
      to: "tools/bazel/generated_drift.py"
      via: "subprocess or import-level self-test check"
      pattern: "generated_drift.py"
    - from: "tools/bazel/generated_drift.py"
      to: "tracked generated outputs"
      via: "check/update registry"
      pattern: "CMakePresets.json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-02T21-03-53
generated_at: 2026-06-02T21:39:34Z
---

<objective>
Create the Phase 3 pure helper layer for release-artifact manifests, generated-file drift checks, and verifier sampling.

Purpose: Give later Bazel wiring a tested, standard-library implementation boundary before touching shared Bazel and `justfile` surfaces.
Output: `tools/bazel/phase3_verify.py`, `tools/bazel/artifact_manifest.py`, and `tools/bazel/generated_drift.py`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
@.planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
@.planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-01-SUMMARY.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-VERIFICATION.md
@tools/bazel/phase2_verify.py
@tools/bazel/BUILD.bazel
@justfile
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md

<interfaces>
From `tools/bazel/phase2_verify.py`:
```python
ROOT = Path(__file__).resolve().parents[2]

def read(path: str) -> str:
    full_path = ROOT / path
    if not full_path.exists():
        raise AssertionError(f"missing required file: {path}")
    return full_path.read_text(encoding="utf-8")

def run_optional(command: list[str]) -> None:
    if not shutil.which(command[0]):
        print(f"Skipping optional check, {command[0]} not found: {' '.join(command)}")
        return
```

From Phase 3 validation:
```text
Quick run command: python3 tools/bazel/phase3_verify.py --quick
Full suite command: bazel query "//tools/bazel/... + //platforms/..." && bazel run //tools/bazel:phase3_verify && just --list
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 3 verifier CLI</name>
  <files>tools/bazel/phase3_verify.py</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
    .planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
    .planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
    tools/bazel/phase2_verify.py
    tools/bazel/BUILD.bazel
    justfile
    AGENTS.md
    AGENTS.bright-builds.md
    standards-overrides.md
  </read_first>
  <behavior>
    - `--quick` checks helper files created by this plan, runs their self-tests when present, scans Phase 3 fixtures/manifests for private-key material, and prints `Phase 3 quick verification passed` on success.
    - `--require-artifacts`, `--require-manifests`, `--require-drift-checks`, `--require-update-targets`, and `--require-facade` fail with explicit missing-file or missing-string messages until Plan 02 wires those surfaces.
    - Optional external commands use the Phase 2 pattern: skip with a visible message when `bazel` or `just` is unavailable, but never silently ignore a failing available command.
  </behavior>
  <action>Create `tools/bazel/phase3_verify.py` as a standard-library Python 3 script following the `tools/bazel/phase2_verify.py` structure. Implement CLI flags `--quick`, `--require-artifacts`, `--require-manifests`, `--require-drift-checks`, `--require-update-targets`, `--require-facade`, and `--all`; `--all` must run every Phase 3 check. Add required-file checks for `tools/bazel/artifact_manifest.py`, `tools/bazel/generated_drift.py`, `tools/bazel/artifact_rules.bzl`, `tools/bazel/generator_rules.bzl`, `tools/bazel/phase3_verify.sh`, `tools/bazel/phase3_workflow.sh`, and `tools/bazel/manifests/representative_products.json`. Add required-string checks for `tools/bazel/BUILD.bazel` labels `phase3_verify`, `generated_check`, `generated_update`, `release_package`, `release_packages`, `representative_release_artifacts`, `artifact_manifest_smoke`, and `reference_release_compare`. Add facade checks that require `justfile` recipes `phase3-verify:`, `generated-check:`, and `release-package:`. Add a private material scanner that rejects `BEGIN PRIVATE KEY`, `BEGIN EC PRIVATE KEY`, `BEGIN RSA PRIVATE KEY`, and `SIGNING_KEY=` in `tools/bazel/fixtures/` and `tools/bazel/manifests/`, while allowing the string `unsigned-local` as signing mode per D-03. Do not execute full firmware builds, simulator flows, signing-sensitive commands, or `BUDDY_BAZEL_EXECUTE_REFERENCE=1` workflows in default or `--quick` mode per D-04, D-11, and D-14.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase3_verify.py && python3 tools/bazel/phase3_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "argparse|--quick|--require-artifacts|--require-manifests|--require-drift-checks|--require-update-targets|--require-facade|--all" tools/bazel/phase3_verify.py` finds every listed flag.
    - `rg -n "BEGIN PRIVATE KEY|BEGIN EC PRIVATE KEY|BEGIN RSA PRIVATE KEY|SIGNING_KEY=|unsigned-local" tools/bazel/phase3_verify.py` finds the private-material scanner and allowed local signing mode.
    - `python3 tools/bazel/phase3_verify.py --quick` exits 0 and prints `Phase 3 quick verification passed`.
  </acceptance_criteria>
  <done>Phase 3 verifier exists, quick mode passes without bootstrap-only tools, and full-mode checks are present for Plan 02 wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create normalized artifact manifest helper</name>
  <files>tools/bazel/artifact_manifest.py</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
    .planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
    utils/pack_fw.py
    utils/dfu.py
    utils/build.py
    ProjectOptions.cmake
    CMakePresets.json
  </read_first>
  <behavior>
    - `--self-test` creates temporary `.bin`, `.map`, `.bbf`, `.dfu`, resource, and provenance inputs, writes a normalized JSON manifest, validates exact keys, and prints `artifact manifest self-test passed`.
    - Manifest hashes are derived from actual files with SHA-256, never accepted from unchecked command-line values.
    - Manifest records `evidence_class` and `signing_mode` values so local package-surface evidence cannot be mistaken for signing-sensitive release parity.
  </behavior>
  <action>Create `tools/bazel/artifact_manifest.py` as a standard-library Python helper. Implement `--self-test` plus a write mode accepting `--output`, `--product`, `--printer`, `--board`, `--mcu`, `--bootloader-mode`, `--artifact-kind`, `--artifact`, `--resource`, `--package-member`, `--provenance`, `--evidence-class`, and `--signing-mode`. Validate product values `COREONE`, `MINI`, `MK4`, `MK3.5`, `XL`, `iX`, `XL_DEV_KIT`; board values `BUDDY`, `XBUDDY`, `XLBUDDY`, `DWARF`, `MODULARBED`, `XL_DEV_KIT_XLB`, `XBUDDY_EXTENSION`; MCU values `STM32F407VG`, `STM32F429VI`, `STM32F427ZI`, `STM32G070RBT6`, `STM32H503CBU7`; bootloader modes `boot`, `noboot`, `auxiliary`; artifact kinds `bin`, `map`, `bbf`, `dfu`, `resource-image`, `provenance`, and `auxiliary-manifest`; evidence classes `local-smoke`, `ci-only`, `reference-only`, `manual-hardware-required`, and `release-candidate`; signing modes `unsigned-local`, `test-key`, `external-release-key`, and `not-applicable`. Write sorted, indented JSON with keys `schema_version`, `product`, `printer`, `board`, `mcu`, `bootloader_mode`, `artifact_kind`, `filename`, `size`, `sha256`, `package_members`, `version_provenance`, `resource_presence`, `evidence_class`, and `signing_mode`. Implement path validation that resolves input files and rejects paths containing `..` segments after normalization per T-03-03; derive `filename`, `size`, and `sha256` from the artifact file. Do not assert byte identity against CMake firmware `.bin` outputs per D-10 and D-16.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/artifact_manifest.py && python3 tools/bazel/artifact_manifest.py --self-test && python3 tools/bazel/phase3_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/artifact_manifest.py --self-test` exits 0 and prints `artifact manifest self-test passed`.
    - `rg -n "schema_version|bootloader_mode|artifact_kind|package_members|version_provenance|resource_presence|signing_mode|unsigned-local|external-release-key" tools/bazel/artifact_manifest.py` finds all manifest fields and signing modes.
    - `rg -n "sha256|resolve|\\.\\." tools/bazel/artifact_manifest.py` finds hash derivation and path traversal rejection logic.
  </acceptance_criteria>
  <done>Artifact manifest helper can produce deterministic release metadata manifests with validated fields, derived hashes, and explicit evidence/signing classification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create generated-output drift helper</name>
  <files>tools/bazel/generated_drift.py</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
    .planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
    .planning/codebase/CONCERNS.md
    .pre-commit-config.yaml
    utils/build.py
    utils/logging/generate_overview.py
    utils/persistent_stores/visit_all_struct_fields_generator.py
    utils/persistent_stores/journal_hashes_generator.py
    utils/translations_and_fonts/generate_pot.sh
    utils/translations_and_fonts/generate_all_fonts.sh
  </read_first>
  <behavior>
    - `--self-test` creates temporary tracked/generated files, proves identical content passes, proves changed content fails, and prints `generated drift self-test passed`.
    - Check mode writes only under `--output-dir` or a temporary directory; source-tree writes are rejected unless `--update` is set.
    - Check registry includes concrete tracked generated surfaces from D-06 and classifies bootstrap/tool-heavy surfaces without making them default local requirements.
  </behavior>
  <action>Create `tools/bazel/generated_drift.py` as a standard-library Python helper. Implement `--self-test`, `--workspace`, `--output-dir`, `--check`, `--update`, and `--list-checks`. Add a registry with check IDs `cmake_presets`, `logging_components`, `visit_all_struct_fields`, `translation_pot`, `font_resources`, and `resource_headers`. For each registry entry, record `tracked_outputs`, `declared_sources`, `update_command`, `check_strategy`, and `evidence_class`. Use `local-smoke` for `cmake_presets`, `logging_components`, and `visit_all_struct_fields`; use `reference-only` or `ci-only` for translation/font/resource checks that need bootstrap-only packages or heavy tooling. Implement check mode so source-writing generators run only in a temporary workspace copy or into `--output-dir`; after generation compare normalized LF text or bytes against the tracked source output and exit nonzero with `DRIFT:` lines on differences. Implement update mode so source writes are allowed only when `--update` is present and the selected check has an explicit `update_command`. Normalize JSON for `CMakePresets.json` before comparison and compare byte-for-byte only for tracked deterministic files where byte identity is the contract per D-10. Do not call `pre-commit`, full firmware builds, simulator flows, or signing-sensitive commands in default check mode.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/generated_drift.py && python3 tools/bazel/generated_drift.py --self-test && python3 tools/bazel/phase3_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/generated_drift.py --self-test` exits 0 and prints `generated drift self-test passed`.
    - `python3 tools/bazel/generated_drift.py --list-checks` prints `cmake_presets`, `logging_components`, `visit_all_struct_fields`, `translation_pot`, `font_resources`, and `resource_headers`.
    - `rg -n "DRIFT:|--update|--output-dir|cmake_presets|logging_components|visit_all_struct_fields|translation_pot|font_resources|resource_headers" tools/bazel/generated_drift.py` finds drift reporting, mode split, and every check ID.
  </acceptance_criteria>
  <done>Generated drift helper has tested check/update separation, named generated-output surfaces, and source-write protection for check mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to helper scripts | Product IDs, artifact paths, generated-check IDs, and output directories enter Python helpers from Bazel, `just`, or direct local invocation. |
| Existing generators to tracked source files | Source-writing generator scripts can mutate committed files unless check mode confines writes. |
| Artifact files to normalized manifests | Release metadata must be derived from actual declared files, not unchecked user-provided truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `tools/bazel/phase3_verify.py`, Phase 3 fixtures/manifests | mitigate | Scan Phase 3 fixture/manifest directories for private-key PEM markers and `SIGNING_KEY=`; require `unsigned-local`, `test-key`, `external-release-key`, or `not-applicable` signing modes instead of committed key paths. |
| T-03-02 | Tampering | `tools/bazel/generated_drift.py` | mitigate | Enforce check/update split; in check mode allow writes only under `--output-dir` or temporary workspaces and reject source-tree writes. |
| T-03-03 | Spoofing/Tampering | `tools/bazel/artifact_manifest.py` | mitigate | Validate enum values, reject path traversal, and derive filename, size, and SHA-256 from resolved artifact files. |
| T-03-04 | Denial of Service / Information Disclosure | `tools/bazel/phase3_verify.py` | mitigate | Default quick checks must not execute full firmware builds, simulator flows, signing-sensitive commands, or reference commands guarded by `BUDDY_BAZEL_EXECUTE_REFERENCE=1`. |
</threat_model>

<verification>
Run the plan-local checks:

```bash
python3 -m py_compile tools/bazel/phase3_verify.py tools/bazel/artifact_manifest.py tools/bazel/generated_drift.py
python3 tools/bazel/artifact_manifest.py --self-test
python3 tools/bazel/generated_drift.py --self-test
python3 tools/bazel/phase3_verify.py --quick
```
</verification>

<success_criteria>
- `tools/bazel/phase3_verify.py --quick` passes and prints `Phase 3 quick verification passed`.
- `tools/bazel/artifact_manifest.py --self-test` passes and prints `artifact manifest self-test passed`.
- `tools/bazel/generated_drift.py --self-test` passes and prints `generated drift self-test passed`.
- No Phase 3 helper requires private signing keys, `.dependencies`, `.venv`, full firmware builds, simulator inputs, hardware, or `BUDDY_BAZEL_EXECUTE_REFERENCE=1` for default local verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-artifact-and-generator-parity/03-01-SUMMARY.md`
</output>
