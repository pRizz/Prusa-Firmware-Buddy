---
phase: 03-artifact-and-generator-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tools/bazel/generator_rules.bzl
  - tools/bazel/phase3_workflow.sh
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - justfile
autonomous: true
requirements:
  - BAZL-03
  - BAZL-05
requirements_addressed:
  - BAZL-03
  - BAZL-05
must_haves:
  truths:
    - "Developer can query and run Bazel-owned generator check/update labels for product profiles, option data, resources, translations, fonts, WUI assets, ESP blobs, puppy/MMU descriptors, package metadata, and tracked generated outputs."
    - "Generated check labels are read-only and generated update labels are the only source-writing targets."
    - "CI/reference/bootstrap-heavy generator surfaces remain explicit evidence-classified targets instead of disappearing from the plan."
    - "Developer can use `just phase3-verify`, `just generated-check`, `just generated-update`, and `just release-package` as the discoverable Phase 3 facade."
    - "`reference_release_compare` compares Bazel manifest field names and evidence classes against the Phase 1 reference-capture surface by default, and only executes CMake/Python reference commands when `BUDDY_BAZEL_EXECUTE_REFERENCE=1` is explicitly set."
  artifacts:
    - path: "tools/bazel/generator_rules.bzl"
      provides: "Bazel generated-file check/update macro surface"
      contains: "phase3_generated_surface"
    - path: "tools/bazel/phase3_workflow.sh"
      provides: "Runnable generator/facade workflow dispatcher"
      contains: "generated_product_profiles_check"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Queryable Phase 3 generator, facade, and reference labels"
      contains: "generated_wui_assets_check"
    - path: "justfile"
      provides: "Developer-facing Phase 3 recipes"
      contains: "generated-update:"
  key_links:
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/generated_drift.py"
      via: "generated check/update shell_binary data and workflow dispatch"
      pattern: "generated_drift.py"
    - from: "tools/bazel/phase3_workflow.sh"
      to: "tools/bazel/generated_drift.py"
      via: "per-surface --check/--update calls"
      pattern: "product_profiles"
    - from: "justfile"
      to: "//tools/bazel:generated_check"
      via: "`generated-check` recipe"
      pattern: "bazel run //tools/bazel:generated_check"
    - from: "tools/bazel/phase3_workflow.sh"
      to: "tools/bazel/manifests/representative_products.json"
      via: "`reference_release_compare` normalized Bazel manifest input"
      pattern: "representative_products.json"
    - from: "tools/bazel/phase3_workflow.sh"
      to: ".planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md and 01-BASELINE-MATRIX.md"
      via: "`reference_release_compare` Phase 1 reference surface input"
      pattern: "01-REFERENCE-CAPTURE.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-02T21-03-53
generated_at: 2026-06-02T21:59:58Z
---

<objective>
Wire full Phase 3 generator ownership, update targets, facade recipes, and guarded reference comparison.

Purpose: Satisfy BAZL-05/D-05/D-06/D-07 across every required generator surface and finish the Phase 3 developer facade after artifact-producing labels exist.
Output: Generator rule surface, workflow dispatcher, BUILD labels, root aliases, and `justfile` recipes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
@.planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
@.planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-01-SUMMARY.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-VERIFICATION.md
@tools/bazel/phase3_verify.py
@tools/bazel/generated_drift.py
@tools/bazel/artifact_rules.bzl
@tools/bazel/shell_rules.bzl
@tools/bazel/reference_contract.sh
@tools/bazel/BUILD.bazel
@BUILD.bazel
@justfile
@.bazelrc
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md

<interfaces>
From Plan 03-01 generated drift contract:
```text
python3 tools/bazel/generated_drift.py --list-checks
python3 tools/bazel/generated_drift.py --workspace "$root" --output-dir "$tmp" --check product_profiles --check option_data
python3 tools/bazel/generated_drift.py --workspace "$root" --update --check product_profiles
```

Required BAZL-05 generator surfaces:
```text
product_profiles
option_data
resources
translations
fonts
wui_assets
esp_blobs
puppy_descriptors
mmu_descriptors
package_metadata
tracked_generated_outputs
```

Reference comparison contract:
```text
Inputs:
- tools/bazel/manifests/representative_products.json
- .planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
- .planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md

Default behavior:
- reference_release_compare compares Bazel manifest field names and evidence_class/signing_mode names against the Phase 1 reference-capture surface.
- It must not execute CMake, utils/build.py, firmware builds, signing commands, simulator flows, or other live reference commands unless BUDDY_BAZEL_EXECUTE_REFERENCE=1 is set.
- Plan 03-01 phase3_verify.py must check for these exact input paths and the BUDDY_BAZEL_EXECUTE_REFERENCE=1 guard text.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generator rule macros and workflow dispatcher</name>
  <files>tools/bazel/generator_rules.bzl, tools/bazel/phase3_workflow.sh</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
    .planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
    .pre-commit-config.yaml
    tools/bazel/shell_rules.bzl
    tools/bazel/generated_drift.py
    utils/build.py
    utils/logging/generate_overview.py
    utils/persistent_stores/visit_all_struct_fields_generator.py
    utils/translations_and_fonts/generate_pot.sh
    utils/translations_and_fonts/generate_all_fonts.sh
    utils/gen_esp_parts.py
    utils/gen_puppies_descriptor.py
    src/resources/CMakeLists.txt
    src/lang/CMakeLists.txt
  </read_first>
  <action>Create `tools/bazel/generator_rules.bzl` with a `phase3_generated_surface` macro plus convenience macros or call helpers for `phase3_generated_check` and `phase3_generated_update`. Each surface must declare the generated-drift registry ID, evidence class, source-writing behavior, and whether the target is local-smoke, ci-only, reference-only, or bootstrap-required. Include every BAZL-05/D-05/D-06/D-07 surface: `product_profiles`, `option_data`, `resources`, `translations`, `fonts`, `wui_assets`, `esp_blobs`, `puppy_descriptors`, `mmu_descriptors`, `package_metadata`, and `tracked_generated_outputs`. Create `tools/bazel/phase3_workflow.sh` as a `#!/usr/bin/env bash` dispatcher with `set -euo pipefail`. It must dispatch aggregate `generated_check` and `generated_update`, every per-surface check/update label, `generated_assets`, `phase3_verify`, `release_package`, `release_packages`, `artifact_manifest_smoke`, and `reference_release_compare`. Check dispatches must call `generated_drift.py --workspace "$root" --output-dir "${TEST_TMPDIR:-${TMPDIR:-/tmp}}/buddy-phase3-generated-check" --check <surface>` and never write the source tree. Update dispatches must call `generated_drift.py --workspace "$root" --update --check <surface>` and be the only source-writing paths. For CI/reference/bootstrap-heavy surfaces, print explicit evidence classification and verifier-readable target names rather than silently skipping. For `reference_release_compare`, read `tools/bazel/manifests/representative_products.json`, `.planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md`, and `.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md`; normalize the Bazel representative manifest field names plus `evidence_class`/`signing_mode` values; compare those names against the Phase 1 reference-capture surface and baseline matrix text; print any missing fields or evidence classes as explicit failures. The default `reference_release_compare` branch must not execute CMake, `utils/build.py`, firmware builds, signing commands, simulator flows, or live reference commands. Only when `BUDDY_BAZEL_EXECUTE_REFERENCE=1` is set may it dispatch any live CMake/Python reference command per D-14/T-03-04, and that guarded path must remain visibly separate from the default manifest-surface comparison.</action>
  <verify>
    <automated>python3 tools/bazel/generated_drift.py --self-test && rg -n "phase3_generated_surface|phase3_generated_check|phase3_generated_update|product_profiles|option_data|resources|translations|fonts|wui_assets|esp_blobs|puppy_descriptors|mmu_descriptors|package_metadata|tracked_generated_outputs" tools/bazel/generator_rules.bzl tools/bazel/phase3_workflow.sh && rg -n "reference_release_compare|representative_products.json|01-REFERENCE-CAPTURE.md|01-BASELINE-MATRIX.md|BUDDY_BAZEL_EXECUTE_REFERENCE=1" tools/bazel/phase3_workflow.sh</automated>
  </verify>
  <acceptance_criteria>
    - `generator_rules.bzl` names every BAZL-05 generator surface and includes check/update macro paths.
    - `phase3_workflow.sh` has separate check and update branches for every surface and aggregate branches for `generated_check` and `generated_update`.
    - `phase3_workflow.sh` defines `reference_release_compare` with exact inputs `representative_products.json`, `01-REFERENCE-CAPTURE.md`, and `01-BASELINE-MATRIX.md`; default behavior compares manifest field/evidence-class surfaces and does not execute CMake/Python reference commands.
    - `rg -n "BUDDY_BAZEL_EXECUTE_REFERENCE=1" tools/bazel/phase3_workflow.sh` finds only guarded reference-comparison logic, not default generator or release-package execution.
  </acceptance_criteria>
  <done>Generator macros and workflow dispatch cover every required surface with explicit check/update separation and evidence classification.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generator labels, root aliases, and facade recipes</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, justfile</files>
  <read_first>
    tools/bazel/BUILD.bazel
    BUILD.bazel
    justfile
    tools/bazel/generator_rules.bzl
    tools/bazel/phase3_workflow.sh
    tools/bazel/phase3_verify.sh
    tools/bazel/manifests/representative_products.json
  </read_first>
  <action>Update `tools/bazel/BUILD.bazel` to load `generator_rules.bzl` and expose per-surface labels `generated_product_profiles_check`, `generated_product_profiles_update`, `generated_option_data_check`, `generated_option_data_update`, `generated_resources_check`, `generated_resources_update`, `generated_translations_check`, `generated_translations_update`, `generated_fonts_check`, `generated_fonts_update`, `generated_wui_assets_check`, `generated_wui_assets_update`, `generated_esp_blobs_check`, `generated_esp_blobs_update`, `generated_puppy_descriptors_check`, `generated_puppy_descriptors_update`, `generated_mmu_descriptors_check`, `generated_mmu_descriptors_update`, `generated_package_metadata_check`, `generated_package_metadata_update`, `tracked_generated_outputs_check`, and `tracked_generated_outputs_update`. Add aggregate `generated_check`, `generated_update`, and `generated_assets` labels using `phase3_workflow.sh`; keep artifact labels from Plan 03-02 intact. Add or update `reference_release_compare` as a guarded `phase3_workflow.sh` label whose data includes `tools/bazel/manifests/representative_products.json`, `.planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md`, and `.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md`; ensure `phase3_verify.py --require-drift-checks --require-update-targets --require-facade` verifies those exact input paths and the `BUDDY_BAZEL_EXECUTE_REFERENCE=1` guard text. Update root `BUILD.bazel` with aliases for `phase3_verify`, `generated_check`, `generated_update`, `release_package`, and `representative_release_artifacts`. Update `justfile` by adding or correcting `phase3-verify:`, `generated-check:`, `generated-update:`, and `release-package:` recipes so each runs the corresponding Bazel label. Do not add any recipe or default label that sets `BUDDY_BAZEL_EXECUTE_REFERENCE=1`.</action>
  <verify>
    <automated>bazel query "//tools/bazel:generated_check + //tools/bazel:generated_update + //tools/bazel:generated_product_profiles_check + //tools/bazel:generated_product_profiles_update + //tools/bazel:generated_option_data_check + //tools/bazel:generated_option_data_update + //tools/bazel:generated_resources_check + //tools/bazel:generated_resources_update + //tools/bazel:generated_translations_check + //tools/bazel:generated_translations_update + //tools/bazel:generated_fonts_check + //tools/bazel:generated_fonts_update + //tools/bazel:generated_wui_assets_check + //tools/bazel:generated_wui_assets_update + //tools/bazel:generated_esp_blobs_check + //tools/bazel:generated_esp_blobs_update + //tools/bazel:generated_puppy_descriptors_check + //tools/bazel:generated_puppy_descriptors_update + //tools/bazel:generated_mmu_descriptors_check + //tools/bazel:generated_mmu_descriptors_update + //tools/bazel:generated_package_metadata_check + //tools/bazel:generated_package_metadata_update + //tools/bazel:tracked_generated_outputs_check + //tools/bazel:tracked_generated_outputs_update + //tools/bazel:reference_release_compare" && just --list && python3 tools/bazel/phase3_verify.py --require-drift-checks --require-update-targets --require-facade</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query` resolves aggregate labels plus every per-surface generator check and update label: `generated_product_profiles_update`, `generated_option_data_update`, `generated_resources_update`, `generated_translations_update`, `generated_fonts_update`, `generated_wui_assets_update`, `generated_esp_blobs_update`, `generated_puppy_descriptors_update`, `generated_mmu_descriptors_update`, `generated_package_metadata_update`, and `tracked_generated_outputs_update`.
    - `just --list` includes `phase3-verify`, `generated-check`, `generated-update`, and `release-package`.
    - `python3 tools/bazel/phase3_verify.py --require-drift-checks --require-update-targets --require-facade` exits 0.
    - `rg -n "representative_products.json|01-REFERENCE-CAPTURE.md|01-BASELINE-MATRIX.md|BUDDY_BAZEL_EXECUTE_REFERENCE=1" tools/bazel/BUILD.bazel tools/bazel/phase3_workflow.sh` finds the reference comparison inputs and guard text.
    - `rg -n "BUDDY_BAZEL_EXECUTE_REFERENCE=1" justfile tools/bazel/BUILD.bazel` returns no matches outside comments or guarded reference-only command text.
  </acceptance_criteria>
  <done>Bazel and `just` expose all Phase 3 generator, update, artifact facade, and guarded reference labels.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 3 local gate and review evidence wording</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, justfile, tools/bazel/phase3_verify.py, tools/bazel/artifact_packager.py, tools/bazel/artifact_manifest.py, tools/bazel/generated_drift.py, tools/bazel/phase3_workflow.sh</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
    tools/bazel/phase3_verify.py
    tools/bazel/artifact_packager.py
    tools/bazel/artifact_manifest.py
    tools/bazel/generated_drift.py
    tools/bazel/phase3_workflow.sh
    tools/bazel/BUILD.bazel
    justfile
  </read_first>
  <action>Run the complete Phase 3 local validation sequence from revised `03-VALIDATION.md`: helper self-tests, artifact label query/build, generator label query, `bazel run //tools/bazel:phase3_verify`, `just --list`, and `git diff --check`. Review output wording for `local-smoke`, `ci-only`, `reference-only`, `bootstrap-required`, `manual-hardware-required`, and `release-candidate` evidence classes. If any output implies full firmware byte parity, simulator parity, hardware parity, or private-key signing parity, fix the wording and rerun the full local gate. If any command fails because a label, facade recipe, manifest field, guard, output suffix, or read-only check/update boundary is missing, fix the owning file and rerun the full sequence. Do not run `python3 utils/bootstrap.py`, full product firmware builds, signing-sensitive release commands, simulator flows, or hardware checks as part of this task.</action>
  <verify>
    <automated>python3 tools/bazel/phase3_verify.py --quick && python3 tools/bazel/artifact_packager.py --self-test && python3 tools/bazel/artifact_manifest.py --self-test && python3 tools/bazel/generated_drift.py --self-test && bazel query "//tools/bazel/... + //platforms/..." && bazel build //tools/bazel:representative_release_artifacts && bazel run //tools/bazel:phase3_verify && just --list && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The full automated command exits 0.
    - `python3 tools/bazel/phase3_verify.py --all` exits 0 or is equivalent to the `bazel run //tools/bazel:phase3_verify` path.
    - Verification output contains no claim of full firmware byte parity, simulator parity, hardware parity, or private-key signing parity.
    - `rg -n "BEGIN PRIVATE KEY|BEGIN EC PRIVATE KEY|BEGIN RSA PRIVATE KEY" tools/bazel/fixtures tools/bazel/manifests` returns no matches.
  </acceptance_criteria>
  <done>Phase 3 local gate passes, BAZL-05 coverage is explicit, facade recipes work, and local verification remains free of private keys and unguarded heavy reference execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel labels to workflow dispatcher | Bazel target names select generator, update, verifier, artifact facade, and reference-comparison behavior. |
| `justfile` facade to Bazel graph | Developer-facing recipes must route to Bazel-owned Phase 3 labels without enabling guarded reference execution by default. |
| Existing generators to tracked source files | Source-writing generator scripts can mutate committed files unless check mode confines writes and update mode is explicit. |
| Reference comparison target to CMake/Python commands | Reference commands may be heavy or secret-sensitive and must remain opt-in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | `tools/bazel/generator_rules.bzl`, `tools/bazel/phase3_workflow.sh`, `tools/bazel/generated_drift.py` | mitigate | Route check mode through `generated_drift.py --output-dir`; expose source-writing behavior only through explicit `*_update` and `generated_update` labels. |
| T-03-04 | Denial of Service / Information Disclosure | `tools/bazel:reference_release_compare`, `justfile` | mitigate | Keep reference execution behind `BUDDY_BAZEL_EXECUTE_REFERENCE=1`; do not set that variable in default Bazel labels or `just` recipes. |
| T-03-05 | Repudiation | `tools/bazel/phase3_workflow.sh`, evidence-classified generator labels | mitigate | Print explicit evidence classes for local-smoke, ci-only, reference-only, bootstrap-required, manual-hardware-required, and release-candidate surfaces so skipped-heavy checks remain auditable. |
</threat_model>

<verification>
Run the full Phase 3 validation command from `03-VALIDATION.md`:

```bash
python3 tools/bazel/phase3_verify.py --quick
python3 tools/bazel/artifact_packager.py --self-test
python3 tools/bazel/artifact_manifest.py --self-test
python3 tools/bazel/generated_drift.py --self-test
bazel query "//tools/bazel/... + //platforms/..."
bazel build //tools/bazel:representative_release_artifacts
bazel run //tools/bazel:phase3_verify
just --list
git diff --check
```
</verification>

<success_criteria>
- `bazel query "//tools/bazel/... + //platforms/..."` resolves every Phase 3 target.
- `bazel build //tools/bazel:representative_release_artifacts` produces representative release artifact outputs from Plan 03-02.
- `bazel run //tools/bazel:phase3_verify` passes.
- `just --list` exposes `phase3-verify`, `generated-check`, `generated-update`, and `release-package`.
- Every BAZL-05 generator surface has both a check label and a matching update label, including `tracked_generated_outputs_update`.
- `generated_check` is read-only and `generated_update` is the only aggregate source-writing generated-file target.
- `reference_release_compare` reads `tools/bazel/manifests/representative_products.json`, Phase 1 `01-REFERENCE-CAPTURE.md`, and Phase 1 `01-BASELINE-MATRIX.md`; default behavior compares normalized manifest field/evidence-class surfaces and only live reference execution is guarded by `BUDDY_BAZEL_EXECUTE_REFERENCE=1`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-artifact-and-generator-parity/03-03-SUMMARY.md`
</output>
