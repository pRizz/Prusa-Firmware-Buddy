---
phase: 03-artifact-and-generator-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tools/bazel/artifact_rules.bzl
  - tools/bazel/generator_rules.bzl
  - tools/bazel/phase3_verify.sh
  - tools/bazel/phase3_workflow.sh
  - tools/bazel/BUILD.bazel
  - tools/bazel/manifests/representative_products.json
  - tools/bazel/fixtures/firmware_payloads/mini_boot.bin
  - tools/bazel/fixtures/firmware_payloads/mini_noboot.bin
  - tools/bazel/fixtures/firmware_payloads/mk4_boot.bin
  - tools/bazel/fixtures/resources/mini_resource.img
  - tools/bazel/fixtures/auxiliary/auxiliary_firmware_manifest.json
  - BUILD.bazel
  - justfile
autonomous: true
requirements:
  - BAZL-03
  - BAZL-05
requirements_addressed:
  - BAZL-03
  - BAZL-05
must_haves:
  truths:
    - "Developer can query and run Phase 3 Bazel labels for representative release artifacts, generated-output checks, update targets, and verifier coverage."
    - "Developer can use `just generated-check`, `just release-package`, and `just phase3-verify` as the discoverable facade for Phase 3."
    - "Maintainer can inspect representative product and auxiliary manifests with explicit local, CI, reference-only, and release-candidate evidence classification."
    - "Reference-only CMake/Python comparison remains guarded and does not run heavy or secret-sensitive workflows by default."
  artifacts:
    - path: "tools/bazel/artifact_rules.bzl"
      provides: "Bazel artifact manifest/package rule surface"
      contains: "phase3_artifact_manifest"
    - path: "tools/bazel/generator_rules.bzl"
      provides: "Bazel generated-file check/update rule surface"
      contains: "phase3_generated_check"
    - path: "tools/bazel/phase3_workflow.sh"
      provides: "Runnable workflow dispatcher for Phase 3 Bazel labels"
      contains: "reference_release_compare"
    - path: "tools/bazel/manifests/representative_products.json"
      provides: "Representative product and auxiliary package matrix"
      contains: "auxiliary-manifest-only"
    - path: "tools/bazel/BUILD.bazel"
      provides: "Queryable Phase 3 labels"
      contains: "representative_release_artifacts"
  key_links:
    - from: "justfile"
      to: "//tools/bazel:phase3_verify"
      via: "`phase3-verify` recipe"
      pattern: "bazel run //tools/bazel:phase3_verify"
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/phase3_workflow.sh"
      via: "shell_binary targets for generated_check and release_package"
      pattern: "phase3_workflow.sh"
    - from: "tools/bazel/phase3_workflow.sh"
      to: "tools/bazel/artifact_manifest.py"
      via: "representative artifact manifest generation"
      pattern: "artifact_manifest.py"
    - from: "tools/bazel/phase3_workflow.sh"
      to: "tools/bazel/generated_drift.py"
      via: "generated check and update dispatch"
      pattern: "generated_drift.py"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-02T21-03-53
generated_at: 2026-06-02T21:39:34Z
---

<objective>
Wire Phase 3 helper scripts into the Bazel graph and `justfile` facade for representative artifacts, generator drift/update targets, and guarded reference comparison.

Purpose: Convert Phase 2 reference-contract labels into Phase 3 queryable/runnable artifact and generator surfaces while preserving honest evidence classification.
Output: Phase 3 Bazel rules, workflow scripts, representative manifests/fixtures, updated Bazel labels, and updated `just` recipes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
@.planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
@.planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
@.planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-01-SUMMARY.md
@.planning/phases/02-bazel-authority-and-developer-facade/02-VERIFICATION.md
@tools/bazel/phase3_verify.py
@tools/bazel/artifact_manifest.py
@tools/bazel/generated_drift.py
@tools/bazel/shell_rules.bzl
@tools/bazel/reference_contract.sh
@tools/bazel/BUILD.bazel
@BUILD.bazel
@justfile
@.bazelrc
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md

<interfaces>
From `tools/bazel/shell_rules.bzl`:
```python
shell_binary = rule(
    implementation = _shell_binary_impl,
    attrs = {
        "data": attr.label_list(allow_files = True),
        "src": attr.label(allow_single_file = True, mandatory = True),
    },
    executable = True,
)
```

From `.bazelrc`:
```text
common --repo_env=BUDDY_BAZEL_EXECUTE_REFERENCE=0
build --action_env=BUDDY_BAZEL_EXECUTE_REFERENCE=0
```

From Plan 01 helper contract:
```text
python3 tools/bazel/phase3_verify.py --quick
python3 tools/bazel/artifact_manifest.py --self-test
python3 tools/bazel/generated_drift.py --self-test
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 3 rule files, workflow dispatcher, and representative inputs</name>
  <files>tools/bazel/artifact_rules.bzl, tools/bazel/generator_rules.bzl, tools/bazel/phase3_verify.sh, tools/bazel/phase3_workflow.sh, tools/bazel/manifests/representative_products.json, tools/bazel/fixtures/firmware_payloads/mini_boot.bin, tools/bazel/fixtures/firmware_payloads/mini_noboot.bin, tools/bazel/fixtures/firmware_payloads/mk4_boot.bin, tools/bazel/fixtures/resources/mini_resource.img, tools/bazel/fixtures/auxiliary/auxiliary_firmware_manifest.json</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
    .planning/phases/03-artifact-and-generator-parity/03-RESEARCH.md
    .planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-BASELINE-MATRIX.md
    .planning/phases/01-reference-baseline-and-safety-envelope/01-REFERENCE-CAPTURE.md
    tools/bazel/shell_rules.bzl
    tools/bazel/reference_contract.sh
    tools/bazel/phase3_verify.py
    tools/bazel/artifact_manifest.py
    tools/bazel/generated_drift.py
    utils/pack_fw.py
    utils/dfu.py
    utils/gen_puppies_descriptor.py
    utils/mklittlefs.py
  </read_first>
  <behavior>
    - Workflow dispatch supports `phase3_verify`, `generated_assets`, `generated_check`, `generated_update`, `release_package`, `release_packages`, `representative_release_artifacts`, `artifact_manifest_smoke`, and `reference_release_compare`.
    - Representative artifact inputs cover `MINI` boot, `MINI` noboot, `MK4` boot, one resource image, and one auxiliary manifest-only surface.
    - Signing is recorded as `unsigned-local` for local artifact fixtures; no private key path or key material appears in fixtures or manifests.
  </behavior>
  <action>Create `tools/bazel/artifact_rules.bzl` with a `phase3_artifact_manifest` rule or macro that declares manifest outputs and calls `tools/bazel/artifact_manifest.py` with declared artifact, resource, package member, product, printer, board, MCU, bootloader mode, evidence class, and signing mode arguments. Create `tools/bazel/generator_rules.bzl` with `phase3_generated_check` and `phase3_generated_update` macros that use `tools/bazel/generated_drift.py` and keep check mode read-only while update mode is explicit per D-07 and D-09. Create `tools/bazel/phase3_verify.sh` as a thin `#!/usr/bin/env bash` wrapper with `set -euo pipefail` that runs `python3 tools/bazel/phase3_verify.py --all` from `$BUILD_WORKSPACE_DIRECTORY` or the repo root. Create `tools/bazel/phase3_workflow.sh` as a `set -euo pipefail` basename dispatcher. For `generated_check`, run `python3 tools/bazel/generated_drift.py --workspace "$root" --output-dir "${TEST_TMPDIR:-${TMPDIR:-/tmp}}/buddy-phase3-generated-check" --check cmake_presets --check logging_components --check visit_all_struct_fields`. For `generated_update`, run the same checks with `--update`. For `release_package`, `release_packages`, `representative_release_artifacts`, and `artifact_manifest_smoke`, create a temporary output directory and call `artifact_manifest.py` for every entry in `tools/bazel/manifests/representative_products.json`; print the output manifest paths and evidence classes. For `reference_release_compare`, print the reference command contract and require `BUDDY_BAZEL_EXECUTE_REFERENCE=1` before executing any CMake/Python reference command, preserving the Phase 2 guard per D-14 and T-03-04. Create `representative_products.json` with entries for `MINI/BUDDY/STM32F407VG` boot, `MINI/BUDDY/STM32F407VG` noboot, `MK4/XBUDDY/STM32F427ZI` boot, and `XBUDDY_EXTENSION/STM32H503CBU7` auxiliary manifest-only evidence. Create deterministic ASCII fixture payload files with content beginning `BUDDY_PHASE3_PACKAGE_SURFACE_FIXTURE` and no secret-like material. Create `auxiliary_firmware_manifest.json` with `evidence_class: "reference-only"` and `scope: "auxiliary-manifest-only"` so D-02 stays visible without claiming auxiliary runtime parity.</action>
  <verify>
    <automated>python3 tools/bazel/artifact_manifest.py --self-test && python3 tools/bazel/generated_drift.py --self-test && python3 tools/bazel/phase3_verify.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase3_artifact_manifest|artifact_manifest.py|unsigned-local" tools/bazel/artifact_rules.bzl` finds the artifact rule surface, helper invocation, and local signing mode.
    - `rg -n "phase3_generated_check|phase3_generated_update|generated_drift.py|--update" tools/bazel/generator_rules.bzl` finds check/update macros and helper invocation.
    - `rg -n "reference_release_compare|BUDDY_BAZEL_EXECUTE_REFERENCE|generated_update|representative_release_artifacts|artifact_manifest_smoke" tools/bazel/phase3_workflow.sh` finds every dispatcher branch and reference guard.
    - `python3 tools/bazel/phase3_verify.py --quick` exits 0 and prints `Phase 3 quick verification passed`.
  </acceptance_criteria>
  <done>Phase 3 rule files, workflow wrapper, representative product manifest, resource fixture, package-surface firmware fixtures, and auxiliary manifest-only fixture exist and pass helper self-tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Bazel labels and `just` facade</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, justfile</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-CONTEXT.md
    .planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
    tools/bazel/BUILD.bazel
    BUILD.bazel
    justfile
    tools/bazel/shell_rules.bzl
    tools/bazel/phase3_verify.sh
    tools/bazel/phase3_workflow.sh
    tools/bazel/manifests/representative_products.json
  </read_first>
  <action>Update `tools/bazel/BUILD.bazel` to load `artifact_rules.bzl`, `generator_rules.bzl`, and `shell_binary`. Add `shell_binary(name = "phase3_verify", src = "phase3_verify.sh", data = ["phase3_verify.py", "artifact_manifest.py", "generated_drift.py", "artifact_rules.bzl", "generator_rules.bzl", "phase3_workflow.sh", "manifests/representative_products.json"])`. Change existing `generated_assets`, `generated_check`, `release_package`, and `release_packages` labels to use `phase3_workflow.sh` instead of `reference_contract.sh`, with data including the Phase 3 helper scripts, rule files, representative manifest, and fixtures. Add new public labels `generated_update`, `representative_release_artifacts`, `artifact_manifest_smoke`, and `reference_release_compare` using `phase3_workflow.sh`. Keep `bootstrap`, `build_firmware`, `rust_firmware`, `retained_foreign_code`, `host_tools`, `test_host`, `unit_tests`, `simulator_inputs`, `format`, `lint`, and `simulator_parity` on `reference_contract.sh` unless a Phase 3 helper directly owns that workflow. Update root `BUILD.bazel` with `alias(name = "phase3_verify", actual = "//tools/bazel:phase3_verify")`. Update `justfile` by adding `phase3-verify:` running `bazel run //tools/bazel:phase3_verify`; keep `generated-check:` running `bazel run //tools/bazel:generated_check`; keep `release-package:` running `bazel run //tools/bazel:release_package`; add `generated-update:` running `bazel run //tools/bazel:generated_update`. Do not add any recipe that sets `BUDDY_BAZEL_EXECUTE_REFERENCE=1` by default.</action>
  <verify>
    <automated>bazel query "//tools/bazel/... + //platforms/..." && bazel run //tools/bazel:phase3_verify && just --list && python3 tools/bazel/phase3_verify.py --require-facade</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query "//tools/bazel:phase3_verify + //tools/bazel:generated_check + //tools/bazel:generated_update + //tools/bazel:release_package + //tools/bazel:release_packages + //tools/bazel:representative_release_artifacts + //tools/bazel:artifact_manifest_smoke + //tools/bazel:reference_release_compare"` exits 0.
    - `bazel run //tools/bazel:phase3_verify` exits 0 and prints Phase 3 verification success text.
    - `just --list` includes `phase3-verify`, `generated-check`, `generated-update`, and `release-package`.
    - `rg -n "BUDDY_BAZEL_EXECUTE_REFERENCE=1" justfile tools/bazel/BUILD.bazel tools/bazel/phase3_workflow.sh` returns no default facade or label path that enables reference execution; if the guard string appears, it is only in explanatory or conditional guard text.
  </acceptance_criteria>
  <done>Bazel exposes all Phase 3 labels, `just` exposes the Phase 3 facade, and guarded reference comparison remains opt-in.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 3 local gate and review classification output</name>
  <files>tools/bazel/BUILD.bazel, justfile, tools/bazel/phase3_verify.py, tools/bazel/artifact_manifest.py, tools/bazel/generated_drift.py, tools/bazel/phase3_workflow.sh, tools/bazel/manifests/representative_products.json</files>
  <read_first>
    .planning/phases/03-artifact-and-generator-parity/03-VALIDATION.md
    tools/bazel/phase3_verify.py
    tools/bazel/artifact_manifest.py
    tools/bazel/generated_drift.py
    tools/bazel/phase3_workflow.sh
    tools/bazel/BUILD.bazel
    justfile
  </read_first>
  <action>Run the complete Phase 3 local validation sequence from `03-VALIDATION.md`: `python3 tools/bazel/phase3_verify.py --quick`, `python3 tools/bazel/artifact_manifest.py --self-test`, `python3 tools/bazel/generated_drift.py --self-test`, `bazel query "//tools/bazel/... + //platforms/..."`, `bazel run //tools/bazel:phase3_verify`, `just --list`, and `git diff --check`. Review the command output and, if any helper reports bootstrap-required, CI-only, reference-only, manual-hardware-required, or release-candidate evidence classes, ensure the wording is explicit and not a hidden local pass/fail requirement. If any command fails because Plan 02 wiring omitted a label, facade recipe, manifest field, guard, or read-only check boundary, fix the corresponding file and rerun the full sequence. Do not run `python3 utils/bootstrap.py`, full product firmware builds, signing-sensitive release commands, simulator flows, or hardware checks as part of this task.</action>
  <verify>
    <automated>python3 tools/bazel/phase3_verify.py --quick && python3 tools/bazel/artifact_manifest.py --self-test && python3 tools/bazel/generated_drift.py --self-test && bazel query "//tools/bazel/... + //platforms/..." && bazel run //tools/bazel:phase3_verify && just --list && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The full automated command exits 0.
    - `python3 tools/bazel/phase3_verify.py --all` exits 0 or is equivalent to the `bazel run //tools/bazel:phase3_verify` path.
    - Verification output contains no claim of full firmware byte parity, simulator parity, hardware parity, or private-key signing parity.
    - `rg -n "BEGIN PRIVATE KEY|BEGIN EC PRIVATE KEY|BEGIN RSA PRIVATE KEY" tools/bazel/fixtures tools/bazel/manifests` returns no matches.
  </acceptance_criteria>
  <done>Phase 3 local gate passes, evidence classes are explicit, and local verification remains free of private keys and unguarded heavy reference execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel labels to workflow dispatcher | Bazel target names select artifact, generator, update, verifier, and reference-comparison behavior. |
| `justfile` facade to Bazel graph | Developer-facing recipes must route to Bazel-owned Phase 3 labels without enabling guarded reference execution by default. |
| Representative fixtures to release evidence | Local package-surface fixtures must be labeled as local smoke evidence and must not imply runtime or signing parity. |
| Reference comparison target to CMake/Python commands | Reference commands may be heavy or secret-sensitive and must remain opt-in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `tools/bazel/fixtures/`, `tools/bazel/manifests/` | mitigate | Use deterministic non-secret fixture contents, `unsigned-local` signing mode, and verifier scans for private-key PEM markers. |
| T-03-02 | Tampering | `tools/bazel/generator_rules.bzl`, `tools/bazel/phase3_workflow.sh` | mitigate | Route check mode through `generated_drift.py --output-dir`; expose source-writing behavior only through `generated_update`. |
| T-03-03 | Spoofing/Tampering | `tools/bazel/artifact_rules.bzl`, representative manifests | mitigate | Build manifests from declared files and validated product metadata; run `artifact_manifest.py` self-test and verifier manifest checks. |
| T-03-04 | Denial of Service / Information Disclosure | `tools/bazel:reference_release_compare`, `justfile` | mitigate | Keep reference execution behind `BUDDY_BAZEL_EXECUTE_REFERENCE=1`; do not set that variable in default Bazel labels or `just` recipes. |
</threat_model>

<verification>
Run the full Phase 3 validation command from `03-VALIDATION.md`:

```bash
bazel query "//tools/bazel/... + //platforms/..." && bazel run //tools/bazel:phase3_verify && just --list
```

Then run:

```bash
python3 tools/bazel/phase3_verify.py --quick
python3 tools/bazel/artifact_manifest.py --self-test
python3 tools/bazel/generated_drift.py --self-test
git diff --check
```
</verification>

<success_criteria>
- `bazel query "//tools/bazel/... + //platforms/..."` resolves every Phase 3 target.
- `bazel run //tools/bazel:phase3_verify` passes.
- `just --list` exposes `phase3-verify`, `generated-check`, `generated-update`, and `release-package`.
- `generated_check` is read-only and `generated_update` is the only source-writing generated-file target.
- `reference_release_compare` is visibly guarded by `BUDDY_BAZEL_EXECUTE_REFERENCE=1`.
- Local artifact fixtures produce normalized manifests with explicit `local-smoke`, `reference-only`, or `ci-only` evidence classification and no private signing key material.
</success_criteria>

<output>
After completion, create `.planning/phases/03-artifact-and-generator-parity/03-02-SUMMARY.md`
</output>
