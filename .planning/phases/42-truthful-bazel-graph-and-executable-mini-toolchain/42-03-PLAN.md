---
phase: 42-truthful-bazel-graph-and-executable-mini-toolchain
plan: 03
type: execute
wave: 3
depends_on:
  - 42-02
files_modified:
  - tools/bazel/phase42/BUILD.bazel
  - tools/bazel/phase42/arm_link_smoke.rs
  - tools/bazel/phase42/arm_link_smoke.ld
  - tools/bazel/phase42/arm_link_smoke.bzl
  - tools/bazel/phase42/arm_link_smoke_test.py
autonomous: true
requirements:
  - BUILD-03
  - TOOL-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-08-03T19-34-09
generated_at: 2026-08-03T20:16:34.924Z
must_haves:
  truths:
    - "Canonical Linux x86_64 builds a non-empty Cortex-M4 hard-float ELF, GNU map, and JSON report using the resolved Rust and Arm GNU executables."
    - "Inspection proves ARM/Cortex-M4, EABI hard-float, FPv4-SP-D16-compatible attributes, and the _phase42_smoke_entry symbol; metadata/version output alone cannot pass."
    - "Darwin exact-target invocation fails during analysis through HostPolicyInfo with detected host and canonical Linux remedy and creates no action/output."
  artifacts:
    - path: "tools/bazel/phase42/arm_link_smoke.rs"
      provides: "Allocation-free no_std smoke entry"
      contains: "_phase42_smoke_entry"
    - path: "tools/bazel/phase42/arm_link_smoke.ld"
      provides: "Dedicated FLASH/RAM linker contract"
      contains: "ENTRY(_phase42_smoke_entry)"
    - path: "tools/bazel/phase42/arm_link_smoke.bzl"
      provides: "Declared compile, link, inspect, map, and report actions"
      contains: "arm_link_smoke"
    - path: "tools/bazel/phase42/arm_link_smoke_test.py"
      provides: "Output identity, ABI, provenance, and host-policy tests"
      contains: "thumbv7em-none-eabihf"
  key_links:
    - from: "tools/bazel/phase42/arm_link_smoke.bzl"
      to: "tools/bazel/toolchains/embedded_toolchain.bzl"
      via: "HostPolicyInfo-first access to EmbeddedToolchainInfo"
      pattern: "phase42_qualification_toolchain_type"
    - from: "tools/bazel/phase42/arm_link_smoke.bzl"
      to: "tools/bazel/phase42/arm_link_smoke.rs"
      via: "Rust object action followed by Arm GNU final link"
      pattern: "--target=thumbv7em-none-eabihf"
---

<objective>
Prove the executable qualification toolchain by producing and inspecting a genuine ARM hard-float link-smoke artifact.

Purpose: Replace metadata-only evidence with real target work while retaining deterministic no-action Darwin rejection.
Output: Rust smoke source, linker script, Bazel rule/actions, ELF/map/report outputs, and exact smoke/host-policy tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-CONTEXT.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-RESEARCH.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-VALIDATION.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-02-SUMMARY.md
@tools/bazel/toolchains/embedded_toolchain.bzl
@tools/bazel/toolchains/BUILD.bazel
@tools/bazel/phase42/platform_contract.bzl
@platforms/BUILD.bazel
@cmake/GccArmNoneEabi.cmake
@Cargo.toml

<interfaces>
The resolved qualification toolchain provides HostPolicyInfo on all declared hosts and EmbeddedToolchainInfo only when `qualifies=True`. Consumers must fail on `host_policy.diagnostic` before reading the embedded provider.
</interfaces>
</context>

<tasks>

<task id="42-03-01" type="auto" tdd="true">
  <name>Task 1: Build and inspect a genuine Cortex-M4 hard-float link smoke</name>
  <files>tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/arm_link_smoke.rs, tools/bazel/phase42/arm_link_smoke.ld, tools/bazel/phase42/arm_link_smoke.bzl, tools/bazel/phase42/arm_link_smoke_test.py</files>
  <read-first>
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-02-SUMMARY.md
    - tools/bazel/toolchains/embedded_toolchain.bzl
    - tools/bazel/toolchains/BUILD.bazel
    - tools/bazel/phase42/platform_contract.bzl
    - platforms/BUILD.bazel
    - cmake/GccArmNoneEabi.cmake
    - Cargo.toml
  </read-first>
  <behavior>
    - Test 1: canonical Linux plus `--config=mini --noskip_incompatible_explicit_targets` creates non-empty `.elf`, `.map`, and JSON report outputs.
    - Test 2: pinned Arm inspection proves ARM/Cortex-M4, EABI hard-float, FPv4-SP-D16-compatible attributes, and `_phase42_smoke_entry`.
    - Test 3: actions use resolved Rust 1.85.0 and Arm GNU 13.2.Rel1 and are not copy, metadata-only, CMake, Cargo, fixture, or reference actions.
    - Test 4: Darwin fails before target work with detected OS/architecture and `canonical Linux x86_64 CI/container`.
  </behavior>
  <action>
Write failing output/provenance/host-policy tests first. Add allocation-free `#![no_std]`, `#![no_main]` Rust source exporting `_phase42_smoke_entry() -> !` and a spinning panic handler. Add a dedicated linker script with `ENTRY(_phase42_smoke_entry)` and declared FLASH/RAM regions. Label every report `phase42-arm-link-smoke`, never firmware, safe-boot, or accepted ELF.

Implement `arm_link_smoke` with only `phase42_qualification_toolchain_type`. Read HostPolicyInfo first; on Darwin call `fail()` with detected `Darwin-<arch>` and `use canonical Linux x86_64 CI/container` before accessing the absent EmbeddedToolchainInfo, registering an action, or declaring a successful provider/output. On Linux, invoke resolved Rust 1.85.0 with `--target=thumbv7em-none-eabihf --emit=obj -C panic=abort -C opt-level=s`; link through resolved Arm GNU 13.2.Rel1 using `-nostdlib -mthumb -mcpu=cortex-m4 -mfloat-abi=hard -mfpu=fpv4-sp-d16 -Wl,--gc-sections -Wl,-Map,$MAP -T $LINKER`.

Inspect with resolved readelf, objdump, nm, and size. Produce Bazel-declared `.elf`, `.map`, and JSON report outputs containing the exact target label, canonical platform, triple, tool versions, output paths, ARM machine identity, and hard-float attributes. Do not execute generated output. Fail the rule/test if inspection cannot prove the ABI or symbol. The rules_python Python 3.12.10 test must also inspect aquery for external-repository executable paths and reject ambient python, Cargo, CMake, .dependencies, fixtures, archives, reference scripts, or copy-only producer edges.
  </action>
  <acceptance-criteria>
    - `//tools/bazel/phase42:arm_link_smoke` is compatible only with the canonical five-value MINI tuple.
    - Linux produces non-empty ELF/map/report paths naming Rust 1.85.0, Arm GNU 13.2.Rel1, and thumbv7em-none-eabihf.
    - Removing any target/ABI/link flag or expected symbol makes the focused test fail.
    - aquery shows distinct declared Rust compile, Arm link, inspection, and report actions with no forbidden provenance.
    - Darwin exact-target build exits nonzero with detected architecture and canonical Linux remedy and no smoke action/output.
    - `arm_link_smoke_tests` is a rules_python Python 3.12.10 test with no ambient interpreter edge.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:arm_link_smoke_tests &amp;&amp; if [ "$(uname -s)-$(uname -m)" = "Linux-x86_64" ]; then bazel build --config=mini --noskip_incompatible_explicit_targets //tools/bazel/phase42:arm_link_smoke; else ! bazel build --config=mini --noskip_incompatible_explicit_targets //tools/bazel/phase42:arm_link_smoke 2&gt;/tmp/phase42-darwin-smoke.err &amp;&amp; grep -E 'Darwin.*canonical Linux x86_64 CI/container' /tmp/phase42-darwin-smoke.err; fi</automated>
  </verify>
  <done>Canonical Linux proves real Rust/Arm target work; Darwin supplies only deterministic rejection evidence and no executable output.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Bazel rule to resolved executable providers | A rule could bypass toolchain executables for ambient or reference tools. |
| Compile/link output to qualification report | Metadata or copied fixtures could be presented as genuine target work. |
| Host policy to action registration | Darwin could register actions before rejection. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation and automated proof |
| --- | --- | --- | --- | --- | --- |
| T-42-02 | Tampering / Elevation | HIGH | compile/link actions | mitigate | aquery requires resolved Rust/Arm and rules_python paths and rejects ambient/reference/Cargo/CMake/fixture edges. |
| T-42-05 | Repudiation | HIGH | ELF/map/report evidence | mitigate | Binary inspection, non-empty outputs, symbol/ABI assertions, exact identities, and output paths. |
| T-42-06 | Spoofing | HIGH | Darwin smoke path | mitigate | Exact-host tests prove HostPolicyInfo failure precedes provider access/action creation. |
</threat-model>

<verification>
1. Run `bazel test //tools/bazel/phase42:arm_link_smoke_tests`.
2. On Linux, build the exact smoke with canonical MINI config and inspect emitted ELF/map/report paths.
3. On Darwin, require exact-host rejection and verify no action/output exists.
4. Run `git diff --check`.
</verification>

<success-criteria>
- D-03 route behavior and D-04 genuine ARM hard-float output are fully implemented.
- TOOL-01 evidence comes only from resolved executable providers.
- BUILD-03 exact canonical platform produces the sole positive smoke.
- Every HIGH provenance/output/host threat has direct automated proof.
</success-criteria>

<decision-coverage>
| Decision | Task | Coverage |
| --- | --- | --- |
| D-03 (smoke route) | 42-03-01 | Full |
| D-04 | 42-03-01 | Full |
| D-15 (smoke/output portion) | 42-03-01 | Full |
</decision-coverage>

<output>
After completion, create `.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-03-SUMMARY.md`.
</output>
