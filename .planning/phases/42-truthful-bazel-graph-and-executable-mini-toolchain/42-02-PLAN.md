---
phase: 42-truthful-bazel-graph-and-executable-mini-toolchain
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - MODULE.bazel
  - platforms/BUILD.bazel
  - tools/bazel/toolchains/BUILD.bazel
  - tools/bazel/toolchains/embedded_toolchain.bzl
  - tools/bazel/phase42/BUILD.bazel
  - tools/bazel/phase42/platform_contract.bzl
  - tools/bazel/phase42/host_policy.bzl
  - tools/bazel/phase42/embedded_toolchain_contract_test.py
  - tools/bazel/phase42/host_policy_contract_test.py
autonomous: true
requirements:
  - BUILD-03
  - TOOL-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-08-03T19-34-09
generated_at: 2026-08-03T20:16:34.924Z
must_haves:
  truths:
    - "The canonical MINI platform contains runtime embedded + MINI + BUDDY + STM32F407VG + thumbv7em-none-eabihf before executable toolchains are registered."
    - "Linux x86_64 resolves HostPolicyInfo plus EmbeddedToolchainInfo from pinned repositories; Darwin resolves only a non-qualifying HostPolicyInfo and cannot register actions."
    - "Reference metadata, missing executable fields, unsupported hosts, and partial constraints cannot satisfy the qualification toolchain contract."
  artifacts:
    - path: "platforms/BUILD.bazel"
      provides: "Canonical rust_target setting, hard-float value, and MINI platform tuple"
      contains: "thumbv7em_none_eabihf"
    - path: "tools/bazel/phase42/platform_contract.bzl"
      provides: "Exact five-value canonical allowlist"
      contains: "PHASE42_MINI_CONSTRAINTS"
    - path: "tools/bazel/phase42/host_policy.bzl"
      provides: "Qualifying/non-qualifying host contract"
      contains: "HostPolicyInfo"
    - path: "tools/bazel/toolchains/embedded_toolchain.bzl"
      provides: "Executable qualification provider and implementations"
      contains: "EmbeddedToolchainInfo"
  key_links:
    - from: "tools/bazel/toolchains/embedded_toolchain.bzl"
      to: "tools/bazel/toolchains/embedded_repositories.bzl"
      via: "declared executable FilesToRunProvider labels"
      pattern: "EmbeddedToolchainInfo"
    - from: "tools/bazel/toolchains/BUILD.bazel"
      to: "tools/bazel/phase42/platform_contract.bzl"
      via: "Linux target compatibility and registration"
      pattern: "PHASE42_MINI_CONSTRAINTS"
    - from: "MODULE.bazel"
      to: "tools/bazel/toolchains/BUILD.bazel"
      via: "executable qualification toolchain registration after canonical constraints exist"
      pattern: "register_toolchains"
---

<objective>
Create the canonical hard-float MINI platform and executable qualification toolchain contract.

Purpose: Ensure host policy is always inspectable before embedded executables, while only canonical Linux x86_64 can receive those executables.
Output: Canonical platform constraints, HostPolicyInfo, EmbeddedToolchainInfo, Linux/Darwin implementations, registrations, and focused contract tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-CONTEXT.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-RESEARCH.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-VALIDATION.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-01-SUMMARY.md
@MODULE.bazel
@platforms/BUILD.bazel
@tools/bazel/toolchains/BUILD.bazel
@tools/bazel/toolchains/reference_toolchain.bzl
@tools/bazel/toolchains/embedded_repositories.bzl

<interfaces>
```starlark
EmbeddedToolchainInfo = provider(fields = {
    "rustc": "rules_rust-resolved Rust 1.85.0 compiler",
    "arm_gcc": "Arm GNU 13.2.Rel1 driver",
    "arm_objcopy": "Arm GNU objcopy",
    "arm_objdump": "Arm GNU objdump",
    "arm_readelf": "Arm GNU readelf",
    "arm_nm": "Arm GNU nm",
    "arm_size": "Arm GNU size",
    "python": "rules_python-resolved Python 3.12.10 interpreter",
    "mini404": "Mini404 0.9.10 executable",
    "versions": "locked identity dictionary",
    "target_triple": "thumbv7em-none-eabihf",
})

HostPolicyInfo = provider(fields = {
    "detected_os": "linux or darwin",
    "detected_arch": "x86_64 or arm64",
    "qualifies": "true only for linux/x86_64",
    "diagnostic": "detected host plus canonical Linux x86_64 CI/container remedy",
})
```
</interfaces>
</context>

<tasks>

<task id="42-02-01" type="auto" tdd="true">
  <name>Task 1: Define the canonical platform and executable qualification toolchain</name>
  <files>MODULE.bazel, platforms/BUILD.bazel, tools/bazel/toolchains/BUILD.bazel, tools/bazel/toolchains/embedded_toolchain.bzl, tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/platform_contract.bzl, tools/bazel/phase42/host_policy.bzl, tools/bazel/phase42/embedded_toolchain_contract_test.py, tools/bazel/phase42/host_policy_contract_test.py</files>
  <read-first>
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-01-SUMMARY.md
    - MODULE.bazel
    - platforms/BUILD.bazel
    - tools/bazel/toolchains/BUILD.bazel
    - tools/bazel/toolchains/reference_toolchain.bzl
    - tools/bazel/toolchains/embedded_repositories.bzl
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-RESEARCH.md
  </read-first>
  <behavior>
    - Test 1: the canonical platform and PHASE42_MINI_CONSTRAINTS contain exactly runtime embedded, MINI, BUDDY, STM32F407VG, and hard-float Rust target values.
    - Test 2: Linux x86_64 qualification returns HostPolicyInfo(qualifies=True) and every EmbeddedToolchainInfo field from declared repositories.
    - Test 3: Darwin x86_64/arm64 return only HostPolicyInfo(qualifies=False) with detected host and Linux remedy and never EmbeddedToolchainInfo.
    - Test 4: reference providers, missing executables/version fields, partial target tuples, or Darwin executable compatibility fail contract tests.
  </behavior>
  <action>
Implement D-03 and the canonical half of D-05 before registration. Add `constraint_setting(name = "rust_target")`, `constraint_value(name = "thumbv7em_none_eabihf", constraint_setting = ":rust_target")`, and that value to `//platforms:mini_buddy_stm32f407vg`. Create `PHASE42_MINI_CONSTRAINTS` containing exactly `//platforms:runtime_embedded`, `//platforms:printer_mini`, `//platforms:board_buddy`, `//platforms:mcu_stm32f407vg`, and `//platforms:thumbv7em_none_eabihf`. Do not create soft-float or wrong-triple fixtures here; Plan 42-04 owns them.

Write the contract tests first. Define the providers exactly as shown above and one `phase42_qualification_toolchain_type`. The Linux x86_64 implementation returns both qualifying HostPolicyInfo and EmbeddedToolchainInfo backed only by Plan 42-01 repositories and the complete canonical constraints. Darwin x86_64 and arm64 implementations return only non-qualifying HostPolicyInfo with `unsupported embedded qualification host: detected Darwin-<arch>; use canonical Linux x86_64 CI/container`; they expose no executable and no EmbeddedToolchainInfo. Register all three implementations only after the canonical platform exists. Keep rules_python registration separate.

Every consuming rule must require this single qualification type, inspect HostPolicyInfo first, call `fail(host_policy.diagnostic)` on non-qualifying hosts, and only then access EmbeddedToolchainInfo. This prevents generic missing-toolchain errors and guarantees zero Darwin actions. Executable fields must be declared FilesToRunProvider labels, never assembled path strings. Keep reference_toolchain descriptive-only and unable to return EmbeddedToolchainInfo.
  </action>
  <acceptance-criteria>
    - `platforms/BUILD.bazel` contains `rust_target`, `thumbv7em_none_eabihf`, and the canonical MINI platform contains all five values.
    - `embedded_toolchain.bzl` defines every EmbeddedToolchainInfo and HostPolicyInfo field exactly once.
    - Linux x86_64 registration requires the exact target tuple and returns declared Rust, Arm, Python, and Mini404 executables.
    - Darwin implementations include detected architecture and exact Linux remedy, export no EmbeddedToolchainInfo, and register no action.
    - rules_python Python 3.12.10 remains the only interpreter for Phase 42 Python test/binary targets.
    - Deleting an executable, changing a version, broadening constraints, adding Darwin embedded compatibility, or connecting a reference provider fails a focused mutation.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:embedded_toolchain_contract_tests //tools/bazel/phase42:host_policy_contract_tests</automated>
  </verify>
  <done>The canonical platform exists before registration, Linux has executable providers, and Darwin has deterministic non-qualifying host policy without executable reachability.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Target/host constraints to toolchain resolution | Partial constraints could resolve a wrong ABI or execution host. |
| Host policy to embedded provider access | A consumer could access executables before rejecting Darwin. |
| Reference metadata to qualification | Descriptive providers could masquerade as executable tools. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation and automated proof |
| --- | --- | --- | --- | --- | --- |
| T-42-03 | Spoofing / Tampering | HIGH | platform and toolchain constraints | mitigate | Exact five-value canonical contract and partial/broadened mutation tests. |
| T-42-06 | Spoofing / Elevation | HIGH | host policy/provider boundary | mitigate | Provider-first contract tests prove Darwin returns only diagnostic HostPolicyInfo and registers no action. |
| T-42-02 | Tampering | HIGH | executable fields | mitigate | FilesToRunProvider-only fields and mutations reject strings, missing fields, reference providers, and ambient lookup. |
</threat-model>

<verification>
1. Run `bazel test //tools/bazel/phase42:embedded_toolchain_contract_tests //tools/bazel/phase42:host_policy_contract_tests`.
2. Inspect registered toolchains for exact Linux execution and canonical target constraints.
3. Confirm Darwin contracts contain the detected host and Linux remedy but no EmbeddedToolchainInfo/action.
4. Run `git diff --check`.
</verification>

<success-criteria>
- D-03 and canonical D-05 behavior are fully implemented.
- BUILD-03 platform/toolchain resolution fails closed.
- TOOL-01 executables remain declared, pinned inputs.
- Every HIGH platform/provider/host threat has focused automated proof.
</success-criteria>

<decision-coverage>
| Decision | Task | Coverage |
| --- | --- | --- |
| D-03 (provider/host foundation) | 42-02-01 | Full |
| D-05 (canonical tuple) | 42-02-01 | Full |
| D-06 (provider-chain foundation) | 42-02-01 | Full |
</decision-coverage>

<output>
After completion, create `.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-02-SUMMARY.md`.
</output>
