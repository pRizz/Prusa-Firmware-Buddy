---
phase: 42-truthful-bazel-graph-and-executable-mini-toolchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .bazelversion
  - MODULE.bazel
  - MODULE.bazel.lock
  - tools/bazel/toolchains/embedded_repositories.bzl
  - tools/bazel/phase42/BUILD.bazel
  - tools/bazel/phase42/toolchain_provenance_test.py
autonomous: true
requirements:
  - TOOL-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-08-03T19-34-09
generated_at: 2026-08-03T20:16:34.924Z
must_haves:
  truths:
    - "Bazel 9.2.0, rules_rust 0.71.3, Rust 1.85.0, rules_cc 0.2.22, rules_python 2.2.0, Python 3.12.10, Arm GNU 13.2.Rel1, Mini404 0.9.10, and thumbv7em-none-eabihf are exact declarations."
    - "Arm GNU and Mini404 Linux x86_64 archives have exact non-placeholder SHA-256 values and no PATH, local-repository, Darwin, or .dependencies fallback."
    - "Ordinary verification resolves Phase 42 Python tests through rules_python Python 3.12.10 and leaves MODULE.bazel.lock unchanged."
  artifacts:
    - path: ".bazelversion"
      provides: "Bazel 9.2.0 pin"
      contains: "9.2.0"
    - path: "MODULE.bazel"
      provides: "Exact rules, Rust target, Python toolchain, and repository-extension declarations"
      contains: "rules_python"
    - path: "tools/bazel/toolchains/embedded_repositories.bzl"
      provides: "Checksum-backed Linux x86_64 Arm GNU and Mini404 repositories"
      contains: "sha256"
    - path: "tools/bazel/phase42/toolchain_provenance_test.py"
      provides: "Declared-Python version, checksum, fallback, interpreter, and lock audit"
      contains: "3.12.10"
  key_links:
    - from: "MODULE.bazel"
      to: "tools/bazel/toolchains/embedded_repositories.bzl"
      via: "use_extension/use_repo"
      pattern: "embedded_repositories"
    - from: "tools/bazel/phase42/BUILD.bazel"
      to: "tools/bazel/phase42/toolchain_provenance_test.py"
      via: "rules_python py_test"
      pattern: "toolchain_provenance_tests"
---

<objective>
Pin every Phase 42 tool and checksum-backed repository before executable platform/toolchain work begins.

Purpose: Establish deterministic provenance and a stable Bzlmod lock boundary that later plans may consume but cannot replace with ambient tools.
Output: Exact Bazel/module/tool declarations, Linux repository archives, stable lock state, and a rules_python provenance suite.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-CONTEXT.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-RESEARCH.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-VALIDATION.md
@.planning/research/STACK.md
@MODULE.bazel
@MODULE.bazel.lock
@utils/bootstrap.py
</context>

<tasks>

<task id="42-01-01" type="auto" tdd="true">
  <name>Task 1: Pin repositories, language toolchains, and immutable provenance</name>
  <files>.bazelversion, MODULE.bazel, MODULE.bazel.lock, tools/bazel/toolchains/embedded_repositories.bzl, tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/toolchain_provenance_test.py</files>
  <read-first>
    - .bazelversion (if present)
    - MODULE.bazel
    - MODULE.bazel.lock
    - utils/bootstrap.py
    - .planning/research/STACK.md
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-CONTEXT.md
  </read-first>
  <behavior>
    - Test 1: every exact version, target triple, URL, SHA-256, and strip prefix matches the Phase 42 contract.
    - Test 2: blank/placeholder hashes, version drift, Darwin archives, local repositories, PATH lookup, .dependencies, Cargo, CMake outputs, fixtures, archives, or planning artifacts fail provenance.
    - Test 3: the provenance suite resolves the registered rules_python Python 3.12.10 interpreter and rejects ambient python3.
    - Test 4: a second ordinary resolution/test run leaves MODULE.bazel.lock byte-for-byte unchanged.
  </behavior>
  <action>
Implement D-01 and D-02 exactly. Set `.bazelversion` to only `9.2.0`. In `MODULE.bazel`, declare `rules_rust=0.71.3`, `rules_cc=0.2.22`, and `rules_python=2.2.0`; declare rules_rust edition 2024 with Rust `1.85.0` and extra target `thumbv7em-none-eabihf`; declare a hermetic Python `3.12.10` toolchain. Declare the repository extension here, but leave executable toolchain registration and the canonical platform to Plan 42-02.

Create `embedded_repositories.bzl` as a Bzlmod extension exposing only Linux x86_64 Arm GNU `13.2.Rel1` and Mini404 `0.9.10` repositories. Use the official URLs already named in `utils/bootstrap.py`, exact non-empty SHA-256 values, and fixed strip prefixes. Do not add Darwin archives, local_repository/new_local_repository, PATH probing, `.dependencies`, host Cargo, or CMake fallback branches.

Write `toolchain_provenance_test.py` first and expose it only as rules_python `py_test(name = "toolchain_provenance_tests")` using Python 3.12.10. Assert all exact versions, URLs, checksums, target triple, interpreter identity, extension names, and stable lock state; add mutation cases for each forbidden fallback class. Intentionally regenerate `MODULE.bazel.lock` once using the explicit lock-update workflow, then snapshot/compare it during ordinary verification. Direct host `python3` invocation is never qualification evidence.
  </action>
  <acceptance-criteria>
    - `.bazelversion` is exactly `9.2.0` followed by a newline.
    - `MODULE.bazel` contains `0.71.3`, `0.2.22`, `2.2.0`, `1.85.0`, `3.12.10`, and `thumbv7em-none-eabihf`.
    - `embedded_repositories.bzl` contains exact non-placeholder SHA-256 values for Arm GNU 13.2.Rel1 and Mini404 0.9.10 and has no Darwin or ambient fallback branch.
    - `toolchain_provenance_tests` fails for every version/checksum/fallback mutation and passes for the committed declarations.
    - aquery identifies rules_python Python 3.12.10 for the provenance action and no ambient python3/PATH interpreter.
    - Repeating the ordinary test produces no `MODULE.bazel.lock` diff.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:toolchain_provenance_tests</automated>
  </verify>
  <done>Every later Phase 42 executable originates in an exact, checksum-backed declaration and the lock cannot drift during qualification.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Network archive to external repository | Downloaded compiler/simulator bytes are untrusted until checksum verification. |
| Developer environment to Bazel resolution | PATH, local repositories, and .dependencies could substitute undeclared tools. |
| Python test declaration to execution | Ambient python could falsely qualify declared-Python evidence. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation and automated proof |
| --- | --- | --- | --- | --- | --- |
| T-42-01 | Tampering | HIGH | Arm GNU/Mini404 archives and module lock | mitigate | Exact SHA-256/version/URL assertions plus stable-lock rerun. |
| T-42-02 | Elevation / Tampering | HIGH | tool and Python resolution | mitigate | Declared Bzlmod/rules_python inputs; mutation tests reject PATH, ambient python, local repositories, .dependencies, Cargo, CMake, fixtures, and archives. |
</threat-model>

<verification>
1. Run `bazel test //tools/bazel/phase42:toolchain_provenance_tests` twice.
2. Inspect aquery for the registered rules_python Python 3.12.10 interpreter and no ambient python3 edge.
3. Confirm the second run leaves `MODULE.bazel.lock` unchanged.
4. Run `git diff --check`.
</verification>

<success-criteria>
- D-01 and D-02 are fully implemented.
- TOOL-01 provenance is checksum-backed and rules_python-only.
- HIGH archive and ambient-tool threats have automated mutation proof.
</success-criteria>

<decision-coverage>
| Decision | Task | Coverage |
| --- | --- | --- |
| D-01 | 42-01-01 | Full |
| D-02 | 42-01-01 | Full |
| D-15 (provenance/lock portion) | 42-01-01 | Full |
</decision-coverage>

<output>
After completion, create `.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-01-SUMMARY.md`.
</output>
