---
phase: 42-truthful-bazel-graph-and-executable-mini-toolchain
plan: 04
type: execute
wave: 4
depends_on:
  - 42-03
files_modified:
  - tools/bazel/phase42/BUILD.bazel
  - tools/bazel/phase42/phase42_test_support.py
  - tools/bazel/phase42/platform_rejection_test.py
  - tools/bazel/phase42/graph_isolation_test.py
autonomous: true
requirements:
  - BUILD-03
  - TOOL-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-08-03T19-34-09
generated_at: 2026-08-03T20:16:34.924Z
must_haves:
  truths:
    - "Only runtime embedded + MINI + BUDDY + STM32F407VG + thumbv7em-none-eabihf resolves the ARM smoke on canonical Linux x86_64."
    - "Missing/default, host_tools, every non-MINI product, wrong printer/board/MCU/triple, soft-float, unsupported host, and missing-tool selections fail exact targets without skips or fallback."
    - "The successful configured/action graph contains no reference script, host Cargo, CMake product, fixture, archive, .dependencies, undeclared local tool, or ambient Python edge."
  artifacts:
    - path: "tools/bazel/phase42/BUILD.bazel"
      provides: "Test-only wrong-triple, soft-float, tuple, and missing-tool fixtures"
      contains: "thumbv7em_none_eabi"
    - path: "tools/bazel/phase42/phase42_test_support.py"
      provides: "Status-preserving declared-Python subprocess helper"
      contains: "capture_output=True"
    - path: "tools/bazel/phase42/platform_rejection_test.py"
      provides: "Exact-target positive/negative platform and missing-tool matrix"
      contains: "--noskip_incompatible_explicit_targets"
    - path: "tools/bazel/phase42/graph_isolation_test.py"
      provides: "Configured/action/provider graph provenance audit"
      contains: "aquery"
  key_links:
    - from: "tools/bazel/phase42/platform_rejection_test.py"
      to: "//tools/bazel/phase42:arm_link_smoke"
      via: "exact label invocations with incompatible skipping disabled"
      pattern: "noskip_incompatible_explicit_targets"
    - from: "tools/bazel/phase42/graph_isolation_test.py"
      to: "//tools/bazel/phase42:arm_link_smoke"
      via: "cquery/aquery configured target and action inspection"
      pattern: "aquery"
---

<objective>
Enforce the canonical MINI tuple as a fail-closed allowlist and audit the entire positive graph for forbidden provenance.

Purpose: Prove incompatible selections cannot silently skip or substitute host/reference/fixture work after the real smoke exists.
Output: Negative fixture platforms/toolchains, exact rejection matrix, shared declared-Python helper, and graph-isolation suite.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-CONTEXT.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-RESEARCH.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-VALIDATION.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-02-SUMMARY.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-03-SUMMARY.md
@platforms/BUILD.bazel
@tools/bazel/toolchains/BUILD.bazel
@tools/bazel/toolchains/embedded_toolchain.bzl
@tools/bazel/phase42/platform_contract.bzl
@tools/bazel/phase42/arm_link_smoke.bzl
@tools/bazel/phase42/arm_link_smoke_test.py

<interfaces>
`PHASE42_MINI_CONSTRAINTS` contains exactly runtime_embedded, printer_mini, board_buddy, mcu_stm32f407vg, and thumbv7em_none_eabihf. The positive label is `//tools/bazel/phase42:arm_link_smoke`; HostPolicyInfo is always inspected before EmbeddedToolchainInfo.
</interfaces>
</context>

<tasks>

<task id="42-04-01" type="auto" tdd="true">
  <name>Task 1: Enforce the one-tuple platform allowlist and exact rejection matrix</name>
  <files>tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/phase42_test_support.py, tools/bazel/phase42/platform_rejection_test.py</files>
  <read-first>
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-02-SUMMARY.md
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-03-SUMMARY.md
    - platforms/BUILD.bazel
    - .bazelrc
    - tools/bazel/toolchains/BUILD.bazel
    - tools/bazel/toolchains/embedded_toolchain.bzl
    - tools/bazel/phase42/BUILD.bazel
    - tools/bazel/phase42/arm_link_smoke.bzl
  </read-first>
  <behavior>
    - Test 1: exact canonical selection succeeds on Linux before and after every negative group.
    - Test 2: missing/default and host_tools fail the exact smoke and cannot be reported as skipped.
    - Test 3: every non-MINI platform plus wrong printer, board, MCU, triple, and soft-float fixtures fail analysis/toolchain resolution.
    - Test 4: Darwin and missing Rust/Arm/Python/Mini404 capabilities fail with bounded owner/remedy diagnostics.
  </behavior>
  <action>
Consume without modifying the canonical platform and `PHASE42_MINI_CONSTRAINTS` from Plan 42-02. Implement the negative half of D-05 through D-08: declare `thumbv7em_none_eabi` soft-float and deliberate wrong-triple values only as test fixtures in `tools/bazel/phase42/BUILD.bazel`, alongside wrong-printer, wrong-board, wrong-MCU, and missing Rust/Arm/Python/Mini404 platforms/targets. Do not edit `platforms/BUILD.bazel` or `platform_contract.bzl`.

Implement `phase42_test_support.py` as a library consumed exclusively by rules_python 3.12.10 py_test/py_binary targets. Preserve command, stdout, stderr, and exit status through `subprocess.run(check=False, capture_output=True, text=True)`; never swallow nonzero results. Invoke exact smoke/missing-capability labels with `--noskip_incompatible_explicit_targets`, never wildcard `//...`. Pair each Linux negative category with the exact positive MINI control. On Darwin, require the Plan 42-02 HostPolicyInfo diagnostic with detected OS/architecture and canonical Linux remedy; generic no-toolchain text is insufficient and no positive control may be reported as passed.
  </action>
  <acceptance-criteria>
    - The test confirms all five canonical constraints and the Plan 42-04 diff does not modify `platforms/BUILD.bazel` or `platform_contract.bzl`.
    - Every existing non-MINI platform and all wrong/missing fixture categories are invoked exactly with incompatible-target skipping disabled.
    - Every negative asserts nonzero actionable analysis/toolchain output and rejects skip/warning-only output.
    - Linux begins and ends with a passing exact MINI control; Darwin runs only expected rejection and emits no positive evidence.
    - The helper never uses `|| true`, ambient tool search, or exit-code coercion.
    - `platform_rejection_tests` uses rules_python Python 3.12.10 with no ambient interpreter edge.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:platform_rejection_tests</automated>
  </verify>
  <done>All unsupported tuples, hosts, and missing capabilities fail closed while the exact Linux MINI control proves the harness remains valid.</done>
</task>

<task id="42-04-02" type="auto" tdd="true">
  <name>Task 2: Audit configured, action, provider, and Python provenance</name>
  <files>tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/phase42_test_support.py, tools/bazel/phase42/graph_isolation_test.py</files>
  <read-first>
    - tools/bazel/phase42/phase42_test_support.py
    - tools/bazel/phase42/platform_contract.bzl
    - tools/bazel/phase42/arm_link_smoke.bzl
    - tools/bazel/phase42/arm_link_smoke_test.py
    - tools/bazel/reference_contract.sh
    - tools/bazel/rust_workflow.sh
    - tools/bazel/artifact_rules.bzl
    - tools/bazel/BUILD.bazel
  </read-first>
  <behavior>
    - Test 1: cquery reports the exact target platform and executable qualification toolchain for the positive smoke.
    - Test 2: aquery reports declared Rust compile, Arm link, inspection, map, and report actions with exact outputs.
    - Test 3: every reference/Cargo/CMake/fixture/archive/planning/.dependencies/undeclared path marker fails a dedicated mutation.
    - Test 4: reference/fixture targets cannot export EmbeddedToolchainInfo.
    - Test 5: every Phase 42 Python action resolves rules_python Python 3.12.10 and no PATH/ambient interpreter.
  </behavior>
  <action>
Implement D-08 and the graph portion of D-15. Query the configured exact smoke under `--config=mini`; require all five allowlist values and locked Rust/Arm identities. Query artifacts, command lines, inputs, and providers; require distinct compile/link/inspect/report actions and `.elf`, `.map`, and report outputs.

Reject case-insensitive markers `reference_contract.sh`, `rust_workflow.sh`, `cargo build`, `cargo test`, `utils/build.py`, `cmake`, `build/products`, `tools/bazel/fixtures`, `.planning/archive`, `.planning/milestones`, `.dependencies`, `representative_release_artifacts`, `/usr/bin/python3`, `/usr/local/bin/python3`, `/opt/homebrew`, `env python3`, and undeclared absolute executable paths outside Bazel external/execroot locations. Positively identify rules_python Python 3.12.10 for every Phase 42 py_test/py_binary action. Prove reference_toolchain and fixture/filegroup targets cannot return EmbeddedToolchainInfo. Unit-test stable parser matchers with one Arrange/Act/Assert concern each.
  </action>
  <acceptance-criteria>
    - Graph tests require all five platform constraints and locked Rust/Arm identities.
    - Action audit requires compile/link/inspect/report actions and exact ELF/map/report outputs.
    - Every forbidden marker has a mutation that fails the audit.
    - Reference and fixture targets cannot provide EmbeddedToolchainInfo.
    - Every Phase 42 Python action identifies rules_python Python 3.12.10; ambient interpreter injection fails.
    - Darwin records only actionable host rejection and cannot configure a positive embedded graph.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:graph_isolation_tests</automated>
  </verify>
  <done>The only passing embedded graph is the declared canonical MINI Rust/Arm chain with rules_python-only test evidence.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Selected platform to target analysis | Missing or adjacent product values could be skipped or broadened. |
| Configured target to action/provider closure | Reference, fixture, archived, or ambient edges could masquerade as embedded work. |
| Test harness to Bazel process | Skips or swallowed status could turn rejection into false success. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation and automated proof |
| --- | --- | --- | --- | --- | --- |
| T-42-03 | Spoofing / Tampering | HIGH | platform selection | mitigate | Exact-target negative matrix with skip disabled and repeated positive control. |
| T-42-02 | Tampering / Elevation | HIGH | action graph and Python interpreter | mitigate | cquery/aquery require declared identities and reject ambient/reference/fixture/Cargo/CMake/.dependencies edges. |
| T-42-04 | Spoofing / Repudiation | HIGH | reference/fixture provider boundary | mitigate | Provider inspection and mutation tests reject EmbeddedToolchainInfo reachability. |
| T-42-06 | Spoofing | HIGH | execution host | mitigate | Darwin matrix requires exact HostPolicyInfo diagnostic and no positive graph. |
</threat-model>

<verification>
1. Run `bazel test //tools/bazel/phase42:platform_rejection_tests //tools/bazel/phase42:graph_isolation_tests`.
2. On Linux, require exact MINI controls before and after every negative group.
3. On Darwin, require exact detected-host rejection and no positive qualification row.
4. Run `git diff --check` and confirm canonical platform files are unchanged by this plan.
</verification>

<success-criteria>
- D-05 through D-08 are fully implemented.
- BUILD-03 negatives use exact-target fail-closed analysis/toolchain evidence.
- TOOL-01 provenance excludes every forbidden fallback class and ambient Python.
- Every HIGH platform/provenance/host threat has automated negative or mutation evidence.
</success-criteria>

<decision-coverage>
| Decision | Task | Coverage |
| --- | --- | --- |
| D-05 (negative fixtures) | 42-04-01 | Full |
| D-06 | 42-04-01, 42-04-02 | Full |
| D-07 | 42-04-01 | Full |
| D-08 | 42-04-01, 42-04-02 | Full |
| D-15 (negative/graph portion) | 42-04-02 | Full |
</decision-coverage>

<output>
After completion, create `.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-04-SUMMARY.md`.
</output>
