---
phase: 42-truthful-bazel-graph-and-executable-mini-toolchain
plan: 05
type: execute
wave: 5
depends_on:
  - 42-04
files_modified:
  - .bazelrc
  - tools/bazel/BUILD.bazel
  - tools/bazel/reference_contract.sh
  - tools/bazel/phase2_verify.py
  - tools/bazel/phase42/BUILD.bazel
  - tools/bazel/phase42/capability_gate.bzl
  - tools/bazel/phase42/facade_contract_test.py
  - tools/bazel/phase42/reference_separation_test.py
  - tools/bazel/phase42/phase42_verify.py
  - tools/bazel/phase42/phase42_verify_test.py
  - justfile
autonomous: true
requirements:
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - TOOL-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-08-03T19-34-09
generated_at: 2026-08-03T20:16:34.924Z
must_haves:
  truths:
    - "Stable build/test/package/simulator labels fail both bazel build and bazel run during analysis with owning phase and working Phase 42 remedy until real authority exists."
    - "CMake/C++ execution and previews exist only through explicit reference_* and reference_*_plan labels/recipes; environment cannot change semantics."
    - "The aggregate reproduces every prior false-positive route, verifies provenance/lock/smoke/negatives/graph/facade/reference isolation, and succeeds only on canonical Linux x86_64."
    - "Darwin smoke, direct authority labels, all four stable just recipes, and aggregate expose the same detected-host Linux-remedy contract without EmbeddedToolchainInfo or actions."
    - "Every Phase 42 Python acceptance surface uses rules_python Python 3.12.10; direct phase2_verify.py or ambient python cannot qualify."
  artifacts:
    - path: "tools/bazel/phase42/capability_gate.bzl"
      provides: "Analysis-time unavailable authority rule"
      contains: "unavailable_capability"
    - path: "tools/bazel/phase2_verify.py"
      provides: "Revised explicit reference/gate expectations"
      contains: "reference_build_plan"
    - path: "tools/bazel/phase42/facade_contract_test.py"
      provides: "Direct Bazel/just false-positive regression matrix"
      contains: "release-package"
    - path: "tools/bazel/phase42/reference_separation_test.py"
      provides: "Reference semantic/provider isolation proof"
      contains: "reference_simulator_plan"
    - path: "tools/bazel/phase42/phase42_verify.py"
      provides: "Canonical rules_python aggregate and Darwin rejection"
      contains: "canonical Linux x86_64"
    - path: "justfile"
      provides: "Thin stable, Phase 42, and reference Bazel recipes"
      contains: "phase42-verify"
  key_links:
    - from: "justfile"
      to: "tools/bazel/BUILD.bazel"
      via: "Bazel-only recipes with propagated status"
      pattern: "bazel (build|run|test)"
    - from: "tools/bazel/BUILD.bazel"
      to: "tools/bazel/phase42/capability_gate.bzl"
      via: "analysis-time authority gates"
      pattern: "unavailable_capability"
    - from: "tools/bazel/phase42/phase42_verify.py"
      to: "//tools/bazel/phase42:arm_link_smoke"
      via: "ordered smoke, negative, graph, facade, and reference verification"
      pattern: "arm_link_smoke"
    - from: "tools/bazel/phase42/reference_separation_test.py"
      to: "tools/bazel/phase2_verify.py"
      via: "revised expectation assertions"
      pattern: "BUDDY_BAZEL_EXECUTE_REFERENCE"
---

<objective>
Make every developer-facing command truthful, isolate the executable CMake/C++ reference oracle, and close Phase 42 with one canonical-Linux aggregate.

Purpose: Remove print-only, fixture-backed, host-Cargo, environment-switched, and unsupported-host false success from build/test/package/simulator interfaces.
Output: Analysis-time authority gates, explicit reference execution/previews, revised Phase 2 expectations, rules_python facade/reference tests, and aggregate verifier/recipes.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-CONTEXT.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-RESEARCH.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-VALIDATION.md
@.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-04-SUMMARY.md
@.bazelrc
@tools/bazel/BUILD.bazel
@tools/bazel/reference_contract.sh
@tools/bazel/rust_workflow.sh
@tools/bazel/phase2_verify.py
@tools/bazel/phase3_artifacts.sh
@tools/bazel/phase42/graph_isolation_test.py
@justfile

<interfaces>
```text
build_firmware   -> unavailable until Phase 46    -> remedy arm_link_smoke
test_firmware    -> unavailable until Phase 43/46 -> remedy phase42_verifier_tests
release_package  -> unavailable until Phase 47    -> remedy arm_link_smoke
simulator_parity -> unavailable until Phase 48    -> remedy phase42_verifier_tests

reference_build / reference_build_plan
reference_test / reference_test_plan
reference_package / reference_package_plan
reference_simulator / reference_simulator_plan
```
</interfaces>
</context>

<tasks>

<task id="42-05-01" type="auto" tdd="true">
  <name>Task 1: Replace false-success authority labels with analysis-time capability gates</name>
  <files>tools/bazel/BUILD.bazel, tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/capability_gate.bzl, tools/bazel/phase42/facade_contract_test.py</files>
  <read-first>
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-04-SUMMARY.md
    - tools/bazel/BUILD.bazel
    - tools/bazel/reference_contract.sh
    - tools/bazel/rust_workflow.sh
    - tools/bazel/artifact_rules.bzl
    - tools/bazel/phase3_artifacts.sh
    - tools/bazel/phase42/phase42_test_support.py
    - justfile
  </read-first>
  <behavior>
    - Test 1: build/test/package/simulator authority fails under both bazel build and bazel run during analysis with exact owner/remedy.
    - Test 2: package authority cannot reach the known 346-byte/Phase 3 fixture lineage.
    - Test 3: no generic authority route prints a command, runs host Cargo/CMake/reference work, or returns zero.
    - Test 4: Darwin direct smoke/build/test/package/simulator calls use the same detected-host Linux-remedy HostPolicyInfo diagnostic.
  </behavior>
  <action>
Implement D-09, D-10, D-13, and D-14. Create `unavailable_capability(name, owning_phase, available_command)` requiring only `phase42_qualification_toolchain_type`. Read HostPolicyInfo before fail: Linux text contains `capability unavailable`, owning phase, and exact remedy; Darwin text contains `unsupported embedded qualification host`, detected `Darwin-<arch>`, `use canonical Linux x86_64 CI/container`, owner, and remedy. Never access absent Darwin EmbeddedToolchainInfo. Produce no executable, output, output group, action, or qualification provider so build and run fail identically during analysis.

Map `build_firmware` to Phase 46 + exact arm-link-smoke command; `test_firmware` to Phase 43/46 + exact verifier-test command; `release_package`/`release_packages` to Phase 47 + smoke command; and `simulator_parity` to Phase 48 + verifier-test command. Include `--config=mini --noskip_incompatible_explicit_targets` on direct authority assertions. Make `test_host` fail with migration guidance to `test_firmware` or explicit `reference_test`. Rename historical fixture authority to `phase3_fixture_release_artifacts`; generic package authority cannot reach it.

Write facade tests first as a rules_python Python 3.12.10 py_test. Reproduce every old print-only/fixture/host-Cargo/CMake/reference/runtime-error route and require nonzero analysis failure under direct build and run. On Darwin require exact route-complete smoke/build/test/package/simulator HostPolicyInfo text; generic no-toolchain failure is insufficient.
  </action>
  <acceptance-criteria>
    - Every generic authority label is unavailable_capability with exact owner/remedy and no outputs/providers/actions.
    - Exact build and run calls fail during analysis for all four capability classes.
    - Package authority cannot reach representative_release_artifacts, the 346-byte fixture, or tools/bazel/fixtures.
    - test_host provides migration guidance; no generic label calls rust_workflow.sh, reference_contract.sh, Cargo, CMake, pytest, or utils/build.py.
    - Darwin smoke/build/test/package/simulator expose detected host and canonical Linux remedy through HostPolicyInfo.
    - Facade mutations to runtime executable/filegroup/zero-success paths fail the rules_python test.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:facade_contract_tests</automated>
  </verify>
  <done>Stable authority surfaces cannot return green until the owning phase provides real capability, and every host route fails truthfully.</done>
</task>

<task id="42-05-02" type="auto" tdd="true">
  <name>Task 2: Separate executable reference commands and previews from Rust authority</name>
  <files>.bazelrc, tools/bazel/BUILD.bazel, tools/bazel/reference_contract.sh, tools/bazel/phase2_verify.py, tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/reference_separation_test.py, justfile</files>
  <read-first>
    - .bazelrc
    - tools/bazel/BUILD.bazel
    - tools/bazel/reference_contract.sh
    - tools/bazel/phase3_workflow.sh
    - tools/bazel/phase42/graph_isolation_test.py
    - tools/bazel/phase2_verify.py
    - justfile
    - .planning/milestones/v1.0-phases/02-bazel-authority-and-developer-facade/02-CONTEXT.md
  </read-first>
  <behavior>
    - Test 1: four reference execution labels run their real comparison/rollback commands and propagate status.
    - Test 2: four matching plan labels only print exact quoted previews.
    - Test 3: BUDDY_BAZEL_EXECUTE_REFERENCE is absent and environment changes cannot alter label semantics.
    - Test 4: reference targets cannot return EmbeddedToolchainInfo or enter smoke/aggregate qualification.
    - Test 5: phase2_verify.py requires the eight explicit labels/recipes and authority gates and rejects retired switch semantics.
  </behavior>
  <action>
Implement D-11 and D-12. Remove `BUDDY_BAZEL_EXECUTE_REFERENCE` from `.bazelrc`, authority rules, and reference dispatch. Make `reference_contract.sh` dispatch only by label basename: `reference_build`, `reference_test`, `reference_package`, and `reference_simulator` execute established CMake/Python reference commands and preserve exit codes; matching `_plan` names print only the exact quoted command; unknown names exit 2.

Declare exactly eight reference labels backed by descriptive reference providers, never EmbeddedToolchainInfo. Add eight matching hyphenated just recipes, each one explicit bazel run. Modify `tools/bazel/phase2_verify.py` current-state expectations to require the eight labels/recipes and analysis-time authority gates, remove the environment switch, and reject old switched semantics. Preserve historical manifests only as historical data.

Write reference separation tests first through rules_python Python 3.12.10. Inspect definitions, cquery/provider closure, actions, and runfiles; inject BUDDY_BAZEL_EXECUTE_REFERENCE=0/1 and prove unchanged semantics. Test revised phase2_verify.py required strings directly, while keeping standalone direct host-Python execution non-qualifying and excluded from the aggregate.
  </action>
  <acceptance-criteria>
    - Current config/rules/script contain no BUDDY_BAZEL_EXECUTE_REFERENCE switch.
    - Four reference execution and four plan labels have one fixed behavior each; unknown names exit 2.
    - Eight just recipes each contain one bazel run and no direct Cargo/CMake/Python command.
    - Reference providers/actions/runfiles contain no EmbeddedToolchainInfo, smoke output, aggregate result, or authority-provider edge.
    - phase2_verify.py requires the explicit split/gates and rejects retired semantics; standalone output cannot qualify Phase 42.
    - reference_separation_tests uses rules_python Python 3.12.10 and no ambient interpreter.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:reference_separation_tests &amp;&amp; just reference-build-plan &amp;&amp; just reference-test-plan &amp;&amp; just reference-package-plan &amp;&amp; just reference-simulator-plan</automated>
  </verify>
  <done>CMake/C++ remains an invocable, explicitly named oracle that cannot satisfy Rust authority or qualification.</done>
</task>

<task id="42-05-03" type="auto" tdd="true">
  <name>Task 3: Wire stable recipes and the canonical aggregate verifier</name>
  <files>tools/bazel/phase42/BUILD.bazel, tools/bazel/phase42/phase42_verify.py, tools/bazel/phase42/phase42_verify_test.py, tools/bazel/BUILD.bazel, justfile</files>
  <read-first>
    - tools/bazel/phase42/toolchain_provenance_test.py
    - tools/bazel/phase42/arm_link_smoke_test.py
    - tools/bazel/phase42/platform_rejection_test.py
    - tools/bazel/phase42/graph_isolation_test.py
    - tools/bazel/phase42/facade_contract_test.py
    - tools/bazel/phase42/reference_separation_test.py
    - tools/bazel/phase2_verify.py
    - tools/bazel/BUILD.bazel
    - justfile
    - .planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-VALIDATION.md
  </read-first>
  <behavior>
    - Test 1: Linux aggregate runs provenance/lock/smoke/negative/graph/facade/reference proofs and reports genuine outputs.
    - Test 2: any old print-only, metadata, host-Cargo/CMake, fixture, ambient-Python, or zero-exit route fails the aggregate.
    - Test 3: Darwin aggregate fails with detected host/Linux remedy; host-check returns zero only after proving all route rejections.
    - Test 4: all just recipes are thin Bazel invocations that preserve status and contain no fallback.
  </behavior>
  <action>
Implement D-10, D-14, and D-15. Add five thin recipes/targets: `phase42-toolchain-resolution`, `phase42-arm-link-smoke`, `phase42-platform-negatives`, `phase42-host-check`, and `phase42-verify`. Rewire stable build/test/release-package/simulator-parity recipes to exact authority labels with `--config=mini --noskip_incompatible_explicit_targets`. On Darwin, host-check must prove exact smoke, direct build/test/package/simulator labels, and all four corresponding just recipes return the same HostPolicyInfo detected-host/Linux-remedy diagnostic. `phase42-verify` must be nonzero anywhere except Linux-x86_64.

Declare `phase42_verify.py` only as rules_python Python 3.12.10 py_binary and `phase42_verify_test.py` as py_test. Linux orchestration snapshots the lock; proves interpreter identity; runs provenance; builds/inspects smoke; runs negatives and graph isolation; reproduces direct Bazel and just false-positive routes and requires gates; runs reference separation without treating it as qualification; reruns smoke; confirms lock stability; and emits tool identities plus genuine output paths. Any absent test/output, lock drift, forbidden edge, or false zero exits nonzero.

Darwin aggregate stops before positive embedded work, prints detected `Darwin-$(uname -m)`, states no positive evidence is possible here, points to `canonical Linux x86_64 CI/container: just phase42-verify`, and exits nonzero. Platform-injected tests cover Linux orchestration and route-complete Darwin smoke/direct/just/aggregate diagnostics on any host. `just phase42-verify` invokes the Bazel py_binary label, never ambient python3.
  </action>
  <acceptance-criteria>
    - just --list exposes five Phase 42 recipes, four stable authority verbs, and eight explicit reference recipes.
    - Recipe bodies contain only Bazel invocations and no fallback or direct Cargo/CMake/Python resolution.
    - Every Phase 42 Python acceptance action identifies rules_python Python 3.12.10; ambient/direct phase2_verify execution cannot qualify.
    - Linux aggregate emits exact identities, tuple, ELF/map/report paths, negative/facade/graph results, and stable lock hash before zero.
    - Darwin smoke, direct build/test/package/simulator, four stable just recipes, and aggregate return nonzero with the same detected-host/Linux remedy; host-check verifies them and then returns zero.
    - Mutating an old false-success route causes phase42_verifier_tests to fail.
  </acceptance-criteria>
  <verify>
    <automated>bazel test //tools/bazel/phase42:phase42_verifier_tests &amp;&amp; if [ "$(uname -s)-$(uname -m)" = "Linux-x86_64" ]; then just phase42-verify; else just phase42-host-check &amp;&amp; ! just phase42-verify 2&gt;/tmp/phase42-darwin-aggregate.err &amp;&amp; grep -E 'Darwin.*canonical Linux x86_64 CI/container' /tmp/phase42-darwin-aggregate.err; fi</automated>
  </verify>
  <done>Phase 42 has one canonical Linux acceptance command, route-complete Darwin rejection evidence, and no developer facade capable of false embedded success.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Developer verb to authority label | Familiar commands could hide previews, fixtures, or runtime-only failures. |
| Reference execution to Rust qualification | Successful CMake/C++ work could be misreported as Rust evidence. |
| Environment to label semantics | The retired switch could alter execution invisibly. |
| Aggregate verifier to final result | Partial/host/reference success could be combined into a false pass. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation and automated proof |
| --- | --- | --- | --- | --- | --- |
| T-42-04 | Spoofing / Repudiation | HIGH | authority/reference/fixture facades | mitigate | Analysis-time no-output gates, explicit reference names/providers, direct build/run/just regression matrix, and graph isolation. |
| T-42-05 | Repudiation | HIGH | aggregate result | mitigate | Ordered status-preserving verifier emits exact identities, lock hash, commands, and genuine output paths. |
| T-42-06 | Spoofing | HIGH | smoke/authority/recipe/aggregate host policy | mitigate | Route-complete Darwin tests require one HostPolicyInfo diagnostic and no EmbeddedToolchainInfo/actions. |
| T-42-02 | Elevation / Tampering | HIGH | Python/verifier/recipe execution | mitigate | rules_python Python 3.12.10 only; aquery rejects ambient PATH and mutation tests reject fallback/zero success. |
</threat-model>

<verification>
1. Run all Phase 42 rules_python suites and `bazel test //tools/bazel/phase42:phase42_verifier_tests`.
2. Run `just --list` and all four reference plan recipes; each preview is explicitly reference-only.
3. On Darwin, run `just phase42-host-check`, then require `just phase42-verify` to fail with the same detected-host/Linux remedy.
4. On Linux x86_64, run `just phase42-verify`; this is the only positive aggregate.
5. Confirm MODULE.bazel.lock remains unchanged.
6. Run `git diff --check`, `bun scripts/bright-builds-check.ts all`, `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success-criteria>
- D-09 through D-15 are fully implemented without reducing any locked decision.
- BUILD-02 direct Bazel/just surfaces are truthful under build and run.
- BUILD-04 reference execution/previews remain usable and excluded from Rust success.
- BUILD-03 and TOOL-01 evidence reaches the aggregate only through canonical declared providers.
- Every HIGH authority/provenance/host threat has concrete automated proof.
</success-criteria>

<decision-coverage>
| Decision | Task | Coverage |
| --- | --- | --- |
| D-09 | 42-05-01 | Full |
| D-10 | 42-05-01, 42-05-03 | Full |
| D-11 | 42-05-02 | Full |
| D-12 | 42-05-02 | Full |
| D-13 | 42-05-01, 42-05-02 | Full |
| D-14 | 42-05-01, 42-05-03 | Full |
| D-15 | 42-05-03 | Full |
</decision-coverage>

<phase-decision-coverage>
| Decision | Plan | Task | Full/Partial | Notes |
| --- | --- | --- | --- | --- |
| D-01 | 01 | 42-01-01 | Full | Exact versions, declarations, and lock |
| D-02 | 01 | 42-01-01 | Full | Checksums and no fallback |
| D-03 | 02/03/05 | 42-02-01, 42-03-01, 42-05-01/03 | Full | Provider-first route-complete host policy |
| D-04 | 03 | 42-03-01 | Full | Genuine ARM hard-float smoke |
| D-05 | 02/04 | 42-02-01, 42-04-01 | Full | Canonical tuple before registrations; negatives isolated |
| D-06 | 02/04 | 42-02-01, 42-04-01/02 | Full | Exact provider chain and graph |
| D-07 | 04 | 42-04-01 | Full | Complete fail-closed selection matrix |
| D-08 | 04 | 42-04-01/02 | Full | Exact negatives and graph proof |
| D-09 | 05 | 42-05-01 | Full | Analysis-time unavailable gates |
| D-10 | 05 | 42-05-01/03 | Full | Named working Phase 42 commands |
| D-11 | 05 | 42-05-02 | Full | Environment switch removed |
| D-12 | 05 | 42-05-02 | Full | Explicit reference execution/previews |
| D-13 | 05 | 42-05-01/02 | Full | Fixture/host/reference exclusion |
| D-14 | 05 | 42-05-01/03 | Full | False positives reproduced and rejected |
| D-15 | 01/03/04/05 | 42-01-01, 42-03-01, 42-04-02, 42-05-03 | Full | Provenance, lock, smoke, negatives, graph, facade, reference |
</phase-decision-coverage>

<output>
After completion, create `.planning/phases/42-truthful-bazel-graph-and-executable-mini-toolchain/42-05-SUMMARY.md`.
</output>
