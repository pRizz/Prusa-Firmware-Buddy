---
phase: 37-reconcile-decisions-into-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
  - tools/bazel/phase34_final_readiness_demotion_dry_run.py
  - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
  - tools/bazel/phase34_decision_reconciliation_integration_test.py
  - tools/bazel/BUILD.bazel
  - tools/bazel/rust_workflow.sh
autonomous: true
requirements:
  - DECIDE-01
  - DECIDE-02
  - READY-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 37-2026-07-26T06-52-46
generated_at: 2026-07-26T07:10:00Z
must_haves:
  truths:
    - "Phase 34's one canonical ledger contains Phase 31 evidence rows and first-class canonical Phase 32 decision-domain rows on distinct evaluation paths."
    - "Complete valid Phase 31 evidence plus exact valid Phase 33 decisions produces readiness_state: unblocked."
    - "Every one-concern invalid decision mutation keeps readiness blocked with a specific diagnostic."
    - "The authoritative Phase 34 verification gate runs the boundary, reconciliation, ledger, and real-producer tests before publishing outputs."
    - "Phase 37 stops at Phase 34 retained readiness artifacts and never publishes a Phase 35 cutover verdict or demotes references."
  artifacts:
    - path: tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
      provides: "Canonical ledger schema for evidence and decision-domain rows"
    - path: tools/bazel/phase34_final_readiness_demotion_dry_run.py
      provides: "Dual-source ledger construction, reconciliation consumption, and retained readiness publication"
    - path: tools/bazel/phase34_decision_reconciliation_integration_test.py
      provides: "Actual Phase 31-through-34 approved-path and negative-mutation regression"
    - path: tools/bazel/BUILD.bazel
      provides: "Hermetic runfiles and phase verification targets"
    - path: tools/bazel/rust_workflow.sh
      provides: "Repository-owned Phase 34 verification orchestration"
  key_links:
    - from: tools/bazel/phase34_final_readiness_demotion_dry_run.py
      to: tools/bazel/phase34_decision_reconciliation.py
      via: "pure exact typed decision reconciliation for Phase 32 decision-domain ledger rows"
      pattern: "phase34_decision_reconciliation"
    - from: tools/bazel/phase34_decision_reconciliation_integration_test.py
      to: "actual Phase 31, Phase 32, Phase 33, and Phase 34 producer entrypoints"
      via: "real retained artifacts and production loading/publication paths"
      pattern: "readiness_state"
    - from: tools/bazel/rust_workflow.sh
      to: tools/bazel/BUILD.bazel
      via: "phase34_verify_tests gate before verifier publication"
      pattern: "phase34_verify"
---

<objective>
Integrate exact typed reconciliation into Phase 34's canonical ledger and prove the real Phase 31-through-34 approved path reaches unblocked readiness only for complete valid inputs.

Purpose: Deliver READY-01 and close audit gap B1 without promoting Phase 32 over Phase 31 evidence authority or crossing into Phase 38's Phase 35/cutover responsibilities.
Output: A dual-source Phase 34 ledger, retained JSON/Markdown outputs, a real-producer integration regression with focused negative mutations, and hermetic verification wiring.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-CONTEXT.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-RESEARCH.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-VALIDATION.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-01-SUMMARY.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-CONTEXT.md
@tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
@tools/bazel/phase34_final_readiness_demotion_dry_run.py
@tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
@tools/bazel/phase32_blocker_register_triage_test.py
@tools/bazel/BUILD.bazel
@tools/bazel/rust_workflow.sh

<interfaces>
From Plan 37-01:

```text
decision target = row_ref + decision_axis + decision_subject_id
reconciliation result = coverage_state + readiness_effect + linked decision refs + stable reason codes
```

The Phase 34 shell must construct evidence rows from Phase 31, construct decision-domain rows from canonical Phase 32 data, invoke the pure reconciliation core only for decision-domain rows, and derive all retained JSON and Markdown views from that single ledger state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Materialize and reconcile first-class decision-domain ledger rows</name>
  <read_first>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-CONTEXT.md</file>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-01-SUMMARY.md</file>
    <file>tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run.py</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</file>
    <file>tools/bazel/phase34_decision_reconciliation.py</file>
  </read_first>
  <files>tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json, tools/bazel/phase34_final_readiness_demotion_dry_run.py, tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</files>
  <behavior>
    - "The canonical ledger is the typed union of Phase 31 evidence rows and canonical Phase 32 decision-domain rows per D-01."
    - "Evidence rows remain Phase 31-derived and are not reconstructed or double-counted from Phase 32 per D-02."
    - "Each decision-domain row preserves row_id, decision_axis, decision_subject_id, source lineage, proof eligibility, blocker classification, affected gates, and requirement IDs in JSON and Markdown per D-03."
    - "Exact valid decisions attach only to their matching row; uncovered or invalid rows remain blocking with the pure core's reason codes."
    - "Readiness state is derived from the one ledger, while reference-demotion authorization stays independent."
  </behavior>
  <action>
Write or extend the Phase 34 tests first. Extend the contract and shell so evidence and decision-domain constructors are explicit and merge into one canonical ledger without duplicating evidence subjects. Replace the unconditional dangling treatment only for valid supported decision-domain rows; retain fail-closed dangling behavior for genuinely invalid or unrelated inputs. Revalidate Phase 33 typed targets against the loaded Phase 32 snapshot, call the Plan 37-01 pure core, and serialize the preserved row identity, lineage, coverage, readiness effect, linked decisions, and stable reason codes into every retained JSON/Markdown view. Do not weaken hard blockers, source validation, lifecycle checks, retained-bundle atomicity, redaction, or demotion independence. Do not invoke Phase 35, replace stale authority, route cutover, or demote production references (D-12).
  </action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "Valid canonical decision-domain rows are first-class ledger rows, never unconditional dangling blockers."
    - "Phase 31 remains the sole completeness/finality authority for evidence rows."
    - "All retained Phase 34 views derive from the same ledger and preserve the required canonical and typed identities."
    - "Readiness and demotion authorization remain orthogonal."
    - "Before committing this task, the executor runs the automated command exactly in its listed order, including the complete mandatory Rust sequence."
  </acceptance_criteria>
  <done>Phase 34 publishes one dual-source typed ledger whose exact valid decisions can clear decision-domain rows while every invalid condition remains visibly blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the real Phase 31-through-34 approved and blocked paths</name>
  <read_first>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-CONTEXT.md</file>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-RESEARCH.md</file>
    <file>tools/bazel/phase32_blocker_register_triage_test.py</file>
    <file>tools/bazel/phase33_maintainer_decision_inputs_test.py</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</file>
    <file>tools/bazel/phase34_final_readiness_demotion_dry_run.py</file>
  </read_first>
  <files>tools/bazel/phase34_decision_reconciliation_integration_test.py</files>
  <behavior>
    - "The baseline uses actual Phase 31, Phase 32, and Phase 33 producer outputs and Phase 34's real loading/evaluation/publication path per D-09."
    - "Complete required evidence and exact typed decisions publish the full retained Phase 34 bundle with readiness_state: unblocked per D-10."
    - "Decision-domain rows are linked to intended decisions and are not emitted as unconditional dangling blockers."
    - "One-concern mutations for omission, row-ref mismatch, axis mismatch, subject mismatch, stale lifecycle, invalid value, duplicate binding, and conflict each publish blocked readiness with their expected specific diagnostic per D-11."
    - "The integration stops at authoritative Phase 34 artifacts and does not invoke Phase 35 or cutover routing per D-12."
  </behavior>
  <action>
Build one readable real-producer fixture by reusing existing Phase 26/27/28/31 fixture helpers where practical, then execute the production Phase 31, Phase 32, Phase 33, and Phase 34 entrypoints rather than reimplementing their logic in the test. Assert the valid retained bundle and unblocked readiness first. Derive each negative case from that exact baseline by mutating only one concern and assert blocked readiness plus the precise reason code. Keep tests deterministic, hermetic, and explicit about Arrange/Act/Assert. Do not synthesize a shortcut ledger, call Phase 35, test stale-authority replacement, or exercise cutover/demotion actions reserved for Phase 38.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase34_decision_reconciliation_integration_test.py -q &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "The valid case consumes actual upstream producer artifacts and reaches Phase 34 readiness_state: unblocked."
    - "All eight required one-concern negative categories remain blocked with specific diagnostics."
    - "The test verifies retained Phase 34 artifacts only and contains no Phase 35 or production-cutover workflow."
    - "Before committing this task, the executor runs the automated command exactly in its listed order, including the complete mandatory Rust sequence."
  </acceptance_criteria>
  <done>The real producer chain proves the complete-approved Phase 34 path works and that every required invalid mutation still fails closed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the reconciliation regressions into the authoritative Phase 34 gate</name>
  <read_first>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-VALIDATION.md</file>
    <file>tools/bazel/BUILD.bazel</file>
    <file>tools/bazel/rust_workflow.sh</file>
    <file>tools/bazel/phase34_decision_reconciliation_test.py</file>
    <file>tools/bazel/phase34_decision_reconciliation_integration_test.py</file>
  </read_first>
  <files>tools/bazel/BUILD.bazel, tools/bazel/rust_workflow.sh</files>
  <action>
Add the new pure module and both new tests to hermetic Bazel targets/runfiles. Extend the existing `phase34_verify_tests`/`phase34_verify` path so it runs the Phase 33 boundary suite, pure reconciliation suite, Phase 34 ledger suite, and real-producer integration suite before Phase 34 verifier publication. Keep the existing `just phase34-verify` public facade rather than adding a redundant Phase 37 alias. Preserve rerunnable shell behavior and fail visibly on any suite failure; do not use `|| true`, swallowed status codes, Phase 35 dispatch, stale-authority replacement, cutover routing, or demotion actions.
  </action>
  <verify>
    <automated>bash -n tools/bazel/rust_workflow.sh &amp;&amp; just phase34-verify &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "Bazel targets carry all required module/test runfiles and execute every Phase 37 suite hermetically."
    - "`just phase34-verify` runs all Phase 33/34 reconciliation tests before verifier publication and propagates failures."
    - "No new public alias, Phase 35 invocation, authority replacement, cutover routing, or demotion action is introduced."
    - "Before committing this task, the executor runs the automated command exactly in its listed order, including the complete mandatory Rust sequence."
  </acceptance_criteria>
  <done>The repository-owned Phase 34 gate deterministically proves Phase 37's exact resolution, ledger, and real-producer behaviors before publication.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 31/32 retained artifacts to Phase 34 | Complete evidence receipts and sparse blocker rows cross from separate authorities into one ledger builder. |
| Phase 33 handoff to Phase 34 reconciliation | Human-authored decisions and copied identities could attempt to clear the wrong, stale, hard, or ambiguous blocker. |
| Canonical ledger to retained JSON/Markdown bundle | Divergent serializers or partial publication could present a misleading readiness state. |
| Bazel test gate to verifier publication | Missing runfiles or incorrect orchestration could publish outputs without executing regressions. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-37-03 | Tampering / Elevation of Privilege | high | Dual-source Phase 34 ledger | mitigate | Keep Phase 31 evidence authority and Phase 32 decision rows on explicit constructors; revalidate exact typed bindings and block every invalid cardinality/value/lifecycle case. |
| T-37-04 | Tampering / Spoofing | high | Real-producer approved path | mitigate | Execute actual Phase 31-34 entrypoints and prove one-concern identity, lifecycle, value, duplicate, conflict, and omission mutations remain blocked. |
| T-37-05 | Tampering / Denial of Service | high | Hermetic verification gate | mitigate | Declare complete runfiles, execute every focused/integration suite before publication, and propagate every nonzero status. |
| T-37-06 | Repudiation / Information Disclosure | medium | Retained Phase 34 bundle | mitigate | Derive JSON and Markdown from one ledger, preserve canonical lineage and reason codes, reuse atomic publication/redaction/security scans, and avoid secrets. |
| T-37-07 | Elevation of Privilege | high | Readiness versus demotion boundary | mitigate | Keep demotion authorization independent and prohibit Phase 35 invocation, stale-authority replacement, cutover routing, or production reference demotion in code and integration tests. |

No high-severity threat may remain accepted, transferred without an enforced boundary, or unresolved when this plan is complete. The full phase gate must block publication if any high-severity mitigation regression fails.
</threat-model>

<verification>
Run each focused test after its implementation task. After Wave 2, run `bash -n tools/bazel/rust_workflow.sh` and `just phase34-verify`. Before every task commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; and `git diff --check`. Before phase verification, repeat `git diff --check`, `just phase34-verify`, and the complete Rust sequence. Review the final diff to confirm Phase 37 stops at retained Phase 34 artifacts.
</verification>

<success-criteria>
- Phase 34 includes canonical evidence and decision-domain rows without treating valid Phase 27/28 rows as unconditional dangling blockers.
- Retained-code, residual-risk, exception, and readiness decisions resolve only exact typed matching rows.
- Uncovered, mismatched, stale, invalid, duplicate, conflicting, rejected, and hard-blocker decisions remain blocking with specific diagnostics.
- Actual Phase 31-33 producer outputs reach Phase 34 readiness_state: unblocked only when all evidence and decisions are valid.
- D-09 through D-12 are fully implemented, completing decision coverage for D-01 through D-12 across both plans.
- All high-severity ASVS L1 threats are mitigated and enforced by the authoritative automated gate.
- Phase 35 approval, durable stale-authority replacement, cutover routing, and production reference demotion remain exclusively Phase 38 scope.
</success-criteria>

<output>
After completion, create `.planning/phases/37-reconcile-decisions-into-readiness/37-02-SUMMARY.md`.
</output>
