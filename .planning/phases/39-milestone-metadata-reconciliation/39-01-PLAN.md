---
phase: 39-milestone-metadata-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-final-evidence-intake/31-01-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/39-milestone-metadata-reconciliation/39-VALIDATION.md
autonomous: true
requirements:
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 39-2026-07-29T01-32-55
generated_at: 2026-07-29T02:15:00Z
must_haves:
  truths:
    - "Supported summary extraction reports INTAKE-01 through INTAKE-04 from the Phase 31 summary without changing its historical provenance."
    - "The Phase 31, Phase 32, and Phase 34 roadmap inventories name exactly their phase-local completed plan/summary pairs."
    - "All sixteen v1.3 requirement IDs agree across requirement checkboxes, traceability, supported summary extraction, passed phase verification, roadmap status, and milestone/state counts."
    - "The canonical v1.3 milestone audit is not refreshed until Phase 39 verification and lifecycle validation pass."
  artifacts:
    - path: ".planning/phases/31-final-evidence-intake/31-01-SUMMARY.md"
      provides: "Canonical requirements-completed provenance for all four Phase 31 intake requirements"
      contains: "requirements-completed"
    - path: ".planning/ROADMAP.md"
      provides: "Exact completed plan inventories for Phases 31, 32, and 34"
      contains: "31-01-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Checked and Complete v1.3 requirement status surfaces"
      contains: "INTAKE-01"
    - path: ".planning/phases/39-milestone-metadata-reconciliation/39-VALIDATION.md"
      provides: "Recorded deterministic metadata-gate results for the reconciliation tasks"
  key_links:
    - from: ".planning/phases/31-final-evidence-intake/31-01-SUMMARY.md"
      to: "gsd-tools.cjs summary-extract"
      via: "canonical requirements-completed frontmatter key"
      pattern: "requirements-completed"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/31-final-evidence-intake, .planning/phases/32-blocker-register-and-evidence-triage, .planning/phases/34-final-readiness-and-demotion-dry-run"
      via: "exact phase-prefixed plan filenames and matching completed summaries"
      pattern: "(31-01|32-01|34-01|34-02)-PLAN\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "supported summary extraction and passed phase verification reports"
      via: "sixteen-ID fail-closed consistency matrix"
      pattern: "Complete"
---

<objective>
Reconcile the v1.3 milestone's completion provenance and roadmap inventories, then prove that all sixteen requirement records agree before re-audit.

Purpose: Restore truthful, parser-supported planning metadata without changing firmware behavior, replaying evidence collection, or laundering a semantic gap through status edits.
Output: A narrowly backfilled Phase 31 summary, exact Phase 31/32/34 roadmap inventories, supported requirement-status mutations, and a deterministic sixteen-ID validation result.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/39-milestone-metadata-reconciliation/39-CONTEXT.md
@.planning/phases/39-milestone-metadata-reconciliation/39-RESEARCH.md
@.planning/phases/39-milestone-metadata-reconciliation/39-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill canonical Phase 31 completion provenance</name>
  <read-first>
    .planning/phases/31-final-evidence-intake/31-01-PLAN.md
    .planning/phases/31-final-evidence-intake/31-01-SUMMARY.md
    .planning/phases/31-final-evidence-intake/31-VERIFICATION.md
    .planning/phases/36-normalize-evidence-and-blocker-rows/36-01-SUMMARY.md
  </read-first>
  <files>.planning/phases/31-final-evidence-intake/31-01-SUMMARY.md</files>
  <action>
Per D-01, insert one contiguous canonical YAML block, with no extra blank line, containing `requirements-completed:` followed by the four list items `INTAKE-01`, `INTAKE-02`, `INTAKE-03`, and `INTAKE-04` in that order. Prove this is the only file change by loading `HEAD:.planning/phases/31-final-evidence-intake/31-01-SUMMARY.md`, locating exactly one canonical block in the working copy, removing that block, and requiring byte-for-byte equality with HEAD. Preserve all existing narrative and provenance per D-02. Reject a duplicate canonical field, any `requirements_completed` underscore alias, or any modification to the extractor per D-03. Copy no raw evidence, secrets, packet bodies, keys, tokens, certificates, crash dumps, or service payloads.
  </action>
  <verify>
    <automated>git diff --check -- .planning/phases/31-final-evidence-intake/31-01-SUMMARY.md &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" summary-extract .planning/phases/31-final-evidence-intake/31-01-SUMMARY.md --fields requirements_completed | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s),a=j.requirements_completed??j,want=["INTAKE-01","INTAKE-02","INTAKE-03","INTAKE-04"];if(JSON.stringify(a)!==JSON.stringify(want))throw Error(`unexpected extraction ${JSON.stringify(a)}`)})' &amp;&amp; node -e 'const fs=require("fs"),cp=require("child_process"),path=".planning/phases/31-final-evidence-intake/31-01-SUMMARY.md",now=fs.readFileSync(path,"utf8"),before=cp.execFileSync("git",["show",`HEAD:${path}`],{encoding:"utf8"}),canonical="requirements-completed:\n  - INTAKE-01\n  - INTAKE-02\n  - INTAKE-03\n  - INTAKE-04\n";if((now.match(/^requirements-completed:/gm)||[]).length!==1)throw Error("canonical field must occur exactly once");if(/^requirements_completed:/m.test(now))throw Error("underscore alias forbidden");if((now.split(canonical).length-1)!==1)throw Error("canonical block must occur exactly once");if(now.replace(canonical,"")!==before)throw Error("Phase 31 summary changed beyond canonical block insertion")'</automated>
  </verify>
  <acceptance-criteria>
    - Supported extraction returns exactly the four ordered Phase 31 intake IDs.
    - All original Phase 31 lifecycle, status, and timestamp fields remain unchanged.
    - The diff is an additive canonical-field backfill only; no underscore alias or sensitive evidence is introduced.
  </acceptance-criteria>
  <done>The Phase 31 summary exposes exactly the four canonical intake IDs through `summary-extract`; removing the single canonical YAML block reconstructs the HEAD file byte-for-byte; and duplicate or underscore completion keys are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Repair exact Phase 31, 32, and 34 roadmap inventories</name>
  <read-first>
    .planning/ROADMAP.md
    .planning/phases/31-final-evidence-intake/31-01-PLAN.md
    .planning/phases/31-final-evidence-intake/31-01-SUMMARY.md
    .planning/phases/32-blocker-register-and-evidence-triage/32-01-PLAN.md
    .planning/phases/32-blocker-register-and-evidence-triage/32-01-SUMMARY.md
    .planning/phases/34-final-readiness-and-demotion-dry-run/34-01-PLAN.md
    .planning/phases/34-final-readiness-and-demotion-dry-run/34-01-SUMMARY.md
    .planning/phases/34-final-readiness-and-demotion-dry-run/34-02-PLAN.md
    .planning/phases/34-final-readiness-and-demotion-dry-run/34-02-SUMMARY.md
  </read-first>
  <files>.planning/ROADMAP.md</files>
  <action>
Per D-05, use the supported GSD mutation path first and run `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress 31`, then the same command for `32` and `34`. These canonical commands exclusively own the phase-detail count/status lines, which must finish as `**Plans**: 1/1 plans complete` for Phases 31 and 32 and `**Plans**: 2/2 plans complete` for Phase 34. The installed command cannot synthesize or replace stale plan-list filenames/descriptions and may rewrite historical completion dates in the Progress table. Record those tooling limitations in the Phase 39 validation evidence, then apply one bounded workflow-owned exception: replace only each `Plans:` list body beneath the canonical count line and restore the exact Phase 31, 32, and 34 Progress-table rows from `HEAD:.planning/ROADMAP.md` after the canonical commands run. In particular, preserve every byte of the original progress rows, including their historical completion dates. Use the repository's stable ` - ` separator and write exactly: `31-01-PLAN.md - Create the shared Phase 31 final-evidence intake gate and retained-receipt workflow.`, `32-01-PLAN.md - Create the Phase 32 blocker register and evidence-triage handoff over Phase 31 final-intake outputs.`, `34-01-PLAN.md - Create the Phase 34 readiness coverage ledger, demotion dry run, generated evidence bundle, and workflow wiring.`, and `34-02-PLAN.md - Close required-stream completeness gaps and add exception-bypass regressions.`; every entry is checked. Require the matching summary before retaining an entry. Per D-06, do not list Phase 36, 37, or 38 plans under these phases and do not add nonstandard provenance prose. Build the expected final roadmap from `HEAD:.planning/ROADMAP.md` by changing only the three known phase-detail count/status lines and replacing only the three list bodies with those exact entries; the Progress table remains the HEAD baseline. Require byte-for-byte equality with the working roadmap, and separately assert that each final Phase 31, 32, and 34 Progress row is byte-for-byte identical to HEAD. Arbitrary count-line, completion-date, or other ROADMAP changes must fail.
  </action>
  <verify>
    <automated>git diff --check -- .planning/ROADMAP.md &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase 31 &gt;/tmp/phase39-roadmap-31.json &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase 32 &gt;/tmp/phase39-roadmap-32.json &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase 34 &gt;/tmp/phase39-roadmap-34.json &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" roadmap analyze &gt;/tmp/phase39-roadmap-analyze.json &amp;&amp; node -e 'const fs=require("fs"),cp=require("child_process"),path=".planning/ROADMAP.md",now=fs.readFileSync(path,"utf8"),before=cp.execFileSync("git",["show",`HEAD:${path}`],{encoding:"utf8"}),blocks={31:"**Plans**: 1/1 plans complete\nPlans:\n- [x] 31-01-PLAN.md - Create the shared Phase 31 final-evidence intake gate and retained-receipt workflow.",32:"**Plans**: 1/1 plans complete\nPlans:\n- [x] 32-01-PLAN.md - Create the Phase 32 blocker register and evidence-triage handoff over Phase 31 final-intake outputs.",34:"**Plans**: 2/2 plans complete\nPlans:\n- [x] 34-01-PLAN.md - Create the Phase 34 readiness coverage ledger, demotion dry run, generated evidence bundle, and workflow wiring.\n- [x] 34-02-PLAN.md - Close required-stream completeness gaps and add exception-bypass regressions."};const bounds=(s,n)=&gt;{const start=s.indexOf(`### Phase ${n}:`);if(start&lt;0)throw Error(`missing phase ${n}`);const next=s.indexOf("\n### Phase ",start+1);return [start,next&lt;0?s.length:next+1]};const withBlock=(s,n,value)=&gt;{const [start,end]=bounds(s,n),sec=s.slice(start,end),at=sec.search(/^\*\*Plans\*\*:/m);if(at&lt;0)throw Error(`missing count ${n}`);return s.slice(0,start)+sec.slice(0,at)+value+(sec.endsWith("\n")?"\n":"")+s.slice(end)};const progressRow=(s,n)=&gt;{const rows=s.split("\n").filter(line=&gt;line.startsWith(`| ${n}. `));if(rows.length!==1)throw Error(`expected one Progress row for Phase ${n}`);return rows[0]};let expected=before;for(const n of [31,32,34])expected=withBlock(expected,n,blocks[n]);for(const n of [31,32,34])if(progressRow(now,n)!==progressRow(before,n))throw Error(`historical Progress row changed for Phase ${n}`);if(now!==expected)throw Error("ROADMAP differs from the three canonical detail count/status changes plus exact list-body replacements; Progress rows must remain at HEAD");const analysis=JSON.parse(fs.readFileSync("/tmp/phase39-roadmap-analyze.json","utf8"));for(const n of [31,32,34]){const count=n===34?2:1,p=analysis.phases.find(x=&gt;x.number===String(n)),gp=JSON.parse(fs.readFileSync(`/tmp/phase39-roadmap-${n}.json`,"utf8"));if(!p||p.plan_count!==count||p.summary_count!==count||p.disk_status!=="complete"||p.roadmap_complete!==true)throw Error(`disk/roadmap mismatch ${n}`);if(!gp.found||gp.section.slice(gp.section.search(/^\*\*Plans\*\*:/m))!==blocks[n])throw Error(`manual block mismatch ${n}`)}'</automated>
  </verify>
  <acceptance-criteria>
    - Manual roadmap detail and disk-derived analysis agree on 1/1 for Phase 31, 1/1 for Phase 32, and 2/2 for Phase 34.
    - The lists contain exactly `31-01`, `32-01`, `34-01`, and `34-02` under their owning phases.
    - The Phase 31, 32, and 34 Progress-table rows, including completion dates, are byte-for-byte identical to HEAD.
    - No later gap-closure plan is duplicated into an earlier phase and no unrelated roadmap content changes.
  </acceptance-criteria>
  <done>The canonical roadmap progress commands produce the exact `1/1`, `1/1`, and `2/2` complete detail count lines; the bounded exception produces the four exact checked ` - ` list entries and restores the three historical Progress rows byte-for-byte from HEAD; and the working roadmap equals the HEAD-derived expected file byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 3: Mutate supported statuses and run the sixteen-ID consistency gate</name>
  <read-first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/v1.3-MILESTONE-AUDIT.md
    .planning/phases/31-final-evidence-intake/31-VERIFICATION.md
    .planning/phases/36-normalize-evidence-and-blocker-rows/36-VERIFICATION.md
    .planning/phases/37-reconcile-decisions-into-readiness/37-VERIFICATION.md
    .planning/phases/38-fail-closed-cutover-workflow/38-VERIFICATION.md
    .planning/phases/39-milestone-metadata-reconciliation/39-VALIDATION.md
  </read-first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/39-milestone-metadata-reconciliation/39-VALIDATION.md</files>
  <action>
First confirm that supported Phase 31 summary extraction and the passed Phase 31, 36, 37, and 38 verification artifacts justify the affected rows. Then invoke the supported mutation command exactly once: `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete INTAKE-01 INTAKE-02 INTAKE-03 READY-02 READY-03 CUTOVER-01 CUTOVER-03`. Do not directly rewrite checkbox or traceability syntax. Build a deterministic pre-execution, read-only matrix whose canonical ID set is the sixteen distinct v1.3 IDs declared by `REQUIREMENTS.md`; for each ID require a checked checkbox, `Complete` traceability row, at least one occurrence from supported `summary-extract`, and a passed owning phase-verification row. This pre-execution evidence matrix intentionally does not require `39-01-SUMMARY.md` or `39-VERIFICATION.md`, because the executor and verifier create those artifacts only after these tasks finish. Separately parse `init milestone-op` and `roadmap analyze`: require v1.3, 10 phases, 9 complete, no phase-count mismatch, 33 plans, 32 summaries, Phase 39 planned with 1 plan/0 summaries and `roadmap_complete: false`, and every other Phase 31 through 40 entry complete. Parse STATE frontmatter/body and require 10 total phases, 9 completed phases, 32 total/completed plans, `9/10` phase progress, and `32/32` currently planned completion; this is consistent with roadmap's 32 summaries while the in-flight Phase 39 plan is not yet summarized. Reassert the exact Phase 31/32/34 manual lists and disk counts from Task 2. Reject missing, duplicate, unexpected, or contradictory rows and require an exact `16/16` evidence result per D-08 and D-09. Update only the three Phase 39 validation task rows and sign-off evidence with the commands/results actually observed, including the Task 2 tooling limitation. Do not edit `.planning/STATE.md` directly. Per D-10 and D-11, do not run the milestone-audit workflow or modify `.planning/v1.3-MILESTONE-AUDIT.md`; post-execution Phase 39 ownership and lifecycle validation are mandatory overall verification gates, not executor-task prerequisites.
  </action>
  <verify>
    <automated>git diff --check -- .planning/REQUIREMENTS.md .planning/phases/39-milestone-metadata-reconciliation/39-VALIDATION.md &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" init milestone-op &gt;/tmp/phase39-milestone-op.json &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" roadmap analyze &gt;/tmp/phase39-roadmap-analyze.json &amp;&amp; node -e 'const fs=require("fs"),cp=require("child_process"),req=fs.readFileSync(".planning/REQUIREMENTS.md","utf8"),state=fs.readFileSync(".planning/STATE.md","utf8"),roadmap=fs.readFileSync(".planning/ROADMAP.md","utf8"),milestone=JSON.parse(fs.readFileSync("/tmp/phase39-milestone-op.json","utf8")),analysis=JSON.parse(fs.readFileSync("/tmp/phase39-roadmap-analyze.json","utf8")),ids=[...req.matchAll(/^[-*] \[[ xX]\] \*\*([A-Z]+-[0-9]+)\*\*/gm)].map(m=&gt;m[1]),checked=new Set([...req.matchAll(/^[-*] \[[xX]\] \*\*([A-Z]+-[0-9]+)\*\*/gm)].map(m=&gt;m[1])),unique=[...new Set(ids)],trace=new Map(req.split("\n").filter(line=&gt;/^\|\s*[A-Z]+-[0-9]+\s*\|/.test(line)).map(line=&gt;{const cells=line.split("|").map(x=&gt;x.trim());return [cells[1],cells]}));if(unique.length!==16||ids.length!==16)throw Error(`expected 16 distinct v1.3 IDs, got ${unique.length}/${ids.length}`);for(const id of unique){if(!checked.has(id))throw Error(`unchecked ${id}`);if(!(trace.get(id)||[]).includes("Complete"))throw Error(`traceability ${id}`)}const summaries=cp.execFileSync("find",[".planning/phases","-maxdepth","2","-name","*-SUMMARY.md"],{encoding:"utf8"}).trim().split("\n").filter(Boolean),completed=new Set();for(const file of summaries){const parsed=JSON.parse(cp.execFileSync("node",[`${process.env.HOME}/.codex/get-shit-done/bin/gsd-tools.cjs`,"summary-extract",file,"--fields","requirements_completed"],{encoding:"utf8"}));for(const id of parsed.requirements_completed??parsed??[])completed.add(id)}for(const id of unique)if(!completed.has(id))throw Error(`summary ${id}`);const passed=cp.execFileSync("find",[".planning/phases","-maxdepth","2","-name","*-VERIFICATION.md"],{encoding:"utf8"}).trim().split("\n").filter(Boolean).map(f=&gt;fs.readFileSync(f,"utf8")).filter(s=&gt;/status:\s*passed/i.test(s)||/overall status[^\n]*passed/i.test(s)).join("\n");for(const id of unique)if(!passed.includes(id))throw Error(`verification ${id}`);if(milestone.milestone_version!=="v1.3"||milestone.phase_count!==10||milestone.completed_phases!==9||milestone.all_phases_complete!==false||milestone.phase_count_directory!==10||milestone.completed_phases_directory!==9||milestone.phase_count_mismatch!==false)throw Error("milestone-op totals");if(analysis.phase_count!==10||analysis.completed_phases!==9||analysis.total_plans!==33||analysis.total_summaries!==32||analysis.progress_percent!==97)throw Error("roadmap totals");for(const p of analysis.phases){if(p.number==="39"){if(p.plan_count!==1||p.summary_count!==0||p.disk_status!=="planned"||p.roadmap_complete!==false)throw Error("Phase 39 pre-execution state")}else if(Number(p.number)&gt;=31&amp;&amp;Number(p.number)&lt;=40&amp;&amp;(p.disk_status!=="complete"||p.roadmap_complete!==true))throw Error(`phase ${p.number} incomplete`)}const stateValues=Object.fromEntries([...state.matchAll(/^\s*([a-z_]+):\s*(\d+)$/gm)].map(m=&gt;[m[1],Number(m[2])]));if(stateValues.total_phases!==10||stateValues.completed_phases!==9||stateValues.total_plans!==32||stateValues.completed_plans!==32||!/9\/10 v1\.3 phases complete/.test(state)||!/32\/32 currently planned/.test(state))throw Error("STATE totals");const blocks={31:["- [x] 31-01-PLAN.md - Create the shared Phase 31 final-evidence intake gate and retained-receipt workflow."],32:["- [x] 32-01-PLAN.md - Create the Phase 32 blocker register and evidence-triage handoff over Phase 31 final-intake outputs."],34:["- [x] 34-01-PLAN.md - Create the Phase 34 readiness coverage ledger, demotion dry run, generated evidence bundle, and workflow wiring.","- [x] 34-02-PLAN.md - Close required-stream completeness gaps and add exception-bypass regressions."]},bounds=(s,n)=&gt;{const start=s.indexOf(`### Phase ${n}:`),next=s.indexOf("\n### Phase ",start+1);if(start&lt;0)throw Error(`missing phase ${n}`);return [start,next&lt;0?s.length:next+1]};for(const n of [31,32,34]){const [start,end]=bounds(roadmap,n),sec=roadmap.slice(start,end),lines=sec.slice(sec.indexOf("Plans:\n")+7).trimEnd().split("\n"),p=analysis.phases.find(x=&gt;x.number===String(n));if(JSON.stringify(lines)!==JSON.stringify(blocks[n])||p.plan_count!==blocks[n].length||p.summary_count!==blocks[n].length)throw Error(`inventory ${n}`)}console.log("16/16; phases 9/10; plans/summaries 33/32; STATE 32/32")' &amp;&amp; test -z "$(git diff --name-only -- .planning/v1.3-MILESTONE-AUDIT.md .planning/STATE.md)"</automated>
  </verify>
  <acceptance-criteria>
    - The supported command marks exactly the seven named pending requirement rows complete on both status surfaces.
    - The fail-closed matrix reports exactly 16/16 distinct IDs with checkbox, traceability, summary, and passed-verification agreement.
    - Roadmap, disk inventory, milestone, and state counts contain no contradiction.
    - Validation records observed evidence, while the canonical milestone audit and direct state metadata remain untouched.
  </acceptance-criteria>
  <done>The supported requirement mutation is complete; the pre-execution gate reports `16/16`, milestone phases `9/10`, roadmap plans/summaries `33/32`, and STATE completed plans `32/32`; exact inventories agree; and neither STATE nor the canonical audit changed.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary                                      | Description                                                                      |
| --------------------------------------------- | -------------------------------------------------------------------------------- |
| Historical summary → supported extractor      | Existing Phase 31 provenance is parsed into milestone completion claims.         |
| Phase directories → roadmap                   | Phase-local plan/summary artifacts determine the manual roadmap inventory.       |
| Passed verification → mutable status metadata | Evidence-backed outcomes authorize requirement and progress mutations.           |
| Planning metadata → future milestone audit    | Reconciled status becomes input to the later audit but must not predetermine it. |

## ASVS L1 STRIDE Threat Register

| Threat ID       | Category                             | Component                         | Disposition | Mitigation Plan                                                                                                                                               |
| --------------- | ------------------------------------ | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-01         | Tampering                            | Requirement completion status     | mitigate    | Require plan claim, supported summary extraction, and passed verification to agree for every ID before status mutation; fail on any missing or duplicate row. |
| T-39-02         | Tampering / Repudiation              | Phase 31 historical summary       | mitigate    | Permit only the canonical additive field and compare lifecycle, status, and timestamp provenance fields before and after the edit.                            |
| T-39-03         | Spoofing / Tampering                 | Roadmap plan inventory            | mitigate    | Match exact phase-prefixed filenames to existing plan/summary pairs and compare manual `get-phase` output with disk-derived `roadmap analyze`.                |
| T-39-04         | Repudiation / Elevation of Privilege | Milestone audit boundary          | mitigate    | Keep the canonical audit unchanged; require passed Phase 39 verification and lifecycle validation before the separate audit workflow may run.                 |
| T-39-05         | Information Disclosure               | Planning and validation artifacts | mitigate    | Record only IDs, paths, statuses, counts, sanitized references, and command outcomes; prohibit copying raw or secret-bearing evidence.                        |
| </threat-model> |                                      |                                   |             |                                                                                                                                                               |

<verification>
Run the three focused task checks, then run the complete metadata gate and repository-required checks:

1. `git diff --check`
1. Confirm `git diff --name-only` is restricted to the four execution files declared by this plan and excludes `.planning/v1.3-MILESTONE-AUDIT.md` and `.planning/STATE.md`.
1. Run supported summary extraction, all three `roadmap get-phase` checks, `roadmap analyze`, and the deterministic sixteen-ID matrix.
1. Run `bun scripts/bright-builds-check.ts all`.
1. Run, in order, `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
1. Post-execution ownership gate: fail unless `.planning/phases/39-milestone-metadata-reconciliation/39-01-SUMMARY.md` exists and `summary-extract --fields requirements_completed` returns exactly `INTAKE-01`, `INTAKE-02`, and `INTAKE-03`.
1. Post-verification lifecycle gate: fail unless `.planning/phases/39-milestone-metadata-reconciliation/39-VERIFICATION.md` exists with passed status and records `INTAKE-01`, `INTAKE-02`, and `INTAKE-03`; then run `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 39 --require-plans --require-verification --raw` and require success.
1. Do not refresh the milestone audit in this plan. Only after steps 6 and 7 pass may the separate milestone-audit workflow run.
   </verification>

<success-criteria>
- Phase 31 supported extraction returns exactly `INTAKE-01`, `INTAKE-02`, `INTAKE-03`, and `INTAKE-04`, with historical provenance unchanged.
- Roadmap inventories and disk artifacts agree exactly at Phase 31 `1/1`, Phase 32 `1/1`, and Phase 34 `2/2`.
- All sixteen v1.3 IDs pass the checkbox, traceability, summary, verification, roadmap, inventory, and milestone/state consistency gate.
- Phase 39 owns `INTAKE-01`, `INTAKE-02`, and `INTAKE-03`; Phase 36 retains separate `INTAKE-04` gap-closure provenance.
- No firmware, evidence-gate, Phase 40, state, or canonical milestone-audit content is changed by the plan tasks.
</success-criteria>

<output>
After completion, create `.planning/phases/39-milestone-metadata-reconciliation/39-01-SUMMARY.md`.
</output>
