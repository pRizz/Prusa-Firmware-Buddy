---
phase: 02-bazel-authority-and-developer-facade
plan: 01
subsystem: build
tags: [bazel, justfile, toolchains, platforms, verification]
requires:
  - Phase 1 baseline matrix
provides:
  - Root Bazel workspace entrypoint
  - Product platform labels
  - Registered firmware toolchain labels
  - Bazel workflow labels
  - Checked `justfile` facade
  - Phase 2 verification script
affects: [phase-3-artifact-parity, phase-4-rust-architecture, phase-5-foreign-code-boundary, phase-11-cutover]
tech-stack:
  added: [Bazel root module, justfile facade]
  patterns: [dry-run reference dispatcher, metadata toolchain labels, explicit platform constraints]
key-files:
  created:
    - MODULE.bazel
    - MODULE.bazel.lock
    - .bazelrc
    - BUILD.bazel
    - platforms/BUILD.bazel
    - tools/bazel/BUILD.bazel
    - tools/bazel/shell_rules.bzl
    - tools/bazel/reference_contract.sh
    - tools/bazel/phase2_verify.sh
    - tools/bazel/phase2_verify.py
    - tools/bazel/toolchains/BUILD.bazel
    - tools/bazel/toolchains/reference_toolchain.bzl
    - justfile
  modified:
    - .gitignore
key-decisions:
  - "Use Bazel labels as the developer authority surface while existing CMake/Python commands remain reference compatibility backends."
  - "Keep reference execution opt-in through BUDDY_BAZEL_EXECUTE_REFERENCE=1."
  - "Expose platform and workflow categories now so later phases extend stable labels."
patterns-established:
  - "Root `justfile` recipes delegate to Bazel labels."
  - "Phase verification checks graph queryability and facade discoverability."
requirements-completed: [BAZL-01, BAZL-02, BAZL-04]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-06-02T20-31-42
generated_at: 2026-06-02T20:31:42.861Z
duration: 20min
completed: 2026-06-02
---

# Phase 2: Bazel Authority and Developer Facade Summary

**Root Bazel graph, product platforms, workflow targets, and checked `justfile` facade**

## Performance

- **Duration:** 20 min
- **Started:** 2026-06-02T20:31:42Z
- **Completed:** 2026-06-02T20:35:09Z
- **Tasks:** 3 completed
- **Files modified:** 13 created and 1 modified by execution task, plus GSD planning artifacts

## Accomplishments

- Added root `MODULE.bazel`, `MODULE.bazel.lock`, `.bazelrc`, and `BUILD.bazel`.
- Added `/bazel-*` to `.gitignore` so normal Bazel runs do not dirty the worktree with output symlinks.
- Added explicit Bazel platforms for host tools/tests and supported embedded product combinations.
- Added registered toolchain labels for Rust firmware, C/C++ firmware, ASM firmware, and asset generation.
- Added Bazel workflow targets for bootstrap, firmware build, Rust firmware, retained foreign code, generated assets, host tools, unit tests, simulator inputs, simulator parity, release packaging, formatting, linting, generated-file checks, and Phase 2 verification.
- Added a root `justfile` facade with discoverable recipes that call Bazel labels.
- Added `tools/bazel/phase2_verify.py` to enforce the Phase 2 contract.

## Task Commits

Task commits are intentionally deferred. The invoked wrapper requires no commit or push before clean phase verification, so all Phase 2 changes remain uncommitted until the final wrapper gate passes.

## Files Created/Modified

- `MODULE.bazel` - Root Bzlmod module and toolchain registration.
- `MODULE.bazel.lock` - Bzlmod lockfile generated by Bazel query.
- `.bazelrc` - Host and product platform configs plus guarded reference execution default.
- `.gitignore` - Ignores Bazel output symlinks.
- `BUILD.bazel` - Root reference-source filegroups and Phase 2 verifier alias.
- `platforms/BUILD.bazel` - Runtime, printer, board, MCU constraints, and product platforms.
- `tools/bazel/toolchains/BUILD.bazel` - Toolchain types and registered toolchain labels.
- `tools/bazel/toolchains/reference_toolchain.bzl` - Minimal metadata toolchain rule.
- `tools/bazel/BUILD.bazel` - Workflow targets.
- `tools/bazel/shell_rules.bzl` - Repo-local shell executable rule for Bazel 9.
- `tools/bazel/reference_contract.sh` - Guarded dispatcher for reference compatibility commands.
- `tools/bazel/phase2_verify.sh` - Bazel wrapper for the verifier.
- `tools/bazel/phase2_verify.py` - Phase verifier.
- `justfile` - Developer workflow facade.

## Decisions Made

- Used repo-local shell target support instead of adding an external shell ruleset during bootstrap.
- Kept heavy reference workflows dry-run by default through `BUDDY_BAZEL_EXECUTE_REFERENCE=0`.
- Made category labels explicit for downstream phases instead of hiding them behind only one generic build target.
- Committed the Bzlmod lockfile and ignored Bazel output symlinks to avoid normal Bazel runs dirtying the tree.

## Deviations from Plan

None. During verification, native `sh_binary` was found unavailable in Bazel 9.1, so the plan was executed with a repo-local `shell_binary` rule that preserves the intended workflow labels.

## Issues Encountered

- Initial Bazel query failed because Bazel 9.1 does not define native `sh_binary`.
- `buildifier` is not installed locally; formatting was kept simple and checked by Bazel parsing plus `git diff --check`.

## User Setup Required

None for Phase 2. Developers need Bazel/Bazelisk and `just` to run the new facade locally.

## Next Phase Readiness

Phase 3 can attach real generator and release artifact actions to the stable Bazel labels created here. Phase 4 can replace the Rust firmware authority target with actual Rust crate builds and concrete compiler toolchains.

---

*Phase: 02-bazel-authority-and-developer-facade*
*Completed: 2026-06-02*
