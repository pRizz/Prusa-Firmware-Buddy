---
phase: 06-printing-core-safety-and-feature-gates
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - rust/crates/domain/src/lib.rs
  - rust/crates/domain/src/print.rs
  - tools/bazel/manifests/phase6_printing_core.json
autonomous: true
requirements:
  - CORE-03
requirements_addressed:
  - CORE-03
must_haves:
  truths:
    - "Developer can model file printing and serial printing as distinct print sources."
    - "Developer can reject impossible pause, resume, cancel, finish, and recovery transitions through typed Rust state."
    - "Developer can classify G-code routing and Buddy-specific G/M-code handlers without rewriting the retained planner."
    - "Printing behavior claims stay tied to retained Marlin/Buddy source paths and Phase 6 fixture identities."
  artifacts:
    - path: "rust/crates/domain/src/print.rs"
      provides: "Typed print job state, fixture identity, command routing, and planner-visible flow policies"
      contains: "PrintJobState"
    - path: "rust/crates/domain/src/lib.rs"
      provides: "Public domain exports for print policy types"
      contains: "pub mod print"
    - path: "tools/bazel/manifests/phase6_printing_core.json"
      provides: "CORE-03 source-backed printing contract rows"
      contains: "rust/crates/domain/src/print.rs"
  key_links:
    - from: "rust/crates/domain/src/lib.rs"
      to: "rust/crates/domain/src/print.rs"
      via: "public module export"
      pattern: "pub mod print"
    - from: "tools/bazel/manifests/phase6_printing_core.json"
      to: "src/common/marlin_server.cpp"
      via: "retained reference source path"
      pattern: "src/common/marlin_server.cpp"
    - from: "tools/bazel/manifests/phase6_printing_core.json"
      to: "rust/crates/domain/src/print.rs"
      via: "rust_surface field"
      pattern: "PrintJobState"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-04T09-48-48
generated_at: 2026-06-04T10:06:26Z
---

<objective>
Add typed Rust printing-core policy surfaces for CORE-03 and connect them to retained Marlin/Buddy fixture contracts.

Purpose: The Rust port gets explicit, locally testable print state and command-routing contracts while retained Marlin remains the behavior oracle per D-01 through D-03.
Output: `rust/crates/domain/src/print.rs`, domain exports, focused unit tests, and updated printing manifest rows.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-VALIDATION.md
@.planning/phases/06-printing-core-safety-and-feature-gates/06-01-PLAN.md
@tools/bazel/manifests/phase6_printing_core.json
@tools/bazel/phase6_verify.py
@rust/crates/domain/src/lib.rs
@rust/crates/domain/src/product.rs
@rust/crates/domain/src/feature.rs
@src/common/marlin_server.cpp
@src/common/marlin_client.cpp
@src/common/marlin_server_request.hpp
@src/common/marlin_client_queue.hpp
@src/common/marlin_vars.cpp
@src/common/serial_printing.cpp
@src/common/gcode/gcode_reader_interface.hpp
@src/marlin_stubs/gcode.cpp
@src/marlin_stubs/M0.cpp
@src/marlin_stubs/pause/M600.cpp
@lib/AddMarlin.cmake
</context>

<interfaces>
From `rust/crates/domain/src/lib.rs`:
```rust
#![forbid(unsafe_code)]
pub mod feature;
pub mod product;
pub use feature::{Feature, FeatureSet};
pub use product::{BoardKind, BootloaderMode, McuKind, PrinterKind, ProductProfile};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add print state and command routing model</name>
  <read_first>
    rust/crates/domain/src/lib.rs
    rust/crates/domain/src/product.rs
    rust/crates/domain/src/feature.rs
    tools/bazel/manifests/phase6_printing_core.json
    .planning/phases/06-printing-core-safety-and-feature-gates/06-CONTEXT.md
    .planning/phases/06-printing-core-safety-and-feature-gates/06-RESEARCH.md
    src/common/marlin_server.cpp
    src/common/marlin_client.cpp
    src/common/marlin_server_request.hpp
    src/common/marlin_client_queue.hpp
    src/common/marlin_vars.cpp
    src/common/serial_printing.cpp
    src/common/gcode/gcode_reader_interface.hpp
    src/marlin_stubs/gcode.cpp
    src/marlin_stubs/M0.cpp
    src/marlin_stubs/pause/M600.cpp
    lib/AddMarlin.cmake
  </read_first>
  <files>rust/crates/domain/src/lib.rs, rust/crates/domain/src/print.rs</files>
  <behavior>
    - Test 1: `FixtureId::new("print-file-start-preview-stream-recovery")` succeeds, while empty, whitespace-only, and path-like fixture IDs fail.
    - Test 2: `PrintJobState::Idle` plus `PrintCommand::StartFile` transitions to `PrintJobState::Previewing(PrintSource::File)`, then `PrintCommand::ConfirmPreview` transitions to `PrintJobState::Printing(PrintSource::File)`.
    - Test 3: `PrintJobState::Idle` plus `PrintCommand::StartSerial` transitions directly to `PrintJobState::Printing(PrintSource::Serial)`.
    - Test 4: pause/resume/cancel transitions are accepted only from printing-compatible states and invalid transitions return `PrintTransitionError::UnsupportedTransition`.
    - Test 5: `route_gcode_mnemonic` maps `M862.1`, `M862.2`, `M862.3`, `M862.4`, `M862.5`, `M862.6`, `M600`, and `M0` to `CommandRoute::BuddyGcodeHandler`; maps `G0`, `G1`, `M104`, `M109`, `M140`, and `M190` to `CommandRoute::MarlinQueue`.
  </behavior>
  <action>Create `rust/crates/domain/src/print.rs` with safe pure Rust types only; keep `#![forbid(unsafe_code)]` in `rust/crates/domain/src/lib.rs`. Add `pub mod print` and public re-exports for `CommandRoute`, `FixtureId`, `GcodeMnemonic`, `PlannerFlowState`, `PrintCommand`, `PrintJobState`, `PrintSource`, `PrintTransitionError`, `route_gcode_mnemonic`, and `transition_print_state`. Implement `FixtureId` as a fallible newtype that rejects empty values, whitespace-only values, and values containing `/` or `\`. Implement `PrintSource::{File, Serial}` so serial and file printing cannot collapse into one flow per D-01 and the research pitfall. Implement `PrintJobState::{Idle, Previewing(PrintSource), Printing(PrintSource), Pausing(PrintSource), Paused(PrintSource), Resuming(PrintSource), Cancelling(PrintSource), Finished, PowerPanicAwaitingResume(PrintSource), MediaErrorAwaitingRecovery}`. Implement `PrintCommand::{StartFile(FixtureId), ConfirmPreview, StartSerial, Pause, PauseComplete, Resume, PlannerReady, Cancel, CancelComplete, Finish, EnterPowerPanic, RecoverPowerPanic, EnterMediaError, RecoverMediaError}`. Implement `PlannerFlowState::{NoActivePrint, WaitingForPreview, PlannerActive, PlannerPaused, PlannerCancelling, RecoveryRequired}`. Implement `transition_print_state` as a pure function with explicit match arms and no retained C/C++ calls. Implement `GcodeMnemonic::new` to uppercase ASCII command text and reject empty text; implement `route_gcode_mnemonic` with exact routing values from the behavior tests. Do not rewrite Marlin motion, queueing, media prefetch, or planner algorithms; this task models the accepted contract surface only, per D-03.</action>
  <verify>
    <automated>cargo test --all-features -p buddy-domain print</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --all-features -p buddy-domain print` exits 0.
    - `rg "pub mod print|PrintJobState|PrintSource|PrintCommand|PlannerFlowState|route_gcode_mnemonic|transition_print_state" rust/crates/domain/src/lib.rs rust/crates/domain/src/print.rs` finds all seven strings.
    - `rg "StartSerial|StartFile|PowerPanicAwaitingResume|MediaErrorAwaitingRecovery|BuddyGcodeHandler|MarlinQueue" rust/crates/domain/src/print.rs` finds all six strings.
    - `rg "// Arrange|// Act|// Assert" rust/crates/domain/src/print.rs` finds all three test-section markers.
  </acceptance_criteria>
  <done>Printing state, fixture identity, serial/file distinction, pause/resume/cancel transitions, planner-visible state, and command routing are represented as tested Rust domain policies.</done>
</task>

<task type="auto">
  <name>Task 2: Bind print policies to reference fixture rows</name>
  <read_first>
    tools/bazel/manifests/phase6_printing_core.json
    tools/bazel/phase6_verify.py
    rust/crates/domain/src/print.rs
    src/common/marlin_server.cpp
    src/common/marlin_client.cpp
    src/common/marlin_server_request.hpp
    src/common/marlin_client_queue.hpp
    src/common/marlin_vars.cpp
    src/common/serial_printing.cpp
    src/common/gcode/gcode_reader_interface.hpp
    src/marlin_stubs/gcode.cpp
    src/marlin_stubs/M0.cpp
    src/marlin_stubs/pause/M600.cpp
    lib/AddMarlin.cmake
  </read_first>
  <files>tools/bazel/manifests/phase6_printing_core.json</files>
  <action>Update each `printing_contracts` row in `tools/bazel/manifests/phase6_printing_core.json` so `rust_surface` names the exact Rust type or function that implements the local policy claim. Use these exact pairings: `print-gcode-routing` -> `rust/crates/domain/src/print.rs::route_gcode_mnemonic`; `print-serial-start-pause-resume-cancel` -> `rust/crates/domain/src/print.rs::PrintSource::Serial`; `print-file-start-preview-stream-recovery` -> `rust/crates/domain/src/print.rs::PrintSource::File`; `print-planner-visible-flow` -> `rust/crates/domain/src/print.rs::PlannerFlowState`; `print-buddy-gmcode-handlers` -> `rust/crates/domain/src/print.rs::CommandRoute::BuddyGcodeHandler`. Ensure `source_paths` still include retained oracle paths and that `intentional_delta` is `null` unless the row names a deliberate behavior change with a requirement ID and evidence. Keep every row `requirement: "CORE-03"`. Do not remove retained Marlin/Buddy source paths after adding Rust surfaces.</action>
  <verify>
    <automated>python3 tools/bazel/phase6_verify.py --printing-only &amp;&amp; cargo test --all-features -p buddy-domain print</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase6_verify.py --printing-only` exits 0.
    - `cargo test --all-features -p buddy-domain print` exits 0.
    - `rg "route_gcode_mnemonic|PrintSource::Serial|PrintSource::File|PlannerFlowState|CommandRoute::BuddyGcodeHandler" tools/bazel/manifests/phase6_printing_core.json` finds all five strings.
    - `rg "src/common/marlin_server.cpp|src/common/serial_printing.cpp|src/marlin_stubs/gcode.cpp|lib/AddMarlin.cmake" tools/bazel/manifests/phase6_printing_core.json` finds all four strings.
  </acceptance_criteria>
  <done>CORE-03 manifest rows identify the exact Rust print policy surfaces and the retained source oracle paths that justify them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw fixture/G-code text -> Rust domain types | Raw identifiers and command mnemonics must be parsed before policy code consumes them. |
| Rust print policy -> retained Marlin/Buddy stack | Local Rust state transitions describe accepted contracts but retained C/C++ remains the behavior oracle. |
| Host tests -> planner/motion behavior | Unit tests prove pure transitions and routing only, not physical planner or motion equivalence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `FixtureId` parsing | mitigate | Fallible constructor rejects empty, whitespace-only, and path-like fixture IDs before manifests or policies use them. |
| T-06-02-02 | Tampering / Denial of service | Print state transitions | mitigate | `transition_print_state` rejects impossible pause/resume/cancel/recovery transitions with `UnsupportedTransition`. |
| T-06-02-03 | Tampering | G-code routing classification | mitigate | `route_gcode_mnemonic` maps named Buddy handlers and Marlin queue commands explicitly and is covered by focused unit tests. |
| T-06-02-04 | Repudiation | Planner and motion parity claims | mitigate | Manifest rows keep retained `src/common/marlin_server.cpp`, `src/common/serial_printing.cpp`, and `lib/Marlin/` source paths; no host test claims planner hardware parity. |
| T-06-02-05 | Denial of service / Safety | File and serial flow collapse | mitigate | `PrintSource::{File, Serial}` and transition tests preserve separate serial/file behavior families. |
</threat_model>

<verification>
- `cargo test --all-features -p buddy-domain print`
- `python3 tools/bazel/phase6_verify.py --printing-only`
- `python3 tools/bazel/phase6_verify.py --quick`
</verification>

<success_criteria>
Plan 02 succeeds when CORE-03 has tested Rust print state and routing policies, file and serial flows remain distinct, impossible transitions are rejected, and all print manifest rows tie Rust surfaces to retained reference sources.
</success_criteria>

<output>
After completion, create `.planning/phases/06-printing-core-safety-and-feature-gates/06-02-SUMMARY.md`.
</output>
