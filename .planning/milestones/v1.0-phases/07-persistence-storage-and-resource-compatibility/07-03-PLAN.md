---
phase: 07-persistence-storage-and-resource-compatibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/crates/domain/src/storage.rs
  - rust/crates/domain/src/resource.rs
  - rust/crates/domain/src/lib.rs
autonomous: true
requirements:
  - IFCE-04
  - IFCE-05
requirements_addressed:
  - IFCE-04
  - IFCE-05
must_haves:
  truths:
    - "Rust domain code represents Phase 7 storage keys, raw reference hash names, schema versions, migration windows, journal/hash facts, credential redaction, filesystem surfaces, resource surfaces, generated output ownership, and fixture identities as typed values."
    - "Invalid storage/resource identifiers, invalid migration windows, invalid journal hash widths, unredacted credential policies, invalid evidence classes, and path-like fixture IDs fail through fallible constructors."
    - "Pure `buddy-domain` Phase 7 modules remain unsafe-free and unit tested with Arrange/Act/Assert structure."
  artifacts:
    - path: "rust/crates/domain/src/storage.rs"
      provides: "IFCE-04 storage and filesystem domain contracts"
      contains: "ReferenceHashName"
    - path: "rust/crates/domain/src/resource.rs"
      provides: "IFCE-05 resource and generated-output domain contracts"
      contains: "ResourceSurface"
    - path: "rust/crates/domain/src/lib.rs"
      provides: "Public exports and invariant error variants for Phase 7 domain types"
      contains: "CredentialRedactionPolicy"
  key_links:
    - from: "rust/crates/domain/src/lib.rs"
      to: "rust/crates/domain/src/storage.rs"
      via: "public module export"
      pattern: "pub mod storage"
    - from: "rust/crates/domain/src/lib.rs"
      to: "rust/crates/domain/src/resource.rs"
      via: "public module export"
      pattern: "pub mod resource"
    - from: "rust/crates/domain/src/resource.rs"
      to: "tools/bazel/generated_drift.py"
      via: "generated label string validation tests"
      pattern: "GeneratedOutputOwnership"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-06T04-24-25
generated_at: 2026-06-06T04:45:46Z
---

<objective>
Extend `buddy-domain` with pure Rust Phase 7 storage and resource compatibility contracts.

Purpose: The Rust port needs typed compatibility surfaces instead of passing config keys, journal hash facts, filesystem paths, resource identities, generated-output ownership, and fixture IDs as unchecked strings.
Output: Extended `storage.rs`, new `resource.rs`, updated `lib.rs`, and passing Rust tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
@.planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@Cargo.toml
@rust/crates/domain/Cargo.toml
@rust/crates/domain/src/lib.rs
@rust/crates/domain/src/storage.rs
@rust/crates/domain/src/artifact.rs
@rust/crates/domain/src/product.rs
@tools/bazel/generated_drift.py
</context>

<interfaces>
From `rust/crates/domain/src/storage.rs`:
```rust
pub struct StorageKey(String);
pub struct StorageSchemaVersion(u16);
pub struct MigrationWindow {
    from: StorageSchemaVersion,
    to: StorageSchemaVersion,
}
```

From `rust/crates/domain/src/artifact.rs`:
```rust
pub enum ArtifactKind {
    Bin,
    Bbf,
    Dfu,
    Map,
    ResourceImage,
    ResourcePackage,
    Provenance,
    AuxiliaryManifest,
}
pub struct ArtifactFileName(String);
pub struct ArtifactRequest {
    profile: ProductProfile,
    kind: ArtifactKind,
    name: ArtifactFileName,
}
```

From `rust/crates/domain/src/lib.rs`:
```rust
#![forbid(unsafe_code)]
pub mod storage;
pub use storage::{MigrationWindow, StorageKey, StorageSchemaVersion};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add storage, journal, credential, filesystem, and fixture domain types</name>
  <read_first>
    rust/crates/domain/src/storage.rs
    rust/crates/domain/src/lib.rs
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
    src/persistent_stores/store_instances/config_store/store_definition.hpp
    src/persistent_stores/store_instances/config_store/old_eeprom/last_migration.hpp
    src/persistent_stores/journal/store.hpp
    utils/persistent_stores/journal_hashes_generator.py
    src/buddy/filesystem_fatfs.cpp
    src/buddy/filesystem_littlefs_internal.cpp
    src/buddy/filesystem_littlefs_bbf.cpp
    src/buddy/filesystem_semihosting.cpp
  </read_first>
  <files>rust/crates/domain/src/storage.rs, rust/crates/domain/src/lib.rs</files>
  <behavior>
    - Test 1: `ReferenceHashName::parse("WIFI AP Password")` succeeds even though `StorageKey::parse("WIFI AP Password")` fails, preserving raw reference hash names per D-01 and D-03.
    - Test 2: `ReferenceHashName::parse("")` fails with `InvariantError::EmptyReferenceHashName`.
    - Test 3: `JournalHashFact::new(14, 0x3FFF)` succeeds and `JournalHashFact::new(13, 0x1FFF)` fails with `InvariantError::InvalidJournalHashFact`.
    - Test 4: `CredentialRedactionPolicy::NameOnlyRedacted.allows_value_material()` returns `false`.
    - Test 5: `FilesystemSurface::UsbFatFs.runtime_path()` returns `/usb`, `InternalLittleFs` returns `/internal`, `BbfLittleFs` returns `/bbf`, and `Semihosting` returns `/semihosting`.
    - Test 6: `EvidenceClass::parse("manual-hardware-required")` succeeds and `EvidenceClass::parse("hardware verified locally")` fails.
    - Test 7: `FixtureIdentity::parse("../secret.bin")` fails with `InvariantError::FixtureIdentityContainsPath`.
  </behavior>
  <action>Extend `rust/crates/domain/src/storage.rs` per D-01, D-03, D-05, D-06, and D-07 without removing existing `StorageKey`, `StorageSchemaVersion`, or `MigrationWindow`. Add public types `ReferenceHashName`, `JournalHashFact`, `CredentialRedactionPolicy`, `EvidenceClass`, `FilesystemSurface`, `StorageCompatibilitySurface`, and `FixtureIdentity`. `ReferenceHashName` must accept non-empty printable ASCII names including spaces and apostrophes, reject empty strings, reject control characters, and expose `as_str()`. `JournalHashFact::new(mask_bits: u8, mask: u16)` must accept only `mask_bits == 14` and `mask == 0x3FFF`. `CredentialRedactionPolicy` must have at least variant `NameOnlyRedacted` and a method `allows_value_material(self) -> bool` returning `false` for that variant. `EvidenceClass::parse` must accept exactly `manifest-check`, `source-audit`, `static-source-audit`, `host-test`, `rust-host-test`, `simulator-flow`, `hardware-smoke`, and `manual-hardware-required`. `FilesystemSurface` must encode variants `UsbFatFs`, `InternalLittleFs`, `BbfLittleFs`, `Semihosting`, `RootListing`, `LibsysbaseDevoptab`, and `EepromStorageDriver`, with `runtime_path()` returning exact strings `/usb`, `/internal`, `/bbf`, `/semihosting`, `/`, `POSIX-like devoptab`, and `EEPROM/internal flash`. `StorageCompatibilitySurface` must combine a `ReferenceHashName` or `FilesystemSurface` with `EvidenceClass` and `CredentialRedactionPolicy` where appropriate so credential rows cannot be represented with value material. `FixtureIdentity::parse` must reject empty strings, slash or backslash path separators, `..` path segments, and strings longer than 96 bytes. Add `InvariantError` variants for `EmptyReferenceHashName`, `InvalidReferenceHashName`, `InvalidJournalHashFact`, `InvalidEvidenceClass`, `EmptyFixtureIdentity`, and `FixtureIdentityContainsPath`, with `Display` text matching the variant purpose. Add focused Arrange/Act/Assert unit tests in `storage.rs` for each behavior above.</action>
  <verify>
    <automated>cargo test -p buddy-domain storage::tests --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p buddy-domain storage::tests --all-features` exits 0.
    - `rg "ReferenceHashName|JournalHashFact|CredentialRedactionPolicy|FilesystemSurface|StorageCompatibilitySurface|FixtureIdentity|NameOnlyRedacted|0x3FFF|manual-hardware-required|UsbFatFs|InternalLittleFs|BbfLittleFs|Semihosting" rust/crates/domain/src/storage.rs` finds every string.
    - `rg "EmptyReferenceHashName|InvalidReferenceHashName|InvalidJournalHashFact|InvalidEvidenceClass|EmptyFixtureIdentity|FixtureIdentityContainsPath" rust/crates/domain/src/lib.rs` finds every error variant.
    - `rg "unsafe \\{|unsafe fn|unsafe trait|unsafe impl|unsafe extern|#\\[allow\\(unsafe_code\\)\\]" rust/crates/domain/src/storage.rs` finds nothing.
  </acceptance_criteria>
  <done>Storage compatibility concepts are typed, tested, exported through `buddy-domain`, and preserve raw reference names separately from Rust-safe manifest IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resource and generated-output domain types</name>
  <read_first>
    rust/crates/domain/src/resource.rs
    rust/crates/domain/src/lib.rs
    rust/crates/domain/src/storage.rs
    rust/crates/domain/src/artifact.rs
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-CONTEXT.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-RESEARCH.md
    .planning/phases/07-persistence-storage-and-resource-compatibility/07-VALIDATION.md
    src/resources/CMakeLists.txt
    src/lang/CMakeLists.txt
    tools/bazel/generated_drift.py
  </read_first>
  <files>rust/crates/domain/src/resource.rs, rust/crates/domain/src/lib.rs</files>
  <behavior>
    - Test 1: `ResourceRuntimePath::parse("/web/index.html")`, `parse("/esp/uart_wifi.bin")`, `parse("qoi.data")`, and `parse("resources/revision_standard.hpp")` succeed.
    - Test 2: `ResourceRuntimePath::parse("../web/index.html")` fails with `InvariantError::ResourcePathContainsTraversal`.
    - Test 3: `GeneratedOutputOwnership::parse("tracked-reviewed-source")` and `parse("generated-at-build")` succeed; any other string fails.
    - Test 4: `BazelLabel::parse("//tools/bazel:generated_resources_check")` succeeds and `BazelLabel::parse("tools/bazel:generated_resources_check")` fails.
    - Test 5: `ResourceSurface::StandardImage.required_runtime_paths()` includes `qoi.data` and `resources/revision_standard.hpp`.
    - Test 6: `GeneratedSurface::new` rejects a check label not ending in `_check` and an update label not ending in `_update`.
  </behavior>
  <action>Create `rust/crates/domain/src/resource.rs` per D-03, D-08, D-09, and D-10 and add it to `rust/crates/domain/src/lib.rs` as `pub mod resource;` with public exports. Add public types `ResourceRuntimePath`, `ResourceSurface`, `GeneratedOutputOwnership`, `BazelLabel`, and `GeneratedSurface`. `ResourceRuntimePath::parse` must reject empty strings, backslashes, `..` path segments, control characters, and strings longer than 160 bytes. `GeneratedOutputOwnership::parse` must accept only `tracked-reviewed-source` and `generated-at-build`. `BazelLabel::parse` must accept only labels starting with `//` and containing one `:` after the package path. `ResourceSurface` must include variants `StandardImage`, `BootloaderImage`, `Esp32Blobs`, `Esp8266Blobs`, `WuiStaticAssets`, `QoiData`, `LanguagePacks`, `FontAssets`, `MmuFirmware`, `HashAndRevision`, and `RuntimeBootstrap`. `ResourceSurface::required_runtime_paths()` must return exact runtime path slices for the relevant variants including `/esp/uart_wifi.bin`, `/esp/bootloader.bin`, `/esp/partition-table.bin`, `/web/index.html`, `/web/favicon.ico`, `qoi.data`, `/bootloader.bin`, `/mmu/fw.bin`, and `resources/revision_standard.hpp`. `GeneratedSurface::new(id, ownership, check_label, update_label)` must reject check labels not ending in `_check` and update labels not ending in `_update`. Add `InvariantError` variants `EmptyResourcePath`, `InvalidResourcePath`, `ResourcePathContainsTraversal`, `InvalidGeneratedOutputOwnership`, `InvalidBazelLabel`, `GeneratedCheckLabelMismatch`, and `GeneratedUpdateLabelMismatch`, with `Display` text. Add focused Arrange/Act/Assert unit tests in `resource.rs` for all behavior above.</action>
  <verify>
    <automated>cargo test -p buddy-domain resource::tests --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p buddy-domain resource::tests --all-features` exits 0.
    - `rg "pub mod resource|ResourceRuntimePath|ResourceSurface|GeneratedOutputOwnership|BazelLabel|GeneratedSurface" rust/crates/domain/src/lib.rs rust/crates/domain/src/resource.rs` finds every string.
    - `rg "StandardImage|BootloaderImage|Esp32Blobs|Esp8266Blobs|WuiStaticAssets|QoiData|LanguagePacks|FontAssets|MmuFirmware|HashAndRevision|RuntimeBootstrap" rust/crates/domain/src/resource.rs` finds every variant.
    - `rg "/web/index.html|/esp/uart_wifi.bin|qoi.data|resources/revision_standard.hpp|tracked-reviewed-source|generated-at-build|_check|_update" rust/crates/domain/src/resource.rs` finds every required string.
    - `rg "EmptyResourcePath|InvalidResourcePath|ResourcePathContainsTraversal|InvalidGeneratedOutputOwnership|InvalidBazelLabel|GeneratedCheckLabelMismatch|GeneratedUpdateLabelMismatch" rust/crates/domain/src/lib.rs` finds every error variant.
    - `rg "unsafe \\{|unsafe fn|unsafe trait|unsafe impl|unsafe extern|#\\[allow\\(unsafe_code\\)\\]" rust/crates/domain/src/resource.rs` finds nothing.
  </acceptance_criteria>
  <done>Resource and generated-output compatibility concepts are represented as pure Rust domain types with tests and public exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw manifest/source strings -> Rust domain values | Storage, resource, fixture, generated-label, and evidence strings must be parsed into domain types before use. |
| Credential-bearing key identity -> Rust contracts | Rust types may represent key identity but must not permit credential value material for Phase 7 evidence. |
| Pure domain crate -> unsafe operations | `buddy-domain` must not introduce unsafe code for compatibility modeling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | Storage and resource identifiers | mitigate | Fallible constructors reject empty, path-like, traversal, invalid evidence, invalid ownership, and invalid label values. |
| T-07-03-02 | Information Disclosure | Credential redaction policy | mitigate | `CredentialRedactionPolicy::NameOnlyRedacted` must return `false` for value material and storage tests must preserve name-only behavior. |
| T-07-03-03 | Tampering / Denial of Service | Journal hash facts | mitigate | `JournalHashFact::new` accepts only `mask_bits == 14` and `mask == 0x3FFF`, preserving reference hash-space identity. |
| T-07-03-04 | Elevation of privilege | Pure domain unsafe posture | mitigate | Keep `#![forbid(unsafe_code)]`, scan touched domain modules for unsafe syntax, and run Rust tests. |
</threat_model>

<verification>
- `cargo test -p buddy-domain storage::tests --all-features`
- `cargo test -p buddy-domain resource::tests --all-features`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
Plan 03 succeeds when Phase 7 storage/resource compatibility concepts are represented by pure, tested, unsafe-free Rust domain types and exported through `buddy-domain`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-persistence-storage-and-resource-compatibility/07-03-SUMMARY.md`.
</output>
