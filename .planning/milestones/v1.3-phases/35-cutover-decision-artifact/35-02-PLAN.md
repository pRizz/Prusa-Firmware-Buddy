---
phase: 35-cutover-decision-artifact
plan: "02"
type: execute
wave: 2
depends_on:
  - "35-01"
gap_closure: true
files_modified:
  - tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
  - tools/bazel/phase35_cutover_decision_artifact.py
  - tools/bazel/phase35_cutover_decision_artifact_test.py
autonomous: true
requirements:
  - CUTOVER-01
  - CUTOVER-02
  - CUTOVER-03
must_haves:
  truths:
    - "When any required Phase 34 or reached Phase 31-33 source is missing, malformed, unreadable, stale, lifecycle-mismatched, secret-tainted, or unsafe, the Phase 35 command returns nonzero only after the canonical output contains a durable blocked decision and targeted-repair route."
    - "A failed regeneration atomically removes every prior approved decision, production-cutover route, audit index, report, and snapshot from the canonical Phase 35 output."
    - "The source-failure decision keeps cutover verdict, demotion decision validation/value/source refs, and demotion gate state/reasons independent; it never synthesizes demotion approval or an open gate."
    - "A valid source bundle still produces the complete eight-artifact Phase 35 bundle and preserves all three verdict and route truth tables from Plan 35-01."
    - "End-to-end regressions begin with a prior approved output, exercise every source-failure family, assert a nonzero command result, and prove no approved artifact remains authoritative."
  artifacts:
    - path: "tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json"
      provides: "Exact minimal source-failure bundle schema, safe reason taxonomy, and no-authority defaults"
      contains: "source_failure_behavior"
    - path: "tools/bazel/phase35_cutover_decision_artifact.py"
      provides: "Atomic full/failure bundle installation and durable fail-closed CLI behavior"
      contains: "write_source_failure_bundle"
    - path: "tools/bazel/phase35_cutover_decision_artifact_test.py"
      provides: "End-to-end stale-approval replacement regressions for every source-failure family"
      contains: "prior_approved"
  key_links:
    - from: "tools/bazel/phase35_cutover_decision_artifact.py"
      to: "build/ci-evidence/phase34/final-readiness-run-manifest.json"
      via: "source loading inside a guarded quick-generation transaction"
      pattern: "load_bundle"
    - from: "tools/bazel/phase35_cutover_decision_artifact.py"
      to: "build/ci-evidence/phase35/cutover-decision.json"
      via: "validated staging bundle followed by canonical directory replacement"
      pattern: "write_source_failure_bundle|install_staged_bundle"
    - from: "build/ci-evidence/phase35/cutover-decision.json"
      to: "build/ci-evidence/phase35/next-milestone-route.json"
      via: "blocked source-failure projection with a fresh-decision requirement and no production authority"
      pattern: "targeted-blocker-repair"
    - from: "tools/bazel/phase35_cutover_decision_artifact_test.py"
      to: "tools/bazel/phase35_cutover_decision_artifact.py"
      via: "main --quick execution against isolated prior-approved outputs and corrupted source fixtures"
      pattern: "main.*--quick"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 35-2026-07-25T21-06-10
generated_at: 2026-07-25T23:30:08Z
---

<objective>
Close the Phase 35 artifact-boundary gap that lets a stale approved decision survive a failed regeneration.

Purpose: CUTOVER-01 is not fail-closed if source validation returns nonzero while a previous approval remains at the canonical output path. This plan implements the already-locked D-21/action 9 behavior: every Phase 34/source failure must durably replace stale output with a minimal blocked manifest, decision, and targeted-repair route before the command fails.

Output: A contract-defined source-failure bundle, atomic canonical-output replacement, safe error classification, and end-to-end regressions proving stale approval cannot survive.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cutover-decision-artifact/35-CONTEXT.md
@.planning/phases/35-cutover-decision-artifact/35-RESEARCH.md
@.planning/phases/35-cutover-decision-artifact/35-VALIDATION.md
@.planning/phases/35-cutover-decision-artifact/35-VERIFICATION.md
@.planning/phases/35-cutover-decision-artifact/35-01-PLAN.md
@.planning/phases/35-cutover-decision-artifact/35-01-SUMMARY.md
@tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
@tools/bazel/phase35_cutover_decision_artifact.py
@tools/bazel/phase35_cutover_decision_artifact_test.py

<interfaces>
Existing failure path that must change:

```python
def run_quick(root: Path, phase34_arg: str, output_arg: str) -> None:
    phase34, output = validate_paths(root, phase34_arg, output_arg)
    contract = load_contract(root)
    source, phase34_contract = load_bundle(root, phase34, contract)
    manifest = load_json(root, phase34 / "final-readiness-run-manifest.json")
    write_bundle(root, output, contract, phase34_contract, manifest, source)
    run_security_scan(root, output.as_posix())
```

`main()` currently catches `VerificationError` and returns `1`, but `load_contract()` and `load_bundle()` run before `write_bundle()` calls `reset_output()`. A failure therefore leaves old canonical output untouched.

The normal Phase 35 bundle contains exactly:

```text
cutover-decision-run-manifest.json
cutover-audit-link-index.json
cutover-decision.json
next-milestone-route.json
redacted-cutover-decision-report.md
contract-snapshots/phase35_cutover_decision_artifact_contract.json
contract-snapshots/phase34_final_readiness_demotion_dry_run_contract.json
contract-snapshots/phase34-final-readiness-run-manifest.json
```

The existing decision and route field orders are defined by `DECISION_FIELDS` and `ROUTE_FIELDS`. The source-failure decision must keep those exact fields so existing consumers can safely read it. Because no trusted audit index or source-backed repair scope exists after source validation fails, the failure decision uses an empty `audit_link_index_ref`, zero counts for all nine audit kinds, no blocker/exception IDs, and `route-scope-incomplete`; the route uses an empty `follow_up_scope` rather than inventing owners, actions, or refs.

Source-failure demotion projection:

```text
cutover_verdict: blocked
demotion_decision_validation_state: invalid
demotion_decision_state: missing
demotion_decision_source_refs: []
demotion_gate_state: blocked
demotion_gate_reason_codes: [the safe source failure reason]
```

This projection means the source lineage could not be validated. It does not claim the Phase 33 demotion input was absent, approved, or rejected, and it never opens the Phase 34 gate.
</interfaces>
</context>

<gap-analysis>
Root-cause category: execution miss. Plan 35-01 Task 2 action 9 already required malformed or unsafe inputs to emit a minimal blocked manifest/decision/route before returning nonzero, but implementation and tests covered only valid blocked inputs and isolated validation exceptions. The repair must implement that existing decision rather than revise the Phase 35 verdict policy.
</gap-analysis>

<decision-coverage>
Every locked Phase 35 decision remains fully implemented. This gap plan changes only the source-failure publication boundary and its regression coverage.

| Decision | Coverage | Plan/task |
| --- | --- | --- |
| D-01 | Full | Task 2 keeps the reducer closed and uses only `blocked` for source failures. |
| D-02 | Full | Tasks 1-2 add durable artifact behavior for missing, malformed, stale, lifecycle, redaction, secret, and unsafe failures. |
| D-03 | Full | Preserved from 35-01; valid exception behavior remains in the full-suite regression gate. |
| D-04 | Full | Preserved from 35-01; valid approved behavior remains in the full-suite regression gate. |
| D-05 | Full | No caller verdict, confirmation, demotion approval, or evidence payload input is added. |
| D-06 | Full | Normal successful output still derives JSON and Markdown from one canonical index. |
| D-07 | Full | Normal expected-link derivation remains source-derived. |
| D-08 | Full | Normal audit-link fields and digest policy remain unchanged. |
| D-09 | Full | Normal exact-set anti-joins remain unchanged and fully tested. |
| D-10 | Full | Task 2 writes only safe reason codes and never copies untrusted source/error text. |
| D-11 | Full | Source failures map only to blocked plus targeted repair. |
| D-12 | Full | The failure route declares `route-scope-incomplete` and leaves scope empty instead of fabricating traceability. |
| D-13 | Full | The failure route requires a fresh cutover decision. |
| D-14 | Full | The failure route is planning-only and authorizes no production action. |
| D-15 | Full | Tasks 1-2 keep Phase 33 validation/value/source separate from the Phase 34 gate. |
| D-16 | Full | Tasks 1-2 assert independent cutover, demotion decision, and demotion gate fields. |
| D-17 | Full | Trusted normal projections retain detailed states; untrusted failure input is explicitly `invalid` without inferred value. |
| D-18 | Full | Reference demotion remains POST-01 and is never authorized by fallback output. |
| D-19 | Full | Existing Bazel/root/workflow/`just` wiring runs the updated verifier and tests without new labels. |
| D-20 | Full | Successful runs keep the exact eight-artifact bundle under `build/ci-evidence/phase35`. |
| D-21 | Full | Tasks 1-2 implement and prove action 9's durable blocked source-failure bundle. |
| D-22 | Full | Task 1 adds end-to-end source-failure/stale-approval coverage; Task 2 runs all existing verdict, route, link, demotion, and security regressions. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the source-failure bundle contract and add RED stale-approval regressions</name>
  <read_first>
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards-overrides.md`
    - `standards/core/architecture.md`
    - `standards/core/code-shape.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `.planning/phases/35-cutover-decision-artifact/35-CONTEXT.md`
    - `.planning/phases/35-cutover-decision-artifact/35-VERIFICATION.md`
    - `.planning/phases/35-cutover-decision-artifact/35-01-PLAN.md`
    - `.planning/phases/35-cutover-decision-artifact/35-01-SUMMARY.md`
    - `tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json`
    - `tools/bazel/phase35_cutover_decision_artifact.py`
    - `tools/bazel/phase35_cutover_decision_artifact_test.py`
  </read_first>
  <files>
    tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
    tools/bazel/phase35_cutover_decision_artifact_test.py
  </files>
  <behavior>
    - A source failure bundle contains exactly `cutover-decision-run-manifest.json`, `cutover-decision.json`, and `next-milestone-route.json`.
    - Its decision is `blocked`, its route is `targeted-blocker-repair`, it requires a fresh cutover decision, and it authorizes no production action.
    - It contains only contract-defined safe reason codes, never exception text or untrusted source content.
    - It projects demotion validation as `invalid`, explicit demotion value as `missing`, source refs as empty, and the gate as `blocked`; these fields never derive from the cutover verdict.
    - Every source-failure case replaces a seeded approved output and returns nonzero.
    - A successful normal run retains the exact eight-artifact full bundle.
  </behavior>
  <action>
Extend `phase35_cutover_decision_artifact_contract.json` with an exact `source_failure_behavior` object while preserving D-01 through D-22 and all existing normal-output schemas.

1. Contract the exact three source-failure artifacts, the existing `DECISION_FIELDS`/`ROUTE_FIELDS`, safe source-failure reason codes, and these immutable semantics:
   - `cutover_verdict: blocked`;
   - `route: targeted-blocker-repair`;
   - `requires_fresh_cutover_decision: true`;
   - `planning_only: true`;
   - `production_actions_authorized: false`;
   - `raw_evidence_consumed: false`;
   - empty active exception IDs, blocker IDs, audit-link ref, and repair scope;
   - zero audit-link counts for all nine kinds;
   - `route-scope-incomplete` whenever trusted source-backed repair scope cannot be derived;
   - independent invalid/missing/empty/blocked demotion projection as specified in `<interfaces>`.
2. Add a focused `Phase35SourceFailureReplacementTest` class with isolated temp-root helpers that:
   - copy only the two safe contract files already used by `make_temp_root`;
   - seed the canonical Phase 35 output with an unmistakable approved decision, `production-cutover-planning` route, audit index, report, and snapshots;
   - build an exact Phase 34 run-manifest shape without depending on ignored `build/ci-evidence` outputs;
   - invoke `phase35.main(["--quick"])` with `phase35.ROOT` patched to the temp root and capture the integer exit result.
3. Add focused Arrange/Act/Assert end-to-end cases for:
   - missing Phase 34 manifest;
   - malformed JSON;
   - unreadable UTF-8 bytes;
   - stale `generated_at_utc`;
   - mismatched Phase 34 lifecycle;
   - secret-tainted manifest content;
   - a source-manifest symlink escape;
   - a safely detected later reached-source failure after the manifest loads.
4. For every case, assert exit status `1`, exact three-file canonical failure bundle, blocked/repair/fresh-decision/no-production fields, safe reason codes, independent demotion fields, absence of the prior approval marker, and absence of the old audit/report/snapshot files.
5. Add a separate output-symlink test proving an unsafe destination is rejected without deleting or writing through the symlink. This is a destination-safety guard; it must not preserve a stale approval at a valid canonical directory.
6. Keep all existing valid-input verdict, audit-link, route, security, and demotion tests unchanged. The new tests must fail against the current implementation because it leaves the seeded approval intact or lets unreadable UTF-8 escape `VerificationError`; do not weaken assertions to accept deletion without a durable failure bundle.
  </action>
  <acceptance_criteria>
    - The contract parses and defines an exact, secret-safe source-failure bundle without changing the normal eight-artifact bundle or verdict/route truth tables.
    - New tests exercise the real `main --quick` result path, start from prior approved canonical output, and verify both the nonzero result and the durable blocked replacement.
    - Missing, malformed, unreadable, stale, lifecycle-mismatched, secret-tainted, symlink-unsafe, and reached-source failures each have one focused regression.
    - Tests explicitly assert that demotion approval/gate state is not inferred and that no raw error message or untrusted source value is persisted.
    - RED verification fails on the intended stale-output/failure-bundle assertions, not on syntax, import, or fixture construction.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -m json.tool tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json &gt;/dev/null; python3 -m py_compile tools/bazel/phase35_cutover_decision_artifact_test.py; red_log=$(mktemp); trap "rm -f $red_log" EXIT; set +e; python3 tools/bazel/phase35_cutover_decision_artifact_test.py -v &gt;"$red_log" 2&gt;&amp;1; red_status=$?; set -e; test "$red_status" -ne 0; grep -F "prior_approved" "$red_log"; grep -F "FAILED" "$red_log"; ! grep -E "SyntaxError|ImportError|NameError" "$red_log"'</automated>
  </verify>
  <done>The failure-bundle contract and end-to-end regression matrix exist and fail only because durable source-failure replacement is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomically install a minimal blocked bundle before returning source failures</name>
  <read_first>
    - `tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json`
    - `tools/bazel/phase35_cutover_decision_artifact.py`
    - `tools/bazel/phase35_cutover_decision_artifact_test.py`
    - `.planning/phases/35-cutover-decision-artifact/35-VERIFICATION.md`
    - `.planning/phases/35-cutover-decision-artifact/35-01-SUMMARY.md`
    - `tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
  </read_first>
  <files>
    tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json
    tools/bazel/phase35_cutover_decision_artifact.py
    tools/bazel/phase35_cutover_decision_artifact_test.py
  </files>
  <behavior>
    - All Task 1 RED tests become green without weakening their stale-approval, durability, demotion-separation, or no-authority assertions.
    - Full and source-failure bundles are constructed and validated in a sibling staging directory before replacing canonical output.
    - No source-validation error returns through `main` while an older approved canonical decision remains.
    - Invalid output destinations are rejected without following symlinks or touching outside paths.
  </behavior>
  <action>
Implement D-21/action 9 as a fail-closed output transaction rather than a catch-and-log patch.

1. Extend `VerificationError` with a contract-defined `reason_code` while preserving existing message behavior. Annotate the source boundary so:
   - missing files use `source-artifact-missing`;
   - JSON shape/parse failure, invalid UTF-8, and read `OSError` use `source-artifact-malformed`;
   - timestamps older than the existing `STALE_BEFORE` boundary use `source-artifact-stale`;
   - pinned lifecycle drift uses `source-artifact-lifecycle-mismatched`;
   - secret scans use `secret-tainted`;
   - unsafe refs, traversal, and source symlink escape use `unsafe-ref` or `source-ref-failed`.
   Unknown validation failures must conservatively map to `source-artifact-malformed`. Persist the code only, never the exception message or source value.
2. Make `load_json` convert `json.JSONDecodeError`, `UnicodeError`, and file-read `OSError` into coded `VerificationError` instances. Add a Phase 34 manifest timestamp check using the already-defined `STALE_BEFORE`; do not add a new clock policy or external dependency.
3. Split path validation so the canonical output destination is validated first and independently of Phase 34 input. A source-root or source-file symlink must reach the guarded source-failure path after the output is proven safe. An output-root symlink remains an immediate non-writing failure and must never be followed.
4. Add `write_source_failure_bundle` and validation helpers using constants safe even when the Phase 35 contract itself cannot load. Write exactly the three contracted files with:
   - a manifest identifying the canonical output, exact failure artifact set, source manifest ref, lifecycle, safe reason codes, `generation_state: blocked-source-error`, and `raw_evidence_consumed: false`;
   - a decision with the exact `DECISION_FIELDS`, blocked verdict, safe reasons plus `route-scope-incomplete`, blocked readiness, no trusted audit/blocker/exception data, and the independent invalid/missing/empty/blocked demotion projection;
   - a route with the exact `ROUTE_FIELDS`, targeted repair, empty source-backed scope, fresh-decision required, planning-only true, and production authorization false.
   Validate exact files, exact field order, enum values, security scans, and cross-file decision/route agreement before installation.
5. Replace direct `reset_output` mutation with sibling staging plus canonical installation:
   - create the stage beneath the validated `build/ci-evidence` parent so it cannot cross filesystems or escape the repository;
   - build and validate the normal eight-file bundle in staging, keeping all embedded refs and manifest `output_root` canonical;
   - on any coded source/generation validation failure, discard the full stage, build and validate the three-file failure stage, and install it before re-raising;
   - rename any old canonical output out of the canonical path, rename the validated stage into place, and remove the old directory;
   - if installation fails after the old directory moves, never restore an old approved directory to the canonical path; clean or quarantine it so it cannot remain authoritative.
6. Guard `run_quick` across contract loading, Phase 34 manifest loading, reached Phase 31-33 source loading, security validation, normal generation, and post-write validation. After the failure bundle is installed, re-raise `VerificationError` so `main` prints a sanitized error and returns `1`.
7. Preserve successful behavior exactly: valid input produces all eight normal artifacts; the three verdicts and exclusive route mapping remain unchanged; `approved-with-exceptions` still requires repair; the default valid quick fixture remains blocked/repair; audit-link exactness and Markdown consistency remain enforced.
8. Do not modify `.planning/config.json`, `35-VERIFICATION.md`, Bazel wiring, `rust_workflow.sh`, `justfile`, Phase 34 artifacts, or any production/demotion authority surface.
  </action>
  <acceptance_criteria>
    - Every Task 1 source-failure case returns `1` only after exact three-file blocked output replaces the prior approved bundle.
    - No prior approved decision, production route, audit link, report, snapshot, or unique marker remains at the canonical Phase 35 output after failure.
    - Failure artifacts contain only safe contract reason codes and no raw exception text, secret-tainted content, untrusted refs, or fabricated repair scope.
    - Demotion validation/value/source refs and gate state/reasons remain separate, invalid/missing/empty/blocked, and non-authorizing.
    - Output symlink tests prove no outside file is read, deleted, or written.
    - All pre-existing 48 Phase 35 tests and the new regression matrix pass; normal quick generation still yields the exact full eight-file blocked/repair bundle.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/phase35_cutover_decision_artifact_test.py &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact_test.py Phase35SourceFailureReplacementTest -q &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact.py --contract-only &amp;&amp; python3 tools/bazel/phase35_cutover_decision_artifact.py --security-only</automated>
  </verify>
  <done>Phase 35 cannot return a source-validation failure while stale approved output remains authoritative, and successful generation, auditability, routing, and demotion separation remain intact.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 34/reached source → quick generator | Missing, malformed, unreadable, stale, lifecycle-drifted, secret-tainted, and symlinked inputs can fail before normal output generation. |
| Staging directory → canonical Phase 35 output | Partial or failed replacement must not preserve a stale approved canonical decision. |
| Validation error → failure artifact | Raw exception/source text may contain unsafe paths or secret material and must not be persisted. |
| Source failure → demotion projection | Untrusted or unreadable decision lineage must not become approval, rejection, or an open gate. |
| Output path → filesystem | A malicious output symlink must not redirect deletion or writes outside the canonical root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-35-07 | Tampering / Elevation of Privilege | Stale canonical output | mitigate | Stage and validate the failure bundle, replace canonical output before returning nonzero, and prove prior approved files/markers are absent. |
| T-35-08 | Tampering / Denial of Service | Directory replacement | mitigate | Validate root containment and symlinks, use sibling staging, keep the canonical path fail-closed during replacement, and never restore stale approval after a failed install. |
| T-35-09 | Information Disclosure | Error-to-artifact projection | mitigate | Persist only closed reason codes; never copy exception messages, source values, raw JSON, secrets, or unsafe refs. |
| T-35-10 | Elevation of Privilege | Demotion fields in failure decision | mitigate | Emit independent invalid/missing/empty/blocked states and assert no fallback path can synthesize demotion approval or open the gate. |
| T-35-11 | Spoofing / Tampering | Failure bundle schema | mitigate | Contract exact artifacts/fields/enums, cross-check decision and route, and security-scan the staged bundle before installation. |
</threat-model>

<verification>
1. Task 1 proves the new end-to-end suite is RED for the current stale-output behavior.
2. Task 2 uses the under-30-second feedback gate in its `<automated>` check: Python compilation, the focused `Phase35SourceFailureReplacementTest` class, contract/lifecycle validation, and security scanning.
3. Inspect the generated valid quick bundle to confirm it remains the exact eight-artifact blocked/repair default.
4. Inspect isolated failure fixtures to confirm exact three-file blocked output, no old approval artifacts, safe reason codes, empty untrusted scope, and independent demotion fields.
5. At the plan-level final verification/commit gate, run the complete Phase 35 suite, lifecycle/contract/wiring/security modes, existing Bazel labels and `just` facade, and the Phase 34 regression:

```bash
python3 tools/bazel/phase35_cutover_decision_artifact_test.py -q
python3 tools/bazel/phase35_cutover_decision_artifact.py --contract-only
python3 tools/bazel/phase35_cutover_decision_artifact.py --wiring-only
python3 tools/bazel/phase35_cutover_decision_artifact.py --security-only
bazel run //tools/bazel:phase35_verify_tests
bazel run //tools/bazel:phase35_verify
just phase35-verify
python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q
```

6. Run scoped pre-commit and diff/lifecycle review without touching `.planning/config.json` or `35-VERIFICATION.md`:

```bash
pre-commit run --files tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/phase35_cutover_decision_artifact_test.py
git diff --check
git diff --stat -- tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/phase35_cutover_decision_artifact_test.py
git diff -- tools/bazel/manifests/phase35_cutover_decision_artifact_contract.json tools/bazel/phase35_cutover_decision_artifact.py tools/bazel/phase35_cutover_decision_artifact_test.py
```

Confirm the contract and generated failure artifacts retain lifecycle `35-2026-07-25T21-06-10`, the source lifecycle checks still reject drift, and neither protected planning file appears in the scoped diff.

7. Before the execution commit, run the Rust gates in repository-mandated order:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success-criteria>
- CUTOVER-01: every valid input produces one closed verdict, and every source failure durably replaces stale approval with `blocked` before returning nonzero.
- CUTOVER-02: successful output keeps the exact nine-kind canonical audit index; source-failure output never fabricates audit links or repair scope from untrusted inputs.
- CUTOVER-03: source failure routes only to targeted repair, requires a fresh cutover decision, and authorizes no production action.
- D-21/action 9 is implemented exactly: minimal blocked manifest/decision/route is durable at the canonical path for every source-validation failure.
- Cutover verdict, demotion decision validation/value/source refs, and demotion gate state/reasons remain explicitly separate on normal and failure paths.
- Existing valid-input Phase 35 behavior, Bazel/`just` wiring, Phase 34 regression behavior, and all repository commit gates remain green.
</success-criteria>

<output>
After completion, create `.planning/phases/35-cutover-decision-artifact/35-02-SUMMARY.md`.
</output>
