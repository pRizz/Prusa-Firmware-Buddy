---
phase: 40-file-length-refactoring
plan: 03
type: execute
wave: 3
depends_on: [40-02]
files_modified:
  - .bright-builds-rules-checks.tsv
  - utils/build.py
  - utils/build_config.py
  - utils/build_presets.py
  - utils/build_products.py
  - utils/phase_stepping/phase_stepping.py
  - utils/phase_stepping/signal_analysis.py
  - utils/phase_stepping/serial_machine.py
  - utils/phase_stepping/plotting.py
  - utils/phase_stepping/test_phase_stepping.py
autonomous: true
requirements: [D-05, D-08, D-09, D-11, D-12, D-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-27T17:17:18.946Z
must_haves:
  truths:
    - "`utils/build.py` keeps its CLI, preset semantics, product paths, and artifact publication contract."
    - "Phase stepping keeps its Click commands and `PhaseCorrection` import while pure analysis, serial effects, and plotting are separated."
    - "Both original files and every new module are below 629 lines, with their temporary exceptions removed."
  artifacts:
    - path: utils/build.py
      provides: "Stable developer/CI build command façade"
    - path: utils/phase_stepping/phase_stepping.py
      provides: "Stable Click and import façade"
  key_links:
    - from: utils/build.py
      to: utils/build_products.py
      via: "artifact publication orchestration"
      pattern: "store_products"
---

<objective>
Split the two developer utilities into deep, testable modules while preserving invocation, imports, and artifacts.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/code-shape.md
@.planning/phases/40-file-length-refactoring/40-02-SUMMARY.md
@utils/build.py
@utils/phase_stepping/phase_stepping.py
</context>

<decision-coverage>
| Decisions | Task | Coverage |
| --- | --- | --- |
| D-05, D-08 | 1-2 | Stable façades with pure/effectful concept modules and deletion-test seams. |
| D-09, D-11, D-12, D-15 | 1-2 | Third campaign, atomic row removal, contract evidence, and executed underlying commands. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor the build command façade</name>
  <read-first>
    <file>utils/build.py</file>
    <file>utils/presets/presets.json</file>
    <file>justfile</file>
  </read-first>
  <files>utils/build.py, utils/build_config.py, utils/build_presets.py, utils/build_products.py, .bright-builds-rules-checks.tsv</files>
  <behavior>
    - "CLI arguments, defaults, enum spellings, preset generation, exit codes, and `build/products` publication stay compatible."
  </behavior>
  <action>Keep argument parsing and top-level orchestration in `build.py`; move configuration types, preset/CMake generation, and product publication into the named modules. Capture pre/post `--help`, generated preset, and representative product metadata comparisons. If `just build` only prints a command, execute the printed underlying Bazel/CMake command and retain its status/output proof per D-15. Remove the ledger row only with green comparisons and `just generated-check`.</action>
  <verify><automated>python3 utils/build.py --help >/dev/null &amp;&amp; just generated-check</automated></verify>
  <acceptance-criteria>
    - "`utils/build.py` remains the entrypoint and is below 629 lines."
    - "CLI/help, preset output, artifact paths, and exit behavior compare equal."
    - "The underlying representative build command actually runs."
  </acceptance-criteria>
  <done>The build utility is a stable façade over cohesive configuration, preset, and publication modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate phase-stepping analysis and adapters</name>
  <read-first>
    <file>utils/phase_stepping/phase_stepping.py</file>
    <file>utils/phase_stepping/test_phase_stepping.py</file>
    <file>utils/phase_stepping/__init__.py</file>
  </read-first>
  <files>utils/phase_stepping/phase_stepping.py, utils/phase_stepping/signal_analysis.py, utils/phase_stepping/serial_machine.py, utils/phase_stepping/plotting.py, utils/phase_stepping/test_phase_stepping.py, .bright-builds-rules-checks.tsv</files>
  <behavior>
    - "Pure signal-analysis results remain numerically compatible."
    - "Click commands and `PhaseCorrection` import path remain stable."
    - "Serial and Plotly side effects stay in their adapters."
  </behavior>
  <action>Per D-05, D-08, D-11, and D-12, move numerical transforms/detection into `signal_analysis.py`, machine/serial code into `serial_machine.py`, and graph creation/output into `plotting.py`; retain Click commands and like-for-like public imports in `phase_stepping.py`. Expand focused one-concern tests around the pure seams and existing CLI/import behavior. Remove the temporary row atomically, run the utility suite and `just phase40-verify`, and record residual hardware/serial risk.</action>
  <verify><automated>python3 -m unittest utils.phase_stepping.test_phase_stepping</automated></verify>
  <acceptance-criteria>
    - "Original and new files are below 629 lines."
    - "All existing tests and new pure-analysis/CLI/import cases pass."
    - "Both utility temporary rows are absent without stale exceptions."
  </acceptance-criteria>
  <done>The utilities campaign preserves external behavior and removes both oversized owned files.</done>
</task>

</tasks>

<threat-model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-03 | Tampering | Build artifacts and CLI | mitigate | Pre/post help, preset, path, artifact, exit-code comparison and executed underlying build. |
| T-40-04 | Information disclosure | Serial/plot adapters | mitigate | Preserve adapters and avoid logging raw device payloads in campaign evidence. |
</threat-model>

<verification>
Run utility tests, `python3 utils/build.py --help`, `just generated-check`, executed underlying representative build, `just phase40-verify`, and `git diff --check`.
</verification>

<success-criteria>
Both utility façades remain compatible, all new modules are cohesive and sub-threshold, and both temporary exceptions are removed.
</success-criteria>

<output>
After completion, create `.planning/phases/40-file-length-refactoring/40-03-SUMMARY.md`.
</output>
