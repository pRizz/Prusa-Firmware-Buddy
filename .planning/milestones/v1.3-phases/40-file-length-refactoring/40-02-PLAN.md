---
phase: 40-file-length-refactoring
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - .bright-builds-rules-checks.tsv
  - rust/crates/domain/src/network.rs
  - rust/crates/domain/src/network/identity.rs
  - rust/crates/domain/src/network/transfer.rs
  - rust/crates/domain/src/network/service.rs
  - rust/crates/domain/src/network/tests.rs
  - rust/crates/domain/src/auxiliary.rs
  - rust/crates/domain/src/auxiliary/identity.rs
  - rust/crates/domain/src/auxiliary/transport.rs
  - rust/crates/domain/src/auxiliary/controller.rs
  - rust/crates/domain/src/auxiliary/tests.rs
  - rust/crates/domain/src/feature.rs
  - rust/crates/domain/src/feature/tests.rs
  - rust/crates/domain/src/gui.rs
  - rust/crates/domain/src/gui/tests.rs
autonomous: true
requirements: [D-05, D-06, D-08, D-09, D-11, D-12, D-13]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-27T17:17:18.946Z
must_haves:
  truths:
    - "All existing buddy-domain public exports and invariant behavior remain unchanged."
    - "The four owned Rust files are below 629 lines and use stable `foo.rs` façades over concept-oriented private children."
    - "The four temporary exceptions disappear atomically after the exact ordered Cargo and repository gates pass."
  artifacts:
    - path: rust/crates/domain/src/network.rs
      provides: "Stable network façade"
    - path: rust/crates/domain/src/auxiliary.rs
      provides: "Stable auxiliary façade"
    - path: rust/crates/domain/src/feature/tests.rs
      provides: "Extracted feature characterization tests"
    - path: rust/crates/domain/src/gui/tests.rs
      provides: "Extracted GUI characterization tests"
  key_links:
    - from: rust/crates/domain/src/lib.rs
      to: rust/crates/domain/src/network.rs
      via: "unchanged public re-exports"
      pattern: "pub use network"
---

<objective>
Refactor the four Rust domain findings behind stable public façades without changing any exported contract.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/40-file-length-refactoring/40-01-SUMMARY.md
@rust/crates/domain/src/lib.rs
</context>

<decision-coverage>
| Decisions | Task | Coverage |
| --- | --- | --- |
| D-05, D-06, D-08 | 1-2 | Stable `foo.rs` façades, private concept modules, deletion-test seams, and sub-629 files. |
| D-09, D-11, D-12, D-13 | 1-2 | Second campaign, atomic ledger removal, campaign evidence, and exact Rust gate order. |
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deepen network and auxiliary domain modules</name>
  <read-first>
    <file>rust/crates/domain/src/lib.rs</file>
    <file>rust/crates/domain/src/network.rs</file>
    <file>rust/crates/domain/src/auxiliary.rs</file>
    <file>standards/languages/rust.md</file>
  </read-first>
  <files>rust/crates/domain/src/network.rs, rust/crates/domain/src/network/identity.rs, rust/crates/domain/src/network/transfer.rs, rust/crates/domain/src/network/service.rs, rust/crates/domain/src/network/tests.rs, rust/crates/domain/src/auxiliary.rs, rust/crates/domain/src/auxiliary/identity.rs, rust/crates/domain/src/auxiliary/transport.rs, rust/crates/domain/src/auxiliary/controller.rs, rust/crates/domain/src/auxiliary/tests.rs, .bright-builds-rules-checks.tsv</files>
  <behavior>
    - "Every existing public type, constructor, parser, error, and re-export keeps its name and semantics."
    - "Identity validation, transfer/service policy, auxiliary transport, and controller policy live in cohesive private modules."
  </behavior>
  <action>Per D-05/D-06, keep `network.rs` and `auxiliary.rs` as the only public module façades and move coherent implementations/tests into the named children. Re-export like-for-like within the façades; do not change `lib.rs` exports or visibility. Apply the deletion test to each child and reject shallow numbered chunks. Remove both temporary ledger rows only after focused tests, the exact ordered Cargo sequence, relevant `just rust-*` and Phase 9/10 gates pass.</action>
  <verify><automated>cargo test -p buddy-domain network &amp;&amp; cargo test -p buddy-domain auxiliary</automated></verify>
  <acceptance-criteria>
    - "Both original files are below 629 physical lines; every new file is below 629."
    - "Public API diff is empty and existing tests remain behavior-equivalent."
    - "Both temporary rows are absent and `just phase40-verify` reports no stale exception."
  </acceptance-criteria>
  <done>Network and auxiliary domain modules are deep, private, and contract-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract cohesive feature and GUI tests</name>
  <read-first>
    <file>rust/crates/domain/src/feature.rs</file>
    <file>rust/crates/domain/src/gui.rs</file>
    <file>rust/crates/domain/src/lib.rs</file>
  </read-first>
  <files>rust/crates/domain/src/feature.rs, rust/crates/domain/src/feature/tests.rs, rust/crates/domain/src/gui.rs, rust/crates/domain/src/gui/tests.rs, .bright-builds-rules-checks.tsv</files>
  <behavior>
    - "Production implementations remain cohesive and only colocated test domains move."
    - "Existing feature-gate and GUI invariant tests keep their cases and assertions."
  </behavior>
  <action>Per D-08, move only the `#[cfg(test)]` modules into the named private test children; do not manufacture production pass-through modules. Preserve one-concern Arrange/Act/Assert cases and all public exports. Remove each ledger row in the same change, run focused tests, then before commit run exactly: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; followed by `just rust-format`, `just rust-lint`, `just rust-build`, `just rust-test`, `just phase8-verify`, and `just phase40-verify`.</action>
  <verify><automated>cargo test -p buddy-domain feature &amp;&amp; cargo test -p buddy-domain gui</automated></verify>
  <acceptance-criteria>
    - "Feature and GUI façades and new test files are each below 629 lines."
    - "The exact ordered Cargo sequence and all listed repository gates pass."
    - "All four Rust campaign temporary exceptions are removed."
  </acceptance-criteria>
  <done>The Rust campaign is complete with unchanged public behavior and zero temporary Rust rows.</done>
</task>

</tasks>

<threat-model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-02 | Tampering | Rust public API | mitigate | Like-for-like façade re-exports, public API comparison, full all-feature build/tests. |
| T-40-03 | Denial | Feature-gated builds | mitigate | All-target/all-feature Cargo sequence and repository Phase 8-10 gates. |
</threat-model>

<verification>
Run the exact D-13 Cargo sequence, Rust `just` gates, Phase 8-10 affected gates, `just phase40-verify`, and `git diff --check`.
</verification>

<success-criteria>
All four Rust owned files are below threshold, all public exports remain compatible, all new files are below threshold, and four temporary ledger rows are gone.
</success-criteria>

<output>
After completion, create `.planning/phases/40-file-length-refactoring/40-02-SUMMARY.md`.
</output>
