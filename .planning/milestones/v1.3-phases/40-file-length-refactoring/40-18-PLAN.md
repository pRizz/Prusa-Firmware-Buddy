---
phase: 40-file-length-refactoring
plan: 18
type: execute
wave: 18
depends_on: [40-17]
files_modified:
  - tools/bazel/phase35_cutover_decision_artifact_test.py
  - tools/bazel/phase35_cutover_decision_cases_test.py
  - tools/bazel/phase35_cutover_decision_failure_test.py
  - tools/bazel/phase35_cutover_decision_security_test.py
  - tools/bazel/phase35_guarded_publication_test.py
  - tools/bazel/phase35_source_failure_replacement_test.py
autonomous: true
gap_closure: true
requirements: [D-05, D-06, D-08, D-11, D-12, D-13, D-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-07-27T16-44-56
generated_at: 2026-07-28T03:30:00Z
must_haves:
  truths:
    - "Phase 35 cutover, security, publication, and source-replacement tests are ordinary Python classes or mixins."
    - "The stable public entrypoint preserves its exact 75 test-method inventory, fail-closed behavior, direct execution, and Bazel labels."
    - "All six touched files are below 629 lines and contain no encoded or reconstructed source."
  artifacts:
    - path: tools/bazel/phase35_cutover_decision_cases_test.py
      provides: "Ordinary Phase 35 cutover cases mixin"
      contains: "class "
    - path: tools/bazel/phase35_guarded_publication_test.py
      provides: "Ordinary guarded-publication test class"
      contains: "class Phase35GuardedPublicationTest"
    - path: tools/bazel/phase35_source_failure_replacement_test.py
      provides: "Ordinary source-failure replacement test class"
      contains: "class Phase35SourceFailureReplacementTest"
  key_links:
    - from: tools/bazel/phase35_cutover_decision_artifact_test.py
      to: tools/bazel/phase35_guarded_publication_test.py
      via: "ordinary class import for unittest discovery"
      pattern: "from phase35_guarded_publication_test import"
---

<objective>
Close the Phase 35 portion of the Phase 40 architecture gap by materializing cutover and guarded-publication tests as ordinary phase-local Python modules.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/40-file-length-refactoring/40-17-SUMMARY.md
@.planning/phases/40-file-length-refactoring/40-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the Phase 35 cutover and guarded-publication suites</name>
  <files>tools/bazel/phase35_cutover_decision_artifact_test.py, tools/bazel/phase35_cutover_decision_cases_test.py, tools/bazel/phase35_cutover_decision_failure_test.py, tools/bazel/phase35_cutover_decision_security_test.py, tools/bazel/phase35_guarded_publication_test.py, tools/bazel/phase35_source_failure_replacement_test.py</files>
  <action>Before editing, load the public entrypoint with `unittest`, recursively enumerate every `_testMethodName`, assert exactly 75 unique names, and save the sorted names to a temporary baseline outside the repository. Convert the three `TEST_METHODS` payloads into ordinary cases, failure, and security mixins with explicit phase-local dependencies. Materialize `Phase35GuardedPublicationTest` and `Phase35SourceFailureReplacementTest` directly in their existing modules. Replace both dynamic assembly loops with explicit imports, mixin inheritance, and ordinary imported classes for `unittest` discovery. Preserve every test body, assertion, fail-closed authority path, guarded replacement, cleanup behavior, direct-script behavior, and runfile contract. After editing, fail unless the sorted public method-name set exactly matches the baseline, remains 75 names, and contains no duplicates. Keep all concepts Phase-local.</action>
  <verify>
    <automated>python3 tools/bazel/phase35_cutover_decision_artifact_test.py &amp;&amp; bazel run //tools/bazel:phase35_verify_tests &amp;&amp; bazel run //tools/bazel:phase35_verify</automated>
    <expected>All 75 tests and both unchanged Phase 35 labels pass.</expected>
  </verify>
  <done>Phase 35 has exact inventory parity through ordinary modules and no reconstructed source.</done>
</task>

</tasks>

<verification>
1. Run the exact before/after method-name set comparison and duplicate assertion.
2. Run `python3 -m py_compile` on the six files.
3. Fail if a scoped scan finds `TEST_METHODS`, `TEST_CLASSES`, `textwrap`, `importlib.import_module`, or `exec(`.
4. Require every touched file to have fewer than 629 physical lines.
5. Run `just phase35-verify`, `just phase38-verify`, `just phase40-verify --terminal`, `bun scripts/bright-builds-check.ts all`, and `git diff --check`.
6. Review the diff for test-body, public-entrypoint, label, runfile, or unrelated changes.
7. Before committing, run in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
</verification>

<success-criteria>
The exact 75 Phase 35 test names execute through ordinary imports/classes, all six files are below 629 lines, forbidden reconstruction patterns are absent, and focused/downstream/terminal gates pass.
</success-criteria>

<output>
Create `.planning/phases/40-file-length-refactoring/40-18-SUMMARY.md`, then rerun Phase 40 verification and replace `40-VERIFICATION.md` with `status: passed` only if every criterion is proven.
</output>
