---
phase: 41-terminal-milestone-metadata-coherence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/phase41_terminal_consistency_policy.py
  - tools/bazel/phase41_terminal_consistency.py
  - tools/bazel/phase41_terminal_consistency_test.py
  - tools/bazel/BUILD.bazel
  - justfile
autonomous: true
requirements:
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
  - READY-02
  - READY-03
  - CUTOVER-01
  - CUTOVER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 41-2026-08-01T16-27-53
generated_at: 2026-08-01T17:02:00Z
must_haves:
  truths:
    - "A repo-owned read-only checker derives terminal expectations from exact evidence identities and fails closed on every missing, malformed, stale, duplicate, or contradictory projection."
    - "Pre-audit and pre-archive modes share one pure normalized comparison core and use deterministic diagnostics with exit codes 0, 1, and 2."
    - "The checker runs through Bazel and just without depending on a user-local GSD installation or modifying the managed Bright Builds checker."
  artifacts:
    - path: tools/bazel/phase41_terminal_consistency_policy.py
      provides: Pure normalized terminal-coherence types and comparison policy
      contains: "class TerminalSnapshot"
    - path: tools/bazel/phase41_terminal_consistency.py
      provides: Read-only pre-audit/pre-archive CLI
      contains: "def main("
    - path: tools/bazel/phase41_terminal_consistency_test.py
      provides: One-concern positive and fail-closed mutations
      contains: "class Phase41TerminalConsistencyTest"
    - path: justfile
      provides: Stable Phase 41 verification facade
      contains: "phase41-verify"
  key_links:
    - from: tools/bazel/phase41_terminal_consistency.py
      to: tools/bazel/phase41_terminal_consistency_policy.py
      via: parsed boundary data passed to pure evaluation
      pattern: "evaluate_terminal_consistency"
    - from: justfile
      to: tools/bazel/BUILD.bazel
      via: Phase 41 Bazel targets
      pattern: "phase41_terminal_consistency"
---

<objective>
Build the fail-closed terminal consistency checker before repairing any metadata.

Purpose: Turn the known M1/Nyquist contradictions into deterministic, fixture-tested failures and establish the gate that every later repair must satisfy.
Output: A pure Python policy core, thin read-only CLI, focused tests, Bazel targets, and `just phase41-verify` facade.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/41-terminal-milestone-metadata-coherence/41-CONTEXT.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-RESEARCH.md
@.planning/phases/41-terminal-milestone-metadata-coherence/41-VALIDATION.md
@.planning/v1.3-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement normalized evidence policy and exhaustive fail-closed fixtures</name>
  <read-first>
    tools/bazel/phase22_metadata_reconciliation.py
    tools/bazel/phase22_metadata_policy.py
    tools/bazel/phase22_metadata_reconciliation_test.py
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/37-reconcile-decisions-into-readiness/37-VALIDATION.md
    .planning/phases/38-fail-closed-cutover-workflow/38-VALIDATION.md
    .planning/phases/40-file-length-refactoring/40-VALIDATION.md
    .planning/v1.3-MILESTONE-AUDIT.md
  </read-first>
  <files>tools/bazel/phase41_terminal_consistency_policy.py, tools/bazel/phase41_terminal_consistency_test.py</files>
  <action>
Create frozen or otherwise immutable normalized records for requirement evidence, phase lifecycle, exact PLAN/SUMMARY inventory, Nyquist validation, milestone projections, and deterministic violations. The top-level `TerminalSnapshot` must contain exact sets/records rather than only counters. Implement `evaluate_terminal_consistency(snapshot, mode)` for exact modes `pre-audit` and `pre-archive`.

The policy must reject: duplicate/missing/extra requirement IDs; unchecked or non-Complete requirement projections when terminal completion is required; changed requirement semantics; wrong owner phase; roadmap/disk phase mismatch; wrong milestone phase/plan counts; exact filename inventory mismatch even when counts agree; PLAN without SUMMARY or SUMMARY without PLAN; STATE counters/narrative inconsistent with disk lifecycle; missing or malformed VALIDATION; false `nyquist_compliant`; false `wave_0_complete`; pending/red task or campaign rows; incomplete sign-off; stale/malformed audit; pre-archive audit without phases 31 through 41, exactly sixteen coherent requirements, and zero integration/flow/Nyquist gaps.

Use stable violation codes prefixed `P41_` and sort by `(path, code, observed)`. Diagnostics may contain paths, IDs, statuses, counts, and bounded messages only; never copy evidence payload contents. Build one minimal coherent fixture tree in test setup, then mutate exactly one invariant per test with explicit Arrange, Act, Assert sections. Include explicit tests for exit-category mapping, stable ordering, count-only inventory spoofing, circular audit authority, and Phase 37/38/40 partial Nyquist states.
  </action>
  <verify>
    <automated>python3 tools/bazel/phase41_terminal_consistency_test.py -q</automated>
  </verify>
  <acceptance-criteria>
    - `phase41_terminal_consistency_policy.py` defines `TerminalSnapshot`, normalized requirement/phase/inventory/validation/audit records, and `evaluate_terminal_consistency`.
    - A coherent fixture produces zero violations in both supported modes after the fresh-audit fields are present.
    - Every named mutation produces at least one stable `P41_` violation and no test combines unrelated concerns.
    - Inventory spoofing with the same count but the wrong basename fails.
    - Diagnostics contain no private keys, tokens, certificates, service payloads, crash dumps, or raw evidence contents.
  </acceptance-criteria>
  <done>The pure core represents exact evidence identities and all focused tests prove the locked fail-closed behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add the read-only CLI and Bazel/just verification facade</name>
  <read-first>
    tools/bazel/phase41_terminal_consistency_policy.py
    tools/bazel/phase41_terminal_consistency_test.py
    tools/bazel/phase22_metadata_reconciliation.py
    tools/bazel/BUILD.bazel
    justfile
    scripts/bright-builds-check.ts
  </read-first>
  <files>tools/bazel/phase41_terminal_consistency.py, tools/bazel/BUILD.bazel, justfile</files>
  <action>
Implement a thin CLI accepting `--root PATH` (default `.`) and required `--mode {pre-audit,pre-archive}`. Parse YAML-frontmatter Markdown and exact tables/lists at the boundary, construct `TerminalSnapshot`, call the pure policy once, print sorted path-qualified violations, and return `0` only for coherence, `1` for repository violations, and `2` only for argparse/invocation misuse. The CLI must be read-only and must not import or execute `$HOME/.codex` tooling.

Add Bazel targets named `phase41_terminal_consistency` and `phase41_terminal_consistency_tests` with declared Python sources and required `.planning` data for the live run. Add `phase41-verify *args:` to `justfile`; it must run the Bazel test target, then `bazel run //tools/bazel:phase41_terminal_consistency -- {{args}}`, then `bun scripts/bright-builds-check.ts all`. Do not edit `scripts/bright-builds-check.ts`.

Prove the current live tree exits `1` in pre-audit mode and prints violations covering the known stale Phase 36/37/39 plan inventories, ROADMAP/REQUIREMENTS/STATE contradictions, and partial Phase 37/38/40 Nyquist state. Treat this expected failure as baseline evidence, not plan failure; fixture/Bazel tests and invocation behavior must be green.
  </action>
  <verify>
    <automated>bazel test //tools/bazel:phase41_terminal_consistency_tests &amp;&amp; python3 tools/bazel/phase41_terminal_consistency.py --root . --mode pre-audit &gt;/tmp/phase41-baseline.out 2&gt;&amp;1; status=$?; test "$status" -eq 1 &amp;&amp; rg -q 'P41_.*(ROADMAP|REQUIREMENTS|STATE|37-VALIDATION|38-VALIDATION|40-VALIDATION)' /tmp/phase41-baseline.out &amp;&amp; test "$(python3 tools/bazel/phase41_terminal_consistency.py --bad-flag 2&gt;/dev/null; echo $?)" -eq 2 &amp;&amp; git diff --exit-code -- scripts/bright-builds-check.ts</automated>
  </verify>
  <acceptance-criteria>
    - The CLI implements exact exit codes 0/1/2 and never writes repository files.
    - Bazel owns one binary and one test target; tests pass without a GSD installation.
    - `just phase41-verify --mode pre-audit` composes Phase 41 tests/live check with the managed checker.
    - The current tree's expected baseline failure names all known M1 and Nyquist surfaces.
    - `scripts/bright-builds-check.ts` is byte-for-byte unchanged.
  </acceptance-criteria>
  <done>The repository has a deterministic Bazel/just terminal-consistency gate and a recorded failing baseline for the exact known drift.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| Markdown/frontmatter to normalized records | Malformed or duplicate input silently accepted | Parse once, reject ambiguity, unit-test every unsupported shape |
| Exact disk evidence to duplicated projections | Count agreement hides wrong ownership or filenames | Compare exact stable identities, not counters alone |
| Historical audit to terminal decision | Circular authority launders stale status | Audit is consumer-only; pre-archive requires independent evidence and freshness |
| Diagnostics to committed logs | Secret-bearing evidence is copied | Emit only paths, IDs, statuses, counts, and bounded policy messages |

## ASVS L1 STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-41-01 | Tampering | Markdown parser | mitigate | Duplicate/malformed rows and frontmatter fail closed. |
| T-41-02 | Spoofing | Requirement and plan identities | mitigate | Require exact unique IDs and phase-prefixed basenames. |
| T-41-03 | Repudiation | Violation output | mitigate | Stable sorted codes include path, observed, and expected values. |
| T-41-04 | Elevation of Privilege | Audit and archival gates | mitigate | Pre-archive cannot pass from audit claims alone. |
| T-41-05 | Information Disclosure | Diagnostics | mitigate | Never load or print raw evidence payloads. |
</threat-model>

<verification>
1. `python3 tools/bazel/phase41_terminal_consistency_test.py -q`
2. `bazel test //tools/bazel:phase41_terminal_consistency_tests`
3. Expected live pre-audit exit `1` with named M1/Nyquist violations.
4. `bun scripts/bright-builds-check.ts all`
5. `cargo fmt --all`
6. `cargo clippy --all-targets --all-features -- -D warnings`
7. `cargo build --all-targets --all-features`
8. `cargo test --all-features`
9. `git diff --check` and exact changed-path review.
</verification>

<success-criteria>
- Pure policy tests cover every locked invariant and fail-closed edge.
- The CLI is read-only, deterministic, CI-capable, and independent of user-local GSD tooling.
- Bazel and `just` expose the stable Phase 41 verification surface.
- Current drift is detected before any repair is attempted.
</success-criteria>

<output>
After completion, create `.planning/phases/41-terminal-milestone-metadata-coherence/41-01-SUMMARY.md`.
</output>
