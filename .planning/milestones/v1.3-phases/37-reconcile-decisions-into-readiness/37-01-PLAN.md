---
phase: 37-reconcile-decisions-into-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
  - tools/bazel/phase33_maintainer_decision_inputs.py
  - tools/bazel/phase33_maintainer_decision_inputs_test.py
  - tools/bazel/phase34_decision_reconciliation.py
  - tools/bazel/phase34_decision_reconciliation_test.py
autonomous: true
requirements:
  - DECIDE-01
  - DECIDE-02
  - READY-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 37-2026-07-26T06-52-46
generated_at: 2026-07-26T07:10:00Z
must_haves:
  truths:
    - "A Phase 33 decision names every target with an explicit row_ref, decision_axis, and decision_subject_id binding."
    - "Only a binding whose complete typed identity matches exactly one canonical Phase 32 row can resolve that row."
    - "Missing, duplicate, conflicting, stale, malformed, mismatched, rejected, invalid, and hard-blocker targets remain blocking with specific diagnostics."
    - "Axis-specific approval semantics never imply reference-demotion authorization."
  artifacts:
    - path: tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
      provides: "Machine-readable typed decision-target contract"
      contains: "decision_targets"
    - path: tools/bazel/phase33_maintainer_decision_inputs.py
      provides: "Phase 33 boundary normalization and validation for typed target bindings"
    - path: tools/bazel/phase34_decision_reconciliation.py
      provides: "Pure exact-match and axis-specific reconciliation core"
    - path: tools/bazel/phase34_decision_reconciliation_test.py
      provides: "Focused fail-closed decision reconciliation regressions"
  key_links:
    - from: tools/bazel/phase33_maintainer_decision_inputs.py
      to: tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
      via: "contract validation of decision_targets and source_row_refs projection"
      pattern: "decision_targets"
    - from: tools/bazel/phase34_decision_reconciliation.py
      to: "Phase 32 canonical row identity"
      via: "exact row_ref + decision_axis + decision_subject_id comparison"
      pattern: "decision_subject_id"
---

<objective>
Create the typed decision-binding boundary and pure reconciliation core that let exact Phase 33 decisions resolve canonical Phase 32 decision-domain rows without weakening fail-closed readiness.

Purpose: Close DECIDE-01/DECIDE-02 at the decision-input boundary and provide the deterministic core READY-01 needs, while preserving Phase 31 evidence authority and Phase 38's cutover boundary.
Output: An extended Phase 33 contract/normalizer with focused tests plus a pure Phase 34 reconciliation module with exhaustive axis and negative-case tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-CONTEXT.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-RESEARCH.md
@.planning/phases/37-reconcile-decisions-into-readiness/37-VALIDATION.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-CONTEXT.md
@.planning/phases/36-normalize-evidence-and-blocker-rows/36-VERIFICATION.md
@tools/bazel/manifests/phase32_blocker_register_triage_contract.json
@tools/bazel/phase32_blocker_normalization.py
@tools/bazel/phase32_blocker_register_triage.py
@tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
@tools/bazel/phase33_maintainer_decision_inputs.py
@tools/bazel/phase33_maintainer_decision_inputs_test.py

<interfaces>
The executor must preserve the established dual identity:

```text
canonical source identity: immutable row_id / row_ref
typed decision identity: decision_axis + decision_subject_id
exact resolution key: row_ref + decision_axis + decision_subject_id
```

Phase 31 remains evidence-completeness and accepted-final provenance authority. Phase 32 supplies sparse canonical decision-domain rows. Phase 33 owns explicit decision inputs. Phase 34 revalidates those inputs rather than trusting copied identity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize explicit typed decision targets at the Phase 33 boundary</name>
  <read_first>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-CONTEXT.md</file>
    <file>tools/bazel/manifests/phase32_blocker_register_triage_contract.json</file>
    <file>tools/bazel/phase32_blocker_normalization.py</file>
    <file>tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json</file>
    <file>tools/bazel/phase33_maintainer_decision_inputs.py</file>
    <file>tools/bazel/phase33_maintainer_decision_inputs_test.py</file>
  </read_first>
  <files>tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json, tools/bazel/phase33_maintainer_decision_inputs.py, tools/bazel/phase33_maintainer_decision_inputs_test.py</files>
  <behavior>
    - "A normalized target contains row_ref, decision_axis, and decision_subject_id per D-04."
    - "source_row_refs, if retained for compatibility, equals the deterministic projection of decision_targets[*].row_ref."
    - "Each target is revalidated against the canonical Phase 32 snapshot and must match exactly once per D-05."
    - "Empty, missing, duplicate-row-ref, duplicate-triple, colliding, mismatched, stale, malformed, and conflicting bindings are rejected or emitted as explicit blockers per D-06."
    - "Safe canonical references and non-secret typed identifiers survive the redacted Phase 34 handoff; unsafe paths and copied opaque-only keys do not."
  </behavior>
  <action>
Write the failing boundary tests first, then extend the Phase 33 JSON contract and normalizer to own explicit per-reference `decision_targets`. Validate all three identity fields against the canonical Phase 32 register and preserve stable clear-text bindings in the normalized handoff. Keep multi-target decisions only when every target is explicitly enumerated and independently valid. Preserve existing safe-ref, containment, snapshot, lifecycle, and redaction protections. Do not add fallback matching by gate, stream, path, prefix, or similar subject. Do not infer reference-demotion approval from any other axis (D-08), and do not change the Phase 32 row-id derivation or Phase 31 evidence authority (D-01, D-02).
  </action>
  <verify>
    <automated>python3 tools/bazel/phase33_maintainer_decision_inputs_test.py -q &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "The Phase 33 contract requires typed decision_targets with row_ref, decision_axis, and decision_subject_id."
    - "The normalizer accepts exact canonical targets and rejects every ambiguous or invalid target category with a stable diagnostic."
    - "No decision is resolved through a fallback identity and no decision axis authorizes reference demotion implicitly."
    - "Before committing this task, the executor runs the automated command exactly in its listed order, including the complete mandatory Rust sequence."
  </acceptance_criteria>
  <done>Phase 33 publishes deterministic, safe, exact typed target bindings and its focused boundary suite proves fail-closed behavior for all invalid target forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure exact-match decision reconciliation core</name>
  <read_first>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-CONTEXT.md</file>
    <file>.planning/phases/37-reconcile-decisions-into-readiness/37-RESEARCH.md</file>
    <file>tools/bazel/phase32_blocker_normalization.py</file>
    <file>tools/bazel/phase33_maintainer_decision_inputs.py</file>
  </read_first>
  <files>tools/bazel/phase34_decision_reconciliation.py, tools/bazel/phase34_decision_reconciliation_test.py</files>
  <behavior>
    - "An exact typed binding resolves one and only one matching canonical decision-domain row."
    - "retained_code, residual_risk, exception, readiness, and reference_demotion use explicit axis-specific value semantics per D-07."
    - "Reference-demotion decisions never clear readiness by themselves, and readiness/acceptance decisions never grant demotion authority per D-08."
    - "Missing, zero-match, multi-match, axis mismatch, subject mismatch, duplicate, conflict, stale lifecycle, invalid value, rejection, and hard-blocker cases remain blocked with distinct stable reason codes."
    - "Hard blockers cannot be approved away even when the supplied value would normally approve that axis."
  </behavior>
  <action>
Write focused failing tests first, one concern per test with explicit Arrange/Act/Assert sections. Create a small pure transformation module that parses already boundary-validated canonical rows and normalized decision targets into typed values, indexes exact triples, evaluates cardinality and conflicts, and returns deterministic coverage state, readiness effect, linked decision refs, and stable reason codes. Make the axis/value table explicit and exhaustive. Keep filesystem loading, JSON publication, evidence-row construction, and Phase 35 invocation out of this module. Do not absorb Phase 38 stale-authority replacement, cutover routing, or production demotion work (D-12).
  </action>
  <verify>
    <automated>python3 tools/bazel/phase34_decision_reconciliation_test.py -q &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "Exact triples link only to their intended canonical row and every non-exact or ambiguous case is blocked."
    - "The module returns stable, specific diagnostics for every negative category required by D-06, D-07, and D-11."
    - "The pure core contains no filesystem orchestration and no Phase 35/cutover behavior."
    - "Before committing this task, the executor runs the automated command exactly in its listed order, including the complete mandatory Rust sequence."
  </acceptance_criteria>
  <done>The pure reconciliation core fully implements exact typed resolution and axis-specific fail-closed semantics with exhaustive focused regression coverage.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 32 artifact to Phase 33 normalizer | Repository-generated but untrusted JSON, paths, lifecycle metadata, and canonical identities cross into the maintainer-decision boundary. |
| Maintainer decision input to normalized handoff | Human-authored decision values and target identities could be malformed, ambiguous, stale, or crafted to resolve a different blocker. |
| Phase 33 handoff to reconciliation core | Copied typed identities must be revalidated against the Phase 32 snapshot rather than treated as authority. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-37-01 | Tampering / Elevation of Privilege | high | Phase 33 target normalization | mitigate | Require exact row_ref, decision_axis, and decision_subject_id matching against one canonical Phase 32 row; reject fallback, zero-match, multi-match, duplicate, conflict, and stale inputs before publication. |
| T-37-02 | Tampering / Elevation of Privilege | high | Axis/value reconciliation | mitigate | Use an exhaustive axis/value policy, keep hard blockers non-coverable, and keep reference-demotion authorization orthogonal to readiness. |
| T-37-03 | Information Disclosure | medium | Normalized and redacted handoff | mitigate | Reuse safe-ref, containment, redaction, and security-scan boundaries; publish canonical references and non-secret identifiers only. |
| T-37-04 | Repudiation | medium | Decision linkage diagnostics | mitigate | Preserve stable decision refs, source lineage, lifecycle metadata, and specific reason codes in deterministic results. |

No high-severity threat may remain accepted, transferred without an enforced boundary, or unresolved when this plan is complete. The automated tests must prove each high-severity mitigation fails closed.
</threat-model>

<verification>
Run the Phase 33 boundary suite and the new pure reconciliation suite independently, then run `just phase34-verify` after the plan wave. Before each task commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; and `git diff --check`. Review the task diff to confirm that Phase 31 remains evidence authority and no Phase 35/cutover behavior was introduced.
</verification>

<success-criteria>
- DECIDE-01 and DECIDE-02 have exact, explicit, machine-readable target bindings at the Phase 33 boundary.
- READY-01 has a pure reconciliation primitive that can clear only valid exact decision-domain rows.
- Every D-01 through D-08 decision implemented by this plan is covered fully; D-09 through D-12 are assigned to Plan 37-02.
- All high-severity ASVS L1 threats are mitigated by executable tests and fail-closed behavior.
- The Phase 38 boundary remains intact.
</success-criteria>

<output>
After completion, create `.planning/phases/37-reconcile-decisions-into-readiness/37-01-SUMMARY.md`.
</output>
