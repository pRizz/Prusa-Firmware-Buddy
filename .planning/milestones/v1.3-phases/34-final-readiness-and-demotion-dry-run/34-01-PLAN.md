---
phase: 34-final-readiness-and-demotion-dry-run
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
  - tools/bazel/phase34_final_readiness_demotion_dry_run.py
  - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
  - tools/bazel/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/rust_workflow.sh
  - justfile
  - .planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md
autonomous: true
requirements:
  - READY-01
  - READY-02
  - READY-03
must_haves:
  truths:
    - "The final readiness packet links every Phase 31 accepted-final evidence row to either its applicable Phase 32 blocker classification or an explicit Phase 31-backed clean/no-blocker coverage state, plus applicable Phase 33 decisions and safe artifact refs."
    - "Readiness blocks on absent, failed, stale, malformed, redaction-failed, source-ref-failed, secret-tainted, lifecycle-mismatched, unknown, underclassified, or uncovered required rows."
    - "Missing, invalid, stale, rejected, or malformed demotion approval produces a durable blocked dry-run result even when evidence rows are green."
    - "The demotion dry run opens only when readiness is unblocked and a valid separate explicit approval decision is present."
    - "Quick/default verification never synthesizes real evidence finality or maintainer approval."
  artifacts:
    - path: "tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json"
      provides: "Phase 34 source, ledger, gate, artifact, reason-code, security, and verification contract"
      contains: "phase34_final_readiness_demotion_dry_run_contract"
    - path: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      provides: "Phase 34 coverage-ledger evaluator, verifier, artifact writer, security scan, and wiring check"
      exports: ["main"]
    - path: "tools/bazel/phase34_final_readiness_demotion_dry_run_test.py"
      provides: "READY-01/02/03 unit and integration coverage"
      contains: "unittest"
    - path: "build/ci-evidence/phase34/demotion-dry-run.json"
      provides: "Durable blocked/open result generated by verification"
      contains: "gate_state"
  key_links:
    - from: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      to: "build/ci-evidence/phase33/downstream-handoff-manifest.json"
      via: "--phase33-handoff loader"
      pattern: "DEFAULT_PHASE33_HANDOFF"
    - from: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      to: "build/ci-evidence/phase31/final-intake-manifest.json"
      via: "explicit --phase31-output-dir loader and accepted receipt refs"
      pattern: "accepted-final"
    - from: "tools/bazel/rust_workflow.sh"
      to: "tools/bazel/phase34_final_readiness_demotion_dry_run.py"
      via: "phase34_verify case arm"
      pattern: "phase34_verify"
    - from: "justfile"
      to: "//tools/bazel:phase34_verify"
      via: "phase34-verify recipe"
      pattern: "phase34-verify:"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 34-2026-07-25T18-18-48
generated_at: 2026-07-25T18:18:48.752Z
---

<objective>
Create the Phase 34 final-readiness coverage ledger, fail-closed packet, reference-demotion dry-run, tests, and developer workflow wiring.

Purpose: READY-01 through READY-03 require a row-complete audit surface over real consumed evidence and explicit decisions, plus proof that reference demotion cannot open through green evidence alone.
Output: A secret-safe Phase 34 bundle under `build/ci-evidence/phase34` whose canonical ledger feeds both the JSON packet and redacted report.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-CONTEXT.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-RESEARCH.md
@.planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md
@.planning/phases/33-maintainer-decision-inputs/33-01-SUMMARY.md
@tools/bazel/manifests/phase31_final_evidence_intake_contract.json
@tools/bazel/manifests/phase32_blocker_register_triage_contract.json
@tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
@tools/bazel/phase33_maintainer_decision_inputs.py
@tools/bazel/manifests/phase28_final_readiness_packet_contract.json
@tools/bazel/phase28_final_readiness_packet.py
@tools/bazel/BUILD.bazel
@BUILD.bazel
@tools/bazel/rust_workflow.sh
@justfile
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md

<interfaces>
Canonical ledger fields:
```text
row_id, source_stream, source_ref, requirement_ids, affected_gates,
proof_eligibility, evidence_status, row_problem_kind, blocker_kind, severity,
evidence_refs, artifact_refs, classification_ref, retained_code_decision_refs,
residual_risk_decision_refs, exception_decision_refs, readiness_decision_refs,
coverage_state, readiness_effect, reason_codes
```

Demotion dry-run fields:
```text
readiness_state: blocked | unblocked
approval_validation_state: missing | invalid | valid
approval_decision_state: missing | approve | reject
gate_state: blocked | open
reason_codes: string[]
source_refs: string[]
```

Generated artifacts:
```text
final-readiness-run-manifest.json
readiness-coverage-ledger.json
final-readiness-packet.json
readiness-blocker-summary.json
demotion-dry-run.json
redacted-readiness-report.md
contract-snapshots/phase34_final_readiness_demotion_dry_run_contract.json
contract-snapshots/phase33_maintainer_decision_inputs_contract.json
contract-snapshots/phase33-downstream-handoff-manifest.json
contract-snapshots/phase32-blocker-register.json
contract-snapshots/phase31-final-intake-manifest.json
contract-snapshots/phase31-accepted-receipts.json
```
</interfaces>

<decision_trace>
- D-01 through D-05: Tasks 1 and 2 define and implement the Phase 31 expected-row ledger, sparse Phase 32 blocker overlay, exact Phase 33 decision joins, blocking anti-joins, and ledger-derived reports.
- D-06 through D-10: Tasks 1 and 2 define and test orthogonal readiness/approval predicates, durable blocked outputs, non-inference, and dry-run-only behavior.
- D-11 through D-15: Tasks 1 through 3 create a new Phase 34 consumer, preserve Phase 28, write the Phase 34 bundle, separate the pure evaluator from the I/O shell, and keep quick/default output blocked while testing one isolated open fixture.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 34 contract and RED-first regression suite</name>
  <files>tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json, tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</files>
  <read_first>
    - .planning/phases/34-final-readiness-and-demotion-dry-run/34-CONTEXT.md
    - .planning/phases/34-final-readiness-and-demotion-dry-run/34-RESEARCH.md
    - .planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md
    - tools/bazel/manifests/phase33_maintainer_decision_inputs_contract.json
    - tools/bazel/phase33_maintainer_decision_inputs_test.py
    - tools/bazel/manifests/phase28_final_readiness_packet_contract.json
    - tools/bazel/phase28_final_readiness_packet_test.py
    - tools/bazel/manifests/phase31_final_evidence_intake_contract.json
    - tools/bazel/manifests/phase32_blocker_register_triage_contract.json
  </read_first>
  <action>For D-01 through D-05, create `tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json` with schema version `1`, id `phase34_final_readiness_demotion_dry_run_contract`, phase `34-final-readiness-and-demotion-dry-run`, lifecycle `34-2026-07-25T18-18-48`, output root `build/ci-evidence/phase34`, requirements `READY-01`, `READY-02`, `READY-03`, source contracts for Phase 31/32/33 and semantic-precedent Phase 28, default Phase 31 output root `build/ci-evidence/phase31`, default Phase 31 manifest `build/ci-evidence/phase31/final-intake-manifest.json`, default Phase 33 handoff `build/ci-evidence/phase33/downstream-handoff-manifest.json`, the exact ledger and dry-run fields from `<interfaces>`, the exact generated artifact list from `<interfaces>`, and stable blocked reason codes for missing, duplicate, dangling, failed, stale, malformed, redaction-failed, source-ref-failed, secret-tainted, lifecycle-mismatched, unsafe, non-final, unknown, underclassified, exception-uncovered, readiness-input-invalid, approval-missing, approval-invalid, and approval-rejected states. Define Phase 32 as a sparse blocker overlay: a Phase 31 row with `accepted-final`, passed evidence, and proof eligibility may legitimately have no Phase 32 blocker row; a row with a failure/non-final/problem signal must have an exact Phase 32 classification or become `underclassified`. For D-06 through D-10, encode `gate_state: open` as requiring `readiness_state == unblocked`, `approval_validation_state == valid`, and `approval_decision_state == approve`; encode quick/default as blocked and prohibit inferred readiness, inferred demotion, production demotion, cutover verdict, and raw evidence consumption. For D-11 through D-15, require a new Phase 34 bundle, preserve Phase 28 as precedent-only, declare pure evaluator outputs separately from I/O validation, and require an isolated open fixture without changing defaults.

Create `tools/bazel/phase34_final_readiness_demotion_dry_run_test.py` with standard-library `unittest`, `tempfile`, `json`, and `pathlib`. Add focused tests named `test_contract_declares_complete_ledger_gate_and_artifacts`, `test_quick_default_writes_blocked_packet_and_dry_run`, `test_expected_rows_come_from_phase31_accepted_final_receipts`, `test_clean_final_passed_row_needs_no_phase32_blocker`, `test_problem_row_without_phase32_classification_is_underclassified`, `test_packet_links_rows_classifications_decisions_blockers_and_artifact_refs`, `test_missing_failed_stale_malformed_redaction_and_underclassified_rows_block`, `test_exception_requires_exact_row_and_gate_coverage`, `test_green_rows_without_explicit_demotion_approval_stay_blocked`, `test_demotion_truth_table_opens_only_for_unblocked_valid_approve`, `test_missing_invalid_stale_and_rejected_approval_write_durable_blocked_result`, `test_absolute_path_is_rejected`, `test_parent_traversal_is_rejected`, `test_wrong_input_root_is_rejected`, `test_input_output_overlap_is_rejected`, `test_symlink_escape_is_rejected`, `test_security_rejects_secret_fields_unsafe_refs_and_overclaim_markers`, `test_lifecycle_and_source_contract_mismatch_fail_closed`, `test_generated_report_derives_from_packet_and_ledger`, and `test_wiring_requires_bazel_root_workflow_and_just_entries`. Use explicit Arrange/Act/Assert comments. Build sanitized fixtures from Phase 31 accepted receipts, Phase 32 blocker rows, and Phase 33 handoffs; no raw evidence or real secrets. Preserve RED state until Task 2.</action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q</automated>
    <expected>RED: exits nonzero before Task 2 because the Phase 34 verifier is absent.</expected>
  </verify>
  <acceptance_criteria>
    - `grep -q '"id": "phase34_final_readiness_demotion_dry_run_contract"' tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`
    - `grep -q '"READY-01"' tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`
    - `grep -q '"readiness-coverage-ledger.json"' tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`
    - `grep -q '"demotion-dry-run.json"' tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json`
    - `grep -q 'test_demotion_truth_table_opens_only_for_unblocked_valid_approve' tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
    - `grep -q 'test_missing_invalid_stale_and_rejected_approval_write_durable_blocked_result' tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
    - `grep -q 'test_clean_final_passed_row_needs_no_phase32_blocker' tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
    - `grep -q 'test_symlink_escape_is_rejected' tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
    - `grep -q '# Arrange' tools/bazel/phase34_final_readiness_demotion_dry_run_test.py`
  </acceptance_criteria>
  <done>The Phase 34 contract captures D-01 through D-15, and RED tests cover every READY requirement, blocking class, threat, artifact, and demotion predicate.</done>
  <commit>test(34): define readiness and demotion dry-run contract</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the coverage-ledger evaluator and generated bundle</name>
  <files>tools/bazel/phase34_final_readiness_demotion_dry_run.py, tools/bazel/phase34_final_readiness_demotion_dry_run_test.py, tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json</files>
  <read_first>
    - tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
    - tools/bazel/manifests/phase34_final_readiness_demotion_dry_run_contract.json
    - tools/bazel/phase33_maintainer_decision_inputs.py
    - tools/bazel/phase32_blocker_register_triage.py
    - tools/bazel/phase31_final_evidence_intake.py
    - tools/bazel/phase28_final_readiness_packet.py
    - .planning/phases/34-final-readiness-and-demotion-dry-run/34-CONTEXT.md
    - .planning/phases/34-final-readiness-and-demotion-dry-run/34-RESEARCH.md
  </read_first>
  <action>For D-11 through D-15, implement `tools/bazel/phase34_final_readiness_demotion_dry_run.py` as a new Python standard-library CLI with `--contract-only`, `--quick`, `--security-only`, and `--wiring-only`, plus `--phase31-output-dir`, `--phase33-handoff`, and `--output-dir`; leave Phase 28 unchanged. Set constants for Phase 34 lifecycle `34-2026-07-25T18-18-48`, Phase 31 lifecycle from its authoritative contract, Phase 33 lifecycle `33-2026-07-04T01-36-41`, default Phase 31 root `build/ci-evidence/phase31`, default handoff `build/ci-evidence/phase33/downstream-handoff-manifest.json`, and output `build/ci-evidence/phase34`.

For D-01 through D-05, validate repository-relative paths under exact allowed roots; reject absolute paths, `..`, symlink escapes, inputs inside the output root, wrong contract ids, stale lifecycles, unresolved refs, malformed JSON, duplicate ids, forbidden field names, secret text, and overclaim markers before trusting output. Load `final-intake-manifest.json` explicitly from `--phase31-output-dir`, validate its contract/lifecycle identity, and resolve every `accepted-final` receipt ref under that exact Phase 31 root. Snapshot the manifest and write one deterministic `contract-snapshots/phase31-accepted-receipts.json` collection containing every validated receipt object plus its original receipt ref, sorted by receipt ref; declare that collection in the exact generated artifact contract and run manifest rather than creating undeclared dynamic files. Separately load only sanitized Phase 33 handoff artifacts and its Phase 32 blocker-register snapshot; do not read raw evidence.

Implement pure functions for expected-row derivation, sparse blocker overlay, exact decision joins, exception coverage, readiness effects, demotion predicate evaluation, and packet/report projection. Derive expected rows from Phase 31 `accepted-final` receipts and `consumed_upstream_row_refs`. Allow an accepted-final, passed, proof-eligible row to have no Phase 32 blocker row and give it a clean coverage state. Require an exact Phase 32 classification only when the Phase 31 row carries a failed, blocked, exception-requested, non-final, redaction, source-ref, secret, lifecycle, unsafe-ref, or other triage signal; otherwise emit a blocked `underclassified` ledger entry. Join Phase 33 retained-code, residual-risk, exception, and readiness decisions by exact `build/ci-evidence/phase32/blocker-register.json#<row_id>` refs and affected gate. Hard blockers (`redaction_failed`, `source_ref_failed`, `secret_tainted`, `lifecycle_mismatch`, `unsafe_ref`) cannot be covered by a normal exception.

For D-06 through D-10, write `demotion-dry-run.json` for every input state; its only `open` state is `unblocked + valid + approve`, and every other combination is `blocked` with all applicable stable reason codes. For malformed/stale approval, write the blocked artifact before returning nonzero. For D-04, D-05, and D-13, make `readiness-coverage-ledger.json` canonical, derive `final-readiness-packet.json`, `readiness-blocker-summary.json`, and `redacted-readiness-report.md` from it, and retain exact evidence/artifact/decision refs. For D-15, quick/default must be blocked and must not synthesize approval; only isolated fixtures may prove open. Make generated-output validation cross-check ledger, packet, report, manifest, and dry-run consistency. Make security-only scan input and generated non-snapshot outputs while allowing prohibition vocabulary inside checked contracts. Keep functions under the repository refactor threshold and use `maybe_` for optional internal values.</action>
  <verify>
    <automated>python3 -m py_compile tools/bazel/phase34_final_readiness_demotion_dry_run.py tools/bazel/phase34_final_readiness_demotion_dry_run_test.py</automated>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q</automated>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --contract-only</automated>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --security-only</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q` exits 0.
    - `python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --contract-only` exits 0.
    - `grep -q 'DEFAULT_PHASE33_HANDOFF' tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `grep -q 'DEFAULT_PHASE31_OUTPUT_DIR' tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `grep -q 'readiness-coverage-ledger.json' tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `grep -q 'approval_validation_state' tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `grep -q 'gate_state' tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `grep -q 'phase31-accepted-receipts.json' tools/bazel/phase34_final_readiness_demotion_dry_run.py`
    - `test -f build/ci-evidence/phase34/demotion-dry-run.json` after `--quick`.
    - A JSON assertion confirms quick output has `gate_state == "blocked"`.
  </acceptance_criteria>
  <done>The GREEN verifier generates a row-complete secret-safe bundle and proves READY-01 through READY-03 without changing Phase 28 or inferring authorization.</done>
  <commit>feat(34): generate final readiness and demotion dry run</commit>
</task>

<task type="auto">
  <name>Task 3: Wire Bazel and just verification, then sign off validation</name>
  <files>tools/bazel/BUILD.bazel, BUILD.bazel, tools/bazel/rust_workflow.sh, justfile, .planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md, tools/bazel/phase34_final_readiness_demotion_dry_run.py</files>
  <read_first>
    - tools/bazel/BUILD.bazel
    - BUILD.bazel
    - tools/bazel/rust_workflow.sh
    - justfile
    - tools/bazel/phase33_maintainer_decision_inputs.py
    - .planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md
    - .planning/phases/33-maintainer-decision-inputs/33-01-SUMMARY.md
  </read_first>
  <action>For D-11 through D-13, add `phase34_source_ref_manifests`, `phase34_verify`, and `phase34_verify_tests` targets to `tools/bazel/BUILD.bazel`, with data for the Phase 31-34 and Phase 28 contracts, Phase 31-34 scripts, Phase 34 tests, and root Phase 34 docs. Add root `phase34_final_readiness_demotion_dry_run_docs` filegroup containing context, research, validation, and `34-01-PLAN.md`, plus root aliases `phase34_verify` and `phase34_verify_tests`.

Add `phase34_verify)` to `tools/bazel/rust_workflow.sh`. It must regenerate the existing Phase 31, Phase 26, Phase 27, Phase 28, Phase 32, and Phase 33 quick outputs in their established order, run Phase 34 `--wiring-only`, then run Phase 34 `--quick --phase31-output-dir build/ci-evidence/phase31 --phase33-handoff build/ci-evidence/phase33/downstream-handoff-manifest.json --output-dir build/ci-evidence/phase34`. Add `phase34_verify_tests)` running the Phase 34 unittest file. Add `phase34-verify:` to `justfile` with `bazel run //tools/bazel:phase34_verify_tests` followed by `bazel run //tools/bazel:phase34_verify`. Extend `--wiring-only` to verify exact target, alias, case-arm, arguments, and recipe ordering.

Run all plan verification commands. Only after all pass, target-edit `34-VALIDATION.md` frontmatter to `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, mark the three task rows green, check every sign-off item, and set approval to `approved 2026-07-25`.</action>
  <verify>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --wiring-only</automated>
    <automated>bazel run //tools/bazel:phase34_verify_tests</automated>
    <automated>bazel run //tools/bazel:phase34_verify</automated>
    <automated>just phase34-verify</automated>
    <automated>python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --security-only</automated>
    <automated>git diff --check</automated>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "phase34_verify"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase34_verify_tests"' tools/bazel/BUILD.bazel`
    - `grep -q 'name = "phase34_final_readiness_demotion_dry_run_docs"' BUILD.bazel`
    - `grep -q 'actual = "//tools/bazel:phase34_verify"' BUILD.bazel`
    - `grep -q 'phase34_verify)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase34_verify_tests)' tools/bazel/rust_workflow.sh`
    - `grep -q 'phase34-verify:' justfile`
    - `grep -q 'status: verified' .planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md`
    - `grep -q 'nyquist_compliant: true' .planning/phases/34-final-readiness-and-demotion-dry-run/34-VALIDATION.md`
    - `just phase34-verify` exits 0 and regenerates a blocked default `demotion-dry-run.json`.
  </acceptance_criteria>
  <done>Bazel, root aliases, workflow facade, `just phase34-verify`, security scans, Nyquist signoff, and mandatory Cargo checks are green.</done>
  <commit>chore(34): wire readiness and demotion verification</commit>
</task>

</tasks>

<threat-model>
## Blocking Threats

| Threat ID | Severity | Boundary | Mitigation |
| --- | --- | --- | --- |
| T-34-01 | high | Phase 33/31 refs into Phase 34 | Consume only sanitized handoff artifacts; recursively reject secret fields/text before trusted output. |
| T-34-02 | high | CLI paths and output root | Require repo-relative exact-root containment; reject traversal, absolute paths, wrong roots, input/output overlap, and symlink escape. |
| T-34-03 | high | Readiness to demotion authorization | Keep orthogonal predicates and require explicit valid `approve`; green evidence never grants authorization. |
| T-34-04 | high | Phase lifecycle and source contracts | Require exact Phase 31-34 lifecycle ids, contract ids, stable row refs, and resolvable snapshots. |
| T-34-05 | high | Phase 31 expected rows to Phase 32 classifications | Exact anti-join; missing, duplicate, unknown, or underclassified rows block readiness. |
| T-34-06 | high | Phase 33 exception coverage | Exact row refs and affected-gate scope; hard blockers outrank ordinary exceptions. |
| T-34-07 | medium | JSON packet to Markdown report | Derive both from one canonical ledger and cross-check generated output consistency. |
</threat-model>

<verification>
```bash
python3 -m py_compile tools/bazel/phase34_final_readiness_demotion_dry_run.py tools/bazel/phase34_final_readiness_demotion_dry_run_test.py
python3 tools/bazel/phase34_final_readiness_demotion_dry_run_test.py -q
python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --contract-only
python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --security-only
python3 tools/bazel/phase34_final_readiness_demotion_dry_run.py --wiring-only
bazel run //tools/bazel:phase34_verify_tests
bazel run //tools/bazel:phase34_verify
just phase34-verify
git diff --check
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- Every READY-01 required link is present in the canonical ledger and final readiness packet.
- Every READY-02 fail-closed class is explicit, reason-coded, tested, and blocks quick/default output.
- READY-03 is proven by the complete readiness/approval truth table and durable blocked artifacts for missing or invalid approval.
- Phase 28 remains unchanged.
- `just phase34-verify` and all mandatory Cargo checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/34-final-readiness-and-demotion-dry-run/34-01-SUMMARY.md`.
</output>
